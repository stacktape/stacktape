{
  "name": "nuxt-web-test",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "build:stp": "npm install --no-audit --no-fund && nuxi build"
  },
  "dependencies": {
    "nuxt": "^3.16.0",
    "vue": "^3.5.0"
  }
}
