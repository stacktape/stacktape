# Test case: Invalid packaging type (discriminated union error)
# Expected error: Invalid packaging type

resources:
  myService:
    type: web-service
    properties:
      packaging:
        type: dockerfile-custom # should be "custom-dockerfile"
        properties:
          dockerfilePath: ./Dockerfile
      resources:
        cpu: 0.25
        memory: 512
