---
name: stacktape-design
description:
  Redesign Stacktape's marketing website or Console through visual exploration, independent design critique, and browser
  verification. Use for substantial page, flow, or visual-system design work, including agent-led redesigns.
---

Read `${CLAUDE_PROJECT_DIR}/.agents/skills/stacktape-design/SKILL.md` completely and follow it for the current request.
That file is the canonical skill. If the project directory is a nested application, locate the public Stacktape root and
read `.agents/skills/stacktape-design/SKILL.md` there. Resolve its repository paths from that root.

Keep the user's selected model, including Fable 5.1 when selected in the host. This skill does not change model
settings.
