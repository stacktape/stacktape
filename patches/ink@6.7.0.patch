diff --git a/node_modules/ink/.bun-tag-d0b9aedd42c16431 b/.bun-tag-d0b9aedd42c16431
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/ink/.bun-tag-db2f913c54a3ac5a b/.bun-tag-db2f913c54a3ac5a
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/build/ink.js b/build/ink.js
index 82f31cccc22fdd81aa656b53d9d20a75095c19be..cd8b134e93f9ada3fe14619b9679ccf619759880 100644
--- a/build/ink.js
+++ b/build/ink.js
@@ -6,7 +6,8 @@ import isInCi from 'is-in-ci';
 import autoBind from 'auto-bind';
 import signalExit from 'signal-exit';
 import patchConsole from 'patch-console';
-import { LegacyRoot, ConcurrentRoot } from 'react-reconciler/constants.js';
+import reconcilerConstants from 'react-reconciler/constants.js';
+const { LegacyRoot, ConcurrentRoot } = reconcilerConstants;
 import Yoga from 'yoga-layout';
 import wrapAnsi from 'wrap-ansi';
 import terminalSize from 'terminal-size';
@@ -245,21 +246,10 @@ export default class Ink {
         // Only apply when writing to a real TTY — piped output always gets trailing newlines.
         const isFullscreen = this.options.stdout.isTTY && outputHeight >= this.options.stdout.rows;
         const outputToRender = isFullscreen ? output : output + '\n';
-        if (this.lastOutputHeight >= this.options.stdout.rows) {
-            const sync = shouldSynchronize(this.options.stdout);
-            if (sync) {
-                this.options.stdout.write(bsu);
-            }
-            this.options.stdout.write(ansiEscapes.clearTerminal + this.fullStaticOutput + output);
-            this.lastOutput = output;
-            this.lastOutputToRender = outputToRender;
-            this.lastOutputHeight = outputHeight;
-            this.log.sync(outputToRender);
-            if (sync) {
-                this.options.stdout.write(esu);
-            }
-            return;
-        }
+        // Note: the original fullscreen path (clearTerminal + rewrite everything) was removed.
+        // It destroyed scrollback/terminal history on every frame when live output >= terminal rows.
+        // With incrementalRendering, the normal path handles fullscreen output correctly by only
+        // rewriting changed lines. Stale content may appear in scrollback but the visible area is correct.
         // To ensure static output is cleanly rendered before main output, clear main output first
         if (hasStaticOutput) {
             const sync = shouldSynchronize(this.options.stdout);
@@ -306,6 +296,16 @@ export default class Ink {
             this.options.stdout.write(data);
             return;
         }
+        // Re-render the tree to compute fresh output so that restoreLastOutput
+        // writes back the current content (not stale content from a dropped
+        // throttle frame). log.clear() still uses the log renderer's existing
+        // previousLines which correctly matches what is on the terminal.
+        this.calculateLayout();
+        const { output, outputHeight } = render(this.rootNode, this.isScreenReaderEnabled);
+        const isFullscreen = this.options.stdout.isTTY && outputHeight >= this.options.stdout.rows;
+        this.lastOutput = output;
+        this.lastOutputToRender = isFullscreen ? output : output + '\n';
+        this.lastOutputHeight = outputHeight;
         const sync = shouldSynchronize(this.options.stdout);
         if (sync) {
             this.options.stdout.write(bsu);
@@ -330,6 +330,12 @@ export default class Ink {
             this.options.stderr.write(data);
             return;
         }
+        this.calculateLayout();
+        const { output, outputHeight } = render(this.rootNode, this.isScreenReaderEnabled);
+        const isFullscreen = this.options.stdout.isTTY && outputHeight >= this.options.stdout.rows;
+        this.lastOutput = output;
+        this.lastOutputToRender = isFullscreen ? output : output + '\n';
+        this.lastOutputHeight = outputHeight;
         const sync = shouldSynchronize(this.options.stdout);
         if (sync) {
             this.options.stdout.write(bsu);
