# React UI

This package intentionally starts empty. A component enters only after at least two applications need the same
presentational, router-neutral primitive. It must not introduce styled-components, React Router, Astro, tRPC, or
Console state.
