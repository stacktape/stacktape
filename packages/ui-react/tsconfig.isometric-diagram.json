{
  "extends": "./tsconfig.json",
  "include": ["src/isometric-diagram"],
  "exclude": [],
  "compilerOptions": {
    // The imported layout/renderer uses guarded indexed geometry extensively. Keep this exception
    // local so new primitives and the future ConfigEditor remain under the workspace default.
    "noUncheckedIndexedAccess": false
  }
}
