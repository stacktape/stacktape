/**
 * Marker replaced by the CLI's asset-replacement custom resource when it injects environment
 * variables into a generated Lambda@Edge wrapper.
 */
export const EDGE_LAMBDA_ENV_ASSET_REPLACER_PLACEHOLDER = '"{{_STP_INJ_ENV_}}"';
