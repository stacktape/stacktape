---
title: 'Self-Hosted GitHub Actions Runners'
order: 108
seoTitle: 'Self-Hosted GitHub Actions Runners | Stacktape Docs'
seoDescription: 'Run GitHub Actions workflows on Stacktape-managed EC2 instances in your AWS account. Auto-hibernation, pre-installed tools, VPC access, and up to 32 vCPU — no runner maintenance required.'
---

# Self-Hosted GitHub Actions Runners

Stacktape can manage self-hosted GitHub Actions runners on EC2 instances in your AWS account. You keep the full flexibility of GitHub Actions — custom steps, matrix builds, reusable workflows — while running jobs on persistent infrastructure with more compute, pre-installed tools, and automatic hibernation to cut idle costs.

## When to use

Self-hosted runners make sense in three scenarios:

- **Slow builds on hosted runners.** Large `node_modules`, Docker image builds, or Java/Go compilations are bottlenecked by GitHub-hosted runner CPU and network. An EC2 `c7a.2xlarge` (8 vCPU, 16 GB RAM) or larger instance finishes these builds significantly faster.
- **VPC access during CI.** Your workflow needs to reach private databases, caches, or internal services during tests or migrations. The runner lives inside your AWS account's default VPC and can reach private resources without exposing them publicly.
- **Cost optimization for high-volume workflows.** GitHub-hosted runner minutes can get expensive at scale. With Stacktape-managed runners, you pay EC2 per-second pricing and the instance hibernates after 15 minutes of idle — so you're not paying for a running instance between pushes.

## When NOT to use

For most teams, GitHub-hosted runners or [GitOps with Console](/ci-cd-and-gitops/gitops-with-console) are simpler starting points. Skip self-hosted runners if:

- Your workflows complete in under 5 minutes on hosted runners — the overhead of managing an EC2 instance isn't worth the marginal speedup.
- You don't need VPC access or large compute — GitHub-hosted `ubuntu-latest` handles standard test-and-deploy workflows well.
- You want zero infrastructure — [GitOps with Console](/ci-cd-and-gitops/gitops-with-console) triggers deployments directly from git events without any pipeline file or runner management.

## How it works

Stacktape's GitHub App listens for `workflow_job` webhook events from your repository. When a job is queued with the `stacktape` label, the handler:

1. **Identifies the project** — matches the repository URL to a Stacktape project in your organization.
2. **Ensures the EC2 runner is available** — starts a hibernated instance (~15 seconds), resumes a stopped instance, or provisions a fresh one (~30 seconds cold start).
3. **Registers a JIT (Just-In-Time) ephemeral runner** — requests a single-use runner configuration from the GitHub API, scoped to just this job.
4. **Dispatches the job via SSM** — sends the runner agent startup command to the EC2 instance. The GitHub Actions runner agent picks up the job, executes your workflow steps, and exits when done.

After 15 minutes of idle, a scheduled Lambda automatically hibernates the instance. Hibernation preserves the instance's memory state, so the next resume is fast. When a newer runner AMI becomes available, the instance is reprovisioned automatically on the next job — no manual maintenance required.

<FlowDiagram steps={[
  { title: "Workflow queued", description: "GitHub sends a workflow_job webhook with the 'stacktape' label to the Stacktape GitHub App." },
  { title: "Runner ensured", description: "The handler resumes a hibernated EC2 instance (~15s) or provisions a new one (~30s cold start)." },
  { title: "JIT runner registered", description: "A single-use runner is registered with GitHub via the JIT config API. Labels: self-hosted, linux, x64, stacktape." },
  { title: "Job dispatched", description: "SSM sends the runner agent script to the EC2 instance. Your workflow steps execute." },
  { title: "Auto-hibernate", description: "After 15 minutes with no new jobs, the instance hibernates automatically to stop billing." }
]} />

## Setting up

### Prerequisites

Before enabling self-hosted runners, you need:

1. **The Stacktape GitHub App installed** on your repository. If you already use [GitOps with Console](/ci-cd-and-gitops/gitops-with-console), the app is already connected.
2. **A connected AWS account** in the Stacktape Console. The runner EC2 instance launches in this account. See [Connecting your AWS account](/stacktape-console/connecting-your-aws-account).
3. **A Stacktape project** linked to the GitHub repository.

### Configuring the runner

Open your project in the Stacktape Console and navigate to the GitHub Actions runner configuration. The configuration includes:

| Setting | Description | Default |
|---------|-------------|---------|
| **Status** | Enable or disable the runner for this project | Enabled |
| **AWS account** | Which connected AWS account to launch the EC2 instance in | Auto-detected from connected accounts |
| **AWS region** | The region for the runner instance | Auto-detected from project deployments |
| **Instance type** | EC2 instance size for the runner | `c7a.2xlarge` (8 vCPU, 16 GB RAM) |

If you don't explicitly set a region, Stacktape auto-detects it from your project's existing deployments or the connected account's primary region. Similarly, if you don't select an AWS account, the first active connected account is used.

### Using the runner in a workflow

Add the `self-hosted` and `stacktape` labels to your workflow's `runs-on` key:

```yaml
name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: [self-hosted, stacktape]
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to production
        run: npx stacktape deploy --stage production --region us-east-1 --autoConfirmOperation
```

The `stacktape` label is what routes the job to your Stacktape-managed runner. Without it, the job runs on GitHub-hosted infrastructure as usual.

<Info>
`STACKTAPE_API_KEY` is automatically injected into the runner environment — you do not need to configure it as a GitHub Actions secret. The handler stores the API key in AWS Systems Manager Parameter Store (encrypted) and makes it available to the runner agent.
</Info>

## Instance types

Choose an instance type based on your workflow's compute needs. All instances use AMD processors and encrypted 50 GB EBS volumes with hibernation support.

| Instance | vCPU | RAM | Best for |
|----------|------|-----|----------|
| `m6a.large` | 2 | 8 GB | Light workflows, simple deploys |
| `m6a.xlarge` | 4 | 16 GB | Medium builds with moderate dependencies |
| `c7a.xlarge` | 4 | 8 GB | CPU-bound builds (Go, Rust compilation) |
| `c7a.2xlarge` | 8 | 16 GB | **Default.** Good balance for most teams — fast Docker builds, large Node.js projects |
| `c7a.4xlarge` | 16 | 32 GB | Large monorepos, parallel test suites, heavy Docker multi-stage builds |
| `c7a.8xlarge` | 32 | 64 GB | Extreme workloads — ML model packaging, very large compilations |

The `c7a` instances are compute-optimized (higher CPU-to-memory ratio), while `m6a` instances are general-purpose (balanced). For most CI workloads — building containers, running tests, packaging Lambda functions — `c7a.2xlarge` is the right starting point.

<Tip>
Start with the default `c7a.2xlarge`. If your workflows consistently finish quickly and don't use much memory, downsize to `c7a.xlarge` or `m6a.large`. If Docker builds or test suites are the bottleneck, try `c7a.4xlarge`.
</Tip>

## Pre-installed tools

The runner AMI comes with a development environment ready to use. Your workflow steps can use these tools without additional setup:

| Tool | Details |
|------|---------|
| **Node.js** | Managed via fnm (Fast Node Manager) |
| **Bun** | Available at `~/.bun/bin` |
| **npm** | Bundled with Node.js |
| **pnpm** | Available at `~/.local/share/pnpm` |
| **Go** | Available at `/usr/local/go/bin` |
| **Java** | Amazon Corretto 21 (`/usr/lib/jvm/java-21-amazon-corretto.x86_64`) |
| **Maven** | Available at `/opt/maven/bin` |
| **Gradle** | Available at `/opt/gradle/bin` |
| **Python 3** | System Python |
| **Docker** | Available for container builds |
| **AWS CLI** | Pre-configured with instance profile credentials |
| **Git** | For repository operations |

If your workflow needs a tool not on this list, install it as a workflow step — the instance persists between jobs (until hibernation), so caches and installed packages survive across runs within the same active session.

## Hibernation and lifecycle

Stacktape manages the runner lifecycle automatically to balance cost and responsiveness:

- **Active** — the instance runs your workflow steps.
- **Idle** — after the last job completes, a 15-minute countdown starts.
- **Hibernated** — after 15 minutes with no new jobs, the instance hibernates. Hibernation preserves the instance's memory to disk, so resume is fast (~15 seconds). You pay only for the EBS storage while hibernated, not for compute.
- **Resumed** — when a new `workflow_job` event arrives, the handler resumes the hibernated instance. The runner agent is ready in ~15 seconds.
- **Reprovisioned** — if the runner AMI has been updated (Stacktape periodically publishes new AMIs with updated tools), the next job terminates the old instance and launches a fresh one with the latest AMI. Cold provisioning takes ~30 seconds.

A scheduled Lambda runs every 5 minutes to enforce the idle timeout. It checks for active deployments and GitHub Actions jobs before hibernating — an instance with in-progress work is never stopped.

<Warning>
If an EC2 runner is stuck (e.g., a zombie operation that never completed), the auto-stop Lambda marks stale operations as failed after 120 minutes and resets the runner state. You do not need to manually clean up stuck runners.
</Warning>

## Cost

You pay standard AWS EC2 pricing for the time the instance is running, plus EBS storage costs while hibernated. There is no Stacktape surcharge for self-hosted runners.

| State | What you pay |
|-------|-------------|
| Running | EC2 per-second pricing for the instance type + EBS storage |
| Hibernated | EBS storage only (50 GB encrypted gp3) |
| Terminated | Nothing |

For a `c7a.2xlarge` instance, expect roughly ~$0.30-0.40/hour while running (varies by region). If your team pushes 10 times per day with 5-minute builds, that's ~50 minutes of compute plus the 15-minute idle tail per session — significantly cheaper than equivalent GitHub-hosted runner minutes for large builds.

## Self-hosted runners vs other CI/CD options

<FeatureComparisonTable
  columns={["Self-hosted runners", "GitOps with Console", "Custom CI/CD (hosted runners)"]}
  features={[
    { name: "Pipeline customization", values: { "Self-hosted runners": "Full GitHub Actions", "GitOps with Console": "None — git event → deploy", "Custom CI/CD (hosted runners)": "Full — any CI system" } },
    { name: "Pre-deploy testing", values: { "Self-hosted runners": true, "GitOps with Console": false, "Custom CI/CD (hosted runners)": true } },
    { name: "VPC access during CI", values: { "Self-hosted runners": true, "GitOps with Console": false, "Custom CI/CD (hosted runners)": false } },
    { name: "Setup effort", values: { "Self-hosted runners": "Enable in Console + modify workflow YAML", "GitOps with Console": "Configure in Console only", "Custom CI/CD (hosted runners)": "Write workflow file" } },
    { name: "Runner maintenance", values: { "Self-hosted runners": "Managed by Stacktape (AMI updates, hibernation)", "GitOps with Console": "Managed by Stacktape", "Custom CI/CD (hosted runners)": "Managed by CI provider" } },
    { name: "Build speed", values: { "Self-hosted runners": "Up to 32 vCPU, warm cache", "GitOps with Console": "Depends on build runner type", "Custom CI/CD (hosted runners)": "2 vCPU on GitHub free tier" } },
    { name: "Cost model", values: { "Self-hosted runners": "EC2 per-second + EBS", "GitOps with Console": "CodeBuild or EC2 runner", "Custom CI/CD (hosted runners)": "CI provider minutes" } }
  ]}
/>

## Example workflows

### Deploy on push to main

```yaml
name: Deploy production
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: [self-hosted, stacktape]
    steps:
      - uses: actions/checkout@v4
      - run: bun install
      - run: bun run test
      - run: npx stacktape deploy --stage prod --region us-east-1 --autoConfirmOperation
```

### PR preview environments

```yaml
name: PR Preview
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  preview:
    runs-on: [self-hosted, stacktape]
    steps:
      - uses: actions/checkout@v4
      - run: bun install
      - run: bun run test
      - run: npx stacktape deploy --stage "pr-${{ github.event.pull_request.number }}" --region us-east-1 --autoConfirmOperation
```

Pair this with a cleanup workflow that runs on PR close:

```yaml
name: Cleanup PR Preview
on:
  pull_request:
    types: [closed]
jobs:
  cleanup:
    runs-on: [self-hosted, stacktape]
    steps:
      - uses: actions/checkout@v4
      - run: npx stacktape delete --stage "pr-${{ github.event.pull_request.number }}" --region us-east-1 --autoConfirmOperation
```

See [Stacks per Git branch pattern](/ci-cd-and-gitops/stacks-per-git-branch-pattern) for naming conventions and cleanup strategies.

### Running tests before deploy

A key advantage over [GitOps with Console](/ci-cd-and-gitops/gitops-with-console) is the ability to run tests, lint, and other checks before deploying:

```yaml
name: Test and Deploy
on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: [self-hosted, stacktape]
    steps:
      - uses: actions/checkout@v4
      - run: bun install
      - run: bun run typecheck
      - run: bun run lint
      - run: bun run test
  deploy:
    needs: test
    runs-on: [self-hosted, stacktape]
    steps:
      - uses: actions/checkout@v4
      - run: npx stacktape deploy --stage prod --region us-east-1 --autoConfirmOperation
```

## Mixing with hosted runners

You don't have to run every job on the self-hosted runner. Within a single workflow, some jobs can use hosted runners and others can target your Stacktape-managed runner:

```yaml
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm run lint

  deploy:
    needs: lint
    runs-on: [self-hosted, stacktape]
    steps:
      - uses: actions/checkout@v4
      - run: npx stacktape deploy --stage prod --region us-east-1 --autoConfirmOperation
```

Run lightweight checks (lint, type-check) on free hosted runners and reserve the self-hosted runner for the resource-intensive deploy step.

## Disabling the runner

To stop using the self-hosted runner for a project, either set the status to **Disabled** in the Console configuration or remove the configuration entirely. When disabled, `workflow_job` events with the `stacktape` label are ignored — your workflows will fail with "no matching runner" unless you update `runs-on` to use a hosted runner instead.

<Warning>
Disabling the runner configuration does not terminate the EC2 instance immediately. The auto-stop Lambda hibernates idle instances after 15 minutes as usual. If you want to stop the instance immediately, terminate it from the AWS EC2 console or wait for the idle timeout.
</Warning>

## FAQ

### How do I set up a self-hosted GitHub Actions runner with Stacktape?

Install the Stacktape GitHub App on your repository, connect an AWS account in the Stacktape Console, then enable the GitHub Actions runner configuration for your project. In your GitHub Actions workflow, set `runs-on: [self-hosted, stacktape]`. The runner infrastructure — EC2 instance, IAM role, security group — is provisioned automatically on the first job.

### Do I need to store AWS credentials or API keys in GitHub Secrets?

No. `STACKTAPE_API_KEY` is automatically injected into the runner environment by the Stacktape handler. It's stored encrypted in AWS Systems Manager Parameter Store and cleaned up after each job. The EC2 instance uses an IAM instance profile for AWS access, so you don't need IAM access keys either.

### How fast does the runner start?

If the instance is hibernated (the common case after 15+ minutes of idle), it resumes in approximately 15 seconds. A cold start — provisioning a brand-new instance — takes approximately 30 seconds. Once the instance is running, it picks up new jobs instantly until the idle timeout triggers hibernation.

### How much does a self-hosted runner cost compared to GitHub-hosted runners?

You pay EC2 per-second pricing while the instance is running and EBS storage while hibernated. A `c7a.2xlarge` costs roughly $0.30-0.40/hour (region-dependent). GitHub charges $0.008/minute for Linux hosted runners (~$0.48/hour). Self-hosted runners save money when your builds are long (leveraging faster hardware to finish sooner) or when you need larger instances that GitHub charges premium rates for. For short, infrequent builds, hosted runners may be simpler and cheaper.

### Can I use self-hosted runners with GitOps?

These are separate features. [GitOps with Console](/ci-cd-and-gitops/gitops-with-console) triggers deployments directly from git events without a pipeline file. Self-hosted runners execute GitHub Actions workflows that you write. If you want both automatic deployment and custom pipeline logic, use self-hosted runners with a GitHub Actions workflow — you get full control over pre-deploy steps while running on managed infrastructure.

### What happens if two jobs are queued at the same time?

The runner handles one job at a time per project per region. If multiple jobs queue simultaneously, the GitHub Actions runner agent processes them sequentially on the same EC2 instance. For parallel jobs across different projects, each project has its own independent runner instance.

### Does the runner get security updates automatically?

Yes. Stacktape periodically publishes updated runner AMIs with the latest security patches and tool versions. The reprovision Lambda detects when a newer AMI is available and replaces the instance on the next job — no manual action required.

### Can I use self-hosted runners for non-Stacktape workflows?

Yes. The runner is a standard GitHub Actions self-hosted runner with pre-installed development tools. Any workflow step that runs on a Linux x64 runner works — you can use it for tests, builds, deployments to other platforms, or any other automation. The `STACKTAPE_API_KEY` is injected automatically, but you're not required to use it.

### What operating system and architecture does the runner use?

The runner uses Amazon Linux on x86_64 (amd64) architecture. It registers with GitHub using the labels `self-hosted`, `linux`, `x64`, and `stacktape`. ARM/Graviton-based runners are not currently available.

### How do I troubleshoot a failed runner job?

Check the GitHub Actions job log first — it shows the workflow step output. If the runner itself failed to start, check the Stacktape Console for the project's runner status. Common issues: the connected AWS account was disconnected, the region has no available subnets in the default VPC, or the runner was disabled in the configuration. The handler guards against duplicate webhook deliveries, so retried webhooks don't cause double runs.