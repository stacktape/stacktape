---
title: 'Deployment Scripts'
order: 67
seoTitle: 'Deployment Scripts — Run Migrations on Deploy | Stacktape Docs'
seoDescription: 'Run database migrations, seed data, and cleanup tasks during deploy or delete with Stacktape deployment scripts. Lambda-backed, VPC-aware, with automatic rollback on failure.'
---

# Deployment Scripts

A Stacktape deployment script runs a [Lambda function](/resources/compute/lambda-function) automatically during stack deployment or deletion. Use it for database migrations, seed data, cache warming, or cleanup tasks that must execute in AWS with direct network access to your stack's resources.

Deployment scripts run as Lambda functions, so you pay only for the execution time — typically fractions of a cent per run.

## When to use

Deployment scripts solve a specific problem: running code *inside AWS* at deploy or delete time, with access to your stack's VPC-protected resources. Common scenarios:

- **Database migrations** — run Prisma, Drizzle, or raw SQL migrations immediately after a deploy, with direct network access to your database
- **Seed or reference data** — populate lookup tables, create initial admin users, or warm caches after first deploy
- **Schema changes** — create DynamoDB indexes, configure OpenSearch mappings, or set up Redis data structures
- **Pre-deletion cleanup** — export data, drain queues, or snapshot resources before the stack is torn down

A deployment script is the right choice when your task needs to run in AWS (not on your local machine or CI runner) and must complete before the deployment is considered successful.

## When NOT to use

Not every deploy-time task needs a deployment script. Consider alternatives:

- **Local build steps** (compiling, linting, generating assets) — use [lifecycle hooks](/configuration/hooks-and-scripts) with a `local-script`. These run on your machine or CI runner and don't need AWS access.
- **Tasks longer than 15 minutes** — deployment scripts are Lambda functions with a 900-second maximum timeout. For long ETL jobs, data backfills, or heavy processing, use a [batch job](/resources/compute/batch-job).
- **Recurring tasks** — if the task should run on a schedule (nightly reports, periodic cleanup), use a [Lambda function](/resources/compute/lambda-function) with a [schedule trigger](/configuration/triggers/schedule-triggers) instead.
- **Tasks needing local files or interactive prompts** — deployment scripts run in AWS Lambda, not on your machine. Use a [local script](/configuration/hooks-and-scripts) for anything that reads local files or needs user input.

<FeatureComparisonTable
  columns={["Deployment script", "Lifecycle hook (local script)"]}
  features={[
    { name: "Runs in", values: { "Deployment script": "AWS Lambda", "Lifecycle hook (local script)": "Local machine / CI" } },
    { name: "VPC resource access", values: { "Deployment script": "Direct (joinDefaultVpc)", "Lifecycle hook (local script)": "Via bastion tunneling" } },
    { name: "Max duration", values: { "Deployment script": "15 minutes", "Lifecycle hook (local script)": "No limit" } },
    { name: "Failure on deploy", values: { "Deployment script": "Rolls back entire stack", "Lifecycle hook (local script)": "Stops the command" } },
    { name: "Needs Lambda packaging", values: { "Deployment script": true, "Lifecycle hook (local script)": false } },
    { name: "Runs manually", values: { "Deployment script": true, "Lifecycle hook (local script)": true } }
  ]}
/>

## Basic example

This deployment script runs database migrations after every deploy. It connects to a relational database, joins the VPC for network access, and has a 2-minute timeout to accommodate large migrations.

<CodeBlock
  intellisense
  tabs={[
    {
      label: 'TypeScript',
      lang: 'typescript',
      code: `import {
  defineConfig,
  DeploymentScript,
  StacktapeLambdaBuildpackPackaging,
  RelationalDatabase,
  RdsEnginePostgresql16
} from 'stacktape';

// [!code focus-start]
export default defineConfig(() => {
  const mainDatabase = new RelationalDatabase({
    engine: new RdsEnginePostgresql16({
      instanceSize: 'db.t4g.micro'
    })
  });

  const runMigrations = new DeploymentScript({
    trigger: 'after:deploy',
    packaging: new StacktapeLambdaBuildpackPackaging({
      entryfilePath: './scripts/migrate.ts'
    }),
    connectTo: ['mainDatabase'],
    joinDefaultVpc: true,
    timeout: 120,
    memory: 512
  });

  return {
    resources: { mainDatabase, runMigrations }
  };
});
// [!code focus-end]
`
    }
  ]}
/>

The handler receives any `parameters` you define as the event payload. Here's what the migration script might look like:

```typescript
import { execSync } from 'child_process';

export default async () => {
  // STP_MAIN_DATABASE_CONNECTION_STRING is auto-injected by connectTo
  execSync('npx prisma migrate deploy', {
    stdio: 'inherit',
    env: {
      ...process.env,
      DATABASE_URL: process.env.STP_MAIN_DATABASE_CONNECTION_STRING
    }
  });

  return { statusCode: 200, body: 'Migrations applied successfully' };
};
```

## Trigger behavior

The `trigger` property controls when your script runs and what happens if it fails. This is the most important decision when configuring a deployment script.

### after:deploy

The script runs after all other resources in the stack are created or updated. Stacktape automatically adds CloudFormation dependencies so the script waits for databases, queues, and other resources to be ready before executing.

**If the script fails, the entire deployment rolls back.** This is the safety mechanism that makes deployment scripts reliable for migrations — a failed migration means your stack stays at the previous working state rather than leaving you with updated code pointing at an un-migrated database.

<CodeBlock
  intellisense
  tabs={[
    {
      label: 'TypeScript',
      lang: 'typescript',
      code: `import { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';

// [!code focus-start]
export default defineConfig(() => {
  const seedData = new DeploymentScript({
    trigger: 'after:deploy',
    packaging: new StacktapeLambdaBuildpackPackaging({
      entryfilePath: './scripts/seed.ts'
    }),
    timeout: 300
  });

  return {
    resources: { seedData }
  };
});
// [!code focus-end]`
    }
  ]}
/>

### before:delete

The script runs before resources are torn down during [`stacktape delete`](/cli/delete). This gives you a window to export data, drain queues, or clean up external integrations.

**If the script fails, deletion continues anyway.** This prevents a broken cleanup script from leaving orphaned stacks you cannot delete. Design before:delete scripts defensively — log failures, but don't depend on them succeeding.

<CodeBlock
  intellisense
  tabs={[
    {
      label: 'TypeScript',
      lang: 'typescript',
      code: `import { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';

// [!code focus-start]
export default defineConfig(() => {
  const cleanup = new DeploymentScript({
    trigger: 'before:delete',
    packaging: new StacktapeLambdaBuildpackPackaging({
      entryfilePath: './scripts/cleanup.ts'
    }),
    connectTo: ['myBucket'],
    timeout: 300
  });

  return {
    resources: { cleanup }
  };
});
// [!code focus-end]`
    }
  ]}
/>

<Warning>
A `before:delete` script runs only during `stacktape delete`. If a stack is deleted directly through the AWS Console or CloudFormation API, the script does not run.
</Warning>

## Packaging

Deployment scripts support two Lambda packaging modes. Most teams use the Stacktape buildpack for zero-config bundling. For pre-built artifacts (from a CI pipeline or custom build process), use custom artifact packaging.

| Mode | Class | When to use |
|------|-------|-------------|
| [Stacktape buildpack](/packaging/function/stacktape-buildpack) | `StacktapeLambdaBuildpackPackaging` | Default. Point to a source file — Stacktape bundles and uploads it. |
| [Custom artifact](/packaging/function/custom-artifact) | `CustomArtifactLambdaPackaging` | You have a pre-built zip or directory from a custom build step. |

The Stacktape buildpack supports JavaScript, TypeScript, Python, Java, Go, Ruby, PHP, and .NET. The runtime is auto-detected from the file extension when not specified explicitly.

<CodeBlock
  intellisense
  tabs={[
    {
      label: 'TypeScript',
      lang: 'typescript',
      code: `import { defineConfig, DeploymentScript, CustomArtifactLambdaPackaging } from 'stacktape';

// [!code focus-start]
export default defineConfig(() => {
  const runMigrations = new DeploymentScript({
    trigger: 'after:deploy',
    packaging: new CustomArtifactLambdaPackaging({
      packagePath: './dist/migration-bundle',
      handler: 'index.handler'
    }),
    timeout: 120
  });

  return {
    resources: { runMigrations }
  };
});
// [!code focus-end]`
    }
  ]}
/>

For language-specific configuration (Node.js version, Python version, module format, etc.), see the [language-specific packaging config](/packaging/function/language-specific-config) page.

## Connecting to resources

Deployment scripts frequently need to interact with databases, buckets, or queues in your stack. Use `connectTo` to grant access and auto-inject connection details as environment variables.

When you list a resource in `connectTo`, Stacktape:
- **Grants IAM permissions** for the script's Lambda execution role (e.g., read/write to a bucket, send messages to a queue)
- **Opens network access** via security group rules (for databases and Redis)
- **Injects environment variables** with connection details, named `STP_[RESOURCE_NAME]_[PARAM]`

For example, connecting to a resource named `mainDatabase` injects `STP_MAIN_DATABASE_CONNECTION_STRING`, `STP_MAIN_DATABASE_HOST`, and `STP_MAIN_DATABASE_PORT`. See [connecting resources](/configuration/connecting-resources) for the full list of injected variables per resource type.

<Info>
For VPC-protected resources like [relational databases](/resources/databases/relational-database) and [Redis clusters](/resources/databases/redis), you must also set `joinDefaultVpc: true`. Without it, the Lambda function runs outside the VPC and cannot reach these resources. Note that joining the VPC means the function loses direct internet access — if your script also needs to call external APIs, configure [NAT gateways](/configuration/configuration-files) in your stack's VPC settings.
</Info>

You can also add raw IAM policy statements with `iamRoleStatements` for AWS services not covered by `connectTo` — for example, calling Amazon Bedrock or writing to a cross-account S3 bucket.

## Environment and parameters

Deployment scripts accept two mechanisms for passing data:

**Environment variables** (`environment`) are injected at runtime. Use them for configuration values, API keys, and secrets via [`$Secret()`](/configuration/directives) or [`$ResourceParam()`](/configuration/directives).

<CodeBlock
  intellisense
  tabs={[
    {
      label: 'TypeScript',
      lang: 'typescript',
      code: `import { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';

// [!code focus-start]
export default defineConfig(() => {
  const seedData = new DeploymentScript({
    trigger: 'after:deploy',
    packaging: new StacktapeLambdaBuildpackPackaging({
      entryfilePath: './scripts/seed.ts'
    }),
    environment: [
      { name: 'SEED_COUNT', value: 100 },
      { name: 'ADMIN_EMAIL', value: '$Secret(admin-email)' }
    ],
    timeout: 60
  });

  return {
    resources: { seedData }
  };
});
// [!code focus-end]`
    }
  ]}
/>

**Parameters** (`parameters`) are passed as the event payload to your handler function. Use them for structured data that your script reads at invocation time. Do not put secrets in parameters — they are visible in CloudFormation; use `environment` with `$Secret()` instead.

```typescript
// Handler receives parameters as the event argument
export default async (event: { tableName: string; region: string }) => {
  console.log(`Processing table: ${event.tableName} in ${event.region}`);
};
```

## Manual execution

You can run any deployment script on-demand without a full deploy using the [`deployment-script:run`](/cli/deployment-script-run) CLI command:

```bash
stacktape deployment-script:run --resourceName runMigrations --stage production --region eu-west-1
```

This packages and uploads the latest script code, then invokes the Lambda function directly. It's useful for re-running migrations after a hotfix, re-seeding data, or testing a script before a full deploy.

<Warning>
Manual execution updates only the function code. Changes to environment variables, memory, timeout, or VPC settings require a full `stacktape deploy`.
</Warning>

## FAQ

### What happens if my after:deploy script fails?

The entire CloudFormation deployment rolls back to its previous state. All resources created or updated during the deployment are reverted. This ensures your stack stays consistent — you won't end up with updated application code pointing at a database that failed to migrate. Fix the script, then redeploy.

### What happens if my before:delete script fails?

Stack deletion continues. This is by design — a broken cleanup script should never prevent you from deleting a stack. If your cleanup script fails, check [logs](/cli/debug-logs) for the error, but the stack resources will still be removed.

### Can I run a deployment script manually without deploying?

Yes. Use [`stacktape deployment-script:run`](/cli/deployment-script-run) to invoke any deployment script on demand. This packages and uploads the latest code, then invokes the Lambda. It only updates the function code — environment variables and other configuration require a full deploy.

### Can a deployment script access my database?

Yes. Add the database to `connectTo` and set `joinDefaultVpc: true`. Stacktape injects the connection string as an environment variable (e.g., `STP_MY_DB_CONNECTION_STRING`) and opens the necessary security group rules. This is the standard pattern for running database migrations in a deployment script.

### How do I pass configuration to a deployment script handler?

Use `environment` for key-value configuration (including secrets via `$Secret()`), and `parameters` for structured data passed as the Lambda event payload. Environment variables are the right choice for connection strings and API keys. Parameters work well for flags and configuration objects your handler logic reads.

### How long can a deployment script run?

Deployment scripts are Lambda functions, so the maximum execution time is 900 seconds (15 minutes). The default timeout is 10 seconds. Set `timeout` explicitly for tasks like database migrations that may take longer. If your task needs more than 15 minutes, use a [batch job](/resources/compute/batch-job) instead.

### Can I have multiple deployment scripts in one stack?

Yes. Define multiple `DeploymentScript` resources in your config, each with its own trigger. All `after:deploy` scripts run after all other resources are ready. Stacktape does not guarantee execution order between multiple scripts with the same trigger — if ordering matters, combine the steps into a single script.

### Should I use a deployment script or a lifecycle hook for migrations?

Use a deployment script when the migration needs direct VPC access to the database (the common case). A deployment script runs inside AWS as a Lambda, so it can reach VPC-protected databases directly. Use a lifecycle hook (`afterDeploy` with a local script) when you need to run the migration from your local machine or CI runner — for example, if the migration tool doesn't package well as a Lambda, or if it takes longer than 15 minutes.

### How much does running a deployment script cost?

Deployment scripts use AWS Lambda pricing: you pay for the compute time consumed during execution, measured in GB-seconds. A typical database migration running for 10 seconds with 512 MB memory costs a fraction of a cent. There's no additional Stacktape charge for deployment scripts. See [AWS Lambda pricing](https://aws.amazon.com/lambda/pricing/) for current rates.

### Can I use Python or other languages for deployment scripts?

Yes. Deployment scripts support the same languages as [Lambda functions](/resources/compute/lambda-function): JavaScript, TypeScript, Python, Java, Go, Ruby, PHP, and .NET. When using `StacktapeLambdaBuildpackPackaging`, the runtime is auto-detected from the file extension. You can also set the `runtime` property explicitly if needed.

<ApiReference definitionName="DeploymentScriptProps" />

<ReferenceableParams resource="deployment-script" />