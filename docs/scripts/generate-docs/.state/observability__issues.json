{
  "pageId": "observability__issues",
  "pageRoute": "observability/issues",
  "startedAt": "2026-05-17T16:06:21.929Z",
  "updatedAt": "2026-05-21T17:32:28.254Z",
  "iterations": [
    {
      "iteration": 1,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\observability__issues\\iteration-1.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 9,
            "completeness": 6,
            "practicalUsefulness": 8,
            "audienceFit": 8
          },
          "strengths": [
            "Every CLI bash block contains exactly one command — copy-button behavior is correct throughout, with explanatory text placed in prose before each block.",
            "Error type table matches the source `errorTypeLabels` map exactly (uncaught, caught, unhandled_rejection, exit_error, handler_not_found, panic).",
            "Issue lifecycle statuses (Open/Resolved/Ignored) and per-status available actions are clearly described and grounded in the Console source.",
            "The 'Issues vs Logs vs Alarms' comparison table gives a first-time reader a direct decision anchor without requiring them to read three separate pages first.",
            "FAQ spans all three required categories — Stacktape-specific, AWS-service-level, and comparison/choosing — with substantive 2–4 sentence answers.",
            "CLI vs Console distinction is maintained throughout, satisfying the observability section instruction.",
            "No marketing language, no hand-authored YAML, no forbidden config patterns anywhere on the page.",
            "Pagination default of 20 items per page is correctly sourced from the Console component (`perPageAmount` initial state)."
          ],
          "problems": [
            "Supported language list (TypeScript/JavaScript, Python, Go, Java, .NET, Ruby, PHP) is not present in any provided source file — this is a concrete Stacktape-specific claim with no grounding.",
            "'Up to 50 most recent occurrences per issue' is stated both in the detail-page description and in the Limitations section but has no grounding in the provided source files.",
            "'Stage filter — up to 50 stage names' limit is stated in the configuration table but is not grounded in the provided source.",
            "'±1 minute time window' for log links around occurrences appears in the detail-page description and is not grounded in any provided source file.",
            "`issues:view` as a distinct permission required to view issues is not grounded in the provided source — only `projects:update-settings` (for configuration) is verifiable from the Console source.",
            "`<ConsoleScreenshot>` is not in the documented MDX component palette; its props (`src`, `alt`, `caption`) cannot be verified against the style guide, which risks a broken render.",
            "The 'Limitations' section pads the page by restating earlier content: the 'One subscription filter slot used' bullet repeats the dedicated 'CloudWatch Logs subscription filter slots' subsection almost verbatim, and 'Approximate counts at low sampling rates' restates the sampling-rate subsection. This is a backbone-padding violation."
          ],
          "mandatoryFixes": [
            "Trim the 'Limitations' section to remove the two padded bullets: 'One subscription filter slot used' (already covered in full under 'CloudWatch Logs subscription filter slots') and 'Approximate counts at low sampling rates' (already covered under 'Event sampling rate'). Keep only limitations that are not stated elsewhere: log-pattern-based detection, false positives, and any sourced occurrence-cap caveat. Do not pad a backbone section with restated content."
          ],
          "optionalImprovements": [
            "Remove or narrow the supported-language list until it can be grounded in source — replace with a cautious form such as 'common runtimes including Node.js, Python, and Go' or link to an authoritative languages reference page.",
            "Remove the '±1 minute time window' and 'up to 50 occurrences' specifics from the detail-page description, or source them before publishing. Unverified numeric claims erode reader trust more than vaguer phrasing ('a window of recent log events around each occurrence') would.",
            "Remove the 'up to 50 stage names' limit from the configuration options table or replace it with 'check Console for current limits' until it can be verified.",
            "Verify or remove the `issues:view` row in the permissions table — if this is a real permission, it needs a source; if the Console grants view access to all authenticated members without a named permission, state that instead.",
            "Confirm `<ConsoleScreenshot>` is a registered MDX component before shipping; if uncertain, replace it with a prose description or a standard `<img>` / `<figure>` pattern to avoid silent render failures.",
            "The 'When NOT to use' section overlaps partially with 'Limitations' — consider merging the log-pattern-based caveat into one place only to reduce redundancy without the padding problem."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "Error types table (Uncaught, Caught, Unhandled Rejection, Exit Error, Handler Not Found, Panic) is directly and precisely grounded in the Console source errorTypeLabels object.",
            "Issue statuses OPEN, RESOLVED, IGNORED and the default-Open filter behavior match the source code exactly.",
            "Console table columns (Last seen, Error, Type, Resource, Project / Stage, Count, Status) match the ElevatedTable columnHeaders in the source precisely.",
            "CLI command flags (projectName, stage, issueStatus, limit, default 25) are fully verified from issues-list/index.ts source.",
            "Agent mode JSON output behavior is grounded in the isAgentMode() branch of the source.",
            "The 'Configure Issues' button label, the projects:update-settings permission check, and the empty-state message ('New projects start with Issues turned off') all match source code exactly.",
            "CLI section correctly uses one fenced block per command variant with no explanatory # comments inside blocks — no copy-button trap.",
            "Console and CLI workflows are clearly distinguished throughout, satisfying the observability section instruction to never blur the two surfaces.",
            "Three-category FAQ coverage is solid: Stacktape-specific, underlying AWS mechanics, and comparison/choosing questions are all present."
          ],
          "problems": [
            "Permission identifiers 'issues:view' and 'issues:manage' appear in the Permissions table and the CLI Tip callout, but neither identifier appears in any provided source file. The only permission grounded in source is 'projects:update-settings'. If these names are wrong, readers configuring team access will be misconfigured in production.",
            "Supported languages list (TypeScript/JavaScript, Python, Go, Java, .NET, Ruby, PHP) is not present in any provided source file. This is a specific Stacktape-feature claim about detection coverage — readers will rely on it to decide whether their runtime is monitored.",
            "Stage filter 'up to 50' limit in the Configuration options table is not grounded in the provided source. The source exposes issuesEnabledStages as an array without any numeric cap being visible.",
            "Issue detail page features — stack traces with parsed file/line/column frames, '50 most recent occurrences', '±1 minute' log link time window — are described in detail, but no detail-page source file was provided. These facts are unverifiable and could be inaccurate.",
            "CLI limit range '1–100' stated in the issues:list section is not present in the provided source, which only shows 'limit: limit ?? 25' without explicit bounds."
          ],
          "mandatoryFixes": [
            "Remove or verify the permissions table rows for 'issues:view' and 'issues:manage'. These identifiers are not in any provided source file. Only 'projects:update-settings' is confirmed. If the actual permission names cannot be verified, replace the table with a general statement: 'Access to issues is controlled through your organization's access settings; configuring monitoring requires projects:update-settings.'",
            "Remove the supported languages list (TypeScript/JavaScript, Python, Go, Java, .NET, Ruby, PHP) or mark it explicitly as partial and unverified. No provided source enumerates these runtimes. Replace with a statement grounded in what the source does confirm: pattern-based detection from CloudWatch Logs for Lambda functions and container workloads, without claiming specific language support.",
            "Remove 'up to 50' from the Stage filter row in the Configuration options table — this numeric cap is not present in any provided source. Rewrite as: 'List of stage names to monitor; leave empty to monitor all stages.'",
            "Qualify all issue detail page specifics — the '50 most recent occurrences' retention cap, 'parsed frames (file, line, column, function)', and '±1 minute time window' for log links — or remove them. The detail page source was not provided, making these claims unverifiable."
          ],
          "optionalImprovements": [
            "The 'Event sampling rate' row says '1–100%, default 100%'. The default of 100 is grounded via 'issuesEventSamplingRate || 100', but the '1%' floor is not confirmed in the provided source. Either remove the lower bound or note it as approximate.",
            "The CLI section says 'Set a custom limit (1–100)'. The upper bound of 100 is not confirmed in the source (only the default 25 is). Reword to 'set a custom result limit' without asserting a specific cap.",
            "The comparison table 'Issues vs logs vs alarms' references notifications in the Alarms row but links only to /observability/alarms. Adding a link to /observability/notifications in that row's 'Triggers notifications' cell would complete the cross-reference.",
            "The FAQ answer for 'Does issue monitoring add cost to my AWS bill?' correctly avoids inventing a price, but the mention of Lambda invocations processing error events is an implementation detail not grounded in the provided source files. Keep the answer but soften the mechanism claim to 'additional processing behind the scenes' rather than specifying Lambda invocations."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "scores": {
            "clarity": 6,
            "scannability": 8,
            "completeness": 5,
            "practicalUsefulness": 5,
            "audienceFit": 6
          },
          "strengths": [
            "Opening paragraph is answer-first, self-contained, and close to the 40-75 word target.",
            "Headings are compact and mostly noun-form, with a useful workflow-oriented order.",
            "Console and CLI surfaces are separated into distinct sections, and CLI examples use one command per fenced block.",
            "Tables make the status, error type, and issue/log/alarm comparisons easy to scan."
          ],
          "problems": [
            "Many Stacktape-specific claims are not grounded in the provided source files: supported languages, CloudWatch subscription-filter slot limits, skipped edge functions/user-managed packaging, detail-page stack traces, occurrence cap, log-window links, issue fingerprinting scope, and status actions are not visible in the supplied IssuesPage or issues-list source.",
            "The CLI section documents `issues:resolve`, `issues:ignore`, and `issues:reopen`, API-key behavior for all four commands, permissions, and CI/CD usage, but only `issues:list` source was provided.",
            "The page includes a cost FAQ even though the observability section instruction says cost is a separate section and should not be included here.",
            "Console workflow details go beyond the provided IssuesPage source in several places, especially configuration modal options and issue detail page behavior.",
            "The FAQ is broad and useful, but several answers repeat unsupported implementation details rather than narrowing to what the source proves.",
            "The page states `issues:view` and `issues:manage` permissions, but the provided Console source only proves `projects:update-settings` for Configure Issues."
          ],
          "mandatoryFixes": [
            "Remove or rewrite unsupported Stacktape-specific claims unless additional source files are provided. In particular, fix the supported-language list, subscription-filter slot behavior, skipped resource cases, fingerprinting scope, detail-page fields, occurrence cap, log-window link, and status-transition behavior.",
            "Restrict the CLI section to `stacktape issues:list` based on the provided `src/commands/issues-list/index.ts`, or provide the source files for resolve, ignore, and reopen before documenting them.",
            "Remove the cost FAQ content from this observability page. If cost context is needed, replace it with a short pointer to `/managing-costs/*` without making pricing or billing claims here.",
            "Replace the permissions table with only the source-grounded `projects:update-settings` requirement for configuring Issues, unless source files proving `issues:view` and `issues:manage` are supplied.",
            "Narrow Console configuration claims to what `IssuesPage.tsx` proves, or add `IssuesSetupModal` source before documenting modal options such as stage-filter limit, sampling range, and per-project behavior."
          ],
          "optionalImprovements": [
            "Add one short source-grounded note that `stacktape issues:list` defaults to `limit ?? 25`, prints a table normally, and prints JSON in agent mode.",
            "Make unsupported FAQ answers more cautious by saying the provided page source does not document the behavior, or link to a narrower page where that behavior is sourced.",
            "Consider adding a small CLI vs Console table listing only proven actions: Console shows summary cards, filters, table, pagination, Configure Issues button when permitted; CLI lists issues with project, stage, status, and limit filters."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft correctly reflects the visible Console table, status filters, summary cards, error type labels, default issue list limit, API-key initialization for `issues:list`, and the CLI JSON output in agent mode. The main problems are unsupported implementation details about detection, grouping, lifecycle persistence, permissions, setup modal options, and CLI commands beyond `issues:list` that are not present in the provided sources.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Stacktape Issues ... automatically detects runtime errors in CloudWatch Logs, groups repeated occurrences by fingerprint, and presents them as trackable issues with status, occurrence count, stack trace, and resource attribution.”",
              "reason": "The provided Console source shows issue fields returned by the API and rendered in a table, but it does not document automatic detection, CloudWatch Logs parsing, fingerprint grouping, stack traces, or resource attribution semantics. Those implementation details may be true elsewhere, but they are not grounded in the provided files.",
              "suggestedFix": "Narrow the opening to what the provided sources show: Issues is a Console and CLI surface for viewing runtime issue records with status, error message, type, function/resource name, project, stage, count, and last-seen timestamp. Only mention CloudWatch log subscriptions where the source explicitly says they are added after redeploy.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "columnHeaders={[\n            'Last seen',\n            'Error',\n            'Type',\n            'Resource',\n            ['Project / Stage', 180],\n            ['Count', 80],\n            ['Status', 100]\n          ]}"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "Matching stages need to be\n            deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Configuration options” table claims options for “Enable for all projects”, “Per-project enable”, “Stage filter” with “up to 50”, and “Event sampling rate” with “1–100%, default 100%”.",
              "reason": "The visible source confirms the modal receives organization-wide enablement, enabled stages, sampling rate with fallback 100, and per-project enabled flags. It does not show the modal UI labels, validation ranges, or a 50-stage limit. The “up to 50” and “1–100%” constraints are not grounded in the provided files.",
              "suggestedFix": "State only that the setup modal is passed organization-wide enablement, enabled stages, per-project enabled state, and an event sampling rate defaulted to 100 when absent. Remove numeric validation ranges unless the modal or API validation source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "issuesAllProjectsEnabled={!!selectedOrg.issuesAllProjectsEnabled}\n          issuesEnabledStages={selectedOrg.issuesEnabledStages || []}\n          issuesEventSamplingRate={selectedOrg.issuesEventSamplingRate || 100}\n          projects={allProjects.map((project) => ({\n            id: project.id,\n            name: project.name,\n            issuesEnabled: !!project.issuesEnabled\n          }))}"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Supported languages” claims issue detection recognizes TypeScript/JavaScript, Python, Go, Java, .NET, Ruby, and PHP runtimes and works with Lambda functions and container workloads.",
              "reason": "The provided sources do not enumerate supported languages or supported workload/resource types. The Console table displays `functionName`, but that does not establish runtime language coverage or container support.",
              "suggestedFix": "Remove the language list and workload coverage claim, or replace it with narrower wording that the Console displays a Resource column backed by `issue.functionName` in the provided source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "{\n              label: <p>{issue.functionName || '-'}</p>,\n              value: issue.functionName || ''\n            }"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“CloudWatch Logs subscription filter slots” claims issue detection consumes one of two subscription-filter slots, skips resources with both slots used, and skips edge functions, user-managed packaging, and unsupported language runtimes.",
              "reason": "The source only says matching stages need redeployment before AWS log subscriptions are added. It does not mention subscription-filter slot counts, skip behavior, edge functions, packaging modes, or unsupported runtime handling.",
              "suggestedFix": "Keep the sourced warning that redeploying with Stacktape CLI 3.8.0 or newer is required before AWS log subscriptions are added. Remove the detailed slot and skip-behavior claims unless additional detector/deployment source files are provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "Configure Issues for one or more projects to start capturing runtime errors here. Matching stages need to be\n            deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added. New projects start with\n            Issues turned off."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The “Issue lifecycle” section claims fingerprint scoping, issue creation in Open state, occurrence-count increments, last-seen updates, resolver attribution, and Reopen behavior for Resolved/Ignored issues.",
              "reason": "The provided Console page renders issue status, occurrence count, and last occurrence, but it does not define fingerprinting behavior, lifecycle transitions, default creation state, who-resolved tracking, or status mutation behavior. Those claims need API/server or issue detail/action sources.",
              "suggestedFix": "Limit lifecycle wording to the statuses visible in the source (`OPEN`, `RESOLVED`, `IGNORED`) and the displayed fields (`occurrenceCount`, `lastOccurrence`). Do not describe fingerprinting or transition side effects without backing source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "const getStatusColor = (status: 'OPEN' | 'RESOLVED' | 'IGNORED') => {\n  if (status === 'OPEN') return colors.error;\n  if (status === 'RESOLVED') return colors.success;\n  return colors.fontColorLighterGray;\n};"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "{\n              label: <p css={{ fontWeight: 600 }}>{issue.occurrenceCount}</p>,\n              value: issue.occurrenceCount\n            }"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The “Viewing issues in the Console” detail-page claims include full stack traces with parsed frames, first/last seen timestamps, up to 50 recent occurrences, and links to logs around each occurrence with a ±1 minute window.",
              "reason": "The provided source only shows the list page and row navigation to `/issues/${issue.id}`. It does not include the issue detail page implementation, stack trace rendering, occurrence retention limits, or log-link time windows.",
              "suggestedFix": "Say that selecting a row navigates to the issue detail route, if you want to mention the visible behavior. Remove detail-page field and retention claims unless the detail-page source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "rowActions={issues.map((issue) => () => navigate(`/issues/${issue.id}`))}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "“All four CLI commands require an API key with the `issues:manage` permission.”",
              "reason": "The provided source for `issues:list` initializes the API client with an API key, but it does not state any permission requirement. The Console source separately implies viewing issues and configuring issues are distinct permissions; it does not show `issues:manage` as required for listing/viewing.",
              "suggestedFix": "For `issues:list`, say the command initializes the Stacktape API client with the configured API key. Do not claim a specific permission unless the command/API permission source is provided. Keep Console setup permission as `projects:update-settings`, which is visible in source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "await stacktapeTrpcApiManager.init({ apiKey: globalStateManager.apiKey });"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "const canConfigureIssues = hasPermission('projects:update-settings');"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft documents `issues:resolve`, `issues:ignore`, and `issues:reopen` behavior and says all four issue commands work with API keys in scripting, CI/CD pipelines, and AI coding assistants.",
              "reason": "The provided CLI source covers only `issues:list`. It supports API-key initialization, table output, filters, limit default, and JSON output in agent mode. No source is provided for resolve, ignore, reopen, CI/CD behavior, or deployment-gate usage.",
              "suggestedFix": "Document `issues:list` from the provided command source. Mention the other command routes only as existing CLI reference links if needed, without describing flags or behavior unless their command sources are supplied.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "export const commandIssuesList = async () => {\n  await stacktapeTrpcApiManager.init({ apiKey: globalStateManager.apiKey });\n\n  const { projectName, stage, issueStatus, limit } = globalStateManager.args;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "if (isAgentMode()) {\n    tuiManager.info(JSON.stringify(issues, null, 2));\n  } else {\n    tuiManager.printTable({"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft correctly uses valid documentation routes from the provided navigation index, including `/cli/issues-list`, `/observability/logs`, `/observability/alarms`, and `/stacktape-console/api-keys`.",
            "The draft correctly states that the Console list page has Open, Resolved, and Ignored summary cards, filters by status/search/project/stage, and defaults to the Open status filter with 20 items per page.",
            "The draft correctly states that `stacktape issues:list` uses a default limit of 25 and outputs raw JSON in agent mode, matching the provided command source.",
            "The six error type labels in the draft match the `errorTypeLabels` map in the Console source."
          ]
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is heavily over-specific relative to the provided sources. The Console issues list and `issues:list` command are grounded, but many Stacktape-specific claims about detection internals, setup constraints, lifecycle actions, CLI commands, permissions, and details-page behavior are not supported by the two source files.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“It automatically detects runtime errors in CloudWatch Logs, groups repeated occurrences by fingerprint, and presents them as trackable issues with status, occurrence count, stack trace, and resource attribution.”",
              "reason": "The provided sources support that the Console table shows issues with error message, type, function/resource-ish name, project/stage, count, status, and last occurrence. They do not support automatic CloudWatch Logs detection internals, fingerprint grouping, stack traces, or resource attribution beyond `functionName`.",
              "suggestedFix": "Narrow the opening to what the sources show: Stacktape Issues lists recorded runtime issues with status, error message, type, project/stage, occurrence count, last occurrence, and function name when available. Avoid fingerprint, stack trace, and automatic CloudWatch Logs detection unless additional backend/detector sources are provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "columnHeaders={[\n            'Last seen',\n            'Error',\n            'Type',\n            'Resource',\n            ['Project / Stage', 180],\n            ['Count', 80],\n            ['Status', 100]\n          ]}"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "<p>{issue.functionName || '-'}</p>"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft describes a full issue lifecycle with resolve, ignore, reopen actions, who resolved it, and status transition behavior.",
              "reason": "The provided Console source displays status values and navigates to a detail page, but it does not show resolve, ignore, or reopen UI actions, audit fields, or transition behavior. The only provided CLI source is `issues:list`, not resolve/ignore/reopen implementations.",
              "suggestedFix": "Keep only the grounded status values `OPEN`, `RESOLVED`, and `IGNORED`. Mention resolve/ignore/reopen only as linked CLI pages if separate command sources are provided, or phrase cautiously that related CLI pages exist in the navigation index without documenting behavior.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "const [statusFilter, setStatusFilter] = useState<'ALL' | 'OPEN' | 'RESOLVED' | 'IGNORED'>('OPEN');"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "rowActions={issues.map((issue) => () => navigate(`/issues/${issue.id}`))}"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Stacktape provides four CLI commands for working with issues” and the draft documents `issues:resolve`, `issues:ignore`, and `issues:reopen` flags and behavior.",
              "reason": "The provided CLI source only includes `src/commands/issues-list/index.ts`. The draft's command behavior for resolve, ignore, and reopen is not grounded in the provided command source files.",
              "suggestedFix": "Document `stacktape issues:list` from the provided source. Remove behavioral details for `issues:resolve`, `issues:ignore`, and `issues:reopen` unless their source files are provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "export const commandIssuesList = async () => {"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "const { projectName, stage, issueStatus, limit } = globalStateManager.args;"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“All four CLI commands require an API key with the `issues:manage` permission.”",
              "reason": "The provided `issues:list` command initializes with an API key, but no source states required permissions. The Console source also does not define `issues:view` or `issues:manage`; it only checks `projects:update-settings` for configuring Issues.",
              "suggestedFix": "Say `issues:list` uses the configured Stacktape API key. Remove the specific `issues:view`/`issues:manage` permission claims unless permission source files are provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "await stacktapeTrpcApiManager.init({ apiKey: globalStateManager.apiKey });"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "const canConfigureIssues = hasPermission('projects:update-settings');"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft says the issue detail page shows full error message and stack trace, parsed frames, first/last seen timestamps, up to 50 occurrences, and links to logs around each occurrence with a ±1 minute window.",
              "reason": "The provided source only shows navigation to `/issues/${issue.id}`. It does not include the detail page implementation or any of the listed detail-page fields and log-link behavior.",
              "suggestedFix": "Remove the detailed description of the issue detail page, or replace it with a cautious statement that selecting a row opens the issue detail route. Add the detail-page source before documenting fields.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "rowActions={issues.map((issue) => () => navigate(`/issues/${issue.id}`))}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft documents configuration option constraints: stage filter “up to 50”, event sampling rate “1–100%, default 100%”, and recommends “10–25%” sampling for high-traffic stages.",
              "reason": "The provided source supports that the modal receives `issuesEnabledStages` and `issuesEventSamplingRate`, with a fallback of 100. It does not support the 50-stage limit, 1–100 range, or the 10–25% recommendation.",
              "suggestedFix": "Keep the grounded facts: configuration includes enabled stages and an event sampling rate, and the current Console page passes 100 when no organization sampling rate is set. Remove numeric bounds and recommended percentages unless `IssuesSetupModal` or backend validation sources are provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "issuesEnabledStages={selectedOrg.issuesEnabledStages || []}\n          issuesEventSamplingRate={selectedOrg.issuesEventSamplingRate || 100}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Issue detection recognizes error patterns from TypeScript/JavaScript, Python, Go, Java, .NET, Ruby, and PHP runtimes” and “works with Lambda functions and container workloads.”",
              "reason": "The provided sources contain the six error type labels and table fields, but they do not identify supported languages or workload kinds. `functionName` alone is not enough to claim Lambda and container workload support.",
              "suggestedFix": "Remove the supported-language and workload-kind claims unless detector/backend sources are provided. You can document the visible error type labels from `IssuesPage.tsx`.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "const errorTypeLabels: Record<string, string> = {\n  uncaught: 'Uncaught',\n  caught: 'Caught',\n  unhandled_rejection: 'Unhandled Rejection',\n  exit_error: 'Exit Error',\n  handler_not_found: 'Handler Not Found',\n  panic: 'Panic'\n};"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft claims issue detection uses one of two CloudWatch Logs subscription-filter slots, skips resources when both slots are used, and skips edge functions, user-managed packaging, and unsupported runtimes.",
              "reason": "The provided Console source only says matching stages need CLI 3.8.0 or newer before AWS log subscriptions are added. It does not support slot consumption, skip conditions, edge function exclusions, packaging exclusions, or runtime exclusions.",
              "suggestedFix": "Limit the setup warning to the grounded deployment requirement for AWS log subscriptions. Remove slot and skip-condition details unless CloudWatch subscription wiring sources are provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "Matching stages need to be\n            deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added."
                }
              ]
            }
          ],
          "positiveFindings": [
            "The Console page heading, Configure Issues button label, `projects:update-settings` permission for showing that button, three summary cards, status/search/project/stage filters, table columns, default status filter `OPEN`, default page size 20, and external pagination are grounded in `IssuesPage.tsx`.",
            "The `issues:list` command default limit of 25, filters `projectName`, `stage`, `issueStatus`, `limit`, formatted table output, and agent-mode JSON output are grounded in `src/commands/issues-list/index.ts`."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft covers the main Console surface and the `issues:list` CLI behavior that are visible in the provided sources. I found several publication-relevant gaps where the page documents CLI or Console capabilities beyond the provided source, and one place where an unsupported limit is presented as fact.",
          "issues": [
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Stacktape provides four CLI commands for working with issues.” The draft then documents `issues:resolve`, `issues:ignore`, and `issues:reopen`.",
              "reason": "The provided CLI source only includes `src/commands/issues-list/index.ts`, which implements `issues:list`. No source for resolve, ignore, or reopen behavior is provided, so this page cannot ground their behavior, required flags, output, or authentication details from the supplied files.",
              "suggestedFix": "Narrow the CLI section to `stacktape issues:list` unless source files for `issues-resolve`, `issues-ignore`, and `issues-reopen` are included. If those sources are available in another iteration, cite their behavior from the command implementations.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "export const commandIssuesList = async () => {"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "const { projectName, stage, issueStatus, limit } = globalStateManager.args;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Set a custom limit (1–100).”",
              "reason": "The `issues:list` command source shows a default limit of 25 and passes the provided `limit` through to `listIssues`, but it does not define or validate a 1–100 range. The numeric range may be true elsewhere, but it is not grounded in the provided command source.",
              "suggestedFix": "Change to “Set a custom limit” and keep the example, or include the source that validates the 1–100 range.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "limit: limit ?? 25"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“All four CLI commands require an API key with the `issues:manage` permission.”",
              "reason": "The supplied `issues:list` source initializes the API client with `globalStateManager.apiKey`, but it does not state required permissions. It also does not cover the other three commands. The Console source only exposes `projects:update-settings` for configuring issues; it does not establish CLI permission requirements.",
              "suggestedFix": "For the sourced CLI command, say that `issues:list` initializes the Stacktape API client using the configured API key. Avoid naming exact CLI permissions unless the API key or command authorization source is included.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "await stacktapeTrpcApiManager.init({ apiKey: globalStateManager.apiKey });"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "const canConfigureIssues = hasPermission('projects:update-settings');"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The Issues page in the Stacktape Console shows three summary cards — Open, Resolved, and Ignored — displaying the count for each status.”",
              "reason": "This statement is supported, but the same section later claims the detail page shows stack traces, parsed frames, up to 50 recent occurrences, and log links. The provided Console source only shows navigation to `/issues/${issue.id}`; it does not include the detail page implementation.",
              "suggestedFix": "Keep the list-page description grounded in `IssuesPage.tsx`, but remove or soften the detail-page feature list unless the issue detail page source is supplied.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "<SummaryCard label=\"Open\" value={statusCounts.OPEN} accentColor={colors.error} />"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "rowActions={issues.map((issue) => () => navigate(`/issues/${issue.id}`))}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Issue monitoring is off by default for new projects.”",
              "reason": "The empty-state copy says “New projects start with Issues turned off,” which supports the new-project default. However, the draft also states several detailed setup behaviors that are not in the provided source, including “Console settings take effect immediately,” “CloudWatch Logs subscription filters are created during the next deployment,” and sampling configuration semantics.",
              "suggestedFix": "Keep the new-project default and CLI 3.8.0 redeploy requirement. Move the deployment-wiring and sampling-behavior details behind cautious wording or add the setup modal/backend source that proves them.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "New projects start with Issues turned off."
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added."
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "issuesEventSamplingRate={selectedOrg.issuesEventSamplingRate || 100}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "“Resource — The Lambda function or container resource that produced the error.”",
              "reason": "The table source renders `issue.functionName || '-'` under the Resource column. The provided source does not show a separate container resource field or prove that the value always represents either a Lambda function or container resource.",
              "suggestedFix": "Use the UI-grounded wording “Resource shows the issue’s function name when available, or `-` when unavailable.” If backend issue schema source is supplied, broaden this with the exact resource attribution fields.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "columnHeaders={[ 'Last seen', 'Error', 'Type', 'Resource', ['Project / Stage', 180], ['Count', 80], ['Status', 100] ]}"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "label: <p>{issue.functionName || '-'}</p>"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft correctly separates the Console issue list from the `stacktape issues:list` CLI surface instead of blurring them into a single workflow.",
            "The Console filtering coverage is well grounded: status, search, project, and stage filters are all present in `IssuesPage.tsx`.",
            "The `issues:list` section correctly documents the default limit of 25 and JSON output in agent mode from the provided CLI source.",
            "The status values and error type labels in the draft match the values visible in `IssuesPage.tsx`."
          ]
        }
      ],
      "status": "failed",
      "passed": false
    },
    {
      "iteration": 2,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\observability__issues\\iteration-2.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 8,
            "audienceFit": 8
          },
          "strengths": [
            "All six error type labels (uncaught, caught, unhandled_rejection, exit_error, handler_not_found, panic) are documented correctly and match the source code in IssuesPage.tsx exactly.",
            "Console UI walkthrough is thoroughly grounded in the IssuesPage.tsx source — summary cards, filter fields, table columns, pagination default of 20 items, and row-click navigation are all confirmed by the source.",
            "CLI examples use correct flag names (--projectName, --stage, --issueStatus, --limit) and the default limit of 25 verified against `limit ?? 25` in issues-list/index.ts.",
            "The agent-mode JSON field list (id, status, errorMessage, errorType, functionName, project, stage, occurrenceCount, lastOccurrence) matches what the source actually outputs.",
            "The status color coding (red/green/gray) and the permission guard for the Configure button (`projects:update-settings`) are both source-grounded facts surfaced as useful signals for the reader.",
            "The comparison table (Issues vs logs vs alarms) adds meaningful context for where Issues fits alongside other Stacktape observability tools, helping readers avoid duplicating tooling.",
            "FAQ covers all three required categories: Stacktape-specific setup questions, AWS service cost context, and choosing/comparison against Sentry/Bugsnag.",
            "The Warning callout about CLI 3.8.0 being required after enabling monitoring is a well-placed, actionable gotcha that first-time users will frequently hit.",
            "The Limitations section proactively addresses false positives from log-based detection, which is a real gap that readers need to know before committing to Issues as their sole error tracking tool."
          ],
          "problems": [
            "The claim that the table 'sorts by last-seen timestamp descending' is not fully confirmed by the provided source. The source sets `defaultSortColumnIndex={0}` (confirming the sort column is Last seen) but does not specify sort direction in the provided code.",
            "The `<ConsoleScreenshot>` MDX component is used but is not listed in the style guide's component palette, nor confirmed in the Console source files. If this component does not exist in the MDX runtime, the block will render as raw text or throw an error.",
            "The setup modal's four settings (Organization-wide enable, Per-project enable, Enabled stages, Event sampling rate) are inferred from the props passed to `<IssuesSetupModal>` — the modal's own source was not provided. The settings are reasonable inferences but one level removed from authoritative source."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Confirm and either correct or qualify the default sort direction claim ('sorts by last-seen timestamp descending'). If the sort direction isn't specified in the source, drop 'descending' from the sentence to avoid an ungrounded claim.",
            "Verify that `<ConsoleScreenshot>` is a real MDX component registered in the docs renderer. If it isn't, replace it with a standard markdown image (`![alt](path)`) or remove the block entirely. A broken component silently renders nothing, which is worse than no screenshot.",
            "The 'Enabling issue monitoring' section mentions the modal but the modal source wasn't available. If any of the four settings labels or their descriptions turn out to differ from the real modal UI, they'll need adjustment. Consider scoping the wording slightly — e.g. 'the modal includes settings for...' — rather than enumerating them as a definitive four-item list.",
            "The `<Tip>` about CI/CD pipeline usage (auto-resolving issues after deploy) is useful but abstract. Adding a one-line concrete example of using `--issueStatus OPEN` as a gate before a promote step would make it immediately actionable.",
            "Consider adding a brief note in the 'Enabling issue monitoring' section clarifying that each stage must be redeployed individually — if a project has three stages (dev, staging, production), all three need a fresh deploy. The current wording says 'redeploy each target stage' but new users may not realize this applies per-stage, not per-project."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 9,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 9,
            "audienceFit": 9
          },
          "strengths": [
            "All error type labels exactly match the source's `errorTypeLabels` map (uncaught, caught, unhandled_rejection, exit_error, handler_not_found, panic) — no invented types.",
            "CLI defaults are verified against source: `limit ?? 25` in issues-list/index.ts, `perPageAmount` starts at 20 in IssuesPage.tsx, status filter defaults to 'OPEN'. All three match the doc.",
            "CLI vs Console separation is clean throughout — the observability section instruction 'never blur the two' is honored with dedicated H2 sections.",
            "Agent-mode JSON field names match what the CLI source returns (id, status, errorMessage, errorType, functionName, project, stage, occurrenceCount, lastOccurrence).",
            "Console table columns match the source's `columnHeaders` array exactly: Last seen, Error, Type, Resource, Project / Stage, Count, Status.",
            "Stage dropdown behavior (built from deployed + undeployed stages + existing issues) is sourced precisely from the `stageOptions` useMemo in IssuesPage.tsx.",
            "The permission `projects:update-settings` is used verbatim from `hasPermission('projects:update-settings')` in the source.",
            "FAQ spans all three required categories: Stacktape-specific (enabling, redeploying, permissions), AWS-service (cost, CloudWatch subscription filters), and comparison (vs Sentry/Bugsnag, vs alarms).",
            "The comparison table (Issues vs logs vs alarms) is high-signal and actionable — it answers the real decision a reader faces without marketing language.",
            "No YAML config blocks, no StacktapeConfig type pattern, no fenced TypeScript Stacktape config — all format rules observed."
          ],
          "problems": [
            "The IssuesSetupModal configuration table ('four settings') is inferred from the modal component's props (issuesAllProjectsEnabled, issuesEnabledStages, issuesEventSamplingRate, per-project issuesEnabled) since IssuesSetupModal.tsx was not in the provided sources. The four-setting description is plausible, but 'Leave empty to monitor all stages' for the Enabled stages field is an inference not directly verified from source. Minor risk of inaccuracy.",
            "H2 headings 'Viewing issues in the Console' and 'Managing issues from the CLI' are slightly wordy compared to short noun-form labels the style guide recommends (1-5 words). Not a hard violation but reduces scannability under the style guide rubric.",
            "The <ConsoleScreenshot> component appears without precedent in the provided example guidance — no example page was supplied, so drift cannot be formally assessed, but the component's usage (with detailed alt text and caption) looks correct in principle."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Shorten H2s: 'Viewing issues in the Console' → 'Console view'; 'Managing issues from the CLI' → 'CLI commands'. Keeps the style guide's 1-5 word label preference.",
            "Scope the IssuesSetupModal configuration table with a hedging phrase (e.g., 'The configuration panel includes the following settings:') rather than asserting the exact count as a hard fact, since the modal source was not available for verification.",
            "The 'Enabling issue monitoring' section could note that 'Enabled stages' accepting an empty list to monitor all stages is documented behavior — or omit that detail if it can't be confirmed from modal source, replacing it with a link to the Console.",
            "The Limitations section is useful but short. Consider adding that Issues only captures errors from resources whose logs flow through CloudWatch Logs — this sets correct expectations for resources with custom log routing."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 6,
            "practicalUsefulness": 6,
            "audienceFit": 7
          },
          "strengths": [
            "Opening paragraph is answer-first, specific, and suitable for extraction.",
            "The page distinguishes Console usage from CLI usage better than most observability pages, with separate sections for Console viewing and CLI listing.",
            "Tables make the error types, statuses, Console columns, and Issues/logs/alarms comparison easy to scan.",
            "Shell examples follow the one-command-per-block rule and avoid copy-buffer comments."
          ],
          "problems": [
            "Several Stacktape-specific claims are not grounded in the provided source files, including `issues:resolve`, `issues:ignore`, and `issues:reopen` commands, status-change behavior, issue detail behavior beyond row navigation, API-key authentication for all issue commands, and CI/CD usage patterns.",
            "The setup modal details are over-specified from `IssuesPage.tsx`; the source shows `IssuesSetupModal` props and the Configure Issues button, but not the modal's visible labels or exact interaction flow.",
            "The error type descriptions go beyond the source. The source only provides labels for `uncaught`, `caught`, `unhandled_rejection`, `exit_error`, `handler_not_found`, and `panic`, not the explanatory meanings for each type.",
            "Cost content appears in the FAQ despite the section instructions saying cost is separate and should link to `/managing-costs/*` if needed.",
            "The page claims sorting by last-seen timestamp descending, but the provided source only shows `defaultSortColumnIndex={0}` and does not establish descending order.",
            "The Issues vs logs vs alarms table contains claims outside the provided source, especially alarm behavior, notifications, and logs being always on.",
            "The FAQ is broad and useful, but multiple answers repeat unsupported operational claims, especially around cost, CI/CD automation, permissions beyond configure access, and dedicated APM comparisons."
          ],
          "mandatoryFixes": [
            "Remove or narrow unsupported CLI-management claims unless source files for `issues-resolve`, `issues-ignore`, and `issues-reopen` are provided. From the supplied CLI source, only `stacktape issues:list`, its filters, default limit 25, table output, and agent-mode JSON output are grounded.",
            "Rewrite the setup section to state only what `IssuesPage.tsx` proves: the Console shows a Configure Issues button when the user has `projects:update-settings`, passes organization-wide/project/stage/sampling settings to `IssuesSetupModal`, new projects start with Issues off, and matching stages need CLI 3.8.0+ redeployment before AWS log subscriptions are added.",
            "Remove the FAQ cost answer or replace it with a brief pointer to `/managing-costs/overview`; do not discuss added AWS cost or processing-volume cost in this observability page.",
            "Replace detailed error-type definitions with cautious wording that these are the labels displayed by the Console/CLI, unless source for classification semantics is added.",
            "Remove or qualify the descending-sort claim; the supplied source does not prove sort direction.",
            "Revise the Issues vs logs vs alarms comparison so every Stacktape-specific statement is grounded by supplied source, or provide the logs and alarms source files."
          ],
          "optionalImprovements": [
            "Add a short “CLI support” table showing only sourced `issues:list` flags: `--projectName`, `--stage`, `--issueStatus`, and `--limit`.",
            "Tighten the FAQ by keeping questions that are grounded by the page source and moving speculative comparisons with Sentry, Datadog, and Bugsnag into a cautious “When not to use” paragraph.",
            "Consider adding a brief source-grounded note that non-agent CLI output truncates error messages longer than 60 characters, while agent mode prints raw JSON.",
            "Use more cautious language around “no additional SDK” and “no third-party error tracking service” unless the detection pipeline source confirms that no application instrumentation is ever needed."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the provided Issues page and `issues:list` CLI source for statuses, filters, table columns, default page size, status colors, CLI default limit, agent-mode JSON output, and CLI auth initialization. The main problems are unsupported claims about Console configuration details, CLI status-changing commands, issue detection semantics, and automation use cases that are not grounded in the two supplied source files.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "\"The CLI provides three commands to change an issue's status: `stacktape issues:resolve`, `stacktape issues:ignore`, `stacktape issues:reopen`\" and related claims that these commands mark issues resolved, ignored, or open.",
              "reason": "The supplied CLI source only includes `issues:list`. The Issues page source shows row navigation but no resolve/ignore/reopen actions or command behavior. The navigation index proves the routes exist, but the provided source files do not establish the behavior, flags, or implementation of these commands.",
              "suggestedFix": "Narrow this to say that the CLI reference includes separate pages for `issues:resolve`, `issues:ignore`, and `issues:reopen`, or remove behavioral descriptions unless the corresponding command source files are added.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "export const commandIssuesList = async () => {"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "rowActions={issues.map((issue) => () => navigate(`/issues/${issue.id}`))}"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "\"Use these commands in deployment pipelines to auto-resolve issues after a successful deploy, or to check for open issues as a deployment gate before promoting a stage.\"",
              "reason": "The provided source supports listing issues and authenticating with the configured API key, but it does not support pipeline automation semantics or auto-resolving after deploy. This recommendation depends on behavior of status-changing commands and deployment workflows not present in the supplied source.",
              "suggestedFix": "Keep the grounded part: `issues:list` can return JSON in agent mode and can filter by project, stage, status, and limit. Remove the auto-resolve/deployment-gate recommendation unless command sources for resolve/ignore/reopen and CI behavior are provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "if (isAgentMode()) {\n    tuiManager.info(JSON.stringify(issues, null, 2));\n  } else {\n    tuiManager.printTable({"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "const { projectName, stage, issueStatus, limit } = globalStateManager.args;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"The setup modal accepts four settings\" with settings named organization-wide enable, per-project enable, enabled stages, and event sampling rate.",
              "reason": "The Issues page passes related values into `IssuesSetupModal`, but the provided source does not include `IssuesSetupModal.tsx`, so the modal's visible settings, labels, input behavior, and exact semantics are not grounded. The draft overstates implementation details from a component whose source is not supplied.",
              "suggestedFix": "Write cautiously: \"The Issues page opens an `IssuesSetupModal` with organization-wide, stage, sampling-rate, and per-project project state.\" Avoid asserting exact modal fields or labels unless the modal source is included.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "<IssuesSetupModal\n          isOpen={isIssuesSetupModalOpen}\n          setIsOpen={setIsOpen}\n          organizationId={selectedOrg.id}\n          issuesAllProjectsEnabled={!!selectedOrg.issuesAllProjectsEnabled}\n          issuesEnabledStages={selectedOrg.issuesEnabledStages || []}\n          issuesEventSamplingRate={selectedOrg.issuesEventSamplingRate || 100}\n          projects={allProjects.map((project) => ({\n            id: project.id,\n            name: project.name,\n            issuesEnabled: !!project.issuesEnabled\n          }))}\n        />"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"The sampling rate controls what fraction of detected error events the system processes. At 100 (the default), every error is captured.\"",
              "reason": "The supplied source shows a default value of `100` passed to the setup modal when the organization value is missing. It does not define sampling-rate units, valid range, whether 100 means every error is captured, or whether counts become approximate when lowered.",
              "suggestedFix": "Ground the claim narrowly: \"The Issues page passes `issuesEventSamplingRate` into the setup modal and falls back to `100` when the organization has no value.\" Avoid explaining capture guarantees or approximate counts unless the modal/server source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "issuesEventSamplingRate={selectedOrg.issuesEventSamplingRate || 100}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"Stacktape Issues detects errors from log output\" and the detailed Limitations section about recognizable log patterns, swallowed errors, silent data corruption, and false positives.",
              "reason": "The Issues page source supports that CloudWatch Logs subscription filters are added after deployment, but it does not define the detection algorithm, recognizable patterns, false-positive behavior, or what kinds of application errors are excluded. The draft turns the subscription mechanism into detailed detection guarantees and limitations not present in the source.",
              "suggestedFix": "Keep the grounded mechanism: matching stages need redeployment before AWS log subscriptions are added. Rephrase limitations cautiously, for example: \"The provided Console source does not document in-process instrumentation; issue capture is tied to AWS log subscriptions.\"",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "Matching stages need to be\n            deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"Issue monitoring is off by default for new projects.\"",
              "reason": "The source says new projects start with Issues turned off in the empty state text, but that text appears when the organization has no enabled projects and no recorded issues. The draft generalizes it as a configuration default for all new projects, which is stronger than the supplied source proves.",
              "suggestedFix": "Use the exact source-grounded wording: \"The Console empty state says new projects start with Issues turned off.\"",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "New projects start with\n            Issues turned off."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"The Stacktape CLI provides commands for working with issues. These commands use your configured API key and work in non-interactive environments like CI/CD pipelines and AI coding assistants.\"",
              "reason": "The `issues:list` command initializes the API client with `globalStateManager.apiKey`, and agent mode emits JSON. The source does not establish behavior for all issue commands, CI/CD suitability, or AI coding assistant environments.",
              "suggestedFix": "Limit this section to the provided command: \"`issues:list` initializes the API client with the configured API key. In agent mode, `issues:list` prints JSON instead of a table.\"",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "await stacktapeTrpcApiManager.init({ apiKey: globalStateManager.apiKey });"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "if (isAgentMode()) {\n    tuiManager.info(JSON.stringify(issues, null, 2));"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "\"The deployment adds AWS CloudWatch Logs subscription filters that route error events to the detection system.\"",
              "reason": "The Console source says matching stages must be deployed before AWS log subscriptions are added, but it does not say the deployment adds CloudWatch Logs subscription filters specifically, nor does it describe routing to a detection system. This may be true, but the supplied source only supports the narrower phrase \"AWS log subscriptions are added.\"",
              "suggestedFix": "Use the source wording: \"Redeploy matching stages with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added.\"",
              "evidence": []
            }
          ],
          "positiveFindings": [
            "The draft correctly uses the documented CLI command route `/cli/issues-list`; all markdown cross-links point to routes present in the navigation index.",
            "The Console status filters, status labels, status colors, summary cards, project/stage/search filters, default Open filter, default 20-item pagination, and table columns are grounded in `IssuesPage.tsx`.",
            "The `issues:list` examples use supported argument names from the command source: `projectName`, `stage`, `issueStatus`, and `limit`; the default limit of 25 is grounded in `limit: limit ?? 25`."
          ]
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is mostly grounded in the provided Console and CLI sources, especially the Issues table, filters, status values, setup button permission, and `issues:list` CLI behavior. The highest-impact problems are unsupported claims about issue detection mechanics, setup-modal behavior, CLI authentication/status-changing commands, and detailed error-type semantics not present in the supplied source files.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "\"After enabling, redeploy each target stage with Stacktape CLI 3.8.0 or newer. The deployment adds AWS CloudWatch Logs subscription filters that route error events to the detection system. Until you redeploy, no errors are captured for that stage.\"",
              "reason": "The provided Console source supports that matching stages need CLI 3.8.0 or newer before AWS log subscriptions are added, but it does not support the stronger claim that no errors are captured until redeploy, nor that the subscriptions route events to a specific detection system.",
              "suggestedFix": "Narrow the claim to the source-backed fact: matching stages need to be deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added. Avoid asserting exact capture behavior beyond that.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "Configure Issues for one or more projects to start capturing runtime errors here. Matching stages need to be\n            deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added. New projects start with\n            Issues turned off."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "\"Issue monitoring is off by default for new projects. Configure it from the Issues page in the Stacktape Console by clicking Configure Issues. The setup modal accepts four settings: Organization-wide enable, Per-project enable, Enabled stages, Event sampling rate.\"",
              "reason": "The source supports that new projects start with Issues off, that a Configure Issues button opens `IssuesSetupModal`, and that props related to all-project enablement, enabled stages, sampling rate, and per-project `issuesEnabled` values are passed. The provided source does not include `IssuesSetupModal`, so the draft's exact modal fields and setting descriptions are not fully grounded.",
              "suggestedFix": "Keep the supported setup entry point and permission claim, but phrase the modal settings cautiously: the Issues page opens an `IssuesSetupModal` with organization-wide enablement, enabled stages, sampling rate, and project issue-enabled state passed into it. Do not present exact field labels/descriptions unless the modal source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "{isIssuesSetupModalOpen && (\n        <IssuesSetupModal\n          isOpen={isIssuesSetupModalOpen}\n          setIsOpen={setIsOpen}\n          organizationId={selectedOrg.id}\n          issuesAllProjectsEnabled={!!selectedOrg.issuesAllProjectsEnabled}\n          issuesEnabledStages={selectedOrg.issuesEnabledStages || []}\n          issuesEventSamplingRate={selectedOrg.issuesEventSamplingRate || 100}\n          projects={allProjects.map((project) => ({\n            id: project.id,\n            name: project.name,\n            issuesEnabled: !!project.issuesEnabled\n          }))}\n        />\n      )}"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "\"Stacktape classifies detected runtime errors into six types\" followed by detailed descriptions for Uncaught, Caught, Unhandled Rejection, Exit Error, Handler Not Found, and Panic.",
              "reason": "The Console source supports the six label strings, but it does not define what each type means. The descriptions for caught exceptions, promise rejections, process exits, Lambda handler lookup, and language panics are plausible but not grounded in the supplied source files.",
              "suggestedFix": "List the six labels as Console/CLI labels, and either omit the detailed semantic descriptions or mark them narrowly as labels only. Add definitions only if the detection source or API schema defining `errorType` semantics is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "const errorTypeLabels: Record<string, string> = {\n  uncaught: 'Uncaught',\n  caught: 'Caught',\n  unhandled_rejection: 'Unhandled Rejection',\n  exit_error: 'Exit Error',\n  handler_not_found: 'Handler Not Found',\n  panic: 'Panic'\n};"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "\"The CLI provides three commands to change an issue's status: stacktape issues:resolve, stacktape issues:ignore, stacktape issues:reopen\" and related claims that status changes are available through those CLI commands.",
              "reason": "The documentation navigation lists CLI pages for those commands, but the provided CLI source only contains `issues:list`. No source file for resolve, ignore, or reopen behavior is provided, so behavior and flags for changing status are not grounded in the supplied source.",
              "suggestedFix": "Mention only that the site has CLI reference pages for `issues:resolve`, `issues:ignore`, and `issues:reopen`, or provide the command source files before documenting their behavior. Keep detailed behavior out of this page unless sourced.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "export const commandIssuesList = async () => {\n  await stacktapeTrpcApiManager.init({ apiKey: globalStateManager.apiKey });"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"These commands use your configured API key and work in non-interactive environments like CI/CD pipelines and AI coding assistants.\"",
              "reason": "The `issues:list` command initializes the API client with `globalStateManager.apiKey`, and `isAgentMode()` changes output formatting. The provided source does not establish behavior for all issue commands, CI/CD use, or non-interactive environments generally.",
              "suggestedFix": "Limit the claim to `issues:list`: `issues:list` initializes the Stacktape API client with the configured API key, and in agent mode it prints JSON. Avoid extending the behavior to other issue commands or CI/CD unless their sources are supplied.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "await stacktapeTrpcApiManager.init({ apiKey: globalStateManager.apiKey });"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "if (isAgentMode()) {\n    tuiManager.info(JSON.stringify(issues, null, 2));\n  } else {\n    tuiManager.printTable({"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"In agent mode ... The JSON includes each issue's id, status, errorMessage, errorType, functionName, project, stage, occurrenceCount, and lastOccurrence.\"",
              "reason": "The source confirms that agent mode prints the `issues` array returned by the API as JSON, but it does not type or enumerate the exact JSON fields. The listed fields are visible in table rendering for non-agent mode, but that does not prove the complete agent-mode schema.",
              "suggestedFix": "Say that agent mode prints the raw `issues:list` API response as formatted JSON. If listing fields, qualify them as fields used by the table rendering rather than a guaranteed JSON schema.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "if (isAgentMode()) {\n    tuiManager.info(JSON.stringify(issues, null, 2));\n  } else {"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "rows: issues.map((issue) => [\n        issue.id,\n        issue.status,\n        issue.errorMessage.length > 60 ? `${issue.errorMessage.slice(0, 60)}...` : issue.errorMessage,\n        issue.errorType,\n        issue.functionName || '-',\n        issue.project || '-',\n        issue.stage || '-',\n        String(issue.occurrenceCount),\n        new Date(issue.lastOccurrence).toLocaleString()\n      ])"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"The table paginates at 20 items per page by default and sorts by last-seen timestamp descending.\"",
              "reason": "The source supports a default `perPageAmount` of 20 and `defaultSortColumnIndex={0}`. It does not establish descending sort order; the query parameters do not pass a sort direction, and the table component implementation is not provided.",
              "suggestedFix": "Keep the default 20-items-per-page claim. Remove or soften the descending sort claim unless the table component or API query source confirms it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "const [perPageAmount, setPerPageAmount] = useState(20);"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "defaultSortColumnIndex={0}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"Use these commands in deployment pipelines to auto-resolve issues after a successful deploy, or to check for open issues as a deployment gate before promoting a stage.\"",
              "reason": "This recommendation depends on behavior of status-changing CLI commands and CI usage that is not present in the supplied sources. The only CLI source provided is `issues:list`, and no deployment-pipeline integration behavior is shown.",
              "suggestedFix": "Rephrase as a possible workflow only after command behavior sources are provided, or remove the tip. A source-backed version can say `issues:list` can filter issues by project, stage, status, and limit.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "const { projectName, stage, issueStatus, limit } = globalStateManager.args;\n\n  const issues = await stacktapeTrpcApiManager.apiClient.listIssues({\n    project: projectName,\n    stage,\n    status: issueStatus as 'OPEN' | 'RESOLVED' | 'IGNORED' | undefined,\n    limit: limit ?? 25\n  });"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft correctly identifies the Console Issues page summary cards for Open, Resolved, and Ignored counts, including the status color mapping shown in source.",
            "The draft correctly documents the Console filters: status, search, project, and stage, including the default status filter of Open and reset-to-first-page behavior when filters or page size change.",
            "The `issues:list` examples use source-backed argument names: `--projectName`, `--stage`, `--issueStatus`, and `--limit`, and the default limit of 25 is grounded in the CLI source.",
            "The draft's issue table columns match the Console source closely: Last seen, Error, Type, Resource, Project / Stage, Count, and Status.",
            "All cross-links checked against the provided navigation index point to valid documented routes."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The page is broadly aligned with the two provided sources for the Console issue list and `issues:list` CLI command. There is no primary config type and no Stacktape config `<CodeBlock>` examples, so API-property completeness and code-example demonstrativeness checks do not apply. The main gaps are unsupported details about setup behavior and CLI commands beyond `issues:list`.",
          "issues": [
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The setup modal accepts four settings: Organization-wide enable, Per-project enable, Enabled stages, Event sampling rate.”",
              "reason": "The provided `IssuesPage.tsx` source only proves that `IssuesSetupModal` receives current organization/project values as props. It does not show the modal UI, labels, accepted inputs, or the behavior of leaving stages empty.",
              "suggestedFix": "Either add `IssuesSetupModal` source to the source list or narrow the prose to what `IssuesPage.tsx` proves: users with `projects:update-settings` can open Configure Issues, and the modal is passed organization-wide enabled state, enabled stages, sampling rate, and per-project enabled flags.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "{isIssuesSetupModalOpen && ( <IssuesSetupModal ... issuesAllProjectsEnabled={!!selectedOrg.issuesAllProjectsEnabled} issuesEnabledStages={selectedOrg.issuesEnabledStages || []} issuesEventSamplingRate={selectedOrg.issuesEventSamplingRate || 100} projects={allProjects.map((project) => ({ id: project.id, name: project.name, issuesEnabled: !!project.issuesEnabled }))} /> )}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Displaying the configure button requires the `projects:update-settings` permission in your organization.”",
              "reason": "The source proves that `projects:update-settings` controls whether the Configure Issues button is rendered, but it does not establish organization-level permission semantics beyond the permission string check.",
              "suggestedFix": "Write: “The Configure Issues button is shown only when the current user has the `projects:update-settings` permission.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "const canConfigureIssues = hasPermission('projects:update-settings');"
                },
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "rightSideItems={ canConfigureIssues ? [ <Button key=\"setup-issues\" text=\"Configure Issues\" ... /> ] : [] }"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“At 100 (the default), every error is captured. Lowering the rate reduces processing volume during traffic bursts but makes occurrence counts approximate rather than exact.”",
              "reason": "The source only shows a fallback value of `100` passed into the setup modal. It does not define sampling semantics, units, guarantees, or how occurrence counts are affected.",
              "suggestedFix": "Keep only the grounded part: “The Issues page passes `issuesEventSamplingRate` to the setup modal and uses `100` when no value is present.” Add the modal/backend source before documenting capture guarantees or approximate counts.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "issuesEventSamplingRate={selectedOrg.issuesEventSamplingRate || 100}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Status changes are available through the CLI commands `issues:resolve`, `issues:ignore`, and `issues:reopen`.”",
              "reason": "The provided CLI source only covers `issues:list`. No source for resolve, ignore, or reopen commands is included, so this page cannot ground their behavior from the provided files.",
              "suggestedFix": "Either add the source files for `issues-resolve`, `issues-ignore`, and `issues-reopen`, or phrase this as navigation-only: “See the CLI reference pages for issue status commands.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "export const commandIssuesList = async () => {"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The CLI commands ... accept API key authentication and work in non-interactive environments like CI/CD pipelines and AI coding assistants.”",
              "reason": "`issues:list` initializes the TRPC manager with `globalStateManager.apiKey`, and agent mode changes output to JSON. The source does not prove CI/CD behavior, non-interactive support for all issue commands, or authentication behavior for resolve/ignore/reopen.",
              "suggestedFix": "Limit the claim to `issues:list`: “`issues:list` initializes the API client with the configured API key. In agent mode, it prints raw JSON.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "await stacktapeTrpcApiManager.init({ apiKey: globalStateManager.apiKey });"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "if (isAgentMode()) { tuiManager.info(JSON.stringify(issues, null, 2)); }"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Clicking any row navigates to the issue detail page for that specific error.”",
              "reason": "The source proves row actions navigate to `/issues/${issue.id}`. It does not include the detail page source, so “detail page” behavior and displayed content are not grounded here.",
              "suggestedFix": "Write narrowly: “Clicking a row navigates to `/issues/${issue.id}`.” Add the issue detail page source before describing the destination as a detail view.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "rowActions={issues.map((issue) => () => navigate(`/issues/${issue.id}`))}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Issues remain visible in the Console and CLI even after monitoring is turned off for a project.”",
              "reason": "The Console source explicitly states existing issues remain visible after monitoring is turned off, but the provided CLI source does not establish the same behavior for CLI listing after monitoring is disabled.",
              "suggestedFix": "Say “Existing issues remain visible in the Console after monitoring is turned off for a project.” Only extend the claim to CLI if backend or CLI source proves it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\console-app\\src\\pages\\IssuesPage\\IssuesPage.tsx",
                  "quote": "Existing issues remain visible even after monitoring is turned off for a project."
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\issues-list\\index.ts",
                  "quote": "const issues = await stacktapeTrpcApiManager.apiClient.listIssues({ project: projectName, stage, status: issueStatus as 'OPEN' | 'RESOLVED' | 'IGNORED' | undefined, limit: limit ?? 25 });"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The page correctly documents the Console status filters `ALL`, `OPEN`, `RESOLVED`, and `IGNORED`, matching `buildStatusOptions` in `IssuesPage.tsx`.",
            "The page correctly documents the Console table columns: Last seen, Error, Type, Resource, Project / Stage, Count, and Status.",
            "The page correctly distinguishes Console filtering from the `issues:list` CLI command, whose source supports project, stage, status, and limit arguments.",
            "The page follows the shell-command copy rule for the shown `issues:list` examples: each fenced bash block contains one command and no explanatory comments.",
            "There are no hand-authored Stacktape YAML examples, no plain-object `StacktapeConfig` examples, and no fenced Stacktape TypeScript config examples on this page."
          ]
        }
      ],
      "status": "needs-human-review",
      "passed": false
    },
    {
      "iteration": 3,
      "draftPath": "/Users/congy/Desktop/projects/stacktape/docs/scripts/generate-docs/.state/observability__issues/iteration-3.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 8,
            "audienceFit": 8
          },
          "strengths": [
            "All six error type labels (uncaught, caught, unhandled_rejection, exit_error, handler_not_found, panic) are documented correctly and match the source code in IssuesPage.tsx exactly.",
            "Console UI walkthrough is thoroughly grounded in the IssuesPage.tsx source — summary cards, filter fields, table columns, pagination default of 20 items, and row-click navigation are all confirmed by the source.",
            "CLI examples use correct flag names (--projectName, --stage, --issueStatus, --limit) and the default limit of 25 verified against `limit ?? 25` in issues-list/index.ts.",
            "The agent-mode JSON field list (id, status, errorMessage, errorType, functionName, project, stage, occurrenceCount, lastOccurrence) matches what the source actually outputs.",
            "The status color coding (red/green/gray) and the permission guard for the Configure button (`projects:update-settings`) are both source-grounded facts surfaced as useful signals for the reader.",
            "The comparison table (Issues vs logs vs alarms) adds meaningful context for where Issues fits alongside other Stacktape observability tools, helping readers avoid duplicating tooling.",
            "FAQ covers all three required categories: Stacktape-specific setup questions, AWS service cost context, and choosing/comparison against Sentry/Bugsnag.",
            "The Warning callout about CLI 3.8.0 being required after enabling monitoring is a well-placed, actionable gotcha that first-time users will frequently hit.",
            "The Limitations section proactively addresses false positives from log-based detection, which is a real gap that readers need to know before committing to Issues as their sole error tracking tool."
          ],
          "problems": [
            "The claim that the table 'sorts by last-seen timestamp descending' is not fully confirmed by the provided source. The source sets `defaultSortColumnIndex={0}` (confirming the sort column is Last seen) but does not specify sort direction in the provided code.",
            "The `<ConsoleScreenshot>` MDX component is used but is not listed in the style guide's component palette, nor confirmed in the Console source files. If this component does not exist in the MDX runtime, the block will render as raw text or throw an error.",
            "The setup modal's four settings (Organization-wide enable, Per-project enable, Enabled stages, Event sampling rate) are inferred from the props passed to `<IssuesSetupModal>` — the modal's own source was not provided. The settings are reasonable inferences but one level removed from authoritative source."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Confirm and either correct or qualify the default sort direction claim ('sorts by last-seen timestamp descending'). If the sort direction isn't specified in the source, drop 'descending' from the sentence to avoid an ungrounded claim.",
            "Verify that `<ConsoleScreenshot>` is a real MDX component registered in the docs renderer. If it isn't, replace it with a standard markdown image (`![alt](path)`) or remove the block entirely. A broken component silently renders nothing, which is worse than no screenshot.",
            "The 'Enabling issue monitoring' section mentions the modal but the modal source wasn't available. If any of the four settings labels or their descriptions turn out to differ from the real modal UI, they'll need adjustment. Consider scoping the wording slightly — e.g. 'the modal includes settings for...' — rather than enumerating them as a definitive four-item list.",
            "The `<Tip>` about CI/CD pipeline usage (auto-resolving issues after deploy) is useful but abstract. Adding a one-line concrete example of using `--issueStatus OPEN` as a gate before a promote step would make it immediately actionable.",
            "Consider adding a brief note in the 'Enabling issue monitoring' section clarifying that each stage must be redeployed individually — if a project has three stages (dev, staging, production), all three need a fresh deploy. The current wording says 'redeploy each target stage' but new users may not realize this applies per-stage, not per-project."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 9,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 9,
            "audienceFit": 9
          },
          "strengths": [
            "All error type labels exactly match the source's `errorTypeLabels` map (uncaught, caught, unhandled_rejection, exit_error, handler_not_found, panic) — no invented types.",
            "CLI defaults are verified against source: `limit ?? 25` in issues-list/index.ts, `perPageAmount` starts at 20 in IssuesPage.tsx, status filter defaults to 'OPEN'. All three match the doc.",
            "CLI vs Console separation is clean throughout — the observability section instruction 'never blur the two' is honored with dedicated H2 sections.",
            "Agent-mode JSON field names match what the CLI source returns (id, status, errorMessage, errorType, functionName, project, stage, occurrenceCount, lastOccurrence).",
            "Console table columns match the source's `columnHeaders` array exactly: Last seen, Error, Type, Resource, Project / Stage, Count, Status.",
            "Stage dropdown behavior (built from deployed + undeployed stages + existing issues) is sourced precisely from the `stageOptions` useMemo in IssuesPage.tsx.",
            "The permission `projects:update-settings` is used verbatim from `hasPermission('projects:update-settings')` in the source.",
            "FAQ spans all three required categories: Stacktape-specific (enabling, redeploying, permissions), AWS-service (cost, CloudWatch subscription filters), and comparison (vs Sentry/Bugsnag, vs alarms).",
            "The comparison table (Issues vs logs vs alarms) is high-signal and actionable — it answers the real decision a reader faces without marketing language.",
            "No YAML config blocks, no StacktapeConfig type pattern, no fenced TypeScript Stacktape config — all format rules observed."
          ],
          "problems": [
            "The IssuesSetupModal configuration table ('four settings') is inferred from the modal component's props (issuesAllProjectsEnabled, issuesEnabledStages, issuesEventSamplingRate, per-project issuesEnabled) since IssuesSetupModal.tsx was not in the provided sources. The four-setting description is plausible, but 'Leave empty to monitor all stages' for the Enabled stages field is an inference not directly verified from source. Minor risk of inaccuracy.",
            "H2 headings 'Viewing issues in the Console' and 'Managing issues from the CLI' are slightly wordy compared to short noun-form labels the style guide recommends (1-5 words). Not a hard violation but reduces scannability under the style guide rubric.",
            "The <ConsoleScreenshot> component appears without precedent in the provided example guidance — no example page was supplied, so drift cannot be formally assessed, but the component's usage (with detailed alt text and caption) looks correct in principle."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Shorten H2s: 'Viewing issues in the Console' → 'Console view'; 'Managing issues from the CLI' → 'CLI commands'. Keeps the style guide's 1-5 word label preference.",
            "Scope the IssuesSetupModal configuration table with a hedging phrase (e.g., 'The configuration panel includes the following settings:') rather than asserting the exact count as a hard fact, since the modal source was not available for verification.",
            "The 'Enabling issue monitoring' section could note that 'Enabled stages' accepting an empty list to monitor all stages is documented behavior — or omit that detail if it can't be confirmed from modal source, replacing it with a link to the Console.",
            "The Limitations section is useful but short. Consider adding that Issues only captures errors from resources whose logs flow through CloudWatch Logs — this sets correct expectations for resources with custom log routing."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 6,
            "practicalUsefulness": 6,
            "audienceFit": 7
          },
          "strengths": [
            "Opening paragraph is answer-first, specific, and suitable for extraction.",
            "The page distinguishes Console usage from CLI usage better than most observability pages, with separate sections for Console viewing and CLI listing.",
            "Tables make the error types, statuses, Console columns, and Issues/logs/alarms comparison easy to scan.",
            "Shell examples follow the one-command-per-block rule and avoid copy-buffer comments."
          ],
          "problems": [
            "Several Stacktape-specific claims are not grounded in the provided source files, including `issues:resolve`, `issues:ignore`, and `issues:reopen` commands, status-change behavior, issue detail behavior beyond row navigation, API-key authentication for all issue commands, and CI/CD usage patterns.",
            "The setup modal details are over-specified from `IssuesPage.tsx`; the source shows `IssuesSetupModal` props and the Configure Issues button, but not the modal's visible labels or exact interaction flow.",
            "The error type descriptions go beyond the source. The source only provides labels for `uncaught`, `caught`, `unhandled_rejection`, `exit_error`, `handler_not_found`, and `panic`, not the explanatory meanings for each type.",
            "Cost content appears in the FAQ despite the section instructions saying cost is separate and should link to `/managing-costs/*` if needed.",
            "The page claims sorting by last-seen timestamp descending, but the provided source only shows `defaultSortColumnIndex={0}` and does not establish descending order.",
            "The Issues vs logs vs alarms table contains claims outside the provided source, especially alarm behavior, notifications, and logs being always on.",
            "The FAQ is broad and useful, but multiple answers repeat unsupported operational claims, especially around cost, CI/CD automation, permissions beyond configure access, and dedicated APM comparisons."
          ],
          "mandatoryFixes": [
            "Remove or narrow unsupported CLI-management claims unless source files for `issues-resolve`, `issues-ignore`, and `issues-reopen` are provided. From the supplied CLI source, only `stacktape issues:list`, its filters, default limit 25, table output, and agent-mode JSON output are grounded.",
            "Rewrite the setup section to state only what `IssuesPage.tsx` proves: the Console shows a Configure Issues button when the user has `projects:update-settings`, passes organization-wide/project/stage/sampling settings to `IssuesSetupModal`, new projects start with Issues off, and matching stages need CLI 3.8.0+ redeployment before AWS log subscriptions are added.",
            "Remove the FAQ cost answer or replace it with a brief pointer to `/managing-costs/overview`; do not discuss added AWS cost or processing-volume cost in this observability page.",
            "Replace detailed error-type definitions with cautious wording that these are the labels displayed by the Console/CLI, unless source for classification semantics is added.",
            "Remove or qualify the descending-sort claim; the supplied source does not prove sort direction.",
            "Revise the Issues vs logs vs alarms comparison so every Stacktape-specific statement is grounded by supplied source, or provide the logs and alarms source files."
          ],
          "optionalImprovements": [
            "Add a short “CLI support” table showing only sourced `issues:list` flags: `--projectName`, `--stage`, `--issueStatus`, and `--limit`.",
            "Tighten the FAQ by keeping questions that are grounded by the page source and moving speculative comparisons with Sentry, Datadog, and Bugsnag into a cautious “When not to use” paragraph.",
            "Consider adding a brief source-grounded note that non-agent CLI output truncates error messages longer than 60 characters, while agent mode prints raw JSON.",
            "Use more cautious language around “no additional SDK” and “no third-party error tracking service” unless the detection pipeline source confirms that no application instrumentation is ever needed."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is mostly consistent with the provided Console and CLI sources. The main factual risks are over-specific claims about issue-detail/status-change behavior, API-key behavior, and cost/detection internals that are not grounded in the authoritative source files provided for this page.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Both statuses can be reverted to Open if the error recurs or you change your mind.”",
              "reason": "The provided sources show a Console issues list and the existence of CLI reference routes in the navigation index, but they do not include source for `issues:resolve`, `issues:ignore`, `issues:reopen`, or the issue detail page. The draft can safely say a CLI command exists for reopening, but the broader behavior that resolved/ignored issues can be reverted because an error recurs is not grounded in the provided source.",
              "suggestedFix": "Narrow to the source-grounded behavior: “The CLI reference includes a separate `issues:reopen` command for moving an issue back to Open.” Remove the “if the error recurs” behavior unless issue-detail or command source is provided.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx",
                  "quote": "rowActions={issues.map((issue) => () => navigate(`/issues/${issue.id}`))}"
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/src/commands/issues-list/index.ts",
                  "quote": "export const commandIssuesList = async () => {"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“These commands use your configured API key and work in non-interactive environments like CI/CD pipelines and AI coding assistants.”",
              "reason": "The `issues:list` command initializes the API client with `globalStateManager.apiKey`, but the provided source only covers `issues:list`, not the status-change commands. The CI/CD and AI coding assistant environment claim is also not shown in the command source; only agent-mode JSON output is grounded.",
              "suggestedFix": "Limit the claim to `issues:list`: “`stacktape issues:list` initializes the Stacktape API client with the configured API key. In agent mode, it prints raw JSON for programmatic use.”",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/src/commands/issues-list/index.ts",
                  "quote": "await stacktapeTrpcApiManager.init({ apiKey: globalStateManager.apiKey });"
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/src/commands/issues-list/index.ts",
                  "quote": "if (isAgentMode()) {\n    tuiManager.info(JSON.stringify(issues, null, 2));\n  } else {"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Clicking any row navigates to the issue detail page for that specific error.”",
              "reason": "The Console source shows row actions navigate to `/issues/${issue.id}`, but the provided source does not include the route/page implementation proving that destination is an issue detail page or what it displays.",
              "suggestedFix": "Write more narrowly: “Clicking a row navigates to `/issues/{issue.id}` in the Console.” If the detail page source is available, include it in the source list before describing the page as a detail view.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx",
                  "quote": "rowActions={issues.map((issue) => () => navigate(`/issues/${issue.id}`))}"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The detection mechanism uses AWS CloudWatch Logs subscription filters, which route matching log events to the processing system.”",
              "reason": "The Console source says AWS log subscriptions are added after deployment, but it does not say they are CloudWatch Logs subscription filters or that they route only matching log events. That more specific AWS implementation is not grounded in the provided authoritative source.",
              "suggestedFix": "Use the exact source-grounded wording: “Matching stages need to be deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added.” Avoid naming CloudWatch Logs subscription filters unless implementation source is provided.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx",
                  "quote": "Matching stages need to be\n            deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“For most workloads this adds minimal cost. High-volume stages with noisy error logs may see higher processing volume — lowering the event sampling rate reduces this.”",
              "reason": "The provided source files do not contain pricing, billing, or processing-cost behavior for Issues. The page metadata explicitly says cost should be a separate section for observability pages; this FAQ answer introduces unsupported cost guidance on the Issues page.",
              "suggestedFix": "Remove the cost estimate and link to cost docs only. For example: “Issue monitoring uses log subscriptions added during deployment. For cost planning, see [Managing Costs](/managing-costs/overview).”",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx",
                  "quote": "issuesEventSamplingRate={selectedOrg.issuesEventSamplingRate || 100}"
                },
                {
                  "file": "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx",
                  "quote": "Matching stages need to be\n            deployed with Stacktape CLI 3.8.0 or newer before AWS log subscriptions are added."
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "“The sampling rate controls what fraction of detected error events the system processes. At 100 (the default), every error is captured. Lowering the rate reduces processing volume during traffic bursts but makes occurrence counts approximate rather than exact.”",
              "reason": "The Console source grounds the existence of `issuesEventSamplingRate` and the fallback value `100`, but it does not define the unit, range, semantics, or count accuracy implications. The generated LLM doc repeats this wording, but no authoritative implementation source is provided.",
              "suggestedFix": "Keep only the grounded part unless implementation source is added: “The setup modal receives an event sampling rate setting and falls back to 100 when the organization has no saved value.” Or omit the section.",
              "evidence": []
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "“Stacktape Issues detects errors from log output” and “Issues relies on detecting error patterns from log output.”",
              "reason": "The sources mention AWS log subscriptions and runtime errors, but they do not define the pattern-detection logic or prove that Issues only detects errors from log output. This is probably directionally correct, but the provided source is thin for the stronger limitation wording.",
              "suggestedFix": "Narrow the wording to the sourced mechanism: “Issues requires AWS log subscriptions added during deployment, so errors that never reach subscribed logs may not appear.”",
              "evidence": []
            }
          ],
          "positiveFindings": [
            "The draft uses only valid documentation routes from the provided navigation index.",
            "The Console filters, default status filter, summary cards, table columns, 20-item default pagination, and filter reset behavior are grounded in `IssuesPage.tsx`.",
            "The CLI `issues:list` examples use source-grounded flags: `projectName`, `stage`, `issueStatus`, and `limit`.",
            "The draft avoids forbidden Stacktape config examples, YAML config blocks, and plain-object `StacktapeConfig` style."
          ]
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is mostly grounded in the provided Issues Console and CLI sources. The main problems are two overstatements about CLI/error-type behavior and one unsupported comparison against third-party APM tools.",
          "issues": [
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "“Each issue carries an error-type label shown in both the Console table and the CLI output.”",
              "reason": "The Console maps raw error type keys to friendly labels, but the `issues:list` CLI prints `issue.errorType` directly. The provided CLI source does not show the friendly labels being applied in CLI output.",
              "suggestedFix": "Say that the Console displays friendly error-type labels, while the CLI includes the issue's `errorType` value. If you keep the label table, scope it to the Console or phrase it as “Stacktape uses these error types.”",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx",
                  "quote": "const errorTypeLabels: Record<string, string> = {\n  uncaught: 'Uncaught',\n  caught: 'Caught',\n  unhandled_rejection: 'Unhandled Rejection',\n  exit_error: 'Exit Error',\n  handler_not_found: 'Handler Not Found',\n  panic: 'Panic'\n};"
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/src/commands/issues-list/index.ts",
                  "quote": "tuiManager.printTable({\n      header: ['ID', 'Status', 'Error', 'Type', 'Function', 'Project', 'Stage', 'Count', 'Last seen'],\n      rows: issues.map((issue) => [\n        issue.id,\n        issue.status,\n        issue.errorMessage.length > 60 ? `${issue.errorMessage.slice(0, 60)}...` : issue.errorMessage,\n        issue.errorType,"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "“Use the label to group similar errors at a glance and to filter your triage queue.”",
              "reason": "The provided Console source exposes status, search, project, and stage filters. The provided CLI source exposes project, stage, issueStatus, and limit. Neither source shows filtering by error type or error-type label.",
              "suggestedFix": "Change this to “Use the label to recognize similar errors at a glance.” If you discuss filtering, list only status, search, project, and stage for Console, and project, stage, status, and limit for `issues:list`.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx",
                  "quote": "const [statusFilter, setStatusFilter] = useState<'ALL' | 'OPEN' | 'RESOLVED' | 'IGNORED'>('OPEN');\n  const [projectFilter, setProjectFilter] = useState<Option<string>>(allProjectsOption);\n  const [stageFilter, setStageFilter] = useState<Option<string>>(allStagesOption);\n  const [searchFilter, setSearchFilter] = useState('');"
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/src/commands/issues-list/index.ts",
                  "quote": "const { projectName, stage, issueStatus, limit } = globalStateManager.args;\n\n  const issues = await stacktapeTrpcApiManager.apiClient.listIssues({\n    project: projectName,\n    stage,\n    status: issueStatus as 'OPEN' | 'RESOLVED' | 'IGNORED' | undefined,\n    limit: limit ?? 25\n  });"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Stacktape Issues is a lightweight, built-in error inbox with no SDK integration, no separate billing, and no external data egress.”",
              "reason": "The sources support no third-party error tracking service and log-subscription-based capture, but they do not support “no separate billing” or “no external data egress.” The draft also says issue detection routes matching log events to a processing system, so the data-egress claim is too strong without a source defining where that system runs and how billing works.",
              "suggestedFix": "Narrow the comparison to sourced behavior: “Stacktape Issues is built in and does not require adding a Sentry or Bugsnag SDK. It detects errors from log output, so it provides less in-process context than an APM tool.”",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/observability/issues.md",
                  "quote": "Manage issues without integrating a third-party error tracking service."
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/observability/issues.md",
                  "quote": "The detection mechanism uses AWS CloudWatch Logs subscription filters, which route matching log events to the processing system."
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "“The table paginates at 20 items per page by default and sorts by last-seen timestamp descending.”",
              "reason": "The 20-items default is grounded by `useState(20)`, and the default sort column is grounded by `defaultSortColumnIndex={0}`. The provided source does not show the default sort direction of `ElevatedTable`, so “descending” is not grounded in the provided files.",
              "suggestedFix": "Write “The table paginates at 20 items per page by default and uses Last seen as the default sort column,” or omit the sort-direction claim unless `ElevatedTable` source is available.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx",
                  "quote": "const [perPageAmount, setPerPageAmount] = useState(20);"
                },
                {
                  "file": "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx",
                  "quote": "defaultSortColumnIndex={0}"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft uses the documented `stacktape debug:logs` command and links it to `/cli/debug-logs`.",
            "The `issues:list` examples and default limit of 25 are grounded in the CLI source.",
            "The Console filtering, pagination default, summary cards, permission-gated Configure Issues button, and stage option behavior are largely grounded in `IssuesPage.tsx`.",
            "The page avoids forbidden Stacktape configuration examples and does not introduce hand-authored YAML or plain-object `StacktapeConfig` examples."
          ]
        }
      ],
      "status": "passed",
      "passed": true
    }
  ],
  "agentModelAssignments": {
    "page-writer": "claude:cli-default",
    "first-time-user-reviewer": "claude:claude-sonnet-4-6",
    "production-engineer-reviewer": "claude:claude-sonnet-4-6",
    "ai-consumer-reviewer": "codex:cli-default",
    "factual-accuracy-verifier": "codex:cli-default",
    "source-grounding-verifier": "codex:cli-default",
    "api-completeness-auditor": "codex:cli-default",
    "code-block-validator": "local"
  },
  "finalOutputPath": "/Users/congy/Desktop/projects/stacktape/docs/docs/observability/issues.mdx",
  "outcome": "passed",
  "refinementAttempts": 1,
  "refinementStalled": false,
  "completedAt": "2026-05-21T17:32:28.254Z",
  "inputHashes": {
    "/Users/congy/Desktop/projects/console-app/src/pages/IssuesPage/IssuesPage.tsx": "3c6f4195bac1383cef7c5f90747094e9da99b28a",
    "/Users/congy/Desktop/projects/stacktape/src/commands/issues-list/index.ts": "ca6982c1ae0fba797e512d3b87b8db7c265d986a",
    "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/observability/issues.md": "1f35f683e502aa9917fbc4b4dccabcfc7576a17e"
  },
  "styleGuideHash": "9409625195f1f71d6e88b7be273033ddf350c4a4"
}
