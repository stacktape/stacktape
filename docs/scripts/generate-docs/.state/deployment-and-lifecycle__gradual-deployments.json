{
  "pageId": "deployment-and-lifecycle__gradual-deployments",
  "pageRoute": "deployment-and-lifecycle/gradual-deployments",
  "startedAt": "2026-05-17T14:36:44.924Z",
  "updatedAt": "2026-05-21T16:26:15.858Z",
  "iterations": [
    {
      "iteration": 1,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\deployment-and-lifecycle__gradual-deployments\\iteration-1.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "Clear 'When to use' / 'When NOT to use' pair gives first-time readers an immediate decision framework before any config detail.",
            "Lambda strategy table enumerates all nine variants from the source type, with brief per-row descriptions — exactly the coverage the union-type discipline rule demands.",
            "Lifecycle hook handler code block uses real AWS SDK calls with success/failure signaling, making it directly copyable rather than pseudocode.",
            "FAQ spans all three required question categories (Stacktape-specific, AWS service mechanics, choosing/comparison) and the answers are concise enough to serve as Featured Snippets.",
            "Warning callout about the ALB requirement for container gradual deployments is correctly placed before the container examples — prevents the most common configuration mistake.",
            "Cost considerations section honestly surfaces the ~$18/month ALB cost as the main incremental expense for container deployments, supporting the reader's make-vs-skip decision.",
            "Rollback behavior section keeps Lambda and container paths clearly separated rather than blending them, which matches the genuinely different underlying mechanisms."
          ],
          "problems": [
            "testListenerPort property appears in a code example and in detailed prose (default value '8080', separate ALB listener behavior) but ContainerWorkloadDeploymentConfig is not among the provided source files — this claim is entirely ungrounded and if the property doesn't exist, the code example will fail.",
            "Container strategy table shows 'Linear10PercentEvery1Minutes' (plural 's') while the source LambdaDeploymentConfig type and Lambda strategy table on the same page use 'Linear10PercentEvery1Minute' (singular) — a naming inconsistency that would cause a deployment error if copied.",
            "ContainerWorkloadDeploymentConfig hook properties (beforeAllowTrafficFunction, afterTrafficShiftFunction) are stated as supported for container workloads but only LambdaDeploymentConfig is present in the source files — the parallel claim for containers is ungrounded.",
            "No <ApiReference> component anywhere on the page. The page introduces and documents LambdaDeploymentConfig (a named typed config surface) but provides no API reference anchor, leaving readers who want to see all properties without a path forward.",
            "Specific timing claims in 'Deploy time impact' table (e.g., '~5-7 minutes', '~15-17 minutes') and rollback timing assertions ('under a second', '30-60 seconds') have no source grounding and present estimates as factual measurements."
          ],
          "mandatoryFixes": [
            "Verify testListenerPort against the actual ContainerWorkloadDeploymentConfig type source. If the property does not exist, remove it from the code example and from the prose paragraph explaining test listener behavior. If it does exist, keep it but name the type explicitly in a surrounding sentence so source-grounding verifiers can accept the claim.",
            "Fix the naming inconsistency in the container strategy table: 'Linear10PercentEvery1Minutes' must match the canonical string value in ContainerWorkloadDeploymentConfig. Given that the Lambda source type uses 'Linear10PercentEvery1Minute' (singular), confirm the correct ECS variant from source and use that exact string in both the table and any code examples. A wrong strategy name here causes a hard deployment failure when copied.",
            "Add exactly one <ApiReference definitionName=\"LambdaDeploymentConfig\" /> to the page (bottom placement fits the tutorial flow). This is the primary typed config surface the page documents and is required by the page rhythm rule."
          ],
          "optionalImprovements": [
            "Verify ContainerWorkloadDeploymentConfig beforeAllowTrafficFunction / afterTrafficShiftFunction from source. If confirmed, mention the type name explicitly in prose (e.g., 'ContainerWorkloadDeploymentConfig exposes the same hook properties') so downstream verifiers can accept the parallel claim.",
            "Replace specific ungrounded timing numbers in 'Deploy time impact' with qualified language: 'typically 5–10 minutes on top of base deploy time, plus container startup and health-check registration' conveys the same guidance without false precision.",
            "Soften rollback timing claims: 'near-instant (under a second)' and '30-60 seconds' are ungrounded. Replace with 'within seconds' for Lambda and 'within the ALB connection-drain window' for containers.",
            "The 'Writing lifecycle hook functions' H2 is slightly long; 'Lifecycle hook handlers' or 'Hook pattern' would fit the 1-5 word heading target from the style guide.",
            "The FAQ answer for 'Can I use gradual deployments for private services or worker services?' makes a definitive negative claim ('do not expose the deployment property directly') about resource types whose source files are not provided. Soften to 'Gradual deployments are documented for web services and multi-container workloads; verify against the PrivateService and WorkerService types if needed.'"
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 6,
            "practicalUsefulness": 8,
            "audienceFit": 8
          },
          "strengths": [
            "Lambda deployment strategy table is exhaustively accurate against the provided source — all 9 strategy names match the `LambdaDeploymentConfig` union exactly.",
            "Lifecycle hook handler code example is realistic and copyable: includes the correct `DeploymentId`/`LifecycleEventHookExecutionId` event shape and both Succeeded/Failed signal paths via `putLifecycleEventHookExecutionStatus`.",
            "The 'When to use / When NOT to use' split is opinionated and concrete — the NLB incompatibility call-out is a useful production gotcha that saves debugging time.",
            "Rollback behavior section clearly distinguishes Lambda (instant alias reweight) from container workloads (ALB listener rule switch + task drain) rather than treating them as equivalent.",
            "FAQ section mixes all three required categories: Stacktape-specific (custom canary percentage, AllAtOnce benefit), AWS-service mechanics (alarm-triggered rollback), and comparison (canary vs blue/green). 9 entries, all non-synthetic.",
            "Cost considerations section correctly identifies the ALB flat-fee uplift (~$18/month) as the primary cost change when switching from http-api-gateway — a real decision factor for teams evaluating gradual deployments."
          ],
          "problems": [
            "MISSING `<ApiReference>`**: The page has no `<ApiReference definitionName=\"...\" />` component. `LambdaDeploymentConfig` is a named type in `types/stacktape-config/functions.d.ts` and would be an appropriate anchor. Style guidance requires exactly one on every resource/reference page. Its absence leaves engineers without the drill-down into all deployment properties.",
            "Container strategy table names are unverifiable: `ContainerWorkloadDeploymentConfig` is referenced in `web-services.d.ts` (`deployment?: ContainerWorkloadDeploymentConfig`) but its definition is NOT in the provided source files. The page asserts 5 container strategies including `Linear10PercentEvery1Minutes` (with trailing 's') — but the Lambda source spells the analogous strategy `Linear10PercentEvery1Minute` (no 's'). The naming inconsistency may reflect a typo in the page, or it may be correct for the container type. Either way, it cannot be verified and risks misleading engineers into a config error.",
            "`testListenerPort` default value of 8080 and its semantics are not grounded in any provided source file. `ContainerWorkloadDeploymentConfig` is not in the inline source, so the page's claim that 8080 is the default and that 'CodeDeploy creates a separate ALB listener' is unverifiable from provided materials.",
            "MultiContainerWorkload gradual deployment support is asserted ('Container workloads (specifically web services and multi-container workloads) support five strategies') but `MultiContainerWorkloadProps` is not in the provided source files. Only `WebServiceProps` is visible and it exposes `deployment?`. This could be wrong.",
            "Deploy time estimates in the 'Deploy time impact' table (~5-7 min, ~15-17 min, ~30-32 min, ~100-102 min) are fabricated approximations with no source grounding. An engineer tuning CI pipelines may rely on these incorrectly."
          ],
          "mandatoryFixes": [
            "Add exactly one `<ApiReference definitionName=\"LambdaDeploymentConfig\" />` to the page (bottom or after the Lambda strategies section). If `ContainerWorkloadDeploymentConfig` is separately exported in the schema, add it as a second `<ApiReference>` only if the style permits two anchors for a concept page spanning two resource types — otherwise pick `LambdaDeploymentConfig` as primary and note the container config separately.",
            "Verify the container strategy enum value `Linear10PercentEvery1Minutes` (with 's') against the actual `ContainerWorkloadDeploymentConfig` type. The Lambda version is definitively `Linear10PercentEvery1Minute` (no 's'). If the container type uses the same spelling (no 's'), fix the table. If it genuinely differs, add an inline note explaining the discrepancy to prevent copy-paste errors."
          ],
          "optionalImprovements": [
            "Qualify the container strategy section with a note that `ContainerWorkloadDeploymentConfig` couldn't be confirmed in the page's provided source, and specifically mark `testListenerPort`'s default value as approximate or remove the parenthetical '(default: 8080)' if the default isn't source-grounded.",
            "Remove or soften the specific deploy time estimates in the 'Deploy time impact' table. Replace fabricated numbers ('~5-7 minutes') with a source-grounded statement about the strategy window (e.g., 'A Canary10Percent5Minutes strategy adds at least 5 minutes to deploy time; task startup and health checks add further overhead for container workloads') rather than fabricating total durations.",
            "The MultiContainerWorkload gradual deployment claim should either be confirmed against `MultiContainerWorkloadProps` source or scoped to 'web services' only until the broader support is verified.",
            "The 'How it works' comparison table's '5 strategies' cell for container workloads would be cleaner if it sourced the count from the actual type definition. If unverifiable, omit the count and say 'fewer than Lambda' or link to the API reference."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "scores": {
            "clarity": 6,
            "scannability": 7,
            "completeness": 5,
            "practicalUsefulness": 5,
            "audienceFit": 6
          },
          "strengths": [
            "Opening paragraph is answer-first, self-contained, and clearly names gradual deployments, canary/linear strategies, Lambda functions, and web services.",
            "The page uses TypeScript class-based Stacktape config inside `<CodeBlock intellisense tabs={[...]}/>` and avoids forbidden hand-authored YAML.",
            "The Lambda strategy table matches the `LambdaDeploymentConfig.strategy` union in the provided type source.",
            "The page gives useful decision guidance for canary vs linear vs AllAtOnce, and the examples are generally realistic and copyable."
          ],
          "problems": [
            "Several high-confidence technical claims are not grounded in the provided source files, including AWS CodeDeploy implementation details, Lambda aliases, ECS blue/green mechanics, second target groups, immediate old-task termination, manual stop behavior, and exact rollback timing.",
            "The container workload strategy table is not supported by the provided source. `web-services.d.ts` only says web service deployment supports canary, linear, or all-at-once and requires `application-load-balancer`; the `ContainerWorkloadDeploymentConfig` union is not present in the source list.",
            "The page claims gradual deployments apply to multi-container workloads, private services, and worker-service limitations, but the provided source only includes `LambdaFunctionProps` and `WebServiceProps`. Multi-container and worker/private service claims are unsupported here.",
            "The page blurs CLI and Console behavior in places. The Tip says rollback can be triggered from the Stacktape Console, but no `console-app` source is provided for Console flows or behavior.",
            "The FAQ includes unsupported Stacktape-specific claims, especially exact rollback timings, CloudWatch alarm behavior for deployment rollback, and custom CodeDeploy deployment configuration via CloudFormation overrides.",
            "The lifecycle hook handler section is useful but overstates behavior beyond the source. The source says hooks must signal success or failure to CodeDeploy and rollback if the before hook fails; it does not provide the event shape, SDK package, timeout behavior, or permissions model.",
            "The deploy-time impact table invents timing overheads that are not present in the provided source files. This is especially risky because the hard requirements prohibit invented defaults, constraints, and timing claims.",
            "The page lacks an `<ApiReference>` for the relevant documented type or a clear reason to omit reference coverage on this deployment page, making it weaker as a reference page for readers checking exact supported properties.",
            "Some section openings are not self-contained enough for extraction, for example `Rollback behavior differs between Lambda functions and container workloads:` and `Both beforeAllowTrafficFunction and afterTrafficShiftFunction receive...` rely on surrounding context more than ideal."
          ],
          "mandatoryFixes": [
            "Remove or narrow unsupported implementation details. Replace claims like Lambda aliases, ECS blue/green, second target groups, listener rule switching, and old-task termination timing with source-grounded wording: Lambda and web-service deployments shift traffic gradually, can roll back, and web-service deployment requires `loadBalancing.type: 'application-load-balancer'`.",
            "Replace the container strategy table with cautious wording unless the `ContainerWorkloadDeploymentConfig` source is added. Do not list exact container strategies or counts from the current source set.",
            "Remove unsupported claims about multi-container workloads, private services, worker services, CloudFormation overrides, Console rollback actions, alarm-triggered rollback specifics, and exact rollback durations unless the corresponding source files are provided.",
            "Delete the deploy-time impact table or rewrite it without invented timing numbers. Keep only source-supported tradeoff wording such as gradual deployments increase deployment duration because traffic is shifted over the selected canary or linear interval.",
            "Revise the lifecycle hook section to only state what the source supports: `beforeAllowTrafficFunction` runs before traffic shifting, `afterTrafficShiftFunction` runs after traffic has shifted, hooks must signal success or failure to CodeDeploy, and a failed before hook rolls back the deployment.",
            "Distinguish CLI behavior from Console behavior. Keep `stacktape rollback` only if CLI source for rollback is available; otherwise remove the CLI command claim too. Do not mention Console actions without `console-app` source.",
            "Add or justify a single relevant API reference section if this page is expected to expose deployment config details, using the exact generated definition name available from the source/schema."
          ],
          "optionalImprovements": [
            "Add a compact table that is fully source-grounded: Lambda strategies from `LambdaDeploymentConfig.strategy`, plus web-service requirement for `application-load-balancer` without unsupported exact container strategy values.",
            "Explain the `memory: 1024`, `timeout: 30`, `resources: { cpu, memory }`, and `testListenerPort: 8080` example values more carefully, or remove nonessential tunables from examples to reduce accidental implication that they are required.",
            "Make each H2 opening paragraph self-contained by naming `Stacktape gradual deployments` rather than starting with generic phrases like `Rollback behavior differs`.",
            "Trim AWS implementation details unless backed by source, and link readers to Lambda function and web service pages for underlying resource behavior.",
            "Keep FAQ answers useful but separate well-known AWS background from Stacktape-specific behavior; avoid precise Stacktape claims unless present in the provided source."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the two provided source files on the existence of Lambda deployment strategies and the WebService ALB requirement, but it overreaches in several Stacktape-specific areas that are not grounded in the provided sources. The highest-impact problems are unsupported container workload details, rollback mechanics, lifecycle-hook behavior, and invented deployment timing numbers.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Container workloads (specifically web services and multi-container workloads) support five strategies” and the container strategy table listing `Canary10Percent5Minutes`, `Canary10Percent15Minutes`, `Linear10PercentEvery1Minutes`, `Linear10PercentEvery3Minutes`, and `AllAtOnce`.",
              "reason": "The provided source only shows that `WebServiceProps.deployment` exists and is typed as `ContainerWorkloadDeploymentConfig`; it does not include the definition of `ContainerWorkloadDeploymentConfig`, does not enumerate the supported container strategies, and does not mention multi-container workloads. This is Stacktape-specific configuration behavior that would affect what readers configure.",
              "suggestedFix": "Narrow the section to what the provided source proves: web services support `deployment?: ContainerWorkloadDeploymentConfig` and require `loadBalancing` type `application-load-balancer`. Do not enumerate container strategies or claim multi-container support unless the container workload type source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "Requires `loadBalancing` type `application-load-balancer`."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“For container workloads, CodeDeploy runs a blue/green deployment. It spins up new tasks (green), registers them with a second target group on the ALB, then shifts listener traffic according to strategy. Once complete, the old tasks (blue) are terminated immediately.”",
              "reason": "The provided WebService source confirms only that gradual deployments exist and require an application load balancer. It does not document ECS blue/green mechanics, second target groups, listener traffic shifting, or immediate old-task termination. Those are Stacktape-specific implementation details in this context and should not be asserted from the available source.",
              "suggestedFix": "Replace with cautious wording such as: “For web services, gradual deployment is available only when `loadBalancing.type` is `application-load-balancer`.” Omit CodeDeploy/ECS internals unless their source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "#### Gradual traffic shifting for safe deployments (canary, linear, or all-at-once)."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "Requires `loadBalancing` type `application-load-balancer`."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“You can also manually trigger a rollback mid-deployment using the `stacktape rollback` command or from the Stacktape Console.”",
              "reason": "Neither provided source file documents CLI rollback behavior or any Stacktape Console rollback flow. The page instructions require distinguishing CLI behavior from Console behavior and grounding Console claims in console-app source, which is not provided.",
              "suggestedFix": "Remove the Console rollback claim and either remove the CLI rollback claim or keep only a link to the rollback page without describing mid-deployment behavior, unless CLI command source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "deployment?: LambdaDeploymentConfig;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“CodeDeploy also watches any CloudWatch alarms you configure on the function — if an alarm enters the ALARM state during traffic shifting, it triggers a rollback.”",
              "reason": "The Lambda source says alarms can be configured and that the deployment rolls back if issues arise, but it does not state that CodeDeploy watches configured CloudWatch alarms or that an ALARM state triggers rollback. That is a specific Stacktape integration behavior not present in the provided source.",
              "suggestedFix": "Say only that Lambda functions expose `alarms?: LambdaAlarm[]` and gradual deployment rolls traffic back automatically if issues arise. Do not describe alarm-triggered CodeDeploy rollback unless the implementation source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "#### Alarms for this function (merged with global alarms from the Stacktape Console)."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Instead of switching all traffic to the new version instantly, shift it gradually\n   * (canary or linear). If issues arise, traffic rolls back automatically."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The “Deploy time impact” table claims additional deploy times such as `~5-7 minutes`, `~15-17 minutes`, `~10-12 minutes`, `~30-32 minutes`, `~100-102 minutes`, and says container workloads add `2-3 minutes` for task registration and health checks.",
              "reason": "The provided sources enumerate Lambda strategy names that include the CodeDeploy interval, but they do not support the extra minutes, total added deploy-time estimates, or container task registration timing. These Stacktape-specific timing numbers could mislead readers planning deployment windows.",
              "suggestedFix": "Keep only the strategy intervals that are explicitly encoded in the strategy names, or phrase timing qualitatively: “gradual deployments extend deploy time by at least the strategy wait interval.” Remove the `2-3 minutes` container claim unless sourced.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "| 'Canary10Percent5Minutes'\n    | 'Canary10Percent10Minutes'\n    | 'Canary10Percent15Minutes'\n    | 'Canary10Percent30Minutes'\n    | 'Linear10PercentEvery1Minute'\n    | 'Linear10PercentEvery2Minutes'\n    | 'Linear10PercentEvery3Minutes'\n    | 'Linear10PercentEvery10Minutes'\n    | 'AllAtOnce';"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The `testListenerPort` (default: `8080`) creates a separate ALB listener that routes to the new (green) target group.”",
              "reason": "The provided WebService source does not include the `ContainerWorkloadDeploymentConfig` definition, does not mention `testListenerPort`, and does not document its default or ALB listener behavior. This is a Stacktape-specific property and default that must be grounded in the container workload source.",
              "suggestedFix": "Remove the `testListenerPort` example and explanation unless the source defining `ContainerWorkloadDeploymentConfig` is provided. If kept with source, state the exact property behavior from that type.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Lambda rollbacks are near-instant — the alias weight change propagates in under a second. Container workload rollbacks take 30-60 seconds…”",
              "reason": "The provided Lambda and WebService sources do not contain rollback latency numbers. These are concrete timing claims about Stacktape deployment behavior, not just general AWS background, and they would affect operational expectations.",
              "suggestedFix": "Remove the specific rollback timing numbers. Use cautious wording such as “Rollback happens at the traffic-routing layer” only if supported, or simply say Lambda gradual deployments roll traffic back automatically when issues arise.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "If issues arise, traffic rolls back automatically."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "#### Gradual traffic shifting for safe deployments (canary, linear, or all-at-once)."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The lifecycle hook section says hook functions “must call back to CodeDeploy” with `putLifecycleEventHookExecutionStatus`, and that timeout without signaling causes rollback.",
              "reason": "The Lambda source only states that hook functions must signal success or failure to CodeDeploy and that a failing before-hook rolls back. It does not prescribe the AWS SDK handler implementation or document timeout behavior. The code example may be generally correct AWS usage, but the timeout rollback claim is not grounded in the provided Stacktape sources.",
              "suggestedFix": "Keep the high-level sourced requirement that hook functions must signal success or failure to CodeDeploy. Remove or soften the timeout rollback sentence unless CodeDeploy hook behavior source is included.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Must signal success/failure to CodeDeploy. If it fails, the deployment rolls back."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Must signal success/failure to CodeDeploy."
                }
              ]
            }
          ],
          "positiveFindings": [
            "The page correctly uses TypeScript `defineConfig` examples inside `<CodeBlock intellisense>` and does not hand-author YAML Stacktape configuration.",
            "The Lambda deployment strategy list exactly matches the `LambdaDeploymentConfig.strategy` union in `functions.d.ts`.",
            "The draft correctly states that web service gradual deployments require `loadBalancing` type `application-load-balancer`, which is explicitly documented in `web-services.d.ts`."
          ]
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft contains several Stacktape-specific behavior claims that go beyond the provided source files. The strongest issues are unsupported container-workload scope, unsupported CodeDeploy/ECS implementation details, and unsupported rollback/timing claims. The Stacktape config examples follow the required CodeBlock TypeScript pattern and the documented ALB requirement for web-service gradual deployments is correctly represented.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Stacktape supports gradual deployments for both Lambda functions and container-based web services ...” and “Container workloads (specifically web services and multi-container workloads) support five strategies.”",
              "reason": "The provided sources document `deployment` on `LambdaFunctionProps` and `WebServiceProps`. They do not include a multi-container workload source or show that gradual deployments are available on multi-container workloads. The draft should not extend the feature scope beyond Lambda functions and web services based on the provided files.",
              "suggestedFix": "Narrow the page to Lambda functions and web services, or add source-backed multi-container workload evidence before documenting multi-container workloads.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "deployment?: LambdaDeploymentConfig;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Stacktape uses AWS CodeDeploy under the hood to orchestrate the traffic shift” and the table claims “CodeDeploy + Lambda aliases” and “CodeDeploy + ECS Blue/Green.”",
              "reason": "The Lambda source mentions CodeDeploy only in lifecycle hook comments. The web-service source says gradual traffic shifting requires an application load balancer, but it does not state that Stacktape uses CodeDeploy, Lambda aliases, or ECS blue/green deployments. These are Stacktape-specific implementation claims, not just general AWS background.",
              "suggestedFix": "State only the source-backed behavior: Stacktape supports canary, linear, and all-at-once traffic shifting for Lambda functions, and web-service gradual deployments require `loadBalancing.type: 'application-load-balancer'`. Avoid naming CodeDeploy/ECS blue-green/aliases unless additional source files support it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Must signal success/failure to CodeDeploy. If it fails, the deployment rolls back."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "Requires `loadBalancing` type `application-load-balancer`."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Auto-rollback on failure: Enabled by default” for Lambda functions, “Deployment failure triggers rollback” for container workloads, and “No manual intervention required in either case.”",
              "reason": "The Lambda source supports rollback when the before-traffic hook fails, and the high-level deployment comment says traffic rolls back automatically if issues arise. The provided web-service source does not describe rollback behavior for container deployments, task health failures, or no-manual-intervention behavior. The draft over-specifies rollback behavior beyond the provided source.",
              "suggestedFix": "For Lambda, keep the source-backed hook-failure rollback wording. For web services, say the `deployment` property enables gradual traffic shifting and requires an ALB; omit detailed rollback triggers unless backed by container deployment source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Must signal success/failure to CodeDeploy. If it fails, the deployment rolls back."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "Gradual traffic shifting for safe deployments (canary, linear, or all-at-once)."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Once complete, the old tasks (blue) are terminated immediately,” “green tasks are terminated,” and “old tasks terminate immediately after completion.”",
              "reason": "The provided web-service source does not describe ECS task lifecycle, blue/green task sets, target groups, draining, or immediate termination. This is a specific Stacktape/AWS implementation detail that could affect operational expectations.",
              "suggestedFix": "Remove immediate task-termination claims or replace them with a cautious statement that the source only documents traffic shifting for web services through an application load balancer.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "Gradual traffic shifting for safe deployments (canary, linear, or all-at-once)."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“even the fastest canary strategy adds 5 minutes,” the deploy-time impact table, and “For container workloads, add 2-3 minutes for new task registration and initial health checks before traffic shifting begins.”",
              "reason": "The Lambda strategy names support fixed strategy windows like 5, 10, 15, and 30 minutes, but the source does not support total overhead ranges such as `~5-7 minutes`, `~30-32 minutes`, or the extra 2-3 minute container registration estimate. These numbers look operationally precise without source backing.",
              "suggestedFix": "Keep only strategy durations that are encoded in the strategy names, and describe additional packaging, CloudFormation, task startup, and health-check time qualitatively unless another source provides concrete ranges.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "| 'Canary10Percent5Minutes'\n    | 'Canary10Percent10Minutes'\n    | 'Canary10Percent15Minutes'\n    | 'Canary10Percent30Minutes'\n    | 'Linear10PercentEvery1Minute'\n    | 'Linear10PercentEvery2Minutes'\n    | 'Linear10PercentEvery3Minutes'\n    | 'Linear10PercentEvery10Minutes'\n    | 'AllAtOnce';"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The `testListenerPort` (default: `8080`) creates a separate ALB listener that routes to the new (green) target group.”",
              "reason": "The provided source list does not define `ContainerWorkloadDeploymentConfig` or `testListenerPort`, so the property, default, and ALB listener behavior are not grounded here. Because this affects a copyable config example, it should be backed by the relevant container workload type source before publication.",
              "suggestedFix": "Remove `testListenerPort` and its default from the draft unless the `ContainerWorkloadDeploymentConfig` source is added and confirms the property and default. If retained with evidence, scope the wording to exactly what the type/source says.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“You can also manually trigger a rollback mid-deployment using the `stacktape rollback` command or from the Stacktape Console.”",
              "reason": "No CLI command source or Console source is included. The navigation index proves the `/cli/rollback` route exists, but the provided source files do not support mid-deployment rollback behavior or any Console rollback flow.",
              "suggestedFix": "Either remove the mid-deployment and Console claims, or verify them against `src/commands/rollback/index.ts` and relevant `console-app` source before including them.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "If issues arise, traffic rolls back automatically."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "Gradual traffic shifting for safe deployments (canary, linear, or all-at-once)."
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "The draft says logs can be monitored “from the new version in real time” with `stacktape debug:logs`.",
              "reason": "The canonical command name is correct, but no CLI source is provided to verify live tail semantics or version-specific log filtering. The provided Lambda JSDoc actually contains an outdated `stacktape logs` mention, while the project instructions require `stacktape debug:logs`; the draft should avoid adding extra behavior not sourced here.",
              "suggestedFix": "Say `stacktape debug:logs` can be used to view logs, and avoid promising new-version-specific real-time tailing unless the CLI source supports it.",
              "evidence": []
            }
          ],
          "positiveFindings": [
            "The draft uses `<CodeBlock intellisense>` with a visible `label: 'TypeScript'` tab for Stacktape configuration examples and does not include hand-authored YAML config.",
            "The draft correctly states that web-service gradual deployments require `loadBalancing.type: 'application-load-balancer'`, which matches `WebServiceProps` source.",
            "The Lambda strategy list matches the `LambdaDeploymentConfig.strategy` union in the provided Lambda source.",
            "The draft’s `beforeAllowTrafficFunction` and `afterTrafficShiftFunction` names match the Lambda deployment config source."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft covers the main Lambda deployment API well, and its gradual-deployment examples are generally demonstrative. The largest problems are unsupported container-workload details where the provided source only confirms that web services expose `deployment?: ContainerWorkloadDeploymentConfig` and require `application-load-balancer`, but does not provide the container deployment strategy union or hook properties.",
          "issues": [
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "Container workloads (specifically web services and multi-container workloads) support five strategies: `Canary10Percent5Minutes`, `Canary10Percent15Minutes`, `Linear10PercentEvery1Minutes`, `Linear10PercentEvery3Minutes`, and `AllAtOnce`.",
              "reason": "The provided source confirms that `WebServiceProps` has a `deployment?: ContainerWorkloadDeploymentConfig` property and that it requires `application-load-balancer`, but the definition of `ContainerWorkloadDeploymentConfig` is not included. The draft names exact strategy variants that are not grounded in the provided source files.",
              "suggestedFix": "Either add the source file that defines `ContainerWorkloadDeploymentConfig`, or narrow the wording to the grounded fact: web service gradual deployment is configured through `deployment` and requires `loadBalancing.type: 'application-load-balancer'`.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "Requires `loadBalancing` type `application-load-balancer`."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The container hook example sets `testListenerPort: 8080` and the prose says `testListenerPort` creates a separate ALB listener, with default `8080`.",
              "reason": "The provided web-service source does not show `testListenerPort` or a container deployment config shape. This may be correct in another source file, but it is not grounded in the provided source set.",
              "suggestedFix": "Add the source for `ContainerWorkloadDeploymentConfig` before documenting `testListenerPort`, or remove the property and the default claim from this page revision.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "Gradual deployments are available on multi-container workloads that have an application load balancer integration.",
              "reason": "The provided source files include `WebServiceProps` and `LambdaFunctionProps`, but no `MultiContainerWorkload` type or property showing gradual deployment support. Mentioning multi-container workload support is not grounded by the supplied files.",
              "suggestedFix": "Either include the multi-container workload type source or limit this page to Lambda functions and web services, which are supported by the provided files.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "interface WebServiceProps extends SimpleServiceContainer {"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "deployment?: LambdaDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "For container workloads, CodeDeploy runs a blue/green deployment, spins up green tasks, registers them with a second target group, shifts listener traffic, and terminates old tasks immediately.",
              "reason": "The provided source documents only the Stacktape configuration surface for web service deployment. It does not describe CodeDeploy/ECS blue-green mechanics, target groups, listener behavior, or task termination timing.",
              "suggestedFix": "Keep the grounded configuration guidance and either cite the runtime source that creates CodeDeploy/ECS resources or phrase this as high-level background without specific mechanics and timing.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "#### Gradual traffic shifting for safe deployments (canary, linear, or all-at-once)."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "Requires `loadBalancing` type `application-load-balancer`."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The page says Lambda auto-rollback is enabled by default, CodeDeploy watches configured CloudWatch alarms, and manual stop reverts traffic immediately.",
              "reason": "The Lambda source supports gradual traffic shifting and says traffic rolls back automatically if issues arise, but it does not document default rollback configuration, CloudWatch alarm integration during traffic shifting, or manual stop behavior.",
              "suggestedFix": "Narrow the rollback section to the sourced behavior: gradual deployment shifts traffic and rolls back automatically if issues arise; hook failure rolls back. Add runtime/CLI source before documenting alarm-triggered rollback or manual stop behavior.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Instead of switching all traffic to the new version instantly, shift it gradually\n   * (canary or linear). If issues arise, traffic rolls back automatically."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Must signal success/failure to CodeDeploy. If it fails, the deployment rolls back."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The page states concrete deploy-time overheads such as `~5-7 minutes`, `~15-17 minutes`, and container startup overhead of `2-3 minutes`.",
              "reason": "The provided sources contain strategy names and some wait intervals in the strategy identifiers, but they do not support the extra overhead estimates or container startup timing claims.",
              "suggestedFix": "Replace the table with sourced duration wording based only on the strategy names, such as 5-minute canary window or 10% every 3 minutes, and avoid extra overhead estimates unless a source file provides them.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "| 'Canary10Percent5Minutes'\n    | 'Canary10Percent10Minutes'\n    | 'Canary10Percent15Minutes'\n    | 'Canary10Percent30Minutes'\n    | 'Linear10PercentEvery1Minute'\n    | 'Linear10PercentEvery2Minutes'\n    | 'Linear10PercentEvery3Minutes'\n    | 'Linear10PercentEvery10Minutes'\n    | 'AllAtOnce';"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The Lambda examples use `StacktapeLambdaBuildpackPackaging`, but the page does not mention the other documented Lambda packaging variant, `custom-artifact`.",
              "reason": "The page documents Lambda functions and shows packaging in every Lambda example. Because `packaging` is a union documented in the source, the page should at least name both supported variants so readers do not infer that buildpack packaging is the only deployment-compatible option.",
              "suggestedFix": "Add one sentence near the first Lambda example: Lambda gradual deployments work with the function packaging you choose; Stacktape documents `stacktape-lambda-buildpack` and `custom-artifact`, with buildpack recommended for most teams.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "- **`stacktape-lambda-buildpack`** (recommended): Point to your source file and Stacktape builds,\n   *   bundles, and uploads it automatically.\n   * - **`custom-artifact`**: Provide a pre-built zip file. Stacktape handles the upload."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The web service examples use `StacktapeImageBuildpackPackaging`, but the page does not acknowledge other container packaging variants or link to the packaging overview.",
              "reason": "The page documents container-based gradual deployments and includes web service packaging in examples. The provided web-service source inherits `SimpleServiceContainer`, but the supplied files do not define the container packaging union. Given the page uses a concrete packaging class, it should avoid implying that buildpack packaging is required for gradual deployments.",
              "suggestedFix": "Add a brief caveat that the example uses Stacktape container buildpack packaging for brevity, and link to `/packaging/overview` or the container packaging pages for other packaging modes. If full union coverage is required, include the source file defining `SimpleServiceContainer` and container packaging.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "interface WebServiceProps extends SimpleServiceContainer {"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The Lambda strategy table covers every variant listed in `LambdaDeploymentConfig.strategy`.",
            "The Lambda lifecycle hook section documents both `beforeAllowTrafficFunction` and `afterTrafficShiftFunction`, matching the Lambda deployment config source.",
            "The web service examples demonstrate the relevant `deployment` property and set `loadBalancing.type: 'application-load-balancer'`, which matches the documented requirement.",
            "The draft keeps the top-level web service `useFirewall` path separate from CDN firewall behavior and does not flatten that nested-property constraint."
          ]
        }
      ],
      "status": "failed",
      "passed": false
    },
    {
      "iteration": 2,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\deployment-and-lifecycle__gradual-deployments\\iteration-2.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 7,
            "audienceFit": 7
          },
          "strengths": [
            "Strategy reference tables are well-structured and scannable — the side-by-side behavior descriptions give readers everything they need to pick a strategy without reading prose.",
            "The `<Warning>` callout for the ALB requirement is prominent and well-placed, preventing the most common configuration mistake before readers hit the code example.",
            "The naming-difference `<Info>` callout (Linear with/without trailing 's') is a high-signal practical tip that prevents silent copy-paste failures.",
            "The FAQ section is comprehensive and correctly spans all three required categories: Stacktape-specific questions, underlying AWS service questions, and choosing/comparison questions.",
            "The 'When to use' / 'When NOT to use' split into two peer H2s with concrete, non-circular scenarios — good opinionated guidance.",
            "The rollback section correctly distinguishes in-progress traffic-routing rollback from the `stacktape rollback` CLI command (CloudFormation-level), which is a genuinely confusing distinction worth separating.",
            "The 'Choosing a strategy' section closes with an explicit recommendation ('For most teams, start with Canary10Percent5Minutes'), which is exactly what a first-time reader needs.",
            "The lifecycle hook handler code example is realistic, complete, and shows the full CodeDeploy signal pattern — useful once a reader is ready for that depth."
          ],
          "problems": [
            "Container strategy strings (`Canary10Percent15Minutes`, `Linear10PercentEvery1Minutes`, `Linear10PercentEvery3Minutes`) and the `testListenerPort` property are not verifiable from the provided source files — `ContainerWorkloadDeploymentConfig` is referenced in `web-services.d.ts` but its definition is not among the provided sources. If these strings are wrong, users will get silent deployment failures.",
            "ALB pricing (~$18/month) and the ~500k requests/day cost-breakeven figure are not sourced from any provided source file. The documentation rules explicitly prohibit inventing or estimating AWS prices not present in the source.",
            "The 'Writing lifecycle hook functions' section drops into `@aws-sdk/client-codedeploy` usage with no explanation of what CodeDeploy is or why it's involved. For a first-time user, this reads as unexplained AWS internals — the term 'CodeDeploy' appears multiple times in the first two sections but is never defined.",
            "Rollback section restates the auto-rollback behavior already explained in the 'When NOT to use' and strategy sections without meaningfully extending it. The only new content is the `stacktape rollback` CLI distinction, which is valuable but buried in repetition.",
            "Duplicate `### Strategy reference` H3 heading appears in both the Lambda and Container subsections. In a page-level TOC, these collide and a reader scanning the TOC cannot distinguish them.",
            "The `FeatureComparisonTable` 'Container options' row shows '2 (1/3 min)' for Linear, but this is internally inconsistent: the container strategy table above the component shows both `Linear10PercentEvery1Minutes` and `Linear10PercentEvery3Minutes`, while the Lambda table shows four linear options. The comparison table notation doesn't match the counts in the preceding strategy tables."
          ],
          "mandatoryFixes": [
            "Source-ground or remove the ALB pricing claim. The ~$18/month figure and ~500k requests/day breakeven are not present in any provided source file. Replace with 'check current AWS pricing for Application Load Balancers' or link to the ALB page where pricing is documented. The documentation rules prohibit inventing AWS prices not present in source.",
            "Verify every container strategy string and the `testListenerPort` property name against the `ContainerWorkloadDeploymentConfig` type definition before publishing. These are claimed but unverifiable from the provided source files — a wrong string causes a silent deploy misconfiguration for users."
          ],
          "optionalImprovements": [
            "Add a one-sentence definition of AWS CodeDeploy the first time it's mentioned (e.g., 'Stacktape uses AWS CodeDeploy under the hood to manage the traffic alias weights and lifecycle hooks'). This bridges the gap for the low-AWS-experience target reader without requiring them to leave the page.",
            "Disambiguate the duplicate `### Strategy reference` H3 headings — rename them to `### Lambda strategy reference` and `### Container strategy reference` so the page TOC is navigable.",
            "Add an explicit callout or note at the top of 'Writing lifecycle hook functions' that this section is optional for most deployments — users can enable gradual deployments without hooks and come back to this section when needed. The section's depth may discourage first-time readers from enabling the feature at all.",
            "Move the first code example (Basic Lambda example) higher up the page — ideally right after the opening paragraph or after the 'When to use' section. A first-time reader following the 'what is this → I have it running' path currently has to read four H2 sections before reaching working code.",
            "Fix the `FeatureComparisonTable` 'Container options' row for Linear: the count '2 (1/3 min)' doesn't match how intervals are described — make the notation consistent with the strategy tables above it."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "Strategy reference tables for both Lambda and container workloads are well-structured, and the naming-difference callout (`Linear10PercentEvery1Minute` vs `Linear10PercentEvery1Minutes`) is exactly the kind of gotcha that saves a production debug session.",
            "The lifecycle hook handler code block is realistic and directly copyable — includes the required CodeDeploy status signaling pattern that engineers need but often have to hunt for.",
            "The 'When NOT to use' section is concrete and actionable: it names specific resource types (worker services, private services) and explains why traffic shifting doesn't apply to them.",
            "Cost section correctly ties the ALB requirement to a real cost change (~$18/month flat vs. pay-per-request) and gives a breakeven signal (~500k requests/day), which is grounded in the web-services.d.ts source comments.",
            "FAQ mixes all three required categories — Stacktape-specific, AWS-specific, and comparison — and the answers are appropriately 2–4 sentences each.",
            "The `<Warning>` callout before the container strategy table is well-placed: it appears exactly where an engineer would reach for the feature without the ALB prerequisite."
          ],
          "problems": [
            "Container deployment strategies (5 variants with their exact string values including `Linear10PercentEvery1Minutes` plural) are presented as verified facts, but `ContainerWorkloadDeploymentConfig` is not in the provided source files. If the strategy name spellings are wrong, engineers will get silent deployment failures.",
            "The `testListenerPort` property described in the container hooks section is not present in `web-services.d.ts` or any other provided source file. The property may not exist, or its name may differ from what is documented.",
            "The 'Rollback behavior' section opens with 'Rollback behavior for Lambda functions is described in the `LambdaDeploymentConfig` source' — this is internal pipeline meta-commentary that leaked into user-facing docs. No end user should read about the 'source'.",
            "The `/cli/rollback` link target is suspicious. The git status shows the CLI file is `cf-rollback.mdx`, not `rollback.mdx`. If the command is actually `stacktape cf:rollback`, engineers clicking the link will hit a 404 and the command usage will be wrong.",
            "FAQ answer 'if an alarm fires during the traffic shift, it can trigger a rollback for Lambda functions' implies this is configurable, but `LambdaDeploymentConfig` has no `alarms` property and no configuration path is explained. AWS CodeDeploy does support alarm-based rollback but only via CloudFormation overrides — presenting this as a first-class feature without a configuration path is misleading.",
            "`<ApiReference definitionName=\"LambdaDeploymentConfig\" />` covers only the Lambda deployment config interface. The page equally documents container gradual deployments, but `ContainerWorkloadDeploymentConfig` properties are never surfaced through an API reference."
          ],
          "mandatoryFixes": [
            "Verify the exact strategy string values for `ContainerWorkloadDeploymentConfig` from the actual type definition (likely in `multi-container-workloads.d.ts` or a shared deployment types file). Do not publish the container strategy table — especially the `Linear10PercentEvery1Minutes` plural spelling — without confirming against the type definition. A wrong strategy string causes a silent deployment misconfiguration.",
            "Verify that `testListenerPort` exists in the container deployment config type before documenting it. If it does not exist in `ContainerWorkloadDeploymentConfig`, remove the `testListenerPort` sub-section entirely.",
            "Remove the sentence 'Rollback behavior for Lambda functions is described in the `LambdaDeploymentConfig` source' from the Rollback behavior section. Replace with the actual behavior description (e.g. 'When a deployment fails — whether because of hook failure or an internal CodeDeploy error — the alias weight is restored to the previous version automatically.').",
            "Verify the rollback CLI command name. If the file is `cf-rollback.mdx`, the command is likely `stacktape cf:rollback`, and the link should be `/cli/cf-rollback`. Fix both the link and the command name in the Rollback behavior section to avoid engineers invoking the wrong command.",
            "Either remove the alarm-based rollback claim from the FAQ or qualify it precisely: explain that alarm-triggered rollback requires a CloudFormation override on the CodeDeploy deployment group (not a first-class `LambdaDeploymentConfig` property), and link to the overrides documentation. As written, engineers will look for an `alarms` field in `deployment` that does not exist."
          ],
          "optionalImprovements": [
            "The `<ApiReference definitionName=\"LambdaDeploymentConfig\" />` is placed between major sections mid-page, which is fine — but consider whether adding a note beneath it like 'For container deployment config, see ContainerWorkloadDeploymentConfig via the web-service or multi-container-workload API reference' would help engineers who navigate here specifically for the container API.",
            "The 'Deploy time impact' section duplicates the per-strategy timing numbers already in the strategy tables earlier on the page. Consider keeping only the second table (with Lambda vs container interval columns) and removing the earlier duplication — or merge the two tables.",
            "In the Lambda lifecycle hooks section, the note 'Set a generous `timeout` on the hook function to give your validation logic enough time to complete' would benefit from a concrete number: hook functions that call external dependencies often need 60–120 seconds. The example already sets `timeout: 60` but the prose doesn't reinforce why.",
            "The 'Container lifecycle hooks and test listener' example uses `resources: { cpu: 0.5, memory: 1024 }` without a prose sentence explaining what `cpu` and `memory` control for containers. Per documentation-quality discipline, non-obvious property values in examples should have a one-sentence anchor.",
            "The FAQ entry 'Why are the strategy names different between Lambda and container workloads?' is accurate but vague on the 'different AWS CodeDeploy deployment group types' explanation. Consider naming these as 'Lambda' vs 'ECS' CodeDeploy application types, which is the actual reason for the naming difference — it's the kind of concrete detail engineers remember."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "scores": {
            "clarity": 6,
            "scannability": 8,
            "completeness": 5,
            "practicalUsefulness": 5,
            "audienceFit": 6
          },
          "strengths": [
            "The page opens with a clear, self-contained answer-first paragraph and keeps most H2 headings short and scannable.",
            "The Lambda strategy table is well structured and matches the strategy union in the provided LambdaDeploymentConfig source.",
            "Stacktape configuration examples use the required class-based defineConfig style inside <CodeBlock intellisense tabs={[...]} /> with the correct TypeScript tab label.",
            "The page includes useful decision support for canary vs linear vs AllAtOnce and has a broad FAQ that covers Stacktape behavior, AWS deployment concepts, and comparison questions."
          ],
          "problems": [
            "Several container-workload claims are not grounded in the provided source files. The draft lists container deployment strategy values, testListenerPort behavior, lifecycle hooks for containers, multi-container workload support, double-capacity behavior, and container rollback details, but the source only shows WebService.deployment?: ContainerWorkloadDeploymentConfig and the ALB requirement; it does not include the ContainerWorkloadDeploymentConfig definition.",
            "The page makes unsupported or over-specific operational claims such as Lambda weighted aliases, rollback propagation within seconds, ALB rollback within a minute, containers being removed after failure, alarms triggering Lambda rollback, and CloudFormation-level rollback semantics for stacktape rollback. These are plausible AWS/Stacktape facts, but they are not in the supplied source files.",
            "Cost claims are partly grounded by WebService loadBalancing JSDoc, but the draft goes beyond the source with pennies/few-dollars examples, Lambda no-overhead claims, double-capacity billing, and ALB/request-volume comparisons repeated as precise guidance. The provided source supports ~$18/month + usage, ~$1/million requests, Free Tier eligibility, and ~500k requests/day only for web service load balancing.",
            "The lifecycle hook application code may be useful, but the exact AWS SDK handler shape, event field names, timeout failure behavior, and required callback status names are not present in the supplied Stacktape source files. The source only says hook functions must signal success/failure to CodeDeploy.",
            "The single <ApiReference definitionName=\"LambdaDeploymentConfig\" /> only covers the Lambda deployment shape while the page spends substantial space on container deployments. Because the container deployment type source is absent, the API reference placement makes the page feel more authoritative than the available source supports.",
            "Some sections are too confident where the source is thin. For example, claims about private services, worker services, HTTP API Gateway routing to a single target, and multi-container workload ALB integration are not grounded in the provided files."
          ],
          "mandatoryFixes": [
            "Remove or narrow every container deployment detail that is not supported by the supplied source. Keep only the grounded facts: WebService has a deployment property for gradual traffic shifting and it requires loadBalancing.type: 'application-load-balancer'.",
            "Replace unsupported container strategy tables and examples with cautious wording unless the ContainerWorkloadDeploymentConfig source is added. Do not name specific container strategy strings, hooks, or testListenerPort unless those properties appear in the provided source.",
            "Remove or source the unsupported operational timing claims: rollback within seconds, ALB rollback within a minute, containers being removed, timeout-without-signaling behavior, and stacktape rollback CloudFormation semantics.",
            "Tighten the cost section to only the source-backed pricing context: http-api-gateway is pay-per-request at approximately ~$1/million requests; application-load-balancer is approximately ~$18/month plus usage, Free Tier eligible, and more cost-effective above ~500k requests/day. Remove invented pennies/few-dollars deployment examples.",
            "Revise the lifecycle hook section to state only what LambdaDeploymentConfig source supports: beforeAllowTrafficFunction runs before traffic shifting, afterTrafficShiftFunction runs after traffic has shifted, both must signal success/failure to CodeDeploy, and failure rolls back. Mark the AWS SDK handler as an example pattern only if separately sourced.",
            "Fix unsupported FAQ answers by removing or narrowing claims about alarms triggering rollback, private/worker service deployment properties, custom strategy escape hatches, exact rollback speed, and container cleanup behavior unless matching source files are provided."
          ],
          "optionalImprovements": [
            "Add a short source-grounded table separating what is confirmed for Lambda functions from what is confirmed for web services. This would prevent readers from assuming Lambda-only hook details apply identically to containers.",
            "Move the API reference near the Lambda-specific section or add prose explaining that the visible API reference is for LambdaDeploymentConfig only. If container deployment type source becomes available, consider documenting that type separately or linking to its own reference.",
            "Reduce repeated assertions that rollback is automatic and instead explain the exact source-backed trigger: Lambda deployment failure or hook failure rolls back traffic; web service deployment requires ALB for gradual shifting.",
            "Add one sentence near the first container example explaining that application-load-balancer is one of three web service loadBalancing variants: http-api-gateway, application-load-balancer, and network-load-balancer. The source explicitly lists all three and this helps union-type coverage.",
            "Shorten the seoDescription because it currently includes unsupported and broad claims about zero-downtime deployments and AWS. A safer version should mention Lambda functions and web services with canary or linear rollout strategies."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft correctly reflects the Lambda deployment strategy union and the web-service ALB requirement from the provided type files, but it makes several Stacktape-specific claims about container workloads, rollback triggers, lifecycle hooks, Console alarms, and hook handler behavior that are not grounded in the provided sources.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Stacktape supports gradual deployments for Lambda functions and web services (and multi-container workloads) that use an application load balancer.”",
              "reason": "The provided sources support Lambda function deployment and web-service deployment, but they do not include a MultiContainerWorkload type or any statement that multi-container workloads support gradual deployments. This would change what resource types a reader tries to configure.",
              "suggestedFix": "Narrow the opening claim to Lambda functions and web services, or add the missing multi-container workload source before documenting that resource type.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "deployment?: LambdaDeploymentConfig;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft lists exact container workload strategy values: `Canary10Percent5Minutes`, `Canary10Percent15Minutes`, `Linear10PercentEvery1Minutes`, `Linear10PercentEvery3Minutes`, and `AllAtOnce`, and says containers support five strategies.",
              "reason": "The provided web-service source only exposes `deployment?: ContainerWorkloadDeploymentConfig` and says it supports canary, linear, or all-at-once. It does not define the exact strategy strings, count, or the singular/plural naming caveat for container workloads.",
              "suggestedFix": "Remove the exact container strategy table and naming warning unless the `ContainerWorkloadDeploymentConfig` source is included. With the current sources, say only that web services support canary, linear, or all-at-once gradual traffic shifting through the `deployment` property and require ALB load balancing.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "#### Gradual traffic shifting for safe deployments (canary, linear, or all-at-once)."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Container workloads support the same `beforeAllowTrafficFunction` and `afterTrafficShiftFunction` hooks as Lambda. Additionally, the `testListenerPort` property creates a separate ALB listener...”",
              "reason": "The provided sources define `beforeAllowTrafficFunction` and `afterTrafficShiftFunction` only on `LambdaDeploymentConfig`. The web-service source does not expose or describe these container deployment hook fields or `testListenerPort`. The example config may therefore document unsupported properties from the provided source set.",
              "suggestedFix": "Remove the container lifecycle hook and `testListenerPort` section, or include the source defining `ContainerWorkloadDeploymentConfig` and revise against it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "beforeAllowTrafficFunction?: string;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "afterTrafficShiftFunction?: string;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“If the deployment fails during the canary window, all traffic reverts to the previous version automatically” and similar claims that Lambda deployment failure or canary issues automatically roll back.",
              "reason": "The Lambda source says traffic rolls back automatically “if issues arise,” and says a before hook failure rolls back. It does not define what counts as “deployment fails during the canary window,” nor does it mention alarms or failure detection semantics. The draft repeatedly broadens this into automatic rollback on any deployment failure.",
              "suggestedFix": "Use the source wording narrowly: gradual deployment shifts traffic gradually, and if issues arise traffic rolls back automatically; `beforeAllowTrafficFunction` can roll back when it fails. Avoid defining additional failure conditions unless sourced from runtime/CodeDeploy implementation files.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Instead of switching all traffic to the new version instantly, shift it gradually\n   * (canary or linear). If issues arise, traffic rolls back automatically."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Must signal success/failure to CodeDeploy. If it fails, the deployment rolls back."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“If an alarm fires during the traffic shift, it can trigger a rollback for Lambda functions.”",
              "reason": "The provided Lambda source has an `alarms` property, but it only says alarms are merged with global alarms from the Stacktape Console. It does not say alarms are attached to CodeDeploy deployment rollback behavior or can trigger Lambda gradual deployment rollback.",
              "suggestedFix": "Remove the alarm-triggered rollback claim, or replace it with a sourced monitoring statement such as configuring alarms on Lambda functions separately from the deployment strategy.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "#### Alarms for this function (merged with global alarms from the Stacktape Console)."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "alarms?: LambdaAlarm[];"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The lifecycle hook handler section says both hooks receive a CodeDeploy lifecycle event, must call `putLifecycleEventHookExecutionStatus`, and timeout without signaling is treated as failure.",
              "reason": "The Lambda source says hook functions must signal success/failure to CodeDeploy, but it does not specify the event shape, AWS SDK call, required status values, or timeout behavior. Those details may be true AWS CodeDeploy mechanics, but the draft presents them as the expected Stacktape handler pattern without source backing.",
              "suggestedFix": "Keep the sourced requirement that hook functions must signal success or failure to CodeDeploy. Present the handler as an AWS CodeDeploy example rather than a Stacktape-defined contract, and avoid unsourced timeout behavior unless implementation or AWS docs are included.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Must signal success/failure to CodeDeploy. If it fails, the deployment rolls back."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Must signal success/failure to CodeDeploy."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Private services and worker services do not expose the `deployment` property.”",
              "reason": "The provided sources include only Lambda and web-service type files. They do not include private-service or worker-service definitions, so this negative property claim is not grounded in the provided source list.",
              "suggestedFix": "Remove the explicit negative claim, or include the private-service and worker-service type sources before documenting their lack of `deployment`. With current sources, say the provided `WebServiceProps` source exposes `deployment` for web services.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "interface WebServiceProps extends SimpleServiceContainer {"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft says `AllAtOnce` “still runs lifecycle hooks” and that a normal deployment without `deployment` has “no hook opportunity.”",
              "reason": "The source defines `AllAtOnce` as an instant switch with no gradual rollout and separately defines optional lifecycle hook properties. It does not explicitly state that hooks run with `AllAtOnce`, nor does it define behavior for a normal deployment without the `deployment` property.",
              "suggestedFix": "Narrow the wording: `AllAtOnce` is a valid strategy value that switches instantly, and `beforeAllowTrafficFunction` / `afterTrafficShiftFunction` can be configured on `LambdaDeploymentConfig`. Avoid contrasting with normal deployments unless sourced.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "- **AllAtOnce**: Instant switch (no gradual rollout)."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "beforeAllowTrafficFunction?: string;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "afterTrafficShiftFunction?: string;"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The Lambda strategy table matches the exact `LambdaDeploymentConfig.strategy` union in `functions.d.ts`.",
            "The draft correctly states that web-service gradual deployments require `loadBalancing` type `application-load-balancer`.",
            "The draft avoids hand-authored Stacktape YAML and uses `<CodeBlock intellisense>` with a `TypeScript` tab for Stacktape config examples.",
            "The documented web-service load balancing tradeoff between `http-api-gateway`, `application-load-balancer`, and `network-load-balancer` is broadly supported by the `WebServiceProps.loadBalancing` JSDoc."
          ]
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the provided Lambda and web-service type sources for the existence of Lambda deployment strategies, web-service ALB requirement, and Lambda lifecycle hooks. The main issues are unsupported container-specific details: the provided sources expose `ContainerWorkloadDeploymentConfig` as a referenced type but do not include its definition, so strategy names, hook behavior, test listener behavior, and container rollback details cannot be verified from the provided files.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "Container workload strategy table lists `Canary10Percent5Minutes`, `Canary10Percent15Minutes`, `Linear10PercentEvery1Minutes`, `Linear10PercentEvery3Minutes`, and `AllAtOnce`, and says web services and multi-container workloads support these five strategies.",
              "reason": "The provided web-service source only says the `deployment` property uses `ContainerWorkloadDeploymentConfig`; the actual `ContainerWorkloadDeploymentConfig` definition and its allowed strategy strings are not included in the provided sources. The draft presents exact enum values and counts as authoritative, but those values are not grounded in the supplied files.",
              "suggestedFix": "Either add/verify against the source file that defines `ContainerWorkloadDeploymentConfig`, or narrow the wording to only what the provided source supports: web-service gradual deployments exist and require `loadBalancing` type `application-load-balancer`. Do not list exact container strategy values without the defining source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "`testListenerPort` creates a separate ALB listener that routes to the new version's target group, and is only needed when you use `beforeAllowTrafficFunction` and custom listeners.",
              "reason": "The provided sources do not define `ContainerWorkloadDeploymentConfig` or `testListenerPort`. The draft gives specific behavior and usage guidance for a property that is not present in the supplied source files.",
              "suggestedFix": "Remove the `testListenerPort` section unless the source defining this property is included. If verified elsewhere, cite the exact property behavior and avoid claiming when it is \"only needed\" unless the source states that constraint.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "Container workloads support the same `beforeAllowTrafficFunction` and `afterTrafficShiftFunction` hooks as Lambda.",
              "reason": "The Lambda source defines these hook properties for `LambdaDeploymentConfig`, but the provided sources do not define the container deployment config type. The draft transfers Lambda hook behavior to container workloads without a supplied source.",
              "suggestedFix": "Keep the Lambda hook explanation. For containers, say only that web-service deployment uses `ContainerWorkloadDeploymentConfig` until the source defining that type is available, or add the defining source and verify the hook names there.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "beforeAllowTrafficFunction?: string;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "afterTrafficShiftFunction?: string;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "For container workloads, if the `beforeAllowTrafficFunction` signals failure or new containers fail to become healthy, the deployment stops and traffic stays on the original version.",
              "reason": "The provided sources confirm only that web-service deployment requires an application load balancer. They do not describe container CodeDeploy lifecycle behavior, health-failure rollback behavior, or whether old traffic routing is preserved in those cases.",
              "suggestedFix": "Narrow the rollback section to the Lambda behavior that is documented in `LambdaDeploymentConfig`, and mention only the ALB requirement for web-service gradual deployments unless container deployment behavior is verified from its defining source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Must signal success/failure to CodeDeploy. If it fails, the deployment rolls back."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "Requires `loadBalancing` type `application-load-balancer`."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "Private services and worker services do not expose the `deployment` property.",
              "reason": "The provided sources include only Lambda and web-service types. There is no private-service or worker-service source in the supplied material, so this negative resource-shape claim is not grounded here.",
              "suggestedFix": "Remove the explicit negative claim or include and verify the private-service and worker-service type definitions. A cautious alternative is: \"The provided web-service source documents gradual deployments for web services with application load balancers; use the API reference for other workload types.\"",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "interface WebServiceProps extends SimpleServiceContainer {"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "If an alarm fires during the traffic shift, it can trigger a rollback for Lambda functions.",
              "reason": "The Lambda source says the deployment rolls back if issues arise and that hooks can fail, but it does not state that Stacktape alarms are wired into CodeDeploy rollback for Lambda gradual deployments. The only alarm-related source line says alarms are configured for the function and merged with global alarms.",
              "suggestedFix": "Remove the alarm-triggered rollback claim unless a source explicitly connects Lambda alarms to CodeDeploy rollback. Keep monitoring guidance separate: users can watch logs and metrics during the rollout.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Alarms for this function (merged with global alarms from the Stacktape Console)."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\functions.d.ts",
                  "quote": "Instead of switching all traffic to the new version instantly, shift it gradually\n   * (canary or linear). If issues arise, traffic rolls back automatically."
                }
              ]
            }
          ],
          "positiveFindings": [
            "The Lambda strategy list exactly matches the `LambdaDeploymentConfig.strategy` union in `functions.d.ts`.",
            "The draft correctly states that Lambda `beforeAllowTrafficFunction` and `afterTrafficShiftFunction` are string references to Lambda functions and must signal success or failure to CodeDeploy.",
            "The draft correctly scopes the web-service gradual-deployment requirement to `loadBalancing` type `application-load-balancer`, matching `web-services.d.ts`.",
            "All Stacktape configuration examples use `<CodeBlock intellisense>` with a `TypeScript` tab and `defineConfig`; no hand-authored YAML Stacktape config was found.",
            "All markdown links in the draft point to routes present in the provided navigation index."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is mostly complete for the gradual-deployment API surface visible in the provided source: Lambda deployment strategies are fully enumerated, lifecycle hook properties are covered, web-service load-balancing variants are acknowledged, and the feature examples demonstrate the properties their sections describe. I found one medium API-completeness issue around container deployment API reference coverage.",
          "issues": [
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The page includes `<ApiReference definitionName=\"LambdaDeploymentConfig\" />` but no comparable API-reference coverage for the container deployment configuration used by web services.",
              "reason": "The draft substantially documents container workload gradual deployments, including `deployment.strategy`, lifecycle hooks, and `testListenerPort`, but the only API reference block is for `LambdaDeploymentConfig`. The provided web-service type exposes `deployment?: ContainerWorkloadDeploymentConfig`, so readers configuring web-service gradual deployments are not given the same exhaustive reference path as Lambda readers.",
              "suggestedFix": "Add a second API reference for the container deployment config if the docs system supports that definition, or add a short sentence near the API reference that the web-service `deployment` property is `ContainerWorkloadDeploymentConfig` and is available through the WebService API reference. If only one `<ApiReference />` is allowed on this page, prefer prose that explicitly points readers to the WebService API reference for the container deployment shape.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                },
                {
                  "file": "Draft page",
                  "quote": "<ApiReference definitionName=\"LambdaDeploymentConfig\" />"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The Lambda deployment strategy union is fully covered: all canary, linear, and `AllAtOnce` values from `LambdaDeploymentConfig.strategy` are listed in the strategy table.",
            "The Lambda lifecycle hook properties `beforeAllowTrafficFunction` and `afterTrafficShiftFunction` are documented and demonstrated with a focused config example.",
            "The web-service load-balancing union variants are acknowledged: `http-api-gateway`, `application-load-balancer`, and `network-load-balancer` are all named, and the ALB requirement for `deployment` is scoped to `loadBalancing.type`.",
            "The Stacktape config examples use `<CodeBlock intellisense>` with `label: 'TypeScript'`, `defineConfig`, class-based resources, and no hand-authored YAML.",
            "The code examples are demonstrative: gradual deployment examples set `deployment.strategy`, the hook example sets `beforeAllowTrafficFunction`, and the web-service examples set `loadBalancing.type: 'application-load-balancer'` as required by the visible source."
          ]
        }
      ],
      "status": "needs-human-review",
      "passed": false
    },
    {
      "iteration": 3,
      "draftPath": "/Users/congy/Desktop/projects/stacktape/docs/scripts/generate-docs/.state/deployment-and-lifecycle__gradual-deployments/iteration-3.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 7,
            "completeness": 5,
            "practicalUsefulness": 6,
            "audienceFit": 8
          },
          "strengths": [
            "Lambda section is thorough and copy-paste ready: strategy reference table, basic example, lifecycle hook example with handler code, and clear tradeoff guidance all in one place.",
            "The lifecycle hook handler pattern is a genuinely useful, realistic TypeScript snippet that shows exactly what the function must do — signal CodeDeploy success or failure.",
            "Decision support is strong throughout: 'When to use' and 'When NOT to use' are concrete and opinionated, the FeatureComparisonTable gives a clear at-a-glance answer for which strategy to pick.",
            "Cost section correctly distinguishes Lambda (no overhead) from web services (double capacity + ALB cost) with concrete numbers grounded in the source.",
            "Rollback mechanics are explained clearly, including the distinction between automatic in-flight rollback vs the separate stacktape rollback CLI command.",
            "CodeBlock components are correctly formatted — TypeScript tab label, intellisense flag, focus markers, no hand-authored YAML tabs."
          ],
          "problems": [
            "Container workload strategies are severely under-documented: the section only says 'The exact strategy values are surfaced by TypeScript IntelliSense' instead of listing the five valid strings. A first-time user cannot know what to pass without an IDE open.",
            "Critical naming trap undocumented: container workloads use `Linear10PercentEvery1Minutes` (plural) while Lambda uses `Linear10PercentEvery1Minute` (singular). Copying a Lambda strategy name for a web service will produce a silent or confusing deploy failure. The source explicitly warns about this.",
            "Container workload strategies are a subset (5 options) not a superset of Lambda strategies — the draft implies they're the same set. `Canary10Percent10Minutes` and `Canary10Percent30Minutes` and all four Lambda linear names do not exist for containers.",
            "`testListenerPort` on ContainerWorkloadDeploymentConfig is completely absent. The source shows a full example using it alongside `beforeAllowTrafficFunction` — this is unique to container workloads and improves pre-production validation considerably.",
            "Multi-container workloads (`MultiContainerWorkload`) are mentioned only in the opening paragraph but never covered in the container strategy section. The source explicitly includes them as a supported resource.",
            "FeatureComparisonTable 'Lambda options' row shows counts (4/4/1) but has no container options row, making the table asymmetric and misleading about what containers support.",
            "Deploy time impact table only shows Lambda intervals with no container intervals column, even though the source table has both.",
            "Lambda vs web service comparison table lists container strategies as 'Supported via `deployment` property' rather than '5', and omits the naming difference row present in the source.",
            "FAQ entry about why strategy names differ between Lambda and containers is missing. This is the most likely question a first-time user hits when a linear strategy fails on a web service."
          ],
          "mandatoryFixes": [
            "Add a container workload strategy reference table listing all five valid strings: `Canary10Percent5Minutes`, `Canary10Percent15Minutes`, `Linear10PercentEvery1Minutes`, `Linear10PercentEvery3Minutes`, `AllAtOnce`. This table must appear in the 'Web service strategies' section before the code example, parallel to the Lambda strategy table.",
            "Document the naming difference explicitly: add an info callout (or dedicated paragraph) in the container section warning that `Linear10PercentEvery1Minutes` (with trailing 's') is the correct spelling for containers, distinct from Lambda's `Linear10PercentEvery1Minute`. Users copying Lambda strategy names to a web service will get a deploy error.",
            "Add `testListenerPort` documentation to the container section: explain what it does (creates a separate ALB listener routing only to the replacement target group), when to use it (with `beforeAllowTrafficFunction` to test the new container before production traffic shifts), and include the container lifecycle hook + testListenerPort code example from the source.",
            "Add `MultiContainerWorkload` to the container section scope — the section header and introductory paragraph currently say 'web services' only, but multi-container workloads expose the same `deployment` property.",
            "Update FeatureComparisonTable to add a 'Container options' row showing 2 canary options, 2 linear options, and 1 AllAtOnce option.",
            "Update the deploy time impact table to include container intervals (5 or 15 min for canary; 1 or 3 min per step for linear), matching the source table.",
            "Update the Lambda vs web service comparison table: replace 'Supported via deployment property' with '5' in the Strategies available row, and add a 'Linear naming' row noting the singular/plural difference.",
            "Add an FAQ entry: 'Why are strategy names different between Lambda and container workloads?' Answer must explain the singular/plural difference and direct users to copy strategy strings from the container-specific table rather than the Lambda table."
          ],
          "optionalImprovements": [
            "Add a `<ApiReference definitionName='ContainerWorkloadDeploymentConfig' />` note or inline type summary so readers can discover the container deployment type without leaving the page. (If the schema exports this type; if not, document the five strategy strings inline in the table as the authoritative reference.)",
            "The 'Rollback behavior' section describes Lambda and web service rollback accurately but could add one sentence specifically about what happens when container health checks fail (containers never become healthy → deployment stops, traffic stays on old version) — this is a distinct failure mode not covered in the current text.",
            "Consider adding a 'Multi-container workloads' subsection or callout pointing to the multi-container workload page for teams running multiple containers behind a single ALB, since the deployment config there is identical but the resource type is different.",
            "FAQ entry 'Can I use gradual deployments with HTTP API Gateway?' could add a one-sentence note that multi-container workloads also require ALB for the same reason, not just web services."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 6,
            "scannability": 7,
            "completeness": 5,
            "practicalUsefulness": 5,
            "audienceFit": 7
          },
          "strengths": [
            "Lambda function strategies section is thorough and well-structured: the strategy reference table is accurate, canary/linear/AllAtOnce distinctions are explained with concrete tradeoffs, and the lifecycle hook handler example with CodeDeploy signaling is exactly what a developer needs.",
            "The cost considerations section is precise and source-grounded — ALB vs http-api-gateway cost comparison, the double-capacity window for containers, and Lambda's zero-overhead are all accurate and actionable.",
            "The 'When to use / When NOT to use' sections are opinionated and concrete, covering the right cases (background workers, early dev, NLB incompatibility).",
            "The lifecycle hook handler code block is correct application code in a fenced typescript block (not wrapped in CodeBlock), and the CodeDeploy signaling pattern is accurate.",
            "FAQ covers a useful spread of questions and answers are concise and standalone."
          ],
          "problems": [
            "Container strategy reference table is completely absent. The web service section says 'the exact strategy values are surfaced by TypeScript IntelliSense' instead of listing the 5 supported strategies. A developer configuring a web service deployment has no way to know the valid values from this page. The LLM source lists them explicitly: Canary10Percent5Minutes, Canary10Percent15Minutes, Linear10PercentEvery1Minutes, Linear10PercentEvery3Minutes, AllAtOnce.",
            "Critical naming gotcha is missing: container workloads use Linear10PercentEvery1Minutes (plural 's') while Lambda uses Linear10PercentEvery1Minute (no 's'). Copying the Lambda strategy name to a web service config will cause a deployment failure. The LLM source flags this explicitly; the draft omits it entirely.",
            "Multi-container workloads are omitted. The LLM source states gradual deployments work for both web services AND multi-container workloads. The draft only mentions web services in the container strategies section and the comparison table.",
            "testListenerPort property is not documented at all. The LLM source describes this as a distinct container-workload feature: it creates a separate ALB listener routing exclusively to the new target group, enabling the beforeAllowTrafficFunction to test the new version before production traffic shifts. Skipping it leaves a meaningful feature undiscovered.",
            "Container lifecycle hooks example is absent. The Lambda section has a full working CodeBlock example with beforeAllowTrafficFunction. The web service section has no equivalent example, creating an asymmetric depth that undersells the container hooks.",
            "The 'Lambda vs web service comparison' table incorrectly shows '—' for lifecycle hooks on web services. Web services support beforeAllowTrafficFunction and afterTrafficShiftFunction (plus testListenerPort). This is actively wrong and would mislead someone trying to add pre-flight validation to a web service deployment.",
            "The FeatureComparisonTable in 'Choosing a strategy' shows Lambda options (4 canary, 4 linear) but has no row for container options (2 canary, 2 linear). The deploy time table also only shows Lambda intervals, omitting the container column (5/15 min canary, 1/3 min linear).",
            "The rollback description for web services says 'if issues arise during the traffic shift, traffic rolls back automatically' — this is vague. The LLM source specifies the concrete triggers: beforeAllowTrafficFunction failure OR new containers failing to become healthy. The imprecise wording could mislead a developer who expects automatic metric-based rollback for containers (which Lambda supports but containers do not in the same way)."
          ],
          "mandatoryFixes": [
            "Add a container strategy reference table in the 'Web service strategies' section listing all five strategy string values with their behavior: Canary10Percent5Minutes, Canary10Percent15Minutes, Linear10PercentEvery1Minutes (note plural), Linear10PercentEvery3Minutes, AllAtOnce. Remove the 'TypeScript IntelliSense' placeholder sentence.",
            "Document the Linear naming difference prominently in the container strategies section (as a Warning callout or bolded note): Lambda uses Linear10PercentEvery1Minute (singular), container workloads use Linear10PercentEvery1Minutes (plural). A copy-paste mistake here causes a deployment failure.",
            "Add a container lifecycle hooks example (CodeBlock intellisense) showing beforeAllowTrafficFunction and testListenerPort on a WebService, equivalent in depth to the Lambda lifecycle hooks example.",
            "Update the 'Lambda vs web service comparison' table: web services DO support lifecycle hooks (beforeAllowTrafficFunction, afterTrafficShiftFunction, testListenerPort). Replace '—' with the correct hook property names.",
            "Document testListenerPort: explain that it opens a separate ALB listener routing only to the new containers, enabling the beforeAllowTrafficFunction to send test requests against the new version before any production traffic shifts.",
            "Expand the FeatureComparisonTable and deploy-time table to include container-specific intervals (canary: 5 or 15 min; linear: 1 or 3 min per step). Add a 'Container options' row to the comparison table.",
            "Update the rollback section's container description to name the specific triggers: 'beforeAllowTrafficFunction signals failure' or 'new containers fail to pass health checks' — not the vague 'if issues arise'."
          ],
          "optionalImprovements": [
            "Add a FAQ entry: 'Why are the strategy names different between Lambda and container workloads?' — the LLM source includes this and it addresses a common confusion point with a concrete production impact.",
            "Rename the 'Web service strategies' H2 to 'Container workload strategies' to match the LLM source, and explicitly mention that multi-container workloads also support the deployment property.",
            "The ApiReference shows LambdaDeploymentConfig but the page also covers ContainerWorkloadDeploymentConfig. Consider either noting that the container deployment config is similar (same hooks, different strategy strings) or moving the ApiReference to LambdaDeploymentConfig while linking the container type separately via a prose note.",
            "The rollback FAQ answer for web services says 'traffic stays on original containers and new containers are removed' — this is correct and more precise than the rollback section body; consider aligning the body to the same level of specificity.",
            "The 'Writing lifecycle hook functions' section comments inside the handler code block ('// Run your validation logic here') are kept in the fenced application code block — these are user-facing instructions the developer would retain, so they are acceptable. No change required, but consider adding a one-line note that the timeout on the hook function should exceed your slowest integration check, with a concrete example (e.g., 'if your DB connectivity check takes 10 s, set timeout to at least 30')."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "scores": {
            "clarity": 7,
            "scannability": 7,
            "completeness": 6,
            "practicalUsefulness": 7,
            "audienceFit": 7
          },
          "strengths": [
            "Opening paragraph is answer-first, self-contained, and gives the main scope clearly: gradual traffic shifting for Lambda functions and ALB-backed web services.",
            "Stacktape config examples use the required class-based `defineConfig` style inside `<CodeBlock intellisense tabs={[...]}/>` with the correct `label: 'TypeScript'`.",
            "The Lambda strategy table is concrete and useful, with exact supported values from `LambdaDeploymentConfig`.",
            "The page includes practical decision guidance for canary vs linear vs AllAtOnce, plus deploy-time and cost tradeoffs.",
            "FAQ coverage is broad enough for search extraction, mixing Stacktape-specific, AWS-use-case, and choosing/comparison questions."
          ],
          "problems": [
            "The web service strategy section is underdeveloped. It says TypeScript IntelliSense surfaces accepted values instead of naming the container/web-service strategy values available in the provided generated source. This weakens union-type coverage and makes the page less useful for readers comparing Lambda and web-service deployments.",
            "The page omits multi-container workloads even though the generated source says gradual deployments are also supported for multi-container workloads using an application load balancer. If the page intentionally narrows to web services, that scope needs to be explicit; otherwise it is an important omission.",
            "Container/web-service lifecycle hook behavior is missing or vague. The generated source describes `beforeAllowTrafficFunction`, `afterTrafficShiftFunction`, and `testListenerPort` for container workloads, but the draft only documents lifecycle hooks for Lambda functions.",
            "`<ApiReference definitionName=\"LambdaDeploymentConfig\" />` only exposes the Lambda deployment config even though the page also documents web service deployments. A reader cannot inspect the web-service/container deployment shape from this page.",
            "Several AWS timing and rollback claims look too specific for the provided source, especially “alias weight change propagates within seconds,” “ALB listener rule switches back ... within a minute,” and “if it times out without signaling, the deployment treats it as a failure.” These may be true AWS behavior, but the supplied Stacktape sources do not ground them clearly.",
            "Some FAQ answers make unsupported cost estimates such as “adds pennies” or “a few dollars per deployment.” The source contains ALB and HTTP API approximate pricing, but not per-deployment Fargate cost examples.",
            "The “How do I monitor the canary” FAQ suggests `stacktape debug:logs` can tail logs in real time. The command name is correct, but the provided source list does not include CLI source grounding for tailing behavior.",
            "The page uses an application-code TypeScript block with explanatory `//` comments. This is allowed for authored code, but the placeholder-style comments make the lifecycle hook example less copy-paste complete for a production validation function."
          ],
          "mandatoryFixes": [
            "Add a concrete web service/container strategy reference or otherwise explicitly narrow the page scope. Include the exact supported web-service/container strategy values from the provided generated source if documenting web services.",
            "Document web-service/container lifecycle hooks with the same practical depth as Lambda hooks, including `beforeAllowTrafficFunction`, `afterTrafficShiftFunction`, and `testListenerPort`, or state clearly that this page only covers Lambda lifecycle hooks.",
            "Fix the API reference coverage: either use the correct root definition for this page if one exists, or add prose/API-reference handling that does not leave web-service deployment configuration undocumented. Do not add multiple `<ApiReference>` blocks unless the docs system explicitly allows concept pages to do so.",
            "Remove or soften unsupported timing and cost specifics: replace “within seconds,” “within a minute,” “adds pennies,” and “a few dollars” with source-grounded, cautious wording or omit the claims.",
            "Ground the `stacktape debug:logs` behavior in CLI source before claiming real-time tailing, or rephrase to the documented safe form: use `stacktape debug:logs` to view logs."
          ],
          "optionalImprovements": [
            "Add a compact table comparing Lambda strategies and web-service/container strategies side by side, including the singular/plural naming difference for `Linear10PercentEvery1Minute` vs `Linear10PercentEvery1Minutes`.",
            "Mention multi-container workloads in the opening paragraph and comparison table if the final page is meant to cover all gradual deployment targets from the generated source.",
            "Move the ALB cost context near the first web-service deployment mention so readers understand the main tradeoff before seeing the config example.",
            "Make the lifecycle hook handler example more concrete by replacing placeholder comments with a minimal real check, or explicitly label it as a handler pattern rather than a complete smoke test.",
            "Consider adding a short “Requirements” section listing: Lambda functions support gradual deployment directly; web services need `loadBalancing.type: 'application-load-balancer'`; HTTP API Gateway and network load balancer do not support web-service traffic shifting."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is generally aligned on Lambda gradual deployment strategy names, ALB requirements for web services, and config-example format. The main factual gaps are around container/web-service deployment capabilities: the draft omits multi-container workloads, hides the actual container strategy union, and incorrectly says web services have no lifecycle hooks.",
          "issues": [
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "The “Lambda vs web service comparison” table says web services have no lifecycle hooks: `Lifecycle hooks | beforeAllowTrafficFunction, afterTrafficShiftFunction | —`.",
              "reason": "The provided source says container workloads support the same before/after lifecycle hook properties as Lambda, plus `testListenerPort`. Since web services use `ContainerWorkloadDeploymentConfig`, this table removes a supported web-service deployment feature.",
              "suggestedFix": "Change the web-service lifecycle-hooks cell to mention `beforeAllowTrafficFunction`, `afterTrafficShiftFunction`, and `testListenerPort`. Add a short web-service lifecycle-hook section or link the explanation to the web-service strategy section.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "Container workloads support the same `beforeAllowTrafficFunction` and `afterTrafficShiftFunction` hooks as Lambda. Additionally, the `testListenerPort` property creates a separate ALB listener that routes to the new version's target group."
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/types/stacktape-config/web-services.d.ts",
                  "quote": "deployment?: ContainerWorkloadDeploymentConfig;"
                }
              ]
            },
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "The draft gives general strategy guidance that includes Lambda-only values such as `Canary10Percent10Minutes`, `Canary10Percent30Minutes`, and `Linear10PercentEvery10Minutes` without clearly scoping them to Lambda.",
              "reason": "The source says container gradual deployments have only five strategies, with fewer interval options than Lambda. A web-service reader could copy a Lambda-only strategy value into `deployment.strategy` and get an invalid config.",
              "suggestedFix": "Add a web-service/container strategy table with the five supported values, and scope the 10/30-minute canary and 10-minute linear guidance to Lambda functions only.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "Container gradual deployments support five strategies with the same canary/linear/all-at-once pattern as Lambda, but with fewer time-interval options."
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "| `Canary10Percent5Minutes` | 10% immediately, remaining 90% after 5 minutes |\n| `Canary10Percent15Minutes` | 10% immediately, remaining 90% after 15 minutes |\n| `Linear10PercentEvery1Minutes` | 10% more every minute (full rollout in ~10 minutes) |\n| `Linear10PercentEvery3Minutes` | 10% more every 3 minutes (full rollout in ~30 minutes) |\n| `AllAtOnce` | Instant switch with no gradual shift |"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The draft frames gradual deployments as supported for Lambda functions and web services, and the comparison section only covers Lambda vs web services.",
              "reason": "The source explicitly includes multi-container workloads as another supported container workload type. Omitting it makes the page look like web services are the only container-based target for gradual deployments.",
              "suggestedFix": "Mention multi-container workloads in the opening paragraph and container/web-service sections, or explicitly state that this page focuses on Lambda and web-service examples while multi-container workloads use the same container deployment strategy family.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "Stacktape supports gradual deployments for [Lambda functions](/resources/compute/lambda-function) and [web services](/resources/compute/web-service) (and [multi-container workloads](/resources/compute/multi-container-workload)) that use an [application load balancer](/resources/networking/application-load-balancer)."
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "[Web services](/resources/compute/web-service) and [multi-container workloads](/resources/compute/multi-container-workload) support gradual deployments through the `deployment` property."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The FAQ claims rollback speed: “For Lambda functions, the alias weight change propagates within seconds” and “For web services, the ALB listener rule switches back ... within a minute.”",
              "reason": "The provided Stacktape sources document automatic rollback behavior, but they do not provide rollback propagation timing. These concrete timing claims should not be stated as Stacktape documentation facts without a source.",
              "suggestedFix": "Replace the timing numbers with cautious wording such as: “Rollback is handled at the traffic-routing layer; exact propagation time depends on AWS service behavior and active connections.”",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/types/stacktape-config/functions.d.ts",
                  "quote": "Instead of switching all traffic to the new version instantly, shift it gradually\n   * (canary or linear). If issues arise, traffic rolls back automatically."
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/types/stacktape-config/web-services.d.ts",
                  "quote": "Gradual traffic shifting for safe deployments (canary, linear, or all-at-once).\n   *\n   * ---\n   *\n   * Requires `loadBalancing` type `application-load-balancer`."
                }
              ]
            }
          ],
          "positiveFindings": [
            "The Lambda strategy table matches the `LambdaDeploymentConfig.strategy` union in `functions.d.ts`.",
            "The draft correctly states that web-service gradual deployments require `loadBalancing.type: 'application-load-balancer'`.",
            "The Stacktape config examples use `<CodeBlock intellisense>` with a `TypeScript` tab and do not include hand-authored YAML or `StacktapeConfig` typed objects.",
            "The ALB and HTTP API Gateway pricing context is grounded in the `WebServiceProps.loadBalancing` JSDoc."
          ]
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is mostly aligned with the Lambda and web-service sources, but it drops important container-workload details from the provided gradual-deployments source and makes several Stacktape-specific claims about web-service rollout behavior, lifecycle hooks, and rollback mechanics that are not supported by the provided type sources.",
          "issues": [
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "\"Stacktape supports gradual deployments for Lambda functions and web services that use an application load balancer.\"",
              "reason": "The provided generated source says gradual deployments also apply to multi-container workloads. Stating only Lambda functions and web services reads as an exhaustive support list and would cause readers to miss a supported resource type.",
              "suggestedFix": "Write that Stacktape supports gradual deployments for Lambda functions, web services, and multi-container workloads that use an application load balancer, or explicitly say the page focuses on Lambda functions and web services while linking to multi-container workloads.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "Stacktape supports gradual deployments for [Lambda functions](/resources/compute/lambda-function) and [web services](/resources/compute/web-service) (and [multi-container workloads](/resources/compute/multi-container-workload)) that use an [application load balancer](/resources/networking/application-load-balancer)."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "\"For web services, the `deployment` property enables the same pattern through the ALB. If issues arise during the traffic shift, traffic rolls back to the previous version automatically.\"",
              "reason": "The authoritative web-service type source only says web-service deployment is gradual traffic shifting and requires an application load balancer. The provided generated page supports narrower container rollback cases: hook failure or new containers failing health checks. It does not support the broad claim that any issues during a web-service traffic shift automatically roll traffic back.",
              "suggestedFix": "Narrow the web-service rollback wording to supported cases: the deployment uses ALB-based traffic shifting; if a before-hook signals failure or new containers fail health checks, the deployment stops and traffic stays on the original version. Avoid claiming automatic rollback for unspecified issues.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/types/stacktape-config/web-services.d.ts",
                  "quote": "Gradual traffic shifting for safe deployments (canary, linear, or all-at-once).\n   *\n   * ---\n   *\n   * Requires `loadBalancing` type `application-load-balancer`."
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "For container workloads, the `deployment` property enables the same pattern through the ALB. If the `beforeAllowTrafficFunction` signals failure or new containers fail to become healthy, the deployment stops and traffic stays on the original version."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft says web services do not have lifecycle hooks: in the comparison table, \"Lifecycle hooks\" for web services is \"—\".",
              "reason": "The provided gradual-deployments source says container workloads, including web services in that section, support `beforeAllowTrafficFunction`, `afterTrafficShiftFunction`, and `testListenerPort`. The draft’s comparison table directly contradicts that source and would incorrectly tell readers that hooks are Lambda-only.",
              "suggestedFix": "Document container/web-service deployment hooks or remove the comparison row. If included, state that container workloads support `beforeAllowTrafficFunction`, `afterTrafficShiftFunction`, and `testListenerPort`, with the caveat that the exact property shape comes from `ContainerWorkloadDeploymentConfig`.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "Container workloads support the same `beforeAllowTrafficFunction` and `afterTrafficShiftFunction` hooks as Lambda. Additionally, the `testListenerPort` property creates a separate ALB listener that routes to the new version's target group."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The web-service section says strategy values are surfaced by TypeScript IntelliSense instead of listing the supported web-service/container strategy values.",
              "reason": "The source provides the container strategy values, including the important spelling difference for `Linear10PercentEvery1Minutes`. Omitting them weakens union-type coverage and can mislead readers into reusing Lambda strategy names that are invalid for container workloads.",
              "suggestedFix": "Add a web-service/container strategy table with `Canary10Percent5Minutes`, `Canary10Percent15Minutes`, `Linear10PercentEvery1Minutes`, `Linear10PercentEvery3Minutes`, and `AllAtOnce`; call out the singular/plural naming difference from Lambda.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "| `Canary10Percent5Minutes` | 10% immediately, remaining 90% after 5 minutes |\n| `Canary10Percent15Minutes` | 10% immediately, remaining 90% after 15 minutes |\n| `Linear10PercentEvery1Minutes` | 10% more every minute (full rollout in ~10 minutes) |\n| `Linear10PercentEvery3Minutes` | 10% more every 3 minutes (full rollout in ~30 minutes) |\n| `AllAtOnce` | Instant switch with no gradual shift |"
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/deployment-and-lifecycle/gradual-deployments.md",
                  "quote": "Container strategies use slightly different naming than Lambda strategies. Note that `Linear10PercentEvery1Minutes` (with an \"s\") is the correct value for container workloads, while Lambda uses `Linear10PercentEvery1Minute` (no \"s\")."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "\"A normal deployment without the `deployment` property switches traffic immediately with no hook opportunity.\"",
              "reason": "The source confirms lifecycle hooks exist inside `LambdaDeploymentConfig`, but it does not define behavior for a \"normal deployment without the deployment property\" or prove that no other hook opportunity exists. This is an unsupported Stacktape-specific contrast, not just general AWS context.",
              "suggestedFix": "Say only that `AllAtOnce` uses the deployment lifecycle hook fields while avoiding claims about all normal deployments. For example: \"Use `AllAtOnce` when you want CodeDeploy lifecycle hooks without a gradual percentage ramp.\"",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/types/stacktape-config/functions.d.ts",
                  "quote": "interface LambdaDeploymentConfig {\n  /**\n   * #### How traffic shifts from the old version to the new one.\n   *\n   * ---\n   *\n   * - **Canary**: Send 10% of traffic first, then all traffic after a wait period.\n   * - **Linear**: Shift 10% of traffic at regular intervals.\n   * - **AllAtOnce**: Instant switch (no gradual rollout).\n   */"
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/types/stacktape-config/functions.d.ts",
                  "quote": "Function to run before traffic shifting begins (e.g., smoke tests).\n   *\n   * ---\n   *\n   * Must signal success/failure to CodeDeploy. If it fails, the deployment rolls back."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"The `smokeTest` function runs before any traffic shifts. Use it for health checks, integration verification, or testing the new version against a staging database. If the function signals failure, the deployment rolls back — no user traffic ever reaches the broken version.\"",
              "reason": "The source supports that `beforeAllowTrafficFunction` runs before traffic shifting and rolls back on failure, but it does not support the example-specific recommendation to test the new version against a staging database. A pre-traffic deployment hook is for validating the new deployed version; pointing it at a staging database may not validate production dependencies and is not grounded in source.",
              "suggestedFix": "Replace \"testing the new version against a staging database\" with source-grounded examples such as smoke tests or validation logic. If mentioning databases, keep it generic: \"checking required dependencies\" rather than prescribing a staging database.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/types/stacktape-config/functions.d.ts",
                  "quote": "Function to run before traffic shifting begins (e.g., smoke tests).\n   *\n   * ---\n   *\n   * Must signal success/failure to CodeDeploy. If it fails, the deployment rolls back."
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "The draft uses `<ApiReference definitionName=\"LambdaDeploymentConfig\" />` on a page that also documents web-service deployments.",
              "reason": "The Lambda API reference is valid for Lambda deployment settings, but the page also documents web-service/container deployment behavior. Because the provided source does not include the `ContainerWorkloadDeploymentConfig` type definition, it is unclear whether the page can render the corresponding API reference. Still, a single Lambda-only API reference may leave web-service deployment properties under-documented.",
              "suggestedFix": "Either add prose that the API reference below covers Lambda deployment configuration only, or include the container deployment API reference if `ContainerWorkloadDeploymentConfig` is available in the generated schema.",
              "evidence": []
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "\"For Lambda functions, the alias weight change propagates within seconds. For web services, the ALB listener rule switches back to the original target group, which typically completes within a minute as connections drain from the new containers.\"",
              "reason": "This is plausible AWS operational behavior, but the provided Stacktape sources do not support these rollback timing claims. The numbers are not necessary for configuration and may drift by workload or AWS behavior.",
              "suggestedFix": "Remove the exact timing claims or frame them as general AWS behavior without promising a specific duration: \"Rollback is usually quick, but the exact timing depends on AWS propagation and connection draining.\"",
              "evidence": []
            }
          ],
          "positiveFindings": [
            "The draft correctly uses `<CodeBlock intellisense>` with a `TypeScript` tab for Stacktape configuration examples and avoids hand-authored YAML.",
            "The Lambda strategy table matches the `LambdaDeploymentConfig.strategy` union in `functions.d.ts`.",
            "The draft correctly states that web-service gradual deployments require `loadBalancing.type: 'application-load-balancer'`, matching `web-services.d.ts`.",
            "The draft correctly links only to routes that appear in the provided documentation navigation index."
          ]
        },
        {
          "verifierId": "code-block-validator",
          "modelProvider": "deterministic",
          "modelName": "local",
          "summary": "Deterministic check of <CodeBlock intellisense> Stacktape config examples and generated MDX component usage.",
          "issues": [
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #3 (TypeScript): TypeScript type error.",
              "reason": "line 5, col 5: Object literal may only specify known properties, and 'containers' does not exist in type 'WebServiceProps'.",
              "suggestedFix": "Fix the example so it type-checks against the public Stacktape TypeScript declarations. The full hidden source must be a valid stacktape.ts file."
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #3 (TypeScript): generated YAML tab does not round-trip to type-correct TypeScript.",
              "reason": "line 5, col 7: Object literal may only specify known properties, and 'containers' does not exist in type 'WebServiceProps'.",
              "suggestedFix": "Fix the TypeScript example or the YAML conversion helper so the generated YAML describes the same valid Stacktape config."
            }
          ],
          "positiveFindings": []
        }
      ],
      "status": "failed",
      "passed": false
    }
  ],
  "agentModelAssignments": {
    "page-writer": "claude:cli-default",
    "first-time-user-reviewer": "claude:claude-sonnet-4-6",
    "production-engineer-reviewer": "claude:claude-sonnet-4-6",
    "ai-consumer-reviewer": "codex:cli-default",
    "factual-accuracy-verifier": "codex:cli-default",
    "source-grounding-verifier": "codex:cli-default",
    "api-completeness-auditor": "codex:cli-default",
    "code-block-validator": "local"
  },
  "finalOutputPath": "/Users/congy/Desktop/projects/stacktape/docs/docs/deployment-and-lifecycle/gradual-deployments.mdx",
  "outcome": "failed",
  "pipelineStatus": "did-not-pass",
  "refinementAttempts": 1,
  "refinementStalled": false,
  "pipelineFailureSummary": "reviewer-avg 6.5, hard blockers 6, advisory high 0, verifier medium 6"
}
