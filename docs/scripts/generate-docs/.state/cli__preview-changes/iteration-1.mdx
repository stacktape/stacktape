---
title: 'preview-changes'
order: 150
seoTitle: 'preview-changes CLI Command | Stacktape Docs'
seoDescription: 'Preview infrastructure changes before deploying with Stacktape. See which resources will be created, updated, replaced, or removed — without modifying your stack.'
---

# preview-changes

The `preview-changes` command shows what would change in your deployed stack if you ran `deploy` with your current configuration. It packages workloads, compiles the CloudFormation template, creates a change set, and displays a categorized summary — without actually modifying anything.

Use this command before deploying to verify that your configuration changes produce the intended infrastructure updates, catch unintended resource replacements, or review changes in a CI pipeline before approving a deployment.

## Usage

```bash
stacktape preview-changes --stage production --region eu-west-1
```

## How it works

The command performs the same preparation steps as [`deploy`](/cli/deploy) — packaging workloads, resolving resources, compiling the CloudFormation template — but stops before executing the change set. It creates a CloudFormation change set in `CREATE` status, reads the proposed changes, then deletes the change set without applying it.

Changes are categorized into four types:

| Symbol | Label | Meaning |
|--------|-------|---------|
| `+` | new | A resource will be created |
| `-` | removed | A resource will be deleted |
| `!` | replaced | A resource will be destroyed and recreated (potential data loss) |
| `~` | updated | A resource will be modified in place |

Stacktape normalizes the raw CloudFormation diff to filter out internal runtime churn and dependency-only re-evaluations. If CloudFormation reports changes but none map to meaningful Stacktape resources, the output explicitly says so.

<Warning>
Resources marked as **replaced** will be destroyed and recreated. For stateful resources like databases or buckets, this can cause data loss. Review replacements carefully before deploying.
</Warning>

## Important flags

**`--preserveTempFiles`** — Keeps the compiled CloudFormation template and packaged artifacts in `.stacktape/[invocation-id]` so you can inspect them after the preview completes. Useful for debugging template-level differences.

**`--configPath`** — Points to a non-default configuration file. Use this when your `stacktape.ts` lives outside the current directory or has a non-standard name.

**`--agent`** — Switches output to machine-readable JSONL format. Each resource change is emitted as a structured line with action, resource name, type, highlights, and replacement warnings. Useful for CI pipelines or AI agent integrations.

## Examples

Preview changes for a staging environment:

```bash
stacktape preview-changes --stage staging --region us-east-1
```

Preview changes using a specific config file and preserve temporary files for inspection:

```bash
stacktape preview-changes --stage production --region eu-west-1 --configPath ./infra/stacktape.ts --preserveTempFiles
```

Preview changes in agent mode for CI pipeline consumption:

```bash
stacktape preview-changes --stage production --region eu-west-1 --agent
```

## Command reference

<CliCommandsApiReference command="preview-changes" sortedArgs={[
  {
    "name": "region",
    "required": true,
    "alias": "r",
    "allowedTypes": [
      "string"
    ],
    "allowedValues": [
      "us-east-2",
      "us-east-1",
      "us-west-1",
      "us-west-2",
      "ap-east-1",
      "ap-south-1",
      "ap-northeast-3",
      "ap-northeast-2",
      "ap-southeast-1",
      "ap-southeast-2",
      "ap-northeast-1",
      "ca-central-1",
      "eu-central-1",
      "eu-west-1",
      "eu-west-2",
      "eu-west-3",
      "eu-north-1",
      "me-south-1",
      "sa-east-1",
      "af-south-1",
      "eu-south-1"
    ],
    "shortDescription": "<p> AWS Region</p>\n",
    "longDescription": "<p>The AWS region for the operation. For a list of available regions, see the <a href=\"https://docs.aws.amazon.com/general/latest/gr/rande.html\" style=\"font-weight: bold;\" target=\"_blank\" rel=\"noreferrer\" onclick=\"event.stopPropagation();\">AWS documentation</a>.</p>\n"
  },
  {
    "name": "stage",
    "required": true,
    "alias": "s",
    "allowedTypes": [
      "string"
    ],
    "shortDescription": "<p> Stage</p>\n",
    "longDescription": "<p>The stage for the operation (e.g., <code>production</code>, <code>staging</code>, <code>dev-john</code>). You can set a default stage using the <code>defaults:configure</code> command. The maximum length is 12 characters.</p>\n"
  },
  {
    "name": "agent",
    "required": false,
    "alias": "ag",
    "allowedTypes": [
      "boolean"
    ],
    "shortDescription": "<p> Agent Mode</p>\n",
    "longDescription": "<p>Optimizes CLI output for programmatic/LLM consumption:</p>\n<ul>\n<li>Uses strict JSONL/NDJSON output (one JSON object per line)</li>\n<li>Disables interactive terminal UI</li>\n<li>Automatically confirms operations (equivalent to --autoConfirmOperation)\nFor dev command: also enables HTTP server for programmatic control.</li>\n</ul>\n"
  },
  {
    "name": "awsAccount",
    "required": false,
    "alias": "aa",
    "allowedTypes": [
      "string"
    ],
    "shortDescription": "<p> AWS Account</p>\n",
    "longDescription": "<p>The name of the AWS account to use for the operation. The account must first be connected in the <a href=\"https://console.stacktape.com/aws-accounts\" style=\"font-weight: bold;\" target=\"_blank\" rel=\"noreferrer\" onclick=\"event.stopPropagation();\">Stacktape console</a>.</p>\n"
  },
  {
    "name": "configPath",
    "required": false,
    "alias": "cp",
    "allowedTypes": [
      "string"
    ],
    "shortDescription": "<p> Config File Path</p>\n",
    "longDescription": "<p>The path to your Stacktape configuration file, relative to the current working directory.</p>\n"
  },
  {
    "name": "currentWorkingDirectory",
    "required": false,
    "alias": "cwd",
    "allowedTypes": [
      "string"
    ],
    "shortDescription": "<p> Current Working Directory</p>\n",
    "longDescription": "<p>The working directory for the operation. All file paths in your configuration will be resolved relative to this directory. By default, this is the directory containing the configuration file.</p>\n"
  },
  {
    "name": "help",
    "required": false,
    "alias": "h",
    "allowedTypes": [
      "string"
    ],
    "shortDescription": "<p> Show Help</p>\n",
    "longDescription": "<p>If provided, the command will not execute and will instead print help information.</p>\n"
  },
  {
    "name": "logLevel",
    "required": false,
    "alias": "ll",
    "allowedTypes": [
      "string"
    ],
    "allowedValues": [
      "info",
      "debug",
      "error"
    ],
    "shortDescription": "<p> Log Level</p>\n",
    "longDescription": "<p>The level of logs to print to the console.</p>\n<ul>\n<li><code>info</code>: Basic information about the operation.</li>\n<li><code>error</code>: Only errors.</li>\n<li><code>debug</code>: Detailed information for debugging.</li>\n</ul>\n"
  },
  {
    "name": "outputFormat",
    "required": false,
    "alias": "ofmt",
    "allowedTypes": [
      "string"
    ],
    "allowedValues": [
      "jsonl",
      "plain",
      "tty"
    ],
    "shortDescription": "<p> Output Format</p>\n",
    "longDescription": "<p>Controls the CLI output format:</p>\n<ul>\n<li><code>jsonl</code>: Machine-readable NDJSON (one JSON object per line). Disables interactive UI.</li>\n<li><code>plain</code>: Simple text output without colors or animations. Used automatically in CI or non-TTY environments.</li>\n<li><code>tty</code>: Full interactive terminal UI with colors, spinners, and animations. Used automatically when a TTY is detected.\nIf not specified, the format is auto-detected from the environment. --agent implies --outputFormat jsonl.</li>\n</ul>\n"
  },
  {
    "name": "preserveTempFiles",
    "required": false,
    "alias": "ptf",
    "allowedTypes": [
      "boolean"
    ],
    "shortDescription": "<p> Preserve Temporary Files</p>\n",
    "longDescription": "<p>If <code>true</code>, preserves the temporary files generated by the operation, such as the CloudFormation template and packaged resources. These files are saved to <code>.stacktape/[invocation-id]</code>.</p>\n"
  },
  {
    "name": "profile",
    "required": false,
    "alias": "p",
    "allowedTypes": [
      "string"
    ],
    "shortDescription": "<p> AWS Profile</p>\n",
    "longDescription": "<p>The AWS profile to use for the command. You can manage profiles using the <code>aws-profile:*</code> commands and set a default profile with <code>defaults:configure</code>.</p>\n"
  },
  {
    "name": "projectName",
    "required": false,
    "alias": "prj",
    "allowedTypes": [
      "string"
    ],
    "shortDescription": "<p> Project Name</p>\n",
    "longDescription": "<p>The name of the Stacktape project for this operation.</p>\n"
  },
  {
    "name": "templateId",
    "required": false,
    "alias": "ti",
    "allowedTypes": [
      "string"
    ],
    "shortDescription": "<p> Template ID</p>\n",
    "longDescription": "<p>The ID of the template to download. You can find a list of available templates on the <a href=\"https://console.stacktape.com/templates\" style=\"font-weight: bold;\" target=\"_blank\" rel=\"noreferrer\" onclick=\"event.stopPropagation();\">Config Builder page</a>.</p>\n"
  }
]} />

## Related commands

- [`deploy`](/cli/deploy) — Apply the changes that `preview-changes` shows you.
- [`compile-template`](/cli/compile-template) — Export the compiled CloudFormation template to a file for manual inspection.
- [`rollback`](/cli/rollback) — Revert to a previous deployment version if a deploy goes wrong.

## FAQ

### Does preview-changes modify my deployed stack?

No. The command creates a CloudFormation change set in read-only mode to compute the diff, then discards it. Your deployed infrastructure remains unchanged. The only side effect is that workloads are packaged and the compiled template is uploaded to your deployment artifact bucket (the same bucket used during normal deploys).

### How long does preview-changes take?

The command runs the same packaging and template compilation as [`deploy`](/cli/deploy), so duration depends on your project size. For a typical project with a few Lambda functions or containers, expect 30–90 seconds. Projects with many container images or large build steps take longer because workloads must be fully packaged.

### Can I use preview-changes in a CI pipeline?

Yes. Pass `--agent` to get structured JSONL output that a script can parse. Each resource change is emitted on a single line with its action type, resource name, and highlights. You can gate deployments on the absence of `replace` actions or unexpected resource deletions.

### What does "no meaningful Stacktape resource changes" mean?

CloudFormation sometimes reports internal dependency re-evaluations or runtime metadata shifts that don't correspond to actual infrastructure changes. Stacktape normalizes the raw diff and filters these out. When you see this message, the underlying stack would not change in any user-visible way.

### Does preview-changes require a deployed stack?

Yes. The command compares your current configuration against an already-deployed stack. If no stack exists for the given project, stage, and region, the command fails. For a first deployment, use [`deploy`](/cli/deploy) directly — every resource will be new.

### How is preview-changes different from compile-template?

[`compile-template`](/cli/compile-template) exports the raw CloudFormation template to a file. It does not compare against a deployed stack or show what would change. `preview-changes` goes further: it packages workloads, uploads the template, creates a change set against the live stack, and shows a categorized diff of resource-level changes.

### What does a "replaced" resource mean?

A replaced resource will be destroyed and recreated by CloudFormation. This happens when you change an immutable property (like a database engine type or a Lambda function name). For stateful resources — databases, buckets, file systems — replacement means data loss unless you have backups. Always review replacements before deploying.

### Can I preview changes for a specific resource only?

No. The command previews the entire stack diff. CloudFormation change sets operate at the stack level and cannot be scoped to a single resource. The output groups changes by Stacktape resource name, so you can quickly scan for the resource you care about.