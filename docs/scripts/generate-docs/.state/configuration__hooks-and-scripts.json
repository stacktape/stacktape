{
  "pageId": "configuration__hooks-and-scripts",
  "pageRoute": "configuration/hooks-and-scripts",
  "startedAt": "2026-05-16T17:07:31.972Z",
  "updatedAt": "2026-05-16T17:47:20.053Z",
  "iterations": [
    {
      "iteration": 1,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\configuration__hooks-and-scripts\\iteration-1.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "The comparison table between hooks+scripts vs deployment scripts at the top is excellent — it immediately answers the most common decision question a beginner faces and gives concrete, actionable criteria (VPC access, timeout, trigger scope) rather than abstract descriptions.",
            "All three script types (local-script, local-script-with-bastion-tunneling, bastion-script) are clearly differentiated with dedicated sections, purpose statements, code examples, and when-to-use signals — good union-type coverage without flattening.",
            "Hook failure behavior table is precise and practically important: explicitly stating that afterDeploy failure does NOT roll back the deployment is a critical gotcha that prevents dangerous assumptions.",
            "The Warning callout about afterDeploy migrations leaving schema out of sync is well-placed and actionable — it tells the reader exactly what to do (re-run manually with script:run).",
            "Common patterns section covers real-world cases a beginner actually encounters (migration, build, export) with realistic, copyable examples.",
            "FAQ mixes all three required question categories — Stacktape-specific, AWS-use-case, and comparison — and each answer is concise enough to stand alone as a snippet.",
            "No mandatory style violations: all Stacktape config examples use defineConfig + class-based resources inside CodeBlock intellisense; no fenced yaml Stacktape configs; no StacktapeConfig typed style; shell blocks each contain exactly one command."
          ],
          "problems": [
            "The first code block (local-script example) references `connectTo: ['mainDatabase']` in the migrateDb script but `mainDatabase` is not defined as a resource anywhere in that config. A beginner copying this example will get an error or a silently useless connectTo. This is the most common adaptation point for hooks and it's broken in the introductory example.",
            "Hook context variables `STP_HOOK_TYPE`, `STP_HOOK_TRIGGER`, and `STP_COMMAND` are stated as fact in the 'Environment variables in scripts' section but are not grounded in any of the provided source files (`_root.d.ts`, `scripts.d.ts`). If these don't exist, the section actively misleads the reader.",
            "The bastion-script Redis example uses `$STP_CACHE_HOST` and `$STP_CACHE_PORT` as the injected env var names for a RedisCluster resource, but the provided source does not verify these are the canonical names. The connectTo JSDoc enumerates specific injected variable names per resource type; if the real injected name is `STP_CACHE_CONNECTION_STRING` (consistent with how the docs describe Redis elsewhere in memory), the example is wrong and would fail at runtime.",
            "The 'Supported resource types for role assumption' enumeration at the end of the IAM section lists specific resource types (Lambda functions, web services, private services, worker services, multi-container workloads, batch jobs, SSR frontends) but this list is not grounded in any provided source file. If the real constraint is different, this is a false ceiling or floor.",
            "The 'Deployment scripts vs hooks' section near the bottom significantly overlaps with the comparison table at the top. The table already covers VPC access, timeout, and rollback. The prose section adds only the CloudFormation rollback nuance — the rest is repetition that pads the page without adding value for a beginner."
          ],
          "mandatoryFixes": [
            "Fix the first local-script code example: either add `mainDatabase` as a RelationalDatabase resource to the config so `connectTo: ['mainDatabase']` is valid and self-contained, or replace the connectTo value with a resource that IS defined in the example (e.g., reference the `api` LambdaFunction). A beginner's first copy-paste of a hooks example must not reference an undefined resource.",
            "Verify or remove the hook context variables block in 'Environment variables in scripts'. If `STP_HOOK_TYPE`, `STP_HOOK_TRIGGER`, and `STP_COMMAND` are not documented in source, remove the bullet points (or qualify them explicitly as 'may be injected — verify in your CLI version'). Presenting unverified env var names as canon causes runtime confusion.",
            "Verify the Redis connectTo env var names used in the bastion-script example (`$STP_CACHE_HOST`, `$STP_CACHE_PORT`). If the canonical injected variable for RedisCluster via connectTo is `STP_CACHE_CONNECTION_STRING` (as used elsewhere in Stacktape Redis docs), update the example command to use the correct variable name. Wrong env var names in shell examples fail silently.",
            "Trim or merge the 'Deployment scripts vs hooks' H2 section at the bottom. It repeats content from the comparison table at the top without meaningful addition. Keep only the CloudFormation rollback nuance (one paragraph), then link to the deployment scripts page. A padded backbone section lowers signal-to-noise for the beginner scanning for what to do next."
          ],
          "optionalImprovements": [
            "Add a brief note before the 'Assuming IAM roles' section flagging it as an advanced topic most beginners can skip: 'Most teams don't need this — skip it on a first pass.' The IAM mental model is non-trivial for low-AWS-experience readers and burying it in the middle of the page interrupts the common-case flow.",
            "The H2 'Why hooks and scripts exist' is slightly long and reads more like a blog heading than a nav label. Consider shortening to 'Motivation' or 'Why this exists' — the current wording is fine in prose but awkward in a sidebar ToC.",
            "In the conditional execution example, add a one-sentence explanation of how Stacktape detects a CI environment for `skipOnCI` (e.g., which env vars it checks). A beginner will immediately wonder 'how does it know I'm on CI?' — answering that removes a support question.",
            "The FAQ answer for 'What tools are available in bastion scripts?' mentions 'standard Unix tools' and 'Amazon Linux' — if the bastion host AMI is configurable or can be customized, note that briefly. If it's fixed, confirm it's fixed. The current answer is slightly vague.",
            "Consider adding a one-line note at the top of the 'Scripts' section stating the three types at a glance before diving into each — a brief enumeration ('Stacktape supports three script types: local-script, local-script-with-bastion-tunneling, and bastion-script') helps a scanning reader know what's coming without reading all three subsections."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 6,
            "audienceFit": 8
          },
          "strengths": [
            "Strong decision-support table at the top comparing hooks+scripts vs deployment scripts — engineers can make the right architectural choice immediately.",
            "All three script types (local-script, local-script-with-bastion-tunneling, bastion-script) are documented with working examples and per-variant tradeoff guidance.",
            "Hook failure behavior section (beforeDeploy aborts vs afterDeploy warns) is exactly the kind of production-critical behavior engineers need and rarely find documented.",
            "FAQ is well-targeted — mixes Stacktape-specific, AWS-behavior, and comparison questions. Eight entries without padding.",
            "Common patterns section (migrations after deploy, build before deploy, export before delete) maps directly to the most common real-world use cases.",
            "Lifecycle event table with clear 'When it fires' descriptions is immediately useful."
          ],
          "problems": [
            "Code examples in the 'Scripts' section define only `api` as a resource but reference `connectTo: ['mainDatabase']` in the `migrateDb` script — `mainDatabase` does not exist in those resource maps. This would be a TypeScript type error and actively misleads engineers trying to reproduce the pattern. The same bug appears in the 'Hooks' section example.",
            "Hook context variables `STP_HOOK_TYPE`, `STP_HOOK_TRIGGER`, and `STP_COMMAND` are documented as auto-injected but are not present in any provided source file. If these don't exist in the product, this section will silently break scripts that rely on them.",
            "The claim that `pipeStdio` defaults to `true` and `cwd` defaults to the directory of the Stacktape command are stated as facts without source backing. Engineers will write scripts that depend on these defaults.",
            "The statement that `bastion-script` type only supports `executeCommand` or `executeCommands` (not file-based execution) is stated as a hard constraint but is not verifiable from the provided source.",
            "`skipOnCI` and `skipOnLocal` hook entry properties are documented with specific behavior (detecting CodeBuild, GitHub Actions, GitLab CI) but are not in any provided source file.",
            "The claim that a failed deployment script triggers 'CloudFormation rollback of the entire deployment' in the 'Deployment scripts vs hooks' section is a consequential production-behavior claim that is unverifiable from the provided source files.",
            "The `--env` flag shown in the CLI examples (`stacktape script:run ... --env VERBOSE=true`) cannot be verified from source — it may be `--set` or another flag name.",
            "`assumeRoleOfResource` appears both as a script config property and as a CLI flag. The dual surface is not explained, and neither is grounded in the provided source."
          ],
          "mandatoryFixes": [
            "Fix the code examples in the 'Scripts' and 'Hooks' sections where `connectTo: ['mainDatabase']` references a resource not defined in the same config block. Either add `mainDatabase` as a RelationalDatabase resource in those examples, or change the connectTo target to the `api` resource (or any resource actually defined in the example). An engineer who copy-pastes these will get a type error or a runtime failure.",
            "Qualify or remove the 'Hook context variables' subsection (`STP_HOOK_TYPE`, `STP_HOOK_TRIGGER`, `STP_COMMAND`). These variables are not present in any provided source file. If they cannot be verified against the actual type definitions (LocalScript, BastionScript, Hooks), remove the claims entirely rather than leave potentially false environment variable names that engineers will try to use."
          ],
          "optionalImprovements": [
            "Add a caveat near the `afterDeploy` hook failure behavior paragraph noting that infrastructure is deployed but may be in an inconsistent state — recommend adding a health check or readiness probe rather than relying solely on manual re-runs.",
            "The 'Assuming IAM roles' section would benefit from one sentence clarifying whether `assumeRoleOfResource` is also available as a script config property (not just a CLI flag) — the page currently shows both but doesn't explain the difference or when each applies.",
            "Add an `<ApiReference>` block for the LocalScript, BastionScript, and LocalScriptWithBastionTunneling types if schema entries exist — the page documents many properties (executeScript, executeScripts, pipeStdio, cwd, skipOnCI, skipOnLocal) that engineers will want to cross-check against the full type.",
            "The comparison of hooks vs deployment scripts appears in both the top-of-page table and in the 'Deployment scripts vs hooks' H2 section. The bottom section adds the rollback distinction (useful) but repeats the VPC-access and timeout points already in the table. Trim the bottom section to only the rollback + consistency angle to eliminate the duplication.",
            "The FAQ answer for 'What tools are available in bastion scripts?' says to install tools via a 'preceding executeCommands entry' — but if bastion-script only supports executeCommand/executeCommands (as stated in the main body), installing tools and running them in a single executeCommands list is the correct pattern, not a 'preceding' entry. Clarify this, and note whether tool state persists across script runs."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 7,
            "completeness": 8,
            "practicalUsefulness": 9,
            "audienceFit": 8
          },
          "strengths": [
            "Answer-first paragraph is exactly right — self-contained, 40-75 words, no meta-framing, answers what it is and why it matters.",
            "Comparison table (hooks vs deployment scripts) is excellent decision support: covers where it runs, use cases, timeout, VPC access, and trigger scope side by side.",
            "All three script types (local-script, local-script-with-bastion-tunneling, bastion-script) are covered with working, copy-paste examples at roughly equal depth.",
            "Common patterns section directly addresses the top three real-world use cases (migrations, builds, pre-delete export) with realistic end-to-end examples.",
            "Hook failure behavior table is explicitly documented with distinct semantics per event — the afterDeploy Warning callout is a high-value callout that most pages omit.",
            "FAQ covers all three required categories: Stacktape-specific (skipOnCI, parallel scripts), AWS service behavior (what tools are on bastion), and comparison (deployment scripts vs hooks).",
            "No forbidden patterns: no YAML resources blocks, no StacktapeConfig type import, no fenced TypeScript with defineConfig, no marketing language.",
            "Shell command blocks are correctly split — one command per block, no explanatory # comments inside blocks."
          ],
          "problems": [
            "H2 heading 'When to use hooks vs deployment scripts' is 8 words and reads as a full label that could be shortened to 'Hooks vs deployment scripts'. The mandatory style rule triggers: overly long H2 headings that a short label would replace → scannability penalty.",
            "H2 heading 'Why hooks and scripts exist' reads as a sentence fragment rather than a noun-form label. Per the mandatory rule this should be shortened (e.g. 'Motivation' or 'Why use hooks').",
            "The bastion-script code example injects `STP_CACHE_HOST` and `STP_CACHE_PORT` via connectTo on a RedisCluster named 'cache'. Neither of these env var names is confirmed by the provided source files. If the actual injected names differ, the example is misleading.",
            "Hook context variables `STP_HOOK_TYPE`, `STP_HOOK_TRIGGER`, and `STP_COMMAND` in the 'Environment variables in scripts' section are not backed by the provided source files. Their existence cannot be verified from the inline source.",
            "The `skipOnCI` and `skipOnLocal` properties on hook entries and `assumeRoleOfResource` on scripts are not visible in the provided source types. These may be real but are unconfirmed by the inline source.",
            "The 'Deployment scripts vs hooks' closing section partially repeats the decision table from 'When to use hooks vs deployment scripts' near the top. The repetition adds bulk without new information.",
            "Tunable property values in Common Patterns examples — `instanceSize: 'db.t4g.micro'`, `cpu: 0.25`, `memory: 512` — appear without any prose explaining what these control or what alternatives exist. The mandatory style rule for unexplained tunable values applies."
          ],
          "mandatoryFixes": [
            "Shorten overlong H2 headings: rename 'When to use hooks vs deployment scripts' to 'Hooks vs deployment scripts' (or similar ≤5-word noun label), and rename 'Why hooks and scripts exist' to a short noun-form label such as 'Motivation' or 'Why use hooks'. Per the mandatory style rule, overly long H2s that read like sentences when a short label would work require a scannability deduction and a mandatory fix.",
            "Add 1-2 sentences of prose around each tunable value in the Common Patterns code examples explaining what the property controls and naming at least one alternative. Specifically: (a) `instanceSize: 'db.t4g.micro'` — note this is an RDS instance size and link to the relational database page for options; (b) `cpu: 0.25` and `memory: 512` — note these are ECS Fargate CPU/memory units and link to the web service page for valid combinations. Without this, the values read as magic or required."
          ],
          "optionalImprovements": [
            "Verify and correct the env var names in the bastion-script example. If RedisCluster named 'cache' injects different variable names (e.g. `STP_CACHE_CONNECTION_STRING` instead of `STP_CACHE_HOST`/`STP_CACHE_PORT`), update the redis-cli command accordingly. If the split host+port vars are not confirmed, use the connection string form or add a note that the exact var names depend on the resource type.",
            "Audit the 'Environment variables in scripts' section: the hook context variables (`STP_HOOK_TYPE`, `STP_HOOK_TRIGGER`, `STP_COMMAND`) are useful if real, but add a caveat or remove them if they cannot be source-confirmed. A wrong env var name is worse than a missing one.",
            "Trim or consolidate the 'Deployment scripts vs hooks' closing H2. The first two sentences restate the comparison table at the top. Keep only the new information: the automatic rollback behavior and the guidance for migration reliability. This removes ~150 words of redundancy without losing value.",
            "The 'Why hooks and scripts exist' (after renaming) section is currently mostly narrative. Consider tightening it to 2-3 concrete sentences and moving the motivation content into the opening paragraph, removing the standalone section entirely since the first paragraph already covers it well.",
            "Add a brief note in the Hooks table or prose that `beforeDelete`/`afterDelete` only fire when both `--configPath` and `--stage` are provided. This constraint is already mentioned in the table but could be reinforced with a sentence in the hook failure behavior section since a missing flag silently skips the hook."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "claude",
          "modelName": "cli-default",
          "issues": [
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "afterDelete — failure is reported as a warning; the stack is already deleted.",
              "reason": "The source code in src/index.ts shows that `continueOnError` is only set to `true` when `globalStateManager.command === 'deploy'`. For the `delete` command, `continueOnError` is `false`, meaning afterDelete hook failures are thrown as errors (not handled as non-blocking warnings). The processHooks method in event-manager/index.ts explicitly throws the error when `continueOnError` is false. While the stack IS already deleted at that point, the command exits with an error — not a warning — which is a different user experience from what the draft promises.",
              "suggestedFix": "Change to: \"afterDelete — failure causes the command to exit with an error. The stack is already deleted at this point, so no rollback occurs, but the error will be visible in CI pipelines.\" Alternatively, note that only afterDeploy hooks have the non-blocking warning behavior.",
              "evidence": [
                {
                  "file": "src/index.ts",
                  "quote": "const shouldContinueAfterHookFailure = globalStateManager.command === 'deploy';"
                },
                {
                  "file": "src/app/event-manager/index.ts",
                  "quote": "if (!continueOnError) {\n            throw error;\n          }"
                }
              ]
            },
            {
              "severity": "low",
              "type": "missing-caveat",
              "statement": "STP_HOOK_TYPE — before or after",
              "reason": "The HookType in src/utils/scripts.ts is defined as 'before' | 'after' | 'onError'. While 'onError' is not user-configurable through the Hooks interface (no onError* hooks exist), scripts can technically receive 'onError' as the STP_HOOK_TYPE value through internal error-handling paths. The draft's enumeration of only two values is incomplete.",
              "suggestedFix": "Either add 'onError' to the list with a note that it's used internally for error-handling hooks, or rephrase to 'typically before or after' to avoid implying an exhaustive list.",
              "evidence": [
                {
                  "file": "src/utils/scripts.ts",
                  "quote": "export type HookType = 'before' | 'after' | 'onError';"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "Bastion scripts run on the bastion EC2 instance, which is a minimal Amazon Linux environment. Standard Unix tools are available.",
              "reason": "The source files for bastion (bastion.d.ts and __helpers.d.ts) describe the bastion as a 'bastion server within your VPC' but do not specify the operating system as 'Amazon Linux' or characterize it as 'minimal'. The logging config references Linux paths (/var/log/messages, /var/log/secure, /var/log/audit/audit.log) which are consistent with Amazon Linux but not explicit. The claim about 'Standard Unix tools' availability is also unsupported — the bastion host could have a limited toolset.",
              "suggestedFix": "Replace with: 'Bastion scripts run on the bastion host inside your VPC. The host has basic shell tools available. If you need specific tools (like redis-cli or psql), use the bastion's runCommandsAtLaunch property to install them when the instance starts.'",
              "evidence": [
                {
                  "file": "types/stacktape-config/__helpers.d.ts",
                  "quote": "Bastion scripts are executed on a bastion server within your VPC, not on your local machine.\n * Logs from the script's execution are streamed in real-time to your terminal."
                }
              ]
            },
            {
              "severity": "low",
              "type": "missing-caveat",
              "statement": "If you need specific tools (like redis-cli or psql), install them via a preceding executeCommands entry or use a local script with bastion tunneling instead.",
              "reason": "The FAQ suggests installing tools via 'a preceding executeCommands entry' in the bastion script, which would run the installation every time the script executes. The bastion resource type has a dedicated runCommandsAtLaunch property designed for persistent tool installation at instance boot time. This is the better approach and should be mentioned.",
              "suggestedFix": "Change to: 'If you need specific tools (like redis-cli or psql), configure runCommandsAtLaunch on the bastion resource to install them when the instance starts. Alternatively, use a local script with bastion tunneling instead.'",
              "evidence": [
                {
                  "file": "types/stacktape-config/bastion.d.ts",
                  "quote": "runCommandsAtLaunch?: string[];\n  /**\n   * #### Shell commands to run when the instance starts (as root — no `sudo` needed).\n   *\n   * ---\n   *\n   * Use to install CLI tools, database clients, or other dependencies."
                }
              ]
            }
          ],
          "positiveFindings": [
            "All 8 hook lifecycle events (beforeDeploy, afterDeploy, beforeDelete, afterDelete, beforeBucketSync, afterBucketSync, beforeDev, afterDev) match the Hooks interface in __helpers.d.ts exactly.",
            "Script types (local-script, local-script-with-bastion-tunneling, bastion-script) and their property sets are accurately documented — including the correct limitation that BastionScript only supports executeCommand/executeCommands (no file-based execution).",
            "Hook entry properties (scriptName, skipOnCI, skipOnLocal) and their defaults match NamedScriptLifecycleHook and LifecycleHookBase types.",
            "pipeStdio default of true is correct per the LocalScriptProps JSDoc (@default true).",
            "Hook context environment variables (STP_HOOK_TYPE, STP_HOOK_TRIGGER, STP_COMMAND) are confirmed in src/utils/scripts.ts getScriptEnv function.",
            "assumeRoleOfResource supported resource types list matches the JSDoc enumeration in ScriptEnvProps exactly.",
            "CLI flags for script:run (--scriptName, --env, --assumeRoleOfResource, --stage) match src/config/cli/commands.ts.",
            "All cross-links (/cli/script-run, /resources/advanced/deployment-scripts, /resources/security/bastion-host, /configuration/connecting-resources, /configuration/directives) are valid routes in the navigation index.",
            "beforeDeploy failure aborting deployment and beforeDelete failure aborting deletion are both correct — before hooks use continueOnError: false (default).",
            "afterDeploy failure behavior is correctly documented — source confirms continueOnError: true only for deploy command, with warning message displayed.",
            "CodeBlock usage follows all conventions: intellisense flag present, TypeScript tab labels correct, imports hidden with focus markers, no hand-authored YAML."
          ],
          "summary": "The draft is well-grounded overall. All script types, hook lifecycle events, property names, defaults, CLI flags, and cross-links are verified against source. The one medium-severity issue: the afterDelete hook failure behavior is described as a non-blocking warning, but source code shows only the deploy command sets continueOnError: true — meaning afterDelete failures are thrown as errors. Three low-severity issues involve: an incomplete STP_HOOK_TYPE value enumeration (missing internal 'onError'), ungrounded claims about the bastion host OS, and a FAQ answer that suggests per-invocation tool installation via executeCommands rather than the purpose-built runCommandsAtLaunch property."
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the high-level source fact that Stacktape has top-level `hooks` and `scripts`, but it makes many detailed Stacktape-specific claims that are not grounded in the provided source files. The available sources only establish the existence of hooks, scripts, three script types, manual `script:run` usage, `connectTo` environment injection at a high level, and broad lifecycle categories for deploy/delete/dev.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft documents detailed script properties: `executeCommand`, `executeCommands`, `executeScript`, `executeScripts`, `connectTo`, `cwd`, `pipeStdio`, plus the rule that exactly one execution property must be used.",
              "reason": "The provided source confirms that scripts exist, can be shell commands or JS/TS/Python files, and can use `connectTo`, but it does not define these property names, defaults, or mutual-exclusion constraints. These are Stacktape-specific API claims that would directly affect configuration.",
              "suggestedFix": "Keep only the source-grounded high-level statement, or replace this section with cautious wording that says scripts can be local, local-with-bastion-tunneling, or bastion scripts and can run shell commands or JS/TS/Python files. Do not list property names or defaults unless the relevant `LocalScript`, `BastionScript`, and `LocalScriptWithBastionTunneling` type definitions are provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "scripts?: { [scriptName: string]: LocalScript | BastionScript | LocalScriptWithBastionTunneling };"
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Scripts can be shell commands or JS/TS/Python files."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft claims hooks support `beforeBucketSync` and `afterBucketSync`.",
              "reason": "The provided root config JSDoc says hooks run before/after deploy, delete, or dev commands. Bucket sync hooks are not present in the provided source, and adding them would change what users configure.",
              "suggestedFix": "Remove `beforeBucketSync` and `afterBucketSync`, or only include them if the `Hooks` type source explicitly lists those properties.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Run scripts automatically before/after deploy, delete, or dev commands."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft specifies hook failure behavior: `beforeDeploy` aborts deployment, `afterDeploy` does not roll back, `beforeDelete` aborts deletion, and `afterDelete` is reported as a warning.",
              "reason": "The provided sources do not define hook failure semantics. These claims are Stacktape-specific operational behavior and would materially change how readers design migrations and cleanup tasks.",
              "suggestedFix": "Remove the failure-behavior section or rewrite it as source-grounded only after providing CLI/runtime source that implements hook execution and error handling.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Each hook references a script defined in the `scripts` section."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft documents `stacktape script:run` flags: `--stage`, `--region`, `--env`, and `--assumeRoleOfResource`.",
              "reason": "The provided source only mentions `stacktape script:run --scriptName myScript`. It does not define the command's flags or runtime environment behavior.",
              "suggestedFix": "Keep the minimal documented invocation with `--scriptName`, or verify flags against the CLI command source before documenting them.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Run scripts with `stacktape script:run --scriptName myScript` or attach them to `hooks`."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft claims `assumeRoleOfResource` can be configured on scripts, injects temporary AWS credentials, is picked up by AWS SDKs, and supports Lambda functions, container workloads, batch jobs, and SSR frontend resources.",
              "reason": "No provided source mentions `assumeRoleOfResource`, temporary AWS credentials, supported resource types for role assumption, or AWS SDK behavior. This is detailed Stacktape-specific behavior and should not be documented from the provided sources.",
              "suggestedFix": "Remove the `Assuming IAM roles` section unless the script type definitions or CLI implementation source are added. If added, document only the exact supported property and resource list shown by those sources.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "scripts?: { [scriptName: string]: LocalScript | BastionScript | LocalScriptWithBastionTunneling };"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft claims hook context variables `STP_HOOK_TYPE`, `STP_HOOK_TRIGGER`, and `STP_COMMAND` are injected when a script runs as a hook.",
              "reason": "The provided sources do not mention hook context environment variables. Environment variable injection is only described generally for script `connectTo`.",
              "suggestedFix": "Remove the hook context variable list, or verify it against the runtime hook execution source before documenting it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Use `connectTo` in a script to auto-inject database URLs, API keys, etc. as environment variables."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft compares hooks with deployment scripts, including claims that deployment scripts run as AWS Lambda, support only `after:deploy` and `before:delete`, have a 15-minute timeout, use `joinDefaultVpc`, and trigger CloudFormation rollback on `after:deploy` failure.",
              "reason": "The provided source files do not define the DeploymentScript resource or its behavior. The comparison may be correct in a broader source set, but it is unsupported by the sources provided for this verification.",
              "suggestedFix": "Either remove the detailed deployment-script comparison or add the DeploymentScript type/runtime source. With the current source set, link to `/resources/advanced/deployment-scripts` without asserting its exact timeout, hooks, VPC property, or rollback behavior.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "scripts?: { [scriptName: string]: LocalScript | BastionScript | LocalScriptWithBastionTunneling };"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft claims `skipOnCI` and `skipOnLocal` are supported on hook entries, including detection of CodeBuild, GitHub Actions, and GitLab CI.",
              "reason": "The provided source only says each hook references a script defined in `scripts`. It does not define hook entry properties or CI/local detection behavior.",
              "suggestedFix": "Remove the conditional execution section unless the `Hooks` type or hook runtime implementation source is available. If supported, document only the exact flags and CI detection rules shown by source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Each hook references a script defined in the `scripts` section."
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft correctly uses `<CodeBlock intellisense>` with a `TypeScript` tab for Stacktape configuration examples and does not hand-author YAML Stacktape configuration.",
            "The draft correctly links existing documented routes such as `/cli/script-run`, `/configuration/connecting-resources`, `/configuration/directives`, `/resources/security/bastion-host`, and `/resources/advanced/deployment-scripts`.",
            "The draft's top-level framing that Stacktape has hooks and scripts, and that scripts can be run manually or attached to hooks, is grounded in `_root.d.ts`."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft covers the two root-level concepts the provided source exposes (`hooks` and `scripts`) and names all three script variants listed in the source. Because the primary type is unknown and the provided files do not include the detailed `Hooks`, `LocalScript`, `BastionScript`, or CLI option definitions, most deep behavioral details cannot be assessed as API-completeness issues from this source set.",
          "issues": [
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "The `Local script` example defines `connectTo: ['mainDatabase']` but only declares `api` in `resources`.",
              "reason": "The example is presented as copyable Stacktape config, but it references a resource name that is not defined in the same config. The root config source says scripts use `connectTo` to inject details and resources are named entries under `resources`; this example does not include the referenced resource.",
              "suggestedFix": "Either add a `mainDatabase` resource to the example and return it in `resources`, or remove `connectTo: ['mainDatabase']` and the database-specific env-var claim from this basic local-script example.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "resources: { [resourceName: string]: StacktapeResourceDefinition };"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "The `Hooks` example defines the `migrateDb` script with `connectTo: ['mainDatabase']` but only returns `resources: { api }`.",
              "reason": "The example demonstrates hook attachment, but the script references an undeclared resource. That makes the example weak as a canonical hooks example because a reader cannot copy it as-is and because the source frames `connectTo` around named resources in the config.",
              "suggestedFix": "Make the hooks example internally consistent by adding a `mainDatabase` resource, or simplify the hook example to a script that does not require `connectTo`.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Each hook references a script defined in the `scripts` section."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "resources: { [resourceName: string]: StacktapeResourceDefinition };"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft gives detailed CLI flags for `stacktape script:run`, including `--stage`, `--region`, `--env`, and `--assumeRoleOfResource`.",
              "reason": "The provided source only supports the existence of `stacktape script:run --scriptName myScript`; it does not include CLI source or option definitions for these additional flags. As an API-completeness auditor, I cannot verify these options from the supplied source files.",
              "suggestedFix": "Either provide the CLI source for `script:run` and verify the flags, or narrow this section to the sourced command shape and link to `/cli/script-run` for option details.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Run scripts with `stacktape script:run --scriptName myScript` or attach them to `hooks`."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft states exact hook failure behavior for `beforeDeploy`, `afterDeploy`, `beforeDelete`, and `afterDelete`.",
              "reason": "The provided source says hooks run automatically before/after deploy, delete, or dev commands, but it does not define abort, warning, rollback, or sequential failure semantics. These may be true, but they are not grounded in the supplied source set.",
              "suggestedFix": "Provide runtime hook implementation source and verify the behavior, or rewrite this section cautiously: hooks run around lifecycle commands, and users should consult command output or CLI reference for failure handling.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Run scripts automatically before/after deploy, delete, or dev commands."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft says hook entries support `skipOnCI` and `skipOnLocal`.",
              "reason": "The supplied source references a `Hooks` type but does not include its definition, so these per-hook fields are not verifiable from the provided files. This is a documented API surface claim, not merely explanatory prose.",
              "suggestedFix": "Add the source definition for `Hooks` and hook entry properties, or remove the conditional-execution section until those fields can be grounded.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "hooks?: Hooks;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft claims scripts receive hook context variables `STP_HOOK_TYPE`, `STP_HOOK_TRIGGER`, and `STP_COMMAND`.",
              "reason": "The source supports `connectTo`-injected environment variables at a high level, but it does not mention hook context variables. Environment-variable names are API details and need source grounding.",
              "suggestedFix": "Provide the runtime source that injects these variables, or remove the hook context variable list and keep only the sourced `connectTo` environment-variable behavior.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Use `connectTo` in a script to auto-inject database URLs, API keys, etc. as environment variables."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft lists supported resource types for `assumeRoleOfResource`.",
              "reason": "The provided source files do not mention `assumeRoleOfResource` or any supported resource-type list. This is a security-relevant API claim and should be grounded in script type definitions or CLI/runtime source.",
              "suggestedFix": "Provide the `LocalScript`/script execution type source that defines `assumeRoleOfResource`, or remove the supported-resource list and defer role-assumption details to the CLI/API reference.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "scripts?: { [scriptName: string]: LocalScript | BastionScript | LocalScriptWithBastionTunneling };"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft mentions both root-level fields exposed by the provided config source: `hooks` and `scripts`.",
            "The draft acknowledges all three script variants named in `_root.d.ts`: `local-script`, `local-script-with-bastion-tunneling`, and `bastion-script`.",
            "The Stacktape configuration examples use `<CodeBlock intellisense>` with a `TypeScript` tab instead of hand-authored YAML or fenced TypeScript config blocks."
          ]
        },
        {
          "verifierId": "code-block-validator",
          "modelProvider": "deterministic",
          "modelName": "local",
          "summary": "Deterministic check of <CodeBlock intellisense> Stacktape config examples and generated MDX component usage.",
          "issues": [
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #2 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #2 (TypeScript): `new RdsEnginePostgresql16(...)` references an unknown class.",
              "reason": "`RdsEnginePostgresql16` is not exported by the `stacktape` package.",
              "suggestedFix": "Use one of the documented Stacktape resource classes (e.g. LambdaFunction, WebService, RelationalDatabase). If `RdsEnginePostgresql16` is meant to be a custom class, define it in the example or remove the example."
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #7 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #7 (TypeScript): `new RdsEnginePostgresql16(...)` references an unknown class.",
              "reason": "`RdsEnginePostgresql16` is not exported by the `stacktape` package.",
              "suggestedFix": "Use one of the documented Stacktape resource classes (e.g. LambdaFunction, WebService, RelationalDatabase). If `RdsEnginePostgresql16` is meant to be a custom class, define it in the example or remove the example."
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #9 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #9 (TypeScript): `new RdsEnginePostgresql16(...)` references an unknown class.",
              "reason": "`RdsEnginePostgresql16` is not exported by the `stacktape` package.",
              "suggestedFix": "Use one of the documented Stacktape resource classes (e.g. LambdaFunction, WebService, RelationalDatabase). If `RdsEnginePostgresql16` is meant to be a custom class, define it in the example or remove the example."
            }
          ],
          "positiveFindings": []
        }
      ],
      "status": "failed",
      "passed": false
    },
    {
      "iteration": 2,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\configuration__hooks-and-scripts\\iteration-2.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 7,
            "completeness": 8,
            "practicalUsefulness": 8,
            "audienceFit": 8
          },
          "strengths": [
            "Comparison table for the three script types (local-script, local-script-with-bastion-tunneling, bastion-script) immediately anchors the decision — reader knows which to reach for before reading any example.",
            "All three script types have working, realistic code examples with concrete real-world scenarios (warm-cache, seed-data, redis-cli) — not toy examples.",
            "Hooks lifecycle event table is exhaustive, well-labelled, and actionable — the 'Common use' column is especially helpful for a first-time reader.",
            "Decision support is present throughout: explicit 'use X when Y, use Z when W' guidance appears in the script-type table, the bastion tunneling section, and the FAQ.",
            "FAQ covers all three required categories: Stacktape-specific questions, underlying AWS/runtime questions (TypeScript/Python execution), and comparison questions (hooks vs deployment scripts, bastion tunneling vs bastion script). Ten entries, none synthetic.",
            "The `skipOnCI` / `skipOnLocal` conditional execution section explains both flags and pairs them with the worked example in the hooks code block — so the feature is introduced, demonstrated, and applied in one flow.",
            "The `assumeRoleOfResource` section appropriately marks itself as an advanced feature, names the credential injection mechanism, and lists the constraint (resource must be deployed first)."
          ],
          "problems": [
            "Internal env-var inconsistency: the prose after the first `local-script` code block says `STP_UPLOADS_BUCKET_NAME`, but the 'Environment variables in scripts' section says connecting to a Bucket named `uploads` injects `STP_UPLOADS_NAME` and `STP_UPLOADS_ARN`. One of these names is wrong. This is a factual inconsistency within the same page that will confuse a first-time reader trying to access the variable in their code.",
            "H2 'When to use deployment scripts instead' is 6 words and reads like a guidance phrase rather than a compact noun-form label. The style guide requires 1-5 word H2 labels. This section heading violates the overly-long-heading rule and drags down scannability.",
            "The 'Environment variables in scripts' table lists variable suffix forms (NAME, ARN, HOST, etc.) without the `STP_[RESOURCE_NAME]_` prefix, making it ambiguous whether readers should prepend the prefix themselves. The prose example above gives `STP_UPLOADS_NAME` but the table reads as if NAME alone is the variable name."
          ],
          "mandatoryFixes": [
            "Fix the env-var name inconsistency: the prose after the first code block says `STP_UPLOADS_BUCKET_NAME`; the 'Environment variables in scripts' section says `STP_UPLOADS_NAME`. Verify the correct variable name from the source (the `connectTo` JSDoc in `types/stacktape-config/__helpers.d.ts`) and use it consistently throughout the page, including in both locations and in the connectTo table.",
            "Shorten the H2 'When to use deployment scripts instead' to a compact 1–5 word noun-form label, e.g. 'Deployment scripts' or 'vs Deployment scripts'. Per the style guide, H2 headings must not read like full sentences or guidance phrases."
          ],
          "optionalImprovements": [
            "In the 'Environment variables in scripts' table, show full `STP_[RESOURCE_NAME]_` prefixed column headers or a header note like '(prefixed with STP_[RESOURCE_NAME]_)' so readers don't have to mentally assemble the variable name themselves.",
            "The 'Assuming IAM roles' section lists a specific set of supported resource types (`function`, `batch-job`, `worker-service`, etc.). Consider a one-line note pointing to the API reference or source for the authoritative list, since this enumeration is hard to keep current in prose.",
            "Add a one-sentence note in the `executeScript` / `executeScripts` description about how the runtime is resolved (e.g., Node for .ts files, Python interpreter for .py files) — the current FAQ mentions 'determined by `defaults:configure` or the system default' but this is buried; a single sentence in the main Scripts section would reduce confusion for first-time users.",
            "The `<Warning>` about afterDeploy hook failures could add one sentence about writing idempotent migration scripts — this is a common gotcha (re-running a non-idempotent migration breaks data) that a first-time reader will hit."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 9,
            "completeness": 7,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "All three script types are covered with equal depth — each has its own example, a clear when-to-use signal, and explicit tradeoffs (e.g. 'Bastion scripts are limited to tools pre-installed on the bastion host — for most workflows, bastion tunneling is the better choice'). No variant is treated as a one-liner afterthought.",
            "The lifecycle events table (beforeDeploy through afterDev with 'Common use' column) is immediately actionable for an engineer wiring up automation — covers every hook without requiring them to enumerate the CLI docs.",
            "The FAQ spans all three required categories: Stacktape-specific, AWS/service-level, and comparison/choosing. Ten well-chosen questions; none feel padded.",
            "The 'skipOnCI / skipOnLocal' section explicitly explains BOTH flags with concrete scenarios ('opening a browser' vs. 'uploading test reports'), which is exactly the kind of detail engineers get wrong the first time.",
            "The 'When to use deployment scripts instead' section provides a genuine comparison rather than just linking away, helping engineers avoid over-engineering with a deployment-script resource when a local script is sufficient."
          ],
          "problems": [
            "INTERNAL INCONSISTENCY — env var naming: The prose in the Local script section says connectTo injects 'STP_UPLOADS_BUCKET_NAME', but the Environment variables table lists Bucket as injecting 'NAME' and 'ARN' (which would map to STP_UPLOADS_NAME and STP_UPLOADS_ARN). An engineer configuring scripts will use one of these and their script will silently fail. Both cannot be correct.",
            "UNGROUNDED specifics in assumeRoleOfResource: The page provides a specific enumerated list of supported resource types for role assumption ('function', 'batch-job', 'worker-service', 'web-service', 'private-service', 'multi-container-workload', plus all SSR types). This list is not in any provided source file. If any entry is missing or wrong, engineers debugging a broken script will waste significant time.",
            "UNGROUNDED constraint claim: 'Each script must define exactly one execution method' is stated as a hard rule, but the LocalScript/BastionScript type definitions are not in the provided source files — this can't be verified and, if wrong, misleads engineers about what configurations are valid.",
            "UNGROUNDED defaults: 'pipeStdio defaults to true' and 'cwd on bastion scripts defaults to /' are stated as facts not present in the provided source. Engineers relying on these defaults in production configs will be surprised if they're wrong.",
            "The env vars table (Bucket, RelationalDatabase, RedisCluster, DynamoDbTable, SqsQueue with specific variable names) is stated without source grounding — the type definitions for these connectTo injections are not in the provided source files. The Bucket row is already internally contradicted by the prose."
          ],
          "mandatoryFixes": [
            "Fix the internal contradiction between the prose ('STP_UPLOADS_BUCKET_NAME') and the Environment variables table ('NAME', 'ARN' for Bucket — implying STP_UPLOADS_NAME). Verify the correct variable name suffix from the connectTo JSDoc in types/stacktape-config/__helpers.d.ts and use it consistently in both locations. This is a concrete wrong claim that will break engineer scripts.",
            "Either ground the assumeRoleOfResource supported resource types list against the LocalScript type definition, or rewrite the sentence to say 'the resource must be a workload type (Lambda function, container service, batch job, or SSR frontend) — check the API reference for the complete list.' An enumerated list that may be wrong or incomplete in this context is worse than a directive to check the source."
          ],
          "optionalImprovements": [
            "Replace or prominently caveat the env vars table with a note that the connecting-resources page is the authoritative source for injected variable names — a partial inline table with potentially wrong names creates a maintenance hazard. Consider showing only one verified example (e.g., RelationalDatabase → CONNECTION_STRING) and linking to the full reference rather than listing all resource types.",
            "Add one sentence about hook failure propagation: if a script in a multi-script hook exits non-zero, does the subsequent scripts run or does the hook abort? Engineers adding multi-step afterDeploy hooks (e.g., migrate then seed) need this behavior to be explicit. The FAQ only addresses 'what to do if afterDeploy fails' — not whether subsequent scripts in the same hook are affected.",
            "The 'executeScript runtime is determined by defaults:configure' claim should be qualified with where to find the current defaults, since engineers setting up a CI pipeline need to know the fallback behavior when no default is configured.",
            "The Info callout about beforeDelete/afterDelete only running when --configPath and --stage are provided could be elevated to a Warning — engineers who skip those flags and expect cleanup scripts to run will get silent non-execution with no error message."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 9,
            "completeness": 8,
            "practicalUsefulness": 9,
            "audienceFit": 9
          },
          "strengths": [
            "Answer-first paragraph is concise (under 75 words) and self-contained — explains both concepts (scripts and hooks) and their relationship without meta-framing.",
            "Script type comparison table at the top of the Scripts section immediately orients the reader before any prose deep-dive — one of the most extractable elements on the page.",
            "All three script types (local-script, local-script-with-bastion-tunneling, bastion-script) are documented with roughly equal depth: an H3 header, a realistic code example, and prose covering tradeoffs and constraints. No variant is buried in a one-liner.",
            "Lifecycle events table with 8 rows is comprehensive and scannable; each row names the hook, firing moment, and common use case — ideal for AI snippet extraction.",
            "Decision support is present throughout: the script type table has a 'Best for' column, the skipOnCI/skipOnLocal section explains each flag's scenario, and the 'When to use deployment scripts instead' section closes the loop for the natural comparison.",
            "FAQ spans all three required categories: Stacktape-specific questions (how do I run a migration?), use-case questions (what happens if afterDeploy fails?), and comparison questions (hooks vs deployment scripts?, bastion tunneling vs bastion script?). 10 pairs, no filler.",
            "No forbidden patterns detected: no fenced YAML Stacktape config, no StacktapeConfig type import, no fenced TypeScript with defineConfig, no marketing language, no multi-command bash blocks.",
            "assumeRoleOfResource section names the concrete list of supported resource types and explains the credential injection mechanism — actionable and specific.",
            "All CodeBlock examples use the correct TypeScript + defineConfig + class-based form with focus markers and imports hidden from rendered view."
          ],
          "problems": [
            "The environment variable table in 'Environment variables in scripts' shows partial variable suffixes (NAME, ARN, CONNECTION_STRING, HOST, PORT, etc.) without the STP_[RESOURCE_NAME]_ prefix in each row. Although the preceding prose states the pattern, the table column header 'Injected variables' implies these are the complete names. A reader extracting the table alone will be confused about whether to type `NAME` or `STP_UPLOADS_NAME`. The column header or each value should make the partial-suffix nature explicit.",
            "The 'Running scripts manually' section consists of a single sentence, one code block, and a second sentence linking to the CLI reference. By AEO and completeness standards this is too thin — it has no prose about common flags (--stage, --region), no mention of what output looks like, and no decision context. It reads as a placeholder rather than a section.",
            "The CLI link to `/cli/script-run` referenced in 'Running scripts manually' and the FAQ appears to be ungrounded — the git status shows `deployment-script-run.mdx` but no `script-run.mdx` or equivalent. If this page does not exist, the link is broken and reduces practical usefulness.",
            "The bastion script code example uses `$STP_CACHE_HOST` and `$STP_CACHE_PORT` inline in a shell command inside `executeCommand`. The page never explains that these variables (injected by `connectTo: ['cache']`) are available in shell commands as standard environment variables. A reader unfamiliar with the connectTo injection mechanism may not know how the variable gets there at runtime.",
            "No `<ApiReference>` component is present. While concept pages may omit it when no clear root type exists, the `scripts` and `hooks` top-level config sections are configurable types. Omitting the component entirely means readers have no drill-down path to the full property reference from this page — they must navigate elsewhere to discover options like `pipeStdio`, `workingDir`, or specific hook entry properties."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "In the environment variable table, rename the 'Injected variables' column to 'Injected variable suffixes' (or similar) and add a table caption or note: 'Each suffix is prefixed with STP_[RESOURCE_NAME]_ at runtime — e.g., STP_UPLOADS_NAME for a resource named uploads.' This prevents misreading the table as complete variable names.",
            "Expand 'Running scripts manually' to 3-5 sentences: note that `--stage` and `--region` are required when targeting a non-default stack, mention that script output is printed to the terminal by default, and briefly note that non-zero exit codes surface as CLI errors. Also verify and correct the /cli/script-run link target.",
            "In the bastion script example, add a sentence after the code block explaining that environment variables injected by `connectTo` (like `STP_CACHE_HOST`) are available as standard shell environment variables inside the command string, the same way they are in any shell process.",
            "Consider adding a one-line ApiReference at the bottom of the page pointing to the Hooks and/or LocalScript props type so readers can drill into the full property list without navigating away. If the definitionName is available in the generated schema (e.g. `HooksProps`, `LocalScriptProps`), one compact ApiReference would cover it.",
            "The `cwd` property on the `buildFrontend` script example is used but not explained in surrounding prose. Add a single sentence noting that `cwd` sets the working directory for the script process and defaults to the project root — this fulfills the property-value-anchor rule for non-obvious values.",
            "The 'beforeBucketSync' and 'afterBucketSync' hooks in the lifecycle table are less commonly known than the deploy/delete hooks. A short parenthetical noting that these fire during `stacktape bucket:sync` would help readers who may not recognize the connection between the hook name and the CLI command."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "claude",
          "modelName": "cli-default",
          "issues": [
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "The `seedBucket` script uses `connectTo` to receive the same bucket connection details as the `api` function — injected as environment variables like `STP_UPLOADS_BUCKET_NAME`.",
              "reason": "The ScriptEnvProps connectTo JSDoc documents Bucket as injecting `NAME` and `ARN`. With the pattern `STP_[RESOURCE_NAME]_[VARIABLE_NAME]`, a resource named `uploads` produces `STP_UPLOADS_NAME` and `STP_UPLOADS_ARN` — not `STP_UPLOADS_BUCKET_NAME`. The draft is internally inconsistent: the 'Environment variables in scripts' section later correctly states `STP_UPLOADS_NAME`, but the prose after the first code example uses the wrong name `STP_UPLOADS_BUCKET_NAME`.",
              "suggestedFix": "Change `STP_UPLOADS_BUCKET_NAME` to `STP_UPLOADS_NAME` in the prose after the first code example.",
              "evidence": [
                {
                  "file": "types/stacktape-config/__helpers.d.ts",
                  "quote": "Environment variable names are in the format `STP_[RESOURCE_NAME]_[VARIABLE_NAME]` (e.g., `STP_MY_DATABASE_CONNECTION_STRING`)."
                },
                {
                  "file": "types/stacktape-config/__helpers.d.ts",
                  "quote": "- **`Bucket`**: `NAME`, `ARN`"
                }
              ]
            }
          ],
          "positiveFindings": [
            "All 8 hook lifecycle events (beforeDeploy, afterDeploy, beforeDelete, afterDelete, beforeBucketSync, afterBucketSync, beforeDev, afterDev) are correctly enumerated and match the Hooks interface in __helpers.d.ts.",
            "The three script types (local-script, local-script-with-bastion-tunneling, bastion-script) are accurately described, including type discriminator strings matching the source.",
            "The four local script execution methods (executeCommand, executeCommands, executeScript, executeScripts) and their mutual exclusivity are correctly documented per the LocalScriptProps source.",
            "The claim that bastion scripts only support executeCommand and executeCommands is correct — BastionScriptProps has only those two properties and lacks executeScript/executeScripts.",
            "Default values are all correctly sourced: pipeStdio defaults to true (@default true), bastion cwd defaults to '/' (@default '/'), skipOnCI defaults to false (@default false), skipOnLocal defaults to false (@default false).",
            "The delete hook caveat ('only runs when --configPath and --stage are provided to the delete command') is verbatim from the Hooks interface JSDoc for both beforeDelete and afterDelete.",
            "The connectTo environment variable table (Bucket, RelationalDatabase, RedisCluster, DynamoDbTable, SqsQueue) matches the ScriptEnvProps connectTo JSDoc precisely.",
            "The assumeRoleOfResource supported resource types list is complete and exactly matches the ScriptEnvProps JSDoc (function, batch-job, worker-service, web-service, private-service, multi-container-workload, and all 7 SSR frontend types).",
            "All cross-links resolve to valid pages in the documentation navigation index — no invented routes.",
            "Code examples use correct class names from the valid whitelist (LambdaFunction, Bucket, RedisCluster, Bastion, HostingBucket, StacktapeLambdaBuildpackPackaging) with proper defineConfig + export default structure and focus markers."
          ],
          "summary": "The draft is exceptionally well-grounded. Every major claim — script types, execution methods, hook lifecycle events, defaults, environment variable injection patterns, and assumeRoleOfResource — was verified against the actual TypeScript type definitions in __helpers.d.ts and matches the source accurately. Only one factual error was found: the prose after the first code example uses `STP_UPLOADS_BUCKET_NAME` when the correct name per the documented `STP_[RESOURCE_NAME]_[VARIABLE]` pattern and Bucket's `NAME` variable is `STP_UPLOADS_NAME`. The draft is internally inconsistent here, as the later 'Environment variables in scripts' section correctly uses `STP_UPLOADS_NAME`. All cross-links, code examples, and structural elements are valid."
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the two available source files on the existence of top-level `scripts`, `hooks`, the three script types, and manual `script:run`. However, the provided sources are thin. Several detailed claims about hook event names, script execution properties, environment variable names, bastion behavior, role assumption, and runtime selection are not grounded in the supplied source files and should be narrowed or removed unless additional authoritative source files are provided.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft documents specific hook names and bucket-sync hooks: `beforeDeploy`, `afterDeploy`, `beforeDelete`, `afterDelete`, `beforeBucketSync`, `afterBucketSync`, `beforeDev`, and `afterDev`.",
              "reason": "The supplied source only says hooks run scripts automatically before/after deploy, delete, or dev commands. It does not expose the `Hooks` type or support the specific property names, and it does not mention bucket sync hooks at all. This is Stacktape-specific configuration shape that affects what readers write.",
              "suggestedFix": "Either provide and verify against the `Hooks` type source, or narrow this section to the sourced statement: hooks reference scripts and run before/after deploy, delete, or dev commands. Remove bucket sync hook names unless backed by the actual type source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Run scripts automatically before/after deploy, delete, or dev commands."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Each hook references a script defined in the `scripts` section."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft states each script must define exactly one execution method and lists `executeCommand`, `executeCommands`, `executeScript`, and `executeScripts`, including claims about separate processes and bastion-script limitations.",
              "reason": "The available source confirms scripts can be shell commands or JS/TS/Python files, but it does not define these property names, mutual exclusivity, process behavior, or bastion-script execution-method restrictions. These are Stacktape-specific API claims that readers would copy into config.",
              "suggestedFix": "Remove or substantially narrow the execution-method details unless the `LocalScript`, `BastionScript`, and `LocalScriptWithBastionTunneling` type definitions are provided and verify these properties.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Scripts can be shell commands or JS/TS/Python files."
                },
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "scripts?: { [scriptName: string]: LocalScript | BastionScript | LocalScriptWithBastionTunneling };"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft gives concrete `connectTo` environment variables for scripts, including `STP_UPLOADS_BUCKET_NAME`, `STP_UPLOADS_NAME`, `STP_UPLOADS_ARN`, relational database variables, Redis variables, DynamoDB variables, and SQS variables.",
              "reason": "The provided source only says `connectTo` auto-injects database URLs, API keys, etc. It does not include the resource-specific injection table or the exact variable names. The draft also conflicts with itself for buckets by saying `STP_UPLOADS_BUCKET_NAME` in one place and `STP_UPLOADS_NAME` in another.",
              "suggestedFix": "Keep only the general sourced claim that `connectTo` injects resource connection details as environment variables, and link to `/configuration/connecting-resources`. Do not list exact variable names unless the authoritative `ResourceAccessProps.connectTo` source is included.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Use `connectTo` in a script to auto-inject database URLs, API keys, etc. as environment variables."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft states `local-script-with-bastion-tunneling` automatically adjusts `connectTo` environment variables to local tunneled endpoints and that scripts connect to `localhost` on a forwarded port.",
              "reason": "The supplied source says this script type runs locally and tunnels connections to VPC-only resources through a bastion host, but it does not specify how environment variables are rewritten, whether localhost is used, or whether ports are forwarded into injected variables.",
              "suggestedFix": "Narrow the wording to the sourced behavior: the script runs locally and tunnels connections to VPC-only resources through a bastion host. Avoid claiming automatic env-var rewriting or localhost endpoints unless supported by the relevant script implementation or type docs.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "**`local-script-with-bastion-tunneling`**: Runs locally but tunnels connections to VPC-only\n   *   resources (e.g., private databases) through a bastion host."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "The draft documents `assumeRoleOfResource`, temporary AWS credential injection, required deployment timing, and a specific supported resource-type list for role assumption.",
              "reason": "None of these role-assumption properties or behaviors appear in the supplied source files. This is a significant Stacktape-specific behavior claim that affects security and script execution.",
              "suggestedFix": "Remove the `Assuming IAM roles` section unless the authoritative script type or runtime source is provided. If source is added, verify the property name, credential behavior, and supported resource list exactly.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "scripts?: { [scriptName: string]: LocalScript | BastionScript | LocalScriptWithBastionTunneling };"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft claims `pipeStdio` defaults to `true`, `cwd` is supported for local scripts, and bastion script `cwd` defaults to `/`.",
              "reason": "The provided source does not define `pipeStdio`, `cwd`, or any defaults for script output or working directory. Defaults are high-risk documentation claims because readers rely on them when debugging automation.",
              "suggestedFix": "Remove these defaults and property claims unless the `LocalScript` and `BastionScript` type definitions or validators are available. If kept, cite the exact default source in the page generation inputs.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "scripts?: { [scriptName: string]: LocalScript | BastionScript | LocalScriptWithBastionTunneling };"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The draft says hook entries support `skipOnCI` and `skipOnLocal`, both default to `false`, and lists CI systems such as CodeBuild, GitHub Actions, and GitLab CI.",
              "reason": "The supplied source does not define hook-entry properties, defaults, or CI detection behavior. This is Stacktape-specific configuration behavior and should not be documented from inference.",
              "suggestedFix": "Remove the conditional execution section or verify it against the `Hooks` type and hook execution source. Avoid naming CI providers unless the detection logic or docs source supports them.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "hooks?: Hooks;"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The FAQ claims local scripts determine JS/TS/Python runtimes from `defaults:configure` or system defaults, with `node` for JS/TS and `python` for Python.",
              "reason": "The source only says scripts can be JS/TS/Python files. It does not mention runtime selection, `defaults:configure`, or default executables. This could mislead readers trying to run scripts in CI.",
              "suggestedFix": "Keep the sourced statement that scripts can be JS/TS/Python files. Remove runtime-selection details unless backed by CLI defaults source or script runner implementation.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\types\\stacktape-config\\_root.d.ts",
                  "quote": "Scripts can be shell commands or JS/TS/Python files."
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft uses `defineConfig` and `<CodeBlock intellisense>` for Stacktape configuration examples, avoiding the forbidden plain-object and hand-authored YAML patterns.",
            "The draft correctly identifies the three script types listed in `_root.d.ts`: `local-script`, `local-script-with-bastion-tunneling`, and `bastion-script`.",
            "The draft links only to routes present in the supplied documentation navigation index.",
            "The draft’s first paragraph clearly answers what hooks and scripts are for without meta-framing."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "No API-completeness, union-variant coverage, or code-example demonstrativeness issues found within the provided source scope. The primary typeName is unknown, and the provided source only exposes the top-level `hooks` and `scripts` entries plus the three script variants, all of which the draft covers.",
          "issues": [],
          "positiveFindings": [
            "The draft covers both top-level StacktapeConfig concepts relevant to this page: `hooks` and `scripts`.",
            "The draft names all script variants visible in the provided source: `local-script`, `local-script-with-bastion-tunneling`, and `bastion-script`.",
            "Each Stacktape config example uses `<CodeBlock intellisense>` with a `TypeScript` tab and demonstrates the section topic it appears under.",
            "Shell examples use a single command per fenced block."
          ]
        }
      ],
      "status": "needs-human-review",
      "passed": false
    }
  ],
  "agentModelAssignments": {
    "page-writer": "claude:cli-default",
    "first-time-user-reviewer": "claude:claude-sonnet-4-6",
    "production-engineer-reviewer": "claude:claude-sonnet-4-6",
    "ai-consumer-reviewer": "claude:claude-sonnet-4-6",
    "factual-accuracy-verifier": "claude:cli-default",
    "source-grounding-verifier": "codex:cli-default",
    "api-completeness-auditor": "codex:cli-default",
    "code-block-validator": "local"
  },
  "finalOutputPath": "C:\\Projects\\stacktape\\docs\\docs\\configuration\\hooks-and-scripts.mdx",
  "outcome": "needs-human-review",
  "pipelineStatus": "needs-human-review",
  "pipelineReviewSummary": "reviewer-avg 7.9, hard blockers 6, advisory high 0, verifier medium 3"
}
