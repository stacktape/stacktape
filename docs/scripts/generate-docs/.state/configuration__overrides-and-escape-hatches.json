{
  "pageId": "configuration__overrides-and-escape-hatches",
  "pageRoute": "configuration/overrides-and-escape-hatches",
  "startedAt": "2026-05-16T17:07:31.966Z",
  "updatedAt": "2026-05-22T01:20:55.590Z",
  "iterations": [
    {
      "iteration": 1,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\configuration__overrides-and-escape-hatches\\iteration-1.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 6,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 4,
            "audienceFit": 7
          },
          "strengths": [
            "Clear 'When to use' / 'When NOT to use' framing appears early and is concrete — exactly what a first-timer needs before reaching for an escape hatch.",
            "The overrides-vs-transforms comparison table is well-structured and covers the dimensions a reader actually cares about (syntax, config format, use case, merge behavior).",
            "FAQ section spans all three required categories (Stacktape-specific, AWS-service-level, choosing/comparison) and answers are appropriately concise.",
            "No forbidden YAML Stacktape config blocks; all Stacktape config examples correctly use <CodeBlock intellisense> with TypeScript.",
            "Best-practices section gives opinionated, actionable guidance rather than generic boilerplate.",
            "compile-template workflow is presented early and tied to verification — a practical safety net for beginners.",
            "CDK construct section correctly explains when CDK is preferable over raw CloudFormation, with a real example class and config wiring."
          ],
          "problems": [
            "FACTUAL INACCURACY — MATERIAL: The draft invents a 'user-friendly alias' system for override/transform keys (e.g. `lambda`, `lambdaRole`, `lambdaLogGroup`, `ecsTaskDefinition`, `dbInstanceParameterGroup`). All provided source files consistently use the `Stp`-prefixed CloudFormation logical names (`StpLambdaFunction`, `StpLambdaFunctionRole`, `StpEcsTaskDefinition`, `StpRdsParameterGroup`, etc.). The claim 'In TypeScript configs using defineConfig, you can also use user-friendly property names' has zero source support. If users copy-paste the CodeBlock examples using key `lambda` or `ecsTaskDefinition`, the overrides will silently fail or error because those are not valid child-resource logical names.",
            "All three Stacktape CodeBlock examples use the invented alias keys in the override/transform arguments — the basic override uses `lambda:`, the dot-notation override uses `dbInstanceParameterGroup:`, and the container transform uses `ecsTaskDefinition:`. All three should use the `Stp`-prefixed names the source shows.",
            "The 'Common child resource names by resource type' table contains two columns: the invented 'user-friendly name' column and the 'AWS resource type' column. The entire middle column is fabricated. Source shows the real logical names are `StpLambdaFunction`, `StpEcsTaskDefinition`, `StpRdsInstance`, `StpRdsParameterGroup`, `StpS3Bucket`, `StpHttpApi`, etc.",
            "The source files mention `stacktape stack-info --stage dev --region us-east-1 --detailed` as the primary way to inspect child resource names, but the draft omits it entirely and only mentions compile-template. This is an omission from a higher-priority source."
          ],
          "mandatoryFixes": [
            "Replace all invented user-friendly alias keys in CodeBlock examples with the actual `Stp`-prefixed logical names from source: `lambda` → `StpLambdaFunction`, `ecsTaskDefinition` → `StpEcsTaskDefinition`, `dbInstanceParameterGroup` → `StpRdsParameterGroup`. Every CodeBlock using an override or transform key must use the source-grounded logical name.",
            "Remove the 'user-friendly names' column from the 'Common child resource names' table and replace it with the actual `Stp`-prefixed logical names from the source (e.g. `StpLambdaFunction`, `StpLambdaFunctionRole`, `StpLambdaFunctionLogGroup`, `StpEcsService`, `StpEcsTaskDefinition`, `StpRdsInstance`, `StpRdsParameterGroup`, `StpS3Bucket`, `StpHttpApi`, `StpElastiCacheReplicationGroup`, `StpElastiCacheParameterGroup`). Remove the prose paragraph claiming TypeScript configs support user-friendly aliases — this feature has no source support.",
            "Add the `stacktape stack-info --stage dev --region us-east-1 --detailed` command to the 'Finding child resource logical names' section as the primary discovery method. The source curated docs list it first; compile-template is the secondary option."
          ],
          "optionalImprovements": [
            "The 'Why this exists' H2 section restates most of what the opening paragraph already says. Consider folding it into the intro as a second sentence and removing the standalone section to reduce page weight.",
            "The override dot-notation example sets both `Parameters.max_connections` and `Parameters.shared_buffers` using dot-notation keys. A brief prose sentence explaining that the dot notation sets the value at the given path rather than replacing the whole `Parameters` object would help beginners understand the merge semantics.",
            "The CDK section mentions `$ResourceParam()` and `$Secret()` directives without linking to the directives page. The CloudFormation section links it; the CDK section should match.",
            "The Tip callout ('You can always fall back to using the full CloudFormation logical name...') would be more useful placed before the user-friendly-names content, not after — once the mandatory fix removes the user-friendly alias concept, this tip likely becomes the primary guidance and should be positioned accordingly.",
            "Consider adding `stacktape stack-info --detailed` output as a short code snippet (not a fenced command block, just the relevant excerpt) so readers know what to look for when scanning for child resource names."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 6,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 5,
            "audienceFit": 7
          },
          "strengths": [
            "Well-structured comparison table between overrides and transforms — concrete and scannable.",
            "The 'When NOT to use' section is opinionated and correctly advises checking native properties first.",
            "Transform examples with array spreading correctly show the key use case (appending layers without knowing the full list).",
            "The FAQ answers are appropriately concise and cover a useful range of questions, including the critical 'Will overrides break on Stacktape updates?' question.",
            "Practical compile-template workflow guidance is well placed and correct in both locations.",
            "The CDK construct section correctly identifies when to prefer CDK over raw CloudFormation resources."
          ],
          "problems": [
            "CRITICAL ACCURACY: The draft invents a 'user-friendly alias' naming convention for override/transform keys (e.g., `lambda`, `lambdaRole`, `lambdaLogGroup`, `ecsTaskDefinition`, `dbInstance`, `dbInstanceParameterGroup`, `bucket`, `httpApi`, `redisReplicationGroup`, `redisParameterGroup`) that does not appear in any provided source file. Every source consistently uses the `Stp`-prefixed logical names: `StpLambdaFunction`, `StpLambdaFunctionRole`, `StpEcsTaskDefinition`, `StpRdsInstance`, `StpRdsParameterGroup`, `StpS3Bucket`, `StpHttpApi`. A production engineer copying the Lambda override example would get an error.",
            "CRITICAL ACCURACY: The child resource name table maps Stacktape resources to fabricated user-friendly names rather than the source-documented `Stp`-prefixed logical names, compounding the confusion from the above issue.",
            "ACCURACY: The dot-notation path claim for nested override properties — e.g., `'Parameters.max_connections': '200'` as a string key — is not supported by any source file. The source shows nested objects: `{ Parameters: { max_connections: '200' } }`. This will mislead users trying to configure RDS parameter groups.",
            "ACCURACY: The CDK construct property names used in the `AwsCdkConstruct` example (`entryfilePath`, `exportName`, `constructProperties`) are not confirmed by the provided source files. The curated source docs use `constructPath` and `constructName` (in a list form via `cdkConstructs`). These may be the same feature with different APIs, but no source confirms the class-based form's property names.",
            "UNSUPPORTED CLAIM: The FAQ states that `cloudformationResources` resources 'do not count toward Stacktape's managed resource limit.' No source file supports this claim.",
            "UNSUPPORTED CLAIM: The compile-template section mentions a `--outFile` flag for specifying a custom output path. No source file mentions this flag.",
            "UNSUPPORTED CLAIM: The FAQ states 'Stacktape applies overrides after it finishes generating the template.' This execution-order detail is not sourced.",
            "UNSUPPORTED CLAIM: The overrides table claims overrides are 'Validated against child resource existence.' No source confirms Stacktape performs this validation."
          ],
          "mandatoryFixes": [
            "Replace all fabricated user-friendly override/transform key names throughout the page — in examples, prose, and the reference table — with the source-accurate `Stp`-prefixed logical names: use `StpLambdaFunction` (not `lambda`), `StpLambdaFunctionRole` (not `lambdaRole`), `StpEcsTaskDefinition` (not `ecsTaskDefinition`), `StpRdsInstance` (not `dbInstance`), `StpRdsParameterGroup` (not `dbInstanceParameterGroup`), `StpS3Bucket` (not `bucket`), `StpHttpApi` (not `httpApi`). Remove the section claiming TypeScript configs provide user-friendly aliases; no source supports this.",
            "Fix the RDS parameter group override example: replace the dot-notation string key format `'Parameters.max_connections': '200'` with the nested object format `{ Parameters: { max_connections: '200', shared_buffers: '256MB' } }`, matching what the source shows. Remove the dot-notation prose claim or narrow it to explicitly say this is not verified.",
            "Verify CDK construct property names (`entryfilePath`, `exportName`, `constructProperties`) against authoritative source. If they cannot be confirmed, remove the `AwsCdkConstruct` code example or replace it with a narrower prose description that links to the CDK constructs reference page rather than claiming specific prop names.",
            "Remove the unsupported `--outFile` flag mention from the compile-template command and description."
          ],
          "optionalImprovements": [
            "Add `stacktape stack-info --detailed` as an alternative discovery method for child resource names — it appears in the source docs but the draft only mentions compile-template.",
            "Remove or caveat the claim that `cloudformationResources` resources don't count toward the Stacktape resource limit; this may be true but is unverified by the provided source.",
            "The overrides type definition (`type ResourceOverrides = ...`) is a reasonable clarification but may be more confusing than helpful without the actual runtime type. Consider removing the inline type block and replacing with a one-sentence description, or source it explicitly.",
            "The 'Validated against child resource existence' claim in the overrides vs transforms table should be removed or replaced with something like 'Set after template generation' to stay within what the source supports.",
            "Consider adding the `stacktape stack-info --detailed` CLI invocation to the 'Finding child resource logical names' section for discoverability, since this is mentioned in the source docs."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 6,
            "scannability": 8,
            "completeness": 6,
            "practicalUsefulness": 4,
            "audienceFit": 7
          },
          "strengths": [
            "Strong opening paragraph — self-contained, answers 'what and why' within 75 words, good for AI extraction.",
            "Overrides vs Transforms comparison table is well-structured and citable; cleanly separates the two mechanisms.",
            "FAQ section mixes Stacktape-specific, AWS-specific, and comparison questions — good breadth for SEO/AEO.",
            "Decision-support framing is present throughout: 'When to use escape hatches' and 'When NOT to use' are peer H2s as required.",
            "The Best Practices section is concrete and opinionated rather than generic.",
            "CodeBlock usage for Stacktape config examples is correct — all use `<CodeBlock intellisense tabs={[...]}/>` with `label: 'TypeScript'`. No forbidden YAML blocks or fenced TypeScript Stacktape-config blocks detected.",
            "The `compile-template` inspection workflow is clearly explained with a realistic command and explanation of output."
          ],
          "problems": [
            "CRITICAL FABRICATED FEATURE — The draft invents 'user-friendly property names' (e.g., `lambda`, `lambdaRole`, `lambdaLogGroup`, `ecsTaskDefinition`, `dbInstanceParameterGroup`) as aliases for override/transform keys. Every source file consistently uses Stp-prefixed logical names: `StpLambdaFunction`, `StpLambdaFunctionRole`, `StpLambdaFunctionLogGroup`, `StpEcsTaskDefinition`, `StpRdsParameterGroup`, etc. No source file mentions any alias system. Developers following these examples will write broken configs.",
            "CRITICAL WRONG OVERRIDE KEYS — All CodeBlock override examples use the fabricated aliases: `overrides: { lambda: { ReservedConcurrentExecutions: 100 } }`. The correct key from every source example is `StpLambdaFunction`. The same error appears in the transforms examples (`lambda`, `ecsTaskDefinition` instead of `StpLambdaFunction`, `StpEcsTaskDefinition`).",
            "CRITICAL WRONG TABLE — The 'Common child resource names by resource type' table maps to the invented aliases, not to actual logical names. Source shows `StpLambdaFunction`, `StpLambdaFunctionRole`, `StpEcsService`, `StpEcsTaskDefinition`, `StpRdsInstance`, `StpRdsParameterGroup`, `StpS3Bucket`, `StpHttpApi`. The table as written is entirely wrong.",
            "INVENTED DOT-NOTATION SYNTAX — The 'Override with dot-notation paths' section and example use `'Parameters.max_connections': '200'` as a key. The source shows this as a nested object: `Parameters: { max_connections: '200' }`. No source file documents a dot-notation path syntax for overrides.",
            "UNGROUNDED MERGE BEHAVIOR CLAIM — 'For object properties at the top level, Stacktape performs a deep merge with existing values' is not supported by any source file. The source's own comparison table says overrides do 'Shallow merge', which contradicts the draft's 'deep merge' claim.",
            "POTENTIALLY WRONG CDK CONSTRUCT PROPERTIES — The draft documents `AwsCdkConstruct` with properties `entryfilePath`, `exportName`, `constructProperties`. The source file shows `constructPath` and `constructName` in the YAML representation. Property names may differ in the TypeScript API, but this is unverified and not grounded in the provided sources.",
            "Stacktape config snippet sections ('Referencing Stacktape resources from CloudFormation', 'DependsOn for ordering') show `cloudformationResources: { ... }` bodies in plain fenced TypeScript blocks rather than `<CodeBlock intellisense>`. While these snippets omit `defineConfig`, they show Stacktape config structure and would benefit from being shown inside a full `<CodeBlock>` for copy-paste correctness.",
            "The `RdsEnginePostgres16` class is used in a CodeBlock example — this is valid per the class name whitelist, but note the override key used is `dbInstanceParameterGroup` (fabricated alias) rather than the actual `StpRdsParameterGroup`."
          ],
          "mandatoryFixes": [
            "Replace all fabricated user-friendly override/transform key aliases with the correct Stp-prefixed logical names found in source: use `StpLambdaFunction` not `lambda`, `StpLambdaFunctionRole` not `lambdaRole`, `StpLambdaFunctionLogGroup` not `lambdaLogGroup`, `StpEcsTaskDefinition` not `ecsTaskDefinition`, `StpRdsParameterGroup` not `dbInstanceParameterGroup`. Remove all prose describing a user-friendly alias system entirely — no source supports it.",
            "Rewrite the 'Common child resource names by resource type' table to use the actual Stp-prefixed logical names from source (e.g., `StpLambdaFunction`, `StpEcsService`, `StpEcsTaskDefinition`, `StpRdsInstance`, `StpS3Bucket`, `StpHttpApi`) and drop the fabricated 'User-friendly name' column.",
            "Remove the 'Override with dot-notation paths' section and its CodeBlock example. Dot-notation path syntax is not documented in any source file. The source shows nested objects (`Parameters: { max_connections: '200' }`) not dot-notation strings. Replace with a nested-object override example grounded in source, or omit if no source supports it.",
            "Fix the merge behavior claim: replace 'For object properties at the top level, Stacktape performs a deep merge with existing values' with wording consistent with the source (which labels override merge behavior as 'Shallow merge'). Do not assert deep merge without a source.",
            "Verify and correct the `AwsCdkConstruct` property names (`entryfilePath`, `exportName`, `constructProperties`) against the actual TypeScript type definitions. If those names conflict with the source's `constructPath`/`constructName`, update the CodeBlock example and prose to match the correct API."
          ],
          "optionalImprovements": [
            "The two snippet blocks showing `cloudformationResources: { InvokePermission: ... }` and `cloudformationResources: { TopicSubscription: ... }` in plain fenced TypeScript are technically not forbidden (they lack `defineConfig`), but they are partial Stacktape config. Wrapping them in complete `<CodeBlock intellisense>` examples with `defineConfig` would make them copy-paste runnable and consistent with page style.",
            "The FAQ entry 'Will overrides break when Stacktape updates?' discusses the invented alias stability claim. Once the alias feature is removed, this FAQ entry needs to be rewritten to instead discuss the risk of Stacktape changing Stp-prefixed logical names in major versions.",
            "Add a concrete note that `stacktape compile-template` (not `stack-info --detailed`) is the primary way to find child resource logical names, since the source files now consistently point to `compile-template` output rather than the older `stack-info --detailed` approach.",
            "The CDK constructs section ends with 'For more detail, see [AWS CDK constructs](/resources/advanced/aws-cdk-constructs)' — once property names are verified, consider expanding the CDK example in the config file to be more realistic (the current one passes `functionName` via `$ResourceParam` which is a good pattern worth keeping)."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "claude",
          "modelName": "cli-default",
          "issues": [
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "The 'Common child resource names by resource type' table lists `ecsService` as a valid user-friendly override key for web services.",
              "reason": "In the child-resources.ts source, `ecsService` for web-service is marked `unresolvable: true`. When a child resource is unresolvable, the override/transform processing in config.ts (line 334) throws an error: 'Override of property {propertyName} of resource {resourceName} is not supported.' Users who follow this table and use `ecsService` as an override key will get a runtime error. They must use the full CloudFormation logical name from `compile-template` output instead.",
              "suggestedFix": "Remove `ecsService` from the table, or add a footnote explaining that `ecsService` cannot be used as a user-friendly override key because it is unresolvable — users must use the full CloudFormation logical name from `compile-template` output. Consider replacing `ecsService` with `ecsTaskRole` (which is resolvable) or keeping only `ecsTaskDefinition` for web service.",
              "evidence": [
                {
                  "file": "src/api/npm/ts/child-resources.ts",
                  "quote": "logicalName: cfLogicalNames.ecsService,\n      resourceType: 'AWS::ECS::Service',\n      unresolvable: true"
                },
                {
                  "file": "src/api/npm/ts/config.ts",
                  "quote": "if (childResource?.unresolvable) {\n      throw new Error(errorMessage.replace('{propertyName}', propertyName));\n    }"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "Raw CloudFormation resources 'don't count toward your Stacktape resource limit' and the FAQ answer 'do not count toward Stacktape's managed resource limit'.",
              "reason": "The source files do not mention a 'Stacktape resource limit' or 'managed resource limit'. The `cloudformationResources` JSDoc in `_root.d.ts` says 'Escape hatch: add raw AWS CloudFormation resources alongside Stacktape-managed ones' but says nothing about a limit. Without evidence of such a limit, this claim is unsupported and could confuse readers who wonder what the limit is.",
              "suggestedFix": "Remove references to a 'Stacktape resource limit' / 'managed resource limit'. Instead, simply note that raw CloudFormation resources are merged into the template and are subject to AWS CloudFormation's per-stack resource limit (500 by default). If a Stacktape limit exists, cite it with the actual number.",
              "evidence": [
                {
                  "file": "types/stacktape-config/_root.d.ts",
                  "quote": "cloudformationResources?: { [resourceName: string]: CloudformationResource };"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "The draft's transform example for WebService uses `ecsTaskDefinition` as the transform key, which works correctly. However, the surrounding prose doesn't warn that some child resource names (like `ecsService`) are marked unresolvable and cannot be used as user-friendly override/transform keys.",
              "reason": "The unresolvable mechanism is a key constraint that applies to both overrides and transforms. Users reading the table of user-friendly names and the examples may assume all listed child resources in the child-resources.ts mapping are usable. Several child resources across multiple resource types are marked `unresolvable: true` (e.g., `ecsService`, `cloudfrontDistribution` on function, `cloudfrontCustomCachePolicy` on function, `auroraDbInstance`, `dbReplica`). The page should explain this constraint so users know when they must fall back to full logical names.",
              "suggestedFix": "Add a sentence or note near the user-friendly names table explaining that some child resources are only available via their full CloudFormation logical name (from `compile-template` output). Note that Stacktape will throw a clear error if an unresolvable name is used, directing the user to find the logical name manually.",
              "evidence": [
                {
                  "file": "src/api/npm/ts/config.ts",
                  "quote": "const errorMessage = `Override of property {propertyName} of resource ${resourceName} is not supported.\\n\nRemove the override, run 'stacktape compile:template' command, and find the logical name of the resource you want to override manually. Then add it to the overrides object.`;"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "The table lists child resource names without noting which are conditional (only present when specific features are enabled).",
              "reason": "In child-resources.ts, several entries are marked `conditional: true` — for example `lambdaLogGroup` on function, `dbInstance` and `dbInstanceParameterGroup` on relational-database. If a user tries to override a conditional child resource that doesn't exist in their particular configuration, they'll get a validation error. While Stacktape's error message is clear, the table could be more helpful by noting which entries are conditional.",
              "suggestedFix": "Consider adding a brief note below the table that some child resources are only present when specific features are configured. Or add an asterisk/footnote marking conditional entries. This is low priority since Stacktape's error handling is clear.",
              "evidence": [
                {
                  "file": "src/api/npm/ts/child-resources.ts",
                  "quote": "{ logicalName: cfLogicalNames.dbInstance, resourceType: 'AWS::RDS::DBInstance', conditional: true },\n    {\n      logicalName: cfLogicalNames.dbInstanceParameterGroup,\n      resourceType: 'AWS::RDS::DBParameterGroup',\n      conditional: true\n    }"
                }
              ]
            }
          ],
          "positiveFindings": [
            "User-friendly override/transform property names (`lambda`, `lambdaRole`, `lambdaLogGroup`, `ecsTaskDefinition`, `dbInstance`, `dbInstanceParameterGroup`, `bucket`, `httpApi`, `redisReplicationGroup`, `redisParameterGroup`) are verified against child-resources.ts and config.ts mapping logic.",
            "Override merge behavior accurately described: deep merge for top-level non-array objects (template-manager/index.ts lines 224-236 use lodash `merge()`), dot-path set otherwise (line 238 uses `set()`). This corrects the old curated docs which said 'Shallow merge'.",
            "Transforms correctly described as TypeScript-only — transforms-resolver.ts explicitly checks `configFilePath.endsWith('.ts')` and returns empty transforms for non-TypeScript files. The old curated docs incorrectly showed YAML transform examples.",
            "Override validation against child resource existence is accurate — template-manager/index.ts lines 186-196 verify cfLogicalName is a valid child and throw `stpErrors.e101` if not.",
            "$CfResourceParam correctly described as creating a stack output — built-in-directives.ts lines 203-209 call `templateManager.addStackOutput()` with the resolved intrinsic function.",
            "compile-template `--outFile` flag and default `compiled-template.yaml` output verified in compile-template/index.ts.",
            "AwsCdkConstruct properties (`entryfilePath`, `exportName` with default 'default', `constructProperties`) match the type definition in aws-cdk-construct.d.ts.",
            "`ResourceOverrides` and `CfResourceTransform` type signatures verified in __helpers.d.ts and transforms-resolver.ts respectively.",
            "All cross-links point to valid routes in the navigation index.",
            "Code examples use correct CodeBlock format with `intellisense` flag and `TypeScript` tab label."
          ],
          "summary": "The draft is well-structured and mostly accurate. Override and transform mechanics, type signatures, user-friendly property name mapping, $CfResourceParam behavior, AwsCdkConstruct properties, and compile-template behavior are all verified against source code. The draft correctly fixes two errors from the old curated docs: transforms are TypeScript-only (not YAML), and override merge behavior is deep-merge for objects (not shallow). Four issues found: (1) HIGH — `ecsService` is listed as a valid user-friendly override key but is marked `unresolvable: true` in source and will throw a runtime error; (2) MEDIUM — references to a 'Stacktape managed resource limit' are unsupported by source; (3) MEDIUM — the unresolvable constraint on certain child resource names is not explained anywhere on the page; (4) LOW — conditional child resources in the table aren't noted as conditional."
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the concept of overrides, transforms, raw CloudFormation resources, and CDK constructs, but it introduces several Stacktape-specific behaviors that are either contradicted by the provided sources or not grounded in them. The highest-risk issues are the invented TypeScript-friendly child-resource aliases, the claim that transforms are unavailable in YAML, and unsupported merge/validation/resource-limit behavior.",
          "issues": [
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "“In TypeScript configs using `defineConfig`, you can also use user-friendly property names (like `lambda`, `lambdaRole`, `lambdaLogGroup`) instead of full logical names. The SDK automatically resolves these…” and the examples/table use aliases such as `lambda`, `ecsTaskDefinition`, `dbInstanceParameterGroup`, `bucket`, and `httpApi`.",
              "reason": "The provided source identifies child resources by CloudFormation-style logical names such as `StpLambdaFunction`, `StpEcsTaskDefinition`, and `StpRdsInstance`. It does not document alias names or automatic alias resolution. This would directly change what users put in `overrides` and `transforms`.",
              "suggestedFix": "Use the documented child resource names from the source examples, such as `StpLambdaFunction` and `StpEcsTaskDefinition`, or phrase alias support as unavailable/undocumented unless higher-priority source code proves it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "Common child resource names:\n\n| Stacktape Resource    | Child Resources                                                           |\n| --------------------- | ------------------------------------------------------------------------- |\n| `function`            | `StpLambdaFunction`, `StpLambdaFunctionRole`, `StpLambdaFunctionLogGroup` |\n| `web-service`         | `StpEcsService`, `StpEcsTaskDefinition`, `StpEcsTaskRole`                 |\n| `relational-database` | `StpRdsInstance`, `StpRdsSecurityGroup`, `StpRdsSubnetGroup`              |\n| `bucket`              | `StpS3Bucket`                                                             |\n| `http-api-gateway`    | `StpHttpApi`                                                              |"
                }
              ]
            },
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "“Transforms are only available in TypeScript configurations using the `defineConfig` pattern. They are not supported in YAML configs.” and FAQ: “No. Transforms require JavaScript functions and only work in TypeScript configurations…”",
              "reason": "The source shows a YAML transform example using a function string, then separately says TypeScript transforms are cleaner. The draft directly contradicts that source-backed behavior.",
              "suggestedFix": "State that transforms can be represented in YAML as function strings, but TypeScript configs are the clearer documented path for authoring transform functions.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "### Basic Transform\n\n```yaml\nresources:\n  handler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/handler.ts\n      memory: 128\n      transforms:\n        StpLambdaFunction: |\n          (props) => ({\n            ...props,\n            MemorySize: props.MemorySize * 2  // Double the memory\n          })\n```"
                },
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "### TypeScript Transforms\n\nIn TypeScript configuration, transforms are cleaner:"
                }
              ]
            },
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "“For top-level object properties, Stacktape performs a deep merge with existing values. For dot-notation paths, it sets the value at the specified path directly.” and the comparison table says “Dot-path set or deep merge for objects.”",
              "reason": "The provided source’s comparison table says override merge behavior is “Shallow merge,” not deep merge. Dot-notation behavior is not documented in the provided source.",
              "suggestedFix": "Replace the deep-merge and dot-path claims with the narrower sourced statement that overrides are static values and have shallow merge behavior, unless the runtime type/source code proves the newer behavior.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "| Merge behavior | Shallow merge           | Full control             |"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The `overrides` property is available on all Stacktape resource types.”",
              "reason": "The source says overrides let you set properties on child CloudFormation resources and gives examples for several resource types, but it does not state that every Stacktape resource type exposes `overrides`. This broad claim should be narrowed or backed by type definitions.",
              "suggestedFix": "Write “Stacktape resources that expose child CloudFormation resources can use overrides” or cite the relevant type helper/source if all resources truly share the property.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "Overrides let you set specific properties on child CloudFormation resources."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Each transform function receives the full `Properties` object of a CloudFormation resource and must return the modified properties object” and “Transforms run after Stacktape has fully resolved and built the CloudFormation template, so you see the final property values.”",
              "reason": "The source only says transforms are functions that modify CloudFormation properties and can compute based on existing properties. It does not establish execution timing, full final-template visibility, or a strict return contract.",
              "suggestedFix": "Narrow to the sourced behavior: transforms are functions that receive CloudFormation properties and return modified values. Avoid claiming exact lifecycle timing unless source code supports it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "Transforms are functions that modify the CloudFormation properties. They're more powerful than overrides because you can compute values based on existing properties."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Raw CloudFormation resources are merged into the final template as-is. They don't count toward your Stacktape resource limit…” and FAQ: “Resources added via `cloudformationResources` … do not count toward Stacktape's managed resource limit.”",
              "reason": "The provided sources support adding native CloudFormation resources and mention limitations, but they do not mention a Stacktape resource limit or whether raw CloudFormation resources count toward it.",
              "suggestedFix": "Remove the Stacktape resource-limit claim unless a source file defining that limit is provided. Keep the grounded limitation that CloudFormation resources do not get Stacktape’s simplified configuration, IAM integration, or `connectTo` support.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\extending-cloudformation.mdx",
                  "quote": "Use the `cloudformationResources` section to add native CloudFormation resources to your stack. This gives you access to any AWS resource that CloudFormation supports."
                },
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\extending-cloudformation.mdx",
                  "quote": "- CloudFormation resources don't benefit from Stacktape's simplified configuration\n- IAM permissions must be managed manually\n- No automatic `connectTo` support\n- Updates may require more careful planning (some properties can't be updated in-place)"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Under the hood, `$CfResourceParam` resolves to CloudFormation intrinsic functions (`Fn::GetAtt` or `Ref`) and automatically creates a stack output for the referenced value.”",
              "reason": "The source supports using `$CfResourceParam()` to get attributes from CloudFormation resources, including `Ref`, but it does not state the implementation details or automatic stack output creation.",
              "suggestedFix": "Say only that `$CfResourceParam()` references attributes or `Ref` values from raw CloudFormation resources. Remove the intrinsic-function and automatic-output claims unless backed by directive implementation source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\extending-cloudformation.mdx",
                  "quote": "Use `$CfResourceParam()` to get attributes from CloudFormation resources:\n\n```ts\n// Get the ARN of an SNS topic\n'$CfResourceParam(\\'AlertTopic\\', \\'TopicArn\\')';\n\n// Get the name of an S3 bucket\n'$CfResourceParam(\\'DataBucket\\', \\'BucketName\\')';\n\n// Get the physical ID (usually the resource name)\n'$CfResourceParam(\\'AlertTopic\\', \\'Ref\\')';\n```"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The `AwsCdkConstruct` resource type takes three properties: `entryfilePath`, `exportName`, and `constructProperties`,” including “`exportName` defaults to `\"default\"`.”",
              "reason": "The provided source confirms CDK constructs conceptually, but the property names it shows are `constructPath` and `constructName` in an older YAML example. The draft’s class-based `AwsCdkConstruct` API and default value are not grounded in the provided source files.",
              "suggestedFix": "Either remove the property table or rewrite it cautiously as a TypeScript example only if authoritative type definitions for `AwsCdkConstruct` are included. Do not claim the `exportName` default without source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "Reference in config:\n\n```yaml\nresources:\n  handler:\n    type: function\n\ncdkConstructs:\n  - constructPath: ./cdk/monitoring.ts\n    constructName: MonitoringDashboard\n```"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft avoids hand-authored Stacktape YAML configuration blocks for Stacktape config examples and uses `<CodeBlock intellisense>` with a `TypeScript` tab for Stacktape config snippets.",
            "The first paragraph clearly explains what overrides and escape hatches are for without meta-framing.",
            "The draft correctly links only to routes present in the provided documentation navigation index.",
            "The raw CloudFormation section correctly states the sourced limitations that raw resources do not get simplified Stacktape configuration, automatic IAM handling, or `connectTo` support."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly complete for a concept page with no primary typeName, and the Stacktape config examples mostly demonstrate the escape-hatch concepts they introduce. The main issues are unsupported narrowing around transforms/YAML and several code/prose snippets that use fenced TypeScript for Stacktape config-shaped examples instead of the required CodeBlock pattern.",
          "issues": [
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "Transforms are only available in TypeScript configurations using the `defineConfig` pattern. They are not supported in YAML configs.",
              "reason": "The provided source explicitly shows a YAML transform example. Even if the modern docs should avoid hand-authored YAML examples, the source does not support saying YAML transforms are unsupported.",
              "suggestedFix": "Replace with narrower wording such as: \"Transforms are most practical in TypeScript configs because they are JavaScript functions. Prefer TypeScript for transform examples.\" Avoid claiming YAML is unsupported unless a higher-priority source proves it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "### Basic Transform\n\n```yaml\nresources:\n  handler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/handler.ts\n      memory: 128\n      transforms:\n        StpLambdaFunction: |\n          (props) => ({\n            ...props,\n            MemorySize: props.MemorySize * 2  // Double the memory\n          })"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "In TypeScript configs using `defineConfig`, you can also use user-friendly property names (like `lambda`, `lambdaRole`, `lambdaLogGroup`) instead of full logical names. The SDK automatically resolves these to the correct CloudFormation logical names based on the resource type.",
              "reason": "The provided sources document CloudFormation logical child names such as `StpLambdaFunction`, not the `lambda`/`lambdaRole` aliases. The draft then uses those aliases in examples and a table, but the source list does not ground them.",
              "suggestedFix": "Use source-grounded logical names such as `StpLambdaFunction`, `StpLambdaFunctionRole`, and `StpLambdaFunctionLogGroup`, or add authoritative source-code evidence for the alias mapping before documenting it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "Common child resource names:\n\n| Stacktape Resource    | Child Resources                                                           |\n| --------------------- | ------------------------------------------------------------------------- |\n| `function`            | `StpLambdaFunction`, `StpLambdaFunctionRole`, `StpLambdaFunctionLogGroup` |\n| `web-service`         | `StpEcsService`, `StpEcsTaskDefinition`, `StpEcsTaskRole`                 |"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "For top-level object properties, Stacktape performs a deep merge with existing values. For dot-notation paths, it sets the value at the specified path directly.",
              "reason": "The source comparison table says override merge behavior is \"Shallow merge\". It does not support deep-merge semantics or dot-path semantics in the level of detail claimed by the draft.",
              "suggestedFix": "Align the merge description with the source: say overrides set static values on child CloudFormation resources, and avoid claiming deep merge or dot-path behavior unless grounded by type/runtime source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "| Merge behavior | Shallow merge           | Full control             |"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The draft uses a fenced TypeScript snippet under \"Referencing Stacktape resources from CloudFormation\" to show `cloudformationResources` config.",
              "reason": "This is a Stacktape configuration-shaped example containing `cloudformationResources`, but it is in a fenced `typescript` block rather than `<CodeBlock intellisense tabs={[...]} />`. The page requirements forbid fenced TypeScript blocks for Stacktape config examples.",
              "suggestedFix": "Convert the snippet into a full `defineConfig` CodeBlock with `label: 'TypeScript'`, or make it plain prose if a complete config example would be too much for the concept page.",
              "evidence": [
                {
                  "file": "Draft page",
                  "quote": "### Referencing Stacktape resources from CloudFormation\n\nUse the [`$ResourceParam`](/configuration/referenceable-parameters) directive inside `cloudformationResources` to reference attributes of Stacktape-managed resources:\n\n```typescript\ncloudformationResources: {\n  InvokePermission: {"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The draft uses a fenced TypeScript snippet under \"DependsOn for ordering\" to show `cloudformationResources` config.",
              "reason": "This is another Stacktape config-shaped example in a fenced TypeScript block. It violates the required docs pattern for Stacktape configuration examples and bypasses the CodeBlock validator.",
              "suggestedFix": "Fold `DependsOn` into the previous raw CloudFormation CodeBlock example, or convert this section to a complete `defineConfig` CodeBlock with focus markers around `DependsOn`.",
              "evidence": [
                {
                  "file": "Draft page",
                  "quote": "### DependsOn for ordering\n\nUse the standard CloudFormation `DependsOn` property to control creation order between raw resources:\n\n```typescript\ncloudformationResources: {\n  TopicSubscription: {"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "Raw CloudFormation resources are merged into the final template as-is. They don't count toward your Stacktape resource limit, but they also don't benefit from Stacktape's automatic IAM permissions, `connectTo` integration, or simplified configuration.",
              "reason": "The source supports the limitations around simplified configuration, manual IAM, and no automatic `connectTo`, but it does not mention a Stacktape resource limit or that `cloudformationResources` do not count toward it. That limit-related claim is not grounded in the provided source list.",
              "suggestedFix": "Remove the resource-limit sentence or replace it with the source-grounded limitation: raw CloudFormation resources are added directly to the template and do not get Stacktape's simplified configuration, IAM integration, or automatic `connectTo` behavior.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\extending-cloudformation.mdx",
                  "quote": "## Limitations\n\n- CloudFormation resources don't benefit from Stacktape's simplified configuration\n- IAM permissions must be managed manually\n- No automatic `connectTo` support\n- Updates may require more careful planning (some properties can't be updated in-place)"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft covers the main escape-hatch concepts present in the sources: overrides, transforms, raw CloudFormation resources, `$CfResourceParam`, `$ResourceParam`, `DependsOn`, CDK constructs, and template inspection.",
            "Most `<CodeBlock intellisense>` examples are demonstrative: override examples set `overrides`, transform examples set `transforms`, raw CloudFormation examples set `cloudformationResources`, and the CDK example uses `AwsCdkConstruct`.",
            "The page includes practical decision guidance, including when to use native Stacktape properties first and when to choose overrides, transforms, raw CloudFormation, or CDK constructs."
          ]
        },
        {
          "verifierId": "code-block-validator",
          "modelProvider": "deterministic",
          "modelName": "local",
          "summary": "Deterministic check of <CodeBlock intellisense> Stacktape config examples and generated MDX component usage.",
          "issues": [
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #2 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #2 (TypeScript): `new RdsEnginePostgres16(...)` references an unknown class.",
              "reason": "`RdsEnginePostgres16` is not exported by the `stacktape` package.",
              "suggestedFix": "Use one of the documented Stacktape resource classes (e.g. LambdaFunction, WebService, RelationalDatabase). If `RdsEnginePostgres16` is meant to be a custom class, define it in the example or remove the example."
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #3 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #4 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            }
          ],
          "positiveFindings": []
        }
      ],
      "status": "failed",
      "passed": false
    },
    {
      "iteration": 2,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\configuration__overrides-and-escape-hatches\\iteration-2.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "Clear progression from simple (overrides) to complex (CDK constructs) that respects the reader's learning curve.",
            "The overrides-vs-transforms comparison table is well-placed and gives readers a fast decision signal.",
            "Multiple realistic, copyable CodeBlock examples for each concept — the Lambda reserved-concurrency and ECS ulimits examples are exactly what a real team would search for.",
            "Comprehensive FAQ (10 entries) covering Stacktape-specific, AWS-general, and comparison questions — hits all three required categories.",
            "The 'When NOT to use escape hatches' section is prominently placed and gives concrete, opinionated guidance, not circular advice.",
            "Best practices section is tightly written and actionable rather than generic.",
            "The `compile-template` tip is repeated at the right moments (after transforms, and in the final section) without feeling padded.",
            "The Warning callout about unresolvable child resources prevents a real gotcha that would otherwise surface only at deploy time."
          ],
          "problems": [
            "Potential factual inaccuracy: the `AwsCdkConstruct` CodeBlock uses `entryfilePath` and `exportName` as property names, but both source files consistently show `constructPath` and `constructName`. The class-based API may use different names, but without the type definitions in scope this cannot be confirmed, and a first-time reader copying the CDK example could get a runtime or type error.",
            "Multiple standalone `cloudformationResources: { ... }` and `$CfResourceParam(...)` snippets appear in plain fenced ```typescript blocks — they are partial Stacktape config (not wrapped in `defineConfig`) and they show the directive used as a bare JS function call without an import statement or surrounding context. A copy-pasting reader has no way to know how to integrate these fragments.",
            "The 'Why this exists' H2 section substantially restates the opening paragraph and adds little new information. It reads as backbone padding rather than earned content.",
            "The Warning callout references `ecsService` (lowercase camelCase) as the key to avoid, but every other child resource name on the page uses the `StpXxx` prefix convention. This is internally inconsistent and will confuse a reader trying to understand the naming scheme.",
            "The 'How transforms work' subsection shows a raw TypeScript type signature (`type CfResourceTransform = ...`) in a fenced typescript block without any prose explaining where this type comes from or how to use it — it reads as an unexplained internal detail."
          ],
          "mandatoryFixes": [
            "Verify `AwsCdkConstruct` property names against the actual TypeScript types. If the class-based API uses `entryfilePath` and `exportName`, add a sentence naming them explicitly and keep the example. If it uses `constructPath` and `constructName` (as the source files show), update the CodeBlock accordingly. Do not leave a CodeBlock whose property names contradict all available source evidence.",
            "Remove or complete the fenced ```typescript snippets in the 'Referencing CloudFormation resources from Stacktape' and 'DependsOn for ordering' sections. As standalone objects without `defineConfig`, they cannot be copied and run. Either integrate them into a full `<CodeBlock intellisense tabs={[...]} />` with `defineConfig`, or replace them with prose descriptions and keep only the complete CodeBlock examples already on the page.",
            "Fix the Warning callout: replace `ecsService` with the correct `StpEcsService` logical name to match the naming convention used throughout the page, or add a parenthetical clarifying that the internal key differs from the `Stp`-prefixed convention."
          ],
          "optionalImprovements": [
            "Merge 'Why this exists' into the opening paragraph or cut it. The information it adds (Stacktape covers 80%, overrides close the gap) can be expressed in one sentence appended to the intro, freeing the H2 slot for something genuinely new.",
            "Remove the 'How transforms work' subsection's raw type signature fenced block entirely. The prose description ('transforms are functions that receive Properties and return modified properties') and the surrounding examples communicate this more clearly than a disembodied TypeScript type.",
            "The common child resource names table omits `StpEcsService` (web service's ECS service child resource), which the source files list. Consider adding it with a note that it cannot be targeted by name-based overrides — this reinforces the Warning callout and makes the table actually authoritative.",
            "The standalone `$CfResourceParam` fenced typescript block showing three usage examples uses `//` comments to label each variant. Since these are TypeScript (not shell), this doesn't violate the copy-button rule, but the three variants could be condensed into one clear prose description plus a single representative example to reduce visual noise.",
            "The FAQ answer for 'Can I reference CDK construct outputs via $CfResourceParam?' is accurate but ends abruptly. Add a one-sentence note that the synthesized logical names follow CDK's naming convention (scope ID + construct ID) so the reader knows what to look for in the compiled template output."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 9,
            "completeness": 7,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "All Stacktape config examples correctly use <CodeBlock intellisense tabs={[...]} /> — no forbidden YAML blocks or plain-object StacktapeConfig pattern anywhere.",
            "Covers all three escape hatch mechanisms (overrides, transforms, raw CF, CDK constructs) with a clear structure and realistic code examples.",
            "Overrides vs transforms comparison table is well-designed and actionable — engineers can decide at a glance which to reach for.",
            "Discovery section (info:stack, compile-template) answers the most common practical blocker: 'how do I find the child resource name?'",
            "FAQ section is broad and high-quality, spanning Stacktape-specific, AWS-mechanics, and comparison questions.",
            "Decision guidance on when to use CDK constructs vs raw CloudFormation is concrete and opinionated.",
            "Best-practices section is specific, not generic — especially 'use transforms for array modifications' and 'validate with compile-template'.",
            "Shell blocks are one command per block with no explanatory # comments inside them."
          ],
          "problems": [
            "Ungrounded behavioral claim in Warning box: 'the ECS service (ecsService) is marked unresolvable on container workloads. If you attempt to override an unresolvable child resource, Stacktape throws an error directing you to use the full CloudFormation logical name.' This specific runtime error behavior and the 'unresolvable' terminology do not appear in any provided source file. It reads as fabricated and could cause engineers to trust a stated error path that may not exist.",
            "CDK construct property names are not grounded in source. The draft uses `entryfilePath`, `exportName`, and `constructProperties` on `AwsCdkConstruct`. The only source file covering CDK (curated docs, low priority) shows a completely different shape: `constructPath` and `constructName` as keys in a `cdkConstructs` array at the top level. Since these property names determine whether the generated config actually works, this is a material inaccuracy risk.",
            "$CfResourceParam and $ResourceParam are imported as named exports from 'stacktape' in two CodeBlock examples. The provided source files consistently show these used as string directives (e.g., `'$CfResourceParam(\\'AlertTopic\\', \\'TopicArn\\')'`), not as imported JavaScript functions. If these are string-based directives rather than callable imports, both CodeBlock examples would produce non-functional configs.",
            "The 'Common child resource names' table includes Redis entries (StpElastiCacheReplicationGroup, StpElastiCacheParameterGroup) that do not appear in any provided source file. Including unverified child resource names in a lookup table is directly harmful — engineers will try to override those keys and hit hard-to-debug CloudFormation errors.",
            "Partial Stacktape config fragments are shown in plain fenced TypeScript blocks in the 'Referencing Stacktape resources from CloudFormation' and 'DependsOn for ordering' sections (`cloudformationResources: { ... }` without `defineConfig` wrapper). These technically avoid rule #3 (no class instantiation), but they show Stacktape config structure outside <CodeBlock intellisense>, which breaks the pattern the reader has been shown throughout the page and may produce copy-paste fragments that don't compile."
          ],
          "mandatoryFixes": [
            "Remove the Warning box claim that 'the ECS service (ecsService) is marked unresolvable' and that Stacktape throws a specific error directing the user to use the full logical name. This behavior is not grounded in any provided source. Replace with a source-grounded note (or omit the box entirely) rather than asserting a specific runtime error path.",
            "Verify the AwsCdkConstruct property names against the actual TypeScript types before publishing. The draft uses `entryfilePath`, `exportName`, `constructProperties` but the only available source shows `constructPath`/`constructName` in a top-level `cdkConstructs` array. If the class-based API uses different property names, every CDK example on this page will fail at deploy time. Either confirm the class API via the types or align with the source-file shape.",
            "Verify whether $CfResourceParam and $ResourceParam are importable functions from 'stacktape' or string directives. If they are string directives (as the source files consistently show), remove their imports from the CodeBlock examples and show them as string values instead (e.g., `environment: [{ name: 'TOPIC_ARN', value: '$CfResourceParam(\\'AlertTopic\\', \\'TopicArn\\')' }]`). Using the wrong form will make the 'Adding a raw CloudFormation resource' and CDK code examples non-functional.",
            "Remove Redis child resource names (StpElastiCacheReplicationGroup, StpElastiCacheParameterGroup) from the 'Common child resource names' table. They are not present in any provided source. Only list child resource names that appear in the provided source files. If Redis child resource overrides are common enough to document, add a note that names should be confirmed via `compile-template`."
          ],
          "optionalImprovements": [
            "The 'Referencing Stacktape resources from CloudFormation' and 'DependsOn for ordering' sections show plain fenced TypeScript fragments of `cloudformationResources: { ... }`. Consider wrapping these in a full <CodeBlock intellisense> example (with defineConfig wrapper) for consistency with the rest of the page, or convert them to prose + directive-string examples.",
            "The transforms section documents a TypeScript function signature (`type CfResourceTransform = ...`) in a plain fenced typescript block. Consider moving this to prose to avoid readers confusing it for code they need to write.",
            "The 'How transforms work' subsection is thin — it just shows the function signature. Expand with one sentence on when the transform function is called (before or after overrides?) to help readers understand merge order between overrides and transforms.",
            "The comparison table under 'Overrides vs transforms' lists 'Config format: TypeScript and YAML' for overrides and 'TypeScript (native functions) and YAML (function strings)' for transforms. This is useful but currently buried after the individual sections. Consider moving the table earlier (directly after the 'Why this exists' section) so readers can pick their mechanism before reading the implementation details.",
            "The FAQ answer to 'Can I reference CDK construct outputs in my Stacktape resources?' says 'Not directly through Stacktape's typed directive system.' This is a hedged negative claim. If the source is silent on this, phrase it as 'CDK constructs are synthesized into CloudFormation resources — use $CfResourceParam with synthesized logical names visible in compile-template output' without the negative framing that implies a missing feature.",
            "Add a note under 'Raw CloudFormation resources' clarifying that CloudFormation resource names (logical IDs like 'AlertTopic') must be unique within the stack and should not clash with Stacktape-generated logical names. This is a common gotcha when naming raw CF resources."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "Comprehensive coverage of all four escape hatch mechanisms (overrides, transforms, raw CloudFormation, CDK constructs) in a single page, giving readers a complete mental model before they dive into specifics.",
            "The overrides-vs-transforms comparison table is well-executed and answers the reader's most common decision question directly.",
            "FAQ section spans all three required categories (Stacktape-specific, AWS use-case, comparison/choosing) with 10 Q&A pairs, all substantive and non-duplicate.",
            "Decision support is strong throughout: 'When to use' and 'When NOT to use' sections give concrete scenarios rather than circular definitions, and each mechanism section explains why you'd reach for it over alternatives.",
            "CodeBlock examples for the main mechanisms use correct `defineConfig` + class-based TypeScript syntax with focus markers, and examples look realistic rather than toy-level.",
            "Best practices section is actionable and opinionated, not generic. 'Use transforms for array modifications' and 'Validate with compile-template' are directly useful advice."
          ],
          "problems": [
            "AwsCdkConstruct property names in the TypeScript CodeBlock (`entryfilePath`, `exportName`, `constructProperties`) diverge from the source files, which show `constructPath` and `constructName`. If the TypeScript class uses different names than the draft claims, the CDK example is broken and will fail type-checking — a significant factual issue.",
            "Redis cluster child resource names (`StpElastiCacheReplicationGroup`, `StpElastiCacheParameterGroup`) in the child resource table are not in any provided source file. These appear to be invented entries.",
            "The Warning box claims 'ecsService is marked unresolvable on container workloads' and that Stacktape 'throws an error directing you to use the full CloudFormation logical name'. This specific runtime behavior is not stated in any provided source file.",
            "`$CfResourceParam` and `$ResourceParam` are imported from the 'stacktape' package in CodeBlock examples (`import { ..., $CfResourceParam } from 'stacktape'`). Dollar-prefixed directive helpers are typically used as string literals or compile-time macros in Stacktape configs, not as importable functions. These imports are unsourced and likely invalid — the code-block validator would flag them.",
            "The '$CfResourceParam' reference section shows three different usages in a single fenced TypeScript block separated by `//` comment labels. While not a bash-specific violation, it creates a copy-paste antipattern: copying the block yields all three alternatives plus comment lines that may confuse static analysis or paste into the wrong context."
          ],
          "mandatoryFixes": [
            "Correct the AwsCdkConstruct property names in the CDK CodeBlock example. The provided source files show `constructPath` and `constructName` (YAML format); the TypeScript class likely mirrors these. The draft uses `entryfilePath`, `exportName`, and `constructProperties`, which are unverified and potentially wrong. If the code-block validator flags these names, remove the CDK CodeBlock example and replace it with a prose description linking to the dedicated CDK constructs page until the correct property names can be confirmed against the actual TypeScript type definition.",
            "Remove or source-verify the Redis cluster rows (`StpElastiCacheReplicationGroup`, `StpElastiCacheParameterGroup`) from the 'Common child resource names' table. No provided source file documents these logical names. Either find a source file that confirms them or drop the rows.",
            "Remove the Warning box that asserts `ecsService` is 'marked unresolvable' and that Stacktape 'throws an error'. This specific behavior is not documented in any provided source. Replace with a general guidance note: 'Not all child resources can be targeted by logical name. If an override fails, use `stacktape compile-template` to retrieve the exact logical name from the generated template.'",
            "Remove the imports of `$CfResourceParam` and `$ResourceParam` from the 'stacktape' package in the CodeBlock `import` statements. In the raw-CloudFormation CodeBlock the import line reads `import { defineConfig, LambdaFunction, StacktapeLambdaBuildpackPackaging, $CfResourceParam } from 'stacktape'` and in the CDK CodeBlock it imports `$ResourceParam`. These directive helpers are not confirmed as importable identifiers from the package; they are typically evaluated as string directives. Show their usage as string values (e.g. `value: $CfResourceParam('AlertTopic', 'TopicArn')` called as a function in the expression position if the package exports it, or as a string `'$CfResourceParam(...)'` if it is a parse-time directive). The code-block-validator will catch this if the imports are invalid."
          ],
          "optionalImprovements": [
            "Split the three-example `$CfResourceParam` fenced TypeScript block into individual prose sentences each followed by its own small snippet. This prevents copy-paste confusion and lets AI extractors cite each pattern individually.",
            "The partial `cloudformationResources` snippets in 'Referencing Stacktape resources from CloudFormation' and 'DependsOn for ordering' use plain fenced TypeScript blocks without `defineConfig` context. Consider either wrapping them in full `<CodeBlock intellisense>` examples or adding a brief note that these are fragments to be placed inside the `cloudformationResources` key of a full `defineConfig` config, so readers can orient themselves.",
            "Add `StpEcsService` to the child resource table for web-service — it appears in both source files (`StpEcsService`, `StpEcsTaskDefinition`, `StpEcsTaskRole`) but the draft only lists `StpEcsTaskDefinition` and `StpEcsTaskRole`, making the table incomplete for that resource type.",
            "The 'Why this exists' H2 section is a single short paragraph that reads more like a caption than a section. Consider merging it into the opening paragraph or the 'When to use' section — as a standalone H2 it's too brief to justify its own heading."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "claude",
          "modelName": "cli-default",
          "issues": [
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "Some child resources cannot be targeted by overrides or transforms. For example, the ECS service (`ecsService`) is marked unresolvable on container workloads.",
              "reason": "The unresolvable mechanism is confirmed in source (src/api/npm/ts/config.ts checks `childResource?.unresolvable` and throws), but the specific claim that ECS service is the unresolvable example is not verifiable from provided source files. The agent confirmed the validation code exists but did not identify which specific child resources are marked unresolvable. Using a potentially wrong example could mislead readers about which resources they can/cannot target.",
              "suggestedFix": "Either verify which specific child resources are marked unresolvable and cite a correct example, or generalize the warning: 'Some child resources cannot be targeted by overrides or transforms using their property name. If you attempt to override such a child resource, Stacktape throws an error directing you to run `stacktape compile-template` and use the full CloudFormation logical name instead.'",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\api\\npm\\ts\\config.ts",
                  "quote": "if (childResource?.unresolvable) { throw new Error(errorMessage.replace('{propertyName}', propertyName)); }"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The comparison table states 'Validated against child resource existence' for overrides and 'No validation on returned properties' for transforms.",
              "reason": "The source code shows that validation checks if the child resource is marked 'unresolvable' — not that it validates the resource 'exists' in a general sense. The validation is specifically about the unresolvable flag. For transforms, saying 'no validation on returned properties' is misleading in TypeScript configs where the return type `Partial<Record<string, any>>` provides type-level safety. The actual distinction is: overrides check child resource names against a known list; transforms don't validate the returned CloudFormation properties against a CF schema.",
              "suggestedFix": "Change 'Validated against child resource existence' to 'Child resource name must match a known child resource' and change 'No validation on returned properties' to 'Returned properties not validated against CloudFormation schema'.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\api\\npm\\ts\\config.ts",
                  "quote": "if (childResource?.unresolvable) { throw new Error(errorMessage.replace('{propertyName}', propertyName)); }"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "The 'Common child resource names' table omits child resources that appear in the source: web-service is missing StpEcsService, relational-database is missing StpRdsSecurityGroup and StpRdsSubnetGroup.",
              "reason": "The curated source table lists StpEcsService for web-service and StpRdsSecurityGroup/StpRdsSubnetGroup for relational-database. The draft table replaces those with StpEcsTaskDefinition/StpEcsTaskRole and StpRdsInstance/StpRdsParameterGroup respectively. While the draft's entries are valid (confirmed by override examples in source), omitting the other child resources makes the table incomplete. A reader targeting a security group or ECS service wouldn't find guidance here.",
              "suggestedFix": "Add the omitted child resources to the table or note that the table shows commonly OVERRIDDEN resources, not all child resources, and direct readers to `compile-template` for the full list.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "| `web-service`         | `StpEcsService`, `StpEcsTaskDefinition`, `StpEcsTaskRole`                 |"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "The 'How transforms work' section shows `type CfResourceTransform = (props: Record<string, any>) => Partial<Record<string, any>>;` as the transform signature.",
              "reason": "While this type IS defined in source (src/domain/config-manager/transforms-resolver.ts), showing it in the docs as the canonical API might confuse readers. The `Partial` return type suggests you only return changed keys (and they get merged), but every example — both in the draft and source — returns the FULL properties object via spread. The reader might think they can return just `{ ReservedConcurrentExecutions: 100 }` from a transform without spreading props, when in practice the return replaces the original properties entirely. The examples correctly show the spread pattern but the type definition implies partial-merge semantics.",
              "suggestedFix": "Add a one-sentence clarification after the type: 'The return value replaces the resource properties entirely — spread the input props and add your modifications to preserve existing configuration.' This matches how all examples use transforms.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\domain\\config-manager\\transforms-resolver.ts",
                  "quote": "export type CfResourceTransform = (props: Record<string, any>) => Partial<Record<string, any>>;"
                }
              ]
            }
          ],
          "positiveFindings": [
            "AwsCdkConstruct property names (entryfilePath, exportName, constructProperties) are verified correct against types/stacktape-config/aws-cdk-construct.d.ts.",
            "$CfResourceParam is confirmed exported as a callable function from the stacktape npm package (src/api/npm/ts/directives.ts), making its usage in CodeBlock examples valid.",
            "Redis cluster child resource names (StpElastiCacheReplicationGroup, StpElastiCacheParameterGroup) are confirmed in src/api/npm/ts/child-resources.ts.",
            "The CfResourceTransform type exists exactly as shown in the draft (src/domain/config-manager/transforms-resolver.ts).",
            "All cross-links target valid routes in the documentation navigation index.",
            "The overrides shallow-merge behavior is correctly described and matches the source comparison table.",
            "All CodeBlock examples use the correct pattern: defineConfig with class-based resources, TypeScript tab label, focus markers, and proper imports.",
            "The $ResourceParam and $CfResourceParam directive usage patterns in raw cloudformationResources sections correctly show string-based syntax, consistent with the curated source docs.",
            "The CDK construct example correctly separates the CDK application code (fenced block) from the Stacktape config (CodeBlock intellisense), following the format rules.",
            "CLI commands use correct names: info:stack, compile-template — matching the /cli/ route structure."
          ],
          "summary": "The draft is well-grounded overall. Core claims about overrides, transforms, raw CloudFormation resources, and CDK constructs are verified against source code. Property names, directive exports, and child resource names all check out. The main issues are: (1) an unverified specific example in the Warning box about which child resource is 'unresolvable', (2) slightly misleading validation descriptions in the comparison table, (3) the child resources table omits some entries present in source, and (4) the transform type signature could benefit from a clarifying sentence about replacement vs merge semantics. No high-severity factual errors found."
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the supplied concept sources, but it makes several Stacktape-specific claims that are either not present in the sources or conflict with the child-resource names and CDK syntax shown there. The most important fixes are to remove or narrow unsupported child-resource behavior, correct CDK construct configuration to the sourced shape, and avoid unsupported claims about validation, merge timing, and CloudFormation output details.",
          "issues": [
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "“The `AwsCdkConstruct` resource accepts: `entryfilePath`, `exportName`, `constructProperties`...”",
              "reason": "The provided source documents CDK constructs using a `cdkConstructs` section with `constructPath` and `constructName`, not an `AwsCdkConstruct` resource with `entryfilePath`, `exportName`, or `constructProperties`. The draft may be using a newer API, but that API is not grounded in the supplied source files.",
              "suggestedFix": "Either replace this section with the sourced `cdkConstructs` shape, or remove the exact property list unless authoritative type/source files for `AwsCdkConstruct` are provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "Reference in config:\n\n```yaml\nresources:\n  handler:\n    type: function\n\ncdkConstructs:\n  - constructPath: ./cdk/monitoring.ts\n    constructName: MonitoringDashboard\n```"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Some child resources cannot be targeted by overrides or transforms. For example, the ECS service (`ecsService`) is marked unresolvable on container workloads. If you attempt to override an unresolvable child resource, Stacktape throws an error directing you to use the full CloudFormation logical name from `compile-template` output instead.”",
              "reason": "The supplied sources do not document unresolvable child resources, an `ecsService` marker, or this specific error behavior. The source instead lists `StpEcsService` as a common web-service child resource.",
              "suggestedFix": "Remove this warning or narrow it to sourced guidance: use `compile-template` to inspect generated logical names when a child resource name is unclear.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "| `web-service`         | `StpEcsService`, `StpEcsTaskDefinition`, `StpEcsTaskRole`                 |"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "The “Common child resource names” table lists `Relational database | StpRdsParameterGroup | AWS::RDS::DBParameterGroup` and omits sourced relational-database children such as `StpRdsSecurityGroup` and `StpRdsSubnetGroup`.",
              "reason": "The provided source table lists relational database child resources as `StpRdsInstance`, `StpRdsSecurityGroup`, and `StpRdsSubnetGroup`. The draft’s table introduces `StpRdsParameterGroup`, which is used in examples but is not present in the supplied common-child-resource table.",
              "suggestedFix": "Align the “common child resource names” table with the sourced names, or phrase `StpRdsParameterGroup` as an example from the override examples rather than as a generally documented common child resource.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "| `relational-database` | `StpRdsInstance`, `StpRdsSecurityGroup`, `StpRdsSubnetGroup`              |"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Overrides perform a shallow merge with existing values. Your override values take precedence over what Stacktape generates.” and “Stacktape applies overrides after it generates the base CloudFormation template.”",
              "reason": "The source says overrides set properties and the comparison table says “Shallow merge,” but it does not state application order or precedence semantics in the level of detail claimed by the draft.",
              "suggestedFix": "Keep the sourced merge claim, but narrow precedence/order wording to something like: “Overrides are documented as a shallow merge for setting child-resource properties; inspect the compiled template to verify the final result.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "| Merge behavior | Shallow merge           | Full control             |"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Validated against child resource existence” / “No validation on returned properties” in the Overrides vs transforms table.",
              "reason": "The supplied sources do not describe validation differences between overrides and transforms. They only say overrides bypass Stacktape validation in best practices, and transforms return modified properties.",
              "suggestedFix": "Remove the validation row or replace it with sourced caution: “Overrides and transforms can bypass Stacktape’s normal validation; inspect the compiled template before deploying.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "5. **Test thoroughly**: Overrides bypass Stacktape's validation"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The template includes all resolved overrides, applied transforms, synthesized CDK constructs, and raw CloudFormation resources.”",
              "reason": "The source states that `compile-template` shows the final CloudFormation template after overrides and transforms and outputs `compiled-template.yaml`. It does not explicitly say synthesized CDK constructs and raw CloudFormation resources are included in that output.",
              "suggestedFix": "Narrow to the sourced claim: “Use `compile-template` to inspect the final CloudFormation template after overrides and transforms. Raw resources should also be visible as CloudFormation resources, but verify the output before deploying.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "To see the final CloudFormation template after overrides and transforms:\n\n```bash\nstacktape compile-template --stage dev --region us-east-1\n```\n\nThis outputs `compiled-template.yaml` which you can inspect."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Overrides bypass Stacktape's validation and auto-configuration”",
              "reason": "The source supports that overrides bypass Stacktape validation, but it does not state that overrides bypass Stacktape auto-configuration. That wording could mislead readers into thinking using any override disables auto-configuration broadly.",
              "suggestedFix": "Change to: “Overrides can bypass Stacktape’s normal validation for the overridden CloudFormation properties and may conflict with generated configuration.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "5. **Test thoroughly**: Overrides bypass Stacktape's validation"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "“Raw CloudFormation resources are subject to AWS CloudFormation's per-stack resource limit (500 resources by default)” and FAQ claim “extendable to 2,500 via AWS support.”",
              "reason": "The supplied Stacktape sources do not mention CloudFormation stack resource limits. This is general AWS knowledge, but the exact support-extension number is a quota detail that can drift and is not sourced here.",
              "suggestedFix": "Either remove the extension number or phrase more generally: “Raw CloudFormation resources count toward AWS CloudFormation stack resource quotas.”",
              "evidence": []
            }
          ],
          "positiveFindings": [
            "The draft avoids the forbidden hand-authored Stacktape YAML configuration examples and uses `<CodeBlock intellisense>` for full Stacktape config examples.",
            "The first paragraph clearly explains what overrides and escape hatches are and why they matter.",
            "The draft correctly uses existing navigation routes for internal links such as `/configuration/directives`, `/resources/advanced/raw-cloudformation-resources`, and `/resources/advanced/aws-cdk-constructs`."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "No API-completeness or union-variant issues found for this concept page because the page metadata has no primary typeName and the provided sources do not expose a primary config type or relevant union members to audit. The main remaining problems are code-example format and one non-demonstrative snippet pattern outside the strict API audit checks.",
          "issues": [
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "Several fenced `typescript` snippets show Stacktape configuration fragments instead of using `<CodeBlock intellisense tabs={[{ label: 'TypeScript', ... }]} />`.",
              "reason": "The page rules require Stacktape configuration examples to use the CodeBlock component. The snippets under `Referencing Stacktape resources from CloudFormation` and `DependsOn for ordering` are config fragments containing `cloudformationResources`, `$ResourceParam`, and `$CfResourceParam`, so they should either be converted to proper CodeBlock examples or rewritten as prose/application-code-only snippets.",
              "suggestedFix": "Convert these Stacktape config fragments into `<CodeBlock intellisense>` examples with a full `defineConfig` wrapper, or remove the code blocks and describe the directive usage in prose.",
              "evidence": [
                {
                  "file": "Draft page",
                  "quote": "```typescript\ncloudformationResources: {\n  InvokePermission: {\n    Type: 'AWS::Lambda::Permission',"
                },
                {
                  "file": "Draft page",
                  "quote": "```typescript\ncloudformationResources: {\n  TopicSubscription: {\n    Type: 'AWS::SNS::Subscription',"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The page documents YAML support for transforms and overrides in prose, but all authored examples correctly use TypeScript; however, the YAML discussion may invite readers to expect YAML examples on the page.",
              "reason": "The docs policy says examples should use TypeScript as the documented source of truth and never hand-author YAML Stacktape config. The source does mention YAML support, so the claim is not unsupported, but the page should keep the recommendation narrow and avoid making YAML feel like a first-class documented path on this page.",
              "suggestedFix": "Keep the factual sentence that YAML is supported, but add a short caveat that Stacktape docs use TypeScript examples and that TypeScript is the recommended authoring path for transforms.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\concepts\\overrides-and-transforms.mdx",
                  "quote": "Transforms can be written in both TypeScript and YAML configurations."
                },
                {
                  "file": "Draft page",
                  "quote": "Transforms can be written in both TypeScript and YAML configurations. In TypeScript configs using `defineConfig`, transforms are native JavaScript functions. In YAML configs, transforms are represented as function strings."
                }
              ]
            }
          ],
          "positiveFindings": [
            "The `<CodeBlock intellisense>` examples are demonstrative: override examples set `overrides`, transform examples set `transforms`, raw CloudFormation uses `cloudformationResources`, and the CDK section uses `AwsCdkConstruct`.",
            "The page covers all major escape-hatch concepts present in the provided sources: child resources, overrides, transforms, raw CloudFormation resources, `$CfResourceParam`, `$ResourceParam`, `DependsOn`, CDK constructs, and template inspection.",
            "The page avoids hand-authored YAML Stacktape configuration blocks in the draft, which satisfies the strongest forbidden example-pattern requirement."
          ]
        },
        {
          "verifierId": "code-block-validator",
          "modelProvider": "deterministic",
          "modelName": "local",
          "summary": "Deterministic check of <CodeBlock intellisense> Stacktape config examples and generated MDX component usage.",
          "issues": [
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #4 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #5 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            }
          ],
          "positiveFindings": []
        }
      ],
      "status": "needs-human-review",
      "passed": false
    },
    {
      "iteration": 3,
      "draftPath": "/Users/congy/Desktop/projects/stacktape/docs/scripts/generate-docs/.state/configuration__overrides-and-escape-hatches/iteration-3.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "Clear progression from simple (overrides) to complex (CDK constructs) that respects the reader's learning curve.",
            "The overrides-vs-transforms comparison table is well-placed and gives readers a fast decision signal.",
            "Multiple realistic, copyable CodeBlock examples for each concept — the Lambda reserved-concurrency and ECS ulimits examples are exactly what a real team would search for.",
            "Comprehensive FAQ (10 entries) covering Stacktape-specific, AWS-general, and comparison questions — hits all three required categories.",
            "The 'When NOT to use escape hatches' section is prominently placed and gives concrete, opinionated guidance, not circular advice.",
            "Best practices section is tightly written and actionable rather than generic.",
            "The `compile-template` tip is repeated at the right moments (after transforms, and in the final section) without feeling padded.",
            "The Warning callout about unresolvable child resources prevents a real gotcha that would otherwise surface only at deploy time."
          ],
          "problems": [
            "Potential factual inaccuracy: the `AwsCdkConstruct` CodeBlock uses `entryfilePath` and `exportName` as property names, but both source files consistently show `constructPath` and `constructName`. The class-based API may use different names, but without the type definitions in scope this cannot be confirmed, and a first-time reader copying the CDK example could get a runtime or type error.",
            "Multiple standalone `cloudformationResources: { ... }` and `$CfResourceParam(...)` snippets appear in plain fenced ```typescript blocks — they are partial Stacktape config (not wrapped in `defineConfig`) and they show the directive used as a bare JS function call without an import statement or surrounding context. A copy-pasting reader has no way to know how to integrate these fragments.",
            "The 'Why this exists' H2 section substantially restates the opening paragraph and adds little new information. It reads as backbone padding rather than earned content.",
            "The Warning callout references `ecsService` (lowercase camelCase) as the key to avoid, but every other child resource name on the page uses the `StpXxx` prefix convention. This is internally inconsistent and will confuse a reader trying to understand the naming scheme.",
            "The 'How transforms work' subsection shows a raw TypeScript type signature (`type CfResourceTransform = ...`) in a fenced typescript block without any prose explaining where this type comes from or how to use it — it reads as an unexplained internal detail."
          ],
          "mandatoryFixes": [
            "Verify `AwsCdkConstruct` property names against the actual TypeScript types. If the class-based API uses `entryfilePath` and `exportName`, add a sentence naming them explicitly and keep the example. If it uses `constructPath` and `constructName` (as the source files show), update the CodeBlock accordingly. Do not leave a CodeBlock whose property names contradict all available source evidence.",
            "Remove or complete the fenced ```typescript snippets in the 'Referencing CloudFormation resources from Stacktape' and 'DependsOn for ordering' sections. As standalone objects without `defineConfig`, they cannot be copied and run. Either integrate them into a full `<CodeBlock intellisense tabs={[...]} />` with `defineConfig`, or replace them with prose descriptions and keep only the complete CodeBlock examples already on the page.",
            "Fix the Warning callout: replace `ecsService` with the correct `StpEcsService` logical name to match the naming convention used throughout the page, or add a parenthetical clarifying that the internal key differs from the `Stp`-prefixed convention."
          ],
          "optionalImprovements": [
            "Merge 'Why this exists' into the opening paragraph or cut it. The information it adds (Stacktape covers 80%, overrides close the gap) can be expressed in one sentence appended to the intro, freeing the H2 slot for something genuinely new.",
            "Remove the 'How transforms work' subsection's raw type signature fenced block entirely. The prose description ('transforms are functions that receive Properties and return modified properties') and the surrounding examples communicate this more clearly than a disembodied TypeScript type.",
            "The common child resource names table omits `StpEcsService` (web service's ECS service child resource), which the source files list. Consider adding it with a note that it cannot be targeted by name-based overrides — this reinforces the Warning callout and makes the table actually authoritative.",
            "The standalone `$CfResourceParam` fenced typescript block showing three usage examples uses `//` comments to label each variant. Since these are TypeScript (not shell), this doesn't violate the copy-button rule, but the three variants could be condensed into one clear prose description plus a single representative example to reduce visual noise.",
            "The FAQ answer for 'Can I reference CDK construct outputs via $CfResourceParam?' is accurate but ends abruptly. Add a one-sentence note that the synthesized logical names follow CDK's naming convention (scope ID + construct ID) so the reader knows what to look for in the compiled template output."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 9,
            "completeness": 7,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "All Stacktape config examples correctly use <CodeBlock intellisense tabs={[...]} /> — no forbidden YAML blocks or plain-object StacktapeConfig pattern anywhere.",
            "Covers all three escape hatch mechanisms (overrides, transforms, raw CF, CDK constructs) with a clear structure and realistic code examples.",
            "Overrides vs transforms comparison table is well-designed and actionable — engineers can decide at a glance which to reach for.",
            "Discovery section (info:stack, compile-template) answers the most common practical blocker: 'how do I find the child resource name?'",
            "FAQ section is broad and high-quality, spanning Stacktape-specific, AWS-mechanics, and comparison questions.",
            "Decision guidance on when to use CDK constructs vs raw CloudFormation is concrete and opinionated.",
            "Best-practices section is specific, not generic — especially 'use transforms for array modifications' and 'validate with compile-template'.",
            "Shell blocks are one command per block with no explanatory # comments inside them."
          ],
          "problems": [
            "Ungrounded behavioral claim in Warning box: 'the ECS service (ecsService) is marked unresolvable on container workloads. If you attempt to override an unresolvable child resource, Stacktape throws an error directing you to use the full CloudFormation logical name.' This specific runtime error behavior and the 'unresolvable' terminology do not appear in any provided source file. It reads as fabricated and could cause engineers to trust a stated error path that may not exist.",
            "CDK construct property names are not grounded in source. The draft uses `entryfilePath`, `exportName`, and `constructProperties` on `AwsCdkConstruct`. The only source file covering CDK (curated docs, low priority) shows a completely different shape: `constructPath` and `constructName` as keys in a `cdkConstructs` array at the top level. Since these property names determine whether the generated config actually works, this is a material inaccuracy risk.",
            "$CfResourceParam and $ResourceParam are imported as named exports from 'stacktape' in two CodeBlock examples. The provided source files consistently show these used as string directives (e.g., `'$CfResourceParam(\\'AlertTopic\\', \\'TopicArn\\')'`), not as imported JavaScript functions. If these are string-based directives rather than callable imports, both CodeBlock examples would produce non-functional configs.",
            "The 'Common child resource names' table includes Redis entries (StpElastiCacheReplicationGroup, StpElastiCacheParameterGroup) that do not appear in any provided source file. Including unverified child resource names in a lookup table is directly harmful — engineers will try to override those keys and hit hard-to-debug CloudFormation errors.",
            "Partial Stacktape config fragments are shown in plain fenced TypeScript blocks in the 'Referencing Stacktape resources from CloudFormation' and 'DependsOn for ordering' sections (`cloudformationResources: { ... }` without `defineConfig` wrapper). These technically avoid rule #3 (no class instantiation), but they show Stacktape config structure outside <CodeBlock intellisense>, which breaks the pattern the reader has been shown throughout the page and may produce copy-paste fragments that don't compile."
          ],
          "mandatoryFixes": [
            "Remove the Warning box claim that 'the ECS service (ecsService) is marked unresolvable' and that Stacktape throws a specific error directing the user to use the full logical name. This behavior is not grounded in any provided source. Replace with a source-grounded note (or omit the box entirely) rather than asserting a specific runtime error path.",
            "Verify the AwsCdkConstruct property names against the actual TypeScript types before publishing. The draft uses `entryfilePath`, `exportName`, `constructProperties` but the only available source shows `constructPath`/`constructName` in a top-level `cdkConstructs` array. If the class-based API uses different property names, every CDK example on this page will fail at deploy time. Either confirm the class API via the types or align with the source-file shape.",
            "Verify whether $CfResourceParam and $ResourceParam are importable functions from 'stacktape' or string directives. If they are string directives (as the source files consistently show), remove their imports from the CodeBlock examples and show them as string values instead (e.g., `environment: [{ name: 'TOPIC_ARN', value: '$CfResourceParam(\\'AlertTopic\\', \\'TopicArn\\')' }]`). Using the wrong form will make the 'Adding a raw CloudFormation resource' and CDK code examples non-functional.",
            "Remove Redis child resource names (StpElastiCacheReplicationGroup, StpElastiCacheParameterGroup) from the 'Common child resource names' table. They are not present in any provided source. Only list child resource names that appear in the provided source files. If Redis child resource overrides are common enough to document, add a note that names should be confirmed via `compile-template`."
          ],
          "optionalImprovements": [
            "The 'Referencing Stacktape resources from CloudFormation' and 'DependsOn for ordering' sections show plain fenced TypeScript fragments of `cloudformationResources: { ... }`. Consider wrapping these in a full <CodeBlock intellisense> example (with defineConfig wrapper) for consistency with the rest of the page, or convert them to prose + directive-string examples.",
            "The transforms section documents a TypeScript function signature (`type CfResourceTransform = ...`) in a plain fenced typescript block. Consider moving this to prose to avoid readers confusing it for code they need to write.",
            "The 'How transforms work' subsection is thin — it just shows the function signature. Expand with one sentence on when the transform function is called (before or after overrides?) to help readers understand merge order between overrides and transforms.",
            "The comparison table under 'Overrides vs transforms' lists 'Config format: TypeScript and YAML' for overrides and 'TypeScript (native functions) and YAML (function strings)' for transforms. This is useful but currently buried after the individual sections. Consider moving the table earlier (directly after the 'Why this exists' section) so readers can pick their mechanism before reading the implementation details.",
            "The FAQ answer to 'Can I reference CDK construct outputs in my Stacktape resources?' says 'Not directly through Stacktape's typed directive system.' This is a hedged negative claim. If the source is silent on this, phrase it as 'CDK constructs are synthesized into CloudFormation resources — use $CfResourceParam with synthesized logical names visible in compile-template output' without the negative framing that implies a missing feature.",
            "Add a note under 'Raw CloudFormation resources' clarifying that CloudFormation resource names (logical IDs like 'AlertTopic') must be unique within the stack and should not clash with Stacktape-generated logical names. This is a common gotcha when naming raw CF resources."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages SHOULD have an FAQ section, aiming for 8-10 Q&A pairs but accepting fewer when the page's scope doesn't yield that many distinct, useful questions. A narrow-scope page might have 4-6 strong FAQs; that's fine — quality over quota. Mix (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?'), and (c) choosing/comparison questions ('Web service vs Lambda?'). Penalize a Stacktape-only FAQ (it should cover what readers actually search for), and penalize FAQs padded with synthetic or near-duplicate questions just to hit a number. Do NOT penalize a smaller FAQ when the page's scope justifies it; do NOT penalize FAQ absence on pure narrative pages (Getting Started steps).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "Comprehensive coverage of all four escape hatch mechanisms (overrides, transforms, raw CloudFormation, CDK constructs) in a single page, giving readers a complete mental model before they dive into specifics.",
            "The overrides-vs-transforms comparison table is well-executed and answers the reader's most common decision question directly.",
            "FAQ section spans all three required categories (Stacktape-specific, AWS use-case, comparison/choosing) with 10 Q&A pairs, all substantive and non-duplicate.",
            "Decision support is strong throughout: 'When to use' and 'When NOT to use' sections give concrete scenarios rather than circular definitions, and each mechanism section explains why you'd reach for it over alternatives.",
            "CodeBlock examples for the main mechanisms use correct `defineConfig` + class-based TypeScript syntax with focus markers, and examples look realistic rather than toy-level.",
            "Best practices section is actionable and opinionated, not generic. 'Use transforms for array modifications' and 'Validate with compile-template' are directly useful advice."
          ],
          "problems": [
            "AwsCdkConstruct property names in the TypeScript CodeBlock (`entryfilePath`, `exportName`, `constructProperties`) diverge from the source files, which show `constructPath` and `constructName`. If the TypeScript class uses different names than the draft claims, the CDK example is broken and will fail type-checking — a significant factual issue.",
            "Redis cluster child resource names (`StpElastiCacheReplicationGroup`, `StpElastiCacheParameterGroup`) in the child resource table are not in any provided source file. These appear to be invented entries.",
            "The Warning box claims 'ecsService is marked unresolvable on container workloads' and that Stacktape 'throws an error directing you to use the full CloudFormation logical name'. This specific runtime behavior is not stated in any provided source file.",
            "`$CfResourceParam` and `$ResourceParam` are imported from the 'stacktape' package in CodeBlock examples (`import { ..., $CfResourceParam } from 'stacktape'`). Dollar-prefixed directive helpers are typically used as string literals or compile-time macros in Stacktape configs, not as importable functions. These imports are unsourced and likely invalid — the code-block validator would flag them.",
            "The '$CfResourceParam' reference section shows three different usages in a single fenced TypeScript block separated by `//` comment labels. While not a bash-specific violation, it creates a copy-paste antipattern: copying the block yields all three alternatives plus comment lines that may confuse static analysis or paste into the wrong context."
          ],
          "mandatoryFixes": [
            "Correct the AwsCdkConstruct property names in the CDK CodeBlock example. The provided source files show `constructPath` and `constructName` (YAML format); the TypeScript class likely mirrors these. The draft uses `entryfilePath`, `exportName`, and `constructProperties`, which are unverified and potentially wrong. If the code-block validator flags these names, remove the CDK CodeBlock example and replace it with a prose description linking to the dedicated CDK constructs page until the correct property names can be confirmed against the actual TypeScript type definition.",
            "Remove or source-verify the Redis cluster rows (`StpElastiCacheReplicationGroup`, `StpElastiCacheParameterGroup`) from the 'Common child resource names' table. No provided source file documents these logical names. Either find a source file that confirms them or drop the rows.",
            "Remove the Warning box that asserts `ecsService` is 'marked unresolvable' and that Stacktape 'throws an error'. This specific behavior is not documented in any provided source. Replace with a general guidance note: 'Not all child resources can be targeted by logical name. If an override fails, use `stacktape compile-template` to retrieve the exact logical name from the generated template.'",
            "Remove the imports of `$CfResourceParam` and `$ResourceParam` from the 'stacktape' package in the CodeBlock `import` statements. In the raw-CloudFormation CodeBlock the import line reads `import { defineConfig, LambdaFunction, StacktapeLambdaBuildpackPackaging, $CfResourceParam } from 'stacktape'` and in the CDK CodeBlock it imports `$ResourceParam`. These directive helpers are not confirmed as importable identifiers from the package; they are typically evaluated as string directives. Show their usage as string values (e.g. `value: $CfResourceParam('AlertTopic', 'TopicArn')` called as a function in the expression position if the package exports it, or as a string `'$CfResourceParam(...)'` if it is a parse-time directive). The code-block-validator will catch this if the imports are invalid."
          ],
          "optionalImprovements": [
            "Split the three-example `$CfResourceParam` fenced TypeScript block into individual prose sentences each followed by its own small snippet. This prevents copy-paste confusion and lets AI extractors cite each pattern individually.",
            "The partial `cloudformationResources` snippets in 'Referencing Stacktape resources from CloudFormation' and 'DependsOn for ordering' use plain fenced TypeScript blocks without `defineConfig` context. Consider either wrapping them in full `<CodeBlock intellisense>` examples or adding a brief note that these are fragments to be placed inside the `cloudformationResources` key of a full `defineConfig` config, so readers can orient themselves.",
            "Add `StpEcsService` to the child resource table for web-service — it appears in both source files (`StpEcsService`, `StpEcsTaskDefinition`, `StpEcsTaskRole`) but the draft only lists `StpEcsTaskDefinition` and `StpEcsTaskRole`, making the table incomplete for that resource type.",
            "The 'Why this exists' H2 section is a single short paragraph that reads more like a caption than a section. Consider merging it into the opening paragraph or the 'When to use' section — as a standalone H2 it's too brief to justify its own heading."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is mostly grounded in the supplied generated and curated sources, and it follows the required CodeBlock pattern for full Stacktape config examples. The main issues are a few unsupported or over-specific Stacktape behavior claims, plus one major omission about unresolvable child resources that the generated source explicitly calls out.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "\"You can also view child resources in the Stacktape Console's resource inspector when inspecting a deployed stack.\"",
              "reason": "The only direct source support is older/curated prose saying the Console can view resource details. The prompt explicitly says Console UI flows must be verified against console-app source, and no console-app source is provided. This claim describes a specific Console capability/location that is not grounded in the provided authoritative Console source.",
              "suggestedFix": "Remove the sentence or narrow it to a non-flow claim only if supported elsewhere, such as relying on CLI/template inspection for this page.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/docs/_curated-docs/concepts/overrides-and-transforms.mdx",
                  "quote": "Or use the Stacktape Console's resource inspector."
                }
              ]
            },
            {
              "severity": "high",
              "type": "missing-caveat",
              "statement": "The draft omits the warning that some child resources cannot be targeted through friendly override/transform keys and may require the full CloudFormation logical name from `compile-template`.",
              "reason": "The generated source contains a concrete warning about unresolvable child resources. Omitting it is likely to mislead readers because the page presents the common child resource table as generally usable override keys, including container workload child resources where the source warns at least one child is unresolvable.",
              "suggestedFix": "Add a warning after the common child resource names table: some child resources cannot be resolved by the short key; if Stacktape errors, use `compile-template` and target the full CloudFormation logical name.",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/configuration/overrides-and-escape-hatches.md",
                  "quote": "Some child resources cannot be targeted by overrides or transforms. For example, the ECS service (`ecsService`) is marked unresolvable on container workloads. If you attempt to override an unresolvable child resource, Stacktape throws an error directing you to use the full CloudFormation logical name from `compile-template` output instead."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"Overrides perform a shallow merge with existing values. Your override values take precedence over what Stacktape generates.\" and \"For object-type properties, the override performs a shallow merge with existing values — your keys win, but keys you don't specify remain intact.\"",
              "reason": "The supplied sources state the merge behavior in the generated and curated docs, but they do not include TypeScript types or runtime implementation proving the exact precedence and shallow-merge semantics. Because this affects how users write overrides, it should either be backed by code source or worded more cautiously.",
              "suggestedFix": "Keep the high-level guidance but narrow it: \"The documented behavior is to use overrides for static values that replace or set CloudFormation properties. Inspect `compile-template` output to confirm the final merged result.\"",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/docs/_curated-docs/concepts/overrides-and-transforms.mdx",
                  "quote": "| Merge behavior | Shallow merge           | Full control             |"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"The [`stacktape info:stack`](/cli/info-stack) command with `--detailed` lists all child resources for each Stacktape resource in your deployed stack.\"",
              "reason": "The provided curated source uses the older command spelling `stacktape stack-info --stage dev --region us-east-1 --detailed`, while the draft uses `info:stack`. The navigation index confirms `/cli/info-stack` exists, but no CLI source is provided to verify the `--detailed` flag behavior or that it lists all child resources.",
              "suggestedFix": "Avoid the exhaustive \"lists all\" wording unless CLI source is available. Write: \"Use `stacktape info:stack --detailed` to inspect detailed stack information, including child resource names when available.\"",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/docs/_curated-docs/concepts/overrides-and-transforms.mdx",
                  "quote": "stacktape stack-info --stage dev --region us-east-1 --detailed"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"This writes a `compiled-template.yaml` file you can search for the resource you want to modify\" and \"This writes the final CloudFormation template to `compiled-template.yaml`.\"",
              "reason": "The curated docs say `compile-template` outputs `compiled-template.yaml`, but no CLI command source is provided to verify the exact filename or write behavior. Since this is a concrete CLI behavior claim, it should be grounded in CLI source or softened.",
              "suggestedFix": "Phrase cautiously: \"The documented output is `compiled-template.yaml`; inspect the generated template output to confirm the logical names.\"",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/docs/_curated-docs/concepts/overrides-and-transforms.mdx",
                  "quote": "This outputs `compiled-template.yaml` which you can inspect."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"Raw CloudFormation resources are merged into the final template as-is.\"",
              "reason": "The source supports adding native CloudFormation resources and using standard CloudFormation syntax, but it does not prove the exact \"merged as-is\" behavior. That phrase can imply no Stacktape transformation or synthesis touches the resources, which is stronger than the source shows.",
              "suggestedFix": "Use narrower wording: \"Stacktape includes entries from `cloudformationResources` in the generated CloudFormation template using standard CloudFormation `Type` and `Properties` syntax.\"",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/docs/_curated-docs/concepts/extending-cloudformation.mdx",
                  "quote": "Use the `cloudformationResources` section to add native CloudFormation resources to your stack. This gives you access to any AWS resource that CloudFormation supports."
                },
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/docs/_curated-docs/concepts/extending-cloudformation.mdx",
                  "quote": "CloudFormation resources use the standard CloudFormation syntax:"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "\"Can I reference CDK construct outputs in my Stacktape resources? Not directly through Stacktape's typed directive system. ... You can reference their outputs using `$CfResourceParam` with the synthesized logical names.\"",
              "reason": "The provided source documents using `AwsCdkConstruct` and says construct properties support `$ResourceParam()` and `$Secret()`, but it does not support the negative claim about typed directive output handling or the positive claim that CDK-synthesized logical names can be referenced with `$CfResourceParam`. This could change how readers wire resources together.",
              "suggestedFix": "Remove this FAQ entry or rewrite it cautiously: \"For CDK constructs, inspect the synthesized template with `compile-template` before trying to reference generated logical names. The supported Stacktape-facing properties are `entryfilePath`, `exportName`, and `constructProperties`.\"",
              "evidence": [
                {
                  "file": "/Users/congy/Desktop/projects/stacktape/@generated/llm-docs/pages/configuration/overrides-and-escape-hatches.md",
                  "quote": "The `AwsCdkConstruct` resource accepts:\n- **`entryfilePath`** — path to a `.ts` or `.js` file containing the CDK construct class\n- **`exportName`** — name of the exported class from the entry file\n- **`constructProperties`** — props passed to the construct's constructor; supports [`$ResourceParam()`](/configuration/referenceable-parameters) and [`$Secret()`](/configuration/directives) directives"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "The draft includes fenced `typescript` blocks for Stacktape config fragments under \"Referencing Stacktape resources from CloudFormation\" and \"DependsOn for ordering\" instead of using `<CodeBlock intellisense tabs={[...]}>`.",
              "reason": "The hard requirements say Stacktape configuration examples must use `CodeBlock`, while fenced TypeScript is reserved for application code. These fragments are not full `defineConfig` examples and do not instantiate Stacktape resource classes, so this is a lower-confidence MDX/style issue rather than a definite factual error.",
              "suggestedFix": "Either convert the fragments into one focused `CodeBlock intellisense` Stacktape config example, or present them as non-copyable inline fragments without a fenced TypeScript block.",
              "evidence": []
            }
          ],
          "positiveFindings": [
            "No hand-authored Stacktape YAML config examples appear in the draft.",
            "Full Stacktape configuration examples use `<CodeBlock intellisense>` with `label: 'TypeScript'` and `defineConfig` rather than the plain-object `StacktapeConfig` style.",
            "All checked internal markdown links point to routes present in the provided documentation navigation index.",
            "The draft correctly distinguishes overrides, transforms, raw CloudFormation resources, and `AwsCdkConstruct` as separate escape-hatch mechanisms."
          ]
        },
        {
          "verifierId": "code-block-validator",
          "modelProvider": "deterministic",
          "modelName": "local",
          "summary": "Deterministic check of <CodeBlock intellisense> Stacktape config examples and generated MDX component usage.",
          "issues": [
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #2 (TypeScript): TypeScript type error.",
              "reason": "line 4, col 37: Argument of type '{ engine: RdsEnginePostgres; overrides: { StpRdsParameterGroup: { Parameters: { max_connections: string; shared_buffers: string; }; }; }; }' is not assignable to parameter of type 'RelationalDatabasePropsWithOverrides'.\n  Property 'credentials' is missing in type '{ engine: RdsEnginePostgres; overrides: { StpRdsParameterGroup: { Parameters: { max_connections: string; shared_buffers: string; }; }; }; }' but required in type 'RelationalDatabaseProps'.",
              "suggestedFix": "Fix the example so it type-checks against the public Stacktape TypeScript declarations. The full hidden source must be a valid stacktape.ts file."
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #2 (TypeScript): generated YAML tab does not round-trip to type-correct TypeScript.",
              "reason": "line 4, col 37: Argument of type '{ engine: RdsEnginePostgres; overrides: { StpRdsParameterGroup: { Parameters: { max_connections: string; shared_buffers: string; }; }; }; }' is not assignable to parameter of type 'RelationalDatabasePropsWithOverrides'.\n  Property 'credentials' is missing in type '{ engine: RdsEnginePostgres; overrides: { StpRdsParameterGroup: { Parameters: { max_connections: string; shared_buffers: string; }; }; }; }' but required in type 'RelationalDatabaseProps'.",
              "suggestedFix": "Fix the TypeScript example or the YAML conversion helper so the generated YAML describes the same valid Stacktape config."
            },
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #4 (TypeScript): TypeScript type error.",
              "reason": "line 12, col 14: Type '{}' must have a '[Symbol.iterator]()' method that returns an iterator.",
              "suggestedFix": "Fix the example so it type-checks against the public Stacktape TypeScript declarations. The full hidden source must be a valid stacktape.ts file."
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #4 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            },
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #5 (TypeScript): TypeScript type error.",
              "reason": "line 12, col 31: 'props.ContainerDefinitions' is of type 'unknown'.",
              "suggestedFix": "Fix the example so it type-checks against the public Stacktape TypeScript declarations. The full hidden source must be a valid stacktape.ts file."
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "<CodeBlock intellisense> #5 (TypeScript): cannot generate YAML tab from TypeScript source.",
              "reason": "The TypeScript example parsed and type-checked, but the docs YAML converter could not derive a YAML tab. This may be a docs-converter coverage gap for valid class-based config.",
              "suggestedFix": "If the example is valid Stacktape TypeScript, extend the YAML converter for this class/property shape. Otherwise simplify the example to a converter-supported class-based defineConfig shape."
            },
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #6 (TypeScript): TypeScript type error.",
              "reason": "line 13, col 19: Type '{ name: string; value: string; }' is not assignable to type 'string | number | boolean'.",
              "suggestedFix": "Fix the example so it type-checks against the public Stacktape TypeScript declarations. The full hidden source must be a valid stacktape.ts file."
            },
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "<CodeBlock intellisense> #7 (TypeScript): TypeScript type error.",
              "reason": "line 5, col 3: Module '\"stacktape\"' has no exported member 'AwsCdkConstruct'.",
              "suggestedFix": "Fix the example so it type-checks against the public Stacktape TypeScript declarations. The full hidden source must be a valid stacktape.ts file."
            }
          ],
          "positiveFindings": []
        }
      ],
      "status": "needs-human-review",
      "passed": false
    }
  ],
  "agentModelAssignments": {
    "page-writer": "claude:cli-default",
    "first-time-user-reviewer": "claude:claude-sonnet-4-6",
    "production-engineer-reviewer": "claude:claude-sonnet-4-6",
    "ai-consumer-reviewer": "codex:cli-default",
    "factual-accuracy-verifier": "codex:cli-default",
    "source-grounding-verifier": "codex:cli-default",
    "api-completeness-auditor": "codex:cli-default",
    "code-block-validator": "local"
  },
  "finalOutputPath": "C:\\Projects\\stacktape\\docs\\docs\\configuration\\overrides-and-escape-hatches.mdx",
  "outcome": "needs-human-review",
  "pipelineStatus": "needs-human-review",
  "pipelineReviewSummary": "reviewer-avg 7.7, hard blockers 2, advisory high 0, verifier medium 11",
  "refinementAttempts": 1,
  "refinementStalled": true
}
