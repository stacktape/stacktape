{
  "pageId": "local-development__dev-mode-overview",
  "pageRoute": "local-development/dev-mode-overview",
  "startedAt": "2026-05-15T22:03:37.026Z",
  "updatedAt": "2026-05-15T22:51:06.941Z",
  "iterations": [
    {
      "iteration": 1,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\local-development__dev-mode-overview\\iteration-1.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 7,
            "completeness": 8,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "Excellent resource-type table mapping each resource to its dev-mode location (local Docker vs AWS) — directly sourced from the command's getDevCompatibleResources and immediately useful for orientation.",
            "All bash command patterns (--resources all, comma-separated, --watch, --skipResources, --freshDb, --noTunnel) are well-sourced from the CLI source and are realistic copy-paste examples.",
            "Terminal command table (rs, rs <name>, c/clear, q/quit) is accurately sourced from handleCommand in index.ts and is the kind of table first-time users genuinely need.",
            "FAQ satisfies all three required categories: Stacktape-specific, underlying AWS service, and choosing/comparison questions — with good 2-4 sentence answers throughout.",
            "Local database emulation table with Docker image names is well-sourced from the curated docs and covers all supported engine types.",
            "Environment variable section clearly shows that STP_* values point to localhost in dev mode, which is a key insight for avoiding .env file confusion — well-explained.",
            "Legacy mode section gives concrete when-to-use guidance and accurately describes the requirement for a pre-existing deployed stack, sourced from the error handling in commandDev."
          ],
          "problems": [
            "<FlowDiagram steps={[...]}> is not a documented Stacktape MDX component. It does not appear in the style guide's component palette or in any provided source. Using an unknown component will cause a rendering error or silent omission.",
            "\"bore-compatible tunnels\" introduces the word \"bore\" (a specific tunneling technology) which does not appear anywhere in the provided source files. The source only confirms tunnels exist via tunnel-manager imports — the implementation detail is unsourced.",
            "The agent HTTP API endpoint table lists specific paths (/status, /rebuild/:workload, /stop, /env/:workload, /health/:resource, /logs, /postgres/:resource/query, /redis/:resource/command, /dynamodb/:resource/scan, /opensearch/:resource/search) that are NOT present in any provided source file. The agent-server.ts source is not provided. These paths appear to be invented.",
            "The database persistence path is stated as .stacktape/dev-data/{stage}/{resourceName}/data but the curated docs (the highest-priority source available) only show .stacktape/dev-data/{stage}/ — the additional /data suffix is unverified.",
            "The agent mode section's detailed endpoint inventory is disproportionately long for an overview page targeting first-time users, especially when the specific endpoints are unverified by source."
          ],
          "mandatoryFixes": [
            "Remove <FlowDiagram steps={[...]} /> — this component is not part of the Stacktape MDX palette. Replace with a brief numbered list in prose or an <Info> callout describing the startup phases (Initialize → Select resources → Deploy dev stack → Start local databases → Start workloads → Ready).",
            "Remove \"bore-compatible\" from the tunnel description — the word \"bore\" is absent from all provided source files. Replace the sentence with: \"Stacktape creates network tunnels so Lambda executions can route traffic to Docker containers running on your machine\" — without naming the tunnel implementation.",
            "Remove or stub out the agent HTTP API endpoint table. The paths /status, /rebuild/:workload, /stop, /env/:workload, /health/:resource, /logs, and all /postgres|redis|dynamodb|opensearch query endpoints are not in any provided source file. Either say \"the agent exposes an HTTP API with endpoints for rebuilding workloads, reading logs, and querying database state\" (linking to the agent mode page for specifics), or remove the table entirely until agent-server.ts is available as a source.",
            "Correct the database data persistence path: remove the unsourced /data suffix. Write .stacktape/dev-data/{stage}/ to match the curated docs source, which is the only provided reference for this path."
          ],
          "optionalImprovements": [
            "Add a Docker prerequisite callout near the top of the page (an <Info> block before the first section). New users may not realize Docker Desktop must be installed and running before stacktape dev will work — this is a hard blocker with no helpful error message in most cases.",
            "The agent mode section is very detailed for an overview page. Consider trimming to 2-3 sentences plus a link to the dedicated /using-with-ai/agent-mode-in-dev page, leaving the endpoint reference there.",
            "\"instant feedback\" in the opening paragraph is vague. Replace with a concrete signal like \"rebuild and restart in seconds\" or remove entirely — the table and examples make the claim concrete enough on their own.",
            "The <Warning> about normal-vs-legacy stack conflict is tucked at the end of the Legacy mode section but is most relevant to new users picking their first stage name. Consider promoting it earlier, for example as an <Info> note right after the first 'stacktape dev' command example.",
            "The --devMode flag in the Command reference table lists values 'normal (default) or legacy' but the curated docs never explicitly list --devMode as a top-level flag. Cross-check with the CLI reference page (/cli/dev) before publishing to confirm the exact flag name and accepted values."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 6,
            "audienceFit": 8
          },
          "strengths": [
            "Strong scannability via well-designed tables: resources-by-run-location, local Docker images, terminal commands, flag reference, and agent endpoints — a developer can scan any of these in under 10 seconds.",
            "Core dev mode mechanics are accurately grounded: interactive picker, `--resources all`, `--skipResources` alone as a valid case, dev stack reuse on subsequent runs, and the `rs`/`rs <name>`/`c`/`q` terminal commands all match the source exactly.",
            "Accurate dev stack conflict behavior: the Warning callout correctly describes the error when a non-dev stack exists at the target stage name, and the auto-recovery logic for failed/in-progress stacks is also captured.",
            "Legacy mode description is precise — requires an existing deployment, no local emulation, and the error hint pointing to `stacktape deploy` first is grounded in the source error message.",
            "FAQ covers all three required categories (Stacktape-specific, AWS-use-case, comparison) with 10 Q&As, and answers are concise and self-contained.",
            "Hosting bucket dev-mode filter (`b.dev`) is correctly noted — only hosting buckets with a `dev` config are included, which matches the source `filter((b) => b.dev)` exactly."
          ],
          "problems": [
            "Agent HTTP API endpoint paths (`GET /status`, `POST /rebuild/:workload`, `POST /stop`, `GET /env/:workload`, `GET /health/:resource`, `GET /logs`, and all four database query endpoints) are not grounded in the provided source files. `agent-server.ts` is not in the source list; only the import site is visible. These specific paths may not match the real implementation and will mislead engineers who try to call them.",
            "\"bore-compatible tunnels\" names a specific tunnel technology that does not appear anywhere in the provided source files. The source only imports `tunnel-manager` without naming the underlying technology.",
            "`dev.remote: true` config property claim — the draft says you can set this on a resource in the config file, but this specific syntax is not visible in the provided source. The source only shows `getRemoteResourceNames()` reading from somewhere.",
            "`<FlowDiagram steps={[...]} />` is not in the documented MDX component palette. If it does not exist in the docs renderer, the section will render as nothing or raw JSX, leaving a blank gap between the table and the Info callout.",
            "Database data persistence path is stated as `.stacktape/dev-data/{stage}/{resourceName}/data` — the curated docs only confirm `.stacktape/dev-data/{stage}/`. The extra `/{resourceName}/data` suffix is not grounded in either source."
          ],
          "mandatoryFixes": [
            "Remove the agent HTTP API endpoints table entirely or replace it with a one-sentence note directing readers to the dedicated `/using-with-ai/agent-mode-in-dev` page. Do not list specific paths (`/status`, `/rebuild/:workload`, etc.) that are not grounded in the provided source. If the actual paths are important to document, they belong on the agent mode page once the agent-server.ts source is available.",
            "Replace \"bore-compatible\" with \"automatic\" (i.e., \"Stacktape sets up automatic network tunnels...\"). The word \"bore\" does not appear in any provided source file; naming the technology without a source creates a factual anchor readers will try to verify and cannot.",
            "Remove or soft-qualify the `dev.remote: true` config option claim. Either write \"consult the resource's `dev` configuration options\" or remove the sentence. The specific property path is not visible in the provided source."
          ],
          "optionalImprovements": [
            "Replace `<FlowDiagram>` with a numbered ordered list — this communicates the same startup sequence without depending on an unknown MDX component.",
            "Narrow the database data persistence path to `.stacktape/dev-data/{stage}/` to match what the curated docs confirm. If the per-resource subdirectory exists, a parenthetical qualifier like \"(one subdirectory per resource)\" is safer than asserting a specific path ending in `/data`.",
            "The agent mode section currently describes the agent port default (7331) and the `--agentPort` override accurately, but does not mention the auto-scan behavior (the source tries ports 7331–7430 before failing). Adding a sentence like \"if port 7331 is occupied, Stacktape scans upward to find a free port\" is grounded and useful for developers running multiple sessions.",
            "The `--devMode` flag table row near the bottom says the default is `normal`, which is correct, but the CLI reference row could note that `normal` is the implicit default so readers don't need to pass the flag at all — matching the prose earlier in the page."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages MUST have a 10-question FAQ that mixes (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?', 'How do I run RDS migrations?'), and (c) choosing/comparison questions ('Web service vs Lambda?', 'ALB vs API Gateway?'). Penalize a Stacktape-only FAQ — it should cover what readers actually search for, not just what's in our types. Penalize FAQs with fewer than 10 questions. Do NOT penalize FAQ absence on pure narrative pages (Getting Started steps, recipes).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 7,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 7,
            "audienceFit": 8
          },
          "strengths": [
            "Answer-first opening paragraph is self-contained and meets the 40-75 word AEO target — describes what dev mode does, what runs locally vs. on AWS, and why it matters.",
            "Resource coverage table (resource type → where it runs → mechanism) is concrete and scannable. Correctly enumerates all dev-compatible resource categories found in the source code.",
            "Command reference section with a complete flag table gives readers a fast lookup path without needing to visit the CLI reference.",
            "FAQ has exactly 10 questions with good category distribution: Stacktape-specific (Docker requirement, reset database), AWS service questions (cost), and comparison questions (dev mode vs. deployed stage, legacy vs. normal).",
            "Lifecycle hooks section correctly identifies `beforeDev` hooks — confirmed in source as `configManager.hooks?.beforeDev || []`.",
            "Local database emulation table correctly maps config resource types to Docker images, matching the `mapResourceType` function in the source.",
            "Normal-mode vs. legacy-mode distinction is explained with concrete when-to-use guidance and a Warning callout for the non-dev stack conflict error — both confirmed in source code."
          ],
          "problems": [
            "FABRICATED AGENT API ENDPOINTS: The agent mode section lists six specific REST endpoints (GET /status, POST /rebuild/:workload, POST /stop, GET /env/:workload, GET /health/:resource, GET /logs) plus per-database query endpoints (/postgres/:resource/query, etc.). The `agent-server.ts` source file is not in the provided source list; none of these routes are grounded. This is a concrete, Stacktape-specific claim that readers will use to write integration code — and it may be wrong.",
            "UNCONFIRMED MDX COMPONENT: The page uses <FlowDiagram steps={[...]} /> which does not appear in any provided source, style guide, or example page. If this component does not exist in the docs MDX runtime, the page will render broken.",
            "DATABASE DATA PATH DISCREPANCY: The draft states data persists in `.stacktape/dev-data/{stage}/{resourceName}/data`. The curated source doc says `.stacktape/dev-data/{stage}/`. The additional `/{resourceName}/data` suffix is not confirmed by any provided source file and is likely wrong.",
            "UNGROUNDED CONFIG PROPERTY: The draft says 'you can also mark a resource as remote in your Stacktape config by setting `dev.remote: true` on the resource.' The function `getRemoteResourceNames` is imported in the source but its implementation is not provided, and the config property `dev.remote` is not confirmed in any provided type definitions.",
            "FAQ answer for agent mode startup speed says 'typically a few seconds for cached images' — a vague timing claim that isn't grounded in source and violates the concrete-numbers-only rule.",
            "The `<Info>` callout claims 'Only Docker containers need to start' on subsequent runs, which slightly understates reality — Lambda functions still need to redeploy per the source logic (which redeploys them each run)."
          ],
          "mandatoryFixes": [
            "Remove the fabricated agent HTTP API endpoint table (GET /status, POST /rebuild/:workload, POST /stop, GET /env/:workload, GET /health/:resource, GET /logs, and all database query endpoints). These routes are not grounded in any provided source file. Replace with: describe that the agent exposes an HTTP API and link to the dedicated agent mode page for full endpoint documentation. You may retain source-confirmed facts: it runs on port 7331 by default, is configurable with --agentPort, and is stopped with `stacktape dev:stop`.",
            "Remove or replace the <FlowDiagram steps={[...]} /> component. This MDX component is not confirmed to exist in the docs runtime. Replace with a simple numbered list or a table for the startup sequence, using only confirmed MDX components (Info, Warning, Tip, standard markdown).",
            "Correct the database data persistence path. The provided curated docs state `.stacktape/dev-data/{stage}/`. Remove the unconfirmed `/{resourceName}/data` suffix. Write it as `.stacktape/dev-data/{stage}/` until the exact subdirectory structure can be confirmed from source.",
            "Remove or soften the claim about `dev.remote: true` config property. This property is not confirmed in the provided type definitions or source files. If it cannot be verified, remove the sentence 'You can also mark a resource as remote in your Stacktape config by setting `dev.remote: true` on the resource' or replace it with a conditional ('check the resource's config type for a `dev.remote` option')."
          ],
          "optionalImprovements": [
            "Add a note under the Lambda functions row in the resource table clarifying that Lambda functions are deployed to AWS on every dev-mode start (not just first run), which has a small cost and startup time implication — this is implied in the source but not stated explicitly.",
            "The FAQ question 'How fast is dev mode startup?' contains the vague claim 'typically a few seconds for cached images' — replace with a more honest statement like 'startup time depends on image pull time and the number of functions being deployed; the first run is slower due to image downloads and dev stack deployment' to avoid a future source-grounding flag.",
            "Consider adding a brief mention of the `c` / `clear` command in the Rebuilding workloads table header — it is listed in the table but not called out in the section's opening paragraph, which reduces scannability for readers who just want to know how to clear the terminal without rebuilding.",
            "The Info callout says 'Only Docker containers need to start' on subsequent runs. Qualify this to say 'local Docker containers and databases start quickly; Lambda functions still redeploy on each run' to match the source behavior more precisely and avoid misleading readers who have Lambda-heavy stacks.",
            "FAQ question about legacy mode could link to the Legacy mode section anchor (#legacy-mode) for readers who arrive at the FAQ directly from search."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "claude",
          "modelName": "cli-default",
          "issues": [
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "The agent also exposes read-only query endpoints for each database type (`/postgres/:resource/query`, `/redis/:resource/command`, `/dynamodb/:resource/scan`, `/opensearch/:resource/search`) so AI assistants can inspect data without separate database clients.",
              "reason": "The draft describes these endpoints as 'read-only query endpoints', but the actual agent server exposes both read AND write endpoints. DynamoDB has POST /dynamodb/:resource/put and POST /dynamodb/:resource/delete. OpenSearch has POST /opensearch/:resource/put and POST /opensearch/:resource/delete. Redis has various write-capable commands. Calling them 'read-only' is misleading and could give users a false sense of safety.",
              "suggestedFix": "Remove 'read-only' from the description. Say 'The agent also exposes database query endpoints for each database type' and note that some endpoints support write operations.",
              "evidence": [
                {
                  "file": "src/commands/dev/agent-server.ts",
                  "quote": "POST /dynamodb/:resource/put, POST /dynamodb/:resource/delete, POST /opensearch/:resource/put, POST /opensearch/:resource/delete"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The agent mode section lists 6 'Key API endpoints' but omits the broader set of database endpoints and the /health general endpoint.",
              "reason": "The agent server exposes a much richer API surface than the 6 endpoints shown. The draft mentions database query endpoints only in passing at the bottom of the section, listing one per database type while the actual API has 7-8 per type (query, schema, tables, explain, indexes, stats, sample for Postgres; keys, get, ttl, info, type for Redis; etc.). Readers building agent integrations need to know the full scope or at least a pointer to discover it.",
              "suggestedFix": "Either expand the table to include at least the major database endpoint groups (query, schema, sample/scan per type), or add a note saying 'Each database type exposes multiple endpoints for querying, schema inspection, and data sampling. Use GET /status for the full endpoint list.' Keep the current table as a highlights summary.",
              "evidence": [
                {
                  "file": "src/commands/dev/agent-server.ts",
                  "quote": "GET /postgres/:resource/schema, GET /postgres/:resource/tables, POST /postgres/:resource/explain, GET /postgres/:resource/indexes, GET /postgres/:resource/stats, GET /postgres/:resource/sample"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "Stacktape creates bore-compatible tunnels so Lambda executions route database traffic back to Docker containers on your machine — no manual network configuration needed.",
              "reason": "While 'bore-compatible' is technically confirmed in the source (tunnel-manager uses the bore protocol with BORE_CONTROL_PORT = 7835), the term 'bore-compatible' is implementation jargon that adds no value for the reader. Most developers won't know what bore is. The phrasing implies the reader should care about bore compatibility when they don't need to.",
              "suggestedFix": "Simplify to 'Stacktape creates network tunnels so Lambda executions route database traffic back to Docker containers on your machine'. The bore protocol is an implementation detail.",
              "evidence": [
                {
                  "file": "src/commands/dev/tunnel-manager/index.ts",
                  "quote": "Tunnel server requested authentication, but Stacktape only supports unauthenticated bore-compatible servers."
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "Tunnels auto-reconnect if the connection drops.",
              "reason": "The source shows tunnel reconnection is limited to 3 attempts with exponential backoff (5s, 10s, 15s), not unlimited auto-reconnection as the draft implies. After 3 failed attempts, the tunnel stays down. This is a meaningful behavioral detail for developers debugging flaky connections.",
              "suggestedFix": "Qualify the claim: 'Tunnels automatically attempt to reconnect (up to 3 retries) if the connection drops.' Or simply remove the sentence — it's in an FAQ answer where brevity is valued.",
              "evidence": [
                {
                  "file": "src/commands/dev/tunnel-manager/index.ts",
                  "quote": "reconnectAttempts: TUNNEL_RECONNECT_ATTEMPTS = 3, reconnectDelayMs: TUNNEL_RECONNECT_DELAY_MS = 5000"
                }
              ]
            },
            {
              "severity": "low",
              "type": "missing-caveat",
              "statement": "The agent mode section describes the HTTP API but doesn't mention the MySQL endpoints.",
              "reason": "The draft mentions Postgres, Redis, DynamoDB, and OpenSearch query endpoints in the agent section, but the actual agent server also exposes MySQL endpoints (POST /mysql/:resource/query, GET /mysql/:resource/schema, etc.). Since MySQL is a supported database emulation target, the omission could confuse users running MySQL locally.",
              "suggestedFix": "Add MySQL to the list of database query endpoint types, or use a generic phrasing like 'query endpoints for each locally-emulated database type (PostgreSQL, MySQL, Redis, DynamoDB, OpenSearch)'.",
              "evidence": [
                {
                  "file": "src/commands/dev/agent-server.ts",
                  "quote": "POST /mysql/:resource/query, GET /mysql/:resource/schema, GET /mysql/:resource/tables, POST /mysql/:resource/explain"
                }
              ]
            }
          ],
          "positiveFindings": [
            "All terminal commands (rs, rs <name>, c/clear, q/quit) exactly match the handleCommand implementation in src/commands/dev/index.ts.",
            "Dev-compatible resource types are correctly and completely enumerated — the table covers all categories from getDevCompatibleResources() including container workloads, functions, hosting buckets (with dev filter), nextjs-web, and all 6 SSR web types.",
            "Legacy mode behavior is precisely described: requires existing deployed stack, no local database emulation (allEmulateableResources = [] for legacy), and the error message/hint text matches the source.",
            "Interactive picker behavior ('all selected by default') matches source: defaultValues: allResources.map((r) => r.name).",
            "The `dev.remote: true` config option claim is confirmed by the DevModeConfig type in types/stacktape-config/__helpers.d.ts and the getRemoteResourceNames() implementation.",
            "The dev stack conflict detection behavior is correctly described — the source auto-recovers failed stacks with dev-like stage names, and errors for non-dev stacks with a helpful hint.",
            "beforeDev hooks are correctly identified from configManager.hooks?.beforeDev in the source.",
            "All cross-links verified against the navigation index — no invented routes found.",
            "Database data persistence path `.stacktape/dev-data/{stage}/{resourceName}/data` matches the getDataDir() function in src/commands/dev/local-resources/index.ts.",
            "Agent mode default port (7331), --agentPort flag, and daemon spawning behavior all confirmed against source.",
            "The freshDb flag, noTunnel flag, and remoteResources behaviors are correctly described and grounded in source.",
            "The fuzzy matching claim for misspelled resource names is confirmed by the findSimilarNames function using Levenshtein distance in the source."
          ],
          "summary": "The draft is well-grounded overall. CLI flags, resource type coverage, terminal commands, legacy mode behavior, dev stack lifecycle, and cross-links are all accurate. The five issues found are medium-to-low severity: the agent database endpoints are incorrectly labeled 'read-only' when the API includes write operations (DynamoDB put/delete, OpenSearch put/delete); the agent endpoint table undersells the API surface; 'bore-compatible' is confirmed but adds jargon readers don't need; tunnel auto-reconnect omits the 3-attempt limit; and MySQL database endpoints are missing from the agent section. No high-severity factual errors found — no wrong defaults, no invented properties, no broken links."
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the provided dev-mode sources, but it contains several high-impact unsupported Stacktape-specific claims around agent HTTP APIs, local database persistence paths, remote resource behavior, and unsupported resource handling. These should be narrowed or removed unless additional source files are provided.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "Agent mode exposes endpoints including `GET /env/:workload`, `GET /health/:resource`, `GET /logs`, and database query endpoints such as `/postgres/:resource/query`, `/redis/:resource/command`, `/dynamodb/:resource/scan`, and `/opensearch/:resource/search`.",
              "reason": "The provided dev command source only shows that agent mode starts an agent server, updates workload/local-resource state, sets rebuild functions, and writes an agent log file. The specific endpoint list is not present in the provided source, and these endpoints would materially affect how readers integrate with agent mode.",
              "suggestedFix": "Remove the endpoint table and database-query endpoint claims, or replace with a cautious source-grounded statement such as: `Agent mode starts a background daemon with an HTTP server, tracks workload and local-resource state, and supports rebuild operations through the agent server. See the dedicated agent-mode page for API details.`",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "setRebuildFunctions(rebuildWorkload, rebuildAllWorkloads);\n    await startAgentServer(agentPort, join(globalStateManager.workingDir, '.stacktape', 'dev-agent'));"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "Database data persists between dev mode sessions in `.stacktape/dev-data/{stage}/{resourceName}/data`.",
              "reason": "The curated docs only ground persistence at `.stacktape/dev-data/{stage}/`; the deeper `{resourceName}/data` path is not present in the provided sources. A wrong path would mislead users trying to inspect or delete local state.",
              "suggestedFix": "Use only the sourced path: `Database data persists between runs in .stacktape/dev-data/{stage}/.` Avoid adding the resource-name/data suffix unless a local-resource implementation source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "Database data persists between runs in `.stacktape/dev-data/{stage}/`."
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "Resources like SQS queues, SNS topics, S3 buckets, and state machines are not emulated locally — they use their deployed AWS counterparts or require legacy mode.",
              "reason": "The provided dev source says normal dev mode selects workloads and emulatable databases, while legacy mode has no local database emulation. It does not state that unsupported resources automatically use deployed AWS counterparts in normal dev mode. This claim could cause readers to expect cloud resources to be wired into normal dev mode when the source does not show that behavior.",
              "suggestedFix": "Narrow the FAQ answer to the grounded support list: `Dev mode selects dev-compatible workloads and emulatable databases. The provided source does not document local emulation for SQS, SNS, S3 buckets, or state machines; use a deployed stack or legacy mode when your workflow needs real AWS resources.`",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const allWorkloads = getDevCompatibleResources();\n  // In legacy mode, we don't offer local database emulation\n  const allEmulateableResources = isLegacy ? [] : getLocalEmulateableResources();"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "Supports two modes:\n * - normal (default): Deploys a stripped-down \"dev stack\" and emulates databases locally\n * - legacy: Requires existing deployed stack, no local database emulation"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "Named resources passed to `--remoteResources` connect to their deployed AWS counterparts while everything else continues running locally. You can also mark a resource as remote in config by setting `dev.remote: true`.",
              "reason": "The provided source confirms `--remoteResources` exists in curated docs and that `getRemoteResourceNames()` is used to skip local registration for matching emulatable resources. It does not show the syntax or existence of `dev.remote: true`, and does not document the full behavior of how named resources connect to deployed counterparts.",
              "suggestedFix": "Keep the sourced CLI flag example, but remove `dev.remote: true` unless the relevant config type source is provided. Phrase the behavior narrowly: `Use --remoteResources for resources you do not want dev mode to emulate locally.`",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "stacktape dev --stage dev --region us-east-1 --remoteResources myDatabase"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const remoteResourceNames = isLegacy ? new Set<string>() : getRemoteResourceNames();"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "for (const resource of emulateableResources) {\n    if (!remoteResourceNames.has(resource.name)) {"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "Local workloads receive the same `STP_*` environment variables as deployed resources, and the example lists `STP_MY_DB_CONNECTION_STRING`, `STP_MY_DB_HOST`, and `STP_MY_DB_PORT`.",
              "reason": "The curated source shows this env-var example, but the canonical instruction says connectTo environment variables must be described as `STP_[RESOURCE_NAME]_[PARAM]`, and concrete names must match the example resource name. The draft says the database is named `myDb`, but uses `STP_MY_DB_*`; whether `myDb` normalizes to `MY_DB` is not grounded in the provided source. The draft also says values point to local endpoints for all local workloads, while the source only provides a simple example.",
              "suggestedFix": "Use the canonical pattern and avoid overgeneralizing: `For a relational database resource named `myDb`, dev mode provides Stacktape-managed `STP_[RESOURCE_NAME]_[PARAM]` variables with local connection values. For example, the connection string, host, and port point at the local emulator.` Consider naming the example resource `myDatabase` only if the exact normalized variable names are proven elsewhere.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "Local workloads receive the same `STP_*` environment variables as deployed resources, pointing to local endpoints:"
                },
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "STP_MY_DB_CONNECTION_STRING=postgres://postgres:postgres@localhost:5432/postgres\nSTP_MY_DB_HOST=localhost\nSTP_MY_DB_PORT=5432"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "Tunnels auto-reconnect if the connection drops.",
              "reason": "The provided source grounds automatic tunneling at a high level and cleanup of active tunnels, but it does not show reconnect behavior. Reconnection behavior is operationally meaningful and should not be claimed without tunnel-manager source evidence.",
              "suggestedFix": "Remove the auto-reconnect sentence, or replace it with the sourced statement: `Stacktape creates Lambda tunnels unless you pass --noTunnel.`",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "Your Lambda functions can still reach your locally-emulated databases through automatic tunneling."
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "if (!globalStateManager.args.noTunnel) {\n      devTuiManager.addSetupStep('tunnels', 'Lambda tunnels');\n    }"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "Dev mode supports `beforeDev` hooks that run before workloads start, and hooks execute automatically each time dev mode starts.",
              "reason": "The source shows that configured `beforeDev` hooks are registered in the TUI, but the provided dev command snippet does not show execution semantics or timing. The claim that they run before workloads start and on every dev-mode start is plausible but not proven by the provided source.",
              "suggestedFix": "Narrow the section to what the source shows: `Dev mode reads configured `beforeDev` hooks and displays script hooks in the dev TUI. Use the hooks and scripts page for the full execution model.`",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "// Register hooks in TUI\n  const hooks = configManager.hooks?.beforeDev || [];\n  for (const hook of hooks) {\n    if ('scriptName' in hook) {\n      devTuiManager.addHook({ name: hook.scriptName });\n    }\n  }"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "The first run deploys the dev stack once per stage; subsequent `stacktape dev` runs reuse the existing dev stack, so startup is faster after the first run and only Docker containers need to start.",
              "reason": "The source supports reusing an existing dev stack and deploying only when no existing dev stack is found. However, `only Docker containers need to start` is too broad because selected Lambda functions are still deployed/run through AWS, setup steps such as metadata loading, secrets/SSM preflight, environment injection, tunnels, and workload startup still occur.",
              "suggestedFix": "Revise to: `Normal dev mode deploys the dev stack only when it does not already exist for the selected stage. Later runs can skip that deployment step, but Stacktape still loads AWS metadata, prepares local resources, starts tunnels when needed, and starts or deploys the selected workloads.`",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "// Deploy dev stack if it doesn't exist\n    if (!stackManager.existingStackDetails) {"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const metadataSpinner = createSpinner('Loading metadata from AWS', tuiManager.colorize.bind(tuiManager));\n  await initializeStackServicesForDevPhase2();\n  metadataSpinner.success();\n\n  await ensureMissingSecretsCreated();\n  await ensureMissingSsmParamsCreated();"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "if (hasLocalResources) {\n    if (!globalStateManager.args.noTunnel) {\n      devTuiManager.addSetupStep('tunnels', 'Lambda tunnels');\n    }\n    devTuiManager.addSetupStep('env-inject', 'Injecting environment');\n  }"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft correctly separates normal dev mode from legacy mode and states that legacy mode requires an existing deployed stack.",
            "The documented interactive picker behavior, `--resources all`, comma-separated resource selection, `--skipResources`, and fuzzy matching are grounded in `src/commands/dev/index.ts`.",
            "The rebuild terminal commands `rs`, `rs <name>`, `c`/`clear`, and `q`/`quit` match the command handler in the provided source.",
            "The draft uses the documented `stacktape dev` and `stacktape dev:stop` command names and links only to routes present in the navigation index."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "No API-completeness blockers found for this page. The page has no primary config type to walk, contains no Stacktape `<CodeBlock intellisense>` examples to audit for demonstrativeness, and it names the main dev-mode variants visible in the provided source.",
          "issues": [],
          "positiveFindings": [
            "The draft distinguishes normal dev mode from legacy mode, which matches the command source comments and branching behavior.",
            "The draft names the selectable workload categories visible in `src/commands/dev/index.ts`: container workloads, functions, hosting buckets with `dev` config, Next.js, and the listed SSR web resources.",
            "The draft covers the local emulation resource categories visible in the command source: relational databases, Redis clusters, DynamoDB tables, and OpenSearch domains.",
            "The resource-selection behavior is well covered: `--resources all`, named resources, `--skipResources`, and the interactive picker with all resources selected by default are all represented in the draft."
          ]
        }
      ],
      "status": "failed",
      "passed": false
    },
    {
      "iteration": 2,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\local-development__dev-mode-overview\\iteration-2.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 7,
            "audienceFit": 7
          },
          "strengths": [
            "The opening paragraph is self-contained and immediately answers 'what is this and why does it matter' — well-optimized for extraction.",
            "The resource-type table ('Where it runs' / 'Mechanism') gives a first-timer the mental model in one glance; this is the most valuable element on the page.",
            "Concrete environment variable example with real values (postgres://postgres:postgres@localhost:5432/postgres) is exactly what the target reader needs.",
            "The warning callout about normal dev mode vs. an already-deployed non-dev stack preempts a common beginner foot-gun.",
            "FAQ covers all three required categories: Stacktape-specific, AWS use-case (cost, Docker requirement), and comparison (dev mode vs. deploy).",
            "Source-grounded accuracy is high — fuzzy matching, hosting-bucket dev config requirement, agent port default 7331, legacy mode's lack of local DB emulation all match the source code.",
            "Command reference table at the bottom works well as a quick-reference cheat sheet."
          ],
          "problems": [
            "Docker is a hard prerequisite — dev mode will not start without it — but this requirement only appears in the FAQ (Q1). A first-time user who reads 'Running dev mode' and runs the command will hit an opaque Docker error with no upfront warning. This is the single most practical gap for the stated audience.",
            "The `<Info>` callout immediately after the 'Startup flow' numbered list restates step 4 almost verbatim ('Normal dev mode deploys the dev stack only when it does not already exist…'). This is mild padding that adds length without adding information.",
            "The 'Lifecycle hooks' section is a single sentence with a link-out and no example. For a first-timer who has never written a beforeDev hook, the section gives no signal about when hooks run, what they look like, or why they'd want one — the decision-support the page's own quality rules require for opt-in features is absent.",
            "The audience for this page ('low AWS experience, first-timer') may not know what 'IAM roles', 'API gateways', and 'tunneling' mean in practice. These terms are used in the first H2 section without even a one-phrase gloss, which creates a small comprehension gap early in the page."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Add a one-line prerequisites callout near the top (or in 'How dev mode works') noting Docker Desktop must be installed and running. Currently this critical requirement is only discoverable by reading the FAQ, which most first-timers will skip until something breaks.",
            "Remove or collapse the `<Info>` callout under 'Startup flow' — its content is already covered by step 4 of the numbered list directly above it. The callout adds noise without adding information.",
            "Expand 'Lifecycle hooks' to include a minimal `beforeDev` hook example (even a one-line script reference) and one concrete scenario (e.g. 'run Prisma migrations before containers start'). One paragraph + one code snippet would make this section actionable instead of a placeholder.",
            "The agent mode section opens directly with a command. Add one sentence explaining the primary use case (AI coding assistant integration, CI scripting) so the reader can decide whether this section is relevant to them before reading the details.",
            "Consider a one-phrase gloss for 'tunneling' and 'IAM roles' the first time they appear in the main content — e.g. 'IAM roles (AWS permission configs)' and 'network tunnels (routes Lambda traffic from AWS back to your machine)'. Low-AWS-experience readers will appreciate the context without requiring a separate docs page."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 8,
            "practicalUsefulness": 9,
            "audienceFit": 9
          },
          "strengths": [
            "All shell examples are copyable and realistic; every flag variant (--resources all, --resources name1,name2, --skipResources alone) is source-grounded against the index.ts case branches",
            "Resource-type table with 'where it runs / mechanism' columns gives exactly the right mental model on first read, and is fully accurate against getDevCompatibleResources()",
            "Legacy mode coverage is honest about the constraint: requires existing stack, no local emulation — matches the source's isLegacy branches precisely",
            "Environment-variables section uses a concrete resource name (myDb → STP_MY_DB_*) that correctly illustrates the STP_[RESOURCE_NAME]_[PARAM] pattern without generic placeholder names",
            "Agent mode section correctly documents the 7331 default port, automatic fallback to nearby port, and dev:stop as the shutdown path — all source-grounded",
            "FAQ spans all three required categories (Stacktape-specific, AWS service behavior, choosing/comparison) and delivers complete 2-4 sentence answers",
            "No forbidden patterns: no fenced YAML Stacktape config, no StacktapeConfig type, no defineConfig in a fenced TS block",
            "The warning about running normal dev mode against an existing non-dev stack is accurate and source-grounded — the source throws an error with exactly that suggestion",
            "Startup flow numbered list correctly preserves the source order: credentials → resource selection → AWS metadata → dev stack deploy → local resources → workloads",
            "Local Docker image table matches the curated source exactly (postgres, mysql/mariadb, redis, amazon/dynamodb-local, opensearchproject/opensearch)"
          ],
          "problems": [
            "The Info callout after the startup flow numbered list restates the same content as steps 4-6 in the list above it with nearly identical wording — the only net-new fact (subsequent runs skip stack deployment) should be a brief follow-on sentence on step 4, not a separate callout that duplicates the whole section",
            "The bottom 'Command reference' table duplicates flags already described with examples in the 'Running dev mode' section — a reader going top-to-bottom encounters each flag twice with the same one-line description, which adds length without adding information",
            "The 'Lifecycle hooks' section (3 sentences + a link) has no concrete grounding in what a beforeDev hook looks like or which hook kinds are surfaced in the TUI vs silently skipped — the source specifically only adds hooks with 'scriptName' (script hooks), not inline-command hooks; this distinction is invisible to the reader and affects what they configure",
            "The 'Rebuilding workloads' section says 'For SSR frontends and hosting buckets, the framework dev server typically handles hot reload' — the hedge 'typically' introduces uncertainty where the behavior is actually per-framework; the sentence is uninformative as written and may mislead readers into thinking they need to do something to enable hot reload"
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Collapse the Info callout into a single follow-on sentence on startup-flow step 4: 'On subsequent runs, Stacktape skips this step and goes straight to starting local resources and workloads.'",
            "Remove the bottom 'Command reference' table or replace it with a one-line pointer to the stacktape dev CLI reference page — all flags are already explained with examples in the section above",
            "Expand the 'Lifecycle hooks' section to note that only script-type hooks (those with scriptName) are shown in the dev TUI; inline-command hooks are executed but not displayed as named steps — this distinction is visible in the source and affects what the user sees",
            "Rewrite the SSR hot-reload sentence to be concrete: say that SSR framework dev servers (Next.js, Vite-based) run with HMR enabled by default and typically don't need a manual rs rebuild — the page currently implies uncertainty where there is none",
            "Consider adding a one-line callout or note that dev mode errors with 'No dev-compatible resources found' if the config has no supported workloads — this is the first error a new user hits when they mis-configure or run dev against a config with only unsupported resource types like SQS or SNS",
            "The fuzzy-matching behavior on resource name typos is documented in the Tip callout and the FAQ but not in the main 'Run specific resources' section where the user is most likely to make the mistake — moving the Tip callout immediately after the --resources example would put it at the right reading moment"
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages MUST have a 10-question FAQ that mixes (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?', 'How do I run RDS migrations?'), and (c) choosing/comparison questions ('Web service vs Lambda?', 'ALB vs API Gateway?'). Penalize a Stacktape-only FAQ — it should cover what readers actually search for, not just what's in our types. Penalize FAQs with fewer than 10 questions. Do NOT penalize FAQ absence on pure narrative pages (Getting Started steps, recipes).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 8,
            "audienceFit": 8
          },
          "strengths": [
            "Answer-first paragraph is self-contained (52 words), clearly answers 'what is this and why does it matter', and is quote-ready for AI extraction.",
            "The resource-type table with 'Where it runs' and 'Mechanism' columns is comprehensive, accurate to the source, and is the kind of structured comparison AI engines extract most readily.",
            "Source-grounded concrete details throughout: default agent port 7331, data path `.stacktape/dev-data/{stage}/`, `beforeDev` hooks, fuzzy-match error suggestions, and the `rs`/`c`/`q` terminal commands all trace directly to `src/commands/dev/index.ts`.",
            "Decision support for `--noTunnel`, `--remoteResources`, and legacy mode is clear and opinionated — each has a concrete 'when to use' signal, which satisfies the guide-not-recipe requirement.",
            "The Warning callout about running normal dev mode against a fully-deployed (non-dev) stack is source-grounded and addresses a real gotcha users would hit silently otherwise.",
            "FAQ with exactly 10 questions is present and includes comparison and cost questions (Q5, Q6, Q10), covering multiple categories as required.",
            "All bash code examples are concrete, copy-paste runnable, use realistic flag combinations, and have no placeholder text."
          ],
          "problems": [
            "FAQ is Stacktape-heavy: 7 of 10 questions are Stacktape-specific operational questions. Category 2 (use-case questions about underlying AWS services/Docker) is almost absent — no questions about Docker networking, Lambda cold-start behavior, or how Docker Desktop resource limits affect local emulation. This misses high-intent searches readers bring from outside.",
            "The 'Lifecycle hooks' section has only two sentences and a link. It lacks any example, any tradeoff context, and any description of how hooks appear in the dev TUI or what 'beforeDev' hook types exist. Per section-depth rules, this is insufficient for an opt-in feature.",
            "The '--watch' subsection under 'Running dev mode' has a single bash command and one explanatory sentence. It lacks decision support: when auto-rebuild is preferable, when manual rebuilds (`rs`) are better (e.g., slow compilers, expensive builds), and what types of file changes trigger it.",
            "The 'Agent mode' section is thin relative to what the source supports. It covers basic startup and stop commands but omits the HTTP API surface (which the section itself teases with 'API endpoints') and doesn't explain what 'workload and local-resource state tracking' means in practice — leaving the reader unable to evaluate whether agent mode suits their use case.",
            "The 'Startup flow' numbered list under '### Startup flow' does not mention hook execution (`beforeDev` hooks are registered after workloads are added to TUI per the source), which makes the flow description incomplete for users troubleshooting hook timing."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Expand FAQ with at least 2 AWS-service use-case questions, e.g.: 'How much memory and CPU does Docker need for local dev mode?' and 'Does running Lambda in AWS during dev mode count toward the free tier?' These are high-intent searches that bring readers to this page from outside Stacktape docs.",
            "Expand the 'Lifecycle hooks' section to include: (a) an example `beforeDev` hook config or a link to the hooks page showing the `scriptName` form, (b) 1-2 sentences on when hooks run relative to workload startup, and (c) a note that only `scriptName`-based hooks appear in the dev TUI (non-script hooks are not displayed).",
            "Add 2-3 sentences of decision support to the '--watch' subsection: when auto-rebuild is ideal (short build times, frequent edits), when manual `rs` is preferable (long compile times, batch changes), and a note that the watch trigger is filesystem-change based.",
            "Add a one-paragraph description of what the agent HTTP API provides (state endpoint, rebuild endpoint) so readers can evaluate agent mode without following an external link. The source's `buildStartupMessage` and `updateAgentState` are enough to derive the surface.",
            "In the 'Startup flow' section, add a step for hook execution — e.g., '3a. Run beforeDev hooks — script hooks defined in your config run before workloads start' — to make the flow accurate and complete.",
            "The 'Command reference' flag table at the bottom repeats flags already described in section prose. Consider renaming it 'Quick reference' and noting it's a summary, or removing it entirely since the `stacktape dev` CLI reference page already serves this role."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "claude",
          "modelName": "cli-default",
          "issues": [
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "Stacktape deploys a minimal 'dev stack' to your AWS account containing only infrastructure that cannot run locally — IAM roles, secrets, and API gateways for Lambda functions. Everything else runs on your machine using Docker.",
              "reason": "The sentence 'Everything else runs on your machine using Docker' is misleading. Lambda functions are not part of the 'dev stack' infrastructure (IAM roles, secrets, API gateways), yet they are deployed to and run on AWS — not on the local machine. The draft's own table immediately below correctly shows Lambda functions running on AWS. The prose should not claim 'everything else' runs locally when a major workload category (Lambda functions) is the exception.",
              "suggestedFix": "Rewrite to: 'Containers, databases, and frontends run on your machine using Docker, while Lambda functions deploy directly to AWS with logs streamed to your terminal.' This matches the table and the curated docs' framing.",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "category: 'function' as const"
                },
                {
                  "file": "docs/_curated-docs/getting-started/dev-mode.mdx",
                  "quote": "**Lambda functions** - Deployed to AWS, but with logs streamed to your terminal"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "Lambda tunnels are set up only when there are local resources, not unconditionally",
              "reason": "The draft says 'Stacktape creates Lambda tunnels by default unless you pass --noTunnel', which could imply tunnels are always created. The source code shows tunnels are only set up when local (emulated) resources exist: `const hasLocalResources = emulateableResources.some((r) => !remoteResourceNames.has(r.name)); if (hasLocalResources) { if (!globalStateManager.args.noTunnel) { devTuiManager.addSetupStep('tunnels', 'Lambda tunnels'); } }`. If no databases are selected or all are remote, no tunnel is created even without --noTunnel.",
              "suggestedFix": "Clarify: 'Stacktape creates Lambda tunnels by default when local database emulation is active, unless you pass --noTunnel.'",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const hasLocalResources = emulateableResources.some((r) => !remoteResourceNames.has(r.name));\n  if (hasLocalResources) {\n    if (!globalStateManager.args.noTunnel) {\n      devTuiManager.addSetupStep('tunnels', 'Lambda tunnels');"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The resource-type table is comprehensive and accurately reflects the source: all categories from getDevCompatibleResources() (container workloads, functions, hosting buckets with dev config, nextjs-web, and all six SSR web types) plus all four EmulateableResource types (relational-database, redis-cluster, dynamo-db-table, open-search-domain) are covered.",
            "All cross-links point to valid routes in the documentation navigation index — no invented routes.",
            "CLI flag names and behaviors (--resources, --skipResources, --noTunnel, --remoteResources, --freshDb, --devMode, --agent, --agentPort) match the source code's globalStateManager.args references.",
            "The interactive picker description correctly states all resources are pre-selected by default, matching `defaultValues: allResources.map((r) => r.name)` in the source.",
            "The dev mode terminal commands table (rs, rs <name>, c/clear, q/quit) exactly matches the handleCommand implementation in the source.",
            "The --devMode flag values 'normal' (default) and 'legacy' are confirmed by the dev-mode-utils.ts source: `return globalStateManager.args.devMode || 'normal'`.",
            "Legacy mode correctly described as requiring an existing deployed stack, matching the source's error: 'Legacy dev mode requires an already deployed stack.'",
            "The agent default port of 7331 matches `const defaultAgentPort = 7331` in the source.",
            "The Warning box about non-dev stack conflict accurately reflects the source's error message and hint about using a different stage name or --devMode legacy.",
            "The beforeDev hooks description correctly reflects the source: `configManager.hooks?.beforeDev || []` with only scriptName hooks displayed in TUI.",
            "FAQ section has exactly 10 questions spanning Stacktape-specific, AWS use-case, and choosing/comparison categories.",
            "The startup flow description correctly orders the phases: Phase 1 initialization, resource selection, Phase 2 AWS metadata, secret/SSM preflight, dev stack check/deploy, then parallel workload startup."
          ],
          "summary": "The draft is well-grounded in source. The resource-type table, CLI flags, terminal commands, agent mode behavior, legacy mode requirements, and startup flow all accurately reflect the source code. Two issues found: (1) the prose claim 'Everything else runs on your machine using Docker' is misleading because Lambda functions deploy to AWS (the table below contradicts this claim), and (2) the tunnel setup description slightly overstates when tunnels are created — they only activate when local database emulation is in use, not unconditionally. All cross-links are valid, all flag names match the source, and the FAQ is well-structured."
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the dev command source and the curated dev-mode overview, but it contains several Stacktape-specific claims that are not grounded in the provided sources or are narrower than the implementation.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“For containers, a rebuild stops the container, re-packages the image, and starts a new container. For Lambda functions, a rebuild re-packages the code and uploads it to AWS. For SSR frontends and hosting buckets, the framework dev server typically handles hot reload.”",
              "reason": "The provided dev command source only shows that `rs` calls `rebuildAllWorkloads()` or `rebuildWorkload(workloadName)`. It does not show the per-resource rebuild mechanics claimed here, and the curated doc only says manual rebuilds can be triggered.",
              "suggestedFix": "Narrow this to the sourced behavior: `rs` rebuilds all workloads and `rs <name>` rebuilds one workload. Avoid describing internal rebuild steps unless the parallel-runner or workload-specific rebuild source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "if (command === 'rs') {\n    devTuiManager.systemLog('Rebuilding all workloads...');\n    await rebuildAllWorkloads();\n    devTuiManager.systemLog('All workloads rebuilt');\n    return;\n  }\n\n  if (command.startsWith('rs ')) {\n    const workloadName = command.slice(3).trim();\n    devTuiManager.systemLog(`Rebuilding ${workloadName}...`);\n    const found = await rebuildWorkload(workloadName);"
                },
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "When running without `--watch`, trigger rebuilds manually:\n\n- Type `rs` + Enter in the terminal to rebuild\n- Or call the HTTP API"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Your workloads receive connection strings pointing to `localhost` instead of AWS endpoints, so your application code works identically in dev mode and production without conditional logic or `.env` files.”",
              "reason": "The sources support that local workloads receive `STP_*` environment variables pointing to local endpoints. They do not support the broader claim that application code works identically without conditional logic or `.env` files in all cases.",
              "suggestedFix": "Say that dev mode uses the same `STP_*` variable names with local endpoint values, so code that already reads Stacktape-provided variables can use the same lookup path in dev mode and deployed stacks.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "Local workloads receive the same `STP_*` environment variables as deployed resources, pointing to local endpoints:"
                }
              ]
            },
            {
              "severity": "high",
              "type": "incorrect-claim",
              "statement": "“Normal dev mode creates a dedicated dev stack that is separate from a full deployment. If you try to run normal dev mode against a stage that already has a fully-deployed (non-dev) stack, Stacktape will error and suggest using a different stage name or switching to legacy mode with `--devMode legacy`.”",
              "reason": "The statement omits an implemented recovery path. If a non-dev stack is in a failed or in-progress state and looks like a dev/local stage or lacks the stack info map output, Stacktape deletes it and redeploys a fresh dev stack instead of erroring.",
              "suggestedFix": "Write that normal dev mode errors for an existing non-dev stack in the usual case, but may auto-recover certain failed or in-progress dev-looking stacks by deleting them and redeploying a dev stack.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const shouldAutoRecoverNonDevStack =\n          stackIsInRecoverableState && (isProbablyDevStage || stackLacksStackInfoMapOutput);\n\n        if (shouldAutoRecoverNonDevStack) {\n          const warnMsg = `Stack '${globalStateManager.targetStack.stackName}' exists but isn't marked as a dev stack and is in ${stackStatus}.`;\n          const infoMsg = 'Deleting the failed stack and redeploying a fresh dev stack...';"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "throw getError({\n            type: 'CLI',\n            message: `Stack '${globalStateManager.targetStack.stackName}' exists but is not a dev stack.`,\n            hint: `Use a different stage name for dev mode (e.g., --stage dev-${globalStateManager.userData?.name?.split(' ')[0]?.toLowerCase() || 'local'}), or use '--devMode legacy' to run against an existing stack.`\n          });"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Dev mode reads configured `beforeDev` hooks and displays script hooks in the dev TUI. Use hooks for setup tasks like database migrations, seed scripts, or dependency installation.”",
              "reason": "The source supports that `beforeDev` script hooks are registered in the dev TUI. It does not show when the hooks execute or support the recommended task examples, and the draft implies they are usable for setup during dev mode without grounding the execution model.",
              "suggestedFix": "Keep the sourced part: dev mode registers configured `beforeDev` script hooks in the TUI. Move task examples to the hooks page or phrase them as examples only if hook type source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "// Register hooks in TUI\n  const hooks = configManager.hooks?.beforeDev || [];\n  for (const hook of hooks) {\n    if ('scriptName' in hook) {\n      devTuiManager.addHook({ name: hook.scriptName });\n    }\n  }"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The daemon tracks workload and local-resource state and supports rebuild operations through the agent server.” and “For full agent mode documentation, including API endpoints and AI assistant integration patterns…”",
              "reason": "The source supports an agent server, state updates, and rebuild functions. It does not provide or name public API endpoints in the supplied files, so promising endpoint documentation is not grounded by the page sources.",
              "suggestedFix": "Say that agent mode runs dev mode as a daemon with an HTTP server and keeps workload/local-resource state for integrations. Avoid mentioning specific API endpoints unless the agent-server route source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "registerAgentCleanupHook();\n    setRebuildFunctions(rebuildWorkload, rebuildAllWorkloads);\n    await startAgentServer(agentPort, join(globalStateManager.workingDir, '.stacktape', 'dev-agent'));"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“If multiple sessions share the same stage name, they will conflict on the dev stack.”",
              "reason": "The provided source shows dev mode uses the target stack name and checks whether an existing stack is a dev stack, but it does not document or enforce a multi-session conflict model in the supplied files.",
              "suggestedFix": "Narrow to a cautious statement: separate stages/projects have separate target stacks, while running multiple sessions against the same project/stage is not documented in this page’s sources and should be avoided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const devHeader = {\n    action: `RUNNING DEV MODE${isLegacy ? ' (legacy)' : ''}`,\n    projectName: globalStateManager.targetStack.projectName,\n    stageName: globalStateManager.targetStack.stage,\n    region: globalStateManager.region\n  } as const;"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "if (!stackManager.existingStackDetails) {\n      if (agentEnabled) {\n        // Agent mode: plain text output for dev stack deployment\n        tuiManager.info('[~] Deploying dev stack (minimal stack for dev mode)...');"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Lambda function deployment adds a few more seconds per function.”",
              "reason": "No provided source gives timing for Lambda deployment in dev mode. This numeric performance claim could mislead expectations and is not grounded.",
              "suggestedFix": "Remove the timing estimate. Say only that Lambda functions are deployed to AWS in dev mode, so startup includes packaging and deployment work for selected functions.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "- **Lambda functions** - Deployed to AWS, but with logs streamed to your terminal"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "“Dev mode's minimal dev stack uses IAM roles, secrets, and API gateways, which have negligible or zero cost.”",
              "reason": "The supplied sources identify IAM roles, secrets, and API gateways as part of the minimal dev stack, but they do not provide pricing details. The broad AWS cost framing is plausible, but the exact “negligible or zero” phrasing is not source-grounded and could understate API Gateway or Secrets Manager charges.",
              "suggestedFix": "Phrase cost cautiously: dev mode avoids running local databases and container workloads in AWS; AWS charges can still apply for Lambda invocations, API Gateway usage, secrets, and any remote resources you connect to.",
              "evidence": []
            }
          ],
          "positiveFindings": [
            "The draft correctly uses the documented `stacktape dev` and `stacktape dev:stop` routes from the navigation index.",
            "The resource picker behavior, default selected resources, `--resources`, `--skipResources`, and fuzzy-match error behavior are grounded in `src\\commands\\dev\\index.ts`.",
            "The draft correctly separates normal dev mode from legacy mode and states that legacy mode requires an existing deployed stack.",
            "The FAQ contains exactly 10 Q&A pairs and avoids invented cross-link routes."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the available dev command source and has no Stacktape config examples to audit. The main completeness risks are around unsupported specificity and a few omitted or under-scoped command behaviors that matter to readers using dev mode day to day.",
          "issues": [
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Container workloads run as local Docker containers, databases are emulated with Docker, and Lambda functions deploy to AWS with logs streamed to your terminal.”",
              "reason": "The source explicitly supports containers, Lambda functions, and several database-like resources, but it also includes hosting buckets with `dev` config, `nextjs-web`, and multiple SSR web resources as dev-compatible workloads. The opening summary narrows dev mode to three categories and omits frontend dev workloads from the first answer paragraph.",
              "suggestedFix": "Mention frontend workloads in the opening paragraph, for example: “Container workloads and supported frontend resources run locally, local resources can emulate databases with Docker, and Lambda functions deploy to AWS with logs streamed to your terminal.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const hostingBuckets = configManager.hostingBuckets\n    .filter((b) => b.dev) // Only include hosting buckets with dev config"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const nextjsWebs = configManager.nextjsWebs.map((n) => ({\n    name: n.name,\n    type: 'nextjs-web' as const,\n    category: 'nextjs-web' as const\n  }));"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const ssrWebs = [\n    ...configManager.astroWebs.map((r) => ({ name: r.name, type: 'astro-web' as const, category: 'ssr-web' as const }))"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "ambiguous-claim",
              "statement": "“Lambda functions deployed to AWS can reach locally-emulated databases through automatic tunneling.”",
              "reason": "The code registers the setup step as “Lambda tunnels” whenever selected local resources exist and `--noTunnel` is not set, but the source does not limit the tunnel behavior to databases only. The local resource set includes relational databases, Redis, DynamoDB, and OpenSearch. Narrowing the statement to databases can mislead readers using other local resources.",
              "suggestedFix": "Use broader wording tied to local resources: “When local resources are selected and tunneling is enabled, Stacktape prepares Lambda tunnels so AWS-running Lambda functions can reach local resources.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "type EmulateableResource = {\n  name: string;\n  type: 'relational-database' | 'redis-cluster' | 'dynamo-db-table' | 'open-search-domain';\n  engineType: string;\n};"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "if (hasLocalResources) {\n    if (!globalStateManager.args.noTunnel) {\n      devTuiManager.addSetupStep('tunnels', 'Lambda tunnels');\n    }\n    devTuiManager.addSetupStep('env-inject', 'Injecting environment');\n  }"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Your workloads receive connection strings pointing to `localhost` instead of AWS endpoints, so your application code works identically in dev mode and production without conditional logic or `.env` files.”",
              "reason": "The provided source supports an environment-injection setup step and curated docs say local workloads receive `STP_*` variables pointing to local endpoints. The stronger claim that application code works identically and never needs `.env` files is not grounded in the source and is too absolute for a docs page that must separate dev-mode behavior from deployed-stack behavior.",
              "suggestedFix": "Narrow the claim: “Dev mode injects Stacktape-managed connection values for local resources, so code that reads the documented `STP_*` variables can use local endpoints during dev mode.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "Local workloads receive the same `STP_*` environment variables as deployed resources, pointing to local endpoints:"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "devTuiManager.addSetupStep('env-inject', 'Injecting environment');"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The command reference table omits the behavior that passing `--agentPort` also enables agent mode.",
              "reason": "The draft documents `--agent` and `--agentPort`, but source code treats either `--agent` or any defined `--agentPort` as enabling agent mode. This is important because readers may use `--agentPort` expecting only to reserve a port, when it actually changes process behavior to agent mode.",
              "suggestedFix": "In the Agent mode section or command table, add: “Passing `--agentPort` also enables agent mode; `--agent` is not required when a port is supplied.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const agentPortArg = globalStateManager.args.agentPort;\n  const agentEnabled = Boolean(globalStateManager.args.agent || agentPortArg !== undefined);"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The Agent mode section says the agent starts on port 7331 by default and automatically finds a nearby available port, but does not document the actual searched range or the failure behavior.",
              "reason": "The source shows Stacktape searches from 7331 through 7430 and throws an actionable error if no port is available. Because port collisions are common for background daemons, this is useful operational behavior for the intended reader.",
              "suggestedFix": "Add a sentence: “When no `--agentPort` is supplied, Stacktape searches ports `7331` through `7430`; if none is available, pass `--agentPort` or free a port in that range.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const defaultAgentPort = 7331;\n    const agentPort = agentPortArg ?? (await findAvailablePort(defaultAgentPort));"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "message: `Could not find available port for agent server (tried ${defaultAgentPort}-${defaultAgentPort + 99}).`,\n        hint: 'Specify a port with --agentPort or free up ports in that range.'"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Database data persists between dev mode sessions in `.stacktape/dev-data/{stage}/`.”",
              "reason": "The path is present in the curated docs, but the command source provided here does not show that path. Because the instruction says source code is authority and existing docs are only hints, the precise filesystem path should be either verified from local-resource source or attributed more cautiously.",
              "suggestedFix": "Either verify the path from the local-resource implementation source or soften it to the sourced behavior: “Dev mode is documented to persist local database data between runs; use `--freshDb` when you want a clean local database.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "Database data persists between runs in `.stacktape/dev-data/{stage}/`."
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "registerLocalResourceCleanupHook();"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“For containers, a rebuild stops the container, re-packages the image, and starts a new container. For Lambda functions, a rebuild re-packages the code and uploads it to AWS. For SSR frontends and hosting buckets, the framework dev server typically handles hot reload.”",
              "reason": "The command source confirms `rs` and `rs <name>` call rebuild functions, but it does not expose these per-workload rebuild mechanics. The statement is plausible, but it is not grounded in the provided `dev/index.ts` or curated docs.",
              "suggestedFix": "Keep the user-facing command behavior and avoid unverified internals: “For rebuild details by workload type, defer to the dev TUI behavior: `rs` rebuilds all workloads and `rs <name>` rebuilds one selected workload.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "if (command === 'rs') {\n    devTuiManager.systemLog('Rebuilding all workloads...');\n    await rebuildAllWorkloads();"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "if (command.startsWith('rs ')) {\n    const workloadName = command.slice(3).trim();\n    devTuiManager.systemLog(`Rebuilding ${workloadName}...`);\n    const found = await rebuildWorkload(workloadName);"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft cleanly separates normal dev mode from legacy mode and explicitly warns that normal dev mode uses a dedicated dev stack rather than a full deployed stack.",
            "The draft has no forbidden hand-authored Stacktape YAML, no `StacktapeConfig` typed examples, and no fenced TypeScript Stacktape config examples.",
            "The resource-selection behavior is well covered: `--resources all`, named resources, `--skipResources`, interactive picker defaults, and fuzzy suggestions are all grounded in the command source.",
            "The FAQ contains exactly 10 Q&A pairs and covers Stacktape-specific use, underlying AWS behavior, and choosing between dev mode, legacy mode, and deployed stages."
          ]
        }
      ],
      "status": "failed",
      "passed": false
    },
    {
      "iteration": 3,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\local-development__dev-mode-overview\\iteration-3.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 9,
            "scannability": 9,
            "completeness": 8,
            "practicalUsefulness": 9,
            "audienceFit": 9
          },
          "strengths": [
            "Resource-type table in 'How dev mode works' immediately answers 'where does my code run?' — this is exactly what a first-time reader needs before any other prose.",
            "Normal mode vs Legacy mode comparison table is decision-supportive and spans prerequisites, Docker requirements, and best-fit use cases without hand-waving.",
            "All 10 FAQ pairs span the three required categories (Stacktape-specific, AWS-service questions, and choosing/comparison), and question phrasing matches the kind of queries readers actually type into search.",
            "CLI examples are copy-paste ready and consistent in flag ordering across sections — a reader can adapt any example with minimal cognitive load.",
            "Environment variable section shows concrete `STP_*` values with realistic example output, not just a description of the mechanism.",
            "Section instructions are followed well: dev mode behavior is explicitly separated from deployed-stack behavior throughout, especially in the mode comparison table and the FAQ answer for Q5.",
            "The `<Warning>` callout about running dev mode against an existing non-dev stack is grounded in actual source behavior (the `shouldAutoRecoverNonDevStack` logic) and prevents a common gotcha.",
            "Interactive picker behavior (all resources pre-selected by default, fuzzy matching on typos) is documented accurately and with enough specificity that a user won't be surprised by the UX."
          ],
          "problems": [
            "The 'Startup flow' numbered list describes normal mode only but is not labeled as such. A reader trying to understand legacy mode startup may follow the same steps and be confused when step 4 ('Deploy dev stack') doesn't happen — especially since step 3 ('Load AWS metadata') and step 4 differ significantly between modes.",
            "Agent mode mentions 'an HTTP server' and links to the agent mode page, but gives no indication of what the HTTP server exposes (even a one-line summary like 'rebuild, status, and log endpoints'). For a first-time reader, 'HTTP server' with no further context reads as an unexplained black box.",
            "Minor redundancy: the `<Info>` callout after the startup flow ('After the first run, normal dev mode skips the dev stack deployment step') restates what the startup flow table already makes clear. The callout adds little signal and slightly inflates the section length."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Label the startup flow section 'Normal mode startup flow' (H3 or inline qualifier) and add a one-sentence note that legacy mode skips steps 4–5 and instead validates the existing stack. This prevents ambiguity without requiring a second full flow diagram.",
            "In the agent mode section, add one sentence describing what the HTTP server exposes — e.g., 'The server exposes rebuild, status, and log endpoints used by AI coding assistants and automated tooling.' This gives readers enough context to decide whether to enable agent mode before following the link.",
            "Trim or remove the `<Info>` callout after the startup flow. The information ('subsequent runs skip dev stack deployment') is already captured in the startup flow table row and the mode comparison table. Removing it tightens the section without losing coverage.",
            "Briefly name what the dev stack contains (IAM roles, secrets handling, API gateway constructs) either in the 'How dev mode works' section or in the FAQ answer for 'Does dev mode cost anything on AWS?' — the curated docs mention this and it helps readers understand the AWS footprint without guessing.",
            "The 'Lifecycle hooks' section is the shortest on the page (two sentences) and reads as filler. Either expand it with a one-line example of a `beforeDev` hook in YAML-free prose, or merge it into the startup flow section as a note under step 2 ('Select resources')."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 9,
            "completeness": 8,
            "practicalUsefulness": 8,
            "audienceFit": 9
          },
          "strengths": [
            "Accurate resource-type table showing exactly what runs locally vs. on AWS, grounded in the `getDevCompatibleResources()` source — includes all six SSR web types, hosting-bucket filter, and all four emulatable database types.",
            "Dev TUI command table (rs, rs <name>, c/clear, q/quit) is a direct read from the `handleCommand` source function — no invented commands.",
            "Normal mode vs. legacy mode comparison table is clearly structured and factually grounded: legacy emits empty emulatable resources (`isLegacy ? [] : ...`), requires existing deployment, no local DB emulation.",
            "Agent mode section accurately captures the 7331 default port, the port-range fallback (confirmed in source as `defaultAgentPort` to `defaultAgentPort + 99`), and the existing-agent detection path.",
            "FAQ covers all three required categories (Stacktape-specific, AWS use-case, and comparison questions) with concrete, actionable answers.",
            "The Warning block about non-dev stack detection is grounded in the source's `shouldAutoRecoverNonDevStack` logic and correctly describes the error path and the auto-delete recovery path.",
            "Fuzzy matching for `--resources` / `--skipResources` validation is confirmed by the `findSimilarNames` (Levenshtein) implementation in source — accurately described in the Tip callout.",
            "Hosting bucket filter ('with `dev` config') correctly reflects the `b.dev` filter in `getDevCompatibleResources`."
          ],
          "problems": [
            "Startup flow step 1 claims 'ensures required secrets and SSM parameters exist' during credential/config loading, but the source code calls `ensureMissingSecretsCreated()` and `ensureMissingSsmParamsCreated()` AFTER `initializeStackServicesForDevPhase2()` (the AWS metadata load). Secrets/SSM creation is step 4 in the actual execution order, not step 1. A developer debugging a startup failure would look in the wrong place.",
            "The Warning block on non-dev stack auto-recovery states recovery triggers 'when the existing stack is in a failed or in-progress state and the stage name suggests a dev context' — but the source also auto-recovers when `stackLacksStackInfoMapOutput` is true regardless of stage name (`isProbablyDevStage || stackLacksStackInfoMapOutput`). This condition is missing, meaning users could see unexpected auto-deletion in stacks whose stage names don't contain 'dev' or 'local'.",
            "The database emulation Docker image table omits version tags. The curated source lists `postgres:latest`, `mysql:latest`/`mariadb:latest`, `redis:latest`. Showing just `postgres` or `redis` could mislead readers who rely on explicit pinning behavior.",
            "The 'Lifecycle hooks' section mentions only `beforeDev` hooks with a single sentence and a link, but does not explain when they execute relative to workload startup (the source shows they are registered before `runParallelWorkloads`). Developers using hooks for database seeding or migrations need to know ordering."
          ],
          "mandatoryFixes": [
            "Fix startup flow step ordering: Move 'ensures required secrets and SSM parameters exist' out of step 1 (credential loading) and into a step after 'Load AWS metadata' — matching the actual source execution order: `initializeStackServicesForDevPhase1` → `initializeStackServicesForDevPhase2` → `ensureMissingSecretsCreated` → `ensureMissingSsmParamsCreated`.",
            "Expand the Warning block on auto-recovery to include the `stackLacksStackInfoMapOutput` condition: 'Stacktape also auto-recovers when the existing stack's outputs lack the expected stack-info map, regardless of the stage name.' Users need to know this to predict when auto-deletion can occur on stacks whose names don't include 'dev' or 'local'."
          ],
          "optionalImprovements": [
            "Add `:latest` (or 'latest tag') notation to the Docker image column in the database emulation table to match the curated source and prevent confusion about image pinning behavior.",
            "Expand the 'Lifecycle hooks' section with one sentence on execution timing: `beforeDev` hooks run before workloads start in `runParallelWorkloads`. Developers using hooks for pre-seeding or migration need to know this.",
            "The agent mode section could note that agent mode also sets up a scoped IAM role for the agent API (`initDevAgentCredentials()`), which is relevant for users who restrict IAM usage in their AWS accounts.",
            "Consider adding a note in the 'Local database emulation' section clarifying what 'dev config' means for hosting buckets (i.e., the resource must have a `dev` property set in the Stacktape config) rather than just saying 'with `dev` config' in the resource type table, since this is a non-obvious prerequisite."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages MUST have a 10-question FAQ that mixes (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?', 'How do I run RDS migrations?'), and (c) choosing/comparison questions ('Web service vs Lambda?', 'ALB vs API Gateway?'). Penalize a Stacktape-only FAQ — it should cover what readers actually search for, not just what's in our types. Penalize FAQs with fewer than 10 questions. Do NOT penalize FAQ absence on pure narrative pages (Getting Started steps, recipes).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 9,
            "audienceFit": 8
          },
          "strengths": [
            "Comprehensive resource-type table clearly and accurately maps every supported resource to its execution location and mechanism — a high-signal, well-sourced reference that AI engines can extract directly.",
            "Normal-mode vs. legacy-mode comparison table is a strong decision-support artifact: it answers the 'when to use which' question in a single scannable structure, reducing cognitive load for first-time readers.",
            "FAQ mixes all three required categories (Stacktape-specific, AWS use-case, choosing/comparison) and hits exactly 10 questions — the Docker Compose comparison and cost FAQ answers are particularly high-value for search intent.",
            "Agent mode documentation is concrete and well-grounded: default port 7331, auto-port-selection behavior, idempotency (returns existing agent details instead of starting a second), and the `stacktape dev:stop` shutdown path are all sourced from the command implementation.",
            "Environment variable examples use realistic values (concrete postgres localhost URL with port) rather than placeholders — copy-paste ready and source-grounded.",
            "Command reference table is accurate and complete, covering all flags mentioned in the source including `--agentPort`, `--noTunnel`, and `--devMode`."
          ],
          "problems": [
            "The 'Lifecycle hooks' section is too thin to be useful. Two sentences and a link to another page give the reader no actionable context about what `beforeDev` hooks do in dev mode specifically — for example, that they run before workloads start (useful for DB migration patterns). A section that is only a link is a backbone section padded with restated content.",
            "Mild promotional language appears in two places: 'giving you instant feedback without redeploying' (opening paragraph) and 'instant code-to-result feedback' (FAQ answer). Neither is a hard forbidden phrase, but 'instant' overpromises — the first run deploys a dev stack, which takes real time.",
            "The `--watch` flag description ('Auto-rebuild workloads when source files change') doesn't explain what change events trigger a rebuild or what the watcher monitors, leaving readers uncertain whether it watches packaging entry points, entire directories, or something else. The source doesn't clarify the watcher mechanism either, but the page should at least acknowledge the scope is workload source files.",
            "The 'Startup flow' numbered steps are correct but step 6 ('Start workloads — Containers, frontends, and Lambda function deploys run in parallel') obscures an important nuance: workloads and local databases start concurrently via `runParallelWorkloads`, which also handles tunnel setup and env injection. The current framing implies databases start before workloads (step 5 then step 6) but the parallel execution model means they overlap.",
            "The Warning callout about 'normal dev mode against a stage that already has a fully-deployed stack' is accurate but slightly misleading — the auto-recovery logic (delete and redeploy) only activates in the specific case where the stack is in a failed/in-progress state AND the stage name contains 'dev' or 'local'. The callout presents this as a broader concern than it is."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Expand 'Lifecycle hooks' section: add 1-2 sentences explaining that `beforeDev` hooks run before workloads start and give a concrete use case (e.g., running a DB migration script before the API service starts). This converts the section from a stub into actionable guidance.",
            "Replace 'giving you instant feedback without redeploying' with a more precise claim: 'Changes to container workloads and frontends rebuild locally; the first run deploys a minimal dev stack to AWS, and subsequent runs skip that deployment.' This is accurate, non-promotional, and source-grounded.",
            "Add one sentence to the `--watch` flag description clarifying scope: 'The file watcher monitors the source files and directories covered by each workload's packaging configuration.' This removes ambiguity without inventing details the source doesn't confirm.",
            "Tighten the Warning callout to reflect the actual auto-recovery condition from source: 'If the existing stack is in a failed or in-progress state AND the stage name contains `dev` or `local`, Stacktape automatically deletes and redeploys a fresh dev stack. Otherwise it errors.' The current wording implies deletion happens more broadly.",
            "The 'Startup flow' section could add a parenthetical to step 5/6 noting that local databases and workload starts overlap: '(steps 5 and 6 run concurrently)'. This is important for understanding why the first `stacktape dev` run can appear to do many things at once.",
            "Add a brief note in the 'Agent mode' section that the agent writes a lock file to `.stacktape/dev-agent/` so that subsequent `stacktape dev --agent` calls can detect and return the running agent's connection details — this explains the idempotency behavior in concrete file-system terms."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "claude",
          "modelName": "cli-default",
          "issues": [
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "Startup flow step 1: 'Load credentials and configuration — Stacktape reads your AWS credentials, config file, and ensures required secrets and SSM parameters exist.'",
              "reason": "The source code shows that `ensureMissingSecretsCreated()` and `ensureMissingSsmParamsCreated()` are called AFTER `initializeStackServicesForDevPhase2()` (metadata loading) at lines 483-484, not during Phase 1 (credential/config initialization at line 402). The draft places secrets/SSM creation in step 1, but they actually happen between step 3 (Load AWS metadata) and step 4 (Deploy dev stack). The source's own flow comment at lines 286-293 does not mention secrets/SSM at all — they are a separate concern from 'Initialize credentials and config.'",
              "suggestedFix": "Remove 'and ensures required secrets and SSM parameters exist' from step 1. Either add a separate step between steps 3 and 4 ('Ensure secrets and SSM parameters — Stacktape creates any missing secrets and SSM parameters required by your config') or fold it into step 3's description.",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "// Phase 1: Initialize credentials, config, packagingManager\n  // This also prompts for stage if not provided\n  await initializeStackServicesForDevPhase1();"
                },
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "// Phase 2: Load AWS metadata (stack info, etc.)\n  const metadataSpinner = createSpinner('Loading metadata from AWS', tuiManager.colorize.bind(tuiManager));\n  await initializeStackServicesForDevPhase2();\n  metadataSpinner.success();\n\n  await ensureMissingSecretsCreated();\n  await ensureMissingSsmParamsCreated();"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "Startup flow step 2: 'Select resources — You choose which resources to run via the interactive picker, or Stacktape uses the --resources and --skipResources flags.'",
              "reason": "The actual source code execution order is: Phase 1 (credentials/config) → resource selection → Phase 2 (metadata loading). But the draft's startup flow lists step 2 as 'Select resources' and step 3 as 'Load AWS metadata'. While this matches the actual order, the problem is the draft says step 4 is 'Deploy dev stack — In normal mode, the dev stack deploys only on the first run. Later runs reuse the existing stack.' In reality, between metadata loading (Phase 2) and the dev stack check, secrets/SSM creation happens — but the draft omits this as a step entirely, having incorrectly moved it to step 1.",
              "suggestedFix": "Restructure the startup flow to accurately reflect the source's execution order: (1) Load credentials and configuration, (2) Select resources, (3) Load AWS metadata, (4) Ensure secrets and SSM parameters, (5) Deploy dev stack (first run only), (6) Start local databases and workloads.",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "await initializeStackServicesForDevPhase2();\n  metadataSpinner.success();\n\n  await ensureMissingSecretsCreated();\n  await ensureMissingSsmParamsCreated();\n\n  if (isLegacy) {"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "Startup flow presents 'Start local databases' (step 5) and 'Start workloads' (step 6) as sequential steps.",
              "reason": "The source code shows a single combined call `await runParallelWorkloads(devCompatibleResources, selectedLocalResourceNames)` that receives both workloads and local resource names. Whether databases start before workloads within that function is not visible from the provided source file — the separation into sequential steps 5 and 6 is an assumption not grounded in the available source.",
              "suggestedFix": "Consider merging steps 5 and 6 into a single step: 'Start local resources and workloads — Docker containers start for selected databases, and containers, frontends, and Lambda deploys run in parallel.' Alternatively, keep the separation but clarify that the exact ordering is internal.",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "await runParallelWorkloads(devCompatibleResources, selectedLocalResourceNames);"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The resource type table is comprehensive and accurately maps to the source's `getDevCompatibleResources()` function — all seven SSR frontend types, hosting buckets (with dev config filter), container workloads, and Lambda functions are correctly listed.",
            "Terminal commands (rs, rs <name>, c/clear, q/quit) are precisely documented and match the source's `handleCommand` function verbatim.",
            "Agent mode behavior is accurately grounded: default port 7331 matches `const defaultAgentPort = 7331`, existing agent detection matches `getRunningAgent()` check, port auto-discovery matches `findAvailablePort()`, and the `dev:stop` command is sourced from the cleanup hint in the agent daemon code.",
            "Normal vs legacy mode distinction is correctly described: legacy mode sets `allEmulateableResources = isLegacy ? [] : getLocalEmulateableResources()` confirming no local database emulation, and the legacy error message text matches the source verbatim.",
            "The interactive picker behavior (all resources pre-selected by default) matches `defaultValues: allResources.map((r) => r.name)` exactly.",
            "Fuzzy matching for misspelled resource names is correctly documented — the source implements Levenshtein distance matching via `findSimilarNames` and surfaces suggestions in the error message.",
            "The emulatable database types (`relational-database`, `redis-cluster`, `dynamo-db-table`, `open-search-domain`) match the `EmulateableResource` type union exactly.",
            "The Warning callout about normal dev mode conflicting with existing non-dev stacks is accurately described — the source shows the exact error message, the `isProbablyDevStage` auto-recovery logic, and the suggestion to use `--devMode legacy`.",
            "All cross-links are valid routes from the navigation index — no invented routes detected.",
            "The FAQ has exactly 10 questions spanning three required categories (Stacktape-specific, underlying AWS service, and comparison/choosing questions).",
            "The page correctly avoids all forbidden patterns: no hand-authored YAML config blocks, no `StacktapeConfig` type usage, no fenced TypeScript blocks containing `defineConfig`, and no Stacktape config examples at all (appropriate for this concept page)."
          ],
          "summary": "The draft is well-grounded overall. The resource type table, terminal commands, agent mode, normal/legacy distinction, and interactive picker behavior all accurately reflect the source. The primary factual issue is in the startup flow section: secrets/SSM parameter creation is incorrectly placed in step 1 (credential loading) when the source clearly shows it happens after step 3 (metadata loading). The startup flow also presents databases and workloads as sequential steps, but the source uses a single combined `runParallelWorkloads` call. All cross-links are valid, the FAQ is complete, and no forbidden patterns are present."
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is broadly aligned with the provided dev command source, especially around resource selection, normal vs legacy mode, agent mode, rebuild commands, and dev-stack creation. The main problems are a few overbroad claims about Docker/frontend behavior, AWS cost, local database persistence, and remote resource behavior that are not grounded in the provided source or are contradicted by the command implementation.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Resources passed to `--remoteResources` connect to their deployed AWS counterparts while everything else continues running locally.”",
              "reason": "The provided `commandDev` source only uses `remoteResourceNames` to skip registering selected emulatable local resources in the TUI. The source file does not show that `--remoteResources` works for arbitrary resources or that everything else continues locally. The curated doc narrows the example to using a deployed remote database instead.",
              "suggestedFix": "Narrow the claim to emulatable database resources: “Use `--remoteResources myDatabase` when you want a selected local database resource to use its deployed counterpart instead of local emulation.” Avoid implying arbitrary resources can be made remote unless another source file proves it.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const remoteResourceNames = isLegacy ? new Set<string>() : getRemoteResourceNames();"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "for (const resource of emulateableResources) {\n    if (!remoteResourceNames.has(resource.name)) {\n      const resourceType = mapResourceType(resource.type, resource.engineType);"
                },
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "**Use deployed (remote) database instead:**"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Dev mode's dev stack uses AWS resources with negligible or zero cost.”",
              "reason": "The provided source says normal dev mode deploys a stripped-down or minimal dev stack, but it does not provide pricing guarantees or support the claim that costs are negligible or zero. This could materially mislead readers about AWS billing.",
              "suggestedFix": "Replace with a cautious cost statement: “Normal dev mode can create AWS resources for the dev stack, and Lambda functions deployed during dev mode can incur standard AWS charges. Local Docker databases and local container workloads run on your machine.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "// normal (default): Deploys a stripped-down \"dev stack\" and emulates databases locally"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "tuiManager.info('[~] Deploying dev stack (minimal stack for dev mode)...');"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Database data persists between dev mode sessions in `.stacktape/dev-data/{stage}/`.” and “This deletes the database data stored in `.stacktape/dev-data/{stage}/`…”",
              "reason": "The curated doc supports that database data persists between runs in that path, but the provided command source does not show that `--freshDb` deletes that exact directory. The deletion behavior is described only as deleting existing local database data, not the exact storage path removed.",
              "suggestedFix": "Keep the persistence sentence, but narrow the reset sentence to: “Use `--freshDb` to delete existing local database data and start fresh.” Do not state the exact deletion target unless the local-resource implementation source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "Database data persists between runs in `.stacktape/dev-data/{stage}/`."
                },
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "| `--freshDb`         | Delete existing local database data                    |"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "incorrect-claim",
              "statement": "“Container workloads, supported frontends, and databases run on your machine using Docker…” and “Dev mode uses Docker to run container workloads, frontends, and emulated databases locally.”",
              "reason": "The provided docs distinguish Docker-based containers/databases from static sites served by a local dev server. The command source lists hosting buckets and SSR web resources as dev-compatible workloads, but this file does not show that frontends run through Docker. The draft later says hosting buckets and SSR frontends use local dev servers, so the broad Docker claim is internally and source-wise overbroad.",
              "suggestedFix": "Say: “Container workloads and emulated databases use Docker. Hosting buckets and supported SSR frontends run through local dev servers.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "- **Containers** (web-service, worker-service) - Run as Docker containers on your machine\n- **Databases** - Emulated locally with Docker (PostgreSQL, MySQL, Redis, DynamoDB, OpenSearch)\n- **Static sites** (hosting-bucket, Next.js) - Served from a local dev server"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const hostingBuckets = configManager.hostingBuckets\n    .filter((b) => b.dev) // Only include hosting buckets with dev config"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const ssrWebs = [\n    ...configManager.astroWebs.map((r) => ({ name: r.name, type: 'astro-web' as const, category: 'ssr-web' as const }))"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“The agent starts on port 7331 by default (configurable with `--agentPort`). If the default port is in use, Stacktape automatically finds an available port in a nearby range.”",
              "reason": "The source supports default port 7331 and searching the range 7331-7430 when no port is found, but it does not prove fallback behavior when a user explicitly passes `--agentPort`. The wording could imply fallback happens even for a specified occupied port.",
              "suggestedFix": "Clarify the condition: “When `--agentPort` is omitted, Stacktape starts at port 7331 and searches ports 7331-7430 if needed. Use `--agentPort` to request a specific port.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const defaultAgentPort = 7331;\n    const agentPort = agentPortArg ?? (await findAvailablePort(defaultAgentPort));"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "message: `Could not find available port for agent server (tried ${defaultAgentPort}-${defaultAgentPort + 99}).`,\n        hint: 'Specify a port with --agentPort or free up ports in that range.'"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Running multiple sessions against the same project and stage is not supported and should be avoided to prevent conflicts.”",
              "reason": "The source explicitly handles an already running agent by returning existing connection details, but it does not state that non-agent sessions for the same project and stage are unsupported. This is plausible operational guidance, but it is not grounded in the provided source.",
              "suggestedFix": "Narrow to a recommendation rather than a support statement: “Avoid running multiple interactive sessions against the same project and stage because they can contend for the same local containers, ports, and dev stack.”",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "if (runningAgent) {\n      process.stdout.write(\n        `${buildAgentReadyMessage({\n          port: runningAgent.port,"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "return;\n    }\n\n    // Find available port"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "“When you omit the `--resources` flag, Stacktape shows an interactive resource picker…”",
              "reason": "The command source shows the picker only when both `--resources` and `--skipResources` are omitted. If `--skipResources` is provided without `--resources`, Stacktape does not show the picker and instead runs all except the skipped resources.",
              "suggestedFix": "Change to: “When you omit both `--resources` and `--skipResources`, Stacktape shows an interactive resource picker…”",
              "evidence": []
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "“Dev mode automatically starts Docker containers to emulate your databases locally.”",
              "reason": "The command source starts selected emulatable resources, and skips resources listed in `--remoteResources`. The sentence is acceptable as a default overview, but it should not imply every database in the config always starts locally regardless of selection or remote-resource settings.",
              "suggestedFix": "Change to: “In normal mode, selected emulatable databases start locally unless you mark them as remote with `--remoteResources`.”",
              "evidence": []
            }
          ],
          "positiveFindings": [
            "The draft correctly distinguishes normal mode and legacy mode: normal mode deploys a stripped-down dev stack and emulates databases locally, while legacy mode requires an existing deployed stack and skips local database emulation.",
            "The documented resource picker behavior, fuzzy name suggestions, `--resources all`, comma-separated resource names, and `--skipResources` behavior are mostly grounded in `src\\commands\\dev\\index.ts`.",
            "The dev terminal commands `rs`, `rs <name>`, `c`/`clear`, and `q`/`quit` match the `handleCommand` implementation.",
            "The warning about normal dev mode refusing to run against an existing non-dev stack, with limited auto-recovery for failed or in-progress likely-dev stages, is well grounded in the command source.",
            "The page uses only valid cross-links from the provided documentation navigation index."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft has no Stacktape config CodeBlock issues and it covers the main dev-mode command surface well. Because the page has no primary config type, API-property completeness is mostly not applicable. The main issues are over-broad wording around frontend/Docker behavior, a few ungrounded operational claims, and missing coverage of local resource/environment injection as a dev-mode-only distinction.",
          "issues": [
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Container workloads, supported frontends, and databases run on your machine using Docker” and “Docker required — for containers, frontends, and databases.”",
              "reason": "The source distinguishes Docker containers for container workloads and databases from frontends served by a local/framework dev server. It does not show that hosting buckets or SSR frontends run using Docker, so the draft overstates Docker as the frontend mechanism.",
              "suggestedFix": "Say containers and emulated databases use Docker. Say hosting buckets with `dev` config and SSR frontends run locally through their dev server path, without claiming Docker unless another source proves it.",
              "evidence": [
                {
                  "file": "docs/_curated-docs/getting-started/dev-mode.mdx",
                  "quote": "**Containers** (web-service, worker-service) - Run as Docker containers on your machine"
                },
                {
                  "file": "docs/_curated-docs/getting-started/dev-mode.mdx",
                  "quote": "**Static sites** (hosting-bucket, Next.js) - Served from a local dev server"
                },
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const hostingBuckets = configManager.hostingBuckets\n    .filter((b) => b.dev) // Only include hosting buckets with dev config"
                },
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const ssrWebs = [\n    ...configManager.astroWebs.map((r) => ({ name: r.name, type: 'astro-web' as const, category: 'ssr-web' as const }))"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Dev mode automatically starts Docker containers to emulate your databases locally.”",
              "reason": "The source shows local resources are selectable and only selected, non-remote resources are registered and started. The draft’s wording implies every emulatable database is always started, which misses selection and `--remoteResources` behavior.",
              "suggestedFix": "Change to: “In normal dev mode, selected emulatable databases run locally unless listed in `--remoteResources`.”",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const selectedResourceNames = await getSelectedResources(allSelectableResources);"
                },
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const emulateableResources = allEmulateableResources.filter((r) => selectedResourceNames.has(r.name));"
                },
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "for (const resource of emulateableResources) {\n    if (!remoteResourceNames.has(resource.name)) {"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The environment-variable section says local workloads receive the same `STP_*` environment variables as deployed resources, but it does not explicitly scope environment injection to local resources being present in dev mode.",
              "reason": "The source registers the “Injecting environment” setup step only when selected local resources exist. The page should avoid implying dev mode always performs local endpoint injection for every run.",
              "suggestedFix": "Add a caveat that dev-mode local endpoint injection is part of normal dev mode when selected local resources are active; legacy mode and resources marked remote use deployed resource values instead.",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const hasLocalResources = emulateableResources.some((r) => !remoteResourceNames.has(r.name));\n  if (hasLocalResources) {\n    if (!globalStateManager.args.noTunnel) {\n      devTuiManager.addSetupStep('tunnels', 'Lambda tunnels');\n    }\n    devTuiManager.addSetupStep('env-inject', 'Injecting environment');\n  }"
                },
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const remoteResourceNames = isLegacy ? new Set<string>() : getRemoteResourceNames();"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Lambda functions deployed to AWS can reach locally-emulated databases through automatic tunneling.”",
              "reason": "The source registers the tunnel setup step when selected local resources exist and `--noTunnel` is not set, but it does not limit that behavior specifically to databases in the visible source. The wording should align with the source’s “local resources” terminology or cite the curated doc separately.",
              "suggestedFix": "Use narrower wording: “When selected local resources are active and `--noTunnel` is not set, dev mode starts Lambda tunnels so AWS-running workloads can reach those local resources.”",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "if (hasLocalResources) {\n    if (!globalStateManager.args.noTunnel) {\n      devTuiManager.addSetupStep('tunnels', 'Lambda tunnels');\n    }"
                },
                {
                  "file": "docs/_curated-docs/getting-started/dev-mode.mdx",
                  "quote": "Your Lambda functions can still reach your locally-emulated databases through automatic tunneling."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Use legacy mode when: You want to test workload code against real deployed AWS resources (databases, queues, storage).”",
              "reason": "The source supports that legacy mode requires an existing deployed stack and has no local database emulation, but the visible source does not specifically document queues or storage as usable legacy-mode targets. This is a broad resource-parity implication on a page that must separate dev-mode behavior from deployed-stack behavior.",
              "suggestedFix": "Say legacy mode connects workloads to the existing deployed stack and deployed AWS resources. Avoid naming queues and storage unless a source for those resource classes in legacy mode is available.",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "// Legacy mode: require existing deployed stack"
                },
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "Legacy dev mode requires an already deployed stack. Deploy with 'stacktape deploy --stage ${globalStateManager.targetStack.stage}' first, or use '--devMode normal' to create a dev stack."
                },
                {
                  "file": "docs/_curated-docs/getting-started/dev-mode.mdx",
                  "quote": "Legacy mode requires an existing deployment and connects directly to deployed AWS resources."
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“If the default port is in use, Stacktape automatically finds an available port in a nearby range.”",
              "reason": "The source says the failure message references ports 7331-7430, but the actual `findAvailablePort` implementation is not included. “Nearby range” is vague and not directly grounded by the provided implementation.",
              "suggestedFix": "Use the source-backed wording: “By default, agent mode starts on port 7331. If Stacktape cannot find an available agent port, it asks you to pass `--agentPort` or free a port in the 7331-7430 range.”",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const defaultAgentPort = 7331;\n    const agentPort = agentPortArg ?? (await findAvailablePort(defaultAgentPort));"
                },
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "message: `Could not find available port for agent server (tried ${defaultAgentPort}-${defaultAgentPort + 99}).`,\n        hint: 'Specify a port with --agentPort or free up ports in that range.'"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "missing-caveat",
              "statement": "The FAQ answer “Does dev mode work with all Stacktape resource types?” says other resource types such as SQS, SNS, S3, and state machines are not emulated locally, then says to use `--remoteResources` or legacy mode for their deployed counterparts.",
              "reason": "The source-backed `--remoteResources` flow applies to local emulatable resources via `getRemoteResourceNames()` and selected local resource filtering. The visible source does not show `--remoteResources` selecting non-emulatable resources such as SQS, SNS, S3, or state machines.",
              "suggestedFix": "Separate the ideas: say non-listed resource types are not local workloads/local emulators in dev mode. For emulatable databases, `--remoteResources` connects to deployed counterparts; for broader deployed-stack behavior, use legacy mode or a deployed stage.",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const allEmulateableResources = isLegacy ? [] : getLocalEmulateableResources();\n  const remoteResourceNames = isLegacy ? new Set<string>() : getRemoteResourceNames();"
                },
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "const emulateableResources = allEmulateableResources.filter((r) => selectedResourceNames.has(r.name));"
                },
                {
                  "file": "docs/_curated-docs/getting-started/dev-mode.mdx",
                  "quote": "**Use deployed (remote) database instead:**\n\n```bash\nstacktape dev --stage dev --region us-east-1 --remoteResources myDatabase\n```"
                }
              ]
            }
          ],
          "positiveFindings": [
            "The draft avoids forbidden hand-authored YAML and does not use plain-object `StacktapeConfig` examples.",
            "The draft documents both dev mode variants, `normal` and `legacy`, and correctly treats them as separate workflows.",
            "The draft covers the main command flags visible in the source: `--resources`, `--skipResources`, `--watch`, `--remoteResources`, `--freshDb`, `--noTunnel`, `--devMode`, `--agent`, and `--agentPort`.",
            "The rebuild terminal commands are grounded in the source: `rs`, `rs <name>`, `c`/`clear`, and `q`/`quit`."
          ]
        }
      ],
      "status": "failed",
      "passed": false
    },
    {
      "iteration": 4,
      "draftPath": "C:\\Projects\\stacktape\\docs\\scripts\\generate-docs\\.state\\local-development__dev-mode-overview\\iteration-4.mdx",
      "reviewerResults": [
        {
          "reviewerId": "first-time-user-reviewer",
          "persona": "Developer trying Stacktape for the first time. Low AWS experience. Wants to copy-paste a working example and ship something, then come back to the page when they need to extend it. Cares about: a clear path from \"what is this\" to \"I have it running\", absence of unexplained AWS jargon, a runnable example near the top, and confidence that the page is telling them everything they need to start. Does NOT care about exhaustive option coverage — that's what the API reference is for.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 9,
            "completeness": 9,
            "practicalUsefulness": 9,
            "audienceFit": 8
          },
          "strengths": [
            "Comprehensive and source-accurate coverage of all dev mode functionality: resource types, startup flow, normal vs legacy mode, agent mode, and auto-recovery conditions are all grounded in the source code.",
            "Excellent use of tables throughout — resource-to-mechanism mapping, mode comparison, command reference, and emulator images make the page scannable in under 30 seconds.",
            "Well-structured FAQ with exactly 10 pairs covering Stacktape-specific, AWS-service-use-case, and comparison questions as required.",
            "Startup flow subsection accurately mirrors the actual execution order from `commandDev()` in the source (Phase1 → select resources → Phase2 → secrets/SSM → dev stack → workloads).",
            "Auto-recovery edge case documented accurately: the condition `isProbablyDevStage || stackLacksStackInfoMapOutput` is correctly described in the Warning callout, including the non-obvious implication that stacks without 'dev'/'local' in their name can still be auto-deleted if outputs are missing.",
            "Normal vs legacy comparison table is a strong decision-support element that concretely answers the most common 'which mode do I use?' question.",
            "Appropriate MDX callout usage: Info for the 'subsequent runs skip dev stack deploy' nuance, Tip for fuzzy matching, Warning for the potentially destructive auto-recovery behavior.",
            "No forbidden patterns: no YAML config blocks, no StacktapeConfig type import, no fenced TypeScript with defineConfig."
          ],
          "problems": [
            "'bore-compatible TCP relay' appears twice (intro paragraph and FAQ) but this specific implementation detail is not found in the provided source files. The curated docs say only 'automatic tunneling'; the source imports `tunnel-manager` but does not document the underlying mechanism. This is an ungrounded technical claim that also adds jargon for a low-AWS-experience audience.",
            "The first paragraph of the 'How dev mode works' H2 section largely restates the page's intro paragraph (same three-way split: containers/databases on Docker, Lambda on AWS, frontends on dev server). The intro paragraph and the section's opening sentence cover the same ground before the table adds new value. This creates mild redundancy."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Replace 'bore-compatible TCP relay' with 'network tunnels' (or 'a TCP tunnel') in both occurrences. The implementation technology isn't needed for users to act, and 'bore-compatible' is jargon that contradicts the page's goal of not requiring deep AWS/networking knowledge. If the mechanism is worth naming, it must be grounded in a source file first.",
            "Trim or remove the opening sentence of the 'How dev mode works' section ('Dev mode splits your stack between your local machine and AWS...') since it repeats the intro. Let the table do the work immediately after a one-sentence orientation that adds something new (e.g., what the first run does vs subsequent runs).",
            "The Warning callout about auto-recovery is accurate but quite long and contains conditional logic that will confuse a first-time reader. Consider moving the nuanced `stackLacksStackInfoMapOutput` condition to a collapsed 'Details' or a troubleshooting note, and keeping only the primary rule ('do not run normal mode against a stage that already has a non-dev stack') in the Warning.",
            "The 'Lifecycle hooks' section is just a one-liner pointing elsewhere. For a first-time reader this is low-signal. Either add one concrete sentence about what `beforeDev` hooks are useful for (e.g., 'run a database migration before dev containers start'), or fold the link into the 'Running dev mode' section rather than giving it a standalone H2.",
            "The 'Command reference' H2 at the bottom duplicates flags already shown inline throughout the page. Consider making it a summary/cheatsheet framed as 'All flags at a glance' and ensure it links to the CLI reference for the full flag descriptions rather than re-describing them, to avoid drift if flags change."
          ]
        },
        {
          "reviewerId": "production-engineer-reviewer",
          "persona": "Engineer already shipping with Stacktape, looking up how to configure something correctly under real conditions. Cares about: accuracy of defaults, limits, and behavior under load; whether edge cases and failure modes are mentioned where relevant (scaling, security defaults, cost ceilings, rollback behavior); whether the page would mislead them when they reach production. Does NOT nitpick prose polish or MDX component choices — only flags things that change what they'd configure.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 9,
            "completeness": 9,
            "practicalUsefulness": 8,
            "audienceFit": 9
          },
          "strengths": [
            "Normal vs legacy mode comparison table is precise and decision-oriented — covers prerequisites, database behavior, tunneling, and Docker requirements in one scannable view.",
            "Terminal rebuild commands (rs, rs <name>, c, clear, q, quit) are documented accurately against the handleCommand() source.",
            "Agent mode port search range (7331–7430) is correctly derived from the source's `defaultAgentPort + 99` logic.",
            "Auto-recovery conditions in the Warning callout are accurately grounded: correctly names the two OR conditions (stage name contains 'dev'/'local' OR outputs lack stack-info map) and the AND condition (failed/in-progress state).",
            "Hosting bucket `dev` config prerequisite is correctly captured — source has `.filter((b) => b.dev)` and the page reflects this.",
            "FAQ section covers all three required categories: Stacktape-specific, AWS service use-case, and comparison/choosing questions.",
            "Section-level instruction to separate dev mode from deployed-stack behavior is followed throughout — features that differ between modes are consistently flagged.",
            "Fuzzy matching behavior for misspelled resource names is accurately documented (Levenshtein-based, closest matches surfaced in error)."
          ],
          "problems": [
            "'bore-compatible TCP relay' in the tunnel description is a specific implementation detail not present in any provided source file. The index.ts source confirms tunneling exists but does not name the underlying technology. This is an unsupported Stacktape-specific claim.",
            "Database persistence path `.stacktape/dev-data/{stage}/` originates from the secondary curated-docs source, not from index.ts. While plausible, it is not grounded in the authoritative source for this page."
          ],
          "mandatoryFixes": [
            "Remove or generalize the 'bore-compatible TCP relay' description in the tunneling paragraph. The source confirms Lambda-to-local tunnels exist but does not name the underlying relay technology. Replace 'using a bore-compatible TCP relay' with source-neutral wording such as 'through a TCP tunnel' or simply 'automatically' — do not name a specific technology that is not grounded in the provided source files."
          ],
          "optionalImprovements": [
            "The Startup flow numbered list could clarify which steps run sequentially vs. in parallel (e.g., step 6 explicitly starts containers and Lambdas in parallel per the source's runParallelWorkloads call).",
            "The `--watch` flag behavior could note that auto-rebuild semantics differ between workload types (container rebuild vs. Lambda redeploy), if that distinction is grounded in source.",
            "The database persistence path (`.stacktape/dev-data/{stage}/`) should be verified against the authoritative source before publication; if unverifiable, soften to 'local data directory' rather than citing a specific path."
          ]
        },
        {
          "reviewerId": "ai-consumer-reviewer",
          "persona": "AI answer engine, search index, or coding assistant reading this page to cite from it or extract a paragraph as a snippet. This reviewer combines what was previously two lenses (LLM consumer + SEO/AEO) and is the only persona judging extractability. Hard criteria — penalize each missing item by at least 1 point in the most relevant category:\n1. **Answer-first paragraph.** The first paragraph (40-75 words) must answer 'what is this and why does it matter' in self-contained prose. No 'this guide will cover...' framing. AI Overviews extract this verbatim.\n2. **Self-contained sections.** Every H2 section, when extracted alone, must make sense — its first paragraph defines the concept being discussed (e.g. 'A Stacktape web service can front traffic through CloudFront to cache...' not 'You can also enable a CDN'). Pronouns like 'this', 'it', 'the resource' that depend on surrounding context score worse.\n3. **Explicit entity naming.** Say 'Stacktape web service' or 'a web service' — never just 'the service' or 'this resource'. Named entities anchor citations.\n4. **Fact-dense, concrete.** Numbers, defaults, limits, supported values written as literals. 'Up to 16 vCPU and 120 GB memory' beats 'large resource limits'. Vague qualifiers ('fast', 'flexible', 'easy') are penalized.\n5. **Tables for comparisons.** When the page compares options (engines, sizes, modes, packaging types), a table is preferred over prose — tables get cited 2-4x more often.\n6. **Complete code examples.** No `<your-here>` / `// fill in` / `...` placeholders inside example bodies. The example must be copy-paste runnable as shown.\n7. **Compact, descriptive headings.** Short noun-form H2s (Scaling, Networking) for navigation. Question-form H3s are allowed and welcome for FAQ-style subsections and common-question anchors. Avoid full-sentence H2s.\n8. **FAQ section, broad scope.** Resource, reference, and concept pages MUST have a 10-question FAQ that mixes (a) Stacktape-specific questions, (b) use-case questions about the underlying AWS service (e.g. 'How much does ECS Fargate cost?', 'How do I run RDS migrations?'), and (c) choosing/comparison questions ('Web service vs Lambda?', 'ALB vs API Gateway?'). Penalize a Stacktape-only FAQ — it should cover what readers actually search for, not just what's in our types. Penalize FAQs with fewer than 10 questions. Do NOT penalize FAQ absence on pure narrative pages (Getting Started steps, recipes).\n9. **Meta description suitability.** A clean 150-character summary should be derivable directly from the opening paragraph and frontmatter.\n10. **No stripped pronouns / antecedent gaps.** A paragraph starting with 'This is configured by...' fails. Extracted paragraphs lose surrounding context — every paragraph should name what it's about up front.\n11. **Union-type coverage discipline.** When a section documents a property that has multiple variants in the source's union type (packaging modes, compute engines, load balancer kinds, database engines, deployment strategies, trigger sources, etc.), all variants must be named somewhere on the page — not just the one being deep-dived. A page that has '### Using a custom Dockerfile' under Packaging without mentioning the four other packaging modes is misleading. Penalize heavily.\n12. **Per-variant equal depth.** When a section covers multiple variants, each must be developed to roughly equivalent depth: code example per variant, tradeoffs per variant, cost/constraint context per variant. Enumeration without follow-through is not documentation. Example: a 'Compute resources' section that gives Fargate an example + cost table while EC2 gets two sentences fails this criterion.\n13. **Decision support for opt-in features.** Sections introducing opt-ins (CDN, custom domains, gradual deployments, side containers, firewall, ARM, EC2 mode, warm pools, etc.) must help the reader DECIDE, not just CONFIGURE. Look for: when-to-enable (concrete scenario), when-default-suffices, cost/tradeoff signal, and an opinionated recommendation. A section that jumps from a 1-line definition to a code example without addressing these is a recipe, not a guide. Penalize as missing completeness AND practicalUsefulness.\n14. **Unexplained tunable values.** Code examples that set specific tunable values (\\`cloudfrontPriceClass: 'PriceClass_100'\\`, \\`instanceTypes: ['t3.medium']\\`, \\`enableWarmPool: true\\`) need 1-2 sentences of prose explaining what the property controls and what the alternatives are. Otherwise the value reads as magic or required when it isn't.\n15. **Section depth — AEO is about the first paragraph only.** The 40-75 word self-contained-snippet rule applies to the FIRST paragraph of each section. The rest of the section must develop the topic — examples, tradeoffs, cost, edge cases. Sections that consist of one short paragraph + one code example with no surrounding context are too brief and fail completeness, even if they look 'scannable'.",
          "modelProvider": "claude",
          "modelName": "claude-sonnet-4-6",
          "scores": {
            "clarity": 8,
            "scannability": 8,
            "completeness": 7,
            "practicalUsefulness": 8,
            "audienceFit": 8
          },
          "strengths": [
            "Excellent resource-type table that precisely maps every workload category to its dev mode mechanism and the correct Docker images — directly grounded in the source code's `getDevCompatibleResources` and `mapResourceType` functions.",
            "Well-structured normal vs legacy mode comparison table with concrete decision guidance (when each mode is appropriate, its prerequisites, and explicit tradeoffs).",
            "Auto-recovery warning is accurate and specific to the source code's conditional logic (`isProbablyDevStage || stackLacksStackInfoMapOutput`), including the non-obvious edge case about stacks missing the stack-info map output.",
            "Concrete environment variable examples with real localhost values give the reader an immediate mental model of how dev mode differs from deployed stacks without requiring them to run anything.",
            "Comprehensive 10-question FAQ mixing Stacktape-specific questions, AWS-general use-case questions, and choosing/comparison questions as required.",
            "Startup flow numbered list accurately represents the phases from the source (`initializeStackServicesForDevPhase1/2`, secrets/SSM preflight, dev stack deploy, parallel workload start).",
            "Agent mode section correctly documents the default port (7331), the search range (7331–7430), and the idempotency behavior (returns existing agent details rather than starting a second one).",
            "Hosting-bucket filtering caveat ('with `dev` config') is correctly sourced from `configManager.hostingBuckets.filter((b) => b.dev)`."
          ],
          "problems": [
            "'bore-compatible TCP relay' in the opening paragraph is unexplained jargon. The tunneling mechanism's internal name ('bore') means nothing to the target audience and is not exposed in the source at the docs layer. The phrase should be simplified to 'a TCP tunnel' or 'an outbound TCP tunnel' without naming the underlying tool.",
            "The 'Lifecycle hooks' section is a two-sentence stub: it states that `beforeDev` hooks are registered and displayed in the TUI, then immediately defers entirely to a link. A reader who doesn't already know what lifecycle hooks are gains nothing actionable. The section is too thin to justify its own H2.",
            "Partial redundancy between inline flag explanations scattered across 'Running dev mode' subsections and the 'Command reference' table at the bottom. Both exist at similar depth. The bottom table adds little if the flags are already prose-explained above — the two should either be consolidated or the bottom table should be clearly positioned as the definitive quick-reference with the inline prose trimmed.",
            "The `<Tip>` callout about fuzzy resource-name matching is placed inside the 'Run specific resources' subsection but the behavior it describes applies equally to `--skipResources`. The placement makes it look like it applies only to `--resources`."
          ],
          "mandatoryFixes": [],
          "optionalImprovements": [
            "Replace 'bore-compatible TCP relay' with 'a TCP tunnel' or 'an outbound TCP tunnel' (or omit the mechanism name entirely). The underlying tool is an implementation detail that adds noise for developers who just want to understand what happens.",
            "Either expand the 'Lifecycle hooks' section with a one-sentence config example showing a `beforeDev` hook in context, or fold the two sentences into the 'Startup flow' numbered list as a note on step 1 or step 6 and remove the standalone H2. A stub H2 hurts scanability and completeness scores.",
            "Consolidate the flag documentation. Keep the 'Command reference' table at the bottom as a definitive reference table and trim the inline prose flags (e.g. the separate 'Watch mode', 'Disabling tunnels' subsections) to one-liners that point to the table. Or do the reverse — keep the subsections and remove the redundant table. Avoid explaining the same flag at the same depth in two places.",
            "Move the fuzzy-match `<Tip>` callout out of the 'Run specific resources' subsection and place it directly after the resource-picker interactive flow description (end of 'Running dev mode' intro), since the behavior applies to both `--resources` and `--skipResources`.",
            "Add a brief note in the 'Agent mode' section naming one concrete example of what the HTTP API exposes (e.g. rebuild endpoint, status endpoint), since the section currently says 'HTTP server for integrations' without any detail on what a developer would actually call.",
            "Consider adding a note that running multiple concurrent dev mode sessions against the same project and stage contends for the same `__dev-tmp` packaging directory, which can cause transient ENOENT errors (grounded in MEMORY.md observations). This is a real gotcha worth a one-liner warning."
          ]
        }
      ],
      "verifierResults": [
        {
          "verifierId": "factual-accuracy-verifier",
          "modelProvider": "claude",
          "modelName": "cli-default",
          "issues": [
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "Stacktape creates network tunnels (using a bore-compatible TCP relay) when local database emulation is active, routing database traffic from Lambda executions back to Docker containers on your machine.",
              "reason": "The provided source files import from './tunnel-manager' but the tunnel-manager implementation is not included in the source. The curated docs say 'automatic tunneling' but never mention 'bore-compatible TCP relay'. The specific tunneling mechanism ('bore-compatible TCP relay') is not grounded in any provided source file. This claim appears twice — once in the main text and once in the FAQ answer for 'How do Lambda functions connect to local databases in dev mode?'.",
              "suggestedFix": "Remove the parenthetical '(using a bore-compatible TCP relay)' from both occurrences. Say 'Stacktape creates network tunnels when local database emulation is active' without specifying the underlying mechanism, since the tunnel-manager source is not available to verify. Alternatively, if the bore mechanism can be confirmed from another source file, add that file to the source list.",
              "evidence": [
                {
                  "file": "src/commands/dev/index.ts",
                  "quote": "import { getActiveTunnels, registerTunnelCleanupHook } from './tunnel-manager';"
                },
                {
                  "file": "docs/_curated-docs/getting-started/dev-mode.mdx",
                  "quote": "Your Lambda functions can still reach your locally-emulated databases through automatic tunneling."
                }
              ]
            }
          ],
          "positiveFindings": [
            "The startup flow (6 steps) exactly matches the execution order in commandDev: Phase 1 init → resource selection → Phase 2 metadata → secrets/SSM → dev stack deploy → parallel workloads.",
            "All terminal commands (rs, rs <name>, c/clear, q/quit) are accurately described and match the handleCommand function in the source.",
            "The interactive picker behavior — all resources pre-selected by default — matches the source: defaultValues: allResources.map((r) => r.name).",
            "The fuzzy matching Tip correctly describes the Levenshtein-based findSimilarNames function, including that the error lists all available resource names.",
            "The auto-recovery Warning accurately captures the complex conditional: stackIsInRecoverableState AND (isProbablyDevStage || stackLacksStackInfoMapOutput), including the nuanced caveat about non-dev stage names.",
            "The agent mode port range (7331–7430) correctly derives from the source's defaultAgentPort + 99 arithmetic and the error message 'tried ${defaultAgentPort}-${defaultAgentPort + 99}'.",
            "The normal vs legacy mode table and descriptions accurately reflect the source: legacy sets emulateableResources to [] and remoteResourceNames to empty Set, while normal mode checks for/deploys a dev stack.",
            "All 30+ cross-links validate against the documentation navigation index — no invented routes.",
            "The dev-compatible resource types table accurately reflects getDevCompatibleResources: container workloads, functions, hostingBuckets (filtered by b.dev), nextjsWebs, and all six SSR web types.",
            "The --remoteResources scope claim ('applies to emulatable database resources') is accurate — the source uses it only to filter emulateableResources from local emulation, not workloads.",
            "The beforeDev hooks section correctly scopes to 'script hooks', matching the source's if ('scriptName' in hook) check.",
            "The FAQ has exactly 10 Q&A pairs covering all three required categories: Stacktape-specific, AWS service, and choosing/comparison questions."
          ],
          "summary": "The draft is well-grounded in the provided source files. The startup flow, terminal commands, interactive picker, auto-recovery logic, agent mode behavior, normal vs legacy mode differences, and resource type coverage all accurately reflect the source code in src/commands/dev/index.ts and the curated docs. Cross-links are all valid. The only factual issue is the 'bore-compatible TCP relay' tunneling mechanism description, which appears twice and is not supported by any provided source file — the tunnel-manager module is imported but its source is not included. Everything else checks out against the source with high fidelity."
        },
        {
          "verifierId": "source-grounding-verifier",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The draft is mostly aligned with the provided dev command source, but it overstates several Stacktape-specific behaviors that are not present in the supplied files. The main risks are unsupported details about tunneling internals, agent behavior scope, frontend execution mechanics, and dev-stack resource contents.",
          "issues": [
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Dev mode creates a minimal dev stack in AWS (IAM roles, secrets, API gateways)” and “deploys only a minimal dev stack to AWS … containers on ECS, databases on RDS, Lambda functions, load balancers” in the dev-mode vs deployed-stage FAQ.",
              "reason": "The source confirms that normal dev mode deploys a dev stack, but the provided files do not enumerate which AWS resources the dev stack contains. Naming IAM roles, secrets, API gateways, or implying that the dev-stack contents are known from this source would change the reader’s mental model of what gets created in AWS.",
              "suggestedFix": "Say only that normal dev mode deploys a dev stack when one does not exist. Avoid listing specific AWS resource types unless those are grounded in additional source files.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "// Deploy dev stack if it doesn't exist"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "tuiManager.info('[~] Deploying dev stack (minimal stack for dev mode)...');"
                }
              ]
            },
            {
              "severity": "high",
              "type": "unsupported-claim",
              "statement": "“Agent mode runs dev mode as a background daemon with an HTTP server, keeping workload and local-resource state available for integrations with AI coding assistants and automated workflows.”",
              "reason": "The source supports a detached daemon and an HTTP API/server, but it does not ground the claim that the state is available for AI coding assistants or automated workflows. The AI-assistant use case may be documented elsewhere, but it is not in the supplied source files.",
              "suggestedFix": "Narrow the section to source-backed behavior: `--agent` starts a detached daemon, starts an agent server on the selected port, writes a lock file, and returns connection details. Link to the AI page without describing integration behavior unless that source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "// Agent mode (--agent):\n * - Spawns as a detached daemon process\n * - Parent waits for AGENT_READY signal, then exits\n * - Daemon keeps running with HTTP API on specified port\n * - Use --stop to stop a running agent"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "await startAgentServer(agentPort, join(globalStateManager.workingDir, '.stacktape', 'dev-agent'));"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Stacktape creates network tunnels (using a bore-compatible TCP relay) when local database emulation is active.”",
              "reason": "The provided source supports automatic Lambda-to-local tunneling, but it does not describe the relay implementation as bore-compatible. That implementation detail is Stacktape-specific and should not be asserted from these files.",
              "suggestedFix": "Remove “using a bore-compatible TCP relay” and describe only automatic tunneling from Lambda functions to locally emulated databases.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "Your Lambda functions can still reach your locally-emulated databases through automatic tunneling."
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "devTuiManager.addSetupStep('tunnels', 'Lambda tunnels');"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Hosting buckets and supported SSR frontends run through local dev servers” and the resource table says SSR frontends use a “Framework dev server.”",
              "reason": "The command source proves that hosting buckets with `dev` config and SSR frontend resources are dev-compatible, but it does not show the run mechanism. The curated doc only says static sites including hosting-bucket and Next.js are served from a local dev server; it does not support all listed SSR frameworks using framework dev servers.",
              "suggestedFix": "Say that hosting buckets with `dev` config and SSR frontend resources are dev-compatible and run locally. Only describe the exact dev-server mechanism where the source supports it, or cite additional runner source files.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const hostingBuckets = configManager.hostingBuckets\n    .filter((b) => b.dev) // Only include hosting buckets with dev config"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const ssrWebs = [\n    ...configManager.astroWebs.map((r) => ({ name: r.name, type: 'astro-web' as const, category: 'ssr-web' as const }))"
                },
                {
                  "file": "C:\\Projects\\stacktape\\docs\\_curated-docs\\getting-started\\dev-mode.mdx",
                  "quote": "**Static sites** (hosting-bucket, Next.js) - Served from a local dev server"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“Stacktape creates any missing secrets and SSM parameters required by your config.”",
              "reason": "The command source calls helpers named `ensureMissingSecretsCreated` and `ensureMissingSsmParamsCreated`, but the supplied files do not show what those helpers create, whether they create “any” missing item, or whether the criterion is “required by your config.” The wording is broader than the available source.",
              "suggestedFix": "Use cautious wording such as “Stacktape runs preflight checks for missing secrets and SSM parameters” unless the helper source is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "await ensureMissingSecretsCreated();\n  await ensureMissingSsmParamsCreated();"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“When `--agentPort` is omitted, Stacktape starts at port 7331 and searches ports 7331–7430 if the default is in use. Use `--agentPort` to request a specific port.”",
              "reason": "The source supports a default of 7331 and an error message saying the search range is 7331–7430. It does not show that a user-specified `--agentPort` is treated as a strict requested port rather than also being checked or searched by `findAvailablePort`, because `port-utils` is not provided.",
              "suggestedFix": "Keep the default/range claim, but phrase the explicit-port behavior narrowly: “`--agentPort` passes a port value to agent startup.” Avoid promising exact fallback behavior for specified ports without `port-utils` source.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const defaultAgentPort = 7331;\n    const agentPort = agentPortArg ?? (await findAvailablePort(defaultAgentPort));"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "message: `Could not find available port for agent server (tried ${defaultAgentPort}-${defaultAgentPort + 99}).`"
                }
              ]
            },
            {
              "severity": "medium",
              "type": "unsupported-claim",
              "statement": "“If an agent is already running for the same project, the command returns the existing agent's connection details instead of starting a second one.”",
              "reason": "The source checks `getRunningAgent()` and returns existing agent details, but the provided code does not show that the lookup is scoped to the same project. That scoping may be true in `agent-daemon`, but it is not visible in the supplied source.",
              "suggestedFix": "Say “If Stacktape detects a running dev agent, the command returns that agent’s connection details” unless the `getRunningAgent` implementation is provided.",
              "evidence": [
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "const runningAgent = await getRunningAgent();\n    if (runningAgent) {"
                },
                {
                  "file": "C:\\Projects\\stacktape\\src\\commands\\dev\\index.ts",
                  "quote": "projectName: runningAgent.projectName,\n          stage: runningAgent.stage,\n          region: runningAgent.region,"
                }
              ]
            },
            {
              "severity": "low",
              "type": "ambiguous-claim",
              "statement": "The command reference table describes `--remoteResources` as “Emulatable databases to connect to AWS instead of local emulation,” but the curated source describes it more broadly as “Resources to connect to AWS instead of local emulation.”",
              "reason": "The command code only loads local emulatable resources from database-like resource types in normal mode, so the draft’s narrower wording is likely correct. However, because `getLocalEmulateableResources` is not included, the exact flag scope is not fully inspectable from the supplied files.",
              "suggestedFix": "Keep the narrower wording if this page intentionally follows the visible command flow, or provide the `local-resources` source if the flag has broader behavior.",
              "evidence": []
            }
          ],
          "positiveFindings": [
            "The draft correctly distinguishes normal dev mode from legacy mode and states that legacy mode requires an existing deployed stack.",
            "The draft correctly documents the interactive resource picker, `--resources all`, named resource selection, and `--skipResources` behavior from `getSelectedResources`.",
            "The draft correctly documents dev TUI commands `rs`, `rs <name>`, `c`/`clear`, and `q`/`quit` from `handleCommand`.",
            "The draft uses valid navigation routes for the cross-links checked against the provided documentation index."
          ]
        },
        {
          "verifierId": "api-completeness-auditor",
          "modelProvider": "codex",
          "modelName": "cli-default",
          "summary": "The page is broadly complete for the documented `stacktape dev` surface in the provided sources. It covers selectable resources, normal versus legacy mode, local emulation, remote resources, rebuild commands, agent mode, and the main CLI flags. I found no API-completeness, union-variant, or code-example demonstrativeness blockers under the audit scope.",
          "issues": [],
          "positiveFindings": [
            "The draft clearly separates normal dev mode from legacy mode and states that legacy mode requires an existing deployed stack, matching the `commandDev` flow.",
            "The supported dev-compatible workload categories are covered: container workloads, Lambda functions, hosting buckets with `dev` config, Next.js, and the listed SSR frontend resources.",
            "The interactive picker, `--resources`, and `--skipResources` behavior is documented, including the `all` selector and fuzzy-match error behavior.",
            "The dev TUI commands documented in the draft match the source: `rs`, `rs <name>`, `c`/`clear`, and `q`/`quit`.",
            "Agent mode coverage includes the default port `7331`, scanning through the next 99 ports when needed, reuse of an existing running agent, and stopping with `stacktape dev:stop`."
          ]
        }
      ],
      "status": "needs-human-review",
      "passed": false
    }
  ],
  "agentModelAssignments": {
    "page-writer": "claude:cli-default",
    "first-time-user-reviewer": "claude:claude-sonnet-4-6",
    "production-engineer-reviewer": "claude:claude-sonnet-4-6",
    "ai-consumer-reviewer": "claude:claude-sonnet-4-6",
    "factual-accuracy-verifier": "claude:cli-default",
    "source-grounding-verifier": "codex:cli-default",
    "api-completeness-auditor": "codex:cli-default",
    "code-block-validator": "local"
  },
  "finalOutputPath": "C:\\Projects\\stacktape\\docs\\docs\\local-development\\dev-mode-overview.mdx",
  "outcome": "needs-human-review",
  "pipelineStatus": "needs-human-review",
  "pipelineReviewSummary": "reviewer-avg 8.3, hard blockers 2, advisory high 0, verifier medium 6"
}
