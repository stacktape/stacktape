---
title: 'S3 Events'
order: 21
seoTitle: 'S3 Event Triggers for Lambda Functions | Stacktape Docs'
seoDescription: 'Trigger Lambda functions and batch jobs when objects are uploaded, deleted, or restored in S3 buckets. Configure event types, key prefix/suffix filters, and handle payloads with Stacktape.'
---

# S3 Events

An S3 event trigger invokes a [Lambda function](/resources/compute/lambda-function) or [batch job](/resources/compute/batch-job) when objects are created, deleted, or restored in an [S3 bucket](/resources/storage/s3-bucket). This is the foundation for event-driven file processing — image resizing, data imports, log parsing, virus scanning, and any workflow that reacts to file changes.

## Why use S3 triggers

S3 triggers eliminate polling. Instead of periodically checking a bucket for new files, the function runs automatically when an event occurs. There is no idle cost — you pay only for the Lambda invocations or batch job runs that the events produce. Use S3 triggers whenever you need to process files as they arrive.

## When NOT to use S3 triggers

If you need to aggregate multiple uploads before processing, an [SQS queue](/resources/messaging/sqs-queue) as an intermediary gives you batching control. If your processing takes longer than the Lambda timeout allows, trigger a [batch job](/resources/compute/batch-job) instead. If the same bucket needs to fan out events to multiple independent consumers, use [SNS](/resources/messaging/sns-topic) or [EventBridge](/resources/messaging/event-bus) as an intermediary — a single S3 event notification configuration supports at most one Lambda destination per event type and filter combination.

## Basic example

This example creates a bucket and a Lambda function that triggers whenever a new object is uploaded under the `uploads/` prefix.

<CodeBlock
  intellisense
  tabs={[
    {
      label: 'TypeScript',
      lang: 'typescript',
      code: `import {
  defineConfig,
  Bucket,
  LambdaFunction,
  StacktapeLambdaBuildpackPackaging,
  S3Integration
} from 'stacktape';

// [!code focus-start]
export default defineConfig(() => {
  const imagesBucket = new Bucket({
    cors: { enabled: true }
  });

  const processImage = new LambdaFunction({
    packaging: new StacktapeLambdaBuildpackPackaging({
      entryfilePath: './src/process-image.ts'
    }),
    memory: 1024,
    timeout: 60,
    connectTo: [imagesBucket],
    events: [
      new S3Integration({
        bucketArn: imagesBucket.arn,
        s3EventType: 's3:ObjectCreated:*',
        filterRule: {
          prefix: 'uploads/'
        }
      })
    ]
  });

  return {
    resources: { imagesBucket, processImage }
  };
});
// [!code focus-end]
`
    }
  ]}
/>

The `bucketArn` property accepts a reference to the bucket's ARN. When the bucket is defined in the same stack, use the resource's `.arn` property — it resolves to the [`$ResourceParam` directive](/configuration/directives) at deploy time. For external buckets, pass the ARN string directly.

The `connectTo` on the Lambda function grants it read/write access to the bucket. Without it, the function can receive the event but cannot read the uploaded object. See [Connecting resources](/configuration/connecting-resources) for details.

## Event types

The `s3EventType` property determines which bucket operations trigger the function. Stacktape supports the full set of S3 event notification types.

| Event type | Fires when |
|---|---|
| `s3:ObjectCreated:*` | Any object creation (Put, Post, Copy, multipart upload) |
| `s3:ObjectCreated:Put` | Object created via PUT |
| `s3:ObjectCreated:Post` | Object created via POST |
| `s3:ObjectCreated:Copy` | Object created via COPY |
| `s3:ObjectCreated:CompleteMultipartUpload` | Multipart upload completed |
| `s3:ObjectRemoved:*` | Any object deletion |
| `s3:ObjectRemoved:Delete` | Object permanently deleted |
| `s3:ObjectRemoved:DeleteMarkerCreated` | Delete marker created (versioned bucket) |
| `s3:ObjectRestore:*` | Any Glacier restore operation |
| `s3:ObjectRestore:Post` | Glacier restore initiated |
| `s3:ObjectRestore:Completed` | Glacier restore completed |
| `s3:Replication:*` | Any replication event |
| `s3:Replication:OperationFailedReplication` | Replication failed |
| `s3:Replication:OperationNotTracked` | Object not tracked by replication |
| `s3:Replication:OperationMissedThreshold` | Replication exceeded the time threshold |
| `s3:Replication:OperationReplicatedAfterThreshold` | Object replicated after time threshold |
| `s3:ReducedRedundancyLostObject` | Object in Reduced Redundancy storage lost |

For most use cases, `s3:ObjectCreated:*` is the right default — it covers all creation paths regardless of how the object was uploaded.

## Filtering events

The optional `filterRule` property narrows which objects trigger the function based on the object key. You can specify a `prefix`, a `suffix`, or both.

<CodeBlock
  intellisense
  tabs={[
    {
      label: 'TypeScript',
      lang: 'typescript',
      code: `import {
  defineConfig,
  Bucket,
  LambdaFunction,
  StacktapeLambdaBuildpackPackaging,
  S3Integration
} from 'stacktape';

export default defineConfig(() => {
  const dataBucket = new Bucket({});

  const csvProcessor = new LambdaFunction({
    packaging: new StacktapeLambdaBuildpackPackaging({
      entryfilePath: './src/process-csv.ts'
    }),
    memory: 512,
    connectTo: [dataBucket],
    // [!code focus-start]
    events: [
      new S3Integration({
        bucketArn: dataBucket.arn,
        s3EventType: 's3:ObjectCreated:*',
        filterRule: {
          prefix: 'imports/',
          suffix: '.csv'
        }
      })
    ]
    // [!code focus-end]
  });

  return {
    resources: { dataBucket, csvProcessor }
  };
});
`
    }
  ]}
/>

The `prefix` filter matches the beginning of the object key. `prefix: 'imports/'` triggers only for objects whose key starts with `imports/`. The `suffix` filter matches the end of the key — `suffix: '.csv'` triggers only for CSV files. When both are set, an object must match both conditions.

<Tip>Use prefix filtering to keep your function focused. Place uploads into a dedicated key prefix (e.g. `uploads/`, `incoming/`) and only trigger on that prefix. This prevents infinite loops where the function writes output back to the same bucket and re-triggers itself.</Tip>

## Event payload

The Lambda function receives the standard AWS S3 event notification payload. Each invocation carries a `Records` array where each record describes one S3 event.

```typescript
const handler = async (event: {
  Records: Array<{
    eventName: string;
    s3: {
      bucket: { name: string };
      object: { key: string; size: number };
    };
  }>;
}) => {
  for (const record of event.Records) {
    const bucketName = record.s3.bucket.name;
    const objectKey = decodeURIComponent(record.s3.object.key.replace(/\+/g, ' '));
    // Process the object...
  }
};

export default handler;
```

<Warning>Object keys in the event payload are URL-encoded. Spaces appear as `+`, and special characters are percent-encoded. Always decode the key before using it to fetch the object from S3.</Warning>

## Throughput and retry behavior

S3 event notifications are asynchronous. AWS S3 delivers each notification independently — there is no batching window or batch size. Each qualifying S3 event produces one Lambda invocation.

If the Lambda function returns an error, AWS retries the invocation twice (for a total of three attempts). If all attempts fail, the event is discarded unless you configure a [dead-letter queue](https://docs.aws.amazon.com/lambda/latest/dg/invocation-async.html) on the Lambda function.

AWS S3 delivers event notifications at least once. In rare cases, a single event may trigger the function more than once. Design your handler to be idempotent — processing the same file twice should produce the same result.

## Avoiding infinite loops

A common mistake: the Lambda function reads an object from the bucket, processes it, and writes the result back to the **same bucket** without a filter rule to exclude the output. The write triggers the function again, creating an infinite loop that runs up your AWS bill.

Prevent this with one of these strategies:

- **Use a separate output bucket.** Write processed files to a different bucket entirely.
- **Use prefix separation.** Read from `uploads/` and write to `processed/`. Set the filter rule's `prefix` to `uploads/` so writes to `processed/` do not trigger the function.
- **Use suffix filtering.** If uploads are `.jpg` and output is `.webp`, filter on `suffix: '.jpg'`.

## Using with batch jobs

S3 events can also trigger [batch jobs](/resources/compute/batch-job). This is useful for heavy processing that exceeds Lambda's 15-minute timeout — video transcoding, ML inference, or large data transformations. The configuration is the same as for Lambda functions.

<CodeBlock
  intellisense
  tabs={[
    {
      label: 'TypeScript',
      lang: 'typescript',
      code: `import {
  defineConfig,
  Bucket,
  BatchJob,
  CustomDockerfilePackaging,
  S3Integration
} from 'stacktape';

export default defineConfig(() => {
  const videoBucket = new Bucket({});

  const transcodeJob = new BatchJob({
    container: {
      packaging: new CustomDockerfilePackaging({
        buildContextPath: './'
      })
    },
    resources: {
      cpu: 4,
      memory: 16000
    },
    connectTo: [videoBucket],
    // [!code focus-start]
    events: [
      new S3Integration({
        bucketArn: videoBucket.arn,
        s3EventType: 's3:ObjectCreated:*',
        filterRule: {
          prefix: 'raw/',
          suffix: '.mp4'
        }
      })
    ]
    // [!code focus-end]
  });

  return {
    resources: { videoBucket, transcodeJob }
  };
});
`
    }
  ]}
/>

## FAQ

### Can I trigger multiple Lambda functions from the same bucket?

AWS S3 supports only one notification configuration per event type and prefix/suffix combination per bucket. If you need multiple consumers for the same event, route S3 notifications through [SNS](/resources/messaging/sns-topic) or [EventBridge](/resources/messaging/event-bus) and subscribe multiple Lambda functions there.

### How do I avoid infinite loops when writing back to the same bucket?

Use prefix or suffix filtering to separate input from output. For example, trigger only on `prefix: 'uploads/'` and write results to `processed/`. Alternatively, use a separate output bucket. See the [Avoiding infinite loops](#avoiding-infinite-loops) section above.

### Is S3 event delivery guaranteed exactly once?

No. AWS S3 event notifications provide at-least-once delivery. In rare cases a single event can be delivered more than once. Make your handler idempotent — for example, check whether the output file already exists before processing.

### What happens if my Lambda function fails?

AWS retries the invocation twice for asynchronous invocations (three total attempts). If all attempts fail, the event is discarded. You can configure a dead-letter queue on the Lambda function to capture failed events for later investigation.

### Can I use S3 events with an external bucket not managed by Stacktape?

Yes. Pass the bucket's ARN directly as a string to the `bucketArn` property instead of using a resource reference. You also need to ensure the external bucket's notification configuration allows your Lambda function to be invoked — this may require manual bucket policy adjustments on the external bucket.

### How fast are S3 event notifications?

AWS S3 typically delivers event notifications within seconds of the object operation. However, delivery time is not guaranteed and can vary under high load. For latency-sensitive pipelines, test with representative traffic patterns.

### What is the maximum payload size for S3 events?

The S3 event notification payload itself is small — it contains metadata about the event (bucket name, object key, size, event type) rather than the object contents. Your Lambda function must fetch the actual object from S3 using the AWS SDK. The Lambda invocation payload limit of 6 MB applies, but S3 notifications are well within that.

### When should I use a batch job instead of a Lambda function for S3 events?

Use a [batch job](/resources/compute/batch-job) when processing takes longer than Lambda's 15-minute timeout, when you need GPU access, or when you need more than 10 GB of memory. Batch jobs are suited for video transcoding, ML inference, and large dataset processing. For lightweight operations like image resizing, thumbnailing, or metadata extraction, Lambda is faster to start and cheaper at low volumes.

### Can I filter S3 events by object size or content type?

No. S3 event notification filtering supports only key prefix and suffix matching. If you need to filter by object metadata, size, or content type, accept all events in the Lambda function and add conditional logic at the start of your handler to skip objects that do not match your criteria.