resources:
  myLoadBalancer:
    type: 'application-load-balancer'
    properties:
      # {start-highlight}
      customDomains:
        - my-app.mydomain.com
      # {stop-highlight}
      listeners:
        # {start-highlight}
        - protocol: HTTPS
          # {stop-highlight}
          port: 443
