resources:
  myContainerWorkload:
    type: multi-container-workload
    properties:
      containers:
        - name: api-container
          # {start-highlight}
          packaging:
            type: prebuilt-image
            properties:
              image: my-repo/my-container
          # {stop-highlight}
      resources:
        cpu: 2
        memory: 2048
