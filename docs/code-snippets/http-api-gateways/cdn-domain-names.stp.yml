resources:
  myHttpApi:
    type: 'http-api-gateway'
    properties:
      cdn:
        enabled: true
        # {start-highlight}
        customDomains:
          - domainName: mydomain.com
        # {stop-highlight}
