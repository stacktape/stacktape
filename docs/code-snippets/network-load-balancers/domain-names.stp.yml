resources:
  myLoadBalancer:
    type: 'network-load-balancer'
    properties:
      # {start-highlight}
      customDomains:
        - my-app.mydomain.com
      # {stop-highlight}
      listeners:
        - port: 8080
          protocol: TLS
