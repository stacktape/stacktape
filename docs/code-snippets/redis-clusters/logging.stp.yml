resources:
  myRedisCluster:
    type: redis-cluster
    properties:
      instanceSize: cache.t3.micro
      defaultUserPassword: $Secret('redis.password')
      # {start-highlight}
      logging:
        format: json
      # {stop-highlight}
