resources:
  webService:
    type: web-service
    properties:
      # {start-highlight}
      packaging:
        type: external-buildpack
        properties:
          sourceDirectoryPath: ./src
          builder: heroku/buildpacks:20
      # {stop-highlight}
      resources:
        cpu: 2
        memory: 2048
