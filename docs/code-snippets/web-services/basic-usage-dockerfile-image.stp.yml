resources:
  webService:
    type: web-service
    properties:
      # {start-highlight}
      packaging:
        type: custom-dockerfile
        properties:
          buildContextPath: ./server
          dockerfilePath: Dockerfile
      # {stop-highlight}
      resources:
        cpu: 2
        memory: 2048
