resources:
  myFunction:
    type: function
    properties:
      packaging:
        type: stacktape-lambda-buildpack
        properties:
          entryfilePath: path/to/my/lambda.ts
    # {start-highlight}
    overrides:
      MyFunctionFunction:
        Timeout: 15
    # {stop-highlight}
