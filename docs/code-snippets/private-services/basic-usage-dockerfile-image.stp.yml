resources:
  myPrivateService:
    type: private-service
    properties:
      # {start-highlight}
      packaging:
        type: custom-dockerfile
        properties:
          buildContextPath: ./server
          dockerfilePath: Dockerfile
      # {stop-highlight}
      resources:
        cpu: 2
        memory: 2048
