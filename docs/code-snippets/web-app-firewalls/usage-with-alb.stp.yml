resources:
  # {start-highlight}
  myFirewall:
    type: web-app-firewall
    properties:
      scope: regional
  # {stop-highlight}
  applicationLoadBalancer:
    type: application-load-balancer
    properties:
      # {start-highlight}
      useFirewall: myFirewall
      # {stop-highlight}
