resources:
  myBucket:
    type: bucket
    properties:
      # {start-highlight}
      lifecycleRules:
        - type: class-transition
          properties:
            daysAfterUpload: 90
            storageClass: 'GLACIER'
      # {stop-highlight}
