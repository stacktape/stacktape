resources:
  myBucket:
    type: bucket
    properties:
      cdn:
        enabled: true
        # {start-highlight}
        forwardingOptions:
          allowedMethods:
            - 'GET'
        # {stop-highlight}
