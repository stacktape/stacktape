resources:
  myBucket:
    type: bucket
    properties:
      cdn:
        enabled: true
        # {start-highlight}
        routeRewrites:
          - path: /api/*
            routeTo:
              type: custom-origin
              properties:
                domainName: my-custom-origin.example.com
        # {stop-highlight}
