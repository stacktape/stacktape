resources:
  myHttpApi:
    type: 'http-api-gateway'
    properties:
      customDomains:
        # {start-highlight}
        - domainName: $Format('{}-{}', $Stage(), 'mydomain.com')
        # {stop-highlight}
