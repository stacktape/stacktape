resources:
  myBatchJob:
    type: batch-job
    properties:
      container:
        # {start-highlight}
        packaging:
          type: custom-dockerfile
          properties:
            dockerfilePath: src/Dockerfile
            buildContextPath: src
        # {stop-highlight}
      resources:
        cpu: 2
        memory: 1800
