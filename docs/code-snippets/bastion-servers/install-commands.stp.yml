resources:
  myBastion:
    type: bastion
    properties:
      # {start-highlight}
      runCommandsAtLaunch:
        - yum update
        - yum install postgresql.x86_64 -y
      # {stop-highlight}
