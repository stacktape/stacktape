resources:
  webService:
    type: web-service
    properties:
      packaging:
        type: nixpacks
        properties:
          sourceDirectoryPath: ./
          startCmd: node ./dist/server/entry.mjs
      resources:
        cpu: 0.25
        memory: 512
