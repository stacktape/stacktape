serviceName: web-html

resources:
  webHostingBucket:
    type: bucket
    properties:
      directoryUpload:
        directoryPath: web
        headersPreset: static-website
      cdn:
        enabled: true
