---
import Logo from '../../components/Logo.astro';
import { FOOTER_COLUMNS, FOOTER_NOTES, LINKS } from './data';
---

<footer class="su-footer">
  <div class="su-container">
    <div class="su-footer__columns">
      {
        FOOTER_COLUMNS.map((column) => (
          <div class="su-footer__col">
            <p class="su-footer__heading">{column.title}</p>
            <ul class="su-footer__links">
              {column.links.map((link) => (
                <li>
                  <a href={link.href}>{link.label}</a>
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>

    <div class="su-footer__bottom">
      <a class="su-footer__logo" href="/" aria-label="Stacktape home"><Logo height={18} /></a>
      {FOOTER_NOTES.map((note) => <span class="su-footer__note">{note}</span>)}
      <a class="su-footer__status" href={LINKS.status}>
        <i class="su-dot su-dot--small" aria-hidden="true"></i>
        System status
      </a>
    </div>
  </div>
</footer>
