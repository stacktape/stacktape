---
/** 03 — deployments: a canary rolling out, the release before it live, a pull request's preview. */
import { DEPLOYMENTS, URLS } from '../data';
import { PreviewButton, StatusBadge } from '../ui';
import Window from '../Window.astro';

const { rolling, live, preview } = DEPLOYMENTS;
---

<Window kind="browser" host={URLS.deployments.host} path={URLS.deployments.path}>
  <div class="rm-scr__head">
    <h3 class="rm-scr__h">{DEPLOYMENTS.heading}</h3>
    <p class="rm-scr__line">{DEPLOYMENTS.line}</p>
  </div>

  <ul class="rm-dep" aria-label="Deployments">
    <li class="rm-dep__row rm-dep__row--rolling">
      <div class="rm-dep__main">
        <p class="rm-dep__title">
          <b>{rolling.version}</b>
          <span class="rm-dep__ref">{rolling.ref}</span>
          <span class="rm-dep__msg">“{rolling.message}”</span>
        </p>
        <div class="rm-dep__traffic">
          <span class="rm-dep__lane" aria-hidden="true">
            <span class="rm-dep__fill" style={`width: ${rolling.traffic.percent}%`}></span>
          </span>
          <span class="rm-dep__shift">
            {rolling.traffic.from} <span aria-hidden="true">→</span> {rolling.traffic.to}
          </span>
          <span class="rm-dep__note">{rolling.traffic.label}</span>
        </div>
      </div>
      <div class="rm-dep__side">
        <StatusBadge tone="brand" dot>{rolling.badge}</StatusBadge>
        <PreviewButton variant="secondary" small>{rolling.rollback}</PreviewButton>
      </div>
    </li>

    <li class="rm-dep__row">
      <div class="rm-dep__main">
        <p class="rm-dep__title">
          <b>{live.version}</b>
          <span class="rm-dep__ref">{live.ref}</span>
        </p>
      </div>
      <div class="rm-dep__side">
        <StatusBadge tone="success" dot>{live.badge}</StatusBadge>
      </div>
    </li>

    <li class="rm-dep__row">
      <div class="rm-dep__main">
        <p class="rm-dep__title">
          <b>{preview.version}</b>
          <span class="rm-dep__msg">“{preview.message}”</span>
        </p>
        <p class="rm-dep__url">{preview.url}</p>
      </div>
      <div class="rm-dep__side">
        <StatusBadge tone="info">{preview.badge}</StatusBadge>
      </div>
    </li>
  </ul>
</Window>
