---
/** 04 — monitoring: three metric tiles, one request followed through the services, one uptime line. */
import { METRICS, sparkline, TRACE, UPTIME, URLS } from '../data';
import Window from '../Window.astro';

const pct = (ms: number) => `${((ms / TRACE.total) * 100).toFixed(2)}%`;
---

<Window kind="browser" host={URLS.monitoring.host} path={URLS.monitoring.path}>
  <ul class="hr-tiles" aria-label="Metrics">
    {
      METRICS.map((metric) => (
        <li class="hr-tile">
          <p class="hr-tile__label">{metric.label}</p>
          <p class="hr-tile__value">
            {metric.value}<span class="hr-tile__unit">{metric.unit}</span>
          </p>
          <svg class="hr-spark" viewBox="0 0 100 32" preserveAspectRatio="none" aria-hidden="true">
            <polyline points={sparkline(metric.points)} fill="none" stroke="currentColor" stroke-width="1.6" vector-effect="non-scaling-stroke" />
          </svg>
        </li>
      ))
    }
  </ul>

  <div class="hr-trace">
    <p class="hr-trace__head">
      <span class="hr-mono">{TRACE.name}</span>
      <span class="hr-trace__total">{TRACE.total} ms</span>
    </p>
    <ol class="hr-trace__spans" aria-label="Spans">
      {
        TRACE.spans.map((span) => (
          <li class={`hr-span hr-span--${span.kind}`}>
            <span class="hr-span__name">{span.name}</span>
            <span class="hr-span__lane">
              <span class="hr-span__bar" style={`left: ${pct(span.start)}; width: max(${pct(span.end - span.start)}, 3px)`} />
            </span>
            <span class="hr-span__ms">{span.end - span.start} ms</span>
          </li>
        ))
      }
    </ol>
  </div>

  <p class="hr-uptime">
    <span class="hr-uptime__ok" aria-hidden="true"></span>
    <span class="hr-uptime__url">{UPTIME.url}</span>
    <span class="hr-uptime__sep" aria-hidden="true">·</span>
    <b>{UPTIME.availability}</b>
    {
      UPTIME.regions.map((check) => (
        <>
          <span class="hr-uptime__sep" aria-hidden="true">·</span>
          <span class="hr-uptime__region">
            {check.region} <span class="hr-mono">{check.latency}</span>
          </span>
        </>
      ))
    }
  </p>
</Window>
