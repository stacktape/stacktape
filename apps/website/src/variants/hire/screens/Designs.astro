---
/** 01 — the wizard's review: the six resources it found and why, the picture, the estimate, Deploy. */
import { ACME_CONFIG, money, RESOURCES, URLS, WIZARD } from '../data';
import DiagramIsland from '../DiagramIsland';
import { PreviewButton } from '../ui';
import Window from '../Window.astro';

const DIAGRAM_LABEL =
  'Architecture diagram of acme-project: web and apiService are public behind the firewall; worker, cache and mainDatabase sit inside the private network';
---

<Window kind="browser" host={URLS.wizard.host} path={URLS.wizard.path} lock={false} class="hr-win--wizard">
  <div class="hr-scr__head">
    <p class="hr-scr__h">{WIZARD.heading}</p>
    <p class="hr-scr__line">{WIZARD.line}</p>
  </div>

  <div class="hr-wz">
    <ul class="hr-wz__list" aria-label="Resources">
      {
        RESOURCES.map((resource) => (
          <li class={`hr-wz__row hr-wz__row--${resource.category}`}>
            <i class="hr-wz__cat" aria-hidden="true" />
            <span class="hr-wz__text">
              <span class="hr-wz__name">
                <b>{resource.name}</b>
                <span class="hr-wz__type">{resource.type}</span>
              </span>
              <span class="hr-wz__because">{resource.because}</span>
            </span>
            <span class="hr-wz__price">{money(resource.price)}</span>
          </li>
        ))
      }
    </ul>
    <div class="hr-wz__picture">
      <DiagramIsland client:only="react" config={ACME_CONFIG} ariaLabel={DIAGRAM_LABEL} />
    </div>
  </div>

  <div class="hr-scr__foot">
    <p class="hr-wz__estimate">{WIZARD.footer}</p>
    <PreviewButton variant="primary" icon="arrow" iconPosition="end">{WIZARD.button}</PreviewButton>
  </div>
</Window>
