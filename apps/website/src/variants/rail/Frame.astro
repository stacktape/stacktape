---
/** Console / wizard window chrome. `url` may contain <b> for the highlighted segment. */
type Props = { url: string; tag?: string; class?: string };

const { url, tag = 'example project', class: className } = Astro.props;
---

<div class={['rl-frame', className].filter(Boolean).join(' ')}>
  <div class="rl-frame__bar">
    <span class="rl-frame__dots" aria-hidden="true"><i></i><i></i><i></i></span>
    <span class="rl-frame__url" set:html={url} />
    <span class="rl-tag">{tag}</span>
  </div>
  <slot />
</div>
