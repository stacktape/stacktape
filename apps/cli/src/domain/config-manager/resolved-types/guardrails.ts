// Guardrails cross the public Console API boundary before the CLI enforces them. Keep one canonical wire contract.
export type { GuardrailDefinition, GuardrailType } from '@stacktape/console-api/guardrails';
