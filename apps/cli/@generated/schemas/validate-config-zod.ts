import { z } from "zod"

export const stacktapeConfigSchema = z.object({
  "projectName": z.string().optional().describe("#### Project name\n\n---\n\nIdentifies the Stacktape project. Together with the selected stage, this determines the\nCloudFormation stack name: `{projectName}-{stage}`.\n\nMust be alphanumeric and can contain dashes. Must match the regex `[a-zA-Z][-a-zA-Z0-9]*`.\nThe CLI's `--projectName` option overrides this value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nprojectName: my-web-app\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/server.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    projectName: 'my-web-app',\n    resources: { api }\n  };\n});\n```").optional(),
  "providerConfig": z.object({
    "mongoDbAtlas": z.object({
      "publicKey": z.string().optional().describe("#### Your MongoDB Atlas public API key.\n\n---\n\nCreate API keys in the MongoDB Atlas console under Organization Settings > API Keys.\n\n**Example (YAML):**\n\n```yaml\nproviderConfig:\n  mongoDbAtlas:\n    publicKey: abcdef12\n    privateKey: $Secret('mongoDbAtlasPrivateKey')\n    organizationId: 5f1a2b3c4d5e6f7a8b9c0d1e\n\nresources:\n  appDatabase:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDatabase = new MongoDbAtlasCluster({ clusterTier: 'M10', version: '7.0' });\n  return {\n    providerConfig: {\n      mongoDbAtlas: {\n        publicKey: 'abcdef12',\n        privateKey: $Secret('mongoDbAtlasPrivateKey'),\n        organizationId: '5f1a2b3c4d5e6f7a8b9c0d1e'\n      }\n    },\n    resources: { appDatabase }\n  };\n});\n```").optional(),
      "privateKey": z.string().optional().describe("#### Your MongoDB Atlas private API key. Store as `$Secret()` for security.\n\n---\n\nCreate API keys in the MongoDB Atlas console under Organization Settings > API Keys.\n\n**Example (YAML):**\n\n```yaml\nproviderConfig:\n  mongoDbAtlas:\n    publicKey: abcdef12\n    privateKey: $Secret('mongoDbAtlasPrivateKey')\n    organizationId: 5f1a2b3c4d5e6f7a8b9c0d1e\n\nresources:\n  appDatabase:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDatabase = new MongoDbAtlasCluster({ clusterTier: 'M10', version: '7.0' });\n  return {\n    providerConfig: {\n      mongoDbAtlas: {\n        publicKey: 'abcdef12',\n        privateKey: $Secret('mongoDbAtlasPrivateKey'),\n        organizationId: '5f1a2b3c4d5e6f7a8b9c0d1e'\n      }\n    },\n    resources: { appDatabase }\n  };\n});\n```").optional(),
      "organizationId": z.string().optional().describe("#### Your MongoDB Atlas Organization ID.\n\n---\n\nFound in the MongoDB Atlas console under Organization Settings.\n\n**Example (YAML):**\n\n```yaml\nproviderConfig:\n  mongoDbAtlas:\n    publicKey: abcdef12\n    privateKey: $Secret('mongoDbAtlasPrivateKey')\n    organizationId: 5f1a2b3c4d5e6f7a8b9c0d1e\n\nresources:\n  appDatabase:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDatabase = new MongoDbAtlasCluster({ clusterTier: 'M10', version: '7.0' });\n  return {\n    providerConfig: {\n      mongoDbAtlas: {\n        publicKey: 'abcdef12',\n        privateKey: $Secret('mongoDbAtlasPrivateKey'),\n        organizationId: '5f1a2b3c4d5e6f7a8b9c0d1e'\n      }\n    },\n    resources: { appDatabase }\n  };\n});\n```").optional(),
      "accessibility": z.object({
        "accessibilityMode": z.enum(["internet","scoping-workloads-in-vpc","vpc","whitelisted-ips-only"]).describe("#### Network access mode.\n\n---\n\n- **`internet`**: Accessible from anywhere (credentials still required).\n- **`vpc`**: Only from resources in your VPC + any `whitelistedIps`.\n- **`scoping-workloads-in-vpc`**: Like `vpc`, but also requires security-group access via `connectTo`.\n- **`whitelisted-ips-only`**: Only from IP addresses listed in `whitelistedIps`.\n\n\n**Example (YAML):**\n\n```yaml\nproviderConfig:\n  mongoDbAtlas:\n    organizationId: 5f1a2b3c4d5e6f7a8b9c0d1e\n    publicKey: abcdefgh\n    privateKey: $Secret('mongo-atlas-private-key')\n    accessibility:\n      accessibilityMode: scoping-workloads-in-vpc\nresources:\n  appDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      joinDefaultVpc: true\n      connectTo:\n        - appDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    joinDefaultVpc: true,\n    connectTo: [appDb]\n  });\n  return {\n    providerConfig: {\n      mongoDbAtlas: {\n        organizationId: '5f1a2b3c4d5e6f7a8b9c0d1e',\n        publicKey: 'abcdefgh',\n        privateKey: $Secret('mongo-atlas-private-key'),\n        accessibility: {\n          accessibilityMode: 'scoping-workloads-in-vpc'\n        }\n      }\n    },\n    resources: { appDb, api }\n  };\n});\n```").default("internet"),
        "whitelistedIps": z.array(z.string()).optional().describe("#### IP addresses or CIDR ranges allowed to access the cluster (e.g., your office IP).\n\n---\n\nNo effect in `internet` mode. In `vpc`/`scoping-workloads-in-vpc`, adds access for IPs outside the VPC.\nIn `whitelisted-ips-only`, these are the only IPs that can connect.\n\n**Example (YAML):**\n\n```yaml\nproviderConfig:\n  mongoDbAtlas:\n    organizationId: 5f1a2b3c4d5e6f7a8b9c0d1e\n    publicKey: abcdefgh\n    privateKey: $Secret('mongo-atlas-private-key')\n    accessibility:\n      accessibilityMode: whitelisted-ips-only\n      whitelistedIps:\n        - 203.0.113.10\n        - 198.51.100.0/24\nresources:\n  appDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true\n  });\n  return {\n    providerConfig: {\n      mongoDbAtlas: {\n        organizationId: '5f1a2b3c4d5e6f7a8b9c0d1e',\n        publicKey: 'abcdefgh',\n        privateKey: $Secret('mongo-atlas-private-key'),\n        accessibility: {\n          accessibilityMode: 'whitelisted-ips-only',\n          whitelistedIps: ['203.0.113.10', '198.51.100.0/24']\n        }\n      }\n    },\n    resources: { appDb }\n  };\n});\n```").optional() }).strict()
      .optional().describe("#### Network connectivity settings for all MongoDB Atlas clusters in this stack.\n\n---\n\nStacktape auto-creates a MongoDB Atlas Project for your clusters. These accessibility settings\napply at the project level — all clusters in the stack share the same network config.\n\n**Example (YAML):**\n\n```yaml\nproviderConfig:\n  mongoDbAtlas:\n    publicKey: abcdef12\n    privateKey: $Secret('mongoDbAtlasPrivateKey')\n    organizationId: 5f1a2b3c4d5e6f7a8b9c0d1e\n    accessibility:\n      accessibilityMode: scoping-workloads-in-vpc\n      whitelistedIps:\n        - 203.0.113.4/32\n\nresources:\n  appDatabase:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDatabase = new MongoDbAtlasCluster({ clusterTier: 'M10', version: '7.0' });\n  return {\n    providerConfig: {\n      mongoDbAtlas: {\n        publicKey: 'abcdef12',\n        privateKey: $Secret('mongoDbAtlasPrivateKey'),\n        organizationId: '5f1a2b3c4d5e6f7a8b9c0d1e',\n        accessibility: {\n          accessibilityMode: 'scoping-workloads-in-vpc',\n          whitelistedIps: ['203.0.113.4/32']\n        }\n      }\n    },\n    resources: { appDatabase }\n  };\n});\n```").optional() }).strict()
    .optional().describe("").optional(),
    "upstash": z.object({
      "accountEmail": z.string().describe("#### Email address of your Upstash account.\n\n---\n\n**Example (YAML):**\n\n```yaml\nproviderConfig:\n  upstash:\n    accountEmail: devops@example.com\n    apiKey: $Secret('upstashApiKey')\n\nresources:\n  cache:\n    type: upstash-redis\n    properties:\n      enableEviction: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UpstashRedis, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const cache = new UpstashRedis({ enableEviction: true });\n  return {\n    providerConfig: {\n      upstash: {\n        accountEmail: 'devops@example.com',\n        apiKey: $Secret('upstashApiKey')\n      }\n    },\n    resources: { cache }\n  };\n});\n```"),
      "apiKey": z.string().describe("#### API key for your Upstash account. Store as `$Secret()` for security.\n\n---\n\nCreate an API key in the Upstash console under Account > API Keys.\n\n**Example (YAML):**\n\n```yaml\nproviderConfig:\n  upstash:\n    accountEmail: devops@example.com\n    apiKey: $Secret('upstashApiKey')\n\nresources:\n  cache:\n    type: upstash-redis\n    properties:\n      enableEviction: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UpstashRedis, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const cache = new UpstashRedis({ enableEviction: true });\n  return {\n    providerConfig: {\n      upstash: {\n        accountEmail: 'devops@example.com',\n        apiKey: $Secret('upstashApiKey')\n      }\n    },\n    resources: { cache }\n  };\n});\n```") }).strict()
    .optional().describe("").optional() }).strict()
  .optional().describe("#### Credentials and settings for 3rd-party services (MongoDB Atlas, Upstash).\n\n---\n\nRequired only if you use `mongo-db-atlas-cluster` or `upstash-redis` resources in your stack.\n\n**Example (YAML):**\n\n```yaml\nproviderConfig:\n  mongoDbAtlas:\n    organizationId: 5f1e9b2c4a7d3e0011223344\n    publicKey: abcdefgh\n    privateKey: $Secret('mongo-atlas.private-key')\n    accessibility:\n      accessibilityMode: scoping-workloads-in-vpc\n      whitelistedIps:\n        - 203.0.113.7/32\nresources:\n  database:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new MongoDbAtlasCluster({ clusterTier: 'M10' });\n\n  return {\n    providerConfig: {\n      mongoDbAtlas: {\n        organizationId: '5f1e9b2c4a7d3e0011223344',\n        publicKey: 'abcdefgh',\n        privateKey: $Secret('mongo-atlas.private-key'),\n        accessibility: {\n          accessibilityMode: 'scoping-workloads-in-vpc',\n          whitelistedIps: ['203.0.113.7/32']\n        }\n      }\n    },\n    resources: { database }\n  };\n});\n```").optional(),
  "variables": z.record(z.string(), z.any()).optional().describe("#### Reusable values you can reference anywhere in the config with `$Var().variableName`.\n\n---\n\nUseful for avoiding repetition. For example, define a shared environment name\nand reference it in multiple resources.\n\n```yaml\nvariables:\n  appPort: 3000\n# Then use: $Var().appPort\nresources: {}\n```\n\n**Example (YAML):**\n\n```yaml\nvariables:\n  appPort: 3000\n  logLevel: info\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: PORT\n          value: $Var('appPort')\n        - name: LOG_LEVEL\n          value: $Var('logLevel')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appPort = 3000;\n  const logLevel = 'info';\n\n  const api = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/server.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      PORT: appPort,\n      LOG_LEVEL: logLevel\n    }\n  });\n\n  return {\n    variables: {\n      appPort,\n      logLevel\n    },\n    resources: { api }\n  };\n});\n```").optional(),
  "hooks": z.object({
    "beforeDeploy": z.array(z.object({
      "scriptName": z.string().describe("#### Script Name\n\n---\n\nThe name of the script to execute. The script must be defined in the `scripts` section of your configuration.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npm run migrate\n      connectTo:\n        - db\nhooks:\n  afterDeploy:\n    - scriptName: migrate\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: mysql\n        properties:\n          version: '8.0.36'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEngineMysql, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEngineMysql({ version: '8.0.36', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({ executeCommand: 'npm run migrate', connectTo: [db] });\n\n  return {\n    scripts: { migrate },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'migrate' }\n      ]\n    },\n    resources: { db }\n  };\n});\n```"),
      "skipOnCI": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook in CI/CD environments (CodeBuild, GitHub Actions, GitLab CI).\n\n---\n\nUseful for hooks that only make sense locally (e.g., opening a browser, interactive prompts).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  openBrowser:\n    type: local-script\n    properties:\n      executeCommand: open http://localhost:3000\nhooks:\n  afterDeploy:\n    - scriptName: openBrowser\n      skipOnCI: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const openBrowser = new LocalScript({ executeCommand: 'open http://localhost:3000' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { openBrowser },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'openBrowser', skipOnCI: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false),
      "skipOnLocal": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook when running locally; only run in CI/CD.\n\n---\n\nUseful for CI-only tasks (e.g., uploading test reports, notifying Slack).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  uploadTestReport:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/upload-report.js\nhooks:\n  afterDeploy:\n    - scriptName: uploadTestReport\n      skipOnLocal: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadTestReport = new LocalScript({ executeCommand: 'node ./scripts/upload-report.js' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { uploadTestReport },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'uploadTestReport', skipOnLocal: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false) }).strict()
    ).optional().describe("#### Scripts to run before deploying. Common use: build frontend, lint code.\n\n---\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  buildFrontend:\n    type: local-script\n    properties:\n      executeCommand: npm run build\nhooks:\n  beforeDeploy:\n    - scriptName: buildFrontend\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const buildFrontend = new LocalScript({ executeCommand: 'npm run build' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { buildFrontend },\n    hooks: {\n      beforeDeploy: [{ scriptName: 'buildFrontend' }]\n    },\n    resources: { web }\n  };\n});\n```").optional(),
    "afterDeploy": z.array(z.object({
      "scriptName": z.string().describe("#### Script Name\n\n---\n\nThe name of the script to execute. The script must be defined in the `scripts` section of your configuration.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npm run migrate\n      connectTo:\n        - db\nhooks:\n  afterDeploy:\n    - scriptName: migrate\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: mysql\n        properties:\n          version: '8.0.36'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEngineMysql, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEngineMysql({ version: '8.0.36', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({ executeCommand: 'npm run migrate', connectTo: [db] });\n\n  return {\n    scripts: { migrate },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'migrate' }\n      ]\n    },\n    resources: { db }\n  };\n});\n```"),
      "skipOnCI": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook in CI/CD environments (CodeBuild, GitHub Actions, GitLab CI).\n\n---\n\nUseful for hooks that only make sense locally (e.g., opening a browser, interactive prompts).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  openBrowser:\n    type: local-script\n    properties:\n      executeCommand: open http://localhost:3000\nhooks:\n  afterDeploy:\n    - scriptName: openBrowser\n      skipOnCI: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const openBrowser = new LocalScript({ executeCommand: 'open http://localhost:3000' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { openBrowser },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'openBrowser', skipOnCI: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false),
      "skipOnLocal": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook when running locally; only run in CI/CD.\n\n---\n\nUseful for CI-only tasks (e.g., uploading test reports, notifying Slack).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  uploadTestReport:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/upload-report.js\nhooks:\n  afterDeploy:\n    - scriptName: uploadTestReport\n      skipOnLocal: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadTestReport = new LocalScript({ executeCommand: 'node ./scripts/upload-report.js' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { uploadTestReport },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'uploadTestReport', skipOnLocal: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false) }).strict()
    ).optional().describe("#### Scripts to run after deploying. Common use: run database migrations, seed data.\n\n---\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  runMigrations:\n    type: local-script\n    properties:\n      executeCommand: npx prisma migrate deploy\n      connectTo:\n        - mainDb\nhooks:\n  afterDeploy:\n    - scriptName: runMigrations\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const runMigrations = new LocalScript({ executeCommand: 'npx prisma migrate deploy', connectTo: [mainDb] });\n\n  return {\n    scripts: { runMigrations },\n    hooks: {\n      afterDeploy: [{ scriptName: 'runMigrations' }]\n    },\n    resources: { mainDb }\n  };\n});\n```").optional(),
    "beforeDelete": z.array(z.object({
      "scriptName": z.string().describe("#### Script Name\n\n---\n\nThe name of the script to execute. The script must be defined in the `scripts` section of your configuration.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npm run migrate\n      connectTo:\n        - db\nhooks:\n  afterDeploy:\n    - scriptName: migrate\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: mysql\n        properties:\n          version: '8.0.36'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEngineMysql, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEngineMysql({ version: '8.0.36', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({ executeCommand: 'npm run migrate', connectTo: [db] });\n\n  return {\n    scripts: { migrate },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'migrate' }\n      ]\n    },\n    resources: { db }\n  };\n});\n```"),
      "skipOnCI": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook in CI/CD environments (CodeBuild, GitHub Actions, GitLab CI).\n\n---\n\nUseful for hooks that only make sense locally (e.g., opening a browser, interactive prompts).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  openBrowser:\n    type: local-script\n    properties:\n      executeCommand: open http://localhost:3000\nhooks:\n  afterDeploy:\n    - scriptName: openBrowser\n      skipOnCI: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const openBrowser = new LocalScript({ executeCommand: 'open http://localhost:3000' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { openBrowser },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'openBrowser', skipOnCI: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false),
      "skipOnLocal": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook when running locally; only run in CI/CD.\n\n---\n\nUseful for CI-only tasks (e.g., uploading test reports, notifying Slack).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  uploadTestReport:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/upload-report.js\nhooks:\n  afterDeploy:\n    - scriptName: uploadTestReport\n      skipOnLocal: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadTestReport = new LocalScript({ executeCommand: 'node ./scripts/upload-report.js' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { uploadTestReport },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'uploadTestReport', skipOnLocal: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false) }).strict()
    ).optional().describe("#### Scripts to run before deleting the stack. Common use: export data, clean up external resources.\n\n---\n\nOnly runs when `--configPath` and `--stage` are provided to the delete command.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  exportData:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/export-data.js\n      connectTo:\n        - mainDb\nhooks:\n  beforeDelete:\n    - scriptName: exportData\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const exportData = new LocalScript({ executeCommand: 'node ./scripts/export-data.js', connectTo: [mainDb] });\n\n  return {\n    scripts: { exportData },\n    hooks: {\n      beforeDelete: [{ scriptName: 'exportData' }]\n    },\n    resources: { mainDb }\n  };\n});\n```").optional(),
    "afterDelete": z.array(z.object({
      "scriptName": z.string().describe("#### Script Name\n\n---\n\nThe name of the script to execute. The script must be defined in the `scripts` section of your configuration.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npm run migrate\n      connectTo:\n        - db\nhooks:\n  afterDeploy:\n    - scriptName: migrate\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: mysql\n        properties:\n          version: '8.0.36'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEngineMysql, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEngineMysql({ version: '8.0.36', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({ executeCommand: 'npm run migrate', connectTo: [db] });\n\n  return {\n    scripts: { migrate },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'migrate' }\n      ]\n    },\n    resources: { db }\n  };\n});\n```"),
      "skipOnCI": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook in CI/CD environments (CodeBuild, GitHub Actions, GitLab CI).\n\n---\n\nUseful for hooks that only make sense locally (e.g., opening a browser, interactive prompts).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  openBrowser:\n    type: local-script\n    properties:\n      executeCommand: open http://localhost:3000\nhooks:\n  afterDeploy:\n    - scriptName: openBrowser\n      skipOnCI: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const openBrowser = new LocalScript({ executeCommand: 'open http://localhost:3000' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { openBrowser },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'openBrowser', skipOnCI: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false),
      "skipOnLocal": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook when running locally; only run in CI/CD.\n\n---\n\nUseful for CI-only tasks (e.g., uploading test reports, notifying Slack).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  uploadTestReport:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/upload-report.js\nhooks:\n  afterDeploy:\n    - scriptName: uploadTestReport\n      skipOnLocal: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadTestReport = new LocalScript({ executeCommand: 'node ./scripts/upload-report.js' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { uploadTestReport },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'uploadTestReport', skipOnLocal: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false) }).strict()
    ).optional().describe("#### Scripts to run after deleting the stack.\n\n---\n\nOnly runs when `--configPath` and `--stage` are provided to the delete command.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  notifyCleanup:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/notify-slack.js\nhooks:\n  afterDelete:\n    - scriptName: notifyCleanup\nresources:\n  worker:\n    type: worker-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, StacktapeImageBuildpackPackaging, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const notifyCleanup = new LocalScript({ executeCommand: 'node ./scripts/notify-slack.js' });\n  const worker = new WorkerService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/worker.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    scripts: { notifyCleanup },\n    hooks: {\n      afterDelete: [{ scriptName: 'notifyCleanup' }]\n    },\n    resources: { worker }\n  };\n});\n```").optional(),
    "beforeBucketSync": z.array(z.object({
      "scriptName": z.string().describe("#### Script Name\n\n---\n\nThe name of the script to execute. The script must be defined in the `scripts` section of your configuration.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npm run migrate\n      connectTo:\n        - db\nhooks:\n  afterDeploy:\n    - scriptName: migrate\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: mysql\n        properties:\n          version: '8.0.36'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEngineMysql, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEngineMysql({ version: '8.0.36', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({ executeCommand: 'npm run migrate', connectTo: [db] });\n\n  return {\n    scripts: { migrate },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'migrate' }\n      ]\n    },\n    resources: { db }\n  };\n});\n```"),
      "skipOnCI": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook in CI/CD environments (CodeBuild, GitHub Actions, GitLab CI).\n\n---\n\nUseful for hooks that only make sense locally (e.g., opening a browser, interactive prompts).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  openBrowser:\n    type: local-script\n    properties:\n      executeCommand: open http://localhost:3000\nhooks:\n  afterDeploy:\n    - scriptName: openBrowser\n      skipOnCI: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const openBrowser = new LocalScript({ executeCommand: 'open http://localhost:3000' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { openBrowser },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'openBrowser', skipOnCI: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false),
      "skipOnLocal": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook when running locally; only run in CI/CD.\n\n---\n\nUseful for CI-only tasks (e.g., uploading test reports, notifying Slack).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  uploadTestReport:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/upload-report.js\nhooks:\n  afterDeploy:\n    - scriptName: uploadTestReport\n      skipOnLocal: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadTestReport = new LocalScript({ executeCommand: 'node ./scripts/upload-report.js' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { uploadTestReport },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'uploadTestReport', skipOnLocal: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false) }).strict()
    ).optional().describe("#### Scripts to run before syncing bucket contents.\n\n---\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  optimizeAssets:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/optimize-images.js\nhooks:\n  beforeBucketSync:\n    - scriptName: optimizeAssets\nresources:\n  assets:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const optimizeAssets = new LocalScript({ executeCommand: 'node ./scripts/optimize-images.js' });\n  const assets = new HostingBucket({ uploadDirectoryPath: './public' });\n\n  return {\n    scripts: { optimizeAssets },\n    hooks: {\n      beforeBucketSync: [{ scriptName: 'optimizeAssets' }]\n    },\n    resources: { assets }\n  };\n});\n```").optional(),
    "afterBucketSync": z.array(z.object({
      "scriptName": z.string().describe("#### Script Name\n\n---\n\nThe name of the script to execute. The script must be defined in the `scripts` section of your configuration.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npm run migrate\n      connectTo:\n        - db\nhooks:\n  afterDeploy:\n    - scriptName: migrate\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: mysql\n        properties:\n          version: '8.0.36'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEngineMysql, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEngineMysql({ version: '8.0.36', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({ executeCommand: 'npm run migrate', connectTo: [db] });\n\n  return {\n    scripts: { migrate },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'migrate' }\n      ]\n    },\n    resources: { db }\n  };\n});\n```"),
      "skipOnCI": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook in CI/CD environments (CodeBuild, GitHub Actions, GitLab CI).\n\n---\n\nUseful for hooks that only make sense locally (e.g., opening a browser, interactive prompts).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  openBrowser:\n    type: local-script\n    properties:\n      executeCommand: open http://localhost:3000\nhooks:\n  afterDeploy:\n    - scriptName: openBrowser\n      skipOnCI: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const openBrowser = new LocalScript({ executeCommand: 'open http://localhost:3000' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { openBrowser },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'openBrowser', skipOnCI: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false),
      "skipOnLocal": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook when running locally; only run in CI/CD.\n\n---\n\nUseful for CI-only tasks (e.g., uploading test reports, notifying Slack).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  uploadTestReport:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/upload-report.js\nhooks:\n  afterDeploy:\n    - scriptName: uploadTestReport\n      skipOnLocal: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadTestReport = new LocalScript({ executeCommand: 'node ./scripts/upload-report.js' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { uploadTestReport },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'uploadTestReport', skipOnLocal: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false) }).strict()
    ).optional().describe("#### Scripts to run after syncing bucket contents.\n\n---\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  invalidateCache:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/purge-cdn.js\nhooks:\n  afterBucketSync:\n    - scriptName: invalidateCache\nresources:\n  assets:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const invalidateCache = new LocalScript({ executeCommand: 'node ./scripts/purge-cdn.js' });\n  const assets = new HostingBucket({ uploadDirectoryPath: './public' });\n\n  return {\n    scripts: { invalidateCache },\n    hooks: {\n      afterBucketSync: [{ scriptName: 'invalidateCache' }]\n    },\n    resources: { assets }\n  };\n});\n```").optional(),
    "beforeDev": z.array(z.object({
      "scriptName": z.string().describe("#### Script Name\n\n---\n\nThe name of the script to execute. The script must be defined in the `scripts` section of your configuration.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npm run migrate\n      connectTo:\n        - db\nhooks:\n  afterDeploy:\n    - scriptName: migrate\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: mysql\n        properties:\n          version: '8.0.36'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEngineMysql, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEngineMysql({ version: '8.0.36', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({ executeCommand: 'npm run migrate', connectTo: [db] });\n\n  return {\n    scripts: { migrate },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'migrate' }\n      ]\n    },\n    resources: { db }\n  };\n});\n```"),
      "skipOnCI": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook in CI/CD environments (CodeBuild, GitHub Actions, GitLab CI).\n\n---\n\nUseful for hooks that only make sense locally (e.g., opening a browser, interactive prompts).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  openBrowser:\n    type: local-script\n    properties:\n      executeCommand: open http://localhost:3000\nhooks:\n  afterDeploy:\n    - scriptName: openBrowser\n      skipOnCI: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const openBrowser = new LocalScript({ executeCommand: 'open http://localhost:3000' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { openBrowser },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'openBrowser', skipOnCI: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false),
      "skipOnLocal": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook when running locally; only run in CI/CD.\n\n---\n\nUseful for CI-only tasks (e.g., uploading test reports, notifying Slack).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  uploadTestReport:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/upload-report.js\nhooks:\n  afterDeploy:\n    - scriptName: uploadTestReport\n      skipOnLocal: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadTestReport = new LocalScript({ executeCommand: 'node ./scripts/upload-report.js' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { uploadTestReport },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'uploadTestReport', skipOnLocal: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false) }).strict()
    ).optional().describe("#### Scripts to run before starting dev mode.\n\n---\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  seedLocalDb:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/seed.js\nhooks:\n  beforeDev:\n    - scriptName: seedLocalDb\nresources:\n  cache:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: key\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const seedLocalDb = new LocalScript({ executeCommand: 'node ./scripts/seed.js' });\n  const cache = new DynamoDbTable({ primaryKey: { partitionKey: { name: 'key', type: 'string' } } });\n\n  return {\n    scripts: { seedLocalDb },\n    hooks: {\n      beforeDev: [{ scriptName: 'seedLocalDb' }]\n    },\n    resources: { cache }\n  };\n});\n```").optional(),
    "afterDev": z.array(z.object({
      "scriptName": z.string().describe("#### Script Name\n\n---\n\nThe name of the script to execute. The script must be defined in the `scripts` section of your configuration.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npm run migrate\n      connectTo:\n        - db\nhooks:\n  afterDeploy:\n    - scriptName: migrate\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: mysql\n        properties:\n          version: '8.0.36'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEngineMysql, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEngineMysql({ version: '8.0.36', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({ executeCommand: 'npm run migrate', connectTo: [db] });\n\n  return {\n    scripts: { migrate },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'migrate' }\n      ]\n    },\n    resources: { db }\n  };\n});\n```"),
      "skipOnCI": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook in CI/CD environments (CodeBuild, GitHub Actions, GitLab CI).\n\n---\n\nUseful for hooks that only make sense locally (e.g., opening a browser, interactive prompts).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  openBrowser:\n    type: local-script\n    properties:\n      executeCommand: open http://localhost:3000\nhooks:\n  afterDeploy:\n    - scriptName: openBrowser\n      skipOnCI: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const openBrowser = new LocalScript({ executeCommand: 'open http://localhost:3000' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { openBrowser },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'openBrowser', skipOnCI: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false),
      "skipOnLocal": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip this hook when running locally; only run in CI/CD.\n\n---\n\nUseful for CI-only tasks (e.g., uploading test reports, notifying Slack).\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  uploadTestReport:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/upload-report.js\nhooks:\n  afterDeploy:\n    - scriptName: uploadTestReport\n      skipOnLocal: true\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadTestReport = new LocalScript({ executeCommand: 'node ./scripts/upload-report.js' });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return {\n    scripts: { uploadTestReport },\n    hooks: {\n      afterDeploy: [\n        { scriptName: 'uploadTestReport', skipOnLocal: true }\n      ]\n    },\n    resources: { web }\n  };\n});\n```").default(false) }).strict()
    ).optional().describe("#### Scripts to run after dev mode exits.\n\n---\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  teardownLocal:\n    type: local-script\n    properties:\n      executeCommand: docker compose down\nhooks:\n  afterDev:\n    - scriptName: teardownLocal\nresources:\n  cache:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: key\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const teardownLocal = new LocalScript({ executeCommand: 'docker compose down' });\n  const cache = new DynamoDbTable({ primaryKey: { partitionKey: { name: 'key', type: 'string' } } });\n\n  return {\n    scripts: { teardownLocal },\n    hooks: {\n      afterDev: [{ scriptName: 'teardownLocal' }]\n    },\n    resources: { cache }\n  };\n});\n```").optional() }).strict()
  .optional().describe("#### Run scripts automatically before/after deploy, delete, or dev commands.\n\n---\n\nEach hook references a script defined in the `scripts` section.\nCommon uses: run database migrations after deploy, build frontend before deploy,\nclean up resources after delete.\n\n**Example (YAML):**\n\n```yaml\nhooks:\n  afterDeploy:\n    - scriptName: migrateDatabase\n  beforeDeploy:\n    - scriptName: buildFrontend\n      skipOnCI: true\nscripts:\n  migrateDatabase:\n    type: local-script\n    properties:\n      executeCommand: npx prisma migrate deploy\n      connectTo:\n        - mainDb\n  buildFrontend:\n    type: local-script\n    properties:\n      executeCommand: npm run build\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      credentials:\n        masterUserPassword: $Secret('db.password')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    credentials: { masterUserPassword: $Secret('db.password') }\n  });\n\n  return {\n    hooks: {\n      afterDeploy: [{ scriptName: 'migrateDatabase' }],\n      beforeDeploy: [{ scriptName: 'buildFrontend', skipOnCI: true }]\n    },\n    scripts: {\n      migrateDatabase: {\n        type: 'local-script',\n        properties: { executeCommand: 'npx prisma migrate deploy', connectTo: ['mainDb'] }\n      },\n      buildFrontend: {\n        type: 'local-script',\n        properties: { executeCommand: 'npm run build' }\n      }\n    },\n    resources: { mainDb }\n  };\n});\n```").optional(),
  "scripts": z.record(z.string(), z.union([z.object({
      "type": z.literal("local-script-with-bastion-tunneling"),
      "properties": z.object({
        "bastionResource": z.string().optional().describe("#### Bastion Resource Name\n\n---\n\nThe name of the bastion resource to use for tunneling to protected resources.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrateThroughTunnel:\n    type: local-script-with-bastion-tunneling\n    properties:\n      bastionResource: jumpBox\n      executeCommand: npx prisma migrate deploy\n      connectTo:\n        - privateDb\nresources:\n  jumpBox:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n  privateDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, RelationalDatabase, RdsEnginePostgres, LocalScriptWithBastionTunneling, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jumpBox = new Bastion({ instanceSize: 't3.micro' });\n  const privateDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrateThroughTunnel = new LocalScriptWithBastionTunneling({\n    bastionResource: 'jumpBox',\n    executeCommand: 'npx prisma migrate deploy',\n    connectTo: [privateDb]\n  });\n\n  return { scripts: { migrateThroughTunnel }, resources: { jumpBox, privateDb } };\n});\n```").optional(),
        "executeScript": z.string().optional().describe("#### Execute Script\n\n---\n\nThe path to a script file to execute. The script can be written in JavaScript, TypeScript, or Python and runs in a separate process.\n\nThe executable is determined by `defaults:configure` or the system default (`node` for JS/TS, `python` for Python). You can only use one of `executeScript`, `executeScripts`, `executeCommand`, or `executeCommands`.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  seed:\n    type: local-script\n    properties:\n      executeScript: scripts/seed.ts\n      connectTo:\n        - db\nresources:\n  db:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: id\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new DynamoDbTable({ primaryKey: { partitionKey: { name: 'id', type: 'string' } } });\n  const seed = new LocalScript({\n    executeScript: 'scripts/seed.ts',\n    connectTo: [db]\n  });\n\n  return { scripts: { seed }, resources: { db } };\n});\n```").optional(),
        "executeCommand": z.string().optional().describe("#### Execute Command\n\n---\n\nA single terminal command to execute in a separate shell process.\n\nThe command runs on the machine executing the Stacktape command. Be aware of potential differences between local and CI environments (e.g., OS, shell). You can only use one of `executeScript`, `executeScripts`, `executeCommand`, or `executeCommands`.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  build:\n    type: local-script\n    properties:\n      executeCommand: npm run build\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const build = new LocalScript({\n    executeCommand: 'npm run build'\n  });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return { scripts: { build }, resources: { web } };\n});\n```").optional(),
        "executeScripts": z.array(z.string()).optional().describe("#### Execute Scripts\n\n---\n\nA list of script files to execute sequentially. Each script runs in a separate process.\n\nThe script can be written in JavaScript, TypeScript, or Python. The executable is determined by `defaults:configure` or the system default. You can only use one of `executeScript`, `executeScripts`, `executeCommand`, or `executeCommands`.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  setup:\n    type: local-script\n    properties:\n      executeScripts:\n        - scripts/migrate.ts\n        - scripts/seed.ts\n      connectTo:\n        - db\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const setup = new LocalScript({\n    executeScripts: ['scripts/migrate.ts', 'scripts/seed.ts'],\n    connectTo: [db]\n  });\n\n  return { scripts: { setup }, resources: { db } };\n});\n```").optional(),
        "executeCommands": z.array(z.string()).optional().describe("#### Execute Commands\n\n---\n\nA list of terminal commands to execute sequentially. Each command runs in a separate shell process.\n\nThe commands run on the machine executing the Stacktape command. Be aware of potential differences between environments. You can only use one of `executeScript`, `executeScripts`, `executeCommand`, or `executeCommands`.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  buildAll:\n    type: local-script\n    properties:\n      executeCommands:\n        - npm ci\n        - npm run lint\n        - npm run build\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const buildAll = new LocalScript({\n    executeCommands: ['npm ci', 'npm run lint', 'npm run build']\n  });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return { scripts: { buildAll }, resources: { web } };\n});\n```").optional(),
        "cwd": z.string().optional().describe("#### Working Directory\n\n---\n\nThe directory where the script or command will be executed.\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  buildFrontend:\n    type: local-script\n    properties:\n      executeCommand: npm run build\n      cwd: ./frontend\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./frontend/dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const buildFrontend = new LocalScript({\n    executeCommand: 'npm run build',\n    cwd: './frontend'\n  });\n  const web = new HostingBucket({ uploadDirectoryPath: './frontend/dist' });\n\n  return { scripts: { buildFrontend }, resources: { web } };\n});\n```"),
        "pipeStdio": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Pipe Stdio\n\n---\n\nIf `true`, pipes the standard input/output (stdio) of the hook process to the main process. This allows you to see logs from your hook and interact with prompts.\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  silentTask:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/background-task.js\n      pipeStdio: false\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const silentTask = new LocalScript({\n    executeCommand: 'node ./scripts/background-task.js',\n    pipeStdio: false\n  });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return { scripts: { silentTask }, resources: { web } };\n});\n```").default(true),
        "connectTo": z.array(z.string()).optional().describe("#### Connect To\n\n---\n\nA list of resources the script needs to interact with. Stacktape automatically injects environment variables with connection details for each specified resource.\n\nEnvironment variable names are in the format `STP_[RESOURCE_NAME]_[VARIABLE_NAME]` (e.g., `STP_MY_DATABASE_CONNECTION_STRING`).\n\n**Injected Variables by Resource Type:**\n- **`Bucket`**: `NAME`, `ARN`\n- **`DynamoDbTable`**: `NAME`, `ARN`, `STREAM_ARN`\n- **`MongoDbAtlasCluster`**: `CONNECTION_STRING`\n- **`RelationalDatabase`**: `CONNECTION_STRING`, `JDBC_CONNECTION_STRING`, `HOST`, `PORT`. For Aurora clusters, `READER_CONNECTION_STRING`, `READER_JDBC_CONNECTION_STRING`, and `READER_HOST` are also included.\n- **`RedisCluster`**: `HOST`, `READER_HOST`, `PORT`\n- **`EventBus`**: `ARN`\n- **`Function`**: `ARN`\n- **`BatchJob`**: `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n- **`UserAuthPool`**: `ID`, `CLIENT_ID`, `ARN`\n- **`SnsTopic`**: `ARN`, `NAME`\n- **`SqsQueue`**: `ARN`, `NAME`, `URL`\n- **`UpstashKafkaTopic`**: `TOPIC_NAME`, `TOPIC_ID`, `USERNAME`, `PASSWORD`, `TCP_ENDPOINT`, `REST_URL`\n- **`UpstashRedis`**: `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n- **`PrivateService`**: `ADDRESS`\n- **`WebService`**: `URL`\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npx prisma migrate deploy\n      connectTo:\n        - mainDb\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({\n    executeCommand: 'npx prisma migrate deploy',\n    connectTo: [mainDb]\n  });\n\n  return { scripts: { migrate }, resources: { mainDb } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment Variables\n\n---\n\nA list of environment variables to pass to the script or command.\n\nValues can be:\n- A static string, number, or boolean.\n- The result of a [custom directive](https://docs.stacktape.com/configuration/directives/#custom-directives).\n- A reference to another resource's parameter using the [`$ResourceParam` directive](https://docs.stacktape.com/configuration/referenceable-parameters/).\n- A value from a [secret](https://docs.stacktape.com/resources/secrets/) using the [`$Secret` directive](https://docs.stacktape.com/configuration/directives/#secret).\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  seed:\n    type: local-script\n    properties:\n      executeScript: scripts/seed.ts\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: API_KEY\n          value: $Secret('seed-api-key')\nresources:\n  table:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: id\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const table = new DynamoDbTable({ primaryKey: { partitionKey: { name: 'id', type: 'string' } } });\n  const seed = new LocalScript({\n    executeScript: 'scripts/seed.ts',\n    environment: {\n      NODE_ENV: 'production',\n      API_KEY: $Secret('seed-api-key')\n    }\n  });\n\n  return { scripts: { seed }, resources: { table } };\n});\n```").optional(),
        "assumeRoleOfResource": z.string().optional().describe("#### Assume Role of Resource\n\n---\n\nThe name of a deployed resource whose IAM role the script should assume. This grants the script the same permissions as the specified resource.\n\nThe resource must be deployed before the script is executed. Stacktape injects temporary AWS credentials as environment variables, which are automatically used by most AWS SDKs and CLIs.\n\n**Supported Resource Types:**\n- `function`\n- `batch-job`\n- `worker-service`\n- `web-service`\n- `private-service`\n- `multi-container-workload`\n- `nextjs-web`\n- `astro-web`\n- `nuxt-web`\n- `sveltekit-web`\n- `solidstart-web`\n- `tanstack-web`\n- `remix-web`\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  invokeAsApi:\n    type: local-script\n    properties:\n      executeScript: scripts/admin-task.ts\n      assumeRoleOfResource: api\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      connectTo:\n        - dataBucket\n  dataBucket:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, Bucket, StacktapeImageBuildpackPackaging, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const dataBucket = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    connectTo: [dataBucket]\n  });\n  const invokeAsApi = new LocalScript({\n    executeScript: 'scripts/admin-task.ts',\n    assumeRoleOfResource: 'api'\n  });\n\n  return { scripts: { invokeAsApi }, resources: { api, dataBucket } };\n});\n```").optional() }).strict()
    }).strict()
    .describe("#### A local script with secure tunneling through a bastion host.\n\n---\n\nThis script type runs locally but tunnels connections to resources through a bastion server.\nIt provides a secure, encrypted connection to resources that are only accessible within the VPC,\nsuch as private databases or Redis clusters.\n\nThe environment variables injected by `connectTo` are automatically adjusted to use the tunneled endpoints."), z.object({
      "type": z.literal("bastion-script"),
      "properties": z.object({
        "bastionResource": z.string().optional().describe("#### Bastion Resource Name\n\n---\n\nThe name of the bastion resource on which the commands will be executed.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  runOnBastion:\n    type: bastion-script\n    properties:\n      bastionResource: jumpBox\n      executeCommand: psql -h $STP_PRIVATE_DB_HOST -c 'SELECT 1'\n      connectTo:\n        - privateDb\nresources:\n  jumpBox:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n  privateDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, RelationalDatabase, RdsEnginePostgres, BastionScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jumpBox = new Bastion({ instanceSize: 't3.micro' });\n  const privateDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const runOnBastion = new BastionScript({\n    bastionResource: 'jumpBox',\n    executeCommand: \"psql -h $STP_PRIVATE_DB_HOST -c 'SELECT 1'\",\n    connectTo: [privateDb]\n  });\n\n  return { scripts: { runOnBastion }, resources: { jumpBox, privateDb } };\n});\n```").optional(),
        "executeCommand": z.string().optional().describe("#### Execute Command\n\n---\n\nA single terminal command to execute on the bastion host. Logs from the execution are streamed to your terminal.\n\nYou can use either `executeCommand` or `executeCommands`, but not both.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  pingDb:\n    type: bastion-script\n    properties:\n      bastionResource: jumpBox\n      executeCommand: pg_isready -h $STP_PRIVATE_DB_HOST\n      connectTo:\n        - privateDb\nresources:\n  jumpBox:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n  privateDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, RelationalDatabase, RdsEnginePostgres, BastionScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jumpBox = new Bastion({ instanceSize: 't3.micro' });\n  const privateDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const pingDb = new BastionScript({\n    bastionResource: 'jumpBox',\n    executeCommand: 'pg_isready -h $STP_PRIVATE_DB_HOST',\n    connectTo: [privateDb]\n  });\n\n  return { scripts: { pingDb }, resources: { jumpBox, privateDb } };\n});\n```").optional(),
        "executeCommands": z.array(z.string()).optional().describe("#### Execute Commands\n\n---\n\nA list of terminal commands to execute sequentially as a script on the bastion host. Logs from the execution are streamed to your terminal.\n\nYou can use either `executeCommand` or `executeCommands`, but not both.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  dbMaintenance:\n    type: bastion-script\n    properties:\n      bastionResource: jumpBox\n      executeCommands:\n        - psql -h $STP_PRIVATE_DB_HOST -c 'VACUUM ANALYZE;'\n        - psql -h $STP_PRIVATE_DB_HOST -c 'REINDEX DATABASE postgres;'\n      connectTo:\n        - privateDb\nresources:\n  jumpBox:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n  privateDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, RelationalDatabase, RdsEnginePostgres, BastionScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jumpBox = new Bastion({ instanceSize: 't3.micro' });\n  const privateDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const dbMaintenance = new BastionScript({\n    bastionResource: 'jumpBox',\n    executeCommands: [\n      \"psql -h $STP_PRIVATE_DB_HOST -c 'VACUUM ANALYZE;'\",\n      \"psql -h $STP_PRIVATE_DB_HOST -c 'REINDEX DATABASE postgres;'\"\n    ],\n    connectTo: [privateDb]\n  });\n\n  return { scripts: { dbMaintenance }, resources: { jumpBox, privateDb } };\n});\n```").optional(),
        "cwd": z.string().optional().describe("#### Working Directory\n\n---\n\nThe directory on the bastion host where the command will be executed.\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  runScript:\n    type: bastion-script\n    properties:\n      bastionResource: jumpBox\n      executeCommand: ./maintenance.sh\n      cwd: /opt/scripts\nresources:\n  jumpBox:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, BastionScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jumpBox = new Bastion({ instanceSize: 't3.micro' });\n  const runScript = new BastionScript({\n    bastionResource: 'jumpBox',\n    executeCommand: './maintenance.sh',\n    cwd: '/opt/scripts'\n  });\n\n  return { scripts: { runScript }, resources: { jumpBox } };\n});\n```"),
        "connectTo": z.array(z.string()).optional().describe("#### Connect To\n\n---\n\nA list of resources the script needs to interact with. Stacktape automatically injects environment variables with connection details for each specified resource.\n\nEnvironment variable names are in the format `STP_[RESOURCE_NAME]_[VARIABLE_NAME]` (e.g., `STP_MY_DATABASE_CONNECTION_STRING`).\n\n**Injected Variables by Resource Type:**\n- **`Bucket`**: `NAME`, `ARN`\n- **`DynamoDbTable`**: `NAME`, `ARN`, `STREAM_ARN`\n- **`MongoDbAtlasCluster`**: `CONNECTION_STRING`\n- **`RelationalDatabase`**: `CONNECTION_STRING`, `JDBC_CONNECTION_STRING`, `HOST`, `PORT`. For Aurora clusters, `READER_CONNECTION_STRING`, `READER_JDBC_CONNECTION_STRING`, and `READER_HOST` are also included.\n- **`RedisCluster`**: `HOST`, `READER_HOST`, `PORT`\n- **`EventBus`**: `ARN`\n- **`Function`**: `ARN`\n- **`BatchJob`**: `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n- **`UserAuthPool`**: `ID`, `CLIENT_ID`, `ARN`\n- **`SnsTopic`**: `ARN`, `NAME`\n- **`SqsQueue`**: `ARN`, `NAME`, `URL`\n- **`UpstashKafkaTopic`**: `TOPIC_NAME`, `TOPIC_ID`, `USERNAME`, `PASSWORD`, `TCP_ENDPOINT`, `REST_URL`\n- **`UpstashRedis`**: `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n- **`PrivateService`**: `ADDRESS`\n- **`WebService`**: `URL`\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npx prisma migrate deploy\n      connectTo:\n        - mainDb\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({\n    executeCommand: 'npx prisma migrate deploy',\n    connectTo: [mainDb]\n  });\n\n  return { scripts: { migrate }, resources: { mainDb } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment Variables\n\n---\n\nA list of environment variables to pass to the script or command.\n\nValues can be:\n- A static string, number, or boolean.\n- The result of a [custom directive](https://docs.stacktape.com/configuration/directives/#custom-directives).\n- A reference to another resource's parameter using the [`$ResourceParam` directive](https://docs.stacktape.com/configuration/referenceable-parameters/).\n- A value from a [secret](https://docs.stacktape.com/resources/secrets/) using the [`$Secret` directive](https://docs.stacktape.com/configuration/directives/#secret).\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  seed:\n    type: local-script\n    properties:\n      executeScript: scripts/seed.ts\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: API_KEY\n          value: $Secret('seed-api-key')\nresources:\n  table:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: id\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const table = new DynamoDbTable({ primaryKey: { partitionKey: { name: 'id', type: 'string' } } });\n  const seed = new LocalScript({\n    executeScript: 'scripts/seed.ts',\n    environment: {\n      NODE_ENV: 'production',\n      API_KEY: $Secret('seed-api-key')\n    }\n  });\n\n  return { scripts: { seed }, resources: { table } };\n});\n```").optional(),
        "assumeRoleOfResource": z.string().optional().describe("#### Assume Role of Resource\n\n---\n\nThe name of a deployed resource whose IAM role the script should assume. This grants the script the same permissions as the specified resource.\n\nThe resource must be deployed before the script is executed. Stacktape injects temporary AWS credentials as environment variables, which are automatically used by most AWS SDKs and CLIs.\n\n**Supported Resource Types:**\n- `function`\n- `batch-job`\n- `worker-service`\n- `web-service`\n- `private-service`\n- `multi-container-workload`\n- `nextjs-web`\n- `astro-web`\n- `nuxt-web`\n- `sveltekit-web`\n- `solidstart-web`\n- `tanstack-web`\n- `remix-web`\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  invokeAsApi:\n    type: local-script\n    properties:\n      executeScript: scripts/admin-task.ts\n      assumeRoleOfResource: api\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      connectTo:\n        - dataBucket\n  dataBucket:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, Bucket, StacktapeImageBuildpackPackaging, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const dataBucket = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    connectTo: [dataBucket]\n  });\n  const invokeAsApi = new LocalScript({\n    executeScript: 'scripts/admin-task.ts',\n    assumeRoleOfResource: 'api'\n  });\n\n  return { scripts: { invokeAsApi }, resources: { api, dataBucket } };\n});\n```").optional() }).strict()
    }).strict()
    .describe("#### A script that runs remotely on a bastion server.\n\n---\n\nBastion scripts are executed on a bastion server within your VPC, not on your local machine.\nLogs from the script's execution are streamed in real-time to your terminal.\n\nThis is useful for running commands that need direct access to VPC resources\nor for ensuring consistent execution environments across different machines."), z.object({
      "type": z.literal("local-script"),
      "properties": z.object({
        "executeScript": z.string().optional().describe("#### Execute Script\n\n---\n\nThe path to a script file to execute. The script can be written in JavaScript, TypeScript, or Python and runs in a separate process.\n\nThe executable is determined by `defaults:configure` or the system default (`node` for JS/TS, `python` for Python). You can only use one of `executeScript`, `executeScripts`, `executeCommand`, or `executeCommands`.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  seed:\n    type: local-script\n    properties:\n      executeScript: scripts/seed.ts\n      connectTo:\n        - db\nresources:\n  db:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: id\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new DynamoDbTable({ primaryKey: { partitionKey: { name: 'id', type: 'string' } } });\n  const seed = new LocalScript({\n    executeScript: 'scripts/seed.ts',\n    connectTo: [db]\n  });\n\n  return { scripts: { seed }, resources: { db } };\n});\n```").optional(),
        "executeCommand": z.string().optional().describe("#### Execute Command\n\n---\n\nA single terminal command to execute in a separate shell process.\n\nThe command runs on the machine executing the Stacktape command. Be aware of potential differences between local and CI environments (e.g., OS, shell). You can only use one of `executeScript`, `executeScripts`, `executeCommand`, or `executeCommands`.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  build:\n    type: local-script\n    properties:\n      executeCommand: npm run build\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const build = new LocalScript({\n    executeCommand: 'npm run build'\n  });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return { scripts: { build }, resources: { web } };\n});\n```").optional(),
        "executeScripts": z.array(z.string()).optional().describe("#### Execute Scripts\n\n---\n\nA list of script files to execute sequentially. Each script runs in a separate process.\n\nThe script can be written in JavaScript, TypeScript, or Python. The executable is determined by `defaults:configure` or the system default. You can only use one of `executeScript`, `executeScripts`, `executeCommand`, or `executeCommands`.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  setup:\n    type: local-script\n    properties:\n      executeScripts:\n        - scripts/migrate.ts\n        - scripts/seed.ts\n      connectTo:\n        - db\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const setup = new LocalScript({\n    executeScripts: ['scripts/migrate.ts', 'scripts/seed.ts'],\n    connectTo: [db]\n  });\n\n  return { scripts: { setup }, resources: { db } };\n});\n```").optional(),
        "executeCommands": z.array(z.string()).optional().describe("#### Execute Commands\n\n---\n\nA list of terminal commands to execute sequentially. Each command runs in a separate shell process.\n\nThe commands run on the machine executing the Stacktape command. Be aware of potential differences between environments. You can only use one of `executeScript`, `executeScripts`, `executeCommand`, or `executeCommands`.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  buildAll:\n    type: local-script\n    properties:\n      executeCommands:\n        - npm ci\n        - npm run lint\n        - npm run build\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const buildAll = new LocalScript({\n    executeCommands: ['npm ci', 'npm run lint', 'npm run build']\n  });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return { scripts: { buildAll }, resources: { web } };\n});\n```").optional(),
        "cwd": z.string().optional().describe("#### Working Directory\n\n---\n\nThe directory where the script or command will be executed.\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  buildFrontend:\n    type: local-script\n    properties:\n      executeCommand: npm run build\n      cwd: ./frontend\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./frontend/dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const buildFrontend = new LocalScript({\n    executeCommand: 'npm run build',\n    cwd: './frontend'\n  });\n  const web = new HostingBucket({ uploadDirectoryPath: './frontend/dist' });\n\n  return { scripts: { buildFrontend }, resources: { web } };\n});\n```"),
        "pipeStdio": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Pipe Stdio\n\n---\n\nIf `true`, pipes the standard input/output (stdio) of the hook process to the main process. This allows you to see logs from your hook and interact with prompts.\n\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  silentTask:\n    type: local-script\n    properties:\n      executeCommand: node ./scripts/background-task.js\n      pipeStdio: false\nresources:\n  web:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const silentTask = new LocalScript({\n    executeCommand: 'node ./scripts/background-task.js',\n    pipeStdio: false\n  });\n  const web = new HostingBucket({ uploadDirectoryPath: './dist' });\n\n  return { scripts: { silentTask }, resources: { web } };\n});\n```").default(true),
        "connectTo": z.array(z.string()).optional().describe("#### Connect To\n\n---\n\nA list of resources the script needs to interact with. Stacktape automatically injects environment variables with connection details for each specified resource.\n\nEnvironment variable names are in the format `STP_[RESOURCE_NAME]_[VARIABLE_NAME]` (e.g., `STP_MY_DATABASE_CONNECTION_STRING`).\n\n**Injected Variables by Resource Type:**\n- **`Bucket`**: `NAME`, `ARN`\n- **`DynamoDbTable`**: `NAME`, `ARN`, `STREAM_ARN`\n- **`MongoDbAtlasCluster`**: `CONNECTION_STRING`\n- **`RelationalDatabase`**: `CONNECTION_STRING`, `JDBC_CONNECTION_STRING`, `HOST`, `PORT`. For Aurora clusters, `READER_CONNECTION_STRING`, `READER_JDBC_CONNECTION_STRING`, and `READER_HOST` are also included.\n- **`RedisCluster`**: `HOST`, `READER_HOST`, `PORT`\n- **`EventBus`**: `ARN`\n- **`Function`**: `ARN`\n- **`BatchJob`**: `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n- **`UserAuthPool`**: `ID`, `CLIENT_ID`, `ARN`\n- **`SnsTopic`**: `ARN`, `NAME`\n- **`SqsQueue`**: `ARN`, `NAME`, `URL`\n- **`UpstashKafkaTopic`**: `TOPIC_NAME`, `TOPIC_ID`, `USERNAME`, `PASSWORD`, `TCP_ENDPOINT`, `REST_URL`\n- **`UpstashRedis`**: `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n- **`PrivateService`**: `ADDRESS`\n- **`WebService`**: `URL`\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  migrate:\n    type: local-script\n    properties:\n      executeCommand: npx prisma migrate deploy\n      connectTo:\n        - mainDb\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const migrate = new LocalScript({\n    executeCommand: 'npx prisma migrate deploy',\n    connectTo: [mainDb]\n  });\n\n  return { scripts: { migrate }, resources: { mainDb } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment Variables\n\n---\n\nA list of environment variables to pass to the script or command.\n\nValues can be:\n- A static string, number, or boolean.\n- The result of a [custom directive](https://docs.stacktape.com/configuration/directives/#custom-directives).\n- A reference to another resource's parameter using the [`$ResourceParam` directive](https://docs.stacktape.com/configuration/referenceable-parameters/).\n- A value from a [secret](https://docs.stacktape.com/resources/secrets/) using the [`$Secret` directive](https://docs.stacktape.com/configuration/directives/#secret).\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  seed:\n    type: local-script\n    properties:\n      executeScript: scripts/seed.ts\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: API_KEY\n          value: $Secret('seed-api-key')\nresources:\n  table:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: id\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, LocalScript, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const table = new DynamoDbTable({ primaryKey: { partitionKey: { name: 'id', type: 'string' } } });\n  const seed = new LocalScript({\n    executeScript: 'scripts/seed.ts',\n    environment: {\n      NODE_ENV: 'production',\n      API_KEY: $Secret('seed-api-key')\n    }\n  });\n\n  return { scripts: { seed }, resources: { table } };\n});\n```").optional(),
        "assumeRoleOfResource": z.string().optional().describe("#### Assume Role of Resource\n\n---\n\nThe name of a deployed resource whose IAM role the script should assume. This grants the script the same permissions as the specified resource.\n\nThe resource must be deployed before the script is executed. Stacktape injects temporary AWS credentials as environment variables, which are automatically used by most AWS SDKs and CLIs.\n\n**Supported Resource Types:**\n- `function`\n- `batch-job`\n- `worker-service`\n- `web-service`\n- `private-service`\n- `multi-container-workload`\n- `nextjs-web`\n- `astro-web`\n- `nuxt-web`\n- `sveltekit-web`\n- `solidstart-web`\n- `tanstack-web`\n- `remix-web`\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  invokeAsApi:\n    type: local-script\n    properties:\n      executeScript: scripts/admin-task.ts\n      assumeRoleOfResource: api\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      connectTo:\n        - dataBucket\n  dataBucket:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, Bucket, StacktapeImageBuildpackPackaging, LocalScript, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const dataBucket = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    connectTo: [dataBucket]\n  });\n  const invokeAsApi = new LocalScript({\n    executeScript: 'scripts/admin-task.ts',\n    assumeRoleOfResource: 'api'\n  });\n\n  return { scripts: { invokeAsApi }, resources: { api, dataBucket } };\n});\n```").optional() }).strict()
    }).strict()
    .describe("#### A script that runs on your local machine.\n\n---\n\nLocal scripts are executed on the same machine where the Stacktape command is run.\nThey are useful for tasks like building your application, running database migrations, or other automation.\n\nThe script must define one of the following: `executeCommand`, `executeScript`, `executeCommands`, or `executeScripts`.")])).optional().describe("#### Custom shell commands or code you can run manually or as lifecycle hooks.\n\n---\n\nUse `connectTo` in a script to auto-inject database URLs, API keys, etc. as environment variables.\nRun scripts with `stacktape script:run --scriptName myScript` or attach them to `hooks`.\n\n**Script types:**\n- **`local-script`**: Runs on your machine (or CI). Good for migrations, builds, seed scripts.\n- **`local-script-with-bastion-tunneling`**: Runs locally but tunnels connections to VPC-only\n  resources (e.g., private databases) through a bastion host.\n- **`bastion-script`**: Runs remotely on the bastion host inside your VPC.\n\nScripts can be shell commands or JS/TS/Python files.\n\n**Example (YAML):**\n\n```yaml\nscripts:\n  seedDatabase:\n    type: local-script\n    properties:\n      executeScript: scripts/seed.ts\n      connectTo:\n        - mainDb\n  runMaintenance:\n    type: bastion-script\n    properties:\n      bastionResource: bastion\n      executeCommand: psql -c 'VACUUM ANALYZE;'\nresources:\n  bastion:\n    type: bastion\n    properties: {}\n  mainDb:\n    type: relational-database\n    properties:\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      credentials:\n        masterUserPassword: $Secret('db.password')\n      accessibility:\n        accessibilityMode: vpc\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({});\n  const mainDb = new RelationalDatabase({\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t4g.micro' }\n      }\n    },\n    credentials: { masterUserPassword: $Secret('db.password') },\n    accessibility: { accessibilityMode: 'vpc' }\n  });\n\n  return {\n    scripts: {\n      seedDatabase: {\n        type: 'local-script',\n        properties: { executeScript: 'scripts/seed.ts', connectTo: ['mainDb'] }\n      },\n      runMaintenance: {\n        type: 'bastion-script',\n        properties: { bastionResource: 'bastion', executeCommand: \"psql -c 'VACUUM ANALYZE;'\" }\n      }\n    },\n    resources: { bastion, mainDb }\n  };\n});\n```").optional(),
  "directives": z.array(z.object({
    "name": z.string().describe("#### Directive Name\n\n---\n\nThe name of the custom directive.\n\n**Example (YAML):**\n\n```yaml\ndirectives:\n  - name: GetCommitSha\n    filePath: directives/git.ts:getCommitSha\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: COMMIT_SHA\n          value: $GetCommitSha()\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: { COMMIT_SHA: '$GetCommitSha()' }\n  });\n\n  return {\n    directives: [{ name: 'GetCommitSha', filePath: 'directives/git.ts:getCommitSha' }],\n    resources: { api }\n  };\n});\n```"),
    "filePath": z.string().describe("#### File Path\n\n---\n\nThe path to the file where the directive is defined, in the format `{file-path}:{handler}`.\n\nIf the `{handler}` is omitted:\n- For `.js` and `.ts` files, the `default` export is used.\n- For `.py` files, the `main` function is used.\n\n**Example (YAML):**\n\n```yaml\ndirectives:\n  - name: GetCommitSha\n    filePath: directives/git.ts:getCommitSha\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: COMMIT_SHA\n          value: $GetCommitSha()\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: { COMMIT_SHA: '$GetCommitSha()' }\n  });\n\n  return {\n    directives: [\n      {\n        name: 'GetCommitSha',\n        filePath: 'directives/git.ts:getCommitSha'\n      }\n    ],\n    resources: { api }\n  };\n});\n```") }).strict()
  ).optional().describe("#### Register custom functions that dynamically compute config values at deploy time.\n\n---\n\nDefine a directive by pointing to a JS/TS/Python file, then use it anywhere in the config\nlike a built-in directive (`$MyDirective()`). Useful for fetching external data,\ncomputing dynamic values, or conditional logic.\n\n**Example (YAML):**\n\n```yaml\ndirectives:\n  - name: getApiKey\n    filePath: directives/secrets.ts:getApiKey\n  - name: computeStageDomain\n    filePath: directives/domain.ts\nresources:\n  worker:\n    type: worker-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: API_KEY\n          value: $GetApiKey()\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new WorkerService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    environment: { API_KEY: '$GetApiKey()' }\n  });\n\n  return {\n    directives: [\n      { name: 'getApiKey', filePath: 'directives/secrets.ts:getApiKey' },\n      { name: 'computeStageDomain', filePath: 'directives/domain.ts' }\n    ],\n    resources: { worker }\n  };\n});\n```").optional(),
  "deploymentConfig": z.object({
    "terminationProtection": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Prevents accidental stack deletion. Must be disabled before you can delete.\n\n---\n\nRecommended for production stacks. To delete a protected stack, first deploy with\n`terminationProtection: false`, then run the delete command.\n\n\n**Example (YAML):**\n\n```yaml\ndeploymentConfig:\n  terminationProtection: true\nresources:\n  prodDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      deletionProtection: true\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const prodDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    deletionProtection: true,\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.r6g.large' } })\n  });\n\n  return {\n    deploymentConfig: { terminationProtection: true },\n    resources: { prodDb }\n  };\n});\n```").default(false),
    "cloudformationRoleArn": z.string().optional().describe("#### IAM role for CloudFormation to assume during create/update/delete operations.\n\n---\n\nUse this when your deploy user has limited permissions and CloudFormation needs\na more privileged role to manage resources. The role is persisted across deployments\nand reused for delete/rollback even if removed from config later.\n\n**Example (YAML):**\n\n```yaml\ndeploymentConfig:\n  cloudformationRoleArn: arn:aws:iam::123456789012:role/CloudFormationDeployRole\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    deploymentConfig: { cloudformationRoleArn: 'arn:aws:iam::123456789012:role/CloudFormationDeployRole' },\n    resources: { api }\n  };\n});\n```").optional(),
    "triggerRollbackOnAlarms": z.array(z.string()).optional().describe("#### Alarms that trigger automatic rollback if they fire during deployment.\n\n---\n\nSpecify alarm names (from `alarms` section) or ARNs. The alarm must already exist -\na newly created alarm only takes effect on the *next* deployment.\n\nUse with `monitoringTimeAfterDeploymentInMinutes` to keep watching after deploy completes.\n\n**Example (YAML):**\n\n```yaml\ndeploymentConfig:\n  triggerRollbackOnAlarms:\n    - arn:aws:cloudwatch:eu-west-1:123456789012:alarm:high-error-rate\n  monitoringTimeAfterDeploymentInMinutes: 10\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n\n  return {\n    deploymentConfig: {\n      triggerRollbackOnAlarms: ['arn:aws:cloudwatch:eu-west-1:123456789012:alarm:high-error-rate'],\n      monitoringTimeAfterDeploymentInMinutes: 10\n    },\n    resources: { api }\n  };\n});\n```").optional(),
    "monitoringTimeAfterDeploymentInMinutes": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How long (in minutes) to monitor rollback alarms after deployment completes.\n\n---\n\nIf an alarm fires during this window, the stack rolls back automatically.\nOnly useful when `triggerRollbackOnAlarms` is configured.\n\n\n**Example (YAML):**\n\n```yaml\ndeploymentConfig:\n  triggerRollbackOnAlarms:\n    - arn:aws:cloudwatch:eu-west-1:123456789012:alarm:high-error-rate\n  monitoringTimeAfterDeploymentInMinutes: 15\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n\n  return {\n    deploymentConfig: {\n      triggerRollbackOnAlarms: ['arn:aws:cloudwatch:eu-west-1:123456789012:alarm:high-error-rate'],\n      monitoringTimeAfterDeploymentInMinutes: 15\n    },\n    resources: { api }\n  };\n});\n```").default(0),
    "disableAutoRollback": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Keep the stack in a failed state instead of rolling back on deployment failure.\n\n---\n\nUseful for debugging: inspect what went wrong, then fix and redeploy\n(or run `stacktape rollback` manually). By default, failed deployments\nauto-rollback to the last working state.\n\n\n**Example (YAML):**\n\n```yaml\ndeploymentConfig:\n  disableAutoRollback: true\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    deploymentConfig: { disableAutoRollback: true },\n    resources: { api }\n  };\n});\n```").default(false),
    "publishEventsToArn": z.array(z.string()).optional().describe("#### SNS topic ARNs to receive CloudFormation stack events during deployment.\n\n---\n\nUseful for monitoring deployments in external systems (Slack, PagerDuty, etc.).\n\n**Example (YAML):**\n\n```yaml\ndeploymentConfig:\n  publishEventsToArn:\n    - arn:aws:sns:eu-west-1:123456789012:deployment-events\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    deploymentConfig: { publishEventsToArn: ['arn:aws:sns:eu-west-1:123456789012:deployment-events'] },\n    resources: { api }\n  };\n});\n```").optional(),
    "previousVersionsToKeep": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many old deployment artifacts (Lambda bundles, container images) to keep.\n\n---\n\nOlder versions are cleaned up automatically. Lower values save storage costs,\nhigher values make it easier to roll back to previous versions.\n\n\n**Example (YAML):**\n\n```yaml\ndeploymentConfig:\n  previousVersionsToKeep: 3\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    deploymentConfig: { previousVersionsToKeep: 3 },\n    resources: { api }\n  };\n});\n```").default(10),
    "disableS3TransferAcceleration": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable faster uploads via S3 Transfer Acceleration.\n\n---\n\nTransfer Acceleration routes uploads through the nearest AWS edge location\nfor faster deploys, especially from distant regions. Adds a small cost per GB.\nAutomatically disabled in regions where it's not available.\n\n\n**Example (YAML):**\n\n```yaml\ndeploymentConfig:\n  disableS3TransferAcceleration: true\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    deploymentConfig: { disableS3TransferAcceleration: true },\n    resources: { api }\n  };\n});\n```").default(false) }).strict()
  .optional().describe("#### Advanced deployment settings: rollback behavior, termination protection, artifact retention.\n\n---\n\nMost projects don't need to change these. Useful for production stacks where you want\nextra safety (termination protection, rollback alarms) or cost control (artifact cleanup).\n\n**Example (YAML):**\n\n```yaml\ndeploymentConfig:\n  terminationProtection: true\n  disableAutoRollback: false\n  previousVersionsToKeep: 5\n  triggerRollbackOnAlarms:\n    - arn:aws:cloudwatch:eu-west-1:123456789012:alarm:api-high-error-rate\n  monitoringTimeAfterDeploymentInMinutes: 10\n  disableS3TransferAcceleration: false\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/server.ts' }\n    },\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n\n  return {\n    deploymentConfig: {\n      terminationProtection: true,\n      disableAutoRollback: false,\n      previousVersionsToKeep: 5,\n      triggerRollbackOnAlarms: ['arn:aws:cloudwatch:eu-west-1:123456789012:alarm:api-high-error-rate'],\n      monitoringTimeAfterDeploymentInMinutes: 10,\n      disableS3TransferAcceleration: false\n    },\n    resources: { api }\n  };\n});\n```").optional(),
  "stackConfig": z.object({
    "outputs": z.array(z.object({
      "name": z.string().describe("#### Name of the output (used as the key in terminal and stack info file)."),
      "value": z.string().describe("#### Value to output. Typically a directive like `$ResourceParam('myApi', 'url')`."),
      "description": z.string().optional().describe("#### Human-readable description shown alongside the output.\n\n---\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  outputs:\n    - name: ApiUrl\n      value: $ResourceParam('api', 'url')\n      description: Public HTTPS endpoint of the API service\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    stackConfig: {\n      outputs: [\n        {\n          name: 'ApiUrl',\n          value: $ResourceParam('api', 'url'),\n          description: 'Public HTTPS endpoint of the API service'\n        }\n      ]\n    },\n    resources: { api }\n  };\n});\n```").optional(),
      "export": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Make this output available to other CloudFormation stacks.\n\n---\n\nExported outputs can be referenced from other stacks using `$CfStackOutput()`.\n\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  outputs:\n    - name: SharedBucketName\n      value: $ResourceParam('assets', 'name')\n      export: true\nresources:\n  assets:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assets = new Bucket({});\n\n  return {\n    stackConfig: {\n      outputs: [\n        {\n          name: 'SharedBucketName',\n          value: $ResourceParam('assets', 'name'),\n          export: true\n        }\n      ]\n    },\n    resources: { assets }\n  };\n});\n```").default(false) }).strict()
    ).optional().describe("#### Custom values to display and save after each deployment.\n\n---\n\nUse outputs to surface dynamic values like API URLs, database endpoints, or resource ARNs\nthat are only known after deployment. Outputs are:\n- Printed in the terminal after deploy\n- Saved to the stack info JSON file\n- Optionally exported for cross-stack references (via `export: true`)\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  outputs:\n    - name: ApiUrl\n      value: $ResourceParam('api', 'url')\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    stackConfig: {\n      outputs: [{ name: 'ApiUrl', value: $ResourceParam('api', 'url') }]\n    },\n    resources: { api }\n  };\n});\n```").optional(),
    "tags": z.array(z.object({
      "name": z.string().describe("#### Tag name (1-128 characters)."),
      "value": z.string().describe("#### Tag value (1-256 characters).") }).strict()
    ).optional().describe("#### Tags applied to every AWS resource in this stack.\n\n---\n\nUseful for cost tracking, access control, and organization. Stacktape automatically\nadds `projectName`, `stage`, and `stackName` tags — your custom tags are merged on top.\n\nMax 45 tags.\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  tags:\n    - name: team\n      value: payments\n    - name: costCenter\n      value: '4100'\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    stackConfig: {\n      tags: [\n        { name: 'team', value: 'payments' },\n        { name: 'costCenter', value: '4100' }\n      ]\n    },\n    resources: { api }\n  };\n});\n```").optional(),
    "disableStackInfoSaving": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Stop saving stack info to a local file after each deployment.\n\n---\n\nBy default, Stacktape saves resource details and custom outputs to\n`.stacktape-stack-info/{stackName}.json` after every deploy.\n\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  disableStackInfoSaving: true\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    stackConfig: { disableStackInfoSaving: true },\n    resources: { api }\n  };\n});\n```").default(false),
    "stackInfoDirectory": z.string().optional().describe("#### Directory for the stack info JSON file.\n\n---\n\nRelative to the project root.\n\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  stackInfoDirectory: ./build/stack-info\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    stackConfig: { stackInfoDirectory: './build/stack-info' },\n    resources: { api }\n  };\n});\n```"),
    "vpc": z.object({
      "reuseVpc": z.object({
        "projectName": z.string().optional().describe("#### Project name of the stack whose VPC you want to share.\n\n---\n\nMust be used together with `stage`. Cannot be combined with `vpcId`.\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  vpc:\n    reuseVpc:\n      projectName: shared-infra\n      stage: production\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    stackConfig: {\n      vpc: {\n        reuseVpc: { projectName: 'shared-infra', stage: 'production' }\n      }\n    },\n    resources: { api }\n  };\n});\n```").optional(),
        "stage": z.string().optional().describe("#### Stage of the stack whose VPC you want to share.\n\n---\n\nMust be used together with `projectName`. Cannot be combined with `vpcId`.").optional(),
        "vpcId": z.string().optional().describe("#### Direct VPC ID to reuse (e.g., `vpc-1234567890abcdef0`).\n\n---\n\nUse this to connect to a VPC not managed by Stacktape. Cannot be combined\nwith `projectName`/`stage`.\n\nThe VPC must use a private CIDR range (10.x, 172.16-31.x, or 192.168.x)\nand have at least 3 public subnets (subnets with a route to an Internet Gateway).\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  vpc:\n    reuseVpc:\n      vpcId: vpc-1234567890abcdef0\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    stackConfig: {\n      vpc: {\n        reuseVpc: {\n          vpcId: 'vpc-1234567890abcdef0'\n        }\n      }\n    },\n    resources: { api }\n  };\n});\n```").optional() }).strict()
      .optional().describe("#### Share a VPC with another Stacktape stack or use an existing VPC.\n\n---\n\nUseful when this stack needs to access VPC-protected resources (databases, Redis)\nfrom another stack. By default, each stack gets its own VPC.\n\n> **Important:** Set this when first creating the stack. Adding it to an already\n> deployed stack can cause resources to be replaced and **data to be lost**.").optional(),
      "nat": z.object({
        "availabilityZones": z.union([z.literal(1), z.literal(2), z.literal(3)]).optional().describe("#### How many availability zones get a NAT Gateway (~$32/month each).\n\n---\n\n- **1**: Cheapest, but no redundancy if that AZ goes down.\n- **2**: Balanced cost and availability.\n- **3**: Highest availability.\n\nEach NAT Gateway gets a static Elastic IP that persists across deployments —\nuseful for IP whitelisting with external services.\n\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  vpc:\n    nat:\n      availabilityZones: 1\nresources:\n  worker:\n    type: worker-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      usePrivateSubnetsWithNAT: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new WorkerService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/worker.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    usePrivateSubnetsWithNAT: true\n  });\n\n  return {\n    stackConfig: { vpc: { nat: { availabilityZones: 1 } } },\n    resources: { worker }\n  };\n});\n```").default(2) }).strict()
      .optional().describe("#### NAT Gateway configuration for private subnets.\n\n---\n\nOnly applies when you have workloads using `usePrivateSubnetsWithNAT: true`.\nControls how many availability zones get a NAT Gateway (affects cost and redundancy).").optional() }).strict()
    .optional().describe("#### VPC configuration: reuse an existing VPC or configure NAT Gateways.").optional() }).strict()
  .optional().describe("#### Stack-wide settings: custom outputs, tags, VPC configuration, and stack info saving.\n\n---\n\n**Example (YAML):**\n\n```yaml\nstackConfig:\n  tags:\n    - name: team\n      value: backend\n    - name: costCenter\n      value: '4711'\n  outputs:\n    - name: apiUrl\n      value: $ResourceParam('api', 'url')\n      description: Public URL of the web service\n  stackInfoDirectory: .stacktape-stack-info/\n  disableStackInfoSaving: false\n  vpc:\n    nat:\n      availabilityZones: 2\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/server.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    stackConfig: {\n      tags: [\n        { name: 'team', value: 'backend' },\n        { name: 'costCenter', value: '4711' }\n      ],\n      outputs: [\n        { name: 'apiUrl', value: $ResourceParam('api', 'url'), description: 'Public URL of the web service' }\n      ],\n      stackInfoDirectory: '.stacktape-stack-info/',\n      disableStackInfoSaving: false,\n      vpc: { nat: { availabilityZones: 2 } }\n    },\n    resources: { api }\n  };\n});\n```").optional(),
  "resources": z.record(z.string(), z.union([z.object({
      "type": z.literal("application-load-balancer"),
      "properties": z.object({
        "interface": z.enum(["internal","internet"]).optional().describe("#### `internet` (public) or `internal` (VPC-only). Internal ALBs are not reachable from the internet.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      interface: internal\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            priority: 1\n            paths:\n              - /api/*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    interface: 'internal'\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          priority: 1,\n          paths: ['/api/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").default("internet"),
        "customDomains": z.union([z.array(z.string()), z.array(z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          )]).optional().describe("#### Custom domains.\n\n---\n\nBy default, Stacktape creates DNS records and TLS certificates for each domain.\nIf you manage DNS yourself, set `disableDnsRecordCreation` and provide `customCertificateArn`.\n\nBackward compatible format `string[]` is still supported.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n        - domainName: legacy.example.com\n          disableDnsRecordCreation: true\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [\n      { domainName: 'api.example.com' },\n      { domainName: 'legacy.example.com', disableDnsRecordCreation: true }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
        "listeners": z.array(z.object({
          "protocol": z.enum(["HTTP","HTTPS"]).describe("#### Listener protocol. `HTTPS` requires a TLS certificate (auto-created with `customDomains` or via `customCertificateArns`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    listeners: [\n      { protocol: 'HTTPS', port: 443 }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```"),
          "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Port this listener accepts traffic on (e.g., 443 for HTTPS, 80 for HTTP).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      listeners:\n        - protocol: HTTPS\n          port: 8443\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 8443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    listeners: [\n      {\n        protocol: 'HTTPS',\n        port: 8443\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 8443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```"),
          "customCertificateArns": z.array(z.string()).optional().describe("#### ARNs of your own ACM certificates. Not needed if using `customDomains` (Stacktape creates certs automatically).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      listeners:\n        - protocol: HTTPS\n          port: 443\n          customCertificateArns:\n            - arn:aws:acm:eu-west-1:123456789012:certificate/abcd1234-5678-90ef-ghij-klmnopqrstuv\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    listeners: [\n      {\n        protocol: 'HTTPS',\n        port: 443,\n        customCertificateArns: [\n          'arn:aws:acm:eu-west-1:123456789012:certificate/abcd1234-5678-90ef-ghij-klmnopqrstuv'\n        ]\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
          "whitelistIps": z.array(z.string()).optional().describe("#### Restrict access to specific IP addresses/CIDRs. Default: all IPs allowed.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      interface: internal\n      listeners:\n        - protocol: HTTP\n          port: 80\n          whitelistIps:\n            - 10.0.0.0/8\n            - 203.0.113.42/32\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 80\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    interface: 'internal',\n    listeners: [\n      {\n        protocol: 'HTTP',\n        port: 80,\n        whitelistIps: ['10.0.0.0/8', '203.0.113.42/32']\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 80,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
          "defaultAction": z.object({
            "type": z.literal("redirect").describe("#### The type of the default action.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n        - protocol: HTTP\n          port: 80\n          defaultAction:\n            type: redirect\n            properties:\n              protocol: HTTPS\n              port: 443\n              statusCode: HTTP_301\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    listeners: [\n      { protocol: 'HTTPS', port: 443 },\n      {\n        protocol: 'HTTP',\n        port: 80,\n        defaultAction: {\n          type: 'redirect',\n          properties: {\n            protocol: 'HTTPS',\n            port: 443,\n            statusCode: 'HTTP_301'\n          }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```"),
            "properties": z.object({
              "path": z.string().optional().describe("#### Redirect path (must start with `/`). Use `#{path}` to reuse the original path.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n          defaultAction:\n            type: redirect\n            properties:\n              path: /new/#{path}\n              statusCode: HTTP_301\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /new/*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'example.com' }],\n    listeners: [\n      {\n        protocol: 'HTTPS',\n        port: 443,\n        defaultAction: {\n          type: 'redirect',\n          properties: {\n            path: '/new/#{path}',\n            statusCode: 'HTTP_301'\n          }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/new/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
              "query": z.string().optional().describe("#### Query string for the redirect (without leading `?`). Use `#{query}` to preserve the original.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n          defaultAction:\n            type: redirect\n            properties:\n              query: '#{query}&source=redirect'\n              statusCode: HTTP_302\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'example.com' }],\n    listeners: [\n      {\n        protocol: 'HTTPS',\n        port: 443,\n        defaultAction: {\n          type: 'redirect',\n          properties: {\n            query: '#{query}&source=redirect',\n            statusCode: 'HTTP_302'\n          }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
              "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Redirect port (1-65535). Use `#{port}` to keep the original.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n        - protocol: HTTP\n          port: 80\n          defaultAction:\n            type: redirect\n            properties:\n              protocol: HTTPS\n              port: 443\n              statusCode: HTTP_301\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'example.com' }],\n    listeners: [\n      { protocol: 'HTTPS', port: 443 },\n      {\n        protocol: 'HTTP',\n        port: 80,\n        defaultAction: {\n          type: 'redirect',\n          properties: {\n            protocol: 'HTTPS',\n            port: 443,\n            statusCode: 'HTTP_301'\n          }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
              "host": z.string().optional().describe("#### Redirect hostname. Use `#{host}` to keep the original.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: old.example.com\n        - domainName: new.example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n          defaultAction:\n            type: redirect\n            properties:\n              host: new.example.com\n              statusCode: HTTP_301\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            hosts:\n              - new.example.com\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'old.example.com' }, { domainName: 'new.example.com' }],\n    listeners: [\n      {\n        protocol: 'HTTPS',\n        port: 443,\n        defaultAction: {\n          type: 'redirect',\n          properties: {\n            host: 'new.example.com',\n            statusCode: 'HTTP_301'\n          }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          hosts: ['new.example.com'],\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
              "protocol": z.enum(["HTTP","HTTPS"]).optional().describe("#### Redirect protocol. Cannot redirect HTTPS to HTTP.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n        - protocol: HTTP\n          port: 80\n          defaultAction:\n            type: redirect\n            properties:\n              protocol: HTTPS\n              port: 443\n              statusCode: HTTP_301\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'example.com' }],\n    listeners: [\n      { protocol: 'HTTPS', port: 443 },\n      {\n        protocol: 'HTTP',\n        port: 80,\n        defaultAction: {\n          type: 'redirect',\n          properties: {\n            protocol: 'HTTPS',\n            port: 443,\n            statusCode: 'HTTP_301'\n          }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
              "statusCode": z.enum(["HTTP_301","HTTP_302"]).describe("#### `HTTP_301` (permanent) or `HTTP_302` (temporary) redirect.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n        - protocol: HTTP\n          port: 80\n          defaultAction:\n            type: redirect\n            properties:\n              protocol: HTTPS\n              port: 443\n              statusCode: HTTP_301\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'example.com' }],\n    listeners: [\n      { protocol: 'HTTPS', port: 443 },\n      {\n        protocol: 'HTTP',\n        port: 80,\n        defaultAction: {\n          type: 'redirect',\n          properties: {\n            protocol: 'HTTPS',\n            port: 443,\n            statusCode: 'HTTP_301'\n          }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```") }).strict()
            .describe("#### Configures where to redirect the request.\n\n---\n\nA redirect changes the URI, which has the format: `protocol://hostname:port/path?query`.\nUnmodified URI components will retain their original values.\n\nTo avoid redirect loops, ensure that you sufficiently modify the URI.\nYou can reuse URI components with the following keywords: `#{protocol}`, `#{host}`, `#{port}`, `#{path}` (the leading `/` is removed), and `#{query}`.\n\nFor example, you can change the path to `/new/#{path}`, the hostname to `example.#{host}`, or the query to `#{query}&value=xyz`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: example.com\n        - domainName: www.example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n          defaultAction:\n            type: redirect\n            properties:\n              host: www.example.com\n              path: /#{path}\n              query: '#{query}'\n              statusCode: HTTP_301\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            hosts:\n              - www.example.com\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'example.com' }, { domainName: 'www.example.com' }],\n    listeners: [\n      {\n        protocol: 'HTTPS',\n        port: 443,\n        defaultAction: {\n          type: 'redirect',\n          properties: {\n            host: 'www.example.com',\n            path: '/#{path}',\n            query: '#{query}',\n            statusCode: 'HTTP_301'\n          }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          hosts: ['www.example.com'],\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```") }).strict()
          .optional().describe("#### Action for requests that don't match any integration. Currently supports `redirect` only.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n        - protocol: HTTP\n          port: 80\n          defaultAction:\n            type: redirect\n            properties:\n              protocol: HTTPS\n              port: 443\n              statusCode: HTTP_301\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    listeners: [\n      { protocol: 'HTTPS', port: 443 },\n      {\n        protocol: 'HTTP',\n        port: 80,\n        defaultAction: {\n          type: 'redirect',\n          properties: {\n            protocol: 'HTTPS',\n            port: 443,\n            statusCode: 'HTTP_301'\n          }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Custom listeners (port + protocol). Defaults to HTTPS on 443 + HTTP on 80 (redirecting to HTTPS).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      listeners:\n        - protocol: HTTPS\n          port: 443\n        - protocol: HTTP\n          port: 8080\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    listeners: [\n      { protocol: 'HTTPS', port: 443 },\n      { protocol: 'HTTP', port: 8080 }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
        "cdn": z.object({
          "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Listener port for CDN traffic. Only needed if using custom listeners.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      listeners:\n        - protocol: HTTPS\n          port: 8443\n      cdn:\n        enabled: true\n        listenerPort: 8443\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 8443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    listeners: [{ protocol: 'HTTPS', port: 8443 }],\n    cdn: {\n      enabled: true,\n      listenerPort: 8443\n    }\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 8443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
          "originDomainName": z.string().optional().describe("#### Explicit origin domain. Only needed if the ALB has no `customDomains` and uses `customCertificateArns`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      listeners:\n        - protocol: HTTPS\n          port: 443\n          customCertificateArns:\n            - arn:aws:acm:eu-west-1:123456789012:certificate/abcd1234-5678-90ef-ghij-klmnopqrstuv\n      cdn:\n        enabled: true\n        originDomainName: alb.internal.example.com\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    listeners: [\n      {\n        protocol: 'HTTPS',\n        port: 443,\n        customCertificateArns: [\n          'arn:aws:acm:eu-west-1:123456789012:certificate/abcd1234-5678-90ef-ghij-klmnopqrstuv'\n        ]\n      }\n    ],\n    cdn: {\n      enabled: true,\n      originDomainName: 'alb.internal.example.com'\n    }\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable CDN (CloudFront) for faster global delivery and lower bandwidth costs.\n\n---\n\nCaches responses at edge locations worldwide so users get content from the nearest server.\nThe CDN itself has no monthly fee — you pay per request (~$0.01/10k) and per GB transferred.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "cachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control how long and what gets cached at the CDN edge.\n\n---\n\nWhen the origin response has no `Cache-Control` header, defaults apply:\n- **Bucket origins**: cached for 6 months (or until invalidated on deploy).\n- **API Gateway / Load Balancer origins**: not cached.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400,\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "forwardingOptions": z.object({
            "customRequestHeaders": z.array(z.object({
              "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
              "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
            ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cookies": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "headers": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "queryString": z.object({
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control which headers, cookies, and query params are forwarded to your origin.\n\n---\n\nBy default, all headers/cookies/query params are forwarded. Use this to restrict\nwhat reaches your app (e.g., strip cookies for static content).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { none: true }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "routeRewrites": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `*.jpg`, `/docs/v2/*`). Wildcards supported.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
            "routePrefix": z.string().optional().describe("#### Prepend a path prefix to requests before forwarding to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routePrefix: /v2\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routePrefix: '/v2',\n        routeTo: { type: 'http-api-gateway', properties: { httpApiGatewayName: 'backendApi' } }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, api } };\n});\n```").optional(),
            "routeTo": z.union([z.object({
                "type": z.literal("application-load-balancer"),
                "properties": z.object({
                  "loadBalancerName": z.string().describe("#### Name of the `application-load-balancer` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```"),
                  "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Listener port on the load balancer. Only needed if using custom listeners.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              listenerPort: 8080\nappLb:\n  type: application-load-balancer\n  properties:\n    listeners:\n      - port: 8080\n        protocol: HTTP\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({\n  listeners: [{ port: 8080, protocol: 'HTTP' }]\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            listenerPort: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional(),
                  "originDomainName": z.string().optional().describe("#### Explicit origin domain. Only needed if the ALB has no `customDomains` and uses `customCertificateArns`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              originDomainName: app.internal.example.com\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            originDomainName: 'app.internal.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("http-api-gateway"),
                "properties": z.object({
                  "httpApiGatewayName": z.string().describe("#### Name of the `http-api-gateway` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnApi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst cdnApi = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routeTo: {\n          type: 'http-api-gateway',\n          properties: {\n            httpApiGatewayName: 'backendApi'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, cdnApi } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("function"),
                "properties": z.object({
                  "functionName": z.string().describe("#### Name of the `function` resource to route to. The function must have `url.enabled: true`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /render/*\n          routeTo:\n            type: function\n            properties:\n              functionName: renderer\nrenderer:\n  type: function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/render.ts\n    url:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst renderer = new LambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/render.ts' }),\n  url: { enabled: true }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/render/*',\n        routeTo: {\n          type: 'function',\n          properties: {\n            functionName: 'renderer'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { renderer, api } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("custom-origin"),
                "properties": z.object({
                  "domainName": z.string().describe("#### Domain name of the external origin (e.g., `api.example.com`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "protocol": z.enum(["HTTP","HTTPS"]).optional().describe("#### Protocol for connecting to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTPS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTPS'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port on the origin. Defaults to 443 for HTTPS, 80 for HTTP.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTP\n              port: 8080\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTP',\n            port: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("bucket"),
                "properties": z.object({
                  "bucketName": z.string().describe("#### Name of the `bucket` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
                  "disableUrlNormalization": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable clean URL normalization (e.g., `/about` → `/about.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n              disableUrlNormalization: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets',\n            disableUrlNormalization: true\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").default(false) }).strict()
              }).strict()
            ]).optional().describe("#### Where to send matching requests. Can be a bucket, API Gateway, load balancer, Lambda, or external domain.\n\n---\n\nIf not set, requests go to the default origin (the resource this CDN is attached to).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /static/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/static/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override caching behavior for requests matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          cachingOptions:\n            defaultTTL: 604800\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        cachingOptions: { defaultTTL: 604800 }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "forwardingOptions": z.object({
              "customRequestHeaders": z.array(z.object({
                "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
              ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override which headers, cookies, and query params are forwarded for this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          forwardingOptions:\n            cookies:\n              none: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        forwardingOptions: { cookies: { none: true } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "edgeFunctions": z.object({
              "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
              "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
              "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
              "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Run edge functions on requests/responses matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /private/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          edgeFunctions:\n            onRequest: authAtEdge\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/private/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        edgeFunctions: { onRequest: 'authAtEdge' }\n      }\n    ]\n  }\n});\nreturn { resources: { authAtEdge, assets, api } };\n});\n```").optional() }).strict()
          ).optional().describe("#### Route specific URL patterns to different origins (e.g., `/api/*` → Lambda, `/assets/*` → S3).\n\n---\n\nEvaluated in order; first match wins. Unmatched requests go to the default origin.\nEach route can have its own caching and forwarding settings.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
          "customDomains": z.array(z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          ).optional().describe("#### Custom domains (e.g., `cdn.example.com`). Stacktape auto-creates DNS records and TLS certificates.\n\n---\n\nYour domain must be added as a Route53 hosted zone in your AWS account first.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      customDomains:\n        - domainName: cdn.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    customDomains: [{ domainName: 'cdn.example.com' }]\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "edgeFunctions": z.object({
            "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
            "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
            "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
            "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Run edge functions on CDN requests/responses (URL rewrites, auth, A/B testing).\n\n---\n\n- `onRequest`: Before cache lookup — modify the request, add auth, or return early.\n- `onResponse`: Before returning to the client — modify headers, add cookies.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
          "cloudfrontPriceClass": z.enum(["PriceClass_100","PriceClass_200","PriceClass_All"]).optional().describe("#### Which regions the CDN serves from. Fewer regions = lower cost, but slower for distant users.\n\n---\n\n- **`PriceClass_100`**: North America + Europe. Cheapest option. Good if your users are in the US/EU.\n- **`PriceClass_200`**: Adds Asia, Middle East, Africa.\n- **`PriceClass_All`** (default): All regions worldwide, including South America and Oceania.\n\nThe CDN itself has no monthly base cost - you only pay per request and per GB transferred.\nThe price class controls which edge locations are used, and some regions cost more per request.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cloudfrontPriceClass: PriceClass_100\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cloudfrontPriceClass: 'PriceClass_100'\n  }\n});\nreturn { resources: { assets } };\n});\n```"),
          "defaultRoutePrefix": z.string().optional().describe("#### Prepend a path prefix to all requests forwarded to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded to the origin as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      defaultRoutePrefix: /v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    defaultRoutePrefix: '/v2'\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "errorDocument": z.string().optional().describe("#### Page to show for 404 errors (e.g., `/error.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      errorDocument: /error.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    errorDocument: '/error.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/404.html"),
          "indexDocument": z.string().optional().describe("#### Page served for requests to `/`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      indexDocument: /index.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    indexDocument: '/index.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/index.html"),
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    disableInvalidationAfterDeploy: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this CDN from common web exploits.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnFirewall:\n  type: web-app-firewall\n  properties:\n    scope: cdn\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      useFirewall: cdnFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cdnFirewall = new WebAppFirewall({ scope: 'cdn' });\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    useFirewall: 'cdnFirewall'\n  }\n});\nreturn { resources: { cdnFirewall, api } };\n});\n```").optional() }).strict()
        .optional().describe("#### Put a CDN (CloudFront) in front of this load balancer for caching and lower latency worldwide.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      cdn:\n        enabled: true\n        cloudfrontPriceClass: PriceClass_100\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    cdn: {\n      enabled: true,\n      cloudfrontPriceClass: 'PriceClass_100'\n    }\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
        "alarms": z.array(z.object({
          "trigger": z.union([z.object({
              "type": z.literal("application-load-balancer-custom"),
              "properties": z.object({
                "metric": z.enum(["ActiveConnectionCount","AnomalousHostCount","ClientTLSNegotiationErrorCount","ConsumedLCUs","DesyncMitigationMode_NonCompliant_Request_Count","DroppedInvalidHeaderRequestCount","ELBAuthError","ELBAuthFailure","ELBAuthLatency","ELBAuthRefreshTokenSuccess","ELBAuthSuccess","ELBAuthUserClaimsSizeExceeded","ForwardedInvalidHeaderRequestCount","GrpcRequestCount","HTTPCode_ELB_3XX_Count","HTTPCode_ELB_4XX_Count","HTTPCode_ELB_500_Count","HTTPCode_ELB_502_Count","HTTPCode_ELB_503_Count","HTTPCode_ELB_504_Count","HTTPCode_ELB_5XX_Count","HTTPCode_Target_2XX_Count","HTTPCode_Target_3XX_Count","HTTPCode_Target_4XX_Count","HTTPCode_Target_5XX_Count","HTTP_Fixed_Response_Count","HTTP_Redirect_Count","HTTP_Redirect_Url_Limit_Exceeded_Count","HealthyHostCount","HealthyStateDNS","HealthyStateRouting","IPv6ProcessedBytes","IPv6RequestCount","LambdaInternalError","LambdaTargetProcessedBytes","LambdaUserError","MitigatedHostCount","NewConnectionCount","NonStickyRequestCount","ProcessedBytes","RejectedConnectionCount","RequestCount","RequestCountPerTarget","RuleEvaluations","TargetConnectionErrorCount","TargetResponseTime","TargetTLSNegotiationErrorCount","UnHealthyHostCount","UnhealthyRoutingRequestCount","UnhealthyStateDNS","UnhealthyStateRouting"]).describe("#### The metric to monitor on the Load Balancer.\n\n---\n\nThe threshold will be compared against the calculated value of `statistic(METRIC)`, where:\n- `statistic` is the function applied to the metric values collected during the evaluation period (default: `avg`).\n- `METRIC` is the chosen metric.\n\n**Available Metrics:**\n\n- `ActiveConnectionCount`: The total number of concurrent TCP connections active from clients to the load balancer and from the load balancer to targets.\n- `AnomalousHostCount`: The number of hosts detected with anomalies.\n- `ClientTLSNegotiationErrorCount`: The number of TLS connections initiated by the client that did not establish a session with the load balancer due to a TLS error.\n- `ConsumedLCUs`: The number of load balancer capacity units (LCU) used by your load balancer.\n- `DesyncMitigationMode_NonCompliant_Request_Count`: The number of requests that do not comply with RFC 7230.\n- `DroppedInvalidHeaderRequestCount`: The number of requests where the load balancer removed HTTP headers with invalid fields before routing the request.\n- `MitigatedHostCount`: The number of targets under mitigation.\n- `ForwardedInvalidHeaderRequestCount`: The number of requests routed by the load balancer that had HTTP headers with invalid fields.\n- `GrpcRequestCount`: The number of gRPC requests processed over IPv4 and IPv6.\n- `HTTP_Fixed_Response_Count`: The number of successful fixed-response actions.\n- `HTTP_Redirect_Count`: The number of successful redirect actions.\n- `HTTP_Redirect_Url_Limit_Exceeded_Count`: The number of redirect actions that failed because the URL in the response location header exceeded 8K.\n- `HTTPCode_ELB_3XX_Count`: The number of HTTP 3XX redirection codes originating from the load balancer.\n- `HTTPCode_ELB_4XX_Count`: The number of HTTP 4XX client error codes originating from the load balancer.\n- `HTTPCode_ELB_5XX_Count`: The number of HTTP 5XX server error codes originating from the load balancer.\n- `HTTPCode_ELB_500_Count`: The number of HTTP 500 error codes originating from the load balancer.\n- `HTTPCode_ELB_502_Count`: The number of HTTP 502 error codes originating from the load balancer.\n- `HTTPCode_ELB_503_Count`: The number of HTTP 503 error codes originating from the load balancer.\n- `HTTPCode_ELB_504_Count`: The number of HTTP 504 error codes originating from the load balancer.\n- `IPv6ProcessedBytes`: The total number of bytes processed by the load balancer over IPv6.\n- `IPv6RequestCount`: The number of IPv6 requests received by the load balancer.\n- `NewConnectionCount`: The total number of new TCP connections established from clients to the load balancer and from the load balancer to targets.\n- `NonStickyRequestCount`: The number of requests where the load balancer chose a new target because it could not use an existing sticky session.\n- `ProcessedBytes`: The total number of bytes processed by the load balancer over IPv4 and IPv6.\n- `RejectedConnectionCount`: The number of connections rejected because the load balancer reached its maximum number of connections.\n- `RequestCount`: The number of requests processed over IPv4 and IPv6.\n- `RuleEvaluations`: The number of rules processed by the load balancer, averaged over an hour.\n- `HealthyHostCount`: The number of targets that are considered healthy.\n- `HTTPCode_Target_2XX_Count`: The number of HTTP 2XX response codes generated by the targets.\n- `HTTPCode_Target_3XX_Count`: The number of HTTP 3XX response codes generated by the targets.\n- `HTTPCode_Target_4XX_Count`: The number of HTTP 4XX response codes generated by the targets.\n- `HTTPCode_Target_5XX_Count`: The number of HTTP 5XX response codes generated by the targets.\n- `RequestCountPerTarget`: The average number of requests per target in a target group.\n- `TargetConnectionErrorCount`: The number of connections that were not successfully established between the load balancer and a target.\n- `TargetResponseTime`: The time elapsed (in seconds) from when a request leaves the load balancer until the target starts sending response headers.\n- `TargetTLSNegotiationErrorCount`: The number of TLS connections initiated by the load balancer that did not establish a session with the target.\n- `UnHealthyHostCount`: The number of targets that are considered unhealthy.\n- `HealthyStateDNS`: The number of zones that meet the DNS healthy state requirements.\n- `HealthyStateRouting`: The number of zones that meet the routing healthy state requirements.\n- `UnhealthyRoutingRequestCount`: The number of requests routed using the routing failover action (fail open).\n- `UnhealthyStateDNS`: The number of zones that do not meet the DNS healthy state requirements.\n- `UnhealthyStateRouting`: The number of zones that do not meet the routing healthy state requirements.\n- `LambdaInternalError`: The number of requests to a Lambda function that failed due to an issue internal to the load balancer or AWS Lambda.\n- `LambdaTargetProcessedBytes`: The total number of bytes processed by the load balancer for requests to and responses from a Lambda function.\n- `LambdaUserError`: The number of requests to a Lambda function that failed due to an issue with the Lambda function itself.\n- `ELBAuthError`: The number of user authentications that could not be completed due to an internal error.\n- `ELBAuthFailure`: The number of user authentications that could not be completed because the IdP denied access.\n- `ELBAuthLatency`: The time elapsed (in milliseconds) to query the IdP for the ID token and user info.\n- `ELBAuthRefreshTokenSuccess`: The number of times the load balancer successfully refreshed user claims using a refresh token.\n- `ELBAuthSuccess`: The number of successful authentication actions.\n- `ELBAuthUserClaimsSizeExceeded`: The number of times a configured IdP returned user claims that exceeded 11K bytes in size.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n  publicLb:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Too many 5xx errors returned by the load balancer\n          trigger:\n            type: application-load-balancer-custom\n            properties:\n              metric: HTTPCode_ELB_5XX_Count\n              threshold: 50\n              statistic: sum\n              comparisonOperator: GreaterThanThreshold\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, ApplicationLoadBalancer, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  const publicLb = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Too many 5xx errors returned by the load balancer',\n        trigger: {\n          type: 'application-load-balancer-custom',\n          properties: {\n            metric: 'HTTPCode_ELB_5XX_Count',\n            threshold: 50,\n            statistic: 'sum',\n            comparisonOperator: 'GreaterThanThreshold'\n          }\n        },\n        evaluation: { period: 60, evaluationPeriods: 5, breachedPeriods: 3 },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { webApi, publicLb } };\n});\n```"),
                "threshold": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The threshold that triggers the alarm.\n\n---\n\nThe threshold is compared against the calculated value of `statistic(METRIC)`, where:\n- `statistic` is the function applied to the metric values collected during the evaluation period (default: `avg`).\n- `METRIC` is the chosen metric.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n  publicLb:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Target response time is too high\n          trigger:\n            type: application-load-balancer-custom\n            properties:\n              metric: TargetResponseTime\n              threshold: 2\n              statistic: p95\n              comparisonOperator: GreaterThanThreshold\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, ApplicationLoadBalancer, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  const publicLb = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Target response time is too high',\n        trigger: {\n          type: 'application-load-balancer-custom',\n          properties: {\n            metric: 'TargetResponseTime',\n            threshold: 2,\n            statistic: 'p95',\n            comparisonOperator: 'GreaterThanThreshold'\n          }\n        },\n        evaluation: { period: 60, evaluationPeriods: 5, breachedPeriods: 3 },\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@example.com',\n              recipient: 'oncall@example.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { webApi, publicLb } };\n});\n```"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("application-load-balancer-error-rate"),
              "properties": z.object({
                "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when 4xx/5xx error rate exceeds this percentage.\n\n---\n\nExample: `5` fires the alarm if more than 5% of requests return errors.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  loadBalancer:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Load balancer error rate too high\n          trigger:\n            type: application-load-balancer-error-rate\n            properties:\n              thresholdPercent: 5\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const loadBalancer = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Load balancer error rate too high',\n        trigger: {\n          type: 'application-load-balancer-error-rate',\n          properties: {\n            thresholdPercent: 5\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { loadBalancer } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("application-load-balancer-unhealthy-targets"),
              "properties": z.object({
                "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when the percentage of unhealthy targets exceeds this value.\n\n---\n\nIf the load balancer has multiple target groups, the alarm fires if *any* group breaches the threshold.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  loadBalancer:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Too many unhealthy targets behind the load balancer\n          trigger:\n            type: application-load-balancer-unhealthy-targets\n            properties:\n              thresholdPercent: 50\n  apiService:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: api\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-org/api:latest\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: loadBalancer\n                priority: 1\n                containerPort: 3000\n                paths:\n                  - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const loadBalancer = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Too many unhealthy targets behind the load balancer',\n        trigger: {\n          type: 'application-load-balancer-unhealthy-targets',\n          properties: {\n            thresholdPercent: 50\n          }\n        }\n      }\n    ]\n  });\n\n  const apiService = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'api',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/api:latest' } },\n        events: [\n          {\n            type: 'application-load-balancer',\n            properties: {\n              loadBalancerName: 'loadBalancer',\n              priority: 1,\n              containerPort: 3000,\n              paths: ['*']\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { loadBalancer, apiService } };\n});\n```"),
                "onlyIncludeTargets": z.array(z.string()).optional().describe("#### Only monitor health of these target container services. If omitted, monitors all targets.\n\n---\n\nOnly services actually targeted by the load balancer can be listed.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  loadBalancer:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Critical service has unhealthy targets\n          trigger:\n            type: application-load-balancer-unhealthy-targets\n            properties:\n              thresholdPercent: 25\n              onlyIncludeTargets:\n                - apiService\n  apiService:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: api\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-org/api:latest\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: loadBalancer\n                priority: 1\n                containerPort: 3000\n                paths:\n                  - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const loadBalancer = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Critical service has unhealthy targets',\n        trigger: {\n          type: 'application-load-balancer-unhealthy-targets',\n          properties: {\n            thresholdPercent: 25,\n            onlyIncludeTargets: ['apiService']\n          }\n        }\n      }\n    ]\n  });\n\n  const apiService = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'api',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/api:latest' } },\n        events: [\n          {\n            type: 'application-load-balancer',\n            properties: {\n              loadBalancerName: 'loadBalancer',\n              priority: 1,\n              containerPort: 3000,\n              paths: ['*']\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { loadBalancer, apiService } };\n});\n```").optional(),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```") }).strict()
            }).strict()
          ]),
          "evaluation": z.object({
            "period": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Duration of one evaluation period in seconds. Must be a multiple of 60.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker running too long\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          evaluation:\n            period: 300\n            evaluationPeriods: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker running too long',\n        trigger: {\n          type: 'lambda-duration',\n          properties: { thresholdMilliseconds: 5000 }\n        },\n        evaluation: {\n          period: 300,\n          evaluationPeriods: 2\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default(60),
            "evaluationPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many recent periods to evaluate. Prevents alarms from firing on short spikes.\n\n---\n\nExample: set to `5` with `breachedPeriods: 3` — the alarm fires only if the threshold is breached\nin at least 3 of the last 5 periods.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/payments.ts\n      alarms:\n        - description: Payments error rate elevated\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/payments.ts' }),\n    alarms: [\n      {\n        description: 'Payments error rate elevated',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 1 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { paymentsFn } };\n});\n```").default(1),
            "breachedPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many periods (within `evaluationPeriods`) must breach the threshold to fire the alarm.\n\n---\n\nMust be ≤ `evaluationPeriods`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/checkout.ts\n      alarms:\n        - description: Checkout errors persisting across multiple periods\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 2\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/checkout.ts' }),\n    alarms: [\n      {\n        description: 'Checkout errors persisting across multiple periods',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { checkoutFn } };\n});\n```").default(1) }).strict()
          .optional().describe("#### How long and how often to evaluate the metric before triggering.\n\n---\n\nControls the evaluation window (period), how many periods to look at, and how many must breach\nthe threshold to fire the alarm. Useful for filtering out short spikes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/api.ts\n      memory: 512\n      timeout: 10\n      alarms:\n        - description: API error rate too high\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    alarms: [\n      {\n        description: 'API error rate too high',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFunction } };\n});\n```").optional(),
          "notificationTargets": z.array(z.union([z.object({
              "type": z.literal("slack"),
              "properties": z.object({
                "conversationId": z.string().describe("#### The Slack channel or DM ID to send notifications to.\n\n---\n\nTo find the ID: open the channel, click its name, and look at the bottom of the **About** tab.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```"),
                "accessToken": z.string().describe("#### Bot User OAuth Token for your Slack app. Store as `$Secret()` for security.\n\n---\n\nCreate a Slack app, add the `chat:write` scope, install it to your workspace, then copy the Bot User OAuth Token.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("ms-teams"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Incoming Webhook URL for the MS Teams channel. Store as `$Secret()` for security.\n\n---\n\nCreate an Incoming Webhook connector in your Teams channel settings to get this URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 1024\n      timeout: 15\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          description: API function is running slow\n          notificationTargets:\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('ms-teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/api.ts' }\n    },\n    memory: 1024,\n    timeout: 15,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 5000 } },\n        description: 'API function is running slow',\n        notificationTargets: [\n          {\n            type: 'ms-teams',\n            properties: {\n              webhookUrl: $Secret('ms-teams-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("email"),
              "properties": z.object({
                "sender": z.string().describe("#### The email address of the sender.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```"),
                "recipient": z.string().describe("#### The email address of the recipient.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("discord"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Discord Webhook URL for the channel. Store as `$Secret()` for security.\n\n---\n\nCreate a webhook in your Discord channel settings (Edit Channel → Integrations → Webhooks).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest.ts\n      memory: 256\n      timeout: 60\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 30000\n          description: Ingest job is taking too long\n          notificationTargets:\n            - type: discord\n              properties:\n                webhookUrl: $Secret('discord-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest.ts' }\n    },\n    memory: 256,\n    timeout: 60,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 30000 } },\n        description: 'Ingest job is taking too long',\n        notificationTargets: [\n          {\n            type: 'discord',\n            properties: {\n              webhookUrl: $Secret('discord-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { ingestFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("webhook"),
              "properties": z.object({
                "url": z.string().describe("#### The URL to send webhook POST requests to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```"),
                "secret": z.string().optional().describe("#### Optional signing secret for HMAC-SHA256 payload verification.\n\n---\n\nIf provided, each request includes an `X-Stacktape-Signature` header.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional(),
                "headers": z.record(z.string(), z.any()).optional().describe("#### Optional custom headers to include in each request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n                headers:\n                  X-Environment: production\n                  Authorization: $Secret('webhook-auth-header')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret'),\n              headers: {\n                'X-Environment': 'production',\n                Authorization: $Secret('webhook-auth-header')\n              }\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional() }).strict()
              .optional().describe("").optional() }).strict()
          ])).optional().describe("#### Where to send notifications when the alarm fires — Slack, MS Teams, or email.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API returning too many errors\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 2\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C12345678\n                accessToken: $Secret('slack-bot-token')\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API returning too many errors',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: { conversationId: 'C12345678', accessToken: $Secret('slack-bot-token') }\n          },\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          },\n          {\n            type: 'ms-teams',\n            properties: { webhookUrl: $Secret('teams-webhook-url') }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "includeInHistory": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether alarm state changes should appear in monitoring history.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Jobs queue backing up\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          includeInHistory: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Jobs queue backing up',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        includeInHistory: false\n      }\n    ]\n  });\n\n  return { resources: { jobsQueue } };\n});\n```").default(true),
          "description": z.string().optional().describe("#### Custom alarm description used in notification messages and the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 80\n          description: Database CPU sustained above 80% — consider scaling up\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: { thresholdPercent: 80 }\n        },\n        description: 'Database CPU sustained above 80% — consider scaling up'\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Alarms for this load balancer (merged with global alarms from the Stacktape Console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      alarms:\n        - trigger:\n            type: application-load-balancer-error-rate\n            properties:\n              thresholdPercent: 5\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n        - trigger:\n            type: application-load-balancer-unhealthy-targets\n            properties:\n              thresholdPercent: 20\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    alarms: [\n      {\n        trigger: {\n          type: 'application-load-balancer-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          }\n        ]\n      },\n      {\n        trigger: {\n          type: 'application-load-balancer-unhealthy-targets',\n          properties: { thresholdPercent: 20 }\n        }\n      }\n    ]\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
        "disabledGlobalAlarms": z.array(z.string()).optional().describe("#### Global alarm names to exclude from this load balancer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      disabledGlobalAlarms:\n        - high-error-rate\n        - unhealthy-targets\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    disabledGlobalAlarms: ['high-error-rate', 'unhealthy-targets']\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { webLoadBalancer, apiFunction } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this load balancer from common web exploits.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n  webLoadBalancer:\n    type: application-load-balancer\n    properties:\n      customDomains:\n        - domainName: api.example.com\n      useFirewall: apiFirewall\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: webLoadBalancer\n            priority: 1\n            paths:\n              - /*\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, LambdaFunction, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({ scope: 'regional' });\n\n  const webLoadBalancer = new ApplicationLoadBalancer({\n    customDomains: [{ domainName: 'api.example.com' }],\n    useFirewall: 'apiFirewall'\n  });\n\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'webLoadBalancer',\n          priority: 1,\n          paths: ['/*']\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFirewall, webLoadBalancer, apiFunction } };\n});\n```").optional() }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### HTTP/HTTPS load balancer with flat ~$18/month pricing. Required for WebSockets, firewalls, and gradual deployments.\n\n---\n\nRoutes requests to containers or Lambda functions based on path, host, headers, or query params.\nMore cost-effective than API Gateway above ~500k requests/day. AWS Free Tier eligible."), z.object({
      "type": z.literal("bucket"),
      "properties": z.object({
        "directoryUpload": z.object({
          "directoryPath": z.string().describe("#### Path to the local directory to upload (relative to project root).\n\n---\n\nThe bucket will mirror this directory exactly — existing files not in the directory are deleted.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./build\n      cdn:\n        enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './build'\n    },\n    cdn: {\n      enabled: true\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
          "fileOptions": z.array(z.object({
            "includePattern": z.string().describe("#### Glob pattern for files this rule applies to (e.g., `*.html`, `assets/**`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "excludePattern": z.string().optional().describe("#### Glob pattern for files to exclude even if they match `includePattern`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            excludePattern: 'assets/uncompressed/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          excludePattern: 'assets/uncompressed/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
            "headers": z.array(z.object({
              "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
              "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
            ).optional().describe("#### HTTP headers (e.g., `Cache-Control`) for matching files. Forwarded through CDN to the browser.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
            "tags": z.array(z.object({
              "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
              "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
            ).optional().describe("#### Tags for matching files. Can be used to target files with `lifecycleRules`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'reports/**'\n            tags:\n              - key: category\n                value: report\n      lifecycleRules:\n        - type: expiration\n          properties:\n            tags:\n              - key: category\n                value: report\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'reports/**',\n          tags: [{ key: 'category', value: 'report' }]\n        }\n      ]\n    },\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          tags: [{ key: 'category', value: 'report' }],\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
          ).optional().describe("#### Set HTTP headers or tags on files matching specific patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n      cdn:\n        enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    },\n    cdn: {\n      enabled: true\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "excludeFilesPatterns": z.array(z.string()).optional().describe("#### Glob patterns for files to skip during upload (relative to `directoryPath`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        excludeFilesPatterns:\n          - '**/*.map'\n          - '.DS_Store'\n      cdn:\n        enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      excludeFilesPatterns: ['**/*.map', '.DS_Store']\n    },\n    cdn: {\n      enabled: true\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "headersPreset": z.enum(["astro-static-website","gatsby-static-website","nuxt-static-website","single-page-app","static-website","sveltekit-static-website"]).optional().describe("#### Preset for HTTP caching headers, optimized for your frontend framework.\n\n---\n\n- **`single-page-app`**: HTML never cached, hashed assets cached forever. For React/Vue/Angular SPAs.\n- **`static-website`**: CDN-cached, never browser-cached. For generic static sites.\n- **`astro-static-website`** / **`sveltekit-static-website`** / **`nuxt-static-website`**: Framework-specific\n  caching (hashed build assets cached forever, HTML always fresh).\n\nOverride individual files with `fileOptions`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        headersPreset: single-page-app\n      cdn:\n        enabled: true\n        rewriteRoutesForSinglePageApp: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      headersPreset: 'single-page-app'\n    },\n    cdn: {\n      enabled: true,\n      rewriteRoutesForSinglePageApp: true\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "disableS3TransferAcceleration": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable faster uploads via S3 Transfer Acceleration. Saves a small per-GB cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        disableS3TransferAcceleration: true\n      cdn:\n        enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      disableS3TransferAcceleration: true\n    },\n    cdn: {\n      enabled: true\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").default(false) }).strict()
        .optional().describe("#### Sync a local directory to this bucket on every deploy.\n\n---\n\n> **Warning:** Replaces all existing bucket contents. Don't use for buckets\n> with user-uploaded or application-generated files.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n      cdn:\n        enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist'\n    },\n    cdn: {\n      enabled: true\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
        "accessibility": z.object({
          "accessibilityMode": z.enum(["private","public-read","public-read-write"]).describe("#### Configures pre-defined accessibility modes for the bucket.\n\n---\n\nThis allows you to easily configure the most common access patterns.\n\nAvailable modes:\n- `public-read-write`: Everyone can read from and write to the bucket.\n- `public-read`: Everyone can read from the bucket. Only compute resources and entities with sufficient IAM permissions can write to it.\n- `private` (default): Only compute resources and entities with sufficient IAM permissions can read from or write to the bucket.\n\nFor functions, batch jobs, and container workloads, you can grant the required IAM permissions using the `allowsAccessTo` or `iamRoleStatements` properties in their respective configurations.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      accessibility:\n        accessibilityMode: public-read\n      cors:\n        enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    accessibility: {\n      accessibilityMode: 'public-read'\n    },\n    cors: {\n      enabled: true\n    }\n  });\n  return { resources: { myBucket } };\n});\n```"),
          "accessPolicyStatements": z.array(z.object({
            "Principal": z.any(),
            "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
            "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions.").default("Allow"),
            "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
            "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
            "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
          ).optional().describe("#### Advanced access configuration that leverages IAM policy statements.\n\n---\n\nThis gives you fine-grained access control over the bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      accessibility:\n        accessibilityMode: private\n        accessPolicyStatements:\n          - Effect: Allow\n            Principal: '*'\n            Action:\n              - 's3:GetObject'\n            Resource:\n              - $Format('{}/public/*', $ResourceParam('myBucket', 'arn'))\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig, $ResourceParam, $CfFormat } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    accessibility: {\n      accessibilityMode: 'private',\n      accessPolicyStatements: [\n        {\n          Effect: 'Allow',\n          Principal: '*',\n          Action: ['s3:GetObject'],\n          Resource: [$CfFormat('{}/public/*', $ResourceParam('myBucket', 'arn'))]\n        }\n      ]\n    }\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
        .optional().describe("#### Who can read/write to this bucket: `private` (default), `public-read`, or `public-read-write`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      accessibility:\n        accessibilityMode: public-read\n      versioning: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    accessibility: {\n      accessibilityMode: 'public-read'\n    },\n    versioning: true\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
        "cors": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable CORS. When `true` with no rules, uses permissive defaults (`*` origins, all methods).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      cors:\n        enabled: true\n      accessibility:\n        accessibilityMode: public-read\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    cors: {\n      enabled: true\n    },\n    accessibility: {\n      accessibilityMode: 'public-read'\n    }\n  });\n  return { resources: { myBucket } };\n});\n```"),
          "corsRules": z.array(z.object({
            "allowedOrigins": z.array(z.string()).optional().describe("#### Allowed origins (e.g., `https://example.com`). Use `*` for any.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      cors:\n        enabled: true\n        corsRules:\n          - allowedOrigins:\n              - 'https://app.example.com'\n              - 'https://admin.example.com'\n            allowedMethods:\n              - GET\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    cors: {\n      enabled: true,\n      corsRules: [\n        {\n          allowedOrigins: ['https://app.example.com', 'https://admin.example.com'],\n          allowedMethods: ['GET']\n        }\n      ]\n    }\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
            "allowedHeaders": z.array(z.string()).optional().describe("#### Allowed request headers.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      cors:\n        enabled: true\n        corsRules:\n          - allowedOrigins:\n              - 'https://app.example.com'\n            allowedMethods:\n              - PUT\n            allowedHeaders:\n              - 'Content-Type'\n              - 'Authorization'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    cors: {\n      enabled: true,\n      corsRules: [\n        {\n          allowedOrigins: ['https://app.example.com'],\n          allowedMethods: ['PUT'],\n          allowedHeaders: ['Content-Type', 'Authorization']\n        }\n      ]\n    }\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
            "allowedMethods": z.array(z.enum(["*","DELETE","GET","HEAD","PATCH","POST","PUT"])).optional().describe("#### Allowed HTTP methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      cors:\n        enabled: true\n        corsRules:\n          - allowedOrigins:\n              - 'https://app.example.com'\n            allowedMethods:\n              - GET\n              - PUT\n              - POST\n              - DELETE\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    cors: {\n      enabled: true,\n      corsRules: [\n        {\n          allowedOrigins: ['https://app.example.com'],\n          allowedMethods: ['GET', 'PUT', 'POST', 'DELETE']\n        }\n      ]\n    }\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
            "exposedResponseHeaders": z.array(z.string()).optional().describe("#### Response headers accessible to browser JavaScript.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      cors:\n        enabled: true\n        corsRules:\n          - allowedOrigins:\n              - 'https://app.example.com'\n            allowedMethods:\n              - GET\n            exposedResponseHeaders:\n              - 'ETag'\n              - 'x-amz-request-id'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    cors: {\n      enabled: true,\n      corsRules: [\n        {\n          allowedOrigins: ['https://app.example.com'],\n          allowedMethods: ['GET'],\n          exposedResponseHeaders: ['ETag', 'x-amz-request-id']\n        }\n      ]\n    }\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
            "maxAge": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How long (seconds) browsers can cache preflight responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      cors:\n        enabled: true\n        corsRules:\n          - allowedOrigins:\n              - 'https://app.example.com'\n            allowedMethods:\n              - GET\n            maxAge: 3600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    cors: {\n      enabled: true,\n      corsRules: [\n        {\n          allowedOrigins: ['https://app.example.com'],\n          allowedMethods: ['GET'],\n          maxAge: 3600\n        }\n      ]\n    }\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
          ).optional().describe("#### Custom CORS rules. First matching rule wins for each preflight request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      cors:\n        enabled: true\n        corsRules:\n          - allowedOrigins:\n              - 'https://example.com'\n            allowedMethods:\n              - GET\n              - PUT\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    cors: {\n      enabled: true,\n      corsRules: [\n        {\n          allowedOrigins: ['https://example.com'],\n          allowedMethods: ['GET', 'PUT']\n        }\n      ]\n    }\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
        .optional().describe("#### CORS settings for browser-based access to the bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      cors:\n        enabled: true\n      accessibility:\n        accessibilityMode: public-read\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    cors: {\n      enabled: true\n    },\n    accessibility: {\n      accessibilityMode: 'public-read'\n    }\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
        "versioning": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Keep previous versions of overwritten/deleted objects. Helps recover from mistakes.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      versioning: true\n      encryption: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    versioning: true,\n    encryption: true\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
        "encryption": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Encrypt stored objects at rest (AES-256).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      encryption: true\n      versioning: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    encryption: true,\n    versioning: true\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
        "lifecycleRules": z.array(z.union([z.object({
            "type": z.literal("expiration"),
            "properties": z.object({
              "daysAfterUpload": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Delete objects this many days after upload.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: tmp/\n            daysAfterUpload: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'tmp/',\n          daysAfterUpload: 7\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
              "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
              "tags": z.array(z.object({
                "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
              ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
          }).strict()
          , z.object({
            "type": z.literal("non-current-version-expiration"),
            "properties": z.object({
              "daysAfterVersioned": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Delete old versions this many days after they become non-current. Requires `versioning: true`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      versioning: true\n      lifecycleRules:\n        - type: non-current-version-expiration\n          properties:\n            daysAfterVersioned: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    versioning: true,\n    lifecycleRules: [\n      {\n        type: 'non-current-version-expiration',\n        properties: {\n          daysAfterVersioned: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
              "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
              "tags": z.array(z.object({
                "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
              ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
          }).strict()
          , z.object({
            "type": z.literal("class-transition"),
            "properties": z.object({
              "daysAfterUpload": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Move objects to a cheaper storage class this many days after upload.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            prefix: archive/\n            daysAfterUpload: 90\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          prefix: 'archive/',\n          daysAfterUpload: 90,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
              "storageClass": z.enum(["DEEP_ARCHIVE","GLACIER","INTELLIGENT_TIERING","ONEZONE_IA","STANDARD_IA"]).describe("#### Target storage class. Cheaper classes have higher retrieval costs/latency.\n\n---\n\n- `STANDARD_IA` / `ONEZONE_IA`: Infrequent access, instant retrieval.\n- `INTELLIGENT_TIERING`: AWS auto-moves between tiers based on access patterns.\n- `GLACIER`: Archive, minutes to hours for retrieval.\n- `DEEP_ARCHIVE`: Cheapest, 12+ hours for retrieval.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            prefix: cold/\n            daysAfterUpload: 180\n            storageClass: DEEP_ARCHIVE\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          prefix: 'cold/',\n          daysAfterUpload: 180,\n          storageClass: 'DEEP_ARCHIVE'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
              "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
              "tags": z.array(z.object({
                "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
              ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
          }).strict()
          , z.object({
            "type": z.literal("non-current-version-class-transition"),
            "properties": z.object({
              "daysAfterVersioned": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Move old versions to a cheaper storage class this many days after becoming non-current.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      versioning: true\n      lifecycleRules:\n        - type: non-current-version-class-transition\n          properties:\n            daysAfterVersioned: 30\n            storageClass: STANDARD_IA\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    versioning: true,\n    lifecycleRules: [\n      {\n        type: 'non-current-version-class-transition',\n        properties: {\n          daysAfterVersioned: 30,\n          storageClass: 'STANDARD_IA'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
              "storageClass": z.enum(["DEEP_ARCHIVE","GLACIER","INTELLIGENT_TIERING","ONEZONE_IA","STANDARD_IA"]).describe("#### Target storage class for non-current versions.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      versioning: true\n      lifecycleRules:\n        - type: non-current-version-class-transition\n          properties:\n            daysAfterVersioned: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    versioning: true,\n    lifecycleRules: [\n      {\n        type: 'non-current-version-class-transition',\n        properties: {\n          daysAfterVersioned: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
              "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
              "tags": z.array(z.object({
                "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
              ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
          }).strict()
          , z.object({
            "type": z.literal("abort-incomplete-multipart-upload"),
            "properties": z.object({
              "daysAfterInitiation": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Clean up incomplete multipart uploads after this many days. Prevents storage waste.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: abort-incomplete-multipart-upload\n          properties:\n            daysAfterInitiation: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'abort-incomplete-multipart-upload',\n        properties: {\n          daysAfterInitiation: 7\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
              "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
              "tags": z.array(z.object({
                "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
              ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
          }).strict()
        ])).optional().describe("#### Auto-delete or move objects to cheaper storage classes over time.\n\n---\n\nUse to control storage costs: expire old files, archive infrequently accessed data,\nor clean up incomplete uploads.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            daysAfterUpload: 90\n      versioning: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          daysAfterUpload: 90\n        }\n      }\n    ],\n    versioning: true\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
        "enableEventBusNotifications": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Send events (object created, deleted, etc.) to EventBridge for event-driven workflows.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      enableEventBusNotifications: true\n      versioning: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    enableEventBusNotifications: true,\n    versioning: true\n  });\n  return { resources: { myBucket } };\n});\n```").default(false),
        "cdn": z.object({
          "rewriteRoutesForSinglePageApp": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Rewrites incoming requests to work for a single-page application.\n\n---\n\nThe routing works as follows:\n- If the path has an extension (e.g., `.css`, `.js`, `.png`), the request is not rewritten, and the appropriate file is returned.\n- If the path does not have an extension, the request is routed to `index.html`.\n\nThis allows single-page applications to handle routing on the client side.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        headersPreset: single-page-app\n      cdn:\n        enabled: true\n        rewriteRoutesForSinglePageApp: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      headersPreset: 'single-page-app'\n    },\n    cdn: {\n      enabled: true,\n      rewriteRoutesForSinglePageApp: true\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "disableUrlNormalization": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disables URL normalization.\n\n---\n\nURL normalization is enabled by default and is useful for serving HTML files from the bucket with clean URLs.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n      cdn:\n        enabled: true\n        disableUrlNormalization: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist'\n    },\n    cdn: {\n      enabled: true,\n      disableUrlNormalization: true\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").default(false),
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable CDN (CloudFront) for faster global delivery and lower bandwidth costs.\n\n---\n\nCaches responses at edge locations worldwide so users get content from the nearest server.\nThe CDN itself has no monthly fee — you pay per request (~$0.01/10k) and per GB transferred.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "cachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control how long and what gets cached at the CDN edge.\n\n---\n\nWhen the origin response has no `Cache-Control` header, defaults apply:\n- **Bucket origins**: cached for 6 months (or until invalidated on deploy).\n- **API Gateway / Load Balancer origins**: not cached.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400,\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "forwardingOptions": z.object({
            "customRequestHeaders": z.array(z.object({
              "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
              "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
            ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cookies": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "headers": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "queryString": z.object({
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control which headers, cookies, and query params are forwarded to your origin.\n\n---\n\nBy default, all headers/cookies/query params are forwarded. Use this to restrict\nwhat reaches your app (e.g., strip cookies for static content).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { none: true }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "routeRewrites": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `*.jpg`, `/docs/v2/*`). Wildcards supported.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
            "routePrefix": z.string().optional().describe("#### Prepend a path prefix to requests before forwarding to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routePrefix: /v2\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routePrefix: '/v2',\n        routeTo: { type: 'http-api-gateway', properties: { httpApiGatewayName: 'backendApi' } }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, api } };\n});\n```").optional(),
            "routeTo": z.union([z.object({
                "type": z.literal("application-load-balancer"),
                "properties": z.object({
                  "loadBalancerName": z.string().describe("#### Name of the `application-load-balancer` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```"),
                  "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Listener port on the load balancer. Only needed if using custom listeners.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              listenerPort: 8080\nappLb:\n  type: application-load-balancer\n  properties:\n    listeners:\n      - port: 8080\n        protocol: HTTP\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({\n  listeners: [{ port: 8080, protocol: 'HTTP' }]\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            listenerPort: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional(),
                  "originDomainName": z.string().optional().describe("#### Explicit origin domain. Only needed if the ALB has no `customDomains` and uses `customCertificateArns`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              originDomainName: app.internal.example.com\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            originDomainName: 'app.internal.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("http-api-gateway"),
                "properties": z.object({
                  "httpApiGatewayName": z.string().describe("#### Name of the `http-api-gateway` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnApi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst cdnApi = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routeTo: {\n          type: 'http-api-gateway',\n          properties: {\n            httpApiGatewayName: 'backendApi'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, cdnApi } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("function"),
                "properties": z.object({
                  "functionName": z.string().describe("#### Name of the `function` resource to route to. The function must have `url.enabled: true`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /render/*\n          routeTo:\n            type: function\n            properties:\n              functionName: renderer\nrenderer:\n  type: function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/render.ts\n    url:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst renderer = new LambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/render.ts' }),\n  url: { enabled: true }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/render/*',\n        routeTo: {\n          type: 'function',\n          properties: {\n            functionName: 'renderer'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { renderer, api } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("custom-origin"),
                "properties": z.object({
                  "domainName": z.string().describe("#### Domain name of the external origin (e.g., `api.example.com`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "protocol": z.enum(["HTTP","HTTPS"]).optional().describe("#### Protocol for connecting to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTPS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTPS'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port on the origin. Defaults to 443 for HTTPS, 80 for HTTP.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTP\n              port: 8080\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTP',\n            port: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("bucket"),
                "properties": z.object({
                  "bucketName": z.string().describe("#### Name of the `bucket` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
                  "disableUrlNormalization": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable clean URL normalization (e.g., `/about` → `/about.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n              disableUrlNormalization: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets',\n            disableUrlNormalization: true\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").default(false) }).strict()
              }).strict()
            ]).optional().describe("#### Where to send matching requests. Can be a bucket, API Gateway, load balancer, Lambda, or external domain.\n\n---\n\nIf not set, requests go to the default origin (the resource this CDN is attached to).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /static/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/static/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override caching behavior for requests matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          cachingOptions:\n            defaultTTL: 604800\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        cachingOptions: { defaultTTL: 604800 }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "forwardingOptions": z.object({
              "customRequestHeaders": z.array(z.object({
                "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
              ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override which headers, cookies, and query params are forwarded for this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          forwardingOptions:\n            cookies:\n              none: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        forwardingOptions: { cookies: { none: true } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "edgeFunctions": z.object({
              "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
              "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
              "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
              "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Run edge functions on requests/responses matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /private/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          edgeFunctions:\n            onRequest: authAtEdge\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/private/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        edgeFunctions: { onRequest: 'authAtEdge' }\n      }\n    ]\n  }\n});\nreturn { resources: { authAtEdge, assets, api } };\n});\n```").optional() }).strict()
          ).optional().describe("#### Route specific URL patterns to different origins (e.g., `/api/*` → Lambda, `/assets/*` → S3).\n\n---\n\nEvaluated in order; first match wins. Unmatched requests go to the default origin.\nEach route can have its own caching and forwarding settings.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
          "customDomains": z.array(z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          ).optional().describe("#### Custom domains (e.g., `cdn.example.com`). Stacktape auto-creates DNS records and TLS certificates.\n\n---\n\nYour domain must be added as a Route53 hosted zone in your AWS account first.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      customDomains:\n        - domainName: cdn.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    customDomains: [{ domainName: 'cdn.example.com' }]\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "edgeFunctions": z.object({
            "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
            "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
            "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
            "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Run edge functions on CDN requests/responses (URL rewrites, auth, A/B testing).\n\n---\n\n- `onRequest`: Before cache lookup — modify the request, add auth, or return early.\n- `onResponse`: Before returning to the client — modify headers, add cookies.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
          "cloudfrontPriceClass": z.enum(["PriceClass_100","PriceClass_200","PriceClass_All"]).optional().describe("#### Which regions the CDN serves from. Fewer regions = lower cost, but slower for distant users.\n\n---\n\n- **`PriceClass_100`**: North America + Europe. Cheapest option. Good if your users are in the US/EU.\n- **`PriceClass_200`**: Adds Asia, Middle East, Africa.\n- **`PriceClass_All`** (default): All regions worldwide, including South America and Oceania.\n\nThe CDN itself has no monthly base cost - you only pay per request and per GB transferred.\nThe price class controls which edge locations are used, and some regions cost more per request.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cloudfrontPriceClass: PriceClass_100\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cloudfrontPriceClass: 'PriceClass_100'\n  }\n});\nreturn { resources: { assets } };\n});\n```"),
          "defaultRoutePrefix": z.string().optional().describe("#### Prepend a path prefix to all requests forwarded to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded to the origin as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      defaultRoutePrefix: /v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    defaultRoutePrefix: '/v2'\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "errorDocument": z.string().optional().describe("#### Page to show for 404 errors (e.g., `/error.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      errorDocument: /error.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    errorDocument: '/error.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/404.html"),
          "indexDocument": z.string().optional().describe("#### Page served for requests to `/`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      indexDocument: /index.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    indexDocument: '/index.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/index.html"),
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    disableInvalidationAfterDeploy: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this CDN from common web exploits.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnFirewall:\n  type: web-app-firewall\n  properties:\n    scope: cdn\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      useFirewall: cdnFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cdnFirewall = new WebAppFirewall({ scope: 'cdn' });\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    useFirewall: 'cdnFirewall'\n  }\n});\nreturn { resources: { cdnFirewall, api } };\n});\n```").optional() }).strict()
        .optional().describe("#### Put a CDN (CloudFront) in front of this bucket for faster downloads and lower bandwidth costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      cdn:\n        enabled: true\n        cloudfrontPriceClass: PriceClass_100\n      directoryUpload:\n        directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    cdn: {\n      enabled: true,\n      cloudfrontPriceClass: 'PriceClass_100'\n    },\n    directoryUpload: {\n      directoryPath: './public'\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### S3 storage bucket for files, images, backups, or any binary data.\n\n---\n\nPay only for what you store and transfer. Highly durable (99.999999999%)."), z.object({
      "type": z.literal("user-auth-pool"),
      "properties": z.object({
        "allowOnlyAdminsToCreateAccount": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Restrict account creation to administrators\n\n---\n\nIf enabled, new users can't sign up themselves. Accounts must be created through an admin flow (for example from an internal admin tool or script),\nwhich helps prevent unwanted self-registrations.\n\nInternally this controls `AdminCreateUserConfig.AllowAdminCreateUserOnly` on the Cognito user pool\n([AWS::Cognito::UserPool](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-cognito-userpool)).\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  adminPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      allowOnlyAdminsToCreateAccount: true\n      inviteMessageConfig:\n        emailSubject: Your new account\n        emailMessage: 'Your username is {username} and temporary password is {####}.'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const adminPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    allowOnlyAdminsToCreateAccount: true,\n    inviteMessageConfig: {\n      emailSubject: 'Your new account',\n      emailMessage: 'Your username is {username} and temporary password is {####}.'\n    }\n  });\n  return { resources: { adminPool } };\n});\n```").default(false),
        "unusedAccountValidityDays": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Expire unused admin-created accounts\n\n---\n\nWhen an admin creates a user account, Cognito issues a temporary password. This setting controls how many days that temporary password\n(and the corresponding account) stays valid if the user never signs in.\n\nInternally this maps to `AdminCreateUserConfig.UnusedAccountValidityDays`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  adminPool:\n    type: user-auth-pool\n    properties:\n      allowOnlyAdminsToCreateAccount: true\n      unusedAccountValidityDays: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const adminPool = new UserAuthPool({\n    allowOnlyAdminsToCreateAccount: true,\n    unusedAccountValidityDays: 7\n  });\n  return { resources: { adminPool } };\n});\n```").default(31),
        "requireEmailVerification": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### (Reserved) Require verified emails\n\n---\n\nPresent for forward compatibility. Stacktape currently derives email / phone verification behavior from `userVerificationType`,\nnot from this flag directly.\n\nTo require email-based verification today, use `userVerificationType: 'email-link' | 'email-code'` instead.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      requireEmailVerification: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    requireEmailVerification: true\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "requirePhoneNumberVerification": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### (Reserved) Require verified phone numbers\n\n---\n\nPresent for forward compatibility. Stacktape currently derives email / phone verification behavior from `userVerificationType`,\nnot from this flag directly.\n\nTo require SMS-based verification today, use `userVerificationType: 'sms'`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: sms\n      requirePhoneNumberVerification: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'sms',\n    requirePhoneNumberVerification: true\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "enableHostedUi": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Enable the Cognito Hosted UI\n\n---\n\nTurns on Cognito's Hosted UI – a pre-built, hosted login and registration page – so you don't have to build your own auth screens.\nThis is useful when you want to get started quickly or keep authentication logic outside of your main app.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: my-company-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      logoutURLs:\n        - https://app.example.com/logout\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n        - email\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'my-company-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    logoutURLs: ['https://app.example.com/logout'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid', 'email']\n  });\n  return { resources: { userPool } };\n});\n```").default(false),
        "hostedUiDomainPrefix": z.string().optional().describe("#### Hosted UI domain prefix\n\n---\n\nSets the first part of your Hosted UI URL: `https://<prefix>.auth.<region>.amazoncognito.com`.\nPick something that matches your project or company name.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: my-company-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'my-company-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid']\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "hostedUiCSS": z.string().optional().describe("#### Custom CSS for the Hosted UI\n\n---\n\nLets you override the default Cognito Hosted UI styling with your own CSS (colors, fonts, layouts, etc.),\nso the login experience matches the rest of your application.\n\nBehind the scenes this is applied using the `AWS::Cognito::UserPoolUICustomizationAttachment` resource.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: my-company-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      hostedUiCSS: |\n        .label-customizable { font-weight: 600; }\n        .submitButton-customizable { background: #4f46e5; }\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'my-company-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    hostedUiCSS: '.label-customizable { font-weight: 600; }\\n.submitButton-customizable { background: #4f46e5; }'\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "hooks": z.object({
          "customMessage": z.string().optional().describe("#### Custom message hook\n\nTriggered whenever Cognito is about to send an email or SMS (sign‑up, verification, password reset, etc.).\nLets you fully customize message contents or dynamically choose language/branding.\n\nValue must be the ARN of a Lambda function configured to handle the \"Custom Message\" trigger.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  customMessageFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/custom-message.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        customMessage: $ResourceParam('customMessageFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const customMessageFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/custom-message.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      customMessage: $ResourceParam('customMessageFn', 'arn')\n    }\n  });\n  return { resources: { customMessageFn, userPool } };\n});\n```").optional(),
          "postAuthentication": z.string().optional().describe("#### Post-authentication hook\n\nRuns after a user has successfully authenticated. You can use this to record analytics, update last‑login timestamps,\nor block access based on additional checks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  postAuthenticationFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/post-authentication.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        postAuthentication: $ResourceParam('postAuthenticationFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const postAuthenticationFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/post-authentication.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      postAuthentication: $ResourceParam('postAuthenticationFn', 'arn')\n    }\n  });\n  return { resources: { postAuthenticationFn, userPool } };\n});\n```").optional(),
          "postConfirmation": z.string().optional().describe("#### Post-confirmation hook\n\nRuns right after a user confirms their account (for example via email link or admin confirmation).\nThis is often used to create user records in your own database or to provision resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  postConfirmationFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/post-confirmation.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        postConfirmation: $ResourceParam('postConfirmationFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const postConfirmationFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/post-confirmation.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      postConfirmation: $ResourceParam('postConfirmationFn', 'arn')\n    }\n  });\n  return { resources: { postConfirmationFn, userPool } };\n});\n```").optional(),
          "preAuthentication": z.string().optional().describe("#### Pre-authentication hook\n\nInvoked just before Cognito validates a user's credentials. You can use this to block sign‑in attempts\nbased on IP, device, or user state (for example, soft‑deleting an account).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  preAuthenticationFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/pre-authentication.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        preAuthentication: $ResourceParam('preAuthenticationFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const preAuthenticationFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/pre-authentication.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      preAuthentication: $ResourceParam('preAuthenticationFn', 'arn')\n    }\n  });\n  return { resources: { preAuthenticationFn, userPool } };\n});\n```").optional(),
          "preSignUp": z.string().optional().describe("#### Pre-sign-up hook\n\nCalled before a new user is created. Useful for validating input, auto‑confirming trusted users,\nor blocking sign‑ups that don't meet your business rules.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  preSignUpFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/pre-sign-up.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        preSignUp: $ResourceParam('preSignUpFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const preSignUpFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/pre-sign-up.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      preSignUp: $ResourceParam('preSignUpFn', 'arn')\n    }\n  });\n  return { resources: { preSignUpFn, userPool } };\n});\n```").optional(),
          "preTokenGeneration": z.string().optional().describe("#### Pre-token-generation hook\n\nRuns right before Cognito issues tokens. Lets you customize token claims (for example, adding roles or flags)\nbased on external systems or additional logic.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  preTokenGenerationFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/pre-token-generation.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        preTokenGeneration: $ResourceParam('preTokenGenerationFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const preTokenGenerationFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/pre-token-generation.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      preTokenGeneration: $ResourceParam('preTokenGenerationFn', 'arn')\n    }\n  });\n  return { resources: { preTokenGenerationFn, userPool } };\n});\n```").optional(),
          "userMigration": z.string().optional().describe("#### User migration hook\n\nLets you migrate users on‑the‑fly from another user store. When someone tries to sign in but doesn't exist in Cognito,\nthis trigger can look them up elsewhere, import them, and let the sign‑in continue.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userMigrationFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/user-migration.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        userMigration: $ResourceParam('userMigrationFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userMigrationFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/user-migration.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      userMigration: $ResourceParam('userMigrationFn', 'arn')\n    }\n  });\n  return { resources: { userMigrationFn, userPool } };\n});\n```").optional(),
          "createAuthChallenge": z.string().optional().describe("#### Create auth challenge hook\n\nPart of Cognito's custom auth flow. This trigger is used to generate a challenge (for example sending a custom OTP)\nafter `DefineAuthChallenge` decides a challenge is needed.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  createAuthChallengeFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/create-auth-challenge.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        createAuthChallenge: $ResourceParam('createAuthChallengeFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const createAuthChallengeFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/create-auth-challenge.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      createAuthChallenge: $ResourceParam('createAuthChallengeFn', 'arn')\n    }\n  });\n  return { resources: { createAuthChallengeFn, userPool } };\n});\n```").optional(),
          "defineAuthChallenge": z.string().optional().describe("#### Define auth challenge hook\n\nAlso part of the custom auth flow. It decides whether a user needs another challenge, has passed, or has failed,\nbased on previous challenges and responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  defineAuthChallengeFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/define-auth-challenge.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        defineAuthChallenge: $ResourceParam('defineAuthChallengeFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const defineAuthChallengeFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/define-auth-challenge.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      defineAuthChallenge: $ResourceParam('defineAuthChallengeFn', 'arn')\n    }\n  });\n  return { resources: { defineAuthChallengeFn, userPool } };\n});\n```").optional(),
          "verifyAuthChallengeResponse": z.string().optional().describe("#### Verify auth challenge response hook\n\nValidates the user's response to a custom challenge (for example, checking an OTP the user provides).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  verifyAuthChallengeResponseFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/verify-auth-challenge-response.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        verifyAuthChallengeResponse: $ResourceParam('verifyAuthChallengeResponseFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const verifyAuthChallengeResponseFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/verify-auth-challenge-response.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      verifyAuthChallengeResponse: $ResourceParam('verifyAuthChallengeResponseFn', 'arn')\n    }\n  });\n  return { resources: { verifyAuthChallengeResponseFn, userPool } };\n});\n```").optional() }).strict()
        .optional().describe("#### Lambda triggers for the user pool\n\n---\n\nConnects AWS Lambda functions to Cognito \"hooks\" (triggers) such as pre-sign-up, post-confirmation, or token generation.\nYou can use these to enforce additional validation, enrich user profiles, migrate users from another system, and more.\n\nInternally this maps to the Cognito user pool `LambdaConfig`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  postConfirmFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/post-confirmation.ts\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      hooks:\n        postConfirmation: $ResourceParam('postConfirmFn', 'arn')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, UserAuthPool, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const postConfirmFn = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/post-confirmation.ts' } }\n  });\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    hooks: {\n      postConfirmation: $ResourceParam('postConfirmFn', 'arn')\n    }\n  });\n  return { resources: { postConfirmFn, userPool } };\n});\n```").optional(),
        "emailConfiguration": z.object({
          "sesAddressArn": z.string().optional().describe("#### SES identity to send emails from\n\n---\n\nARN of an SES verified identity (email address or domain) that Cognito should use when sending emails.\nRequired when you want full control over sending (for example for MFA via `EMAIL_OTP`), because Cognito\nmust switch into `DEVELOPER` email sending mode.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      emailConfiguration:\n        sesAddressArn: arn:aws:ses:eu-west-1:123456789012:identity/example.com\n        from: no-reply@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    emailConfiguration: {\n      sesAddressArn: 'arn:aws:ses:eu-west-1:123456789012:identity/example.com',\n      from: 'no-reply@example.com'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "from": z.string().optional().describe("#### From address\n\n---\n\nThe email address that appears in the \"From\" field of messages sent by Cognito (if you're using SES).\nThis address must be verified in SES if you're sending through your own identity.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      emailConfiguration:\n        sesAddressArn: arn:aws:ses:eu-west-1:123456789012:identity/example.com\n        from: no-reply@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    emailConfiguration: {\n      sesAddressArn: 'arn:aws:ses:eu-west-1:123456789012:identity/example.com',\n      from: 'no-reply@example.com'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "replyToEmailAddress": z.string().optional().describe("#### Reply-to address\n\n---\n\nOptional address where replies to Cognito emails should be delivered.\nIf not set, replies go to the `from` address (or the default Cognito sender).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      emailConfiguration:\n        sesAddressArn: arn:aws:ses:eu-west-1:123456789012:identity/example.com\n        from: no-reply@example.com\n        replyToEmailAddress: support@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    emailConfiguration: {\n      sesAddressArn: 'arn:aws:ses:eu-west-1:123456789012:identity/example.com',\n      from: 'no-reply@example.com',\n      replyToEmailAddress: 'support@example.com'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional() }).strict()
        .optional().describe("#### Email delivery settings\n\n---\n\nControls how Cognito sends emails (verification messages, password reset codes, admin invitations, etc.).\nYou can either use Cognito's built-in email service or plug in your own SES identity for full control over the sender.\n\nThis config is used to build the Cognito `EmailConfiguration` block\n([AWS docs](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-cognito-userpool)).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      emailConfiguration:\n        sesAddressArn: arn:aws:ses:eu-west-1:123456789012:identity/example.com\n        from: no-reply@example.com\n        replyToEmailAddress: support@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    emailConfiguration: {\n      sesAddressArn: 'arn:aws:ses:eu-west-1:123456789012:identity/example.com',\n      from: 'no-reply@example.com',\n      replyToEmailAddress: 'support@example.com'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "inviteMessageConfig": z.object({
          "emailMessage": z.string().optional().describe("#### Invitation email body\n\n---\n\nThe text of the email sent when an administrator creates a new user.\nYou can reference placeholders like `{username}` and `{####}` (temporary password or code) in the message.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  adminPool:\n    type: user-auth-pool\n    properties:\n      allowOnlyAdminsToCreateAccount: true\n      inviteMessageConfig:\n        emailSubject: Welcome to Acme\n        emailMessage: 'Hi {username}, your temporary password is {####}.'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const adminPool = new UserAuthPool({\n    allowOnlyAdminsToCreateAccount: true,\n    inviteMessageConfig: {\n      emailSubject: 'Welcome to Acme',\n      emailMessage: 'Hi {username}, your temporary password is {####}.'\n    }\n  });\n  return { resources: { adminPool } };\n});\n```").optional(),
          "emailSubject": z.string().optional().describe("#### Invitation email subject\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  adminPool:\n    type: user-auth-pool\n    properties:\n      allowOnlyAdminsToCreateAccount: true\n      inviteMessageConfig:\n        emailSubject: Welcome to Acme\n        emailMessage: 'Hi {username}, your temporary password is {####}.'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const adminPool = new UserAuthPool({\n    allowOnlyAdminsToCreateAccount: true,\n    inviteMessageConfig: {\n      emailSubject: 'Welcome to Acme',\n      emailMessage: 'Hi {username}, your temporary password is {####}.'\n    }\n  });\n  return { resources: { adminPool } };\n});\n```").optional(),
          "smsMessage": z.string().optional().describe("#### Invitation SMS body\n\n---\n\nThe content of the SMS message sent when new users are created with a phone number.\nAs with email, you can include placeholders such as `{username}` and `{####}`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  adminPool:\n    type: user-auth-pool\n    properties:\n      allowOnlyAdminsToCreateAccount: true\n      allowPhoneNumberAsUserName: true\n      inviteMessageConfig:\n        emailSubject: Welcome to Acme\n        emailMessage: 'Hi {username}, your temporary password is {####}.'\n        smsMessage: 'Acme login: {username} / {####}'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const adminPool = new UserAuthPool({\n    allowOnlyAdminsToCreateAccount: true,\n    allowPhoneNumberAsUserName: true,\n    inviteMessageConfig: {\n      emailSubject: 'Welcome to Acme',\n      emailMessage: 'Hi {username}, your temporary password is {####}.',\n      smsMessage: 'Acme login: {username} / {####}'\n    }\n  });\n  return { resources: { adminPool } };\n});\n```").optional() }).strict()
        .optional().describe("#### Invite message overrides\n\n---\n\nCustomizes the contents of the \"invitation\" message that users receive when an administrator creates their account\n(for example, when sending a temporary password and sign-in instructions).\n\nIf you want to send custom emails through SES, you must also configure `emailConfiguration.sesAddressArn`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  adminPool:\n    type: user-auth-pool\n    properties:\n      allowOnlyAdminsToCreateAccount: true\n      inviteMessageConfig:\n        emailSubject: Welcome to Acme\n        emailMessage: 'Hi {username}, your temporary password is {####}.'\n        smsMessage: 'Acme login: {username} / {####}'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const adminPool = new UserAuthPool({\n    allowOnlyAdminsToCreateAccount: true,\n    inviteMessageConfig: {\n      emailSubject: 'Welcome to Acme',\n      emailMessage: 'Hi {username}, your temporary password is {####}.',\n      smsMessage: 'Acme login: {username} / {####}'\n    }\n  });\n  return { resources: { adminPool } };\n});\n```").optional(),
        "userVerificationType": z.enum(["email-code","email-link","none","sms"]).optional().describe("#### Verification strategy\n\n---\n\nChooses how new users prove that they own their contact information:\n\n- `email-link`: Cognito emails a clickable link.\n- `email-code`: Cognito emails a short numeric code.\n- `sms`: Cognito sends a code via SMS to the user's phone number.\n- `none`: Users aren't required to verify email or phone during sign-up.\n\nStacktape uses this value to configure `AutoVerifiedAttributes` and `VerificationMessageTemplate`\non the underlying Cognito user pool.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-link\n      allowEmailAsUserName: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-link',\n    allowEmailAsUserName: true\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "userVerificationMessageConfig": z.object({
          "emailMessageUsingCode": z.string().optional().describe("#### Email body when verifying with a code\n\nUsed when `userVerificationType` is `email-code`. The message typically contains a `{####}` placeholder\nthat Cognito replaces with a one‑time verification code.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      userVerificationMessageConfig:\n        emailSubjectUsingCode: Verify your Acme account\n        emailMessageUsingCode: 'Your verification code is {####}.'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    userVerificationMessageConfig: {\n      emailSubjectUsingCode: 'Verify your Acme account',\n      emailMessageUsingCode: 'Your verification code is {####}.'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "emailMessageUsingLink": z.string().optional().describe("#### Email body when verifying with a link\n\nUsed when `userVerificationType` is `email-link`. Cognito replaces special markers like `{##verify your email##}`\nwith a clickable URL.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-link\n      userVerificationMessageConfig:\n        emailSubjectUsingLink: Confirm your Acme account\n        emailMessageUsingLink: 'Please click {##verify your email##} to confirm.'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-link',\n    userVerificationMessageConfig: {\n      emailSubjectUsingLink: 'Confirm your Acme account',\n      emailMessageUsingLink: 'Please click {##verify your email##} to confirm.'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "emailSubjectUsingCode": z.string().optional().describe("#### Email subject when verifying with a code\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      userVerificationMessageConfig:\n        emailSubjectUsingCode: Verify your Acme account\n        emailMessageUsingCode: 'Your verification code is {####}.'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    userVerificationMessageConfig: {\n      emailSubjectUsingCode: 'Verify your Acme account',\n      emailMessageUsingCode: 'Your verification code is {####}.'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "emailSubjectUsingLink": z.string().optional().describe("#### Email subject when verifying with a link\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-link\n      userVerificationMessageConfig:\n        emailSubjectUsingLink: Confirm your Acme account\n        emailMessageUsingLink: 'Please click {##verify your email##} to confirm.'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-link',\n    userVerificationMessageConfig: {\n      emailSubjectUsingLink: 'Confirm your Acme account',\n      emailMessageUsingLink: 'Please click {##verify your email##} to confirm.'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "smsMessage": z.string().optional().describe("#### SMS verification message\n\n---\n\nText of the SMS Cognito sends when verifying a phone number (for example containing `{####}`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: sms\n      userVerificationMessageConfig:\n        smsMessage: 'Your Acme verification code is {####}.'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'sms',\n    userVerificationMessageConfig: {\n      smsMessage: 'Your Acme verification code is {####}.'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional() }).strict()
        .optional().describe("#### Verification message text\n\n---\n\nLets you customize the exact email and SMS texts that Cognito sends when asking users to verify their email / phone.\nFor example, you can change subjects, body text, or the message that contains the `{####}` verification code.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      userVerificationMessageConfig:\n        emailSubjectUsingCode: Verify your Acme account\n        emailMessageUsingCode: 'Your verification code is {####}.'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    userVerificationMessageConfig: {\n      emailSubjectUsingCode: 'Verify your Acme account',\n      emailMessageUsingCode: 'Your verification code is {####}.'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "mfaConfiguration": z.object({
          "status": z.enum(["OFF","ON","OPTIONAL"]).optional().describe("#### MFA requirement\n\n- `OFF`: MFA is completely disabled.\n- `ON`: All users must complete MFA during sign‑in.\n- `OPTIONAL`: Users can opt in to MFA; it's recommended but not strictly required.\n\nThis value configures the Cognito `MfaConfiguration` property.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      mfaConfiguration:\n        status: ON\n        enabledTypes:\n          - SOFTWARE_TOKEN\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    mfaConfiguration: {\n      status: 'ON',\n      enabledTypes: ['SOFTWARE_TOKEN']\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "enabledTypes": z.array(z.enum(["EMAIL_OTP","SMS","SOFTWARE_TOKEN"])).optional().describe("#### Enabled MFA factor types\n\n---\n\nChooses which MFA methods users can use:\n\n- `SMS`: One‑time codes are sent via text message. Requires an SNS role so Cognito can send SMS.\n- `SOFTWARE_TOKEN`: Time‑based one‑time codes from an authenticator app.\n- `EMAIL_OTP`: Codes are sent by email. AWS requires that you configure a developer email sending identity\n  (which Stacktape does when you provide `emailConfiguration.sesAddressArn`).\n\nThese values are mapped to Cognito's `EnabledMfas` setting (`SMS_MFA`, `SOFTWARE_TOKEN_MFA`, `EMAIL_OTP`),\nwhose behavior is described in\n[EnabledMfas in the AWS::Cognito::UserPool docs](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-cognito-userpool).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      mfaConfiguration:\n        status: OPTIONAL\n        enabledTypes:\n          - SOFTWARE_TOKEN\n          - SMS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    mfaConfiguration: {\n      status: 'OPTIONAL',\n      enabledTypes: ['SOFTWARE_TOKEN', 'SMS']\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional() }).strict()
        .optional().describe("#### Multi-factor authentication\n\n---\n\nControls whether you use Multi‑Factor Authentication (MFA) and which second factors are allowed.\nMFA makes it much harder for attackers to access accounts even if they know a user's password.\n\nUnder the hood this config drives both the `MfaConfiguration` and `EnabledMfas` properties in Cognito\n(see \"MFA configuration\" in the\n[AWS::Cognito::UserPool docs](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-cognito-userpool)).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      mfaConfiguration:\n        status: OPTIONAL\n        enabledTypes:\n          - SOFTWARE_TOKEN\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    mfaConfiguration: {\n      status: 'OPTIONAL',\n      enabledTypes: ['SOFTWARE_TOKEN']\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "passwordPolicy": z.object({
          "minimumLength": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum password length\n\nThe fewest characters a password can have. Longer passwords are generally more secure.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      passwordPolicy:\n        minimumLength: 12\n        requireNumbers: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    passwordPolicy: {\n      minimumLength: 12,\n      requireNumbers: true\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "requireLowercase": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Require at least one lowercase letter\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      passwordPolicy:\n        minimumLength: 12\n        requireLowercase: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    passwordPolicy: {\n      minimumLength: 12,\n      requireLowercase: true\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "requireNumbers": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Require at least one number\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      passwordPolicy:\n        minimumLength: 12\n        requireNumbers: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    passwordPolicy: {\n      minimumLength: 12,\n      requireNumbers: true\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "requireSymbols": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Require at least one symbol\n\nSymbols are non‑alphanumeric characters such as `!`, `@`, or `#`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      passwordPolicy:\n        minimumLength: 12\n        requireSymbols: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    passwordPolicy: {\n      minimumLength: 12,\n      requireSymbols: true\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "requireUppercase": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Require at least one uppercase letter\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      passwordPolicy:\n        minimumLength: 12\n        requireUppercase: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    passwordPolicy: {\n      minimumLength: 12,\n      requireUppercase: true\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "temporaryPasswordValidityDays": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Temporary password validity (days)\n\nHow long a temporary password issued to a new user is valid before it must be changed on first sign‑in.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      passwordPolicy:\n        minimumLength: 12\n        temporaryPasswordValidityDays: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    passwordPolicy: {\n      minimumLength: 12,\n      temporaryPasswordValidityDays: 7\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional() }).strict()
        .optional().describe("#### Password strength rules\n\n---\n\nDefines how strong user passwords must be – minimum length and whether they must include lowercase, uppercase,\nnumbers, and/or symbols – plus how long temporary passwords issued to new users remain valid.\n\nThis is applied to the Cognito `Policies.PasswordPolicy` block.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      passwordPolicy:\n        minimumLength: 12\n        requireLowercase: true\n        requireUppercase: true\n        requireNumbers: true\n        requireSymbols: true\n        temporaryPasswordValidityDays: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    passwordPolicy: {\n      minimumLength: 12,\n      requireLowercase: true,\n      requireUppercase: true,\n      requireNumbers: true,\n      requireSymbols: true,\n      temporaryPasswordValidityDays: 7\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "schema": z.array(z.object({
          "name": z.string().optional().describe("#### Attribute name\n\nThe logical name of the attribute as it appears on the user (for example `given_name`, `plan`, or `tenantId`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: plan\n          attributeDataType: String\n          mutable: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'plan',\n        attributeDataType: 'String',\n        mutable: true\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "attributeDataType": z.string().optional().describe("#### Attribute data type\n\nThe value type stored for this attribute (`String`, `Number`, etc.).\nThis is passed to Cognito's `AttributeDataType`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: plan\n          attributeDataType: String\n          mutable: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'plan',\n        attributeDataType: 'String',\n        mutable: true\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "developerOnlyAttribute": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Developer-only attribute\n\nIf true, the attribute is only readable/writable by privileged backend code and not exposed to end users directly.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: plan\n          attributeDataType: String\n          developerOnlyAttribute: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'plan',\n        attributeDataType: 'String',\n        developerOnlyAttribute: true\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "mutable": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Mutable after sign-up\n\nControls whether the attribute can be changed after it has been initially set.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: plan\n          attributeDataType: String\n          mutable: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'plan',\n        attributeDataType: 'String',\n        mutable: true\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "required": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Required at sign-up\n\nIf true, users must supply this attribute when creating an account.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: plan\n          attributeDataType: String\n          required: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'plan',\n        attributeDataType: 'String',\n        required: false\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "numberMaxValue": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum numeric value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: seats\n          attributeDataType: Number\n          mutable: true\n          numberMaxValue: 1000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'seats',\n        attributeDataType: 'Number',\n        mutable: true,\n        numberMaxValue: 1000\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "numberMinValue": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum numeric value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: seats\n          attributeDataType: Number\n          mutable: true\n          numberMinValue: 1\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'seats',\n        attributeDataType: 'Number',\n        mutable: true,\n        numberMinValue: 1\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "stringMaxLength": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum string length\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: plan\n          attributeDataType: String\n          stringMaxLength: 128\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'plan',\n        attributeDataType: 'String',\n        stringMaxLength: 128\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "stringMinLength": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum string length\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: plan\n          attributeDataType: String\n          stringMinLength: 1\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'plan',\n        attributeDataType: 'String',\n        stringMinLength: 1\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Custom attributes schema\n\n---\n\nLets you define additional attributes (like `role`, `plan`, `companyId`, etc.) that are stored on each user,\nincluding their data type and validation constraints.\n\nThese translate into the Cognito user pool `Schema` entries\n([schema docs](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-cognito-userpool)).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      schema:\n        - name: plan\n          attributeDataType: String\n          mutable: true\n          stringMinLength: 1\n          stringMaxLength: 32\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    schema: [\n      {\n        name: 'plan',\n        attributeDataType: 'String',\n        mutable: true,\n        stringMinLength: 1,\n        stringMaxLength: 32\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "allowPhoneNumberAsUserName": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Allow phone numbers as usernames\n\n---\n\nIf enabled (the default), users can sign in using their phone number in addition to any traditional username.\nTurning this off means phone numbers can still be stored, but can't be used to log in.\n\nThis is implemented via Cognito's `UsernameAttributes` configuration.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      allowPhoneNumberAsUserName: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    allowPhoneNumberAsUserName: false\n  });\n  return { resources: { userPool } };\n});\n```").default(true),
        "allowEmailAsUserName": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Allow email addresses as usernames\n\n---\n\nIf enabled (the default), users can sign in using their email address instead of a dedicated username.\nTurning this off means emails can still be stored, but can't be used to log in directly.\n\nThis is also controlled through Cognito `UsernameAttributes`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      allowEmailAsUserName: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    allowEmailAsUserName: true\n  });\n  return { resources: { userPool } };\n});\n```").default(true),
        "accessTokenValiditySeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Access token lifetime\n\n---\n\nControls how long an access token issued by Cognito stays valid after login. Shorter lifetimes reduce the window\nin which a stolen token can be abused, at the cost of more frequent refreshes.\n\nThis value is passed to the user pool client as `AccessTokenValidity`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      accessTokenValiditySeconds: 3600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    accessTokenValiditySeconds: 3600\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "idTokenValiditySeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### ID token lifetime\n\n---\n\nControls how long an ID token (which contains user profile and claims) is accepted before clients must obtain a new one.\n\nThis is set on the user pool client as `IdTokenValidity`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      idTokenValiditySeconds: 3600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    idTokenValiditySeconds: 3600\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "refreshTokenValidityDays": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Refresh token lifetime\n\n---\n\nSets for how many days a refresh token can be used to obtain new access / ID tokens without requiring the user to sign in again.\nLonger lifetimes mean fewer re-authentications, but keep sessions alive for longer.\n\nThis value is used as `RefreshTokenValidity` on the Cognito user pool client.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      refreshTokenValidityDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    refreshTokenValidityDays: 30\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "allowedOAuthFlows": z.array(z.enum(["client_credentials","code","implicit"])).optional().describe("#### OAuth flows\n\n---\n\nSpecifies which OAuth 2.0 flows the user pool client is allowed to use:\n\n- `code`: Authorization Code flow (recommended for web apps and backends).\n- `implicit`: Implicit flow (legacy browser-only flow).\n- `client_credentials`: Server‑to‑server (no end user) machine credentials.\n\nThese values populate `AllowedOAuthFlows` on the Cognito user pool client\n([AWS::Cognito::UserPoolClient](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-cognito-userpoolclient)).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthScopes:\n        - openid\n        - email\n      allowedOAuthFlows:\n        - code\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthScopes: ['openid', 'email'],\n    allowedOAuthFlows: ['code']\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "allowedOAuthScopes": z.array(z.string()).optional().describe("#### OAuth scopes\n\n---\n\nLists which scopes clients can request when using OAuth (for example `email`, `openid`, `profile`).\nScopes control which user information and permissions your app receives in tokens.\n\nThese values are passed to the user pool client as `AllowedOAuthScopes`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n        - email\n        - profile\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid', 'email', 'profile']\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "callbackURLs": z.array(z.string()).optional().describe("#### OAuth callback URLs\n\n---\n\nThe allowed URLs where Cognito is permitted to redirect users after successful authentication.\nThese must exactly match the URLs registered with your frontend / backend, otherwise the redirect will fail.\n\nMapped into `CallbackURLs` and `DefaultRedirectURI` on the user pool client.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      callbackURLs:\n        - https://app.example.com/callback\n        - http://localhost:3000/callback\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    callbackURLs: ['https://app.example.com/callback', 'http://localhost:3000/callback']\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "logoutURLs": z.array(z.string()).optional().describe("#### OAuth logout URLs\n\n---\n\nThe URLs Cognito can redirect users to after they log out of the Hosted UI or end their session.\nMust also be explicitly configured so that sign-out redirects don't fail.\n\nThese populate the `LogoutURLs` list on the user pool client.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      logoutURLs:\n        - https://app.example.com/logout\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    logoutURLs: ['https://app.example.com/logout']\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "identityProviders": z.array(z.object({
          "type": z.enum(["Facebook","Google","LoginWithAmazon","OIDC","SAML","SignInWithApple"]).describe("#### Provider type\n\n---\n\nThe kind of external identity provider you want to integrate:\n\n- `Facebook`, `Google`, `LoginWithAmazon`, `SignInWithApple`: social identity providers.\n- `OIDC`: a generic OpenID Connect provider.\n- `SAML`: a SAML 2.0 identity provider (often used for enterprise SSO).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      identityProviders:\n        -\n          type: Facebook\n          clientId: my-facebook-app-id\n          clientSecret: $Secret('facebook-oauth.client-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    identityProviders: [\n      {\n        type: 'Facebook',\n        clientId: 'my-facebook-app-id',\n        clientSecret: $Secret('facebook-oauth.client-secret')\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```"),
          "clientId": z.string().describe("#### OAuth / OIDC client ID\n\n---\n\nThe client ID (sometimes called app ID) that you obtained from the external provider's console.\nCognito presents this ID when redirecting users to the provider.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      identityProviders:\n        - type: Google\n          clientId: my-google-client-id.apps.googleusercontent.com\n          clientSecret: $Secret('google-oauth.client-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    identityProviders: [\n      {\n        type: 'Google',\n        clientId: 'my-google-client-id.apps.googleusercontent.com',\n        clientSecret: $Secret('google-oauth.client-secret')\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```"),
          "clientSecret": z.string().describe("#### OAuth / OIDC client secret\n\n---\n\nThe client secret associated with the `clientId`, used by Cognito when exchanging authorization codes for tokens.\nThis value should be kept confidential and only configured from secure sources.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      identityProviders:\n        - type: Google\n          clientId: my-google-client-id.apps.googleusercontent.com\n          clientSecret: $Secret('google-oauth.client-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    identityProviders: [\n      {\n        type: 'Google',\n        clientId: 'my-google-client-id.apps.googleusercontent.com',\n        clientSecret: $Secret('google-oauth.client-secret')\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```"),
          "attributeMapping": z.record(z.string(), z.string()).optional().describe("#### Attribute mapping\n\n---\n\nMaps attributes from the external provider (for example `email`, `given_name`) to Cognito user pool attributes.\nKeys are Cognito attribute names, values are attribute names from the identity provider.\n\nIf not provided, Stacktape defaults to mapping `email -> email`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      identityProviders:\n        - type: Google\n          clientId: my-google-client-id.apps.googleusercontent.com\n          clientSecret: $Secret('google-oauth.client-secret')\n          attributeMapping:\n            email: email\n            given_name: given_name\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    identityProviders: [\n      {\n        type: 'Google',\n        clientId: 'my-google-client-id.apps.googleusercontent.com',\n        clientSecret: $Secret('google-oauth.client-secret'),\n        attributeMapping: {\n          email: 'email',\n          given_name: 'given_name'\n        }\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "authorizeScopes": z.array(z.string()).optional().describe("#### Requested scopes\n\n---\n\nAdditional OAuth scopes to request from the identity provider (for example `openid`, `email`, `profile`).\nThese control which pieces of user information and permissions your app receives in the provider's tokens.\n\nIf omitted, Stacktape uses a reasonable default per provider (see\n[AWS::Cognito::UserPoolIdentityProvider](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-cognito-userpoolidentityprovider)).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      identityProviders:\n        - type: Google\n          clientId: my-google-client-id.apps.googleusercontent.com\n          clientSecret: $Secret('google-oauth.client-secret')\n          authorizeScopes:\n            - openid\n            - email\n            - profile\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    identityProviders: [\n      {\n        type: 'Google',\n        clientId: 'my-google-client-id.apps.googleusercontent.com',\n        clientSecret: $Secret('google-oauth.client-secret'),\n        authorizeScopes: ['openid', 'email', 'profile']\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "providerDetails": z.record(z.string(), z.any()).optional().describe("#### Advanced provider options\n\n---\n\nLow‑level configuration passed directly into Cognito's `ProviderDetails` map.\nYou can use this to override endpoints or supply provider‑specific keys as documented by AWS,\nfor example `authorize_url`, `token_url`, `attributes_request_method`, `oidc_issuer`, and others.\n\nIn most cases you don't need to set this – Stacktape configures sensible defaults for common providers.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      identityProviders:\n        - type: OIDC\n          clientId: my-oidc-client-id\n          clientSecret: $Secret('oidc-provider.client-secret')\n          authorizeScopes:\n            - openid\n            - email\n          providerDetails:\n            oidc_issuer: https://idp.example.com\n            attributes_request_method: GET\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    identityProviders: [\n      {\n        type: 'OIDC',\n        clientId: 'my-oidc-client-id',\n        clientSecret: $Secret('oidc-provider.client-secret'),\n        authorizeScopes: ['openid', 'email'],\n        providerDetails: {\n          oidc_issuer: 'https://idp.example.com',\n          attributes_request_method: 'GET'\n        }\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional() }).strict()
        ).optional().describe("#### External identity providers\n\n---\n\nAllows users to sign in with third‑party identity providers like Google, Facebook, Login with Amazon, OIDC, SAML, or Sign in with Apple.\nEach entry configures one external provider (client ID/secret, attribute mapping, requested scopes, and advanced provider‑specific options).\n\nUnder the hood Stacktape creates separate `AWS::Cognito::UserPoolIdentityProvider` resources and registers them\nin the user pool client's `SupportedIdentityProviders`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n        - email\n      identityProviders:\n        - type: Google\n          clientId: my-google-client-id.apps.googleusercontent.com\n          clientSecret: $Secret('google-oauth.client-secret')\n          authorizeScopes:\n            - openid\n            - email\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid', 'email'],\n    identityProviders: [\n      {\n        type: 'Google',\n        clientId: 'my-google-client-id.apps.googleusercontent.com',\n        clientSecret: $Secret('google-oauth.client-secret'),\n        authorizeScopes: ['openid', 'email']\n      }\n    ]\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Associate a WAF\n\n---\n\nLinks the user pool to a `web-app-firewall` resource, so requests to the Hosted UI and token endpoints are inspected\nby AWS WAF rules you configure in Stacktape.\n\nStacktape does this by creating a `WebACLAssociation` between the user pool and the referenced firewall.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 10\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      useFirewall: authFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 10\n        }\n      }\n    ]\n  });\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    useFirewall: 'authFirewall'\n  });\n  return { resources: { authFirewall, userPool } };\n});\n```").optional(),
        "customDomain": z.object({
          "domainName": z.string().describe("#### Domain Name\n\n---\n\nFully qualified domain name for the Cognito Hosted UI (e.g., `auth.example.com`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      customDomain:\n        domainName: auth.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    customDomain: {\n      domainName: 'auth.example.com'\n    }\n  });\n  return { resources: { userPool } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Custom Certificate ARN\n\n---\n\nARN of an ACM certificate in **us-east-1** to use for this domain.\nBy default, Stacktape uses the certificate associated with your domain in us-east-1.\n\nThe certificate must be in us-east-1 because Cognito uses CloudFront for custom domains.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      customDomain:\n        domainName: auth.example.com\n        customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-5678-90ab-cdef-1234567890ab\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    customDomain: {\n      domainName: 'auth.example.com',\n      customCertificateArn: 'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-5678-90ab-cdef-1234567890ab'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable DNS Record Creation\n\n---\n\nIf `true`, Stacktape will not create a DNS record for this domain.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      customDomain:\n        domainName: auth.example.com\n        disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    customDomain: {\n      domainName: 'auth.example.com',\n      disableDnsRecordCreation: true\n    }\n  });\n  return { resources: { userPool } };\n});\n```").default(false) }).strict()
        .optional().describe("#### Custom Domain\n\n---\n\nConfigures a custom domain for the Cognito Hosted UI (e.g., `auth.example.com`).\n\nWhen configured, Cognito creates a CloudFront distribution to serve your custom domain.\nStacktape automatically:\n- Configures the user pool domain with your custom domain and an ACM certificate from us-east-1\n- Creates a DNS record pointing to the CloudFront distribution\n\nThe domain must be registered and verified in your Stacktape account with a valid ACM certificate in us-east-1.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n      customDomain:\n        domainName: auth.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid'],\n    customDomain: {\n      domainName: 'auth.example.com'\n    }\n  });\n  return { resources: { userPool } };\n});\n```").optional(),
        "generateClientSecret": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Generate a client secret\n\n---\n\nAsks Cognito to generate a secret for the user pool client. Use this when you have trusted backends (like APIs or server‑side apps)\nthat can safely store a client secret and use confidential OAuth flows.\n\nThis flag controls the `GenerateSecret` property on the user pool client.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n      generateClientSecret: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    userVerificationType: 'email-code',\n    generateClientSecret: true\n  });\n  return { resources: { userPool } };\n});\n```").default(false),
        "allowOnlyExternalIdentityProviders": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Force external identity providers\n\n---\n\nIf `true`, users can't sign in with a username/password against the Cognito user directory at all.\nInstead, they must always use one of the configured external identity providers (Google, SAML, etc.).\n\nInternally this removes `COGNITO` from `SupportedIdentityProviders` on the user pool client.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  userPool:\n    type: user-auth-pool\n    properties:\n      enableHostedUi: true\n      hostedUiDomainPrefix: acme-auth\n      callbackURLs:\n        - https://app.example.com/callback\n      allowedOAuthFlows:\n        - code\n      allowedOAuthScopes:\n        - openid\n        - email\n      identityProviders:\n        - type: Google\n          clientId: my-google-client-id.apps.googleusercontent.com\n          clientSecret: $Secret('google-oauth.client-secret')\n      allowOnlyExternalIdentityProviders: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const userPool = new UserAuthPool({\n    enableHostedUi: true,\n    hostedUiDomainPrefix: 'acme-auth',\n    callbackURLs: ['https://app.example.com/callback'],\n    allowedOAuthFlows: ['code'],\n    allowedOAuthScopes: ['openid', 'email'],\n    identityProviders: [\n      {\n        type: 'Google',\n        clientId: 'my-google-client-id.apps.googleusercontent.com',\n        clientSecret: $Secret('google-oauth.client-secret')\n      }\n    ],\n    allowOnlyExternalIdentityProviders: true\n  });\n  return { resources: { userPool } };\n});\n```").default(false) }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### A resource for managing user authentication and authorization.\n\n---\n\nA user pool is a fully managed identity provider that handles user sign-up, sign-in, and access control.\nIt provides a secure and scalable way to manage user identities for your applications."), z.object({
      "type": z.literal("http-api-gateway"),
      "properties": z.object({
        "payloadFormat": z.enum(["1.0","2.0"]).optional().describe("#### Lambda event payload format. `2.0` is simpler and recommended for new projects.\n\n---\n\nOnly used if not overridden at the integration level.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      payloadFormat: '2.0'\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    payloadFormat: '2.0'\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
        "cors": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable CORS. With no other options, uses permissive defaults (`*` origins, common headers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").default(false),
          "allowedOrigins": z.array(z.string()).optional().describe("#### Allowed origins (e.g., `https://myapp.com`). Use `*` for any origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n          - https://admin.example.com\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com', 'https://admin.example.com']\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").default(["*"]),
          "allowedHeaders": z.array(z.string()).optional().describe("#### Allowed request headers in CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        allowedHeaders:\n          - Content-Type\n          - Authorization\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      allowedHeaders: ['Content-Type', 'Authorization']\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
          "allowedMethods": z.array(z.enum(["*","DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### Allowed HTTP methods. Auto-detected from integrations if not set.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        allowedMethods:\n          - GET\n          - POST\n          - OPTIONS\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      allowedMethods: ['GET', 'POST', 'OPTIONS']\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
          "allowCredentials": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Allow cookies/auth headers in cross-origin requests.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        allowCredentials: true\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      allowCredentials: true\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
          "exposedResponseHeaders": z.array(z.string()).optional().describe("#### Response headers accessible to browser JavaScript.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        exposedResponseHeaders:\n          - X-Request-Id\n          - X-Total-Count\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      exposedResponseHeaders: ['X-Request-Id', 'X-Total-Count']\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
          "maxAge": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How long (seconds) browsers can cache preflight responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        maxAge: 3600\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      maxAge: 3600\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional() }).strict()
        .optional().describe("#### CORS settings. Overrides any CORS headers from your application code.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://myapp.com\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://myapp.com']\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
        "logging": z.object({
          "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable access logging.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      logging:\n        disabled: true\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    logging: {\n      disabled: true\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").default(false),
          "format": z.enum(["CLF","CSV","JSON","XML"]).optional().describe("#### Log format. Logs include: requestId, IP, method, status, protocol, responseLength.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      logging:\n        format: CLF\n        retentionDays: 30\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    logging: {\n      format: 'CLF',\n      retentionDays: 30\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```"),
          "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      logging:\n        format: JSON\n        retentionDays: 365\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    logging: {\n      format: 'JSON',\n      retentionDays: 365\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").default(30),
          "logForwarding": z.union([z.object({
              "type": z.literal("http-endpoint"),
              "properties": z.object({
                "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("highlight"),
              "properties": z.object({
                "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("datadog"),
              "properties": z.object({
                "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
            }).strict()
          ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Access logging (request ID, IP, method, status, etc.). Viewable with `stacktape logs`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      logging:\n        format: JSON\n        retentionDays: 90\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    logging: {\n      format: 'JSON',\n      retentionDays: 90\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Custom domains (e.g., `api.example.com`). Stacktape auto-creates DNS records and TLS certificates.\n\n---\n\nYour domain must be added as a Route53 hosted zone in your AWS account first.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      customDomains:\n        - domainName: api.example.com\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    customDomains: [{ domainName: 'api.example.com' }]\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
        "cdn": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable CDN (CloudFront) for faster global delivery and lower bandwidth costs.\n\n---\n\nCaches responses at edge locations worldwide so users get content from the nearest server.\nThe CDN itself has no monthly fee — you pay per request (~$0.01/10k) and per GB transferred.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "cachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control how long and what gets cached at the CDN edge.\n\n---\n\nWhen the origin response has no `Cache-Control` header, defaults apply:\n- **Bucket origins**: cached for 6 months (or until invalidated on deploy).\n- **API Gateway / Load Balancer origins**: not cached.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400,\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "forwardingOptions": z.object({
            "customRequestHeaders": z.array(z.object({
              "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
              "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
            ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cookies": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "headers": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "queryString": z.object({
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control which headers, cookies, and query params are forwarded to your origin.\n\n---\n\nBy default, all headers/cookies/query params are forwarded. Use this to restrict\nwhat reaches your app (e.g., strip cookies for static content).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { none: true }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "routeRewrites": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `*.jpg`, `/docs/v2/*`). Wildcards supported.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
            "routePrefix": z.string().optional().describe("#### Prepend a path prefix to requests before forwarding to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routePrefix: /v2\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routePrefix: '/v2',\n        routeTo: { type: 'http-api-gateway', properties: { httpApiGatewayName: 'backendApi' } }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, api } };\n});\n```").optional(),
            "routeTo": z.union([z.object({
                "type": z.literal("application-load-balancer"),
                "properties": z.object({
                  "loadBalancerName": z.string().describe("#### Name of the `application-load-balancer` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```"),
                  "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Listener port on the load balancer. Only needed if using custom listeners.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              listenerPort: 8080\nappLb:\n  type: application-load-balancer\n  properties:\n    listeners:\n      - port: 8080\n        protocol: HTTP\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({\n  listeners: [{ port: 8080, protocol: 'HTTP' }]\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            listenerPort: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional(),
                  "originDomainName": z.string().optional().describe("#### Explicit origin domain. Only needed if the ALB has no `customDomains` and uses `customCertificateArns`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              originDomainName: app.internal.example.com\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            originDomainName: 'app.internal.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("http-api-gateway"),
                "properties": z.object({
                  "httpApiGatewayName": z.string().describe("#### Name of the `http-api-gateway` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnApi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst cdnApi = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routeTo: {\n          type: 'http-api-gateway',\n          properties: {\n            httpApiGatewayName: 'backendApi'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, cdnApi } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("function"),
                "properties": z.object({
                  "functionName": z.string().describe("#### Name of the `function` resource to route to. The function must have `url.enabled: true`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /render/*\n          routeTo:\n            type: function\n            properties:\n              functionName: renderer\nrenderer:\n  type: function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/render.ts\n    url:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst renderer = new LambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/render.ts' }),\n  url: { enabled: true }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/render/*',\n        routeTo: {\n          type: 'function',\n          properties: {\n            functionName: 'renderer'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { renderer, api } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("custom-origin"),
                "properties": z.object({
                  "domainName": z.string().describe("#### Domain name of the external origin (e.g., `api.example.com`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "protocol": z.enum(["HTTP","HTTPS"]).optional().describe("#### Protocol for connecting to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTPS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTPS'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port on the origin. Defaults to 443 for HTTPS, 80 for HTTP.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTP\n              port: 8080\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTP',\n            port: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("bucket"),
                "properties": z.object({
                  "bucketName": z.string().describe("#### Name of the `bucket` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
                  "disableUrlNormalization": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable clean URL normalization (e.g., `/about` → `/about.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n              disableUrlNormalization: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets',\n            disableUrlNormalization: true\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").default(false) }).strict()
              }).strict()
            ]).optional().describe("#### Where to send matching requests. Can be a bucket, API Gateway, load balancer, Lambda, or external domain.\n\n---\n\nIf not set, requests go to the default origin (the resource this CDN is attached to).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /static/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/static/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override caching behavior for requests matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          cachingOptions:\n            defaultTTL: 604800\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        cachingOptions: { defaultTTL: 604800 }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "forwardingOptions": z.object({
              "customRequestHeaders": z.array(z.object({
                "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
              ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override which headers, cookies, and query params are forwarded for this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          forwardingOptions:\n            cookies:\n              none: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        forwardingOptions: { cookies: { none: true } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "edgeFunctions": z.object({
              "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
              "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
              "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
              "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Run edge functions on requests/responses matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /private/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          edgeFunctions:\n            onRequest: authAtEdge\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/private/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        edgeFunctions: { onRequest: 'authAtEdge' }\n      }\n    ]\n  }\n});\nreturn { resources: { authAtEdge, assets, api } };\n});\n```").optional() }).strict()
          ).optional().describe("#### Route specific URL patterns to different origins (e.g., `/api/*` → Lambda, `/assets/*` → S3).\n\n---\n\nEvaluated in order; first match wins. Unmatched requests go to the default origin.\nEach route can have its own caching and forwarding settings.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
          "customDomains": z.array(z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          ).optional().describe("#### Custom domains (e.g., `cdn.example.com`). Stacktape auto-creates DNS records and TLS certificates.\n\n---\n\nYour domain must be added as a Route53 hosted zone in your AWS account first.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      customDomains:\n        - domainName: cdn.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    customDomains: [{ domainName: 'cdn.example.com' }]\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "edgeFunctions": z.object({
            "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
            "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
            "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
            "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Run edge functions on CDN requests/responses (URL rewrites, auth, A/B testing).\n\n---\n\n- `onRequest`: Before cache lookup — modify the request, add auth, or return early.\n- `onResponse`: Before returning to the client — modify headers, add cookies.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
          "cloudfrontPriceClass": z.enum(["PriceClass_100","PriceClass_200","PriceClass_All"]).optional().describe("#### Which regions the CDN serves from. Fewer regions = lower cost, but slower for distant users.\n\n---\n\n- **`PriceClass_100`**: North America + Europe. Cheapest option. Good if your users are in the US/EU.\n- **`PriceClass_200`**: Adds Asia, Middle East, Africa.\n- **`PriceClass_All`** (default): All regions worldwide, including South America and Oceania.\n\nThe CDN itself has no monthly base cost - you only pay per request and per GB transferred.\nThe price class controls which edge locations are used, and some regions cost more per request.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cloudfrontPriceClass: PriceClass_100\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cloudfrontPriceClass: 'PriceClass_100'\n  }\n});\nreturn { resources: { assets } };\n});\n```"),
          "defaultRoutePrefix": z.string().optional().describe("#### Prepend a path prefix to all requests forwarded to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded to the origin as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      defaultRoutePrefix: /v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    defaultRoutePrefix: '/v2'\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "errorDocument": z.string().optional().describe("#### Page to show for 404 errors (e.g., `/error.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      errorDocument: /error.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    errorDocument: '/error.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/404.html"),
          "indexDocument": z.string().optional().describe("#### Page served for requests to `/`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      indexDocument: /index.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    indexDocument: '/index.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/index.html"),
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    disableInvalidationAfterDeploy: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this CDN from common web exploits.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnFirewall:\n  type: web-app-firewall\n  properties:\n    scope: cdn\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      useFirewall: cdnFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cdnFirewall = new WebAppFirewall({ scope: 'cdn' });\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    useFirewall: 'cdnFirewall'\n  }\n});\nreturn { resources: { cdnFirewall, api } };\n});\n```").optional() }).strict()
        .optional().describe("#### Put a CDN (CloudFront) in front of this API for caching and lower latency worldwide.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cdn:\n        enabled: true\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cdn: {\n      enabled: true\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
        "alarms": z.array(z.object({
          "trigger": z.union([z.object({
              "type": z.literal("http-api-gateway-error-rate"),
              "properties": z.object({
                "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when 4xx/5xx error rate exceeds this percentage.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API Gateway error rate too high\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API Gateway error rate too high',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: {\n            thresholdPercent: 3\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("http-api-gateway-latency"),
              "properties": z.object({
                "thresholdMilliseconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when request-to-response latency exceeds this value (ms).\n\n---\n\nDefault: fires if **average** latency > threshold. Customize with `statistic` and `comparisonOperator`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API latency above SLA\n          trigger:\n            type: http-api-gateway-latency\n            properties:\n              thresholdMilliseconds: 1000\n              statistic: p95\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API latency above SLA',\n        trigger: {\n          type: 'http-api-gateway-latency',\n          properties: {\n            thresholdMilliseconds: 1000,\n            statistic: 'p95'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
            }).strict()
          ]),
          "evaluation": z.object({
            "period": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Duration of one evaluation period in seconds. Must be a multiple of 60.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker running too long\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          evaluation:\n            period: 300\n            evaluationPeriods: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker running too long',\n        trigger: {\n          type: 'lambda-duration',\n          properties: { thresholdMilliseconds: 5000 }\n        },\n        evaluation: {\n          period: 300,\n          evaluationPeriods: 2\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default(60),
            "evaluationPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many recent periods to evaluate. Prevents alarms from firing on short spikes.\n\n---\n\nExample: set to `5` with `breachedPeriods: 3` — the alarm fires only if the threshold is breached\nin at least 3 of the last 5 periods.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/payments.ts\n      alarms:\n        - description: Payments error rate elevated\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/payments.ts' }),\n    alarms: [\n      {\n        description: 'Payments error rate elevated',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 1 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { paymentsFn } };\n});\n```").default(1),
            "breachedPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many periods (within `evaluationPeriods`) must breach the threshold to fire the alarm.\n\n---\n\nMust be ≤ `evaluationPeriods`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/checkout.ts\n      alarms:\n        - description: Checkout errors persisting across multiple periods\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 2\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/checkout.ts' }),\n    alarms: [\n      {\n        description: 'Checkout errors persisting across multiple periods',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { checkoutFn } };\n});\n```").default(1) }).strict()
          .optional().describe("#### How long and how often to evaluate the metric before triggering.\n\n---\n\nControls the evaluation window (period), how many periods to look at, and how many must breach\nthe threshold to fire the alarm. Useful for filtering out short spikes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/api.ts\n      memory: 512\n      timeout: 10\n      alarms:\n        - description: API error rate too high\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    alarms: [\n      {\n        description: 'API error rate too high',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFunction } };\n});\n```").optional(),
          "notificationTargets": z.array(z.union([z.object({
              "type": z.literal("slack"),
              "properties": z.object({
                "conversationId": z.string().describe("#### The Slack channel or DM ID to send notifications to.\n\n---\n\nTo find the ID: open the channel, click its name, and look at the bottom of the **About** tab.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```"),
                "accessToken": z.string().describe("#### Bot User OAuth Token for your Slack app. Store as `$Secret()` for security.\n\n---\n\nCreate a Slack app, add the `chat:write` scope, install it to your workspace, then copy the Bot User OAuth Token.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("ms-teams"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Incoming Webhook URL for the MS Teams channel. Store as `$Secret()` for security.\n\n---\n\nCreate an Incoming Webhook connector in your Teams channel settings to get this URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 1024\n      timeout: 15\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          description: API function is running slow\n          notificationTargets:\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('ms-teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/api.ts' }\n    },\n    memory: 1024,\n    timeout: 15,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 5000 } },\n        description: 'API function is running slow',\n        notificationTargets: [\n          {\n            type: 'ms-teams',\n            properties: {\n              webhookUrl: $Secret('ms-teams-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("email"),
              "properties": z.object({
                "sender": z.string().describe("#### The email address of the sender.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```"),
                "recipient": z.string().describe("#### The email address of the recipient.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("discord"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Discord Webhook URL for the channel. Store as `$Secret()` for security.\n\n---\n\nCreate a webhook in your Discord channel settings (Edit Channel → Integrations → Webhooks).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest.ts\n      memory: 256\n      timeout: 60\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 30000\n          description: Ingest job is taking too long\n          notificationTargets:\n            - type: discord\n              properties:\n                webhookUrl: $Secret('discord-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest.ts' }\n    },\n    memory: 256,\n    timeout: 60,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 30000 } },\n        description: 'Ingest job is taking too long',\n        notificationTargets: [\n          {\n            type: 'discord',\n            properties: {\n              webhookUrl: $Secret('discord-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { ingestFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("webhook"),
              "properties": z.object({
                "url": z.string().describe("#### The URL to send webhook POST requests to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```"),
                "secret": z.string().optional().describe("#### Optional signing secret for HMAC-SHA256 payload verification.\n\n---\n\nIf provided, each request includes an `X-Stacktape-Signature` header.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional(),
                "headers": z.record(z.string(), z.any()).optional().describe("#### Optional custom headers to include in each request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n                headers:\n                  X-Environment: production\n                  Authorization: $Secret('webhook-auth-header')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret'),\n              headers: {\n                'X-Environment': 'production',\n                Authorization: $Secret('webhook-auth-header')\n              }\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional() }).strict()
              .optional().describe("").optional() }).strict()
          ])).optional().describe("#### Where to send notifications when the alarm fires — Slack, MS Teams, or email.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API returning too many errors\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 2\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C12345678\n                accessToken: $Secret('slack-bot-token')\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API returning too many errors',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: { conversationId: 'C12345678', accessToken: $Secret('slack-bot-token') }\n          },\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          },\n          {\n            type: 'ms-teams',\n            properties: { webhookUrl: $Secret('teams-webhook-url') }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "includeInHistory": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether alarm state changes should appear in monitoring history.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Jobs queue backing up\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          includeInHistory: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Jobs queue backing up',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        includeInHistory: false\n      }\n    ]\n  });\n\n  return { resources: { jobsQueue } };\n});\n```").default(true),
          "description": z.string().optional().describe("#### Custom alarm description used in notification messages and the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 80\n          description: Database CPU sustained above 80% — consider scaling up\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: { thresholdPercent: 80 }\n        },\n        description: 'Database CPU sustained above 80% — consider scaling up'\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Alarms for this API Gateway (merged with global alarms from the Stacktape Console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 5\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: ops@example.com\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    alarms: [\n      {\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        notificationTargets: [\n          { type: 'email', properties: { sender: 'alerts@example.com', recipient: 'ops@example.com' } }\n        ]\n      }\n    ]\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
        "disabledGlobalAlarms": z.array(z.string()).optional().describe("#### Global alarm names to exclude from this API Gateway.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      disabledGlobalAlarms:\n        - global-api-latency\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    disabledGlobalAlarms: ['global-api-latency']\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional() }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Serverless HTTP API with pay-per-request pricing (~$1/million requests).\n\n---\n\nRoutes HTTP requests to Lambda functions, containers, or other backends.\nNo servers to manage. Includes built-in throttling, CORS, and TLS."), z.object({
      "type": z.literal("function"),
      "properties": z.object({
        "packaging": z.union([z.object({
            "type": z.literal("stacktape-lambda-buildpack"),
            "properties": z.object({
              "handlerFunction": z.string().optional().describe("#### The name of the handler function to be executed when the Lambda is invoked.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         handlerFunction: handler\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       handlerFunction: 'handler'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
              "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "languageSpecificConfig": z.union([z.object({
                  "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                  "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                  "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                , z.object({
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                  "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                  "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                , z.object({
                  "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                , z.record(z.string(), z.any()), z.object({
                  "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                , z.object({
                  "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                , z.object({
                  "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                  "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
              ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### A zero-config buildpack that packages your code for AWS Lambda.\n\n---\n\nThe `stacktape-lambda-buildpack` automatically bundles your code and dependencies into an optimized Lambda deployment package.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, Go, Ruby, PHP, and .NET.\n\nFor JS/TS, your code is bundled into a single file. Source maps are automatically generated.\nPackages are cached based on a checksum, so unchanged code is not re-packaged."), z.object({
            "type": z.literal("custom-artifact"),
            "properties": z.object({
              "packagePath": z.string().describe("#### The path to a pre-built deployment package.\n\n---\n\nIf the path points to a directory or a non-zip file, Stacktape will automatically zip it for you.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: custom-artifact\n       properties:\n         packagePath: ./dist/lambda.zip\n         handler: index.js:handler\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'custom-artifact',\n     properties: {\n       packagePath: './dist/lambda.zip',\n       handler: 'index.js:handler'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```"),
              "handler": z.string().optional().describe("#### The handler function to be executed when the Lambda is invoked.\n\n---\n\nThe syntax is `{{filepath}}:{{functionName}}`.\n\nExample: `my-lambda/index.js:default`\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: custom-artifact\n       properties:\n         packagePath: ./dist/lambda.zip\n         handler: my-lambda/index.js:default\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'custom-artifact',\n     properties: {\n       packagePath: './dist/lambda.zip',\n       handler: 'my-lambda/index.js:default'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Uses a pre-built artifact for Lambda deployment.\n\n---\n\nWith `custom-artifact`, you provide a path to your own pre-built deployment package.\nIf the specified path is a directory or an unzipped file, Stacktape will automatically zip it.\n\nThis is useful when you have custom build processes or need full control over the packaging.")]).describe("#### How your code is built and packaged for deployment.\n\n---\n\n- **`stacktape-lambda-buildpack`** (recommended): Point to your source file and Stacktape builds,\n  bundles, and uploads it automatically.\n- **`custom-artifact`**: Provide a pre-built zip file. Stacktape handles the upload.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handlers/api.ts\n      memory: 512\n      timeout: 15\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: mainApi\n            method: GET\n            path: /health\n  mainApi:\n    type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/handlers/api.ts' }\n    },\n    memory: 512,\n    timeout: 15,\n    events: [\n      { type: 'http-api-gateway', properties: { httpApiGatewayName: 'mainApi', method: 'GET', path: '/health' } }\n    ]\n  });\n  const mainApi = new HttpApiGateway({});\n  return { resources: { apiFunction, mainApi } };\n});\n```"),
        "events": z.array(z.union([z.object({
            "type": z.literal("application-load-balancer").describe("#### Triggers a function when an Application Load Balancer receives a matching HTTP request.\n\n---\n\nYou can route requests based on HTTP method, path, headers, query parameters, and source IP address."),
            "properties": z.object({
              "loadBalancerName": z.string().describe("#### The name of the Application Load Balancer.\n\n---\n\nThis must reference a load balancer defined in your Stacktape configuration.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /api/users\n              - '/api/articles/*'\n            methods:\n              - GET\n              - POST\n            hosts:\n              - api.example.com\n              - '*.myapp.com'\n            headers:\n              - headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n                  - globex\n            queryParams:\n              - paramName: version\n                values:\n                  - v2\n            sourceIps:\n              - 10.0.0.0/16\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/api/users', '/api/articles/*'],\n          methods: ['GET', 'POST'],\n          hosts: ['api.example.com', '*.myapp.com'],\n          headers: [{ headerName: 'X-Tenant-Id', values: ['acme-corp', 'globex'] }],\n          queryParams: [{ paramName: 'version', values: ['v2'] }],\n          sourceIps: ['10.0.0.0/16']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
              "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The port of the load balancer listener to attach to.\n\n---\n\nYou only need to specify this if the load balancer uses custom listeners.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /api/users\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/api/users']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "priority": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The priority of this integration rule.\n\n---\n\nLoad balancer rules are evaluated in order from the lowest priority to the highest.\nThe first rule that matches an incoming request will handle it.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
              "paths": z.array(z.string()).optional().describe("#### A list of URL paths that will trigger this integration.\n\n---\n\nThe request will be routed if its path matches any of the paths in this list.\nThe comparison is case-sensitive and supports `*` and `?` wildcards.\n\nExample: `/users`, `/articles/*`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n              - '/api/articles/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users', '/api/articles/*']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "methods": z.array(z.string()).optional().describe("#### A list of HTTP methods that will trigger this integration.\n\n---\n\nExample: `GET`, `POST`, `DELETE`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            methods:\n              - GET\n              - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          methods: ['GET', 'POST']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "hosts": z.array(z.string()).optional().describe("#### A list of hostnames that will trigger this integration.\n\n---\n\nThe hostname is parsed from the `Host` header of the request.\nWildcards (`*` and `?`) are supported.\n\nExample: `api.example.com`, `*.myapp.com`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            hosts:\n              - api.example.com\n              - '*.myapp.com'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          hosts: ['api.example.com', '*.myapp.com']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "headers": z.array(z.object({
                "headerName": z.string().describe("#### The name of the HTTP header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            headers:\n              -\n                headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          headers: [\n            {\n              headerName: 'X-Tenant-Id',\n              values: ['acme-corp']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
                "values": z.array(z.string()).describe("#### A list of allowed values for the header.\n\n---\n\nThe condition is met if the header's value in the incoming request matches any of the values in this list. The comparison is case-insensitive.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            headers:\n              - headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n                  - globex\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          headers: [\n            {\n              headerName: 'X-Tenant-Id',\n              values: ['acme-corp', 'globex']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```") }).strict()
              ).optional().describe("#### A list of header conditions that the request must match.\n\n---\n\nAll header conditions must be met for the request to be routed.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            headers:\n              - headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n                  - globex\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          headers: [{ headerName: 'X-Tenant-Id', values: ['acme-corp', 'globex'] }]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "queryParams": z.array(z.object({
                "paramName": z.string().describe("#### The name of the query parameter.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            queryParams:\n              -\n                paramName: version\n                values:\n                  - v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          queryParams: [\n            {\n              paramName: 'version',\n              values: ['v2']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
                "values": z.array(z.string()).describe("#### A list of allowed values for the query parameter.\n\n---\n\nThe condition is met if the query parameter's value in the incoming request matches any of the values in this list. The comparison is case-insensitive.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            queryParams:\n              - paramName: version\n                values:\n                  - v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          queryParams: [\n            {\n              paramName: 'version',\n              values: ['v2']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```") }).strict()
              ).optional().describe("#### A list of query parameter conditions that the request must match.\n\n---\n\nAll query parameter conditions must be met for the request to be routed.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            queryParams:\n              - paramName: version\n                values:\n                  - v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          queryParams: [{ paramName: 'version', values: ['v2'] }]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "sourceIps": z.array(z.string()).optional().describe("#### A list of source IP addresses (in CIDR format) that are allowed to trigger this integration.\n\n---\n\n> **Note:** If the client is behind a proxy, this will be the IP address of the proxy.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            sourceIps:\n              - 10.0.0.0/16\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          sourceIps: ['10.0.0.0/16']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when an Application Load Balancer receives a matching HTTP request.\n\n---\n\nYou can route requests based on HTTP method, path, headers, query parameters, and source IP address."), z.object({
            "type": z.literal("kafka-topic"),
            "properties": z.object({
              "customKafkaConfiguration": z.object({
                "bootstrapServers": z.array(z.string()).describe("#### A list of `host:port` addresses for your Kafka brokers."),
                "topicName": z.string().describe("#### The name of the Kafka topic to consume messages from."),
                "authentication": z.union([z.object({
                    "type": z.enum(["BASIC_AUTH","SASL_SCRAM_256_AUTH","SASL_SCRAM_512_AUTH"]).describe("#### The SASL authentication protocol.\n\n---\n\n- `BASIC_AUTH`: SASL/PLAIN\n- `SASL_SCRAM_256_AUTH`: SASL SCRAM-256\n- `SASL_SCRAM_512_AUTH`: SASL SCRAM-512"),
                    "properties": z.object({
                      "authenticationSecretArn": z.string().describe("#### The ARN of a secret containing the Kafka credentials.\n\n---\n\nThe secret must be a JSON object with `username` and `password` keys.\nYou can create secrets using the `stacktape secret:create` command.") }).strict()
                    .describe("#### Properties of authentication method") }).strict()
                  , z.object({
                    "type": z.literal("MTLS").describe("#### The authentication protocol.\n\n---\n\n`MTLS`: Mutual TLS authentication."),
                    "properties": z.object({
                      "clientCertificate": z.string().describe("#### The ARN of a secret containing the client certificate.\n\n---\n\nThis secret should contain the certificate chain (X.509 PEM), private key (PKCS#8 PEM), and an optional private key password.\nYou can create secrets using the `stacktape secret:create` command."),
                      "serverRootCaCertificate": z.string().optional().describe("#### The ARN of a secret containing the server's root CA certificate.\n\n---\n\nYou can create secrets using the `stacktape secret:create` command.").optional() }).strict()
                    .describe("#### Properties of authentication method") }).strict()
                ]).describe("#### The authentication method for connecting to the Kafka cluster.\n\n---\n\n- `SASL`: Authenticate using a username and password (PLAIN or SCRAM).\n- `MTLS`: Authenticate using a client-side TLS certificate.") }).strict()
              .optional().describe("#### The details of your Kafka cluster.\n\n---\n\nSpecifies the bootstrap servers and topic name.").optional(),
              "batchSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum number of records to process in a single batch.\n\n---\n\nThe function will be invoked with up to this many records. Maximum is 10,000.").default(100),
              "maxBatchWindowSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum time (in seconds) to wait before invoking the function with a batch of records.\n\n---\n\nThe function will be triggered when either the `batchSize` is reached or this time window expires.\nMaximum is 300 seconds.").default(0.5) }).strict()
          }).strict()
          .describe("#### Triggers a function when new messages are available in a Kafka topic."), z.object({
            "type": z.literal("sns"),
            "properties": z.object({
              "snsTopicName": z.string().optional().describe("#### The name of an SNS topic defined in your stack's resources.\n\n---\n\nYou must specify either `snsTopicName` or `snsTopicArn`.").optional(),
              "snsTopicArn": z.string().optional().describe("#### The ARN of an existing SNS topic.\n\n---\n\nUse this to subscribe to a topic that is not managed by your stack.\nYou must specify either `snsTopicName` or `snsTopicArn`.").optional(),
              "filterPolicy": z.any().optional().describe("#### Filter messages by attributes so only relevant ones trigger the function.\n\n---\n\nUses SNS subscription filter policy syntax. For content-based filtering, use EventBridge instead.").optional(),
              "onDeliveryFailure": z.object({
                "sqsQueueArn": z.string().optional().describe("#### The ARN of the SQS queue for failed messages.").optional(),
                "sqsQueueName": z.string().optional().describe("#### The name of an SQS queue (defined in your Stacktape configuration) for failed messages.").optional() }).strict()
              .optional().describe("#### A destination for messages that fail to be delivered to the target.\n\n---\n\nIn rare cases (e.g., if the target function cannot scale fast enough), a message might fail to be delivered.\nThis property specifies an SQS queue where failed messages will be sent.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when a new message is published to an SNS topic.\n\n---\n\nSNS is a pub/sub messaging service. Reference a topic from your stack's `snsTopics` or use an external ARN."), z.object({
            "type": z.literal("sqs"),
            "properties": z.object({
              "sqsQueueName": z.string().optional().describe("#### The name of an SQS queue defined in your stack's resources.\n\n---\n\nYou must specify either `sqsQueueName` or `sqsQueueArn`.").optional(),
              "sqsQueueArn": z.string().optional().describe("#### The ARN of an existing SQS queue.\n\n---\n\nUse this to consume messages from a queue that is not managed by your stack.\nYou must specify either `sqsQueueName` or `sqsQueueArn`.").optional(),
              "batchSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum number of records to process in a single batch.\n\n---\n\nMaximum is 10,000.").default(10),
              "maxBatchWindowSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum time (in seconds) to wait before invoking the function with a batch of records.\n\n---\n\nMaximum is 300 seconds. If not set, the function is invoked as soon as messages are available.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when new messages are available in an SQS queue.\n\n---\n\nMessages are processed in batches. The function fires when `batchSize` is reached,\n`maxBatchWindowSeconds` expires, or the 6 MB payload limit is hit.\n\n**Important:** A single SQS queue should only have one consumer function. For fan-out (multiple\nconsumers for the same message), use an SNS topic or EventBridge event bus instead."), z.object({
            "type": z.literal("kinesis-stream"),
            "properties": z.object({
              "kinesisStreamName": z.string().optional().describe("#### The name of a Kinesis stream defined in your stack's resources.\n\n---\n\nYou must specify either `kinesisStreamName` or `streamArn`.").optional(),
              "streamArn": z.string().optional().describe("#### The ARN of an existing Kinesis stream to consume records from.\n\n---\n\nUse this to consume from a stream that is not managed by your stack.\nYou must specify either `kinesisStreamName` or `streamArn`.").optional(),
              "consumerArn": z.string().optional().describe("#### The ARN of a specific stream consumer to use.\n\n---\n\nThis cannot be used with `autoCreateConsumer`.").optional(),
              "autoCreateConsumer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Automatically creates a dedicated stream consumer for this integration.\n\n---\n\nThis is recommended for minimizing latency and maximizing throughput.\nFor more details, see the [AWS documentation on stream consumers](https://docs.aws.amazon.com/streams/latest/dev/amazon-kinesis-consumers.html).\nThis cannot be used with `consumerArn`.").optional(),
              "maxBatchWindowSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum time (in seconds) to wait before invoking the function with a batch of records.\n\n---\n\nMaximum is 300 seconds.").optional(),
              "batchSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum number of records to process in a single batch.\n\n---\n\nMaximum is 10,000.").default(10),
              "startingPosition": z.enum(["LATEST","TRIM_HORIZON"]).optional().describe("#### The position in the stream from which to start reading records.\n\n---\n\n- `LATEST`: Read only new records.\n- `TRIM_HORIZON`: Read all available records from the beginning of the stream."),
              "maximumRetryAttempts": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The number of times to retry a failed batch of records.\n\n---\n\n> **Important:** If an error occurs, the entire batch is retried, including records that were processed successfully. Your function should be idempotent to handle this.").optional(),
              "onFailure": z.object({
                "arn": z.string().describe("#### The ARN of the SNS topic or SQS queue for failed batches."),
                "type": z.enum(["sns","sqs"]).describe("#### The type of the destination.") }).strict()
              .optional().describe("#### A destination (SQS queue or SNS topic) for batches that fail after all retry attempts.").optional(),
              "parallelizationFactor": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The number of batches to process concurrently from the same shard.").optional(),
              "bisectBatchOnFunctionError": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Splits a failed batch in two before retrying.\n\n---\n\nThis can be useful if a failure is caused by a batch being too large.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when new records are available in a Kinesis Data Stream.\n\n---\n\nRecords are processed in batches. Two consumption modes:\n- **Direct**: Polls each shard ~1/sec, throughput shared with other consumers.\n- **Stream Consumer** (`autoCreateConsumer`): Dedicated connection per shard — higher throughput, lower latency."), z.object({
            "type": z.literal("dynamo-db-stream"),
            "properties": z.object({
              "streamArn": z.string().describe("#### The ARN of the DynamoDB table stream."),
              "maxBatchWindowSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum time (in seconds) to wait before invoking the function with a batch of records.\n\n---\n\nMaximum is 300 seconds.").optional(),
              "batchSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum number of records to process in a single batch.\n\n---\n\nMaximum is 1,000.").default(100),
              "startingPosition": z.string().optional().describe("#### The position in the stream from which to start reading records.\n\n---\n\n- `LATEST`: Read only new records.\n- `TRIM_HORIZON`: Read all available records from the beginning of the stream.").default("TRIM_HORIZON"),
              "maximumRetryAttempts": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The number of times to retry a failed batch of records.\n\n---\n\n> **Important:** If an error occurs, the entire batch is retried, including records that were processed successfully. Your function should be idempotent to handle this.").optional(),
              "onFailure": z.object({
                "arn": z.string().describe("#### The ARN of the SNS topic or SQS queue for failed batches."),
                "type": z.enum(["sns","sqs"]).describe("#### The type of the destination.") }).strict()
              .optional().describe("#### A destination (SQS queue or SNS topic) for batches that fail after all retry attempts.").optional(),
              "parallelizationFactor": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The number of batches to process concurrently from the same shard.").optional(),
              "bisectBatchOnFunctionError": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Splits a failed batch in two before retrying.\n\n---\n\nThis can be useful if a failure is caused by a batch being too large.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when items are created, updated, or deleted in a DynamoDB table.\n\n---\n\nRecords are processed in batches. You must enable streams on the DynamoDB table first\n(set `streaming` in your `dynamoDbTables` config)."), z.object({
            "type": z.literal("s3"),
            "properties": z.object({
              "bucketArn": z.string().describe("#### The ARN of the S3 bucket to monitor for events."),
              "s3EventType": z.enum(["s3:ObjectCreated:*","s3:ObjectCreated:CompleteMultipartUpload","s3:ObjectCreated:Copy","s3:ObjectCreated:Post","s3:ObjectCreated:Put","s3:ObjectRemoved:*","s3:ObjectRemoved:Delete","s3:ObjectRemoved:DeleteMarkerCreated","s3:ObjectRestore:*","s3:ObjectRestore:Completed","s3:ObjectRestore:Post","s3:ReducedRedundancyLostObject","s3:Replication:*","s3:Replication:OperationFailedReplication","s3:Replication:OperationMissedThreshold","s3:Replication:OperationNotTracked","s3:Replication:OperationReplicatedAfterThreshold"]).describe("#### The type of S3 event that will trigger the function."),
              "filterRule": z.object({
                "prefix": z.string().optional().describe("#### The prefix that an object's key must have to trigger the function.").optional(),
                "suffix": z.string().optional().describe("#### The suffix that an object's key must have to trigger the function.").optional() }).strict()
              .optional().describe("#### A filter to apply to objects, so the function is only triggered for relevant objects.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when files are created, deleted, or restored in an S3 bucket."), z.object({
            "type": z.literal("schedule"),
            "properties": z.object({
              "scheduleRate": z.string().describe("#### The schedule rate or cron expression.\n\n---\n\nExamples: `rate(2 hours)`, `cron(0 10 * * ? *)`"),
              "input": z.any().optional().describe("#### A fixed JSON object to be passed as the event payload.\n\n---\n\nIf you need to customize the payload based on the event, use `inputTransformer` instead.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/report.ts\n      events:\n        - type: schedule\n          properties:\n            scheduleRate: rate(1 hour)\n            input:\n              source: my-scheduled-event\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/report.ts' }),\n    events: [\n      {\n        type: 'schedule',\n        properties: {\n          scheduleRate: 'rate(1 hour)',\n          input: { source: 'my-scheduled-event' }\n        }\n      }\n    ]\n  });\n  return { resources: { reportFunction } };\n});\n```").optional(),
              "inputPath": z.string().optional().describe("#### A JSONPath expression to extract a portion of the event to pass to the target.\n\n---\n\nThis is useful for forwarding only a specific part of the event payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/report.ts\n      events:\n        - type: schedule\n          properties:\n            scheduleRate: rate(1 hour)\n            inputPath: $.detail\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/report.ts' }),\n    events: [\n      {\n        type: 'schedule',\n        properties: {\n          scheduleRate: 'rate(1 hour)',\n          inputPath: '$.detail'\n        }\n      }\n    ]\n  });\n  return { resources: { reportFunction } };\n});\n```").optional(),
              "inputTransformer": z.object({
                "inputPathsMap": z.any().optional().describe("#### A map of key-value pairs to extract from the event payload.\n\n---\n\nEach value is a JSONPath expression that extracts data from the event. These extracted values can then be used in the `inputTemplate`.").optional(),
                "inputTemplate": z.any().describe("#### A template for constructing a new event payload.\n\n---\n\nUse placeholders (`<placeholder>`) to insert the values extracted with `inputPathsMap`.") }).strict()
              .optional().describe("#### Customizes the event payload sent to the target.\n\n---\n\nThis allows you to extract values from the original event and use them to construct a new payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/report.ts\n      events:\n        - type: schedule\n          properties:\n            scheduleRate: rate(1 hour)\n            inputTransformer:\n              inputPathsMap:\n                eventTime: $.time\n              inputTemplate:\n                message: This event occurred at <eventTime>.\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/report.ts' }),\n    events: [\n      {\n        type: 'schedule',\n        properties: {\n          scheduleRate: 'rate(1 hour)',\n          inputTransformer: {\n            inputPathsMap: { eventTime: '$.time' },\n            inputTemplate: { message: 'This event occurred at <eventTime>.' }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { reportFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function on a recurring schedule (cron jobs, periodic tasks).\n\n---\n\nTwo formats:\n- **Rate**: `rate(5 minutes)`, `rate(1 hour)`, `rate(7 days)`\n- **Cron**: `cron(0 18 ? * MON-FRI *)` (6-field AWS cron, all times UTC)"), z.object({
            "type": z.literal("cloudwatch-alarm"),
            "properties": z.object({
              "alarmName": z.string().describe("#### The name of the alarm (defined in the `alarms` section) that will trigger the function.") }).strict()
          }).strict()
          , z.object({
            "type": z.literal("cloudwatch-log"),
            "properties": z.object({
              "logGroupArn": z.string().describe("#### The ARN of the log group to watch for new records."),
              "filter": z.string().optional().describe("#### A filter pattern to apply to the log records.\n\n---\n\nOnly logs that match this pattern will trigger the function.\nFor details on the syntax, see the [AWS documentation on filter and pattern syntax](https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/FilterAndPatternSyntax.html).").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when new log records appear in a CloudWatch log group.\n\n---\n\n**Note:** The event payload is base64-encoded and gzipped — you must decode and decompress it in your handler."), z.object({
            "type": z.literal("http-api-gateway"),
            "properties": z.object({
              "httpApiGatewayName": z.string().describe("#### The name of the HTTP API Gateway."),
              "method": z.enum(["*","DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"]).describe("#### The HTTP method that will trigger this integration.\n\n---\n\nYou can specify an exact method (e.g., `GET`) or use `*` to match any method."),
              "path": z.string().describe("#### The URL path that will trigger this integration.\n\n---\n\n- **Exact path**: `/users`\n- **Path with parameter**: `/users/{id}`. The `id` will be available in `event.pathParameters.id`.\n- **Greedy path**: `/files/{proxy+}`. This will match any path starting with `/files/`."),
              "authorizer": z.union([z.object({
                  "type": z.literal("cognito").describe("#### Cognito JWT authorizer\n\n---\n\nConfigures an HTTP API authorizer that validates JSON Web Tokens (JWTs) issued by a Cognito user pool.\nThis is the simplest way to protect routes when your users sign in via `user-auth-pool`.\n\nStacktape turns this into an API Gateway v2 authorizer of type `JWT` that checks the token's issuer and audience.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /me\n            authorizer:\n              type: cognito\n              properties:\n                userPoolName: authPool\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, HttpApiGateway, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authPool = new UserAuthPool({ userVerificationType: 'email-code' });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/me',\n          authorizer: {\n            type: 'cognito',\n            properties: {\n              userPoolName: 'authPool'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authPool, httpApi, apiFunction } };\n});\n```"),
                  "properties": z.object({
                    "userPoolName": z.string().describe("#### Name of the user pool to protect the API\n\n---\n\nThe Stacktape name of the `user-auth-pool` resource whose tokens should be accepted by this HTTP API authorizer.\nStacktape uses this to:\n\n- Set the expected **audience** to the user pool client ID.\n- Build the expected **issuer** URL based on the user pool and AWS region.\n\nIn practice this means only JWTs issued by this pool (and its client) will be considered valid.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /me\n            authorizer:\n              type: cognito\n              properties:\n                userPoolName: authPool\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, HttpApiGateway, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authPool = new UserAuthPool({ userVerificationType: 'email-code' });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/me',\n          authorizer: {\n            type: 'cognito',\n            properties: {\n              userPoolName: 'authPool'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authPool, httpApi, apiFunction } };\n});\n```"),
                    "identitySources": z.array(z.string()).optional().describe("#### Where to read the JWT from in the request\n\n---\n\nA list of identity sources that tell API Gateway where to look for the bearer token, using the\n`$request.*` syntax from API Gateway (for example `'$request.header.Authorization'`).\n\nIf you omit this, Stacktape defaults to reading the token from the `Authorization` HTTP header,\nusing a JWT authorizer as described in the API Gateway v2 authorizer docs\n([AWS::ApiGatewayV2::Authorizer](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-apigatewayv2-authorizer)).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /me\n            authorizer:\n              type: cognito\n              properties:\n                userPoolName: authPool\n                identitySources:\n                  - $request.header.Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, HttpApiGateway, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authPool = new UserAuthPool({ userVerificationType: 'email-code' });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/me',\n          authorizer: {\n            type: 'cognito',\n            properties: {\n              userPoolName: 'authPool',\n              identitySources: ['$request.header.Authorization']\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authPool, httpApi, apiFunction } };\n});\n```").optional() }).strict()
                }).strict()
                , z.object({
                  "type": z.literal("lambda").describe("#### Lambda-based HTTP API authorizer\n\n---\n\nConfigures an API Gateway **request** authorizer that runs a Lambda function to decide whether a request is allowed.\nThis is useful when your authorization logic can't be expressed as simple JWT validation – for example when you\ncheck API keys, look up permissions in a database, or integrate with a non-JWT identity system.\n\nStacktape creates an `AWS::ApiGatewayV2::Authorizer` of type `REQUEST` and wires it up to your Lambda.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```"),
                  "properties": z.object({
                    "functionName": z.string().describe("#### Name of the authorizer function\n\n---\n\nThe Stacktape name of a `function` resource that should run for each authorized request.\nAPI Gateway calls this Lambda, passes request details, and uses its response to allow or deny access.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```"),
                    "iamResponse": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use IAM-style (v1) authorizer responses\n\n---\n\n- If `true`, your Lambda must return a full IAM policy document (the \"v1\" format).\n- If `false` or omitted, Stacktape enables **simple responses** (the HTTP API v2 payload format)\n  so your Lambda can return a small JSON object with an `isAuthorized` flag and optional context.\n\nThis flag is wired to `EnableSimpleResponses` on the underlying `AWS::ApiGatewayV2::Authorizer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n                iamResponse: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction',\n              iamResponse: true\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```").optional(),
                    "identitySources": z.array(z.string()).optional().describe("#### Where to read identity data from\n\n---\n\nA list of request fields API Gateway should pass into your Lambda authorizer (for example headers, query parameters,\nor stage variables) using the `$request.*` syntax.\n\nWhen left empty, no specific identity sources are configured and your Lambda must inspect the incoming event directly.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n                identitySources:\n                  - $request.header.Authorization\n                  - $request.querystring.apiKey\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction',\n              identitySources: ['$request.header.Authorization', '$request.querystring.apiKey']\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```").optional(),
                    "cacheResultSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Cache authorizer results\n\n---\n\nNumber of seconds API Gateway should cache the result of the Lambda authorizer for a given identity.\nWhile cached, repeated requests skip calling your authorizer function and reuse the previous result.\n\nThis value is applied to `AuthorizerResultTtlInSeconds`. If omitted, Stacktape sets it to `0` (no caching).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n                identitySources:\n                  - $request.header.Authorization\n                cacheResultSeconds: 300\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction',\n              identitySources: ['$request.header.Authorization'],\n              cacheResultSeconds: 300\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```").optional() }).strict()
                }).strict()
              ]).optional().describe("#### An authorizer to protect this route.\n\n---\n\nUnauthorized requests will be rejected with a `401 Unauthorized` response.").optional(),
              "payloadFormat": z.enum(["1.0","2.0"]).optional().describe("#### The payload format version for the Lambda integration.\n\n---\n\nFor details on the differences between formats, see the [AWS documentation](https://docs.aws.amazon.com/apigateway/latest/developerguide/http-api-develop-integrations-lambda.html).") }).strict()
          }).strict()
          .describe("#### Triggers a function when an HTTP API Gateway receives a matching request.\n\n---\n\nRoutes are matched by specificity — exact paths take priority over wildcard paths."), z.object({
            "type": z.literal("event-bus"),
            "properties": z.object({
              "eventBusArn": z.string().optional().describe("#### The ARN of an existing event bus.\n\n---\n\nUse this to subscribe to an event bus that is not managed by your stack.\nYou must specify only one of `eventBusArn`, `eventBusName`, or `useDefaultBus`.").optional(),
              "eventBusName": z.string().optional().describe("#### The name of an event bus defined in your stack's resources.\n\n---\n\nYou must specify only one of `eventBusArn`, `eventBusName`, or `useDefaultBus`.").optional(),
              "useDefaultBus": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Uses the default AWS event bus.\n\n---\n\nYou must specify only one of `eventBusArn`, `eventBusName`, or `useDefaultBus`.").optional(),
              "eventPattern": z.object({
                "version": z.any().optional().describe("#### Filter by event version.").optional(), "detail-type": z.any().optional().describe("#### Filter by event detail-type (e.g., `[\"OrderPlaced\"]`). This is the primary field for routing custom events.").optional(),
                "source": z.any().optional().describe("#### Filter by event source (e.g., `[\"my-app\"]` or `[\"aws.ec2\"]` for AWS service events).").optional(),
                "account": z.any().optional().describe("#### Filter by AWS account ID.").optional(),
                "region": z.any().optional().describe("#### Filter by AWS region.").optional(),
                "resources": z.any().optional().describe("#### Filter by resource ARNs.").optional(),
                "detail": z.any().optional().describe("#### Filter by event payload content. Supports nested matching, prefix/suffix, numeric comparisons.").optional(), "replay-name": z.any().optional().describe("#### Filter by replay name (only present on replayed events).").optional() }).strict()
              .describe("#### A pattern to filter events from the event bus.\n\n---\n\nOnly events that match this pattern will trigger the target.\nFor details on the syntax, see the [AWS EventBridge documentation on event patterns](https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-event-patterns.html)."),
              "onDeliveryFailure": z.object({
                "sqsQueueArn": z.string().optional().describe("#### The ARN of the SQS queue for failed events.").optional(),
                "sqsQueueName": z.string().optional().describe("#### The name of an SQS queue (defined in your Stacktape configuration) for failed events.").optional() }).strict()
              .optional().describe("#### A destination for events that fail to be delivered to the target.\n\n---\n\nIn rare cases, an event might fail to be delivered. This property specifies an SQS queue where failed events will be sent.").optional(),
              "input": z.any().optional().describe("#### A fixed JSON object to be passed as the event payload.\n\n---\n\nIf you need to customize the payload based on the event, use `inputTransformer` instead.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n            input:\n              source: my-custom-event\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EventBus, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n  const orderProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/process-order.ts' }),\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] },\n          input: { source: 'my-custom-event' }\n        }\n      }\n    ]\n  });\n  return { resources: { orderEvents, orderProcessor } };\n});\n```").optional(),
              "inputPath": z.string().optional().describe("#### A JSONPath expression to extract a portion of the event to pass to the target.\n\n---\n\nThis is useful for forwarding only a specific part of the event payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n            inputPath: $.detail\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EventBus, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n  const orderProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/process-order.ts' }),\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] },\n          inputPath: '$.detail'\n        }\n      }\n    ]\n  });\n  return { resources: { orderEvents, orderProcessor } };\n});\n```").optional(),
              "inputTransformer": z.object({
                "inputPathsMap": z.any().optional().describe("#### A map of key-value pairs to extract from the event payload.\n\n---\n\nEach value is a JSONPath expression that extracts data from the event. These extracted values can then be used in the `inputTemplate`.").optional(),
                "inputTemplate": z.any().describe("#### A template for constructing a new event payload.\n\n---\n\nUse placeholders (`<placeholder>`) to insert the values extracted with `inputPathsMap`.") }).strict()
              .optional().describe("#### Customizes the event payload sent to the target.\n\n---\n\nThis allows you to extract values from the original event and use them to construct a new payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n            inputTransformer:\n              inputPathsMap:\n                instanceId: $.detail.instance-id\n                instanceState: $.detail.state\n              inputTemplate:\n                message: Instance <instanceId> is now in state <instanceState>.\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EventBus, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n  const orderProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/process-order.ts' }),\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] },\n          inputTransformer: {\n            inputPathsMap: {\n              instanceId: '$.detail.instance-id',\n              instanceState: '$.detail.state'\n            },\n            inputTemplate: { message: 'Instance <instanceId> is now in state <instanceState>.' }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { orderEvents, orderProcessor } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Triggers a batch job when an event matching a specified pattern is received by an event bus.\n\n---\n\nYou can use a custom event bus or the default AWS event bus.")])).optional().describe("#### What triggers this function: HTTP requests, file uploads, queues, schedules, etc.\n\n---\n\nStacktape auto-configures permissions for each trigger.\nThe event payload your function receives depends on the trigger type.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersApi:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/orders.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: publicApi\n            method: POST\n            path: /orders\n        - type: schedule\n          properties:\n            scheduleRate: rate(1 hour)\n  publicApi:\n    type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersApi = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/orders.ts' } },\n    events: [\n      { type: 'http-api-gateway', properties: { httpApiGatewayName: 'publicApi', method: 'POST', path: '/orders' } },\n      { type: 'schedule', properties: { scheduleRate: 'rate(1 hour)' } }\n    ]\n  });\n  const publicApi = new HttpApiGateway({});\n  return { resources: { ordersApi, publicApi } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables available to the function at runtime.\n\n---\n\nVariables from `connectTo` (e.g., `STP_MY_DATABASE_CONNECTION_STRING`) are added automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      environment:\n        - name: STAGE\n          value: production\n        - name: STRIPE_SECRET_KEY\n          value: $Secret('stripe-key')\n        - name: MAX_RETRIES\n          value: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/worker.ts' } },\n    environment: {\n      STAGE: 'production',\n      STRIPE_SECRET_KEY: $Secret('stripe-key'),\n      MAX_RETRIES: 3\n    }\n  });\n  return { resources: { worker } };\n});\n```").optional(),
        "runtime": z.enum(["dotnet6","dotnet7","dotnet8","java11","java17","java8","java8.al2","nodejs18.x","nodejs20.x","nodejs22.x","nodejs24.x","provided.al2","provided.al2023","python3.10","python3.11","python3.12","python3.13","python3.8","python3.9","ruby3.3"]).optional().describe("#### The language runtime (e.g., `nodejs22.x`, `python3.13`).\n\n---\n\nAuto-detected from your source file extension when using `stacktape-lambda-buildpack`.\nOverride only if you need a specific version.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportGenerator:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/report.py\n      runtime: python3.13\n      memory: 1024\n      timeout: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportGenerator = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/report.py' } },\n    runtime: 'python3.13',\n    memory: 1024,\n    timeout: 60\n  });\n  return { resources: { reportGenerator } };\n});\n```").optional(),
        "architecture": z.enum(["arm64","x86_64"]).optional().describe("#### Processor architecture: `x86_64` (default) or `arm64` (Graviton, ~20% cheaper).\n\n---\n\n`arm64` is cheaper per GB-second and often faster. Works with most code out of the box.\nIf using `stacktape-lambda-buildpack`, Stacktape builds for the selected architecture automatically.\nWith `custom-artifact`, you must pre-compile for the target architecture.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  imageResizer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/resize.ts\n      architecture: arm64\n      memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const imageResizer = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/resize.ts' } },\n    architecture: 'arm64',\n    memory: 1024\n  });\n  return { resources: { imageResizer } };\n});\n```").default("x86_64"),
        "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128 - 10,240). Also determines CPU power.\n\n---\n\nLambda scales CPU proportionally to memory: 1,769 MB = 1 vCPU, 3,538 MB = 2 vCPUs, etc.\nIf your function is slow, increasing memory gives it more CPU, which often makes it faster\nand cheaper overall (less execution time).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  pdfRenderer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/pdf.ts\n      memory: 3538\n      timeout: 120\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const pdfRenderer = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/pdf.ts' } },\n    memory: 3538,\n    timeout: 120\n  });\n  return { resources: { pdfRenderer } };\n});\n```").optional(),
        "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Function is killed if it exceeds this.\n\n---\n\nMaximum: 900 seconds (15 minutes). For longer tasks, use a `batch-job` or `worker-service`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  dataImporter:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/import.ts\n      memory: 512\n      timeout: 300\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const dataImporter = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/import.ts' } },\n    memory: 512,\n    timeout: 300\n  });\n  return { resources: { dataImporter } };\n});\n```").default(10),
        "joinDefaultVpc": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Connects the function to your VPC so it can reach databases, Redis, and other VPC-only resources.\n\n---\n\nSet this to `true` when the function must reach VPC-only resources such as a database with\n`accessibilityMode: 'vpc'`/`'scoping-workloads-in-vpc'`, a Redis cluster, or EFS.\n\n**Tradeoff:** The function loses direct internet access. It can still reach S3 and DynamoDB\n(Stacktape auto-creates VPC endpoints), but calls to external APIs (Stripe, OpenAI, etc.) will fail.\nIf you need both VPC access and internet, use a `web-service` or `worker-service` instead.\n\nRequired when using `volumeMounts` (EFS or S3 Files).\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  dbMigrator:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/migrate.ts\n      joinDefaultVpc: true\n      connectTo:\n        - mainDb\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: { type: 'postgres', properties: { version: '16.2', primaryInstance: { instanceSize: 'db.t3.micro' } } }\n  });\n  const dbMigrator = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/migrate.ts' } },\n    joinDefaultVpc: true,\n    connectTo: [mainDb]\n  });\n  return { resources: { dbMigrator, mainDb } };\n});\n```").default(false),
        "tags": z.array(z.object({
          "name": z.string().describe("#### Tag name (1-128 characters)."),
          "value": z.string().describe("#### Tag value (1-256 characters).") }).strict()
        ).optional().describe("#### Additional tags for this function (on top of stack-level tags). Max 50.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  billingFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/billing.ts\n      tags:\n        - name: team\n          value: payments\n        - name: cost-center\n          value: \"4400\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const billingFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/billing.ts' } },\n    tags: [\n      { name: 'team', value: 'payments' },\n      { name: 'cost-center', value: '4400' }\n    ]\n  });\n  return { resources: { billingFunction } };\n});\n```").optional(),
        "destinations": z.object({
          "onSuccess": z.string().optional().describe("#### ARN to receive the result when the function succeeds (SQS, SNS, EventBus, or Lambda ARN).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  asyncWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      destinations:\n        onSuccess: $ResourceParam('resultsTopic', 'arn')\n  resultsTopic:\n    type: sns-topic\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, SnsTopic, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const asyncWorker = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/worker.ts' } },\n    destinations: {\n      onSuccess: $ResourceParam('resultsTopic', 'arn')\n    }\n  });\n  const resultsTopic = new SnsTopic({});\n  return { resources: { asyncWorker, resultsTopic } };\n});\n```").optional(),
          "onFailure": z.string().optional().describe("#### ARN to receive error details when the function fails. Useful for dead-letter processing.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  asyncWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      destinations:\n        onFailure: $ResourceParam('failureQueue', 'arn')\n  failureQueue:\n    type: sqs-queue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, SqsQueue, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const asyncWorker = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/worker.ts' } },\n    destinations: {\n      onFailure: $ResourceParam('failureQueue', 'arn')\n    }\n  });\n  const failureQueue = new SqsQueue({});\n  return { resources: { asyncWorker, failureQueue } };\n});\n```").optional() }).strict()
        .optional().describe("#### Route async invocation results to another service (SQS, SNS, EventBus, or another function).\n\n---\n\nUseful for building event-driven workflows: send successful results to one destination\nand failures to another for error handling.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  asyncProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process.ts\n      destinations:\n        onSuccess: $ResourceParam('successTopic', 'arn')\n        onFailure: $ResourceParam('deadLetterQueue', 'arn')\n  successTopic:\n    type: sns-topic\n  deadLetterQueue:\n    type: sqs-queue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, SnsTopic, SqsQueue, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const asyncProcessor = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/process.ts' } },\n    destinations: {\n      onSuccess: $ResourceParam('successTopic', 'arn'),\n      onFailure: $ResourceParam('deadLetterQueue', 'arn')\n    }\n  });\n  const successTopic = new SnsTopic({});\n  const deadLetterQueue = new SqsQueue({});\n  return { resources: { asyncProcessor, successTopic, deadLetterQueue } };\n});\n```").optional(),
        "logging": z.object({
          "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highVolumeFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highVolumeFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { highVolumeFunction } };\n});\n```").default(false),
          "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs. Longer retention = higher storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/audit.ts' } },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditFunction } };\n});\n```").default(90),
          "logForwarding": z.union([z.object({
              "type": z.literal("http-endpoint"),
              "properties": z.object({
                "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("highlight"),
              "properties": z.object({
                "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("datadog"),
              "properties": z.object({
                "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
            }).strict()
          ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Logging configuration (retention, forwarding).\n\n---\n\nLogs (`stdout`/`stderr`) are auto-sent to CloudWatch. View with `stacktape logs` or in the Stacktape Console.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      logging:\n        retentionDays: 30\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog-api-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiHandler = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    logging: {\n      retentionDays: 30,\n      logForwarding: { type: 'datadog', properties: { apiKey: $Secret('datadog-api-key') } }\n    }\n  });\n  return { resources: { apiHandler } };\n});\n```").optional(),
        "provisionedConcurrency": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Eliminates cold starts by keeping function instances warm and ready.\n\n---\n\nWhen a function hasn't been called recently, the first request can take 1-5+ seconds (\"cold start\").\nThis setting pre-warms the specified number of instances so they respond instantly.\n\n**When to use:** User-facing APIs, web/mobile backends, or any function where response time matters.\nSkip this for background jobs, cron tasks, or data pipelines.\n\n**Cost:** You pay for each provisioned instance even when idle. Also increases deploy time by ~2-5 minutes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutApi:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/checkout.ts\n      memory: 1024\n      provisionedConcurrency: 5\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: storeApi\n            method: POST\n            path: /checkout\n  storeApi:\n    type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutApi = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/checkout.ts' } },\n    memory: 1024,\n    provisionedConcurrency: 5,\n    events: [\n      { type: 'http-api-gateway', properties: { httpApiGatewayName: 'storeApi', method: 'POST', path: '/checkout' } }\n    ]\n  });\n  const storeApi = new HttpApiGateway({});\n  return { resources: { checkoutApi, storeApi } };\n});\n```").optional(),
        "reservedConcurrency": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Cap the maximum number of concurrent instances for this function.\n\n---\n\nReserves this many execution slots exclusively for this function — other functions can't use them,\nand this function can't scale beyond it. **No additional cost.**\n\nCommon uses:\n- Prevent overwhelming a database with too many connections\n- Guarantee capacity for critical functions\n- Throttle expensive downstream API calls\n\n**Example (YAML):**\n\n```yaml\nresources:\n  legacyDbWriter:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/write.ts\n      reservedConcurrency: 10\n      connectTo:\n        - legacyDb\n  legacyDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const legacyDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: { type: 'postgres', properties: { version: '16.2', primaryInstance: { instanceSize: 'db.t3.micro' } } }\n  });\n  const legacyDbWriter = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/write.ts' } },\n    reservedConcurrency: 10,\n    connectTo: [legacyDb]\n  });\n  return { resources: { legacyDbWriter, legacyDb } };\n});\n```").optional(),
        "layers": z.array(z.string()).optional().describe("#### Lambda Layer ARNs to attach (shared libraries, custom runtimes, etc.).\n\n---\n\nLayers are zip archives with additional code/data mounted into the function.\nProvide the layer ARN (e.g., from AWS console or another stack). Max 5 layers per function.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  monitoredFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      layers:\n        - arn:aws:lambda:eu-west-1:464622532012:layer:Datadog-Extension:62\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const monitoredFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    layers: ['arn:aws:lambda:eu-west-1:464622532012:layer:Datadog-Extension:62']\n  });\n  return { resources: { monitoredFunction } };\n});\n```").optional(),
        "deployment": z.object({
          "strategy": z.enum(["AllAtOnce","Canary10Percent10Minutes","Canary10Percent15Minutes","Canary10Percent30Minutes","Canary10Percent5Minutes","Linear10PercentEvery10Minutes","Linear10PercentEvery1Minute","Linear10PercentEvery2Minutes","Linear10PercentEvery3Minutes"]).describe("#### How traffic shifts from the old version to the new one.\n\n---\n\n- **Canary**: Send 10% of traffic first, then all traffic after a wait period.\n- **Linear**: Shift 10% of traffic at regular intervals.\n- **AllAtOnce**: Instant switch (no gradual rollout).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentApi:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payment.ts\n      deployment:\n        strategy: Linear10PercentEvery2Minutes\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentApi = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/payment.ts' } },\n    deployment: {\n      strategy: 'Linear10PercentEvery2Minutes'\n    }\n  });\n  return { resources: { paymentApi } };\n});\n```"),
          "beforeAllowTrafficFunction": z.string().optional().describe("#### Function to run before traffic shifting begins (e.g., smoke tests).\n\n---\n\nMust signal success/failure to CodeDeploy. If it fails, the deployment rolls back.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentApi:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payment.ts\n      deployment:\n        strategy: Canary10Percent5Minutes\n        beforeAllowTrafficFunction: preTrafficCheck\n  preTrafficCheck:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/pre-traffic.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentApi = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/payment.ts' } },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes',\n      beforeAllowTrafficFunction: 'preTrafficCheck'\n    }\n  });\n  const preTrafficCheck = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/pre-traffic.ts' } }\n  });\n  return { resources: { paymentApi, preTrafficCheck } };\n});\n```").optional(),
          "afterTrafficShiftFunction": z.string().optional().describe("#### Function to run after all traffic has shifted (e.g., post-deploy validation).\n\n---\n\nMust signal success/failure to CodeDeploy.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentApi:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payment.ts\n      deployment:\n        strategy: Canary10Percent5Minutes\n        afterTrafficShiftFunction: postTrafficCheck\n  postTrafficCheck:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/post-traffic.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentApi = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/payment.ts' } },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes',\n      afterTrafficShiftFunction: 'postTrafficCheck'\n    }\n  });\n  const postTrafficCheck = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/post-traffic.ts' } }\n  });\n  return { resources: { paymentApi, postTrafficCheck } };\n});\n```").optional() }).strict()
        .optional().describe("#### Gradual traffic shifting for safe deployments.\n\n---\n\nInstead of switching all traffic to the new version instantly, shift it gradually\n(canary or linear). If issues arise, traffic rolls back automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentApi:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payment.ts\n      deployment:\n        strategy: Canary10Percent5Minutes\n        beforeAllowTrafficFunction: smokeTest\n  smokeTest:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/smoke-test.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentApi = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/payment.ts' } },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes',\n      beforeAllowTrafficFunction: 'smokeTest'\n    }\n  });\n  const smokeTest = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/smoke-test.ts' } }\n  });\n  return { resources: { paymentApi, smokeTest } };\n});\n```").optional(),
        "alarms": z.array(z.object({
          "trigger": z.union([z.object({
              "type": z.literal("lambda-error-rate"),
              "properties": z.object({
                "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when the percentage of failed Lambda invocations exceeds this value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker failing too often\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker failing too often',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: {\n            thresholdPercent: 5\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("lambda-duration"),
              "properties": z.object({
                "thresholdMilliseconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when Lambda execution time exceeds this value (ms).\n\n---\n\nDefault: fires if **average** duration > threshold. Customize with `statistic` and `comparisonOperator`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  imageProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/process-image.ts\n      timeout: 30\n      alarms:\n        - description: Image processing approaching timeout\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 25000\n              statistic: max\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const imageProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/process-image.ts' }),\n    timeout: 30,\n    alarms: [\n      {\n        description: 'Image processing approaching timeout',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 25000,\n            statistic: 'max'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { imageProcessor } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
            }).strict()
          ]),
          "evaluation": z.object({
            "period": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Duration of one evaluation period in seconds. Must be a multiple of 60.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker running too long\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          evaluation:\n            period: 300\n            evaluationPeriods: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker running too long',\n        trigger: {\n          type: 'lambda-duration',\n          properties: { thresholdMilliseconds: 5000 }\n        },\n        evaluation: {\n          period: 300,\n          evaluationPeriods: 2\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default(60),
            "evaluationPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many recent periods to evaluate. Prevents alarms from firing on short spikes.\n\n---\n\nExample: set to `5` with `breachedPeriods: 3` — the alarm fires only if the threshold is breached\nin at least 3 of the last 5 periods.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/payments.ts\n      alarms:\n        - description: Payments error rate elevated\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/payments.ts' }),\n    alarms: [\n      {\n        description: 'Payments error rate elevated',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 1 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { paymentsFn } };\n});\n```").default(1),
            "breachedPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many periods (within `evaluationPeriods`) must breach the threshold to fire the alarm.\n\n---\n\nMust be ≤ `evaluationPeriods`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/checkout.ts\n      alarms:\n        - description: Checkout errors persisting across multiple periods\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 2\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/checkout.ts' }),\n    alarms: [\n      {\n        description: 'Checkout errors persisting across multiple periods',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { checkoutFn } };\n});\n```").default(1) }).strict()
          .optional().describe("#### How long and how often to evaluate the metric before triggering.\n\n---\n\nControls the evaluation window (period), how many periods to look at, and how many must breach\nthe threshold to fire the alarm. Useful for filtering out short spikes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/api.ts\n      memory: 512\n      timeout: 10\n      alarms:\n        - description: API error rate too high\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    alarms: [\n      {\n        description: 'API error rate too high',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFunction } };\n});\n```").optional(),
          "notificationTargets": z.array(z.union([z.object({
              "type": z.literal("slack"),
              "properties": z.object({
                "conversationId": z.string().describe("#### The Slack channel or DM ID to send notifications to.\n\n---\n\nTo find the ID: open the channel, click its name, and look at the bottom of the **About** tab.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```"),
                "accessToken": z.string().describe("#### Bot User OAuth Token for your Slack app. Store as `$Secret()` for security.\n\n---\n\nCreate a Slack app, add the `chat:write` scope, install it to your workspace, then copy the Bot User OAuth Token.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("ms-teams"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Incoming Webhook URL for the MS Teams channel. Store as `$Secret()` for security.\n\n---\n\nCreate an Incoming Webhook connector in your Teams channel settings to get this URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 1024\n      timeout: 15\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          description: API function is running slow\n          notificationTargets:\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('ms-teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/api.ts' }\n    },\n    memory: 1024,\n    timeout: 15,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 5000 } },\n        description: 'API function is running slow',\n        notificationTargets: [\n          {\n            type: 'ms-teams',\n            properties: {\n              webhookUrl: $Secret('ms-teams-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("email"),
              "properties": z.object({
                "sender": z.string().describe("#### The email address of the sender.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```"),
                "recipient": z.string().describe("#### The email address of the recipient.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("discord"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Discord Webhook URL for the channel. Store as `$Secret()` for security.\n\n---\n\nCreate a webhook in your Discord channel settings (Edit Channel → Integrations → Webhooks).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest.ts\n      memory: 256\n      timeout: 60\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 30000\n          description: Ingest job is taking too long\n          notificationTargets:\n            - type: discord\n              properties:\n                webhookUrl: $Secret('discord-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest.ts' }\n    },\n    memory: 256,\n    timeout: 60,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 30000 } },\n        description: 'Ingest job is taking too long',\n        notificationTargets: [\n          {\n            type: 'discord',\n            properties: {\n              webhookUrl: $Secret('discord-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { ingestFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("webhook"),
              "properties": z.object({
                "url": z.string().describe("#### The URL to send webhook POST requests to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```"),
                "secret": z.string().optional().describe("#### Optional signing secret for HMAC-SHA256 payload verification.\n\n---\n\nIf provided, each request includes an `X-Stacktape-Signature` header.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional(),
                "headers": z.record(z.string(), z.any()).optional().describe("#### Optional custom headers to include in each request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n                headers:\n                  X-Environment: production\n                  Authorization: $Secret('webhook-auth-header')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret'),\n              headers: {\n                'X-Environment': 'production',\n                Authorization: $Secret('webhook-auth-header')\n              }\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional() }).strict()
              .optional().describe("").optional() }).strict()
          ])).optional().describe("#### Where to send notifications when the alarm fires — Slack, MS Teams, or email.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API returning too many errors\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 2\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C12345678\n                accessToken: $Secret('slack-bot-token')\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API returning too many errors',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: { conversationId: 'C12345678', accessToken: $Secret('slack-bot-token') }\n          },\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          },\n          {\n            type: 'ms-teams',\n            properties: { webhookUrl: $Secret('teams-webhook-url') }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "includeInHistory": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether alarm state changes should appear in monitoring history.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Jobs queue backing up\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          includeInHistory: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Jobs queue backing up',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        includeInHistory: false\n      }\n    ]\n  });\n\n  return { resources: { jobsQueue } };\n});\n```").default(true),
          "description": z.string().optional().describe("#### Custom alarm description used in notification messages and the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 80\n          description: Database CPU sustained above 80% — consider scaling up\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: { thresholdPercent: 80 }\n        },\n        description: 'Database CPU sustained above 80% — consider scaling up'\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Alarms for this function (merged with global alarms from the Stacktape Console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  criticalApi:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/critical.ts\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const criticalApi = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/critical.ts' } },\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        notificationTargets: [\n          { type: 'email', properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' } }\n        ]\n      }\n    ]\n  });\n  return { resources: { criticalApi } };\n});\n```").optional(),
        "disabledGlobalAlarms": z.array(z.string()).optional().describe("#### Global alarm names to exclude from this function.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  batchReporter:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/batch.ts\n      timeout: 300\n      disabledGlobalAlarms:\n        - lambda-duration-global\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const batchReporter = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/batch.ts' } },\n    timeout: 300,\n    disabledGlobalAlarms: ['lambda-duration-global']\n  });\n  return { resources: { batchReporter } };\n});\n```").optional(),
        "url": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable the function URL.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/public.ts\n      url:\n        enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const publicFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/public.ts' } },\n    url: {\n      enabled: true\n    }\n  });\n  return { resources: { publicFunction } };\n});\n```"),
          "cors": z.object({
            "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable CORS. When `true` with no other settings, uses permissive defaults (`*` for origins and methods).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      url:\n        enabled: true\n        cors:\n          enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    url: {\n      enabled: true,\n      cors: {\n        enabled: true\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
            "allowedOrigins": z.array(z.string()).optional().describe("#### Allowed origins (e.g., `https://example.com`). Use `*` for any origin.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      url:\n        enabled: true\n        cors:\n          enabled: true\n          allowedOrigins:\n            - https://app.example.com\n            - https://admin.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    url: {\n      enabled: true,\n      cors: {\n        enabled: true,\n        allowedOrigins: ['https://app.example.com', 'https://admin.example.com']\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(["*"]),
            "allowedHeaders": z.array(z.string()).optional().describe("#### Allowed request headers (e.g., `Content-Type`, `Authorization`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      url:\n        enabled: true\n        cors:\n          enabled: true\n          allowedOrigins:\n            - https://app.example.com\n          allowedHeaders:\n            - Content-Type\n            - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    url: {\n      enabled: true,\n      cors: {\n        enabled: true,\n        allowedOrigins: ['https://app.example.com'],\n        allowedHeaders: ['Content-Type', 'Authorization']\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
            "allowedMethods": z.array(z.enum(["*","DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### Allowed HTTP methods (e.g., `GET`, `POST`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      url:\n        enabled: true\n        cors:\n          enabled: true\n          allowedOrigins:\n            - https://app.example.com\n          allowedMethods:\n            - GET\n            - POST\n            - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    url: {\n      enabled: true,\n      cors: {\n        enabled: true,\n        allowedOrigins: ['https://app.example.com'],\n        allowedMethods: ['GET', 'POST', 'OPTIONS']\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
            "allowCredentials": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Allow cookies and credentials in cross-origin requests.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      url:\n        enabled: true\n        cors:\n          enabled: true\n          allowedOrigins:\n            - https://app.example.com\n          allowCredentials: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    url: {\n      enabled: true,\n      cors: {\n        enabled: true,\n        allowedOrigins: ['https://app.example.com'],\n        allowCredentials: true\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
            "exposedResponseHeaders": z.array(z.string()).optional().describe("#### Response headers accessible to browser JavaScript.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      url:\n        enabled: true\n        cors:\n          enabled: true\n          allowedOrigins:\n            - https://app.example.com\n          exposedResponseHeaders:\n            - X-Request-Id\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    url: {\n      enabled: true,\n      cors: {\n        enabled: true,\n        allowedOrigins: ['https://app.example.com'],\n        exposedResponseHeaders: ['X-Request-Id']\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
            "maxAge": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How long (seconds) browsers can cache preflight responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      url:\n        enabled: true\n        cors:\n          enabled: true\n          allowedOrigins:\n            - https://app.example.com\n          maxAge: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    url: {\n      enabled: true,\n      cors: {\n        enabled: true,\n        allowedOrigins: ['https://app.example.com'],\n        maxAge: 600\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### CORS settings for the function URL. Overrides any CORS headers from the function itself.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      url:\n        enabled: true\n        cors:\n          enabled: true\n          allowedOrigins:\n            - https://app.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    url: {\n      enabled: true,\n      cors: { enabled: true, allowedOrigins: ['https://app.example.com'] }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
          "authMode": z.enum(["AWS_IAM","NONE"]).optional().describe("#### Who can call this URL.\n\n---\n\n- `NONE` — public, anyone can call it.\n- `AWS_IAM` — only authenticated AWS users/roles with invoke permission.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  internalFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/internal.ts\n      url:\n        enabled: true\n        authMode: AWS_IAM\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const internalFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/internal.ts' } },\n    url: {\n      enabled: true,\n      authMode: 'AWS_IAM'\n    }\n  });\n  return { resources: { internalFunction } };\n});\n```"),
          "responseStreamEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Stream the response progressively instead of buffering the entire response.\n\n---\n\nImproves Time to First Byte and increases max response size from 6 MB to 20 MB.\nRequires using the AWS streaming handler pattern in your code.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  streamingFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/stream.ts\n      memory: 1024\n      url:\n        enabled: true\n        responseStreamEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const streamingFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/stream.ts' } },\n    memory: 1024,\n    url: {\n      enabled: true,\n      responseStreamEnabled: true\n    }\n  });\n  return { resources: { streamingFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Give this function its own HTTPS URL (no API Gateway needed).\n\n---\n\nSimpler and cheaper than an API Gateway for single-function endpoints.\nURL format: `https://{id}.lambda-url.{region}.on.aws`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webhookReceiver:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/webhook.ts\n      url:\n        enabled: true\n        authMode: NONE\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webhookReceiver = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/webhook.ts' } },\n    url: { enabled: true, authMode: 'NONE' }\n  });\n  return { resources: { webhookReceiver } };\n});\n```").optional(),
        "cdn": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable CDN (CloudFront) for faster global delivery and lower bandwidth costs.\n\n---\n\nCaches responses at edge locations worldwide so users get content from the nearest server.\nThe CDN itself has no monthly fee — you pay per request (~$0.01/10k) and per GB transferred.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "cachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control how long and what gets cached at the CDN edge.\n\n---\n\nWhen the origin response has no `Cache-Control` header, defaults apply:\n- **Bucket origins**: cached for 6 months (or until invalidated on deploy).\n- **API Gateway / Load Balancer origins**: not cached.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400,\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "forwardingOptions": z.object({
            "customRequestHeaders": z.array(z.object({
              "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
              "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
            ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cookies": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "headers": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "queryString": z.object({
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control which headers, cookies, and query params are forwarded to your origin.\n\n---\n\nBy default, all headers/cookies/query params are forwarded. Use this to restrict\nwhat reaches your app (e.g., strip cookies for static content).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { none: true }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "routeRewrites": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `*.jpg`, `/docs/v2/*`). Wildcards supported.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
            "routePrefix": z.string().optional().describe("#### Prepend a path prefix to requests before forwarding to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routePrefix: /v2\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routePrefix: '/v2',\n        routeTo: { type: 'http-api-gateway', properties: { httpApiGatewayName: 'backendApi' } }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, api } };\n});\n```").optional(),
            "routeTo": z.union([z.object({
                "type": z.literal("application-load-balancer"),
                "properties": z.object({
                  "loadBalancerName": z.string().describe("#### Name of the `application-load-balancer` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```"),
                  "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Listener port on the load balancer. Only needed if using custom listeners.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              listenerPort: 8080\nappLb:\n  type: application-load-balancer\n  properties:\n    listeners:\n      - port: 8080\n        protocol: HTTP\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({\n  listeners: [{ port: 8080, protocol: 'HTTP' }]\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            listenerPort: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional(),
                  "originDomainName": z.string().optional().describe("#### Explicit origin domain. Only needed if the ALB has no `customDomains` and uses `customCertificateArns`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              originDomainName: app.internal.example.com\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            originDomainName: 'app.internal.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("http-api-gateway"),
                "properties": z.object({
                  "httpApiGatewayName": z.string().describe("#### Name of the `http-api-gateway` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnApi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst cdnApi = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routeTo: {\n          type: 'http-api-gateway',\n          properties: {\n            httpApiGatewayName: 'backendApi'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, cdnApi } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("function"),
                "properties": z.object({
                  "functionName": z.string().describe("#### Name of the `function` resource to route to. The function must have `url.enabled: true`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /render/*\n          routeTo:\n            type: function\n            properties:\n              functionName: renderer\nrenderer:\n  type: function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/render.ts\n    url:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst renderer = new LambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/render.ts' }),\n  url: { enabled: true }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/render/*',\n        routeTo: {\n          type: 'function',\n          properties: {\n            functionName: 'renderer'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { renderer, api } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("custom-origin"),
                "properties": z.object({
                  "domainName": z.string().describe("#### Domain name of the external origin (e.g., `api.example.com`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "protocol": z.enum(["HTTP","HTTPS"]).optional().describe("#### Protocol for connecting to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTPS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTPS'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port on the origin. Defaults to 443 for HTTPS, 80 for HTTP.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTP\n              port: 8080\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTP',\n            port: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("bucket"),
                "properties": z.object({
                  "bucketName": z.string().describe("#### Name of the `bucket` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
                  "disableUrlNormalization": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable clean URL normalization (e.g., `/about` → `/about.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n              disableUrlNormalization: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets',\n            disableUrlNormalization: true\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").default(false) }).strict()
              }).strict()
            ]).optional().describe("#### Where to send matching requests. Can be a bucket, API Gateway, load balancer, Lambda, or external domain.\n\n---\n\nIf not set, requests go to the default origin (the resource this CDN is attached to).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /static/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/static/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override caching behavior for requests matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          cachingOptions:\n            defaultTTL: 604800\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        cachingOptions: { defaultTTL: 604800 }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "forwardingOptions": z.object({
              "customRequestHeaders": z.array(z.object({
                "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
              ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override which headers, cookies, and query params are forwarded for this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          forwardingOptions:\n            cookies:\n              none: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        forwardingOptions: { cookies: { none: true } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "edgeFunctions": z.object({
              "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
              "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
              "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
              "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Run edge functions on requests/responses matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /private/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          edgeFunctions:\n            onRequest: authAtEdge\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/private/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        edgeFunctions: { onRequest: 'authAtEdge' }\n      }\n    ]\n  }\n});\nreturn { resources: { authAtEdge, assets, api } };\n});\n```").optional() }).strict()
          ).optional().describe("#### Route specific URL patterns to different origins (e.g., `/api/*` → Lambda, `/assets/*` → S3).\n\n---\n\nEvaluated in order; first match wins. Unmatched requests go to the default origin.\nEach route can have its own caching and forwarding settings.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
          "customDomains": z.array(z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          ).optional().describe("#### Custom domains (e.g., `cdn.example.com`). Stacktape auto-creates DNS records and TLS certificates.\n\n---\n\nYour domain must be added as a Route53 hosted zone in your AWS account first.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      customDomains:\n        - domainName: cdn.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    customDomains: [{ domainName: 'cdn.example.com' }]\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "edgeFunctions": z.object({
            "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
            "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
            "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
            "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Run edge functions on CDN requests/responses (URL rewrites, auth, A/B testing).\n\n---\n\n- `onRequest`: Before cache lookup — modify the request, add auth, or return early.\n- `onResponse`: Before returning to the client — modify headers, add cookies.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
          "cloudfrontPriceClass": z.enum(["PriceClass_100","PriceClass_200","PriceClass_All"]).optional().describe("#### Which regions the CDN serves from. Fewer regions = lower cost, but slower for distant users.\n\n---\n\n- **`PriceClass_100`**: North America + Europe. Cheapest option. Good if your users are in the US/EU.\n- **`PriceClass_200`**: Adds Asia, Middle East, Africa.\n- **`PriceClass_All`** (default): All regions worldwide, including South America and Oceania.\n\nThe CDN itself has no monthly base cost - you only pay per request and per GB transferred.\nThe price class controls which edge locations are used, and some regions cost more per request.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cloudfrontPriceClass: PriceClass_100\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cloudfrontPriceClass: 'PriceClass_100'\n  }\n});\nreturn { resources: { assets } };\n});\n```"),
          "defaultRoutePrefix": z.string().optional().describe("#### Prepend a path prefix to all requests forwarded to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded to the origin as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      defaultRoutePrefix: /v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    defaultRoutePrefix: '/v2'\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "errorDocument": z.string().optional().describe("#### Page to show for 404 errors (e.g., `/error.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      errorDocument: /error.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    errorDocument: '/error.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/404.html"),
          "indexDocument": z.string().optional().describe("#### Page served for requests to `/`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      indexDocument: /index.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    indexDocument: '/index.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/index.html"),
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    disableInvalidationAfterDeploy: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this CDN from common web exploits.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnFirewall:\n  type: web-app-firewall\n  properties:\n    scope: cdn\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      useFirewall: cdnFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cdnFirewall = new WebAppFirewall({ scope: 'cdn' });\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    useFirewall: 'cdnFirewall'\n  }\n});\nreturn { resources: { cdnFirewall, api } };\n});\n```").optional() }).strict()
        .optional().describe("#### Put a CDN (CloudFront) in front of this function for caching and lower latency.\n\n---\n\nCaches responses at edge locations worldwide. Reduces function invocations and bandwidth costs.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ssrFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/render.ts\n      memory: 1024\n      url:\n        enabled: true\n      cdn:\n        enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ssrFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/render.ts' } },\n    memory: 1024,\n    url: { enabled: true },\n    cdn: { enabled: true }\n  });\n  return { resources: { ssrFunction } };\n});\n```").optional(),
        "storage": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Size of the `/tmp` directory in MB (512 - 10,240). Ephemeral per invocation.\n\n---\n\nIncrease if your function downloads/processes large files temporarily.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  videoTranscoder:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/transcode.ts\n      memory: 3008\n      timeout: 600\n      storage: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const videoTranscoder = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/transcode.ts' } },\n    memory: 3008,\n    timeout: 600,\n    storage: 4096\n  });\n  return { resources: { videoTranscoder } };\n});\n```").default(512),
        "volumeMounts": z.array(z.union([z.object({
            "type": z.literal("efs").describe("#### The type of the volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mediaProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/media.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: efs\n          properties:\n            efsFilesystemName: mediaStore\n            mountPath: /mnt/media\n  mediaStore:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EfsFilesystem, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mediaProcessor = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/media.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      {\n        type: 'efs',\n        properties: { efsFilesystemName: 'mediaStore', mountPath: '/mnt/media' }\n      }\n    ]\n  });\n  const mediaStore = new EfsFilesystem({});\n  return { resources: { mediaProcessor, mediaStore } };\n});\n```"),
            "properties": z.object({
              "efsFilesystemName": z.string().describe("#### Name of the `efs-filesystem` resource defined in your config.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mediaProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/media.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: efs\n          properties:\n            efsFilesystemName: mediaStore\n            mountPath: /mnt/media\n  mediaStore:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EfsFilesystem, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mediaProcessor = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/media.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      {\n        type: 'efs',\n        properties: {\n          efsFilesystemName: 'mediaStore',\n          mountPath: '/mnt/media'\n        }\n      }\n    ]\n  });\n  const mediaStore = new EfsFilesystem({});\n  return { resources: { mediaProcessor, mediaStore } };\n});\n```"),
              "rootDirectory": z.string().optional().describe("#### Subdirectory within the EFS filesystem to mount. Omit for full access.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mediaProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/media.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: efs\n          properties:\n            efsFilesystemName: mediaStore\n            rootDirectory: /uploads\n            mountPath: /mnt/media\n  mediaStore:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EfsFilesystem, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mediaProcessor = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/media.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      {\n        type: 'efs',\n        properties: {\n          efsFilesystemName: 'mediaStore',\n          rootDirectory: '/uploads',\n          mountPath: '/mnt/media'\n        }\n      }\n    ]\n  });\n  const mediaStore = new EfsFilesystem({});\n  return { resources: { mediaProcessor, mediaStore } };\n});\n```").default("/"),
              "mountPath": z.string().describe("#### Path inside the function where the volume appears. Must start with `/mnt/` (e.g., `/mnt/data`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mediaProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/media.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: efs\n          properties:\n            efsFilesystemName: mediaStore\n            mountPath: /mnt/media\n  mediaStore:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EfsFilesystem, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mediaProcessor = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/media.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      {\n        type: 'efs',\n        properties: {\n          efsFilesystemName: 'mediaStore',\n          mountPath: '/mnt/media'\n        }\n      }\n    ]\n  });\n  const mediaStore = new EfsFilesystem({});\n  return { resources: { mediaProcessor, mediaStore } };\n});\n```") }).strict()
            .describe("#### Properties for the EFS volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mediaProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/media.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: efs\n          properties:\n            efsFilesystemName: mediaStore\n            mountPath: /mnt/media\n  mediaStore:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EfsFilesystem, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mediaProcessor = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/media.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      {\n        type: 'efs',\n        properties: { efsFilesystemName: 'mediaStore', mountPath: '/mnt/media' }\n      }\n    ]\n  });\n  const mediaStore = new EfsFilesystem({});\n  return { resources: { mediaProcessor, mediaStore } };\n});\n```") }).strict()
          , z.object({
            "type": z.literal("s3files").describe("#### The type of the volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  datasetReader:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/read.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: s3files\n          properties:\n            accessPointArn: arn:aws:s3:eu-west-1:123456789012:accesspoint/my-ap\n            mountPath: /mnt/s3data\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const datasetReader = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/read.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      {\n        type: 's3files',\n        properties: { accessPointArn: 'arn:aws:s3:eu-west-1:123456789012:accesspoint/my-ap', mountPath: '/mnt/s3data' }\n      }\n    ]\n  });\n  return { resources: { datasetReader } };\n});\n```"),
            "properties": z.object({
              "accessPointArn": z.union([z.object({
                  "name": z.string(),
                  "payload": z.any() }).strict()
                , z.string()]).describe("#### ARN of an existing S3 Files access point.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  datasetReader:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/read.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: s3files\n          properties:\n            accessPointArn: arn:aws:s3:eu-west-1:123456789012:accesspoint/my-ap\n            mountPath: /mnt/s3data\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const datasetReader = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/read.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      {\n        type: 's3files',\n        properties: {\n          accessPointArn: 'arn:aws:s3:eu-west-1:123456789012:accesspoint/my-ap',\n          mountPath: '/mnt/s3data'\n        }\n      }\n    ]\n  });\n  return { resources: { datasetReader } };\n});\n```"),
              "mountPath": z.string().describe("#### Path inside the function where the volume appears. Must start with `/mnt/` (e.g., `/mnt/s3data`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  datasetReader:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/read.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: s3files\n          properties:\n            accessPointArn: arn:aws:s3:eu-west-1:123456789012:accesspoint/my-ap\n            mountPath: /mnt/s3data\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const datasetReader = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/read.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      {\n        type: 's3files',\n        properties: {\n          accessPointArn: 'arn:aws:s3:eu-west-1:123456789012:accesspoint/my-ap',\n          mountPath: '/mnt/s3data'\n        }\n      }\n    ]\n  });\n  return { resources: { datasetReader } };\n});\n```") }).strict()
            .describe("#### Properties for the S3 Files volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  datasetReader:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/read.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: s3files\n          properties:\n            accessPointArn: arn:aws:s3:eu-west-1:123456789012:accesspoint/my-ap\n            mountPath: /mnt/s3data\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const datasetReader = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/read.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      {\n        type: 's3files',\n        properties: { accessPointArn: 'arn:aws:s3:eu-west-1:123456789012:accesspoint/my-ap', mountPath: '/mnt/s3data' }\n      }\n    ]\n  });\n  return { resources: { datasetReader } };\n});\n```") }).strict()
        ])).optional().describe("#### Persistent file-system mounts shared across invocations and functions.\n\n---\n\nUnlike `/tmp`, mounted file systems persist independently from the function runtime and can be\nshared across multiple functions.\nRequires `joinDefaultVpc: true` (Stacktape will remind you if you forget).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  sharedDataFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      joinDefaultVpc: true\n      volumeMounts:\n        - type: efs\n          properties:\n            efsFilesystemName: sharedStorage\n            mountPath: /mnt/data\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EfsFilesystem, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedDataFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    joinDefaultVpc: true,\n    volumeMounts: [\n      { type: 'efs', properties: { efsFilesystemName: 'sharedStorage', mountPath: '/mnt/data' } }\n    ]\n  });\n  const sharedStorage = new EfsFilesystem({});\n  return { resources: { sharedDataFunction, sharedStorage } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### A serverless compute resource that runs your code in response to events.\n\n---\n\nLambda functions are short-lived, stateless, and scale automatically. You only pay for the compute time you consume."), z.object({
      "type": z.literal("astro-web"),
      "properties": z.object({
        "appDirectory": z.string().optional().describe("#### Directory containing your `astro.config.mjs`. For monorepos, point to the Astro workspace.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      appDirectory: packages/storefront\n      buildCommand: npm run build\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    appDirectory: 'packages/storefront',\n    buildCommand: 'npm run build'\n  });\n  return { resources: { web } };\n});\n```"),
        "buildCommand": z.string().optional().describe("#### Override the default `astro build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      appDirectory: .\n      buildCommand: npm run build:production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    appDirectory: '.',\n    buildCommand: 'npm run build:production'\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables for the SSR function. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  marketingDb:\n    type: relational-database\n    properties:\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      credentials:\n        masterUserPassword: $Secret('db.password')\n  web:\n    type: astro-web\n    properties:\n      environment:\n        - name: PUBLIC_SITE_NAME\n          value: My Store\n        - name: DATABASE_URL\n          value: $ResourceParam('marketingDb', 'connectionString')\n        - name: STRIPE_SECRET\n          value: $Secret('stripe.secretKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, RelationalDatabase, defineConfig, $ResourceParam, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const marketingDb = new RelationalDatabase({\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.6',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    credentials: { masterUserPassword: $Secret('db.password') }\n  });\n  const web = new AstroWeb({\n    environment: [\n      { name: 'PUBLIC_SITE_NAME', value: 'My Store' },\n      { name: 'DATABASE_URL', value: $ResourceParam('marketingDb', 'connectionString') },\n      { name: 'STRIPE_SECRET', value: $Secret('stripe.secretKey') }\n    ]\n  });\n  return { resources: { marketingDb, web } };\n});\n```").optional(),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Attach custom domains with auto-managed DNS records and TLS certificates.\n\n---\n\n**Prerequisite:** A Route 53 hosted zone for your domain must exist in your AWS account.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      customDomains:\n        - domainName: shop.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    customDomains: [{ domainName: 'shop.example.com' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "serverLambda": z.object({
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128–10,240). CPU scales proportionally — 1,769 MB = 1 vCPU.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      serverLambda:\n        memory: 3008\n        timeout: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    serverLambda: {\n      memory: 3008,\n      timeout: 30\n    }\n  });\n  return { resources: { web } };\n});\n```").default(1024),
          "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Max: 30.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      serverLambda:\n        memory: 1024\n        timeout: 25\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    serverLambda: {\n      memory: 1024,\n      timeout: 25\n    }\n  });\n  return { resources: { web } };\n});\n```").default(30),
          "joinDefaultVpc": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Connect to VPC resources (databases, Redis). **Warning:** function loses direct internet access.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  cache:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t3.micro\n  web:\n    type: astro-web\n    properties:\n      connectTo:\n        - cache\n      serverLambda:\n        joinDefaultVpc: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, RedisCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const cache = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t3.micro'\n  });\n  const web = new AstroWeb({\n    connectTo: ['cache'],\n    serverLambda: {\n      joinDefaultVpc: true\n    }\n  });\n  return { resources: { cache, web } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highVolumeFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highVolumeFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { highVolumeFunction } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs. Longer retention = higher storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/audit.ts' } },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditFunction } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Logging config. Logs are sent to CloudWatch. View with `stacktape logs` or in the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      serverLambda:\n        logging:\n          retentionDays: 90\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    serverLambda: {\n      logging: {\n        retentionDays: 90\n      }\n    }\n  });\n  return { resources: { web } };\n});\n```").optional() }).strict()
        .optional().describe("#### Customize the SSR Lambda function (memory, timeout, VPC, logging).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      serverLambda:\n        memory: 2048\n        timeout: 20\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    serverLambda: {\n      memory: 2048,\n      timeout: 20\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this app. Firewall `scope` must be `cdn`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  storefrontFirewall:\n    type: web-app-firewall\n    properties:\n      scope: cdn\n  web:\n    type: astro-web\n    properties:\n      useFirewall: storefrontFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const storefrontFirewall = new WebAppFirewall({ scope: 'cdn' });\n  const web = new AstroWeb({\n    useFirewall: 'storefrontFirewall'\n  });\n  return { resources: { storefrontFirewall, web } };\n});\n```").optional(),
        "dev": z.object({
          "command": z.string().optional().describe("#### Override the default `astro dev` command (e.g., `npm run dev`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      dev:\n        command: npm run dev\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    dev: {\n      command: 'npm run dev'\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
          "workingDirectory": z.string().optional().describe("#### Working directory for the dev command, relative to project root.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      dev:\n        command: astro dev\n        workingDirectory: packages/storefront\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    dev: {\n      command: 'astro dev',\n      workingDirectory: 'packages/storefront'\n    }\n  });\n  return { resources: { web } };\n});\n```").optional() }).strict()
        .optional().describe("#### Dev server config for `stacktape dev`. Defaults to `astro dev`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      dev:\n        command: npm run dev\n        workingDirectory: packages/storefront\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    dev: {\n      command: 'npm run dev',\n      workingDirectory: 'packages/storefront'\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "fileOptions": z.array(z.object({
          "includePattern": z.string().describe("#### Glob pattern for files this rule applies to (e.g., `*.html`, `assets/**`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
          "excludePattern": z.string().optional().describe("#### Glob pattern for files to exclude even if they match `includePattern`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            excludePattern: 'assets/uncompressed/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          excludePattern: 'assets/uncompressed/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "headers": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### HTTP headers (e.g., `Cache-Control`) for matching files. Forwarded through CDN to the browser.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "tags": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### Tags for matching files. Can be used to target files with `lifecycleRules`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'reports/**'\n            tags:\n              - key: category\n                value: report\n      lifecycleRules:\n        - type: expiration\n          properties:\n            tags:\n              - key: category\n                value: report\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'reports/**',\n          tags: [{ key: 'category', value: 'report' }]\n        }\n      ]\n    },\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          tags: [{ key: 'category', value: 'report' }],\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Set custom headers (e.g., `Cache-Control`) for static files matching a pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      fileOptions:\n        - includePattern: '_astro/**'\n          headers:\n            - key: Cache-Control\n              value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    fileOptions: [\n      {\n        includePattern: '_astro/**',\n        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n      }\n    ]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "cdn": z.object({
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      disableInvalidationAfterDeploy: true\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(false),
          "defaultCachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Override default SSR caching behavior for all routes handled by the server.\n\n---\n\nUseful when you want to cache SSR responses longer than the framework defaults.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n          cacheKeyParameters:\n            queryString:\n              all: true\n            cookies:\n              none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 3600,\n        cacheKeyParameters: {\n          queryString: { all: true },\n          cookies: { none: true }\n        }\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "pathCachingOverrides": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `/_server-islands/*`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          -\n            path: /_nuxt/*\n            cachingOptions:\n              minTTL: 31536000\n              defaultTTL: 31536000\n              maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_nuxt/*',\n          cachingOptions: { minTTL: 31536000, defaultTTL: 31536000, maxTTL: 31536000 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```"),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .describe("#### Caching behavior override for this path pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n              minTTL: 0\n              defaultTTL: 15\n              maxTTL: 300\n              cacheKeyParameters:\n                queryString:\n                  whitelist:\n                    - page\n                    - limit\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/api/*',\n          cachingOptions: {\n            cacheMethods: ['GET', 'HEAD'],\n            minTTL: 0,\n            defaultTTL: 15,\n            maxTTL: 300,\n            cacheKeyParameters: {\n              queryString: { whitelist: ['page', 'limit'] }\n            }\n          }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```") }).strict()
          ).optional().describe("#### Override caching for specific CDN path patterns.\n\n---\n\nMatches existing framework-managed paths (e.g. `_astro/*`, `_next/data/*`) or adds\nnew server-path caching rules (e.g. `/_server-islands/*`) while preserving managed routing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              minTTL: 0\n              defaultTTL: 0\n              maxTTL: 0\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n                - OPTIONS\n              defaultTTL: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_server-islands/*',\n          cachingOptions: { minTTL: 0, defaultTTL: 0, maxTTL: 0 }\n        },\n        {\n          path: '/api/*',\n          cachingOptions: { cacheMethods: ['GET', 'HEAD', 'OPTIONS'], defaultTTL: 30 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### CDN cache controls for SSR routes and specific path patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: astro-web\n    properties:\n      cdn:\n        disableInvalidationAfterDeploy: false\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 86400\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              defaultTTL: 0\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AstroWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new AstroWeb({\n    cdn: {\n      disableInvalidationAfterDeploy: false,\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 86400\n      },\n      pathCachingOverrides: [\n        { path: '/_server-islands/*', cachingOptions: { defaultTTL: 0 } }\n      ]\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Deploy an Astro SSR app with Lambda for server rendering, S3 for static assets, and CloudFront CDN.\n\n---\n\nFor static-only Astro sites, use `hosting-bucket` with `hostingContentType: 'astro-static-website'` instead."), z.object({
      "type": z.literal("aws-cdk-construct"),
      "properties": z.object({
        "entryfilePath": z.string().describe("#### Path to the file containing your CDK construct class.\n\n---\n\nSupports `.js` and `.ts` files. The file must export a class that extends `Construct` from `aws-cdk-lib`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  notifications:\n    type: aws-cdk-construct\n    properties:\n      entryfilePath: cdk/notification-topic.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AwsCdkConstruct, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const notifications = new AwsCdkConstruct({\n    entryfilePath: 'cdk/notification-topic.ts'\n  });\n  return { resources: { notifications } };\n});\n```"),
        "exportName": z.string().optional().describe("#### Name of the exported construct class from the entry file.\n\n---\n\nMust match the exact export name. Use this when the file has multiple exports or uses named exports.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  notifications:\n    type: aws-cdk-construct\n    properties:\n      entryfilePath: cdk/constructs.ts\n      exportName: NotificationTopic\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AwsCdkConstruct, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const notifications = new AwsCdkConstruct({\n    entryfilePath: 'cdk/constructs.ts',\n    exportName: 'NotificationTopic'\n  });\n  return { resources: { notifications } };\n});\n```"),
        "constructProperties": z.any().optional().describe("#### Custom props passed to the construct's constructor.\n\n---\n\nThis object is forwarded as the third argument (`props`) to your construct class. Use `$ResourceParam()` and `$Secret()`\ndirectives here to pass dynamic values from other resources in your stack.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  notifications:\n    type: aws-cdk-construct\n    properties:\n      entryfilePath: cdk/notification-topic.ts\n      constructProperties:\n        displayName: Production Alerts\n        emailSubscription: $Secret('alerts-email')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { AwsCdkConstruct, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const notifications = new AwsCdkConstruct({\n    entryfilePath: 'cdk/notification-topic.ts',\n    constructProperties: {\n      displayName: 'Production Alerts',\n      emailSubscription: $Secret('alerts-email')\n    }\n  });\n  return { resources: { notifications } };\n});\n```").optional() }).strict()
      .optional().describe("").optional() }).strict()
    .describe("#### Embed an AWS CDK construct directly in your Stacktape stack.\n\n---\n\nEscape hatch for resources not natively supported by Stacktape. Write a CDK construct class\nin TypeScript/JavaScript and Stacktape will synthesize and deploy it as part of your stack."), z.object({
      "type": z.literal("bastion"),
      "properties": z.object({
        "instanceSize": z.string().optional().describe("#### EC2 instance type. `t3.micro` is sufficient for SSH tunneling and basic admin tasks.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('database.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.4'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      accessibility:\n        accessibilityMode: vpc\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.small\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserName: 'admin', masterUserPassword: $Secret('database.password') },\n    engine: { type: 'postgres', properties: { version: '16.4', primaryInstance: { instanceSize: 'db.t4g.micro' } } },\n    accessibility: { accessibilityMode: 'vpc' }\n  });\n  const bastion = new Bastion({\n    instanceSize: 't3.small'\n  });\n  return { resources: { database, bastion } };\n});\n```").default("t3.micro"),
        "runCommandsAtLaunch": z.array(z.string()).optional().describe("#### Shell commands to run when the instance starts (as root — no `sudo` needed).\n\n---\n\nUse to install CLI tools, database clients, or other dependencies.\n**Warning:** changing this list after creation replaces the instance — any data on the old instance is lost.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('database.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.4'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      accessibility:\n        accessibilityMode: vpc\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      runCommandsAtLaunch:\n        - yum install -y postgresql15\n        - yum install -y redis6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserName: 'admin', masterUserPassword: $Secret('database.password') },\n    engine: { type: 'postgres', properties: { version: '16.4', primaryInstance: { instanceSize: 'db.t4g.micro' } } },\n    accessibility: { accessibilityMode: 'vpc' }\n  });\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    runCommandsAtLaunch: ['yum install -y postgresql15', 'yum install -y redis6']\n  });\n  return { resources: { database, bastion } };\n});\n```").optional(),
        "logging": z.object({
          "messages": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable this log type. Stops sending to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        secure:\n          retentionDays: 180\n        audit:\n          disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      secure: { retentionDays: 180 },\n      audit: {\n        disabled: true\n      }\n    }\n  });\n  return { resources: { bastion } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### Days to keep logs in CloudWatch before automatic deletion.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        messages:\n          retentionDays: 90\n        secure:\n          retentionDays: 180\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      messages: {\n        retentionDays: 90\n      },\n      secure: { retentionDays: 180 }\n    }\n  });\n  return { resources: { bastion } };\n});\n```").optional(),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### System messages (`/var/log/messages`) — startup info, kernel messages, service logs.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        messages:\n          retentionDays: 14\n        secure:\n          retentionDays: 180\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      messages: { retentionDays: 14 },\n      secure: { retentionDays: 180 }\n    }\n  });\n  return { resources: { bastion } };\n});\n```"),
          "secure": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable this log type. Stops sending to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        secure:\n          retentionDays: 180\n        audit:\n          disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      secure: { retentionDays: 180 },\n      audit: {\n        disabled: true\n      }\n    }\n  });\n  return { resources: { bastion } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### Days to keep logs in CloudWatch before automatic deletion.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        messages:\n          retentionDays: 90\n        secure:\n          retentionDays: 180\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      messages: {\n        retentionDays: 90\n      },\n      secure: { retentionDays: 180 }\n    }\n  });\n  return { resources: { bastion } };\n});\n```").optional(),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Auth logs (`/var/log/secure`) — SSH login attempts, authentication events.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        secure:\n          retentionDays: 365\n        messages:\n          retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      secure: { retentionDays: 365 },\n      messages: { retentionDays: 30 }\n    }\n  });\n  return { resources: { bastion } };\n});\n```"),
          "audit": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable this log type. Stops sending to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        secure:\n          retentionDays: 180\n        audit:\n          disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      secure: { retentionDays: 180 },\n      audit: {\n        disabled: true\n      }\n    }\n  });\n  return { resources: { bastion } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### Days to keep logs in CloudWatch before automatic deletion.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        messages:\n          retentionDays: 90\n        secure:\n          retentionDays: 180\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      messages: {\n        retentionDays: 90\n      },\n      secure: { retentionDays: 180 }\n    }\n  });\n  return { resources: { bastion } };\n});\n```").optional(),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Audit logs (`/var/log/audit/audit.log`) — detailed security events from the Linux audit system.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        audit:\n          retentionDays: 731\n        secure:\n          retentionDays: 180\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      audit: { retentionDays: 731 },\n      secure: { retentionDays: 180 }\n    }\n  });\n  return { resources: { bastion } };\n});\n```") }).strict()
        .optional().describe("#### Log retention settings for system, security, and audit logs. Logs are sent to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  bastion:\n    type: bastion\n    properties:\n      instanceSize: t3.micro\n      logging:\n        messages:\n          retentionDays: 30\n        secure:\n          retentionDays: 180\n        audit:\n          retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bastion, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const bastion = new Bastion({\n    instanceSize: 't3.micro',\n    logging: {\n      messages: { retentionDays: 30 },\n      secure: { retentionDays: 180 },\n      audit: { retentionDays: 365 }\n    }\n  });\n  return { resources: { bastion } };\n});\n```").optional() }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Secure jump box for accessing private resources (databases, Redis, OpenSearch) in your VPC.\n\n---\n\nUses keyless SSH via AWS Systems Manager — no SSH keys to manage. Connect with `stacktape bastion:ssh`\nor create port-forwarding tunnels with `stacktape bastion:tunnel`. Costs ~$4/month (t3.micro)."), z.object({
      "type": z.literal("batch-job"),
      "properties": z.object({
        "container": z.object({
          "packaging": z.union([z.object({
              "type": z.literal("prebuilt-image"),
              "properties": z.object({
                "image": z.string().describe("#### The name or URL of the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu'\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n           command:\n             - -i\n             - input.mp4\n             - output.webm\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu',\n         command: ['-i', 'input.mp4', 'output.webm']\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```").optional() }).strict()
              .describe("#### Configures a pre-built container image.") }).strict()
            , z.object({
              "type": z.literal("custom-dockerfile"),
              "properties": z.object({
                "dockerfilePath": z.string().optional().describe("#### The path to the Dockerfile, relative to `buildContextPath`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           dockerfilePath: docker/Dockerfile.prod\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         dockerfilePath: 'docker/Dockerfile.prod'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "buildContextPath": z.string().describe("#### The path to the build context directory, relative to your Stacktape configuration file.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                "buildArgs": z.array(z.object({
                  "argName": z.string().describe("#### Argument name\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                  "value": z.string().describe("#### Argument value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```") }).strict()
                ).optional().describe("#### A list of arguments to pass to the `docker build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: NODE_ENV\n               value: production\n             - argName: BUILD_VERSION\n               value: \"1.4.2\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           { argName: 'NODE_ENV', value: 'production' },\n           { argName: 'BUILD_VERSION', value: '1.4.2' }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           command:\n             - node\n             - dist/process.js\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         command: ['node', 'dist/process.js']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built by Stacktape from a specified Dockerfile.") }).strict()
            , z.object({
              "type": z.literal("external-buildpack"),
              "properties": z.object({
                "builder": z.string().optional().describe("#### The Buildpack Builder to use.\n\n---\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           builder: paketobuildpacks/builder-jammy-full\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         builder: 'paketobuildpacks/builder-jammy-full'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "buildpacks": z.array(z.string()).optional().describe("#### The specific Buildpack to use.\n\n---\n\nBy default, the buildpack is detected automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           buildpacks:\n             - paketo-buildpacks/nodejs\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         buildpacks: ['paketo-buildpacks/nodejs']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional(),
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           command:\n             - npm\n             - run\n             - import\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         command: ['npm', 'run', 'import']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("nixpacks"),
              "properties": z.object({
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                "buildImage": z.string().optional().describe("#### The base image to use for building the application.\n\n---\n\nFor more details, see the [Nixpacks documentation](https://nixpacks.com/docs/configuration/file#build-image).\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         buildImage: ubuntu:22.04\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       buildImage: 'ubuntu:22.04'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "providers": z.array(z.string()).optional().describe("#### A list of providers to use for determining the build and runtime environments.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         providers:\n           - node\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       providers: ['node']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startCmd": z.string().optional().describe("#### The command to execute when starting the application.\n\n---\n\nThis overrides the default start command inferred by Nixpacks.\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startCmd: node dist/worker.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startCmd: 'node dist/worker.js'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startRunImage": z.string().optional().describe("#### The base image to use for running the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startRunImage: gcr.io/distroless/nodejs22-debian12\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startRunImage: 'gcr.io/distroless/nodejs22-debian12'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startOnlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in the runtime environment; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startOnlyIncludeFiles:\n           - dist\n           - node_modules\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startOnlyIncludeFiles: ['dist', 'node_modules']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "phases": z.array(z.object({
                  "name": z.string().describe("#### The name of the build phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                  "cmds": z.array(z.string()).optional().describe("#### A list of shell commands to execute in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n               - npm prune --omit=dev\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build', 'npm prune --omit=dev']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixPkgs": z.array(z.string()).optional().describe("#### A list of Nix packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - ffmpeg\n               - imagemagick\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['ffmpeg', 'imagemagick']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixLibs": z.array(z.string()).optional().describe("#### A list of Nix libraries to include in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixLibs:\n               - openssl\n               - zlib\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixLibs: ['openssl', 'zlib']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixOverlay": z.array(z.string()).optional().describe("#### A list of Nix overlay files to apply in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixOverlay:\n               - ./nix/overlay.nix\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixOverlay: ['./nix/overlay.nix']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixpkgsArchive": z.string().optional().describe("#### The Nixpkgs archive to use.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - nodejs_22\n             nixpkgsArchive: bf3287dac860542719b3849d6970d22f635f9da1\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['nodejs_22'],\n           nixpkgsArchive: 'bf3287dac860542719b3849d6970d22f635f9da1'\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "aptPkgs": z.array(z.string()).optional().describe("#### A list of APT packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             aptPkgs:\n               - libpq-dev\n               - poppler-utils\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           aptPkgs: ['libpq-dev', 'poppler-utils']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "cacheDirectories": z.array(z.string()).optional().describe("#### A list of directories to cache between builds to speed up subsequent builds.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n             cacheDirectories:\n               - node_modules/.cache\n               - /root/.npm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci'],\n           cacheDirectories: ['node_modules/.cache', '/root/.npm']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "onlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in this phase; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n             onlyIncludeFiles:\n               - src\n               - package.json\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build'],\n           onlyIncludeFiles: ['src', 'package.json']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
                ).optional().describe("#### The build phases for the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n           - name: build\n             cmds:\n               - npm run build\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         { name: 'install', cmds: ['npm ci'] },\n         { name: 'build', cmds: ['npm run build'] }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("stacktape-image-buildpack"),
              "properties": z.object({
                "languageSpecificConfig": z.union([z.object({
                    "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                    "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                    "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                  , z.object({
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                    "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                    "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                  , z.object({
                    "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                  , z.record(z.string(), z.any()), z.object({
                    "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                  , z.object({
                    "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                  , z.object({
                    "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                    "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
                ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { apiService } };\n});\n```").optional(),
                "requiresGlibcBinaries": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use glibc instead of musl (Alpine default). Enable if native dependencies require glibc.\n\n---\n\nResults in a larger image. Common packages needing this: `sharp`, `canvas`, `bcrypt`, `puppeteer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         requiresGlibcBinaries: true\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       requiresGlibcBinaries: true\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "customDockerBuildCommands": z.array(z.string()).optional().describe("#### A list of commands to be executed during the `docker build` process.\n\n---\n\nThese commands are executed using the `RUN` directive in the Dockerfile.\nThis is useful for installing additional system dependencies in your container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         customDockerBuildCommands:\n           - apt-get update && apt-get install -y poppler-utils\n           - fc-cache -f\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       customDockerBuildCommands: [\n         'apt-get update && apt-get install -y poppler-utils',\n         'fc-cache -f'\n       ]\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
                "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built automatically by Stacktape from your source code.") }).strict()
          ]).describe("#### How to build or specify the container image for this job.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  dockerJob:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: custom-dockerfile\n          properties:\n            buildContextPath: ./job\n            dockerfilePath: Dockerfile\n      resources:\n        cpu: 2\n        memory: 3840\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const dockerJob = new BatchJob({\n    container: {\n      packaging: {\n        type: 'custom-dockerfile',\n        properties: { buildContextPath: './job', dockerfilePath: 'Dockerfile' }\n      }\n    },\n    resources: { cpu: 2, memory: 3840 }\n  });\n  return { resources: { dockerJob } };\n});\n```"),
          "environment": z.array(z.object({
            "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
            "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
          ).optional().describe("#### Environment variables injected into the container at runtime.\n\n---\n\nUse `$ResourceParam()` or `$Secret()` to inject database URLs, API keys, etc.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/worker.ts\n        environment:\n          - name: DATABASE_URL\n            value: $ResourceParam('mainDb', 'connectionString')\n          - name: API_KEY\n            value: $Secret('externalApiKey')\n      resources:\n        cpu: 2\n        memory: 3840\n      connectTo:\n        - mainDb\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('dbPassword')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, RelationalDatabase, defineConfig, $ResourceParam, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('dbPassword') },\n    engine: {\n      type: 'postgres',\n      properties: { version: '16.2', primaryInstance: { instanceSize: 'db.t3.micro' } }\n    }\n  });\n  const worker = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/worker.ts' }\n      },\n      environment: {\n        DATABASE_URL: $ResourceParam('mainDb', 'connectionString'),\n        API_KEY: $Secret('externalApiKey')\n      }\n    },\n    resources: { cpu: 2, memory: 3840 },\n    connectTo: [mainDb]\n  });\n  return { resources: { mainDb, worker } };\n});\n```").optional() }).strict()
        .describe("#### Docker container image and environment for the job.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  imageProcessor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/process.ts\n        environment:\n          - name: OUTPUT_BUCKET\n            value: $ResourceParam('resultsBucket', 'name')\n      resources:\n        cpu: 2\n        memory: 3840\n      connectTo:\n        - resultsBucket\n  resultsBucket:\n    type: bucket\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, Bucket, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const resultsBucket = new Bucket({});\n  const imageProcessor = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/process.ts' }\n      },\n      environment: { OUTPUT_BUCKET: $ResourceParam('resultsBucket', 'name') }\n    },\n    resources: { cpu: 2, memory: 3840 },\n    connectTo: [resultsBucket]\n  });\n  return { resources: { resultsBucket, imageProcessor } };\n});\n```"),
        "resources": z.object({
          "cpu": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Number of vCPUs for the job (e.g., 1, 2, 4).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  computeJob:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/compute.ts\n      resources:\n        cpu: 4\n        memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const computeJob = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/compute.ts' }\n      }\n    },\n    resources: {\n      cpu: 4,\n      memory: 7680\n    }\n  });\n  return { resources: { computeJob } };\n});\n```"),
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Memory in MB. Use slightly less than powers of 2 for efficient instance sizing.\n\n---\n\nAWS reserves some memory for system processes. Requesting exactly 8192 MB (8 GB) may provision\na larger instance than needed. Use 7680 MB instead to fit on a standard 8 GB instance.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  memoryJob:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/compute.ts\n      resources:\n        cpu: 2\n        memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const memoryJob = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/compute.ts' }\n      }\n    },\n    resources: {\n      cpu: 2,\n      memory: 7680\n    }\n  });\n  return { resources: { memoryJob } };\n});\n```"),
          "gpu": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Number of GPUs. The job will run on a GPU instance (NVIDIA A100, A10G, etc.).\n\n---\n\nOmit for CPU-only workloads.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  inferenceJob:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/infer.ts\n      resources:\n        cpu: 4\n        memory: 15360\n        gpu: 1\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const inferenceJob = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/infer.ts' }\n      }\n    },\n    resources: {\n      cpu: 4,\n      memory: 15360,\n      gpu: 1\n    }\n  });\n  return { resources: { inferenceJob } };\n});\n```").optional() }).strict()
        .describe("#### CPU, memory, and GPU requirements. AWS auto-provisions a matching instance.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  videoEncoder:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/encode.ts\n      resources:\n        cpu: 4\n        memory: 15360\n        gpu: 1\n      timeout: 7200\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const videoEncoder = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/encode.ts' }\n      }\n    },\n    resources: { cpu: 4, memory: 15360, gpu: 1 },\n    timeout: 7200\n  });\n  return { resources: { videoEncoder } };\n});\n```"),
        "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max run time in seconds. The job is killed if it exceeds this, then retried if `retryConfig` is set.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  dataImporter:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/import.ts\n      resources:\n        cpu: 2\n        memory: 3840\n      timeout: 3600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const dataImporter = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/import.ts' }\n      }\n    },\n    resources: { cpu: 2, memory: 3840 },\n    timeout: 3600\n  });\n  return { resources: { dataImporter } };\n});\n```").optional(),
        "useSpotInstances": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use discounted spare AWS capacity. Saves up to 90%, but jobs can be interrupted.\n\n---\n\n**Use this when:** Your job can safely be restarted from the beginning (e.g., data imports,\nimage processing, ML training with checkpoints). Combine with `retryConfig` to auto-retry\non interruption.\n\n**Don't use when:** Your job has side effects that can't be repeated (e.g., sending emails,\ncharging payments) or must finish within a strict deadline.\n\nIf interrupted, your container gets a `SIGTERM` and 120 seconds to shut down gracefully.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mlTrainer:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/train.ts\n      resources:\n        cpu: 4\n        memory: 15360\n        gpu: 1\n      useSpotInstances: true\n      retryConfig:\n        attempts: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mlTrainer = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/train.ts' }\n      }\n    },\n    resources: { cpu: 4, memory: 15360, gpu: 1 },\n    useSpotInstances: true,\n    retryConfig: { attempts: 3 }\n  });\n  return { resources: { mlTrainer } };\n});\n```").default(false),
        "logging": z.object({
          "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  quietJob:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 1920\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const quietJob = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/job.ts' }\n      }\n    },\n    resources: { cpu: 1, memory: 1920 },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { quietJob } };\n});\n```").default(false),
          "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditJob:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/audit.ts\n      resources:\n        cpu: 1\n        memory: 1920\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditJob = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/audit.ts' }\n      }\n    },\n    resources: { cpu: 1, memory: 1920 },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditJob } };\n});\n```").default(90),
          "logForwarding": z.union([z.object({
              "type": z.literal("http-endpoint"),
              "properties": z.object({
                "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("highlight"),
              "properties": z.object({
                "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("datadog"),
              "properties": z.object({
                "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
            }).strict()
          ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Container logging (stdout/stderr). Sent to CloudWatch, viewable with `stacktape logs`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportGenerator:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/report.ts\n      resources:\n        cpu: 1\n        memory: 1920\n      logging:\n        retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportGenerator = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/report.ts' }\n      }\n    },\n    resources: { cpu: 1, memory: 1920 },\n    logging: { retentionDays: 30 }\n  });\n  return { resources: { reportGenerator } };\n});\n```").optional(),
        "retryConfig": z.object({
          "attempts": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max retry attempts before the job is marked as failed.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  batchProcessor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/process.ts\n      resources:\n        cpu: 2\n        memory: 3840\n      retryConfig:\n        attempts: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const batchProcessor = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/process.ts' }\n      }\n    },\n    resources: { cpu: 2, memory: 3840 },\n    retryConfig: {\n      attempts: 3\n    }\n  });\n  return { resources: { batchProcessor } };\n});\n```").default(1),
          "retryIntervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds to wait between retries.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  batchProcessor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/process.ts\n      resources:\n        cpu: 2\n        memory: 3840\n      retryConfig:\n        attempts: 3\n        retryIntervalSeconds: 10\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const batchProcessor = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/process.ts' }\n      }\n    },\n    resources: { cpu: 2, memory: 3840 },\n    retryConfig: {\n      attempts: 3,\n      retryIntervalSeconds: 10\n    }\n  });\n  return { resources: { batchProcessor } };\n});\n```").default(0),
          "retryIntervalMultiplier": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Multiply wait time by this factor after each retry (exponential backoff).\n\n---\n\nE.g., with `retryIntervalSeconds: 5` and `retryIntervalMultiplier: 2`, waits are 5s, 10s, 20s, etc.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  batchProcessor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/process.ts\n      resources:\n        cpu: 2\n        memory: 3840\n      retryConfig:\n        attempts: 4\n        retryIntervalSeconds: 5\n        retryIntervalMultiplier: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const batchProcessor = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/process.ts' }\n      }\n    },\n    resources: { cpu: 2, memory: 3840 },\n    retryConfig: {\n      attempts: 4,\n      retryIntervalSeconds: 5,\n      retryIntervalMultiplier: 2\n    }\n  });\n  return { resources: { batchProcessor } };\n});\n```").default(1) }).strict()
        .optional().describe("#### Auto-retry on failure, timeout, or Spot interruption.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  flakyJob:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 2\n        memory: 3840\n      retryConfig:\n        attempts: 4\n        retryIntervalSeconds: 5\n        retryIntervalMultiplier: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const flakyJob = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/job.ts' }\n      }\n    },\n    resources: { cpu: 2, memory: 3840 },\n    retryConfig: { attempts: 4, retryIntervalSeconds: 5, retryIntervalMultiplier: 2 }\n  });\n  return { resources: { flakyJob } };\n});\n```").optional(),
        "events": z.array(z.union([z.object({
            "type": z.literal("application-load-balancer").describe("#### Triggers a function when an Application Load Balancer receives a matching HTTP request.\n\n---\n\nYou can route requests based on HTTP method, path, headers, query parameters, and source IP address."),
            "properties": z.object({
              "loadBalancerName": z.string().describe("#### The name of the Application Load Balancer.\n\n---\n\nThis must reference a load balancer defined in your Stacktape configuration.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /api/users\n              - '/api/articles/*'\n            methods:\n              - GET\n              - POST\n            hosts:\n              - api.example.com\n              - '*.myapp.com'\n            headers:\n              - headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n                  - globex\n            queryParams:\n              - paramName: version\n                values:\n                  - v2\n            sourceIps:\n              - 10.0.0.0/16\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/api/users', '/api/articles/*'],\n          methods: ['GET', 'POST'],\n          hosts: ['api.example.com', '*.myapp.com'],\n          headers: [{ headerName: 'X-Tenant-Id', values: ['acme-corp', 'globex'] }],\n          queryParams: [{ paramName: 'version', values: ['v2'] }],\n          sourceIps: ['10.0.0.0/16']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
              "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The port of the load balancer listener to attach to.\n\n---\n\nYou only need to specify this if the load balancer uses custom listeners.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /api/users\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/api/users']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "priority": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The priority of this integration rule.\n\n---\n\nLoad balancer rules are evaluated in order from the lowest priority to the highest.\nThe first rule that matches an incoming request will handle it.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
              "paths": z.array(z.string()).optional().describe("#### A list of URL paths that will trigger this integration.\n\n---\n\nThe request will be routed if its path matches any of the paths in this list.\nThe comparison is case-sensitive and supports `*` and `?` wildcards.\n\nExample: `/users`, `/articles/*`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n              - '/api/articles/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users', '/api/articles/*']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "methods": z.array(z.string()).optional().describe("#### A list of HTTP methods that will trigger this integration.\n\n---\n\nExample: `GET`, `POST`, `DELETE`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            methods:\n              - GET\n              - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          methods: ['GET', 'POST']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "hosts": z.array(z.string()).optional().describe("#### A list of hostnames that will trigger this integration.\n\n---\n\nThe hostname is parsed from the `Host` header of the request.\nWildcards (`*` and `?`) are supported.\n\nExample: `api.example.com`, `*.myapp.com`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            hosts:\n              - api.example.com\n              - '*.myapp.com'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          hosts: ['api.example.com', '*.myapp.com']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "headers": z.array(z.object({
                "headerName": z.string().describe("#### The name of the HTTP header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            headers:\n              -\n                headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          headers: [\n            {\n              headerName: 'X-Tenant-Id',\n              values: ['acme-corp']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
                "values": z.array(z.string()).describe("#### A list of allowed values for the header.\n\n---\n\nThe condition is met if the header's value in the incoming request matches any of the values in this list. The comparison is case-insensitive.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            headers:\n              - headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n                  - globex\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          headers: [\n            {\n              headerName: 'X-Tenant-Id',\n              values: ['acme-corp', 'globex']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```") }).strict()
              ).optional().describe("#### A list of header conditions that the request must match.\n\n---\n\nAll header conditions must be met for the request to be routed.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            headers:\n              - headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n                  - globex\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          headers: [{ headerName: 'X-Tenant-Id', values: ['acme-corp', 'globex'] }]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "queryParams": z.array(z.object({
                "paramName": z.string().describe("#### The name of the query parameter.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            queryParams:\n              -\n                paramName: version\n                values:\n                  - v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          queryParams: [\n            {\n              paramName: 'version',\n              values: ['v2']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
                "values": z.array(z.string()).describe("#### A list of allowed values for the query parameter.\n\n---\n\nThe condition is met if the query parameter's value in the incoming request matches any of the values in this list. The comparison is case-insensitive.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            queryParams:\n              - paramName: version\n                values:\n                  - v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          queryParams: [\n            {\n              paramName: 'version',\n              values: ['v2']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```") }).strict()
              ).optional().describe("#### A list of query parameter conditions that the request must match.\n\n---\n\nAll query parameter conditions must be met for the request to be routed.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            queryParams:\n              - paramName: version\n                values:\n                  - v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          queryParams: [{ paramName: 'version', values: ['v2'] }]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
              "sourceIps": z.array(z.string()).optional().describe("#### A list of source IP addresses (in CIDR format) that are allowed to trigger this integration.\n\n---\n\n> **Note:** If the client is behind a proxy, this will be the IP address of the proxy.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            sourceIps:\n              - 10.0.0.0/16\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          sourceIps: ['10.0.0.0/16']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when an Application Load Balancer receives a matching HTTP request.\n\n---\n\nYou can route requests based on HTTP method, path, headers, query parameters, and source IP address."), z.object({
            "type": z.literal("sns"),
            "properties": z.object({
              "snsTopicName": z.string().optional().describe("#### The name of an SNS topic defined in your stack's resources.\n\n---\n\nYou must specify either `snsTopicName` or `snsTopicArn`.").optional(),
              "snsTopicArn": z.string().optional().describe("#### The ARN of an existing SNS topic.\n\n---\n\nUse this to subscribe to a topic that is not managed by your stack.\nYou must specify either `snsTopicName` or `snsTopicArn`.").optional(),
              "filterPolicy": z.any().optional().describe("#### Filter messages by attributes so only relevant ones trigger the function.\n\n---\n\nUses SNS subscription filter policy syntax. For content-based filtering, use EventBridge instead.").optional(),
              "onDeliveryFailure": z.object({
                "sqsQueueArn": z.string().optional().describe("#### The ARN of the SQS queue for failed messages.").optional(),
                "sqsQueueName": z.string().optional().describe("#### The name of an SQS queue (defined in your Stacktape configuration) for failed messages.").optional() }).strict()
              .optional().describe("#### A destination for messages that fail to be delivered to the target.\n\n---\n\nIn rare cases (e.g., if the target function cannot scale fast enough), a message might fail to be delivered.\nThis property specifies an SQS queue where failed messages will be sent.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when a new message is published to an SNS topic.\n\n---\n\nSNS is a pub/sub messaging service. Reference a topic from your stack's `snsTopics` or use an external ARN."), z.object({
            "type": z.literal("sqs"),
            "properties": z.object({
              "sqsQueueName": z.string().optional().describe("#### The name of an SQS queue defined in your stack's resources.\n\n---\n\nYou must specify either `sqsQueueName` or `sqsQueueArn`.").optional(),
              "sqsQueueArn": z.string().optional().describe("#### The ARN of an existing SQS queue.\n\n---\n\nUse this to consume messages from a queue that is not managed by your stack.\nYou must specify either `sqsQueueName` or `sqsQueueArn`.").optional(),
              "batchSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum number of records to process in a single batch.\n\n---\n\nMaximum is 10,000.").default(10),
              "maxBatchWindowSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum time (in seconds) to wait before invoking the function with a batch of records.\n\n---\n\nMaximum is 300 seconds. If not set, the function is invoked as soon as messages are available.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when new messages are available in an SQS queue.\n\n---\n\nMessages are processed in batches. The function fires when `batchSize` is reached,\n`maxBatchWindowSeconds` expires, or the 6 MB payload limit is hit.\n\n**Important:** A single SQS queue should only have one consumer function. For fan-out (multiple\nconsumers for the same message), use an SNS topic or EventBridge event bus instead."), z.object({
            "type": z.literal("kinesis-stream"),
            "properties": z.object({
              "kinesisStreamName": z.string().optional().describe("#### The name of a Kinesis stream defined in your stack's resources.\n\n---\n\nYou must specify either `kinesisStreamName` or `streamArn`.").optional(),
              "streamArn": z.string().optional().describe("#### The ARN of an existing Kinesis stream to consume records from.\n\n---\n\nUse this to consume from a stream that is not managed by your stack.\nYou must specify either `kinesisStreamName` or `streamArn`.").optional(),
              "consumerArn": z.string().optional().describe("#### The ARN of a specific stream consumer to use.\n\n---\n\nThis cannot be used with `autoCreateConsumer`.").optional(),
              "autoCreateConsumer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Automatically creates a dedicated stream consumer for this integration.\n\n---\n\nThis is recommended for minimizing latency and maximizing throughput.\nFor more details, see the [AWS documentation on stream consumers](https://docs.aws.amazon.com/streams/latest/dev/amazon-kinesis-consumers.html).\nThis cannot be used with `consumerArn`.").optional(),
              "maxBatchWindowSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum time (in seconds) to wait before invoking the function with a batch of records.\n\n---\n\nMaximum is 300 seconds.").optional(),
              "batchSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum number of records to process in a single batch.\n\n---\n\nMaximum is 10,000.").default(10),
              "startingPosition": z.enum(["LATEST","TRIM_HORIZON"]).optional().describe("#### The position in the stream from which to start reading records.\n\n---\n\n- `LATEST`: Read only new records.\n- `TRIM_HORIZON`: Read all available records from the beginning of the stream."),
              "maximumRetryAttempts": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The number of times to retry a failed batch of records.\n\n---\n\n> **Important:** If an error occurs, the entire batch is retried, including records that were processed successfully. Your function should be idempotent to handle this.").optional(),
              "onFailure": z.object({
                "arn": z.string().describe("#### The ARN of the SNS topic or SQS queue for failed batches."),
                "type": z.enum(["sns","sqs"]).describe("#### The type of the destination.") }).strict()
              .optional().describe("#### A destination (SQS queue or SNS topic) for batches that fail after all retry attempts.").optional(),
              "parallelizationFactor": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The number of batches to process concurrently from the same shard.").optional(),
              "bisectBatchOnFunctionError": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Splits a failed batch in two before retrying.\n\n---\n\nThis can be useful if a failure is caused by a batch being too large.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when new records are available in a Kinesis Data Stream.\n\n---\n\nRecords are processed in batches. Two consumption modes:\n- **Direct**: Polls each shard ~1/sec, throughput shared with other consumers.\n- **Stream Consumer** (`autoCreateConsumer`): Dedicated connection per shard — higher throughput, lower latency."), z.object({
            "type": z.literal("dynamo-db-stream"),
            "properties": z.object({
              "streamArn": z.string().describe("#### The ARN of the DynamoDB table stream."),
              "maxBatchWindowSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum time (in seconds) to wait before invoking the function with a batch of records.\n\n---\n\nMaximum is 300 seconds.").optional(),
              "batchSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum number of records to process in a single batch.\n\n---\n\nMaximum is 1,000.").default(100),
              "startingPosition": z.string().optional().describe("#### The position in the stream from which to start reading records.\n\n---\n\n- `LATEST`: Read only new records.\n- `TRIM_HORIZON`: Read all available records from the beginning of the stream.").default("TRIM_HORIZON"),
              "maximumRetryAttempts": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The number of times to retry a failed batch of records.\n\n---\n\n> **Important:** If an error occurs, the entire batch is retried, including records that were processed successfully. Your function should be idempotent to handle this.").optional(),
              "onFailure": z.object({
                "arn": z.string().describe("#### The ARN of the SNS topic or SQS queue for failed batches."),
                "type": z.enum(["sns","sqs"]).describe("#### The type of the destination.") }).strict()
              .optional().describe("#### A destination (SQS queue or SNS topic) for batches that fail after all retry attempts.").optional(),
              "parallelizationFactor": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The number of batches to process concurrently from the same shard.").optional(),
              "bisectBatchOnFunctionError": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Splits a failed batch in two before retrying.\n\n---\n\nThis can be useful if a failure is caused by a batch being too large.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when items are created, updated, or deleted in a DynamoDB table.\n\n---\n\nRecords are processed in batches. You must enable streams on the DynamoDB table first\n(set `streaming` in your `dynamoDbTables` config)."), z.object({
            "type": z.literal("s3"),
            "properties": z.object({
              "bucketArn": z.string().describe("#### The ARN of the S3 bucket to monitor for events."),
              "s3EventType": z.enum(["s3:ObjectCreated:*","s3:ObjectCreated:CompleteMultipartUpload","s3:ObjectCreated:Copy","s3:ObjectCreated:Post","s3:ObjectCreated:Put","s3:ObjectRemoved:*","s3:ObjectRemoved:Delete","s3:ObjectRemoved:DeleteMarkerCreated","s3:ObjectRestore:*","s3:ObjectRestore:Completed","s3:ObjectRestore:Post","s3:ReducedRedundancyLostObject","s3:Replication:*","s3:Replication:OperationFailedReplication","s3:Replication:OperationMissedThreshold","s3:Replication:OperationNotTracked","s3:Replication:OperationReplicatedAfterThreshold"]).describe("#### The type of S3 event that will trigger the function."),
              "filterRule": z.object({
                "prefix": z.string().optional().describe("#### The prefix that an object's key must have to trigger the function.").optional(),
                "suffix": z.string().optional().describe("#### The suffix that an object's key must have to trigger the function.").optional() }).strict()
              .optional().describe("#### A filter to apply to objects, so the function is only triggered for relevant objects.").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when files are created, deleted, or restored in an S3 bucket."), z.object({
            "type": z.literal("schedule"),
            "properties": z.object({
              "scheduleRate": z.string().describe("#### The schedule rate or cron expression.\n\n---\n\nExamples: `rate(2 hours)`, `cron(0 10 * * ? *)`"),
              "input": z.any().optional().describe("#### A fixed JSON object to be passed as the event payload.\n\n---\n\nIf you need to customize the payload based on the event, use `inputTransformer` instead.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/report.ts\n      events:\n        - type: schedule\n          properties:\n            scheduleRate: rate(1 hour)\n            input:\n              source: my-scheduled-event\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/report.ts' }),\n    events: [\n      {\n        type: 'schedule',\n        properties: {\n          scheduleRate: 'rate(1 hour)',\n          input: { source: 'my-scheduled-event' }\n        }\n      }\n    ]\n  });\n  return { resources: { reportFunction } };\n});\n```").optional(),
              "inputPath": z.string().optional().describe("#### A JSONPath expression to extract a portion of the event to pass to the target.\n\n---\n\nThis is useful for forwarding only a specific part of the event payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/report.ts\n      events:\n        - type: schedule\n          properties:\n            scheduleRate: rate(1 hour)\n            inputPath: $.detail\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/report.ts' }),\n    events: [\n      {\n        type: 'schedule',\n        properties: {\n          scheduleRate: 'rate(1 hour)',\n          inputPath: '$.detail'\n        }\n      }\n    ]\n  });\n  return { resources: { reportFunction } };\n});\n```").optional(),
              "inputTransformer": z.object({
                "inputPathsMap": z.any().optional().describe("#### A map of key-value pairs to extract from the event payload.\n\n---\n\nEach value is a JSONPath expression that extracts data from the event. These extracted values can then be used in the `inputTemplate`.").optional(),
                "inputTemplate": z.any().describe("#### A template for constructing a new event payload.\n\n---\n\nUse placeholders (`<placeholder>`) to insert the values extracted with `inputPathsMap`.") }).strict()
              .optional().describe("#### Customizes the event payload sent to the target.\n\n---\n\nThis allows you to extract values from the original event and use them to construct a new payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/report.ts\n      events:\n        - type: schedule\n          properties:\n            scheduleRate: rate(1 hour)\n            inputTransformer:\n              inputPathsMap:\n                eventTime: $.time\n              inputTemplate:\n                message: This event occurred at <eventTime>.\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/report.ts' }),\n    events: [\n      {\n        type: 'schedule',\n        properties: {\n          scheduleRate: 'rate(1 hour)',\n          inputTransformer: {\n            inputPathsMap: { eventTime: '$.time' },\n            inputTemplate: { message: 'This event occurred at <eventTime>.' }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { reportFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function on a recurring schedule (cron jobs, periodic tasks).\n\n---\n\nTwo formats:\n- **Rate**: `rate(5 minutes)`, `rate(1 hour)`, `rate(7 days)`\n- **Cron**: `cron(0 18 ? * MON-FRI *)` (6-field AWS cron, all times UTC)"), z.object({
            "type": z.literal("cloudwatch-log"),
            "properties": z.object({
              "logGroupArn": z.string().describe("#### The ARN of the log group to watch for new records."),
              "filter": z.string().optional().describe("#### A filter pattern to apply to the log records.\n\n---\n\nOnly logs that match this pattern will trigger the function.\nFor details on the syntax, see the [AWS documentation on filter and pattern syntax](https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/FilterAndPatternSyntax.html).").optional() }).strict()
          }).strict()
          .describe("#### Triggers a function when new log records appear in a CloudWatch log group.\n\n---\n\n**Note:** The event payload is base64-encoded and gzipped — you must decode and decompress it in your handler."), z.object({
            "type": z.literal("http-api-gateway"),
            "properties": z.object({
              "httpApiGatewayName": z.string().describe("#### The name of the HTTP API Gateway."),
              "method": z.enum(["*","DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"]).describe("#### The HTTP method that will trigger this integration.\n\n---\n\nYou can specify an exact method (e.g., `GET`) or use `*` to match any method."),
              "path": z.string().describe("#### The URL path that will trigger this integration.\n\n---\n\n- **Exact path**: `/users`\n- **Path with parameter**: `/users/{id}`. The `id` will be available in `event.pathParameters.id`.\n- **Greedy path**: `/files/{proxy+}`. This will match any path starting with `/files/`."),
              "authorizer": z.union([z.object({
                  "type": z.literal("cognito").describe("#### Cognito JWT authorizer\n\n---\n\nConfigures an HTTP API authorizer that validates JSON Web Tokens (JWTs) issued by a Cognito user pool.\nThis is the simplest way to protect routes when your users sign in via `user-auth-pool`.\n\nStacktape turns this into an API Gateway v2 authorizer of type `JWT` that checks the token's issuer and audience.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /me\n            authorizer:\n              type: cognito\n              properties:\n                userPoolName: authPool\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, HttpApiGateway, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authPool = new UserAuthPool({ userVerificationType: 'email-code' });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/me',\n          authorizer: {\n            type: 'cognito',\n            properties: {\n              userPoolName: 'authPool'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authPool, httpApi, apiFunction } };\n});\n```"),
                  "properties": z.object({
                    "userPoolName": z.string().describe("#### Name of the user pool to protect the API\n\n---\n\nThe Stacktape name of the `user-auth-pool` resource whose tokens should be accepted by this HTTP API authorizer.\nStacktape uses this to:\n\n- Set the expected **audience** to the user pool client ID.\n- Build the expected **issuer** URL based on the user pool and AWS region.\n\nIn practice this means only JWTs issued by this pool (and its client) will be considered valid.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /me\n            authorizer:\n              type: cognito\n              properties:\n                userPoolName: authPool\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, HttpApiGateway, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authPool = new UserAuthPool({ userVerificationType: 'email-code' });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/me',\n          authorizer: {\n            type: 'cognito',\n            properties: {\n              userPoolName: 'authPool'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authPool, httpApi, apiFunction } };\n});\n```"),
                    "identitySources": z.array(z.string()).optional().describe("#### Where to read the JWT from in the request\n\n---\n\nA list of identity sources that tell API Gateway where to look for the bearer token, using the\n`$request.*` syntax from API Gateway (for example `'$request.header.Authorization'`).\n\nIf you omit this, Stacktape defaults to reading the token from the `Authorization` HTTP header,\nusing a JWT authorizer as described in the API Gateway v2 authorizer docs\n([AWS::ApiGatewayV2::Authorizer](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-apigatewayv2-authorizer)).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /me\n            authorizer:\n              type: cognito\n              properties:\n                userPoolName: authPool\n                identitySources:\n                  - $request.header.Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, HttpApiGateway, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authPool = new UserAuthPool({ userVerificationType: 'email-code' });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/me',\n          authorizer: {\n            type: 'cognito',\n            properties: {\n              userPoolName: 'authPool',\n              identitySources: ['$request.header.Authorization']\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authPool, httpApi, apiFunction } };\n});\n```").optional() }).strict()
                }).strict()
                , z.object({
                  "type": z.literal("lambda").describe("#### Lambda-based HTTP API authorizer\n\n---\n\nConfigures an API Gateway **request** authorizer that runs a Lambda function to decide whether a request is allowed.\nThis is useful when your authorization logic can't be expressed as simple JWT validation – for example when you\ncheck API keys, look up permissions in a database, or integrate with a non-JWT identity system.\n\nStacktape creates an `AWS::ApiGatewayV2::Authorizer` of type `REQUEST` and wires it up to your Lambda.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```"),
                  "properties": z.object({
                    "functionName": z.string().describe("#### Name of the authorizer function\n\n---\n\nThe Stacktape name of a `function` resource that should run for each authorized request.\nAPI Gateway calls this Lambda, passes request details, and uses its response to allow or deny access.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```"),
                    "iamResponse": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use IAM-style (v1) authorizer responses\n\n---\n\n- If `true`, your Lambda must return a full IAM policy document (the \"v1\" format).\n- If `false` or omitted, Stacktape enables **simple responses** (the HTTP API v2 payload format)\n  so your Lambda can return a small JSON object with an `isAuthorized` flag and optional context.\n\nThis flag is wired to `EnableSimpleResponses` on the underlying `AWS::ApiGatewayV2::Authorizer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n                iamResponse: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction',\n              iamResponse: true\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```").optional(),
                    "identitySources": z.array(z.string()).optional().describe("#### Where to read identity data from\n\n---\n\nA list of request fields API Gateway should pass into your Lambda authorizer (for example headers, query parameters,\nor stage variables) using the `$request.*` syntax.\n\nWhen left empty, no specific identity sources are configured and your Lambda must inspect the incoming event directly.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n                identitySources:\n                  - $request.header.Authorization\n                  - $request.querystring.apiKey\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction',\n              identitySources: ['$request.header.Authorization', '$request.querystring.apiKey']\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```").optional(),
                    "cacheResultSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Cache authorizer results\n\n---\n\nNumber of seconds API Gateway should cache the result of the Lambda authorizer for a given identity.\nWhile cached, repeated requests skip calling your authorizer function and reuse the previous result.\n\nThis value is applied to `AuthorizerResultTtlInSeconds`. If omitted, Stacktape sets it to `0` (no caching).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n                identitySources:\n                  - $request.header.Authorization\n                cacheResultSeconds: 300\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction',\n              identitySources: ['$request.header.Authorization'],\n              cacheResultSeconds: 300\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```").optional() }).strict()
                }).strict()
              ]).optional().describe("#### An authorizer to protect this route.\n\n---\n\nUnauthorized requests will be rejected with a `401 Unauthorized` response.").optional(),
              "payloadFormat": z.enum(["1.0","2.0"]).optional().describe("#### The payload format version for the Lambda integration.\n\n---\n\nFor details on the differences between formats, see the [AWS documentation](https://docs.aws.amazon.com/apigateway/latest/developerguide/http-api-develop-integrations-lambda.html).") }).strict()
          }).strict()
          .describe("#### Triggers a function when an HTTP API Gateway receives a matching request.\n\n---\n\nRoutes are matched by specificity — exact paths take priority over wildcard paths."), z.object({
            "type": z.literal("event-bus"),
            "properties": z.object({
              "eventBusArn": z.string().optional().describe("#### The ARN of an existing event bus.\n\n---\n\nUse this to subscribe to an event bus that is not managed by your stack.\nYou must specify only one of `eventBusArn`, `eventBusName`, or `useDefaultBus`.").optional(),
              "eventBusName": z.string().optional().describe("#### The name of an event bus defined in your stack's resources.\n\n---\n\nYou must specify only one of `eventBusArn`, `eventBusName`, or `useDefaultBus`.").optional(),
              "useDefaultBus": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Uses the default AWS event bus.\n\n---\n\nYou must specify only one of `eventBusArn`, `eventBusName`, or `useDefaultBus`.").optional(),
              "eventPattern": z.object({
                "version": z.any().optional().describe("#### Filter by event version.").optional(), "detail-type": z.any().optional().describe("#### Filter by event detail-type (e.g., `[\"OrderPlaced\"]`). This is the primary field for routing custom events.").optional(),
                "source": z.any().optional().describe("#### Filter by event source (e.g., `[\"my-app\"]` or `[\"aws.ec2\"]` for AWS service events).").optional(),
                "account": z.any().optional().describe("#### Filter by AWS account ID.").optional(),
                "region": z.any().optional().describe("#### Filter by AWS region.").optional(),
                "resources": z.any().optional().describe("#### Filter by resource ARNs.").optional(),
                "detail": z.any().optional().describe("#### Filter by event payload content. Supports nested matching, prefix/suffix, numeric comparisons.").optional(), "replay-name": z.any().optional().describe("#### Filter by replay name (only present on replayed events).").optional() }).strict()
              .describe("#### A pattern to filter events from the event bus.\n\n---\n\nOnly events that match this pattern will trigger the target.\nFor details on the syntax, see the [AWS EventBridge documentation on event patterns](https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-event-patterns.html)."),
              "onDeliveryFailure": z.object({
                "sqsQueueArn": z.string().optional().describe("#### The ARN of the SQS queue for failed events.").optional(),
                "sqsQueueName": z.string().optional().describe("#### The name of an SQS queue (defined in your Stacktape configuration) for failed events.").optional() }).strict()
              .optional().describe("#### A destination for events that fail to be delivered to the target.\n\n---\n\nIn rare cases, an event might fail to be delivered. This property specifies an SQS queue where failed events will be sent.").optional(),
              "input": z.any().optional().describe("#### A fixed JSON object to be passed as the event payload.\n\n---\n\nIf you need to customize the payload based on the event, use `inputTransformer` instead.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n            input:\n              source: my-custom-event\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EventBus, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n  const orderProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/process-order.ts' }),\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] },\n          input: { source: 'my-custom-event' }\n        }\n      }\n    ]\n  });\n  return { resources: { orderEvents, orderProcessor } };\n});\n```").optional(),
              "inputPath": z.string().optional().describe("#### A JSONPath expression to extract a portion of the event to pass to the target.\n\n---\n\nThis is useful for forwarding only a specific part of the event payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n            inputPath: $.detail\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EventBus, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n  const orderProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/process-order.ts' }),\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] },\n          inputPath: '$.detail'\n        }\n      }\n    ]\n  });\n  return { resources: { orderEvents, orderProcessor } };\n});\n```").optional(),
              "inputTransformer": z.object({
                "inputPathsMap": z.any().optional().describe("#### A map of key-value pairs to extract from the event payload.\n\n---\n\nEach value is a JSONPath expression that extracts data from the event. These extracted values can then be used in the `inputTemplate`.").optional(),
                "inputTemplate": z.any().describe("#### A template for constructing a new event payload.\n\n---\n\nUse placeholders (`<placeholder>`) to insert the values extracted with `inputPathsMap`.") }).strict()
              .optional().describe("#### Customizes the event payload sent to the target.\n\n---\n\nThis allows you to extract values from the original event and use them to construct a new payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n            inputTransformer:\n              inputPathsMap:\n                instanceId: $.detail.instance-id\n                instanceState: $.detail.state\n              inputTemplate:\n                message: Instance <instanceId> is now in state <instanceState>.\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EventBus, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n  const orderProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/process-order.ts' }),\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] },\n          inputTransformer: {\n            inputPathsMap: {\n              instanceId: '$.detail.instance-id',\n              instanceState: '$.detail.state'\n            },\n            inputTemplate: { message: 'Instance <instanceId> is now in state <instanceState>.' }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { orderEvents, orderProcessor } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Triggers a batch job when an event matching a specified pattern is received by an event bus.\n\n---\n\nYou can use a custom event bus or the default AWS event bus.")])).optional().describe("#### Events that trigger this job (schedules, HTTP requests, S3 uploads, SQS messages, etc.).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  uploadProcessor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/onUpload.ts\n      resources:\n        cpu: 2\n        memory: 3840\n      events:\n        - type: s3\n          properties:\n            bucketArn: $ResourceParam('uploadsBucket', 'arn')\n            s3EventType: 's3:ObjectCreated:*'\n        - type: schedule\n          properties:\n            scheduleRate: rate(1 hour)\n      connectTo:\n        - uploadsBucket\n  uploadsBucket:\n    type: bucket\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, Bucket, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadsBucket = new Bucket({});\n  const uploadProcessor = new BatchJob({\n    container: {\n      packaging: {\n        type: 'stacktape-image-buildpack',\n        properties: { entryfilePath: 'src/onUpload.ts' }\n      }\n    },\n    resources: { cpu: 2, memory: 3840 },\n    events: [\n      {\n        type: 's3',\n        properties: {\n          bucketArn: $ResourceParam('uploadsBucket', 'arn'),\n          s3EventType: 's3:ObjectCreated:*'\n        }\n      },\n      { type: 'schedule', properties: { scheduleRate: 'rate(1 hour)' } }\n    ],\n    connectTo: [uploadsBucket]\n  });\n  return { resources: { uploadsBucket, uploadProcessor } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Run containerized tasks to completion — data processing, ML training, video encoding, etc.\n\n---\n\nPay only for the compute time used. Supports CPU and GPU workloads, retries on failure,\nand can be triggered by schedules, HTTP requests, S3 uploads, or queue messages."), z.object({
      "type": z.literal("multi-container-workload"),
      "properties": z.object({
        "containers": z.array(z.object({
          "events": z.array(z.union([z.object({
              "type": z.literal("application-load-balancer"),
              "properties": z.object({
                "containerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The container port that will receive traffic from the load balancer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  appWorkload:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: web\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/web-app:latest'\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: publicAlb\n                priority: 1\n                paths:\n                  - '/*'\n                containerPort: 8080\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const appWorkload = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'web',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/web-app:latest' } },\n        events: [\n          {\n            type: 'application-load-balancer',\n            properties: {\n              loadBalancerName: 'publicAlb',\n              priority: 1,\n              paths: ['/*'],\n              containerPort: 8080\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { publicAlb, appWorkload } };\n});\n```"),
                "loadBalancerName": z.string().describe("#### The name of the Application Load Balancer.\n\n---\n\nThis must reference a load balancer defined in your Stacktape configuration.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /api/users\n              - '/api/articles/*'\n            methods:\n              - GET\n              - POST\n            hosts:\n              - api.example.com\n              - '*.myapp.com'\n            headers:\n              - headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n                  - globex\n            queryParams:\n              - paramName: version\n                values:\n                  - v2\n            sourceIps:\n              - 10.0.0.0/16\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/api/users', '/api/articles/*'],\n          methods: ['GET', 'POST'],\n          hosts: ['api.example.com', '*.myapp.com'],\n          headers: [{ headerName: 'X-Tenant-Id', values: ['acme-corp', 'globex'] }],\n          queryParams: [{ paramName: 'version', values: ['v2'] }],\n          sourceIps: ['10.0.0.0/16']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
                "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The port of the load balancer listener to attach to.\n\n---\n\nYou only need to specify this if the load balancer uses custom listeners.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            listenerPort: 443\n            priority: 1\n            paths:\n              - /api/users\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          listenerPort: 443,\n          priority: 1,\n          paths: ['/api/users']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
                "priority": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The priority of this integration rule.\n\n---\n\nLoad balancer rules are evaluated in order from the lowest priority to the highest.\nThe first rule that matches an incoming request will handle it.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
                "paths": z.array(z.string()).optional().describe("#### A list of URL paths that will trigger this integration.\n\n---\n\nThe request will be routed if its path matches any of the paths in this list.\nThe comparison is case-sensitive and supports `*` and `?` wildcards.\n\nExample: `/users`, `/articles/*`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n              - '/api/articles/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users', '/api/articles/*']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
                "methods": z.array(z.string()).optional().describe("#### A list of HTTP methods that will trigger this integration.\n\n---\n\nExample: `GET`, `POST`, `DELETE`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            methods:\n              - GET\n              - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          methods: ['GET', 'POST']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
                "hosts": z.array(z.string()).optional().describe("#### A list of hostnames that will trigger this integration.\n\n---\n\nThe hostname is parsed from the `Host` header of the request.\nWildcards (`*` and `?`) are supported.\n\nExample: `api.example.com`, `*.myapp.com`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            hosts:\n              - api.example.com\n              - '*.myapp.com'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          hosts: ['api.example.com', '*.myapp.com']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
                "headers": z.array(z.object({
                  "headerName": z.string().describe("#### The name of the HTTP header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            headers:\n              -\n                headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          headers: [\n            {\n              headerName: 'X-Tenant-Id',\n              values: ['acme-corp']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
                  "values": z.array(z.string()).describe("#### A list of allowed values for the header.\n\n---\n\nThe condition is met if the header's value in the incoming request matches any of the values in this list. The comparison is case-insensitive.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            headers:\n              - headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n                  - globex\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          headers: [\n            {\n              headerName: 'X-Tenant-Id',\n              values: ['acme-corp', 'globex']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```") }).strict()
                ).optional().describe("#### A list of header conditions that the request must match.\n\n---\n\nAll header conditions must be met for the request to be routed.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            headers:\n              - headerName: X-Tenant-Id\n                values:\n                  - acme-corp\n                  - globex\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          headers: [{ headerName: 'X-Tenant-Id', values: ['acme-corp', 'globex'] }]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
                "queryParams": z.array(z.object({
                  "paramName": z.string().describe("#### The name of the query parameter.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            queryParams:\n              -\n                paramName: version\n                values:\n                  - v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          queryParams: [\n            {\n              paramName: 'version',\n              values: ['v2']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```"),
                  "values": z.array(z.string()).describe("#### A list of allowed values for the query parameter.\n\n---\n\nThe condition is met if the query parameter's value in the incoming request matches any of the values in this list. The comparison is case-insensitive.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            queryParams:\n              - paramName: version\n                values:\n                  - v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          queryParams: [\n            {\n              paramName: 'version',\n              values: ['v2']\n            }\n          ]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```") }).strict()
                ).optional().describe("#### A list of query parameter conditions that the request must match.\n\n---\n\nAll query parameter conditions must be met for the request to be routed.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            queryParams:\n              - paramName: version\n                values:\n                  - v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          queryParams: [{ paramName: 'version', values: ['v2'] }]\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional(),
                "sourceIps": z.array(z.string()).optional().describe("#### A list of source IP addresses (in CIDR format) that are allowed to trigger this integration.\n\n---\n\n> **Note:** If the client is behind a proxy, this will be the IP address of the proxy.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicAlb:\n    type: application-load-balancer\n    properties: {}\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: application-load-balancer\n          properties:\n            loadBalancerName: publicAlb\n            priority: 1\n            paths:\n              - /api/users\n            sourceIps:\n              - 10.0.0.0/16\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, ApplicationLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicAlb = new ApplicationLoadBalancer({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'application-load-balancer',\n        properties: {\n          loadBalancerName: 'publicAlb',\n          priority: 1,\n          paths: ['/api/users'],\n          sourceIps: ['10.0.0.0/16']\n        }\n      }\n    ]\n  });\n  return { resources: { publicAlb, apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Triggers a container when a request matches the specified conditions on an Application Load Balancer.\n\n---\n\nYou can route requests based on HTTP method, path, headers, query parameters, and source IP address."), z.object({
              "type": z.literal("http-api-gateway"),
              "properties": z.object({
                "containerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The container port that will receive traffic from the API Gateway.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  publicApi:\n    type: http-api-gateway\n    properties: {}\n  appWorkload:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: web\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/web-app:latest'\n          events:\n            - type: http-api-gateway\n              properties:\n                httpApiGatewayName: publicApi\n                method: '*'\n                path: '/{proxy+}'\n                containerPort: 8080\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, HttpApiGateway, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const publicApi = new HttpApiGateway({});\n  const appWorkload = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'web',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/web-app:latest' } },\n        events: [\n          {\n            type: 'http-api-gateway',\n            properties: {\n              httpApiGatewayName: 'publicApi',\n              method: '*',\n              path: '/{proxy+}',\n              containerPort: 8080\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { publicApi, appWorkload } };\n});\n```"),
                "httpApiGatewayName": z.string().describe("#### The name of the HTTP API Gateway."),
                "method": z.enum(["*","DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"]).describe("#### The HTTP method that will trigger this integration.\n\n---\n\nYou can specify an exact method (e.g., `GET`) or use `*` to match any method."),
                "path": z.string().describe("#### The URL path that will trigger this integration.\n\n---\n\n- **Exact path**: `/users`\n- **Path with parameter**: `/users/{id}`. The `id` will be available in `event.pathParameters.id`.\n- **Greedy path**: `/files/{proxy+}`. This will match any path starting with `/files/`."),
                "authorizer": z.union([z.object({
                    "type": z.literal("cognito").describe("#### Cognito JWT authorizer\n\n---\n\nConfigures an HTTP API authorizer that validates JSON Web Tokens (JWTs) issued by a Cognito user pool.\nThis is the simplest way to protect routes when your users sign in via `user-auth-pool`.\n\nStacktape turns this into an API Gateway v2 authorizer of type `JWT` that checks the token's issuer and audience.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /me\n            authorizer:\n              type: cognito\n              properties:\n                userPoolName: authPool\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, HttpApiGateway, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authPool = new UserAuthPool({ userVerificationType: 'email-code' });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/me',\n          authorizer: {\n            type: 'cognito',\n            properties: {\n              userPoolName: 'authPool'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authPool, httpApi, apiFunction } };\n});\n```"),
                    "properties": z.object({
                      "userPoolName": z.string().describe("#### Name of the user pool to protect the API\n\n---\n\nThe Stacktape name of the `user-auth-pool` resource whose tokens should be accepted by this HTTP API authorizer.\nStacktape uses this to:\n\n- Set the expected **audience** to the user pool client ID.\n- Build the expected **issuer** URL based on the user pool and AWS region.\n\nIn practice this means only JWTs issued by this pool (and its client) will be considered valid.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /me\n            authorizer:\n              type: cognito\n              properties:\n                userPoolName: authPool\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, HttpApiGateway, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authPool = new UserAuthPool({ userVerificationType: 'email-code' });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/me',\n          authorizer: {\n            type: 'cognito',\n            properties: {\n              userPoolName: 'authPool'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authPool, httpApi, apiFunction } };\n});\n```"),
                      "identitySources": z.array(z.string()).optional().describe("#### Where to read the JWT from in the request\n\n---\n\nA list of identity sources that tell API Gateway where to look for the bearer token, using the\n`$request.*` syntax from API Gateway (for example `'$request.header.Authorization'`).\n\nIf you omit this, Stacktape defaults to reading the token from the `Authorization` HTTP header,\nusing a JWT authorizer as described in the API Gateway v2 authorizer docs\n([AWS::ApiGatewayV2::Authorizer](https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-apigatewayv2-authorizer)).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authPool:\n    type: user-auth-pool\n    properties:\n      userVerificationType: email-code\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /me\n            authorizer:\n              type: cognito\n              properties:\n                userPoolName: authPool\n                identitySources:\n                  - $request.header.Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UserAuthPool, HttpApiGateway, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authPool = new UserAuthPool({ userVerificationType: 'email-code' });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/me',\n          authorizer: {\n            type: 'cognito',\n            properties: {\n              userPoolName: 'authPool',\n              identitySources: ['$request.header.Authorization']\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authPool, httpApi, apiFunction } };\n});\n```").optional() }).strict()
                  }).strict()
                  , z.object({
                    "type": z.literal("lambda").describe("#### Lambda-based HTTP API authorizer\n\n---\n\nConfigures an API Gateway **request** authorizer that runs a Lambda function to decide whether a request is allowed.\nThis is useful when your authorization logic can't be expressed as simple JWT validation – for example when you\ncheck API keys, look up permissions in a database, or integrate with a non-JWT identity system.\n\nStacktape creates an `AWS::ApiGatewayV2::Authorizer` of type `REQUEST` and wires it up to your Lambda.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```"),
                    "properties": z.object({
                      "functionName": z.string().describe("#### Name of the authorizer function\n\n---\n\nThe Stacktape name of a `function` resource that should run for each authorized request.\nAPI Gateway calls this Lambda, passes request details, and uses its response to allow or deny access.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction'\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```"),
                      "iamResponse": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use IAM-style (v1) authorizer responses\n\n---\n\n- If `true`, your Lambda must return a full IAM policy document (the \"v1\" format).\n- If `false` or omitted, Stacktape enables **simple responses** (the HTTP API v2 payload format)\n  so your Lambda can return a small JSON object with an `isAuthorized` flag and optional context.\n\nThis flag is wired to `EnableSimpleResponses` on the underlying `AWS::ApiGatewayV2::Authorizer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n                iamResponse: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction',\n              iamResponse: true\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```").optional(),
                      "identitySources": z.array(z.string()).optional().describe("#### Where to read identity data from\n\n---\n\nA list of request fields API Gateway should pass into your Lambda authorizer (for example headers, query parameters,\nor stage variables) using the `$request.*` syntax.\n\nWhen left empty, no specific identity sources are configured and your Lambda must inspect the incoming event directly.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n                identitySources:\n                  - $request.header.Authorization\n                  - $request.querystring.apiKey\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction',\n              identitySources: ['$request.header.Authorization', '$request.querystring.apiKey']\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```").optional(),
                      "cacheResultSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Cache authorizer results\n\n---\n\nNumber of seconds API Gateway should cache the result of the Lambda authorizer for a given identity.\nWhile cached, repeated requests skip calling your authorizer function and reuse the previous result.\n\nThis value is applied to `AuthorizerResultTtlInSeconds`. If omitted, Stacktape sets it to `0` (no caching).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authorizerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/authorizer.ts\n  httpApi:\n    type: http-api-gateway\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: httpApi\n            method: GET\n            path: /orders\n            authorizer:\n              type: lambda\n              properties:\n                functionName: authorizerFunction\n                identitySources:\n                  - $request.header.Authorization\n                cacheResultSeconds: 300\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authorizerFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/authorizer.ts' } }\n  });\n  const httpApi = new HttpApiGateway({});\n  const apiFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/api.ts' } },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: {\n          httpApiGatewayName: 'httpApi',\n          method: 'GET',\n          path: '/orders',\n          authorizer: {\n            type: 'lambda',\n            properties: {\n              functionName: 'authorizerFunction',\n              identitySources: ['$request.header.Authorization'],\n              cacheResultSeconds: 300\n            }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { authorizerFunction, httpApi, apiFunction } };\n});\n```").optional() }).strict()
                  }).strict()
                ]).optional().describe("#### An authorizer to protect this route.\n\n---\n\nUnauthorized requests will be rejected with a `401 Unauthorized` response.").optional(),
                "payloadFormat": z.enum(["1.0","2.0"]).optional().describe("#### The payload format version for the Lambda integration.\n\n---\n\nFor details on the differences between formats, see the [AWS documentation](https://docs.aws.amazon.com/apigateway/latest/developerguide/http-api-develop-integrations-lambda.html).") }).strict()
            }).strict()
            .describe("#### Triggers a container when an HTTP API Gateway receives a matching request.\n\n---\n\nYou can route requests based on HTTP method and path."), z.object({
              "type": z.literal("workload-internal"),
              "properties": z.object({
                "containerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The container port to open for internal traffic.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  appWorkload:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: web\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/web-app:latest'\n        - name: metrics\n          essential: false\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/metrics-sidecar:latest'\n          events:\n            - type: workload-internal\n              properties:\n                containerPort: 9090\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const appWorkload = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'web',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/web-app:latest' } }\n      },\n      {\n        name: 'metrics',\n        essential: false,\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/metrics-sidecar:latest' } },\n        events: [\n          {\n            type: 'workload-internal',\n            properties: {\n              containerPort: 9090\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { appWorkload } };\n});\n```") }).strict()
            }).strict()
            .describe("#### Opens a container port for connections from other containers within the same workload."), z.object({
              "type": z.literal("service-connect"),
              "properties": z.object({
                "containerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The container port to open for service-to-service communication.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  appWorkload:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: web\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/web-app:latest'\n          events:\n            - type: service-connect\n              properties:\n                containerPort: 8080\n                alias: web-api\n                protocol: http\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const appWorkload = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'web',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/web-app:latest' } },\n        events: [\n          {\n            type: 'service-connect',\n            properties: {\n              containerPort: 8080,\n              alias: 'web-api',\n              protocol: 'http'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { appWorkload } };\n});\n```"),
                "alias": z.string().optional().describe("#### An alias for this service, used for service discovery.\n\n---\n\nOther resources in the stack can connect to this service using a URL like `protocol://alias:port` (e.g., `http://my-service:8080`).\nBy default, the alias is derived from the resource and container names (e.g., `my-resource-my-container`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  appWorkload:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: web\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/web-app:latest'\n          events:\n            - type: service-connect\n              properties:\n                containerPort: 8080\n                alias: web-api\n                protocol: http\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const appWorkload = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'web',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/web-app:latest' } },\n        events: [\n          {\n            type: 'service-connect',\n            properties: {\n              containerPort: 8080,\n              alias: 'web-api',\n              protocol: 'http'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { appWorkload } };\n});\n```").optional(),
                "protocol": z.enum(["grpc","http","http2"]).optional().describe("#### The protocol used for service-to-service communication.\n\n---\n\nSpecifying the protocol allows AWS to capture protocol-specific metrics, such as the number of HTTP 5xx errors.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  appWorkload:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: web\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/web-app:latest'\n          events:\n            - type: service-connect\n              properties:\n                containerPort: 8080\n                alias: web-api\n                protocol: http\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const appWorkload = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'web',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/web-app:latest' } },\n        events: [\n          {\n            type: 'service-connect',\n            properties: {\n              containerPort: 8080,\n              alias: 'web-api',\n              protocol: 'http'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { appWorkload } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Opens a container port for connections from other compute resources in the same stack."), z.object({
              "type": z.literal("network-load-balancer"),
              "properties": z.object({
                "containerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The container port that will receive traffic from the load balancer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpNlb:\n    type: network-load-balancer\n    properties:\n      listeners:\n        - port: 5432\n          protocol: TCP\n  proxyWorkload:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.25\n        memory: 512\n      containers:\n        - name: proxy\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/tcp-proxy:latest'\n          events:\n            - type: network-load-balancer\n              properties:\n                loadBalancerName: tcpNlb\n                listenerPort: 5432\n                containerPort: 5432\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, NetworkLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const tcpNlb = new NetworkLoadBalancer({ listeners: [{ port: 5432, protocol: 'TCP' }] });\n  const proxyWorkload = new MultiContainerWorkload({\n    resources: { cpu: 0.25, memory: 512 },\n    containers: [\n      {\n        name: 'proxy',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/tcp-proxy:latest' } },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: {\n              loadBalancerName: 'tcpNlb',\n              listenerPort: 5432,\n              containerPort: 5432\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { tcpNlb, proxyWorkload } };\n});\n```"),
                "loadBalancerName": z.string().describe("#### The name of the Network Load Balancer.\n\n---\n\nThis must reference a load balancer defined in your Stacktape configuration.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpNlb:\n    type: network-load-balancer\n    properties:\n      listeners:\n        - port: 5432\n          protocol: TCP\n  proxyWorkload:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.25\n        memory: 512\n      containers:\n        - name: proxy\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/tcp-proxy:latest'\n          events:\n            - type: network-load-balancer\n              properties:\n                loadBalancerName: tcpNlb\n                listenerPort: 5432\n                containerPort: 5432\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, NetworkLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const tcpNlb = new NetworkLoadBalancer({ listeners: [{ port: 5432, protocol: 'TCP' }] });\n  const proxyWorkload = new MultiContainerWorkload({\n    resources: { cpu: 0.25, memory: 512 },\n    containers: [\n      {\n        name: 'proxy',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/tcp-proxy:latest' } },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: {\n              loadBalancerName: 'tcpNlb',\n              listenerPort: 5432,\n              containerPort: 5432\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { tcpNlb, proxyWorkload } };\n});\n```"),
                "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The port of the listener that will forward traffic to this integration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpNlb:\n    type: network-load-balancer\n    properties:\n      listeners:\n        - port: 5432\n          protocol: TCP\n  proxyWorkload:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.25\n        memory: 512\n      containers:\n        - name: proxy\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: 'my-org/tcp-proxy:latest'\n          events:\n            - type: network-load-balancer\n              properties:\n                loadBalancerName: tcpNlb\n                listenerPort: 5432\n                containerPort: 5432\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, NetworkLoadBalancer, defineConfig } from 'stacktape';\nexport default defineConfig(() => {\n  const tcpNlb = new NetworkLoadBalancer({ listeners: [{ port: 5432, protocol: 'TCP' }] });\n  const proxyWorkload = new MultiContainerWorkload({\n    resources: { cpu: 0.25, memory: 512 },\n    containers: [\n      {\n        name: 'proxy',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/tcp-proxy:latest' } },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: {\n              loadBalancerName: 'tcpNlb',\n              listenerPort: 5432,\n              containerPort: 5432\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { tcpNlb, proxyWorkload } };\n});\n```") }).strict()
            }).strict()
            .describe("#### Triggers a container when a request is made to a Network Load Balancer.\n\n---\n\nA Network Load Balancer operates at the transport layer (Layer 4) and can handle TCP and TLS traffic.")])).optional().describe("#### How this container receives traffic (API Gateway, load balancer, or service-connect).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                paths:\n                  - /*\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n  appLb:\n    type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          {\n            type: 'application-load-balancer',\n            properties: { loadBalancerName: 'appLb', priority: 1, paths: ['/*'], containerPort: 3000 }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, appLb } };\n});\n```").optional(),
          "loadBalancerHealthCheck": z.object({
            "healthcheckPath": z.string().optional().describe("#### Path the load balancer pings to check container health.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n          loadBalancerHealthCheck:\n            healthcheckPath: /health\n      resources:\n        cpu: 0.5\n        memory: 1024\n  appLb:\n    type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ],\n        loadBalancerHealthCheck: {\n          healthcheckPath: '/health'\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, appLb } };\n});\n```").default("/"),
            "healthcheckInterval": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n          loadBalancerHealthCheck:\n            healthcheckPath: /health\n            healthcheckInterval: 15\n      resources:\n        cpu: 0.5\n        memory: 1024\n  appLb:\n    type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ],\n        loadBalancerHealthCheck: {\n          healthcheckPath: '/health',\n          healthcheckInterval: 15\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, appLb } };\n});\n```").default(5),
            "healthcheckTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a health check is considered failed.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n          loadBalancerHealthCheck:\n            healthcheckPath: /health\n            healthcheckTimeout: 3\n      resources:\n        cpu: 0.5\n        memory: 1024\n  appLb:\n    type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ],\n        loadBalancerHealthCheck: {\n          healthcheckPath: '/health',\n          healthcheckTimeout: 3\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, appLb } };\n});\n```").default(4),
            "healthCheckProtocol": z.enum(["HTTP","TCP"]).optional().describe("#### Health check protocol. ALB defaults to `HTTP`, NLB defaults to `TCP`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n          loadBalancerHealthCheck:\n            healthCheckProtocol: HTTP\n            healthcheckPath: /health\n      resources:\n        cpu: 0.5\n        memory: 1024\n  appLb:\n    type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ],\n        loadBalancerHealthCheck: {\n          healthCheckProtocol: 'HTTP',\n          healthcheckPath: '/health'\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, appLb } };\n});\n```").optional(),
            "healthCheckPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Health check port. Defaults to the traffic port.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n          loadBalancerHealthCheck:\n            healthcheckPath: /health\n            healthCheckPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n  appLb:\n    type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ],\n        loadBalancerHealthCheck: {\n          healthcheckPath: '/health',\n          healthCheckPort: 3000\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, appLb } };\n});\n```").optional() }).strict()
          .optional().describe("#### Load balancer health check settings. Only applies when integrated with an ALB or NLB.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n          loadBalancerHealthCheck:\n            healthcheckPath: /health\n            healthcheckInterval: 10\n      resources:\n        cpu: 0.5\n        memory: 1024\n  appLb:\n    type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ],\n        loadBalancerHealthCheck: { healthcheckPath: '/health', healthcheckInterval: 10 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, appLb } };\n});\n```").optional(),
          "name": z.string().describe("#### Unique container name within this workload.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' })\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "packaging": z.union([z.object({
              "type": z.literal("prebuilt-image"),
              "properties": z.object({
                "repositoryCredentialsSecretArn": z.string().optional().describe("#### The ARN of a secret containing credentials for a private container registry.\n\n---\n\nThe secret must be a JSON object with `username` and `password` keys.\nYou can create secrets using the `stacktape secret:create` command.\n\n**Example (YAML):**\n\n```yaml\nresources:\n privateImageService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: registry.example.com/my-org/my-app:latest\n         repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const privateImageService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'registry.example.com/my-org/my-app:latest',\n       repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { privateImageService } };\n});\n```").optional(),
                "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: node:22-alpine\n         entryPoint:\n           - /bin/sh\n           - -c\n           - node server.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'node:22-alpine',\n       entryPoint: ['/bin/sh', '-c', 'node server.js']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
                "image": z.string().describe("#### The name or URL of the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu'\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n           command:\n             - -i\n             - input.mp4\n             - output.webm\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu',\n         command: ['-i', 'input.mp4', 'output.webm']\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```").optional() }).strict()
              .describe("#### Configures a pre-built container image.") }).strict()
            .describe("#### Uses a pre-built container image.\n\n---\n\nWith `prebuilt-image`, you provide a reference to an existing container image.\nThis can be a public image from Docker Hub or a private image from any container registry.\n\nFor private registries, configure `repositoryCredentialsSecretArn` with credentials stored in AWS Secrets Manager."), z.object({
              "type": z.literal("custom-dockerfile"),
              "properties": z.object({
                "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: custom-dockerfile\n       properties:\n         buildContextPath: ./app\n         entryPoint:\n           - /app/entrypoint.sh\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'custom-dockerfile',\n     properties: {\n       buildContextPath: './app',\n       entryPoint: ['/app/entrypoint.sh']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
                "dockerfilePath": z.string().optional().describe("#### The path to the Dockerfile, relative to `buildContextPath`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           dockerfilePath: docker/Dockerfile.prod\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         dockerfilePath: 'docker/Dockerfile.prod'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "buildContextPath": z.string().describe("#### The path to the build context directory, relative to your Stacktape configuration file.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                "buildArgs": z.array(z.object({
                  "argName": z.string().describe("#### Argument name\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                  "value": z.string().describe("#### Argument value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```") }).strict()
                ).optional().describe("#### A list of arguments to pass to the `docker build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: NODE_ENV\n               value: production\n             - argName: BUILD_VERSION\n               value: \"1.4.2\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           { argName: 'NODE_ENV', value: 'production' },\n           { argName: 'BUILD_VERSION', value: '1.4.2' }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           command:\n             - node\n             - dist/process.js\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         command: ['node', 'dist/process.js']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built by Stacktape from a specified Dockerfile.") }).strict()
            .describe("#### Builds a container image from your own Dockerfile.\n\n---\n\nWith `custom-dockerfile`, you provide a path to your Dockerfile and build context.\nStacktape builds the image and uploads it to a managed ECR repository.\n\nThis gives you full control over the container environment and is ideal for complex setups."), z.object({
              "type": z.literal("external-buildpack"),
              "properties": z.object({
                "builder": z.string().optional().describe("#### The Buildpack Builder to use.\n\n---\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           builder: paketobuildpacks/builder-jammy-full\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         builder: 'paketobuildpacks/builder-jammy-full'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "buildpacks": z.array(z.string()).optional().describe("#### The specific Buildpack to use.\n\n---\n\nBy default, the buildpack is detected automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           buildpacks:\n             - paketo-buildpacks/nodejs\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         buildpacks: ['paketo-buildpacks/nodejs']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional(),
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           command:\n             - npm\n             - run\n             - import\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         command: ['npm', 'run', 'import']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Builds a container image using an external buildpack.\n\n---\n\nExternal buildpacks (buildpacks.io) automatically detect your application type\nand build an optimized container image with zero configuration.\n\nThe default builder is `paketobuildpacks/builder-jammy-base`.\nYou can find buildpacks for almost any language or framework."), z.object({
              "type": z.literal("nixpacks"),
              "properties": z.object({
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                "buildImage": z.string().optional().describe("#### The base image to use for building the application.\n\n---\n\nFor more details, see the [Nixpacks documentation](https://nixpacks.com/docs/configuration/file#build-image).\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         buildImage: ubuntu:22.04\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       buildImage: 'ubuntu:22.04'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "providers": z.array(z.string()).optional().describe("#### A list of providers to use for determining the build and runtime environments.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         providers:\n           - node\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       providers: ['node']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startCmd": z.string().optional().describe("#### The command to execute when starting the application.\n\n---\n\nThis overrides the default start command inferred by Nixpacks.\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startCmd: node dist/worker.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startCmd: 'node dist/worker.js'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startRunImage": z.string().optional().describe("#### The base image to use for running the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startRunImage: gcr.io/distroless/nodejs22-debian12\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startRunImage: 'gcr.io/distroless/nodejs22-debian12'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startOnlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in the runtime environment; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startOnlyIncludeFiles:\n           - dist\n           - node_modules\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startOnlyIncludeFiles: ['dist', 'node_modules']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "phases": z.array(z.object({
                  "name": z.string().describe("#### The name of the build phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                  "cmds": z.array(z.string()).optional().describe("#### A list of shell commands to execute in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n               - npm prune --omit=dev\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build', 'npm prune --omit=dev']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixPkgs": z.array(z.string()).optional().describe("#### A list of Nix packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - ffmpeg\n               - imagemagick\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['ffmpeg', 'imagemagick']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixLibs": z.array(z.string()).optional().describe("#### A list of Nix libraries to include in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixLibs:\n               - openssl\n               - zlib\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixLibs: ['openssl', 'zlib']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixOverlay": z.array(z.string()).optional().describe("#### A list of Nix overlay files to apply in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixOverlay:\n               - ./nix/overlay.nix\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixOverlay: ['./nix/overlay.nix']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixpkgsArchive": z.string().optional().describe("#### The Nixpkgs archive to use.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - nodejs_22\n             nixpkgsArchive: bf3287dac860542719b3849d6970d22f635f9da1\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['nodejs_22'],\n           nixpkgsArchive: 'bf3287dac860542719b3849d6970d22f635f9da1'\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "aptPkgs": z.array(z.string()).optional().describe("#### A list of APT packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             aptPkgs:\n               - libpq-dev\n               - poppler-utils\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           aptPkgs: ['libpq-dev', 'poppler-utils']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "cacheDirectories": z.array(z.string()).optional().describe("#### A list of directories to cache between builds to speed up subsequent builds.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n             cacheDirectories:\n               - node_modules/.cache\n               - /root/.npm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci'],\n           cacheDirectories: ['node_modules/.cache', '/root/.npm']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "onlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in this phase; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n             onlyIncludeFiles:\n               - src\n               - package.json\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build'],\n           onlyIncludeFiles: ['src', 'package.json']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
                ).optional().describe("#### The build phases for the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n           - name: build\n             cmds:\n               - npm run build\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         { name: 'install', cmds: ['npm ci'] },\n         { name: 'build', cmds: ['npm run build'] }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Builds a container image using Nixpacks.\n\n---\n\nNixpacks automatically detects your application type and builds an optimized container image.\nIn most cases, no configuration is required.\n\nIt supports a wide range of languages and frameworks out of the box."), z.object({
              "type": z.literal("stacktape-image-buildpack"),
              "properties": z.object({
                "languageSpecificConfig": z.union([z.object({
                    "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                    "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                    "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                  , z.object({
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                    "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                    "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                  , z.object({
                    "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                  , z.record(z.string(), z.any()), z.object({
                    "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                  , z.object({
                    "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                  , z.object({
                    "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                    "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
                ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { apiService } };\n});\n```").optional(),
                "requiresGlibcBinaries": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use glibc instead of musl (Alpine default). Enable if native dependencies require glibc.\n\n---\n\nResults in a larger image. Common packages needing this: `sharp`, `canvas`, `bcrypt`, `puppeteer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         requiresGlibcBinaries: true\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       requiresGlibcBinaries: true\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "customDockerBuildCommands": z.array(z.string()).optional().describe("#### A list of commands to be executed during the `docker build` process.\n\n---\n\nThese commands are executed using the `RUN` directive in the Dockerfile.\nThis is useful for installing additional system dependencies in your container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         customDockerBuildCommands:\n           - apt-get update && apt-get install -y poppler-utils\n           - fc-cache -f\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       customDockerBuildCommands: [\n         'apt-get update && apt-get install -y poppler-utils',\n         'fc-cache -f'\n       ]\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
                "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built automatically by Stacktape from your source code.") }).strict()
            .describe("#### A zero-config buildpack that creates a container image from your source code.\n\n---\n\nThe `stacktape-image-buildpack` automatically bundles your code and dependencies into an optimized container image.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, and Go.\n\nFor JS/TS, your code is bundled into a single file with source maps.\nThe resulting image is uploaded to a managed ECR repository.")]).describe("#### How to build or specify the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: custom-dockerfile\n            properties:\n              buildContextPath: ./api\n              dockerfilePath: Dockerfile\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, CustomDockerfilePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new CustomDockerfilePackaging({ buildContextPath: './api', dockerfilePath: 'Dockerfile' })\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "essential": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### If `true` (default), the entire workload restarts when this container fails.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n        - name: metrics-sidecar\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: prom/statsd-exporter:latest\n          essential: false\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, PrebuiltImagePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) },\n      {\n        name: 'metrics-sidecar',\n        packaging: new PrebuiltImagePackaging({ image: 'prom/statsd-exporter:latest' }),\n        essential: false\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            disabled: true\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { disabled: true }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 30 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Container logging (stdout/stderr). Sent to CloudWatch, viewable with `stacktape logs`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 14\n            disabled: false\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 14, disabled: false }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "dependsOn": z.array(z.object({
            "containerName": z.string().describe("The name of the container that this container depends on."),
            "condition": z.enum(["COMPLETE","HEALTHY","START","SUCCESS"]).describe("#### The condition that the dependency container must meet.\n---\nAvailable conditions:\n- `START`: The dependency has started.\n- `COMPLETE`: The dependency has finished executing (regardless of success).\n- `SUCCESS`: The dependency has finished with an exit code of `0`.\n- `HEALTHY`: The dependency has passed its first health check.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: redis\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: redis:7\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - redis-cli ping || exit 1\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          dependsOn:\n            - containerName: redis\n              condition: HEALTHY\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, PrebuiltImagePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'redis',\n        packaging: new PrebuiltImagePackaging({ image: 'redis:7' }),\n        internalHealthCheck: { healthCheckCommand: ['CMD-SHELL', 'redis-cli ping || exit 1'] }\n      },\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        dependsOn: [\n          {\n            containerName: 'redis',\n            condition: 'HEALTHY'\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
          ).optional().describe("#### Start this container only after the listed containers reach a specific state.\n\n---\n\nE.g., wait for a database sidecar to be `HEALTHY` before starting the app container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: migrations\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/migrate.ts\n          essential: false\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          dependsOn:\n            - containerName: migrations\n              condition: SUCCESS\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'migrations', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/migrate.ts' }), essential: false },\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        dependsOn: [{ containerName: 'migrations', condition: 'SUCCESS' }]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "environment": z.array(z.object({
            "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
            "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
          ).optional().describe("#### Environment variables injected at runtime. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          environment:\n            - name: NODE_ENV\n              value: production\n            - name: DATABASE_URL\n              value: $ResourceParam('appDb', 'connectionString')\n            - name: STRIPE_KEY\n              value: $Secret('stripe.secretKey')\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - appDb\n  appDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t3.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, RelationalDatabase, StacktapeImageBuildpackPackaging, $ResourceParam, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db.password') },\n    engine: { type: 'postgres', properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t3.micro' } } }\n  });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        environment: {\n          NODE_ENV: 'production',\n          DATABASE_URL: $ResourceParam('appDb', 'connectionString'),\n          STRIPE_KEY: $Secret('stripe.secretKey')\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [appDb]\n  });\n  return { resources: { app, appDb } };\n});\n```").optional(),
          "internalHealthCheck": z.object({
            "healthCheckCommand": z.array(z.string()).describe("#### Command to check health. E.g., `[\"CMD-SHELL\", \"curl -f http://localhost/ || exit 1\"]`. Exit 0 = healthy.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1']\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
            "intervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks (5-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            intervalSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          intervalSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(30),
            "timeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a check is considered failed (2-60).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            timeoutSeconds: 10\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          timeoutSeconds: 10\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(5),
            "retries": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Consecutive failures before marking unhealthy (1-10).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            retries: 5\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          retries: 5\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(3),
            "startPeriodSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Grace period (seconds) before counting failures. Gives the container time to start (0-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            startPeriodSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          startPeriodSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
          .optional().describe("#### Command-based health check. If it fails on an essential container, the workload instance is replaced.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/health || exit 1\n            intervalSeconds: 30\n            retries: 3\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/health || exit 1'],\n          intervalSeconds: 30,\n          retries: 3\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "stopTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds to wait after SIGTERM before SIGKILL (2-120).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          stopTimeout: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        stopTimeout: 30\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(2),
          "volumeMounts": z.array(z.object({
            "type": z.literal("efs").describe("#### The type of the volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "properties": z.object({
              "efsFilesystemName": z.string().describe("#### Name of the `efs-filesystem` resource defined in your config.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
              "rootDirectory": z.string().optional().describe("#### Subdirectory within the EFS filesystem to mount. Restricts access to that directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                rootDirectory: /uploads\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              rootDirectory: '/uploads',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
              "mountPath": z.string().describe("#### Absolute path inside the container where the volume is mounted (e.g., `/data`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
            .describe("#### Properties for the EFS volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
          ).optional().describe("#### Mount EFS volumes for persistent, shared storage across containers.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          { type: 'efs', properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```").optional() }).strict()
        ).describe("#### Containers in this workload. They share compute resources and scale together.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n  appLb:\n    type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, appLb } };\n});\n```"),
        "resources": z.object({
          "cpu": z.union([z.literal(0.25), z.literal(0.5), z.literal(1), z.literal(16), z.literal(2), z.literal(4), z.literal(8)]).optional().describe("#### vCPUs for the workload (Fargate). Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 2\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 2,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB. Must be compatible with the vCPU count on Fargate.\n\n---\n\nFargate valid combos: 0.25 vCPU → 512-2048 MB, 0.5 → 1024-4096, 1 → 2048-8192, 2 → 4096-16384,\n4 → 8192-30720, 8 → 16384-61440, 16 → 32768-122880.\nFor EC2: auto-detected from instance type if omitted.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 1,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "instanceTypes": z.array(z.string()).optional().describe("#### EC2 instance types for the workload (e.g., `t3.medium`, `c6g.large`). Use instead of `cpu`/`memory`.\n\n---\n\nFirst type in the list is preferred. Instances auto-scale and are refreshed weekly for patching.\nTip: specify a single type and omit `cpu`/`memory` for optimal sizing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - c6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['c6g.large']\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "enableWarmPool": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Keep pre-initialized EC2 instances ready for faster scaling. Only works with a single instance type.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - t3.medium\n        enableWarmPool: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['t3.medium'],\n      enableWarmPool: true\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "architecture": z.enum(["arm64","x86_64"]).optional().describe("#### CPU architecture for Fargate. `arm64` is ~20% cheaper. Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 0.5,\n      memory: 1024,\n      architecture: 'arm64'\n    }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
        .describe("#### CPU, memory, and compute engine (Fargate or EC2).\n\n---\n\n- **Fargate** (set `cpu` + `memory`): Serverless, no servers to manage.\n- **EC2** (set `instanceTypes`): Choose specific instance types for more control or GPU access.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 2048\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 1, memory: 2048, architecture: 'arm64' }\n  });\n  return { resources: { app } };\n});\n```"),
        "scaling": z.object({
          "minInstances": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum running instances. Set to 0 is not supported — minimum is 1.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8\n    }\n  });\n  return { resources: { app } };\n});\n```").default(1),
          "maxInstances": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum running instances. Traffic is distributed across all instances.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8\n    }\n  });\n  return { resources: { app } };\n});\n```").default(1),
          "scalingPolicy": z.object({
            "keepAvgCpuUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Scale out when avg CPU exceeds this %, scale in when it drops below.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 65\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: {\n        keepAvgCpuUtilizationUnder: 65\n      }\n    }\n  });\n  return { resources: { app } };\n});\n```").default(80),
            "keepAvgMemoryUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Scale out when avg memory exceeds this %, scale in when it drops below.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgMemoryUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: {\n        keepAvgMemoryUtilizationUnder: 70\n      }\n    }\n  });\n  return { resources: { app } };\n});\n```").default(80) }).strict()
          .optional().describe("#### When to scale: CPU and/or memory utilization targets.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 60\n          keepAvgMemoryUtilizationUnder: 75\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: { keepAvgCpuUtilizationUnder: 60, keepAvgMemoryUtilizationUnder: 75 }\n    }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
        .optional().describe("#### Auto-scaling: how many instances and when to add/remove them.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 10\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 10,\n      scalingPolicy: { keepAvgCpuUtilizationUnder: 70 }\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
        "deployment": z.object({
          "strategy": z.enum(["AllAtOnce","Canary10Percent15Minutes","Canary10Percent5Minutes","Linear10PercentEvery1Minutes","Linear10PercentEvery3Minutes"]).describe("#### How traffic shifts to the new version during deployment.\n\n---\n\n- `Canary10Percent5Minutes`: 10% first, then all after 5 min.\n- `Canary10Percent15Minutes`: 10% first, then all after 15 min.\n- `Linear10PercentEvery1Minutes`: 10% more every minute.\n- `Linear10PercentEvery3Minutes`: 10% more every 3 minutes.\n- `AllAtOnce`: Instant switch.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n      deployment:\n        strategy: Linear10PercentEvery3Minutes\n  appLb:\n    type: application-load-balancer\n  smokeTest:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/smoke-test.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, LambdaFunction, StacktapeImageBuildpackPackaging, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const smokeTest = new LambdaFunction({ packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/smoke-test.ts' }) });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    deployment: {\n      strategy: 'Linear10PercentEvery3Minutes'\n    }\n  });\n  return { resources: { app, appLb, smokeTest } };\n});\n```"),
          "beforeAllowTrafficFunction": z.string().optional().describe("#### Lambda function to run before traffic shifts to the new version (for validation/smoke tests).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n      deployment:\n        strategy: Canary10Percent5Minutes\n        beforeAllowTrafficFunction: smokeTest\n  appLb:\n    type: application-load-balancer\n  smokeTest:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/smoke-test.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, LambdaFunction, StacktapeImageBuildpackPackaging, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const smokeTest = new LambdaFunction({ packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/smoke-test.ts' }) });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes',\n      beforeAllowTrafficFunction: 'smokeTest'\n    }\n  });\n  return { resources: { app, appLb, smokeTest } };\n});\n```").optional(),
          "afterTrafficShiftFunction": z.string().optional().describe("#### Lambda function to run after all traffic has shifted (for post-deployment checks).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n      deployment:\n        strategy: Canary10Percent5Minutes\n        afterTrafficShiftFunction: smokeTest\n  appLb:\n    type: application-load-balancer\n  smokeTest:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/smoke-test.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, LambdaFunction, StacktapeImageBuildpackPackaging, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const smokeTest = new LambdaFunction({ packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/smoke-test.ts' }) });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes',\n      afterTrafficShiftFunction: 'smokeTest'\n    }\n  });\n  return { resources: { app, appLb, smokeTest } };\n});\n```").optional(),
          "testListenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### ALB listener port for test traffic. Only needed with `beforeAllowTrafficFunction` and custom listeners.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n      deployment:\n        strategy: Canary10Percent5Minutes\n        beforeAllowTrafficFunction: smokeTest\n        testListenerPort: 8443\n  appLb:\n    type: application-load-balancer\n  smokeTest:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/smoke-test.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, LambdaFunction, StacktapeImageBuildpackPackaging, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const smokeTest = new LambdaFunction({ packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/smoke-test.ts' }) });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes',\n      beforeAllowTrafficFunction: 'smokeTest',\n      testListenerPort: 8443\n    }\n  });\n  return { resources: { app, appLb, smokeTest } };\n});\n```").optional() }).strict()
        .optional().describe("#### Gradual traffic shifting (canary/linear) for safe deployments. Requires an ALB integration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n      deployment:\n        strategy: Canary10Percent5Minutes\n  appLb:\n    type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    deployment: { strategy: 'Canary10Percent5Minutes' }\n  });\n  return { resources: { app, appLb } };\n});\n```").optional(),
        "enableRemoteSessions": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Enable `stacktape container:session` for interactive shell access to running containers.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      enableRemoteSessions: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    enableRemoteSessions: true\n  });\n  return { resources: { app } };\n});\n```").optional(),
        "usePrivateSubnetsWithNAT": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Run in private subnets with a NAT Gateway for outbound internet. Gives you a static public IP.\n\n---\n\nUseful for IP whitelisting with third-party APIs. NAT Gateway costs ~$32/month per AZ + data processing fees.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: worker\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/worker.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      usePrivateSubnetsWithNAT: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'worker', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/worker.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    usePrivateSubnetsWithNAT: true\n  });\n  return { resources: { app } };\n});\n```").default(false),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Run multiple containers together as a single unit with shared compute resources.\n\n---\n\nFor advanced setups: sidecars, init containers, or services that need multiple processes.\nSupports Fargate (serverless) or EC2 (custom instances). Auto-scales horizontally."), z.object({
      "type": z.literal("relational-database"),
      "properties": z.object({
        "credentials": z.object({
          "masterUserName": z.string().optional().describe("#### Admin username. Avoid special characters: `[]{}(),;?*=!@`.\n\n---\n\n> **Warning:** Changing this after creation **replaces the database and deletes all data**.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserName: app_admin\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: {\n    masterUserName: 'app_admin',\n    masterUserPassword: $Secret('mainDatabase.password')\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.micro' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
          "masterUserPassword": z.string().describe("#### Admin password. Avoid special characters: `[]{}(),;?*=!@`.\n\n---\n\nUse `$Secret()` to store it securely instead of hardcoding.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserName: db_master_user\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: {\n    masterUserName: 'db_master_user',\n    masterUserPassword: $Secret('mainDatabase.password')\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.micro' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```") }).strict()
        .describe("#### Master user credentials (username and password).\n\n---\n\nIncluded in the auto-generated connection string. Store the password in a Stacktape secret\nto avoid exposing it in your config file.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserName: db_master_user\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: {\n    masterUserName: 'db_master_user',\n    masterUserPassword: $Secret('mainDatabase.password')\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.micro' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
        "engine": z.union([z.object({
            "type": z.enum(["aurora-mysql","aurora-postgresql"]),
            "properties": z.object({
              "dbName": z.string().optional().describe("#### Name of the initial database.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql\n      properties:\n        version: '16.6'\n        dbName: appdb\n        instances:\n          - instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql',\n    properties: {\n      version: '16.6',\n      dbName: 'appdb',\n      instances: [{ instanceSize: 'db.r6g.large' }]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default("defdb"),
              "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port. Defaults: aurora-mysql 3306, aurora-postgresql 5432.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql\n      properties:\n        version: '16.6'\n        port: 5433\n        instances:\n          - instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql',\n    properties: {\n      version: '16.6',\n      port: 5433,\n      instances: [{ instanceSize: 'db.r6g.large' }]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
              "instances": z.array(z.object({
                "instanceSize": z.string().describe("#### Instance size (e.g., `db.t4g.medium`, `db.r6g.large`).\n\n---\n\n`t` family = burstable (dev/low-traffic). `r` family = memory-optimized (production).\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql\n      properties:\n        version: '16.6'\n        instances:\n          - instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql',\n    properties: {\n      version: '16.6',\n      instances: [\n        { instanceSize: 'db.r6g.large' }\n      ]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```") }).strict()
              ).describe("#### Cluster instances. First = primary (writer), rest = read replicas.\n\n---\n\nReads are load-balanced across all instances. If the primary fails,\na replica is automatically promoted (usually within 30 seconds).\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql\n      properties:\n        version: '16.6'\n        instances:\n          - instanceSize: db.r6g.large\n          - instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql',\n    properties: {\n      version: '16.6',\n      instances: [\n        { instanceSize: 'db.r6g.large' },\n        { instanceSize: 'db.r6g.large' }\n      ]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
              "version": z.string().describe("#### Engine version (e.g., `16.6` for PostgreSQL, `8.0.36` for MySQL).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
              "disableAutoMinorVersionUpgrade": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip automatic minor version upgrades (e.g., 16.4 → 16.5).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        disableAutoMinorVersionUpgrade: true\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      disableAutoMinorVersionUpgrade: true,\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false) }).strict()
          }).strict()
          .describe("#### Aurora: high-performance clustered database with auto-failover.\n\n---\n\nUp to 5x faster than MySQL, 3x faster than PostgreSQL. Data is replicated across 3 AZs\nautomatically. If the primary instance fails, a read replica is promoted in seconds."), z.object({
            "type": z.enum(["aurora-mysql-serverless","aurora-postgresql-serverless"]),
            "properties": z.object({
              "version": z.string().optional().describe("#### Engine version. Usually managed by AWS automatically for serverless v1.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: scoping-workloads-in-vpc\n    engine:\n      type: aurora-postgresql-serverless\n      properties:\n        version: '13.12'\n        minCapacity: 2\n        maxCapacity: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n  engine: {\n    type: 'aurora-postgresql-serverless',\n    properties: {\n      version: '13.12',\n      minCapacity: 2,\n      maxCapacity: 8\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
              "dbName": z.string().optional().describe("#### Name of the initial database.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: scoping-workloads-in-vpc\n    engine:\n      type: aurora-postgresql-serverless\n      properties:\n        dbName: appdb\n        minCapacity: 2\n        maxCapacity: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n  engine: {\n    type: 'aurora-postgresql-serverless',\n    properties: {\n      dbName: 'appdb',\n      minCapacity: 2,\n      maxCapacity: 8\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default("defdb"),
              "minCapacity": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum ACUs to scale down to (~1 ACU ≈ 2 GB RAM).\n\n---\n\nMySQL: 1-256 (powers of 2). PostgreSQL: 2-256 (powers of 2).\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: scoping-workloads-in-vpc\n    engine:\n      type: aurora-postgresql-serverless\n      properties:\n        minCapacity: 2\n        maxCapacity: 16\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n  engine: {\n    type: 'aurora-postgresql-serverless',\n    properties: {\n      minCapacity: 2,\n      maxCapacity: 16\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(2),
              "maxCapacity": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum ACUs to scale up to.\n\n---\n\nMySQL: 1-256 (powers of 2). PostgreSQL: 2-256 (powers of 2).\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: scoping-workloads-in-vpc\n    engine:\n      type: aurora-postgresql-serverless\n      properties:\n        minCapacity: 2\n        maxCapacity: 32\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n  engine: {\n    type: 'aurora-postgresql-serverless',\n    properties: {\n      minCapacity: 2,\n      maxCapacity: 32\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(4),
              "pauseAfterSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Pause the database after this many seconds of inactivity (no connections).\n\n---\n\nWhen paused, you only pay for storage. Range: 300 (5 min) - 86400 (24 hr).\nOmit to never pause.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: scoping-workloads-in-vpc\n    engine:\n      type: aurora-postgresql-serverless\n      properties:\n        minCapacity: 2\n        maxCapacity: 8\n        pauseAfterSeconds: 900\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n  engine: {\n    type: 'aurora-postgresql-serverless',\n    properties: {\n      minCapacity: 2,\n      maxCapacity: 8,\n      pauseAfterSeconds: 900\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
              "disableAutoMinorVersionUpgrade": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip automatic minor version upgrades (e.g., 16.4 → 16.5).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        disableAutoMinorVersionUpgrade: true\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      disableAutoMinorVersionUpgrade: true,\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false) }).strict()
            .optional().describe("").optional() }).strict()
          .describe("#### Aurora Serverless v1: auto-scaling database that can pause when idle.\n\n---\n\nScales compute capacity automatically and pauses during inactivity (you only pay for storage).\n\n> **For new projects, use Aurora Serverless v2 instead** — it has faster scaling and more granular capacity control."), z.object({
            "type": z.enum(["aurora-mysql-serverless-v2","aurora-postgresql-serverless-v2"]),
            "properties": z.object({
              "dbName": z.string().optional().describe("#### Name of the initial database.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql-serverless-v2\n      properties:\n        version: '16.6'\n        dbName: appdb\n        minCapacity: 0.5\n        maxCapacity: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql-serverless-v2',\n    properties: {\n      version: '16.6',\n      dbName: 'appdb',\n      minCapacity: 0.5,\n      maxCapacity: 8\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default("defdb"),
              "minCapacity": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum ACUs (0.5-128 in 0.5 increments). ~1 ACU ≈ 2 GB RAM.\n\n---\n\nSet low (0.5) for dev/staging to minimize cost. The database scales up instantly under load.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql-serverless-v2\n      properties:\n        version: '16.6'\n        minCapacity: 0.5\n        maxCapacity: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql-serverless-v2',\n    properties: {\n      version: '16.6',\n      minCapacity: 0.5,\n      maxCapacity: 8\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(0.5),
              "maxCapacity": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum ACUs (0.5-128 in 0.5 increments). Caps your scaling and cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql-serverless-v2\n      properties:\n        version: '16.6'\n        minCapacity: 0.5\n        maxCapacity: 16\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql-serverless-v2',\n    properties: {\n      version: '16.6',\n      minCapacity: 0.5,\n      maxCapacity: 16\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(10),
              "serverlessReadersCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Number of reader instances in the Aurora Serverless v2 cluster.\n\n---\n\nAurora Serverless v2 always has one writer instance. This value adds additional readers\n(`0` means writer only, `2` means writer + 2 readers).\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql-serverless-v2\n      properties:\n        version: '16.6'\n        minCapacity: 0.5\n        maxCapacity: 16\n        serverlessReadersCount: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql-serverless-v2',\n    properties: {\n      version: '16.6',\n      minCapacity: 0.5,\n      maxCapacity: 16,\n      serverlessReadersCount: 2\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(0),
              "version": z.string().describe("#### Engine version (e.g., `16.6` for PostgreSQL, `8.0.36` for MySQL).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
              "disableAutoMinorVersionUpgrade": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip automatic minor version upgrades (e.g., 16.4 → 16.5).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        disableAutoMinorVersionUpgrade: true\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      disableAutoMinorVersionUpgrade: true,\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false) }).strict()
          }).strict()
          .describe("#### Aurora Serverless v2: recommended for most new projects.\n\n---\n\nScales instantly from 0.5 to 128 ACUs in 0.5-ACU increments (~1 ACU ≈ 2 GB RAM).\nYou pay only for the capacity used, making it cost-effective for variable workloads."), z.object({
            "type": z.enum(["mariadb","mysql","oracle-ee","oracle-se2","postgres","sqlserver-ee","sqlserver-ex","sqlserver-se","sqlserver-web"]),
            "properties": z.object({
              "dbName": z.string().optional().describe("#### Name of the database created on initialization. For Oracle, this is the SID. Not applicable to SQL Server.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        dbName: appdb\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      dbName: 'appdb',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
              "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port the database listens on. Defaults: PostgreSQL 5432, MySQL/MariaDB 3306, Oracle 1521, SQL Server 1433.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        port: 5433\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      port: 5433,\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
              "storage": z.object({
                "initialSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Initial storage in GB. Auto-scales up when free space is low.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        storage:\n          initialSize: 100\n          maxSize: 500\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      storage: {\n        initialSize: 100,\n        maxSize: 500\n      },\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(20),
                "maxSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max storage in GB. The database won't auto-scale beyond this.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        storage:\n          initialSize: 100\n          maxSize: 1000\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      storage: {\n        initialSize: 100,\n        maxSize: 1000\n      },\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(200) }).strict()
              .optional().describe("#### Storage configuration. Auto-scales up when free space is low.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        storage:\n          initialSize: 50\n          maxSize: 500\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      storage: { initialSize: 50, maxSize: 500 },\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
              "primaryInstance": z.object({
                "instanceSize": z.string().describe("#### Instance size (e.g., `db.t4g.micro`, `db.r6g.large`).\n\n---\n\nDetermines CPU, memory, and network capacity. Quick guide:\n- **db.t4g.micro** (~$12/mo): Dev/testing, 2 vCPU, 1 GB RAM\n- **db.t4g.medium** (~$50/mo): Small production, 2 vCPU, 4 GB RAM\n- **db.r6g.large** (~$180/mo): Production, 2 vCPU, 16 GB RAM\n\n`t` family instances are burstable (fine for low/variable load). Use `r` family for steady workloads.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.r6g.large'\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
                "multiAz": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Create a standby replica in another availability zone for automatic failover.\n\n---\n\nIf the primary goes down, traffic fails over to the standby automatically.\nAlso reduces downtime during maintenance. Doubles the instance cost.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n          multiAz: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.t4g.medium',\n        multiAz: true\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional() }).strict()
              .describe("#### The primary (writer) instance. Handles all write operations.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.r6g.large\n          multiAz: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.r6g.large',\n        multiAz: true\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
              "readReplicas": z.array(z.object({
                "instanceSize": z.string().describe("#### Instance size (e.g., `db.t4g.micro`, `db.r6g.large`).\n\n---\n\nDetermines CPU, memory, and network capacity. Quick guide:\n- **db.t4g.micro** (~$12/mo): Dev/testing, 2 vCPU, 1 GB RAM\n- **db.t4g.medium** (~$50/mo): Small production, 2 vCPU, 4 GB RAM\n- **db.r6g.large** (~$180/mo): Production, 2 vCPU, 16 GB RAM\n\n`t` family instances are burstable (fine for low/variable load). Use `r` family for steady workloads.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.r6g.large'\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
                "multiAz": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Create a standby replica in another availability zone for automatic failover.\n\n---\n\nIf the primary goes down, traffic fails over to the standby automatically.\nAlso reduces downtime during maintenance. Doubles the instance cost.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n          multiAz: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.t4g.medium',\n        multiAz: true\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional() }).strict()
              ).optional().describe("#### Read replicas to offload read traffic from the primary instance.\n\n---\n\nEach replica gets its own endpoint. Data is replicated asynchronously from the primary.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.r6g.large\n        readReplicas:\n          - instanceSize: db.r6g.large\n          - instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.r6g.large' },\n      readReplicas: [\n        { instanceSize: 'db.r6g.large' },\n        { instanceSize: 'db.t4g.medium' }\n      ]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
              "version": z.string().describe("#### Engine version (e.g., `16.6` for PostgreSQL, `8.0.36` for MySQL).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
              "disableAutoMinorVersionUpgrade": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip automatic minor version upgrades (e.g., 16.4 → 16.5).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        disableAutoMinorVersionUpgrade: true\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      disableAutoMinorVersionUpgrade: true,\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false) }).strict()
          }).strict()
          .describe("#### Standard RDS: single-instance database with predictable pricing.\n\n---\n\nChoose a fixed instance size and pay hourly. AWS handles patching, backups, and recovery.\nFor high availability, enable `multiAz` on the primary instance.")]).describe("#### Database engine: what type of database and how it runs.\n\n---\n\n- **RDS** (`postgres`, `mysql`, `mariadb`, etc.): Single-node, fixed-size. Simple and predictable pricing.\n- **Aurora** (`aurora-postgresql`, `aurora-mysql`): High-performance clustered DB with auto-failover.\n  Up to 5x faster than standard MySQL / 3x faster than standard PostgreSQL.\n- **Aurora Serverless v2** (`aurora-postgresql-serverless-v2`): Auto-scales from 0.5 to 128 ACUs.\n  **Recommended for most new projects** — pay only for what you use.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql-serverless-v2\n      properties:\n        version: '16.6'\n        minCapacity: 0.5\n        maxCapacity: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql-serverless-v2',\n    properties: {\n      version: '16.6',\n      minCapacity: 0.5,\n      maxCapacity: 8\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
        "accessibility": z.object({
          "accessibilityMode": z.enum(["internet","scoping-workloads-in-vpc","vpc","whitelisted-ips-only"]).describe("#### Controls who can connect to your database.\n\n---\n\n- **`internet`** (default): Anyone with the credentials can connect. Simplest setup, great for development.\n  The database is still protected by username/password.\n- **`vpc`**: Only your app's resources (and anything in the same VPC) can connect.\n  You can also whitelist specific IPs (e.g., your office) using `whitelistedIps`.\n- **`scoping-workloads-in-vpc`**: Most restrictive. Only resources that explicitly list this\n  database in their `connectTo` can reach it. Best for production.\n- **`whitelisted-ips-only`**: Only the IP addresses you list in `whitelistedIps` can connect.\n\n> Aurora Serverless engines only support `vpc` or `scoping-workloads-in-vpc`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: vpc\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: {\n    accessibilityMode: 'vpc'\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default("internet"),
          "forceDisablePublicIp": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Remove the database's public IP entirely (VPC-only access).\n\n---\n\n> For Aurora, this can only be set at creation time and cannot be changed later.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: vpc\n      forceDisablePublicIp: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: {\n    accessibilityMode: 'vpc',\n    forceDisablePublicIp: true\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
          "whitelistedIps": z.array(z.string()).optional().describe("#### IP addresses or CIDR ranges allowed to connect (e.g., `203.0.113.50/32`).\n\n---\n\n- In `vpc`/`scoping-workloads-in-vpc`: adds external IPs on top of VPC access (e.g., your office).\n- In `whitelisted-ips-only`: only these IPs can connect.\n- No effect in `internet` mode.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: whitelisted-ips-only\n      whitelistedIps:\n        - 203.0.113.50/32\n        - 198.51.100.0/24\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: {\n    accessibilityMode: 'whitelisted-ips-only',\n    whitelistedIps: ['203.0.113.50/32', '198.51.100.0/24']\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional() }).strict()
        .optional().describe("#### Who can connect to this database (network-level access control).\n\n---\n\nDefault is `internet` — anyone with credentials can connect (fine for development).\nFor production, use `scoping-workloads-in-vpc` to restrict access to only resources\nthat list this database in their `connectTo`.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: scoping-workloads-in-vpc\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.micro' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
        "deletionProtection": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Prevent accidental deletion of the database. Must be disabled before deleting.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    deletionProtection: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  deletionProtection: true,\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false),
        "automatedBackupRetentionDays": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Days to keep automated daily backups (0-35). Set to 0 to disable (RDS only).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    automatedBackupRetentionDays: 7\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  automatedBackupRetentionDays: 7,\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(1),
        "preferredMaintenanceWindow": z.string().optional().describe("#### When maintenance (patching, upgrades) happens. Format: `Sun:02:00-Sun:04:00` (UTC).\n\n---\n\nThe database may be briefly unavailable during this window.\nUse multi-AZ or Aurora to minimize downtime.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    preferredMaintenanceWindow: Sun:02:00-Sun:04:00\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  preferredMaintenanceWindow: 'Sun:02:00-Sun:04:00',\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
        "alarms": z.array(z.object({
          "trigger": z.union([z.object({
              "type": z.literal("database-read-latency"),
              "properties": z.object({
                "thresholdSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when average read I/O latency exceeds this value (seconds).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database read latency too high\n          trigger:\n            type: database-read-latency\n            properties:\n              thresholdSeconds: 0.05\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database read latency too high',\n        trigger: {\n          type: 'database-read-latency',\n          properties: {\n            thresholdSeconds: 0.05\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("database-write-latency"),
              "properties": z.object({
                "thresholdSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when average write I/O latency exceeds this value (seconds).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database write latency too high\n          trigger:\n            type: database-write-latency\n            properties:\n              thresholdSeconds: 0.1\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database write latency too high',\n        trigger: {\n          type: 'database-write-latency',\n          properties: {\n            thresholdSeconds: 0.1\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("database-cpu-utilization"),
              "properties": z.object({
                "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when CPU utilization exceeds this percentage.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database CPU utilization too high\n          trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 85\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database CPU utilization too high',\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: {\n            thresholdPercent: 85\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("database-free-storage"),
              "properties": z.object({
                "thresholdMB": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when free disk space drops below this value (MB).\n\n---\n\nDefault: fires if **minimum** free storage < threshold.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database running low on free storage\n          trigger:\n            type: database-free-storage\n            properties:\n              thresholdMB: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database running low on free storage',\n        trigger: {\n          type: 'database-free-storage',\n          properties: {\n            thresholdMB: 2048\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("database-free-memory"),
              "properties": z.object({
                "thresholdMB": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when free memory drops below this value (MB).\n\n---\n\nDefault: fires if **average** free memory < threshold. Customize with `statistic` and `comparisonOperator`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database running low on free memory\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 256\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database running low on free memory',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 256\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("database-connection-count"),
              "properties": z.object({
                "thresholdCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when the number of active database connections exceeds this value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Too many active database connections\n          trigger:\n            type: database-connection-count\n            properties:\n              thresholdCount: 180\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Too many active database connections',\n        trigger: {\n          type: 'database-connection-count',\n          properties: {\n            thresholdCount: 180\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
            }).strict()
          ]),
          "evaluation": z.object({
            "period": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Duration of one evaluation period in seconds. Must be a multiple of 60.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker running too long\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          evaluation:\n            period: 300\n            evaluationPeriods: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker running too long',\n        trigger: {\n          type: 'lambda-duration',\n          properties: { thresholdMilliseconds: 5000 }\n        },\n        evaluation: {\n          period: 300,\n          evaluationPeriods: 2\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default(60),
            "evaluationPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many recent periods to evaluate. Prevents alarms from firing on short spikes.\n\n---\n\nExample: set to `5` with `breachedPeriods: 3` — the alarm fires only if the threshold is breached\nin at least 3 of the last 5 periods.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/payments.ts\n      alarms:\n        - description: Payments error rate elevated\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/payments.ts' }),\n    alarms: [\n      {\n        description: 'Payments error rate elevated',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 1 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { paymentsFn } };\n});\n```").default(1),
            "breachedPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many periods (within `evaluationPeriods`) must breach the threshold to fire the alarm.\n\n---\n\nMust be ≤ `evaluationPeriods`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/checkout.ts\n      alarms:\n        - description: Checkout errors persisting across multiple periods\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 2\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/checkout.ts' }),\n    alarms: [\n      {\n        description: 'Checkout errors persisting across multiple periods',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { checkoutFn } };\n});\n```").default(1) }).strict()
          .optional().describe("#### How long and how often to evaluate the metric before triggering.\n\n---\n\nControls the evaluation window (period), how many periods to look at, and how many must breach\nthe threshold to fire the alarm. Useful for filtering out short spikes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/api.ts\n      memory: 512\n      timeout: 10\n      alarms:\n        - description: API error rate too high\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    alarms: [\n      {\n        description: 'API error rate too high',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFunction } };\n});\n```").optional(),
          "notificationTargets": z.array(z.union([z.object({
              "type": z.literal("slack"),
              "properties": z.object({
                "conversationId": z.string().describe("#### The Slack channel or DM ID to send notifications to.\n\n---\n\nTo find the ID: open the channel, click its name, and look at the bottom of the **About** tab.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```"),
                "accessToken": z.string().describe("#### Bot User OAuth Token for your Slack app. Store as `$Secret()` for security.\n\n---\n\nCreate a Slack app, add the `chat:write` scope, install it to your workspace, then copy the Bot User OAuth Token.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("ms-teams"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Incoming Webhook URL for the MS Teams channel. Store as `$Secret()` for security.\n\n---\n\nCreate an Incoming Webhook connector in your Teams channel settings to get this URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 1024\n      timeout: 15\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          description: API function is running slow\n          notificationTargets:\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('ms-teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/api.ts' }\n    },\n    memory: 1024,\n    timeout: 15,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 5000 } },\n        description: 'API function is running slow',\n        notificationTargets: [\n          {\n            type: 'ms-teams',\n            properties: {\n              webhookUrl: $Secret('ms-teams-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("email"),
              "properties": z.object({
                "sender": z.string().describe("#### The email address of the sender.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```"),
                "recipient": z.string().describe("#### The email address of the recipient.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("discord"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Discord Webhook URL for the channel. Store as `$Secret()` for security.\n\n---\n\nCreate a webhook in your Discord channel settings (Edit Channel → Integrations → Webhooks).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest.ts\n      memory: 256\n      timeout: 60\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 30000\n          description: Ingest job is taking too long\n          notificationTargets:\n            - type: discord\n              properties:\n                webhookUrl: $Secret('discord-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest.ts' }\n    },\n    memory: 256,\n    timeout: 60,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 30000 } },\n        description: 'Ingest job is taking too long',\n        notificationTargets: [\n          {\n            type: 'discord',\n            properties: {\n              webhookUrl: $Secret('discord-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { ingestFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("webhook"),
              "properties": z.object({
                "url": z.string().describe("#### The URL to send webhook POST requests to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```"),
                "secret": z.string().optional().describe("#### Optional signing secret for HMAC-SHA256 payload verification.\n\n---\n\nIf provided, each request includes an `X-Stacktape-Signature` header.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional(),
                "headers": z.record(z.string(), z.any()).optional().describe("#### Optional custom headers to include in each request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n                headers:\n                  X-Environment: production\n                  Authorization: $Secret('webhook-auth-header')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret'),\n              headers: {\n                'X-Environment': 'production',\n                Authorization: $Secret('webhook-auth-header')\n              }\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional() }).strict()
              .optional().describe("").optional() }).strict()
          ])).optional().describe("#### Where to send notifications when the alarm fires — Slack, MS Teams, or email.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API returning too many errors\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 2\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C12345678\n                accessToken: $Secret('slack-bot-token')\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API returning too many errors',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: { conversationId: 'C12345678', accessToken: $Secret('slack-bot-token') }\n          },\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          },\n          {\n            type: 'ms-teams',\n            properties: { webhookUrl: $Secret('teams-webhook-url') }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "includeInHistory": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether alarm state changes should appear in monitoring history.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Jobs queue backing up\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          includeInHistory: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Jobs queue backing up',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        includeInHistory: false\n      }\n    ]\n  });\n\n  return { resources: { jobsQueue } };\n});\n```").default(true),
          "description": z.string().optional().describe("#### Custom alarm description used in notification messages and the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 80\n          description: Database CPU sustained above 80% — consider scaling up\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: { thresholdPercent: 80 }\n        },\n        description: 'Database CPU sustained above 80% — consider scaling up'\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Alarms for this database (merged with global alarms from the Stacktape Console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    alarms:\n      - trigger:\n          type: database-cpu-utilization\n          properties:\n            thresholdPercent: 85\n        notificationTargets:\n          - type: email\n            properties:\n              sender: alerts@example.com\n              recipient: ops@example.com\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  alarms: [\n    {\n      trigger: {\n        type: 'database-cpu-utilization',\n        properties: { thresholdPercent: 85 }\n      },\n      notificationTargets: [\n        {\n          type: 'email',\n          properties: { sender: 'alerts@example.com', recipient: 'ops@example.com' }\n        }\n      ]\n    }\n  ],\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
        "disabledGlobalAlarms": z.array(z.string()).optional().describe("#### Global alarm names to exclude from this database.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    disabledGlobalAlarms:\n      - db-cpu-high\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  disabledGlobalAlarms: ['db-cpu-high'],\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
        "logging": z.object({
          "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      disabled: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    disabled: true\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false),
          "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      retentionDays: 180\n      logTypes:\n        - postgresql\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    retentionDays: 180,\n    logTypes: ['postgresql']\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(90),
          "logTypes": z.array(z.string()).optional().describe("#### Which log types to export. Depends on engine:\n\n- **PostgreSQL**: `postgresql`\n- **MySQL/MariaDB**: `audit`, `error`, `general`, `slowquery`\n- **Oracle**: `alert`, `audit`, `listener`, `trace`\n- **SQL Server**: `agent`, `error`\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - error\n        - slowquery\n    engine:\n      type: mysql\n      properties:\n        version: '8.0.36'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['error', 'slowquery']\n  },\n  engine: {\n    type: 'mysql',\n    properties: {\n      version: '8.0.36',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
          "engineSpecificOptions": z.union([z.object({
              "log_connections": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Log new client connections.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_connections: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_connections: true\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false),
              "log_disconnections": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Log client disconnections.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_disconnections: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_disconnections: true\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false),
              "log_lock_waits": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Log sessions waiting for locks (helps find lock contention issues).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_lock_waits: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_lock_waits: true\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false),
              "log_min_duration_statement": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Log queries slower than this (ms). `-1` = disabled, `0` = log all. Great for finding slow queries.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_min_duration_statement: 500\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_min_duration_statement: 500\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(10000),
              "log_statement": z.enum(["all","ddl","mod","none"]).optional().describe("#### Which SQL statements to log: `none`, `ddl` (CREATE/ALTER), `mod` (ddl + INSERT/UPDATE/DELETE), `all`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_statement: mod\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_statement: 'mod'\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```") }).strict()
            , z.object({
              "server_audit_events": z.array(z.enum(["CONNECT","QUERY","QUERY_DCL","QUERY_DDL","QUERY_DML","QUERY_DML_NO_SELECT"])).optional().describe("#### What to record in the audit log: connections, all queries, DDL only, DML only, etc.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - audit\n      engineSpecificOptions:\n        server_audit_events:\n          - CONNECT\n          - QUERY_DDL\n    engine:\n      type: mysql\n      properties:\n        version: '8.0.36'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['audit'],\n    engineSpecificOptions: {\n      server_audit_events: ['CONNECT', 'QUERY_DDL']\n    }\n  },\n  engine: {\n    type: 'mysql',\n    properties: {\n      version: '8.0.36',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(["QUERY_DDL"]),
              "long_query_time": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Queries slower than this (seconds) are logged as \"slow queries\". `-1` to disable.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - slowquery\n      engineSpecificOptions:\n        long_query_time: 2\n    engine:\n      type: mysql\n      properties:\n        version: '8.0.36'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['slowquery'],\n    engineSpecificOptions: {\n      long_query_time: 2\n    }\n  },\n  engine: {\n    type: 'mysql',\n    properties: {\n      version: '8.0.36',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(10) }).strict()
          ]).optional().describe("#### Fine-grained logging settings (PostgreSQL: slow queries, statements; MySQL: audit events).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_min_duration_statement: 1000\n        log_statement: ddl\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_min_duration_statement: 1000,\n      log_statement: 'ddl'\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
          "logForwarding": z.union([z.object({
              "type": z.literal("http-endpoint"),
              "properties": z.object({
                "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("highlight"),
              "properties": z.object({
                "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("datadog"),
              "properties": z.object({
                "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
            }).strict()
          ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Database logging (connections, slow queries, errors).\n\n---\n\nLogs are sent to CloudWatch and retained for 90 days by default.\nAvailable log types vary by engine.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      retentionDays: 30\n      logTypes:\n        - postgresql\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    retentionDays: 30,\n    logTypes: ['postgresql']\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
        "dev": z.object({
          "remote": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use the deployed AWS resource instead of a local emulation.\n\n---\n\nBy default, databases, Redis, and DynamoDB run locally in Docker during dev mode.\nSet to `true` to connect to the real deployed resource instead (must be deployed first).\n\nUseful when local emulation doesn't match production behavior closely enough,\nor when you need to work with real data.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      connectTo:\n        - sessionsTable\n  sessionsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: id\n          type: string\n      dev:\n        remote: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, DynamoDbTable, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sessionsTable = new DynamoDbTable({\n    primaryKey: { partitionKey: { name: 'id', type: 'string' } },\n    dev: { remote: true }\n  });\n\n  const myApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    connectTo: [sessionsTable]\n  });\n\n  return { resources: { myApi, sessionsTable } };\n});\n```").default(false) }).strict()
        .optional().describe("#### Dev mode: runs locally in Docker by default. Set `remote: true` to use the deployed database.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    dev:\n      remote: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  dev: { remote: true },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.micro' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### A fully managed relational (SQL) database resource.\n\n---\n\nSupports various database engines like PostgreSQL, MySQL, and MariaDB, with features like clustering and high availability."), z.object({
      "type": z.literal("convex"),
      "properties": z.object({
        "appDirectory": z.string().describe("#### Path to the `convex/` directory in your project.\n\n---\n\nAfter each `stacktape deploy`, Stacktape runs `npx convex deploy` from the parent project directory\nagainst the freshly-deployed backend.\n\nExample: `appDirectory: './convex'`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      backend:\n        resources:\n          cpu: 1\n          memory: 2048\n      database:\n        engine:\n          type: postgres\n          properties:\n            version: '16.6'\n            primaryInstance:\n              instanceSize: db.t4g.small\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    backend: {\n      resources: {\n        cpu: 1,\n        memory: 2048\n      }\n    },\n    database: {\n      engine: {\n        type: 'postgres',\n        properties: {\n          version: '16.6',\n          primaryInstance: {\n            instanceSize: 'db.t4g.small'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```"),
        "functionsDeployment": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether Stacktape should deploy Convex functions after infrastructure deploy.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      functionsDeployment:\n        enabled: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    functionsDeployment: {\n      enabled: false\n    }\n  });\n  return { resources: { backend } };\n});\n```").default(true),
          "command": z.string().optional().describe("#### Custom command to deploy Convex functions.\n\n---\n\nStacktape injects `CONVEX_SELF_HOSTED_URL` and `CONVEX_SELF_HOSTED_ADMIN_KEY` into the command\nenvironment. If omitted, Stacktape runs:\n\n`npx convex deploy --codegen disable --typecheck try`\n\nExamples: `pnpm convex deploy --codegen disable`, `bunx convex deploy --typecheck disable`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      functionsDeployment:\n        command: bunx convex deploy --typecheck disable\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    functionsDeployment: {\n      command: 'bunx convex deploy --typecheck disable'\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "workingDirectory": z.string().optional().describe("#### Working directory for the deploy command.\n\n---\n\nDefaults to the project directory containing `appDirectory` when `appDirectory` points at a\n`convex/` folder.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./packages/api/convex\n      functionsDeployment:\n        workingDirectory: ./packages/api\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './packages/api/convex',\n    functionsDeployment: {\n      workingDirectory: './packages/api'\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional() }).strict()
        .optional().describe("#### How Stacktape deploys Convex functions after infrastructure is ready.\n\n---\n\nBy default, Stacktape runs `npx convex deploy --codegen disable --typecheck try` from the\nproject directory containing `appDirectory`, with `CONVEX_SELF_HOSTED_URL` and\n`CONVEX_SELF_HOSTED_ADMIN_KEY` injected automatically.\n\nSet `enabled: false` if your CI/CD pipeline deploys functions separately, or set `command`\nwhen your project uses a custom package-manager command.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      functionsDeployment:\n        enabled: true\n        command: pnpm convex deploy --codegen disable\n        workingDirectory: ./\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    functionsDeployment: {\n      enabled: true,\n      command: 'pnpm convex deploy --codegen disable',\n      workingDirectory: './'\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
        "customDomains": z.object({
          "cloud": z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          .describe("#### API + WebSocket origin. Set as `CONVEX_CLOUD_ORIGIN` on the backend.\n\n---\n\nFrontend clients connect here via the `convex-js` client. Example: `api.myapp.com`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      customDomains:\n        cloud:\n          domainName: api.myapp.com\n        site:\n          domainName: webhooks.myapp.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    customDomains: {\n      cloud: {\n        domainName: 'api.myapp.com'\n      },\n      site: {\n        domainName: 'webhooks.myapp.com'\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```"),
          "site": z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          .describe("#### HTTP-actions origin. Set as `CONVEX_SITE_ORIGIN` on the backend.\n\n---\n\nUser-defined `httpAction()` routes (webhooks, OAuth callbacks) are served here.\nExample: `webhooks.myapp.com`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      customDomains:\n        cloud:\n          domainName: api.myapp.com\n        site:\n          domainName: webhooks.myapp.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    customDomains: {\n      cloud: {\n        domainName: 'api.myapp.com'\n      },\n      site: {\n        domainName: 'webhooks.myapp.com'\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```"),
          "dashboard": z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          .optional().describe("#### Dashboard domain. Required if `dashboard.enabled` is `true`.\n\n---\n\nExample: `convex-admin.myapp.com`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      dashboard:\n        enabled: true\n      customDomains:\n        cloud:\n          domainName: api.myapp.com\n        site:\n          domainName: webhooks.myapp.com\n        dashboard:\n          domainName: convex-admin.myapp.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    dashboard: {\n      enabled: true\n    },\n    customDomains: {\n      cloud: {\n        domainName: 'api.myapp.com'\n      },\n      site: {\n        domainName: 'webhooks.myapp.com'\n      },\n      dashboard: {\n        domainName: 'convex-admin.myapp.com'\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional() }).strict()
        .optional().describe("#### Custom domains for the Convex backend.\n\n---\n\nConvex exposes two distinct origins that the outside world reaches:\n\n- **`cloud`** — the API + WebSocket endpoint (`CONVEX_CLOUD_ORIGIN`). All client traffic\n  (queries, mutations, actions, reactive subscriptions) hits this URL via the `convex-js`\n  client. Required.\n- **`site`** — the HTTP-actions endpoint (`CONVEX_SITE_ORIGIN`). User-defined `httpAction()`\n  routes (webhooks, OAuth callbacks, etc.) live here. Kept separate from `cloud` so webhook\n  URLs don't collide with internal API paths. Required.\n- **`dashboard`** — required when `dashboard.enabled` is `true`. The dashboard serves at this domain.\n\nEach domain must have a Route53 hosted zone in your AWS account. Stacktape provisions free\nTLS certificates and DNS records automatically.\n\nIf `customDomains` is omitted entirely, the ALB's default DNS is used with port-based routing\n(3210 cloud, 3211 site, 6791 dashboard). Fine for dev/staging; **not recommended for\nproduction** — the ALB DNS is unstable across stack recreations, and clients hard-code the URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      dashboard:\n        enabled: true\n      customDomains:\n        cloud:\n          domainName: api.myapp.com\n        site:\n          domainName: webhooks.myapp.com\n        dashboard:\n          domainName: convex-admin.myapp.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    dashboard: {\n      enabled: true\n    },\n    customDomains: {\n      cloud: {\n        domainName: 'api.myapp.com'\n      },\n      site: {\n        domainName: 'webhooks.myapp.com'\n      },\n      dashboard: {\n        domainName: 'convex-admin.myapp.com'\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
        "backend": z.object({
          "resources": z.object({
            "cpu": z.union([z.literal(0.25), z.literal(0.5), z.literal(1), z.literal(16), z.literal(2), z.literal(4), z.literal(8)]).optional().describe("#### vCPUs for the workload (Fargate). Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 2\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 2,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
            "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB. Must be compatible with the vCPU count on Fargate.\n\n---\n\nFargate valid combos: 0.25 vCPU → 512-2048 MB, 0.5 → 1024-4096, 1 → 2048-8192, 2 → 4096-16384,\n4 → 8192-30720, 8 → 16384-61440, 16 → 32768-122880.\nFor EC2: auto-detected from instance type if omitted.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 1,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
            "instanceTypes": z.array(z.string()).optional().describe("#### EC2 instance types for the workload (e.g., `t3.medium`, `c6g.large`). Use instead of `cpu`/`memory`.\n\n---\n\nFirst type in the list is preferred. Instances auto-scale and are refreshed weekly for patching.\nTip: specify a single type and omit `cpu`/`memory` for optimal sizing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - c6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['c6g.large']\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
            "enableWarmPool": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Keep pre-initialized EC2 instances ready for faster scaling. Only works with a single instance type.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - t3.medium\n        enableWarmPool: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['t3.medium'],\n      enableWarmPool: true\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
            "architecture": z.enum(["arm64","x86_64"]).optional().describe("#### CPU architecture for Fargate. `arm64` is ~20% cheaper. Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 0.5,\n      memory: 1024,\n      architecture: 'arm64'\n    }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
          .optional().describe("#### CPU, memory, and compute engine for the backend container.\n\n---\n\nDefaults to `{ cpu: 0.5, memory: 1024 }`. Override this for production traffic:\n\n- **Hobby / small dev**: `{ cpu: 0.5, memory: 1024 }` — fine for a few dozen concurrent users\n- **Production baseline**: `{ cpu: 1, memory: 2048 }` — handles hundreds of concurrent reactive subscribers\n- **Heavier production**: `{ cpu: 2, memory: 4096 }` or `{ cpu: 4, memory: 8192 }` — thousands of subscribers, vector search\n\nFor EC2 instead of Fargate, specify `instanceTypes` (e.g., `['c6g.large']`). EC2 is typically\ncheaper per vCPU and supports `enableWarmPool: true` for faster cold-starts.\n\nConvex backend is single-process — scale **vertically** (bigger box), not horizontally.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      backend:\n        resources:\n          cpu: 4\n          memory: 8192\n          architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    backend: {\n      resources: {\n        cpu: 4,\n        memory: 8192,\n        architecture: 'arm64'\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "image": z.string().optional().describe("#### Pinned Convex backend Docker image.\n\n---\n\nDefaults to a known-good version pinned by Stacktape (currently from `ghcr.io/get-convex/convex-backend`).\nOverride to test newer/older versions. Image upgrades trigger Convex's in-place migration path.\n\nExample: `image: 'ghcr.io/get-convex/convex-backend:0a8d9ae0f0e5c6c9c0c0c0c0'`\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      backend:\n        image: ghcr.io/get-convex/convex-backend:0a8d9ae0f0e5c6c9c0c0c0c0\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    backend: {\n      image: 'ghcr.io/get-convex/convex-backend:0a8d9ae0f0e5c6c9c0c0c0c0'\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            disabled: true\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { disabled: true }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 30 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Logging configuration for the backend container.\n\n---\n\nContainer `stdout`/`stderr` are sent to CloudWatch and retained for 90 days by default.\nView logs with `stacktape logs <resourceName>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      backend:\n        logging:\n          retentionDays: 14\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    backend: {\n      logging: {\n        retentionDays: 14\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "enableRemoteSessions": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Allow SSH-like sessions into the running backend container for debugging.\n\n---\n\nStacktape enables ECS Exec for Convex internally because it is required to generate the managed\nadmin key after the backend starts. This property is kept for compatibility with generic workload\ncontrols and may be removed in a future Convex resource revision.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      backend:\n        enableRemoteSessions: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    backend: {\n      enableRemoteSessions: true\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional() }).strict()
        .optional().describe("#### Configuration for the Convex backend container (the Rust server process).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      backend:\n        resources:\n          cpu: 2\n          memory: 4096\n        logging:\n          retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    backend: {\n      resources: {\n        cpu: 2,\n        memory: 4096\n      },\n      logging: {\n        retentionDays: 30\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
        "dashboard": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether to provision the admin dashboard.\n\n---\n\nThe dashboard is a small stateless Next.js app (~$3–5/month at default sizing) that gives\nyou a data browser, log viewer, function REPL, env-var manager, and snapshot export/import\nUI. Disable only if you have a strong reason — self-hosted Convex without the dashboard is\noperationally painful.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      dashboard:\n        enabled: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    dashboard: {\n      enabled: false\n    }\n  });\n  return { resources: { backend } };\n});\n```").default(true),
          "allowedIpRanges": z.array(z.string()).optional().describe("#### CIDR ranges allowed to reach the dashboard. By default the dashboard is internet-reachable.\n\n---\n\nThe dashboard has no built-in authentication — the admin key (which you paste on login) is\nthe only security barrier. Convex's admin key is high-entropy and is the same model managed\nConvex uses, but if you want defense-in-depth, pin access to your office IPs or VPN range.\n\nExample: `allowedIpRanges: ['203.0.113.0/24', '198.51.100.42/32']`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      dashboard:\n        enabled: true\n        allowedIpRanges:\n          - 203.0.113.0/24\n          - 198.51.100.42/32\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    dashboard: {\n      enabled: true,\n      allowedIpRanges: ['203.0.113.0/24', '198.51.100.42/32']\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "resources": z.object({
            "cpu": z.union([z.literal(0.25), z.literal(0.5), z.literal(1), z.literal(16), z.literal(2), z.literal(4), z.literal(8)]).optional().describe("#### vCPUs for the workload (Fargate). Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 2\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 2,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
            "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB. Must be compatible with the vCPU count on Fargate.\n\n---\n\nFargate valid combos: 0.25 vCPU → 512-2048 MB, 0.5 → 1024-4096, 1 → 2048-8192, 2 → 4096-16384,\n4 → 8192-30720, 8 → 16384-61440, 16 → 32768-122880.\nFor EC2: auto-detected from instance type if omitted.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 1,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
            "instanceTypes": z.array(z.string()).optional().describe("#### EC2 instance types for the workload (e.g., `t3.medium`, `c6g.large`). Use instead of `cpu`/`memory`.\n\n---\n\nFirst type in the list is preferred. Instances auto-scale and are refreshed weekly for patching.\nTip: specify a single type and omit `cpu`/`memory` for optimal sizing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - c6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['c6g.large']\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
            "enableWarmPool": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Keep pre-initialized EC2 instances ready for faster scaling. Only works with a single instance type.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - t3.medium\n        enableWarmPool: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['t3.medium'],\n      enableWarmPool: true\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
            "architecture": z.enum(["arm64","x86_64"]).optional().describe("#### CPU architecture for Fargate. `arm64` is ~20% cheaper. Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 0.5,\n      memory: 1024,\n      architecture: 'arm64'\n    }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
          .optional().describe("#### CPU, memory, and compute engine for the dashboard container.\n\n---\n\nDefaults to `{ cpu: 0.25, memory: 512 }`. The dashboard is a Next.js app and is very light —\n`{ cpu: 0.25, memory: 512 }` is plenty for most teams.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      dashboard:\n        enabled: true\n        resources:\n          cpu: 0.5\n          memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    dashboard: {\n      enabled: true,\n      resources: {\n        cpu: 0.5,\n        memory: 1024\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "image": z.string().optional().describe("#### Pinned Convex dashboard Docker image.\n\n---\n\nDefaults to a known-good version pinned by Stacktape (currently from `ghcr.io/get-convex/convex-dashboard`).\nOverride to test newer/older versions.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      dashboard:\n        enabled: true\n        image: ghcr.io/get-convex/convex-dashboard:0a8d9ae0f0e5c6c9c0c0c0c0\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    dashboard: {\n      enabled: true,\n      image: 'ghcr.io/get-convex/convex-dashboard:0a8d9ae0f0e5c6c9c0c0c0c0'\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            disabled: true\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { disabled: true }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 30 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Logging configuration for the dashboard container.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      dashboard:\n        enabled: true\n        logging:\n          retentionDays: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    dashboard: {\n      enabled: true,\n      logging: {\n        retentionDays: 7\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional() }).strict()
        .optional().describe("#### Configuration for the Convex admin dashboard.\n\n---\n\nEnabled by default. The dashboard is a stateless Next.js app that talks to the backend's\nREST API using the admin key (which you paste on first login). To opt out, set\n`dashboard.enabled: false`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      dashboard:\n        enabled: true\n        allowedIpRanges:\n          - 203.0.113.0/24\n        resources:\n          cpu: 0.25\n          memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    dashboard: {\n      enabled: true,\n      allowedIpRanges: ['203.0.113.0/24'],\n      resources: {\n        cpu: 0.25,\n        memory: 512\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
        "database": z.object({
          "engine": z.union([z.object({
              "type": z.enum(["aurora-mysql","aurora-postgresql"]),
              "properties": z.object({
                "dbName": z.string().optional().describe("#### Name of the initial database.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql\n      properties:\n        version: '16.6'\n        dbName: appdb\n        instances:\n          - instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql',\n    properties: {\n      version: '16.6',\n      dbName: 'appdb',\n      instances: [{ instanceSize: 'db.r6g.large' }]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default("defdb"),
                "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port. Defaults: aurora-mysql 3306, aurora-postgresql 5432.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql\n      properties:\n        version: '16.6'\n        port: 5433\n        instances:\n          - instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql',\n    properties: {\n      version: '16.6',\n      port: 5433,\n      instances: [{ instanceSize: 'db.r6g.large' }]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
                "instances": z.array(z.object({
                  "instanceSize": z.string().describe("#### Instance size (e.g., `db.t4g.medium`, `db.r6g.large`).\n\n---\n\n`t` family = burstable (dev/low-traffic). `r` family = memory-optimized (production).\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql\n      properties:\n        version: '16.6'\n        instances:\n          - instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql',\n    properties: {\n      version: '16.6',\n      instances: [\n        { instanceSize: 'db.r6g.large' }\n      ]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```") }).strict()
                ).describe("#### Cluster instances. First = primary (writer), rest = read replicas.\n\n---\n\nReads are load-balanced across all instances. If the primary fails,\na replica is automatically promoted (usually within 30 seconds).\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql\n      properties:\n        version: '16.6'\n        instances:\n          - instanceSize: db.r6g.large\n          - instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql',\n    properties: {\n      version: '16.6',\n      instances: [\n        { instanceSize: 'db.r6g.large' },\n        { instanceSize: 'db.r6g.large' }\n      ]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
                "version": z.string().describe("#### Engine version (e.g., `16.6` for PostgreSQL, `8.0.36` for MySQL).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
                "disableAutoMinorVersionUpgrade": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip automatic minor version upgrades (e.g., 16.4 → 16.5).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        disableAutoMinorVersionUpgrade: true\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      disableAutoMinorVersionUpgrade: true,\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false) }).strict()
            }).strict()
            .describe("#### Aurora: high-performance clustered database with auto-failover.\n\n---\n\nUp to 5x faster than MySQL, 3x faster than PostgreSQL. Data is replicated across 3 AZs\nautomatically. If the primary instance fails, a read replica is promoted in seconds."), z.object({
              "type": z.enum(["aurora-mysql-serverless-v2","aurora-postgresql-serverless-v2"]),
              "properties": z.object({
                "dbName": z.string().optional().describe("#### Name of the initial database.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql-serverless-v2\n      properties:\n        version: '16.6'\n        dbName: appdb\n        minCapacity: 0.5\n        maxCapacity: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql-serverless-v2',\n    properties: {\n      version: '16.6',\n      dbName: 'appdb',\n      minCapacity: 0.5,\n      maxCapacity: 8\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default("defdb"),
                "minCapacity": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum ACUs (0.5-128 in 0.5 increments). ~1 ACU ≈ 2 GB RAM.\n\n---\n\nSet low (0.5) for dev/staging to minimize cost. The database scales up instantly under load.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql-serverless-v2\n      properties:\n        version: '16.6'\n        minCapacity: 0.5\n        maxCapacity: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql-serverless-v2',\n    properties: {\n      version: '16.6',\n      minCapacity: 0.5,\n      maxCapacity: 8\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(0.5),
                "maxCapacity": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum ACUs (0.5-128 in 0.5 increments). Caps your scaling and cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql-serverless-v2\n      properties:\n        version: '16.6'\n        minCapacity: 0.5\n        maxCapacity: 16\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql-serverless-v2',\n    properties: {\n      version: '16.6',\n      minCapacity: 0.5,\n      maxCapacity: 16\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(10),
                "serverlessReadersCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Number of reader instances in the Aurora Serverless v2 cluster.\n\n---\n\nAurora Serverless v2 always has one writer instance. This value adds additional readers\n(`0` means writer only, `2` means writer + 2 readers).\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: aurora-postgresql-serverless-v2\n      properties:\n        version: '16.6'\n        minCapacity: 0.5\n        maxCapacity: 16\n        serverlessReadersCount: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'aurora-postgresql-serverless-v2',\n    properties: {\n      version: '16.6',\n      minCapacity: 0.5,\n      maxCapacity: 16,\n      serverlessReadersCount: 2\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(0),
                "version": z.string().describe("#### Engine version (e.g., `16.6` for PostgreSQL, `8.0.36` for MySQL).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
                "disableAutoMinorVersionUpgrade": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip automatic minor version upgrades (e.g., 16.4 → 16.5).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        disableAutoMinorVersionUpgrade: true\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      disableAutoMinorVersionUpgrade: true,\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false) }).strict()
            }).strict()
            .describe("#### Aurora Serverless v2: recommended for most new projects.\n\n---\n\nScales instantly from 0.5 to 128 ACUs in 0.5-ACU increments (~1 ACU ≈ 2 GB RAM).\nYou pay only for the capacity used, making it cost-effective for variable workloads."), z.object({
              "type": z.enum(["mariadb","mysql","oracle-ee","oracle-se2","postgres","sqlserver-ee","sqlserver-ex","sqlserver-se","sqlserver-web"]),
              "properties": z.object({
                "dbName": z.string().optional().describe("#### Name of the database created on initialization. For Oracle, this is the SID. Not applicable to SQL Server.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        dbName: appdb\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      dbName: 'appdb',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
                "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port the database listens on. Defaults: PostgreSQL 5432, MySQL/MariaDB 3306, Oracle 1521, SQL Server 1433.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        port: 5433\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      port: 5433,\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
                "storage": z.object({
                  "initialSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Initial storage in GB. Auto-scales up when free space is low.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        storage:\n          initialSize: 100\n          maxSize: 500\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      storage: {\n        initialSize: 100,\n        maxSize: 500\n      },\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(20),
                  "maxSize": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max storage in GB. The database won't auto-scale beyond this.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        storage:\n          initialSize: 100\n          maxSize: 1000\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      storage: {\n        initialSize: 100,\n        maxSize: 1000\n      },\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(200) }).strict()
                .optional().describe("#### Storage configuration. Auto-scales up when free space is low.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        storage:\n          initialSize: 50\n          maxSize: 500\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      storage: { initialSize: 50, maxSize: 500 },\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
                "primaryInstance": z.object({
                  "instanceSize": z.string().describe("#### Instance size (e.g., `db.t4g.micro`, `db.r6g.large`).\n\n---\n\nDetermines CPU, memory, and network capacity. Quick guide:\n- **db.t4g.micro** (~$12/mo): Dev/testing, 2 vCPU, 1 GB RAM\n- **db.t4g.medium** (~$50/mo): Small production, 2 vCPU, 4 GB RAM\n- **db.r6g.large** (~$180/mo): Production, 2 vCPU, 16 GB RAM\n\n`t` family instances are burstable (fine for low/variable load). Use `r` family for steady workloads.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.r6g.large'\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
                  "multiAz": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Create a standby replica in another availability zone for automatic failover.\n\n---\n\nIf the primary goes down, traffic fails over to the standby automatically.\nAlso reduces downtime during maintenance. Doubles the instance cost.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n          multiAz: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.t4g.medium',\n        multiAz: true\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional() }).strict()
                .describe("#### The primary (writer) instance. Handles all write operations.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.r6g.large\n          multiAz: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.r6g.large',\n        multiAz: true\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
                "readReplicas": z.array(z.object({
                  "instanceSize": z.string().describe("#### Instance size (e.g., `db.t4g.micro`, `db.r6g.large`).\n\n---\n\nDetermines CPU, memory, and network capacity. Quick guide:\n- **db.t4g.micro** (~$12/mo): Dev/testing, 2 vCPU, 1 GB RAM\n- **db.t4g.medium** (~$50/mo): Small production, 2 vCPU, 4 GB RAM\n- **db.r6g.large** (~$180/mo): Production, 2 vCPU, 16 GB RAM\n\n`t` family instances are burstable (fine for low/variable load). Use `r` family for steady workloads.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.r6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.r6g.large'\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
                  "multiAz": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Create a standby replica in another availability zone for automatic failover.\n\n---\n\nIf the primary goes down, traffic fails over to the standby automatically.\nAlso reduces downtime during maintenance. Doubles the instance cost.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n          multiAz: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: {\n        instanceSize: 'db.t4g.medium',\n        multiAz: true\n      }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional() }).strict()
                ).optional().describe("#### Read replicas to offload read traffic from the primary instance.\n\n---\n\nEach replica gets its own endpoint. Data is replicated asynchronously from the primary.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.r6g.large\n        readReplicas:\n          - instanceSize: db.r6g.large\n          - instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.r6g.large' },\n      readReplicas: [\n        { instanceSize: 'db.r6g.large' },\n        { instanceSize: 'db.t4g.medium' }\n      ]\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
                "version": z.string().describe("#### Engine version (e.g., `16.6` for PostgreSQL, `8.0.36` for MySQL).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```"),
                "disableAutoMinorVersionUpgrade": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip automatic minor version upgrades (e.g., 16.4 → 16.5).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        disableAutoMinorVersionUpgrade: true\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      disableAutoMinorVersionUpgrade: true,\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false) }).strict()
            }).strict()
            .describe("#### Standard RDS: single-instance database with predictable pricing.\n\n---\n\nChoose a fixed instance size and pay hourly. AWS handles patching, backups, and recovery.\nFor high availability, enable `multiAz` on the primary instance.")]).optional().describe("#### Database engine override. Defaults to RDS PostgreSQL 16 on `db.t4g.micro`.\n\n---\n\nConvex requires PostgreSQL 13+. To use Aurora Serverless v2 instead (auto-scales 0.5–8 ACU,\nhigher idle cost but elastic), set `{ type: 'aurora-postgresql-serverless-v2', properties: { ... } }`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      database:\n        engine:\n          type: postgres\n          properties:\n            version: '16.6'\n            primaryInstance:\n              instanceSize: db.t4g.medium\n              multiAz: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    database: {\n      engine: {\n        type: 'postgres',\n        properties: {\n          version: '16.6',\n          primaryInstance: {\n            instanceSize: 'db.t4g.medium',\n            multiAz: true\n          }\n        }\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "accessibility": z.object({
            "accessibilityMode": z.enum(["internet","scoping-workloads-in-vpc","vpc","whitelisted-ips-only"]).describe("#### Controls who can connect to your database.\n\n---\n\n- **`internet`** (default): Anyone with the credentials can connect. Simplest setup, great for development.\n  The database is still protected by username/password.\n- **`vpc`**: Only your app's resources (and anything in the same VPC) can connect.\n  You can also whitelist specific IPs (e.g., your office) using `whitelistedIps`.\n- **`scoping-workloads-in-vpc`**: Most restrictive. Only resources that explicitly list this\n  database in their `connectTo` can reach it. Best for production.\n- **`whitelisted-ips-only`**: Only the IP addresses you list in `whitelistedIps` can connect.\n\n> Aurora Serverless engines only support `vpc` or `scoping-workloads-in-vpc`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: vpc\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: {\n    accessibilityMode: 'vpc'\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default("internet"),
            "forceDisablePublicIp": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Remove the database's public IP entirely (VPC-only access).\n\n---\n\n> For Aurora, this can only be set at creation time and cannot be changed later.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: vpc\n      forceDisablePublicIp: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: {\n    accessibilityMode: 'vpc',\n    forceDisablePublicIp: true\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
            "whitelistedIps": z.array(z.string()).optional().describe("#### IP addresses or CIDR ranges allowed to connect (e.g., `203.0.113.50/32`).\n\n---\n\n- In `vpc`/`scoping-workloads-in-vpc`: adds external IPs on top of VPC access (e.g., your office).\n- In `whitelisted-ips-only`: only these IPs can connect.\n- No effect in `internet` mode.\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    accessibility:\n      accessibilityMode: whitelisted-ips-only\n      whitelistedIps:\n        - 203.0.113.50/32\n        - 198.51.100.0/24\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  accessibility: {\n    accessibilityMode: 'whitelisted-ips-only',\n    whitelistedIps: ['203.0.113.50/32', '198.51.100.0/24']\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional() }).strict()
          .optional().describe("#### Database network accessibility. Defaults to `scoping-workloads-in-vpc`.\n\n---\n\nThe Convex backend auto-connects internally, so users have no reason to set `internet` here\n— direct `psql` access to Convex's internal Postgres is almost always wrong (use the dashboard\nor `npx convex export` instead).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      database:\n        accessibility:\n          accessibilityMode: scoping-workloads-in-vpc\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    database: {\n      accessibility: {\n        accessibilityMode: 'scoping-workloads-in-vpc'\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "automatedBackupRetentionDays": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Days to keep automated daily backups (0–35). Defaults to 1.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      database:\n        automatedBackupRetentionDays: 14\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    database: {\n      automatedBackupRetentionDays: 14\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "preferredMaintenanceWindow": z.string().optional().describe("#### When maintenance happens. Format: `Sun:02:00-Sun:04:00` (UTC).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      database:\n        preferredMaintenanceWindow: Sun:02:00-Sun:04:00\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    database: {\n      preferredMaintenanceWindow: 'Sun:02:00-Sun:04:00'\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      disabled: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    disabled: true\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      retentionDays: 180\n      logTypes:\n        - postgresql\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    retentionDays: 180,\n    logTypes: ['postgresql']\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(90),
            "logTypes": z.array(z.string()).optional().describe("#### Which log types to export. Depends on engine:\n\n- **PostgreSQL**: `postgresql`\n- **MySQL/MariaDB**: `audit`, `error`, `general`, `slowquery`\n- **Oracle**: `alert`, `audit`, `listener`, `trace`\n- **SQL Server**: `agent`, `error`\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - error\n        - slowquery\n    engine:\n      type: mysql\n      properties:\n        version: '8.0.36'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['error', 'slowquery']\n  },\n  engine: {\n    type: 'mysql',\n    properties: {\n      version: '8.0.36',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
            "engineSpecificOptions": z.union([z.object({
                "log_connections": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Log new client connections.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_connections: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_connections: true\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false),
                "log_disconnections": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Log client disconnections.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_disconnections: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_disconnections: true\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false),
                "log_lock_waits": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Log sessions waiting for locks (helps find lock contention issues).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_lock_waits: true\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_lock_waits: true\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(false),
                "log_min_duration_statement": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Log queries slower than this (ms). `-1` = disabled, `0` = log all. Great for finding slow queries.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_min_duration_statement: 500\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_min_duration_statement: 500\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(10000),
                "log_statement": z.enum(["all","ddl","mod","none"]).optional().describe("#### Which SQL statements to log: `none`, `ddl` (CREATE/ALTER), `mod` (ddl + INSERT/UPDATE/DELETE), `all`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_statement: mod\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_statement: 'mod'\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```") }).strict()
              , z.object({
                "server_audit_events": z.array(z.enum(["CONNECT","QUERY","QUERY_DCL","QUERY_DDL","QUERY_DML","QUERY_DML_NO_SELECT"])).optional().describe("#### What to record in the audit log: connections, all queries, DDL only, DML only, etc.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - audit\n      engineSpecificOptions:\n        server_audit_events:\n          - CONNECT\n          - QUERY_DDL\n    engine:\n      type: mysql\n      properties:\n        version: '8.0.36'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['audit'],\n    engineSpecificOptions: {\n      server_audit_events: ['CONNECT', 'QUERY_DDL']\n    }\n  },\n  engine: {\n    type: 'mysql',\n    properties: {\n      version: '8.0.36',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(["QUERY_DDL"]),
                "long_query_time": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Queries slower than this (seconds) are logged as \"slow queries\". `-1` to disable.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - slowquery\n      engineSpecificOptions:\n        long_query_time: 2\n    engine:\n      type: mysql\n      properties:\n        version: '8.0.36'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['slowquery'],\n    engineSpecificOptions: {\n      long_query_time: 2\n    }\n  },\n  engine: {\n    type: 'mysql',\n    properties: {\n      version: '8.0.36',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").default(10) }).strict()
            ]).optional().describe("#### Fine-grained logging settings (PostgreSQL: slow queries, statements; MySQL: audit events).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nmainDatabase:\n  type: relational-database\n  properties:\n    credentials:\n      masterUserPassword: $Secret('mainDatabase.password')\n    logging:\n      logTypes:\n        - postgresql\n      engineSpecificOptions:\n        log_min_duration_statement: 1000\n        log_statement: ddl\n    engine:\n      type: postgres\n      properties:\n        version: '16.6'\n        primaryInstance:\n          instanceSize: db.t4g.medium\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst mainDatabase = new RelationalDatabase({\n  credentials: { masterUserPassword: $Secret('mainDatabase.password') },\n  logging: {\n    logTypes: ['postgresql'],\n    engineSpecificOptions: {\n      log_min_duration_statement: 1000,\n      log_statement: 'ddl'\n    }\n  },\n  engine: {\n    type: 'postgres',\n    properties: {\n      version: '16.6',\n      primaryInstance: { instanceSize: 'db.t4g.medium' }\n    }\n  }\n});\nreturn { resources: { mainDatabase } };\n});\n```").optional(),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Database logging (connections, slow queries, errors).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      database:\n        logging:\n          retentionDays: 30\n          logTypes:\n            - postgresql\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    database: {\n      logging: {\n        retentionDays: 30,\n        logTypes: ['postgresql']\n      }\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional() }).strict()
        .optional().describe("#### Override the PostgreSQL database that backs the Convex deployment.\n\n---\n\nDefaults to a single-AZ RDS PostgreSQL `db.t4g.micro` instance (cheapest production-viable\noption, ~$13/month). The shape mirrors a subset of [`relational-database`](https://docs.stacktape.com/resources/relational-databases/) — override only what\nyou need. Common reasons to override: bump to Aurora Serverless v2 for auto-scaling, enable\nmulti-AZ for HA, or increase storage retention.\n\nYou cannot bring an existing external database — Convex assumes it owns its Postgres entirely.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      database:\n        engine:\n          type: aurora-postgresql-serverless-v2\n          properties:\n            version: '16.6'\n            minCapacity: 0.5\n            maxCapacity: 4\n        automatedBackupRetentionDays: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    database: {\n      engine: {\n        type: 'aurora-postgresql-serverless-v2',\n        properties: {\n          version: '16.6',\n          minCapacity: 0.5,\n          maxCapacity: 4\n        }\n      },\n      automatedBackupRetentionDays: 7\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
        "storage": z.object({
          "encryption": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Encrypt stored objects at rest (AES-256).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      storage:\n        encryption: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    storage: {\n      encryption: true\n    }\n  });\n  return { resources: { backend } };\n});\n```").default(true),
          "versioning": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Keep previous versions of overwritten/deleted objects. Useful for recovery; increases storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      storage:\n        versioning: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    storage: {\n      versioning: true\n    }\n  });\n  return { resources: { backend } };\n});\n```").default(false),
          "lifecycleRules": z.array(z.union([z.object({
              "type": z.literal("expiration"),
              "properties": z.object({
                "daysAfterUpload": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Delete objects this many days after upload.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: tmp/\n            daysAfterUpload: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'tmp/',\n          daysAfterUpload: 7\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
                "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
                "tags": z.array(z.object({
                  "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                  "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
                ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("non-current-version-expiration"),
              "properties": z.object({
                "daysAfterVersioned": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Delete old versions this many days after they become non-current. Requires `versioning: true`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      versioning: true\n      lifecycleRules:\n        - type: non-current-version-expiration\n          properties:\n            daysAfterVersioned: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    versioning: true,\n    lifecycleRules: [\n      {\n        type: 'non-current-version-expiration',\n        properties: {\n          daysAfterVersioned: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
                "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
                "tags": z.array(z.object({
                  "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                  "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
                ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("class-transition"),
              "properties": z.object({
                "daysAfterUpload": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Move objects to a cheaper storage class this many days after upload.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            prefix: archive/\n            daysAfterUpload: 90\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          prefix: 'archive/',\n          daysAfterUpload: 90,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
                "storageClass": z.enum(["DEEP_ARCHIVE","GLACIER","INTELLIGENT_TIERING","ONEZONE_IA","STANDARD_IA"]).describe("#### Target storage class. Cheaper classes have higher retrieval costs/latency.\n\n---\n\n- `STANDARD_IA` / `ONEZONE_IA`: Infrequent access, instant retrieval.\n- `INTELLIGENT_TIERING`: AWS auto-moves between tiers based on access patterns.\n- `GLACIER`: Archive, minutes to hours for retrieval.\n- `DEEP_ARCHIVE`: Cheapest, 12+ hours for retrieval.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            prefix: cold/\n            daysAfterUpload: 180\n            storageClass: DEEP_ARCHIVE\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          prefix: 'cold/',\n          daysAfterUpload: 180,\n          storageClass: 'DEEP_ARCHIVE'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
                "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
                "tags": z.array(z.object({
                  "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                  "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
                ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("non-current-version-class-transition"),
              "properties": z.object({
                "daysAfterVersioned": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Move old versions to a cheaper storage class this many days after becoming non-current.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      versioning: true\n      lifecycleRules:\n        - type: non-current-version-class-transition\n          properties:\n            daysAfterVersioned: 30\n            storageClass: STANDARD_IA\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    versioning: true,\n    lifecycleRules: [\n      {\n        type: 'non-current-version-class-transition',\n        properties: {\n          daysAfterVersioned: 30,\n          storageClass: 'STANDARD_IA'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
                "storageClass": z.enum(["DEEP_ARCHIVE","GLACIER","INTELLIGENT_TIERING","ONEZONE_IA","STANDARD_IA"]).describe("#### Target storage class for non-current versions.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      versioning: true\n      lifecycleRules:\n        - type: non-current-version-class-transition\n          properties:\n            daysAfterVersioned: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    versioning: true,\n    lifecycleRules: [\n      {\n        type: 'non-current-version-class-transition',\n        properties: {\n          daysAfterVersioned: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
                "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
                "tags": z.array(z.object({
                  "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                  "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
                ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("abort-incomplete-multipart-upload"),
              "properties": z.object({
                "daysAfterInitiation": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Clean up incomplete multipart uploads after this many days. Prevents storage waste.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: abort-incomplete-multipart-upload\n          properties:\n            daysAfterInitiation: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'abort-incomplete-multipart-upload',\n        properties: {\n          daysAfterInitiation: 7\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```"),
                "prefix": z.string().optional().describe("#### Only apply this rule to objects with this key prefix (e.g., `logs/`, `uploads/`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: expiration\n          properties:\n            prefix: logs/\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          prefix: 'logs/',\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional(),
                "tags": z.array(z.object({
                  "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
                  "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
                ).optional().describe("#### Only apply this rule to objects with these tags.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myBucket:\n    type: bucket\n    properties:\n      lifecycleRules:\n        - type: class-transition\n          properties:\n            tags:\n              - key: tier\n                value: archive\n            daysAfterUpload: 60\n            storageClass: GLACIER\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const myBucket = new Bucket({\n    lifecycleRules: [\n      {\n        type: 'class-transition',\n        properties: {\n          tags: [{ key: 'tier', value: 'archive' }],\n          daysAfterUpload: 60,\n          storageClass: 'GLACIER'\n        }\n      }\n    ]\n  });\n  return { resources: { myBucket } };\n});\n```").optional() }).strict()
            }).strict()
          ])).optional().describe("#### Auto-delete or move objects to cheaper storage classes over time.\n\n---\n\nApplied to all five buckets. Most useful for the `exports` bucket if you don't want old\nsnapshot exports accumulating indefinitely.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      storage:\n        versioning: true\n        lifecycleRules:\n          - type: expiration\n            properties:\n              prefix: exports/\n              daysAfterUpload: 30\n          - type: non-current-version-expiration\n            properties:\n              daysAfterVersioned: 90\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    storage: {\n      versioning: true,\n      lifecycleRules: [\n        {\n          type: 'expiration',\n          properties: {\n            prefix: 'exports/',\n            daysAfterUpload: 30\n          }\n        },\n        {\n          type: 'non-current-version-expiration',\n          properties: {\n            daysAfterVersioned: 90\n          }\n        }\n      ]\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional() }).strict()
        .optional().describe("#### Shared configuration applied to all five Convex S3 buckets (`modules`, `files`, `search`,\n`exports`, `snapshot_imports`).\n\n---\n\nEach Convex deployment requires five separate buckets internally. By default they are all\nprivate, encrypted at rest, with versioning disabled. Use this property to override defaults\nacross all five at once (e.g., enable versioning for prod).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      storage:\n        encryption: true\n        versioning: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    storage: {\n      encryption: true,\n      versioning: true\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
        "dev": z.object({
          "remote": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use the deployed AWS resource instead of a local emulation.\n\n---\n\nBy default, databases, Redis, and DynamoDB run locally in Docker during dev mode.\nSet to `true` to connect to the real deployed resource instead (must be deployed first).\n\nUseful when local emulation doesn't match production behavior closely enough,\nor when you need to work with real data.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      connectTo:\n        - sessionsTable\n  sessionsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: id\n          type: string\n      dev:\n        remote: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, DynamoDbTable, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sessionsTable = new DynamoDbTable({\n    primaryKey: { partitionKey: { name: 'id', type: 'string' } },\n    dev: { remote: true }\n  });\n\n  const myApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    connectTo: [sessionsTable]\n  });\n\n  return { resources: { myApi, sessionsTable } };\n});\n```").default(false) }).strict()
        .optional().describe("#### Dev mode: runs the convex-backend locally in Docker by default with SQLite + local\nfilesystem storage.\n\n---\n\nSet `remote: true` to point `stacktape dev` at the deployed AWS backend instead. Local mode\nis recommended because Convex's save-push-reload loop is noticeably faster over loopback than\nacross the WAN, and avoids 24/7 Fargate + RDS cost per developer.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      dev:\n        remote: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    dev: {\n      remote: true\n    }\n  });\n  return { resources: { backend } };\n});\n```").optional(),
        "deletionProtection": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Prevent accidental deletion of the database and the five storage buckets.\n\n---\n\nWhen `true`, Stacktape sets `deletionProtection` on the underlying RDS instance and retention\npolicies on the buckets. You must set this to `false` and redeploy before you can delete the stack.\n\nRecommended for production stages.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      deletionProtection: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    deletionProtection: true\n  });\n  return { resources: { backend } };\n});\n```").default(false),
        "alarms": z.array(z.union([z.object({
            "trigger": z.union([z.object({
                "type": z.literal("application-load-balancer-custom"),
                "properties": z.object({
                  "metric": z.enum(["ActiveConnectionCount","AnomalousHostCount","ClientTLSNegotiationErrorCount","ConsumedLCUs","DesyncMitigationMode_NonCompliant_Request_Count","DroppedInvalidHeaderRequestCount","ELBAuthError","ELBAuthFailure","ELBAuthLatency","ELBAuthRefreshTokenSuccess","ELBAuthSuccess","ELBAuthUserClaimsSizeExceeded","ForwardedInvalidHeaderRequestCount","GrpcRequestCount","HTTPCode_ELB_3XX_Count","HTTPCode_ELB_4XX_Count","HTTPCode_ELB_500_Count","HTTPCode_ELB_502_Count","HTTPCode_ELB_503_Count","HTTPCode_ELB_504_Count","HTTPCode_ELB_5XX_Count","HTTPCode_Target_2XX_Count","HTTPCode_Target_3XX_Count","HTTPCode_Target_4XX_Count","HTTPCode_Target_5XX_Count","HTTP_Fixed_Response_Count","HTTP_Redirect_Count","HTTP_Redirect_Url_Limit_Exceeded_Count","HealthyHostCount","HealthyStateDNS","HealthyStateRouting","IPv6ProcessedBytes","IPv6RequestCount","LambdaInternalError","LambdaTargetProcessedBytes","LambdaUserError","MitigatedHostCount","NewConnectionCount","NonStickyRequestCount","ProcessedBytes","RejectedConnectionCount","RequestCount","RequestCountPerTarget","RuleEvaluations","TargetConnectionErrorCount","TargetResponseTime","TargetTLSNegotiationErrorCount","UnHealthyHostCount","UnhealthyRoutingRequestCount","UnhealthyStateDNS","UnhealthyStateRouting"]).describe("#### The metric to monitor on the Load Balancer.\n\n---\n\nThe threshold will be compared against the calculated value of `statistic(METRIC)`, where:\n- `statistic` is the function applied to the metric values collected during the evaluation period (default: `avg`).\n- `METRIC` is the chosen metric.\n\n**Available Metrics:**\n\n- `ActiveConnectionCount`: The total number of concurrent TCP connections active from clients to the load balancer and from the load balancer to targets.\n- `AnomalousHostCount`: The number of hosts detected with anomalies.\n- `ClientTLSNegotiationErrorCount`: The number of TLS connections initiated by the client that did not establish a session with the load balancer due to a TLS error.\n- `ConsumedLCUs`: The number of load balancer capacity units (LCU) used by your load balancer.\n- `DesyncMitigationMode_NonCompliant_Request_Count`: The number of requests that do not comply with RFC 7230.\n- `DroppedInvalidHeaderRequestCount`: The number of requests where the load balancer removed HTTP headers with invalid fields before routing the request.\n- `MitigatedHostCount`: The number of targets under mitigation.\n- `ForwardedInvalidHeaderRequestCount`: The number of requests routed by the load balancer that had HTTP headers with invalid fields.\n- `GrpcRequestCount`: The number of gRPC requests processed over IPv4 and IPv6.\n- `HTTP_Fixed_Response_Count`: The number of successful fixed-response actions.\n- `HTTP_Redirect_Count`: The number of successful redirect actions.\n- `HTTP_Redirect_Url_Limit_Exceeded_Count`: The number of redirect actions that failed because the URL in the response location header exceeded 8K.\n- `HTTPCode_ELB_3XX_Count`: The number of HTTP 3XX redirection codes originating from the load balancer.\n- `HTTPCode_ELB_4XX_Count`: The number of HTTP 4XX client error codes originating from the load balancer.\n- `HTTPCode_ELB_5XX_Count`: The number of HTTP 5XX server error codes originating from the load balancer.\n- `HTTPCode_ELB_500_Count`: The number of HTTP 500 error codes originating from the load balancer.\n- `HTTPCode_ELB_502_Count`: The number of HTTP 502 error codes originating from the load balancer.\n- `HTTPCode_ELB_503_Count`: The number of HTTP 503 error codes originating from the load balancer.\n- `HTTPCode_ELB_504_Count`: The number of HTTP 504 error codes originating from the load balancer.\n- `IPv6ProcessedBytes`: The total number of bytes processed by the load balancer over IPv6.\n- `IPv6RequestCount`: The number of IPv6 requests received by the load balancer.\n- `NewConnectionCount`: The total number of new TCP connections established from clients to the load balancer and from the load balancer to targets.\n- `NonStickyRequestCount`: The number of requests where the load balancer chose a new target because it could not use an existing sticky session.\n- `ProcessedBytes`: The total number of bytes processed by the load balancer over IPv4 and IPv6.\n- `RejectedConnectionCount`: The number of connections rejected because the load balancer reached its maximum number of connections.\n- `RequestCount`: The number of requests processed over IPv4 and IPv6.\n- `RuleEvaluations`: The number of rules processed by the load balancer, averaged over an hour.\n- `HealthyHostCount`: The number of targets that are considered healthy.\n- `HTTPCode_Target_2XX_Count`: The number of HTTP 2XX response codes generated by the targets.\n- `HTTPCode_Target_3XX_Count`: The number of HTTP 3XX response codes generated by the targets.\n- `HTTPCode_Target_4XX_Count`: The number of HTTP 4XX response codes generated by the targets.\n- `HTTPCode_Target_5XX_Count`: The number of HTTP 5XX response codes generated by the targets.\n- `RequestCountPerTarget`: The average number of requests per target in a target group.\n- `TargetConnectionErrorCount`: The number of connections that were not successfully established between the load balancer and a target.\n- `TargetResponseTime`: The time elapsed (in seconds) from when a request leaves the load balancer until the target starts sending response headers.\n- `TargetTLSNegotiationErrorCount`: The number of TLS connections initiated by the load balancer that did not establish a session with the target.\n- `UnHealthyHostCount`: The number of targets that are considered unhealthy.\n- `HealthyStateDNS`: The number of zones that meet the DNS healthy state requirements.\n- `HealthyStateRouting`: The number of zones that meet the routing healthy state requirements.\n- `UnhealthyRoutingRequestCount`: The number of requests routed using the routing failover action (fail open).\n- `UnhealthyStateDNS`: The number of zones that do not meet the DNS healthy state requirements.\n- `UnhealthyStateRouting`: The number of zones that do not meet the routing healthy state requirements.\n- `LambdaInternalError`: The number of requests to a Lambda function that failed due to an issue internal to the load balancer or AWS Lambda.\n- `LambdaTargetProcessedBytes`: The total number of bytes processed by the load balancer for requests to and responses from a Lambda function.\n- `LambdaUserError`: The number of requests to a Lambda function that failed due to an issue with the Lambda function itself.\n- `ELBAuthError`: The number of user authentications that could not be completed due to an internal error.\n- `ELBAuthFailure`: The number of user authentications that could not be completed because the IdP denied access.\n- `ELBAuthLatency`: The time elapsed (in milliseconds) to query the IdP for the ID token and user info.\n- `ELBAuthRefreshTokenSuccess`: The number of times the load balancer successfully refreshed user claims using a refresh token.\n- `ELBAuthSuccess`: The number of successful authentication actions.\n- `ELBAuthUserClaimsSizeExceeded`: The number of times a configured IdP returned user claims that exceeded 11K bytes in size.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n  publicLb:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Too many 5xx errors returned by the load balancer\n          trigger:\n            type: application-load-balancer-custom\n            properties:\n              metric: HTTPCode_ELB_5XX_Count\n              threshold: 50\n              statistic: sum\n              comparisonOperator: GreaterThanThreshold\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, ApplicationLoadBalancer, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  const publicLb = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Too many 5xx errors returned by the load balancer',\n        trigger: {\n          type: 'application-load-balancer-custom',\n          properties: {\n            metric: 'HTTPCode_ELB_5XX_Count',\n            threshold: 50,\n            statistic: 'sum',\n            comparisonOperator: 'GreaterThanThreshold'\n          }\n        },\n        evaluation: { period: 60, evaluationPeriods: 5, breachedPeriods: 3 },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { webApi, publicLb } };\n});\n```"),
                  "threshold": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The threshold that triggers the alarm.\n\n---\n\nThe threshold is compared against the calculated value of `statistic(METRIC)`, where:\n- `statistic` is the function applied to the metric values collected during the evaluation period (default: `avg`).\n- `METRIC` is the chosen metric.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n  publicLb:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Target response time is too high\n          trigger:\n            type: application-load-balancer-custom\n            properties:\n              metric: TargetResponseTime\n              threshold: 2\n              statistic: p95\n              comparisonOperator: GreaterThanThreshold\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, ApplicationLoadBalancer, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  const publicLb = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Target response time is too high',\n        trigger: {\n          type: 'application-load-balancer-custom',\n          properties: {\n            metric: 'TargetResponseTime',\n            threshold: 2,\n            statistic: 'p95',\n            comparisonOperator: 'GreaterThanThreshold'\n          }\n        },\n        evaluation: { period: 60, evaluationPeriods: 5, breachedPeriods: 3 },\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@example.com',\n              recipient: 'oncall@example.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { webApi, publicLb } };\n});\n```"),
                  "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("application-load-balancer-error-rate"),
                "properties": z.object({
                  "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when 4xx/5xx error rate exceeds this percentage.\n\n---\n\nExample: `5` fires the alarm if more than 5% of requests return errors.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  loadBalancer:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Load balancer error rate too high\n          trigger:\n            type: application-load-balancer-error-rate\n            properties:\n              thresholdPercent: 5\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const loadBalancer = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Load balancer error rate too high',\n        trigger: {\n          type: 'application-load-balancer-error-rate',\n          properties: {\n            thresholdPercent: 5\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { loadBalancer } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("application-load-balancer-unhealthy-targets"),
                "properties": z.object({
                  "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when the percentage of unhealthy targets exceeds this value.\n\n---\n\nIf the load balancer has multiple target groups, the alarm fires if *any* group breaches the threshold.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  loadBalancer:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Too many unhealthy targets behind the load balancer\n          trigger:\n            type: application-load-balancer-unhealthy-targets\n            properties:\n              thresholdPercent: 50\n  apiService:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: api\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-org/api:latest\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: loadBalancer\n                priority: 1\n                containerPort: 3000\n                paths:\n                  - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const loadBalancer = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Too many unhealthy targets behind the load balancer',\n        trigger: {\n          type: 'application-load-balancer-unhealthy-targets',\n          properties: {\n            thresholdPercent: 50\n          }\n        }\n      }\n    ]\n  });\n\n  const apiService = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'api',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/api:latest' } },\n        events: [\n          {\n            type: 'application-load-balancer',\n            properties: {\n              loadBalancerName: 'loadBalancer',\n              priority: 1,\n              containerPort: 3000,\n              paths: ['*']\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { loadBalancer, apiService } };\n});\n```"),
                  "onlyIncludeTargets": z.array(z.string()).optional().describe("#### Only monitor health of these target container services. If omitted, monitors all targets.\n\n---\n\nOnly services actually targeted by the load balancer can be listed.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  loadBalancer:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Critical service has unhealthy targets\n          trigger:\n            type: application-load-balancer-unhealthy-targets\n            properties:\n              thresholdPercent: 25\n              onlyIncludeTargets:\n                - apiService\n  apiService:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: api\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-org/api:latest\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: loadBalancer\n                priority: 1\n                containerPort: 3000\n                paths:\n                  - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const loadBalancer = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Critical service has unhealthy targets',\n        trigger: {\n          type: 'application-load-balancer-unhealthy-targets',\n          properties: {\n            thresholdPercent: 25,\n            onlyIncludeTargets: ['apiService']\n          }\n        }\n      }\n    ]\n  });\n\n  const apiService = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'api',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/api:latest' } },\n        events: [\n          {\n            type: 'application-load-balancer',\n            properties: {\n              loadBalancerName: 'loadBalancer',\n              priority: 1,\n              containerPort: 3000,\n              paths: ['*']\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { loadBalancer, apiService } };\n});\n```").optional(),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```") }).strict()
              }).strict()
            ]),
            "evaluation": z.object({
              "period": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Duration of one evaluation period in seconds. Must be a multiple of 60.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker running too long\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          evaluation:\n            period: 300\n            evaluationPeriods: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker running too long',\n        trigger: {\n          type: 'lambda-duration',\n          properties: { thresholdMilliseconds: 5000 }\n        },\n        evaluation: {\n          period: 300,\n          evaluationPeriods: 2\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default(60),
              "evaluationPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many recent periods to evaluate. Prevents alarms from firing on short spikes.\n\n---\n\nExample: set to `5` with `breachedPeriods: 3` — the alarm fires only if the threshold is breached\nin at least 3 of the last 5 periods.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/payments.ts\n      alarms:\n        - description: Payments error rate elevated\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/payments.ts' }),\n    alarms: [\n      {\n        description: 'Payments error rate elevated',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 1 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { paymentsFn } };\n});\n```").default(1),
              "breachedPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many periods (within `evaluationPeriods`) must breach the threshold to fire the alarm.\n\n---\n\nMust be ≤ `evaluationPeriods`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/checkout.ts\n      alarms:\n        - description: Checkout errors persisting across multiple periods\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 2\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/checkout.ts' }),\n    alarms: [\n      {\n        description: 'Checkout errors persisting across multiple periods',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { checkoutFn } };\n});\n```").default(1) }).strict()
            .optional().describe("#### How long and how often to evaluate the metric before triggering.\n\n---\n\nControls the evaluation window (period), how many periods to look at, and how many must breach\nthe threshold to fire the alarm. Useful for filtering out short spikes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/api.ts\n      memory: 512\n      timeout: 10\n      alarms:\n        - description: API error rate too high\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    alarms: [\n      {\n        description: 'API error rate too high',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFunction } };\n});\n```").optional(),
            "notificationTargets": z.array(z.union([z.object({
                "type": z.literal("slack"),
                "properties": z.object({
                  "conversationId": z.string().describe("#### The Slack channel or DM ID to send notifications to.\n\n---\n\nTo find the ID: open the channel, click its name, and look at the bottom of the **About** tab.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```"),
                  "accessToken": z.string().describe("#### Bot User OAuth Token for your Slack app. Store as `$Secret()` for security.\n\n---\n\nCreate a Slack app, add the `chat:write` scope, install it to your workspace, then copy the Bot User OAuth Token.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("ms-teams"),
                "properties": z.object({
                  "webhookUrl": z.string().describe("#### Incoming Webhook URL for the MS Teams channel. Store as `$Secret()` for security.\n\n---\n\nCreate an Incoming Webhook connector in your Teams channel settings to get this URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 1024\n      timeout: 15\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          description: API function is running slow\n          notificationTargets:\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('ms-teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/api.ts' }\n    },\n    memory: 1024,\n    timeout: 15,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 5000 } },\n        description: 'API function is running slow',\n        notificationTargets: [\n          {\n            type: 'ms-teams',\n            properties: {\n              webhookUrl: $Secret('ms-teams-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("email"),
                "properties": z.object({
                  "sender": z.string().describe("#### The email address of the sender.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```"),
                  "recipient": z.string().describe("#### The email address of the recipient.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("discord"),
                "properties": z.object({
                  "webhookUrl": z.string().describe("#### Discord Webhook URL for the channel. Store as `$Secret()` for security.\n\n---\n\nCreate a webhook in your Discord channel settings (Edit Channel → Integrations → Webhooks).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest.ts\n      memory: 256\n      timeout: 60\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 30000\n          description: Ingest job is taking too long\n          notificationTargets:\n            - type: discord\n              properties:\n                webhookUrl: $Secret('discord-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest.ts' }\n    },\n    memory: 256,\n    timeout: 60,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 30000 } },\n        description: 'Ingest job is taking too long',\n        notificationTargets: [\n          {\n            type: 'discord',\n            properties: {\n              webhookUrl: $Secret('discord-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { ingestFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("webhook"),
                "properties": z.object({
                  "url": z.string().describe("#### The URL to send webhook POST requests to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```"),
                  "secret": z.string().optional().describe("#### Optional signing secret for HMAC-SHA256 payload verification.\n\n---\n\nIf provided, each request includes an `X-Stacktape-Signature` header.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional(),
                  "headers": z.record(z.string(), z.any()).optional().describe("#### Optional custom headers to include in each request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n                headers:\n                  X-Environment: production\n                  Authorization: $Secret('webhook-auth-header')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret'),\n              headers: {\n                'X-Environment': 'production',\n                Authorization: $Secret('webhook-auth-header')\n              }\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional() }).strict()
                .optional().describe("").optional() }).strict()
            ])).optional().describe("#### Where to send notifications when the alarm fires — Slack, MS Teams, or email.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API returning too many errors\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 2\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C12345678\n                accessToken: $Secret('slack-bot-token')\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API returning too many errors',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: { conversationId: 'C12345678', accessToken: $Secret('slack-bot-token') }\n          },\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          },\n          {\n            type: 'ms-teams',\n            properties: { webhookUrl: $Secret('teams-webhook-url') }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
            "includeInHistory": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether alarm state changes should appear in monitoring history.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Jobs queue backing up\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          includeInHistory: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Jobs queue backing up',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        includeInHistory: false\n      }\n    ]\n  });\n\n  return { resources: { jobsQueue } };\n});\n```").default(true),
            "description": z.string().optional().describe("#### Custom alarm description used in notification messages and the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 80\n          description: Database CPU sustained above 80% — consider scaling up\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: { thresholdPercent: 80 }\n        },\n        description: 'Database CPU sustained above 80% — consider scaling up'\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").optional() }).strict()
          , z.object({
            "trigger": z.union([z.object({
                "type": z.literal("database-read-latency"),
                "properties": z.object({
                  "thresholdSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when average read I/O latency exceeds this value (seconds).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database read latency too high\n          trigger:\n            type: database-read-latency\n            properties:\n              thresholdSeconds: 0.05\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database read latency too high',\n        trigger: {\n          type: 'database-read-latency',\n          properties: {\n            thresholdSeconds: 0.05\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                  "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("database-write-latency"),
                "properties": z.object({
                  "thresholdSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when average write I/O latency exceeds this value (seconds).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database write latency too high\n          trigger:\n            type: database-write-latency\n            properties:\n              thresholdSeconds: 0.1\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database write latency too high',\n        trigger: {\n          type: 'database-write-latency',\n          properties: {\n            thresholdSeconds: 0.1\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                  "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("database-cpu-utilization"),
                "properties": z.object({
                  "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when CPU utilization exceeds this percentage.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database CPU utilization too high\n          trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 85\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database CPU utilization too high',\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: {\n            thresholdPercent: 85\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                  "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("database-free-storage"),
                "properties": z.object({
                  "thresholdMB": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when free disk space drops below this value (MB).\n\n---\n\nDefault: fires if **minimum** free storage < threshold.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database running low on free storage\n          trigger:\n            type: database-free-storage\n            properties:\n              thresholdMB: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database running low on free storage',\n        trigger: {\n          type: 'database-free-storage',\n          properties: {\n            thresholdMB: 2048\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                  "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("database-free-memory"),
                "properties": z.object({
                  "thresholdMB": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when free memory drops below this value (MB).\n\n---\n\nDefault: fires if **average** free memory < threshold. Customize with `statistic` and `comparisonOperator`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database running low on free memory\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 256\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database running low on free memory',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 256\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                  "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("database-connection-count"),
                "properties": z.object({
                  "thresholdCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when the number of active database connections exceeds this value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Too many active database connections\n          trigger:\n            type: database-connection-count\n            properties:\n              thresholdCount: 180\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Too many active database connections',\n        trigger: {\n          type: 'database-connection-count',\n          properties: {\n            thresholdCount: 180\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                  "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
              }).strict()
            ]),
            "evaluation": z.object({
              "period": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Duration of one evaluation period in seconds. Must be a multiple of 60.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker running too long\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          evaluation:\n            period: 300\n            evaluationPeriods: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker running too long',\n        trigger: {\n          type: 'lambda-duration',\n          properties: { thresholdMilliseconds: 5000 }\n        },\n        evaluation: {\n          period: 300,\n          evaluationPeriods: 2\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default(60),
              "evaluationPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many recent periods to evaluate. Prevents alarms from firing on short spikes.\n\n---\n\nExample: set to `5` with `breachedPeriods: 3` — the alarm fires only if the threshold is breached\nin at least 3 of the last 5 periods.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/payments.ts\n      alarms:\n        - description: Payments error rate elevated\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/payments.ts' }),\n    alarms: [\n      {\n        description: 'Payments error rate elevated',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 1 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { paymentsFn } };\n});\n```").default(1),
              "breachedPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many periods (within `evaluationPeriods`) must breach the threshold to fire the alarm.\n\n---\n\nMust be ≤ `evaluationPeriods`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/checkout.ts\n      alarms:\n        - description: Checkout errors persisting across multiple periods\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 2\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/checkout.ts' }),\n    alarms: [\n      {\n        description: 'Checkout errors persisting across multiple periods',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { checkoutFn } };\n});\n```").default(1) }).strict()
            .optional().describe("#### How long and how often to evaluate the metric before triggering.\n\n---\n\nControls the evaluation window (period), how many periods to look at, and how many must breach\nthe threshold to fire the alarm. Useful for filtering out short spikes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/api.ts\n      memory: 512\n      timeout: 10\n      alarms:\n        - description: API error rate too high\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    alarms: [\n      {\n        description: 'API error rate too high',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFunction } };\n});\n```").optional(),
            "notificationTargets": z.array(z.union([z.object({
                "type": z.literal("slack"),
                "properties": z.object({
                  "conversationId": z.string().describe("#### The Slack channel or DM ID to send notifications to.\n\n---\n\nTo find the ID: open the channel, click its name, and look at the bottom of the **About** tab.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```"),
                  "accessToken": z.string().describe("#### Bot User OAuth Token for your Slack app. Store as `$Secret()` for security.\n\n---\n\nCreate a Slack app, add the `chat:write` scope, install it to your workspace, then copy the Bot User OAuth Token.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("ms-teams"),
                "properties": z.object({
                  "webhookUrl": z.string().describe("#### Incoming Webhook URL for the MS Teams channel. Store as `$Secret()` for security.\n\n---\n\nCreate an Incoming Webhook connector in your Teams channel settings to get this URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 1024\n      timeout: 15\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          description: API function is running slow\n          notificationTargets:\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('ms-teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/api.ts' }\n    },\n    memory: 1024,\n    timeout: 15,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 5000 } },\n        description: 'API function is running slow',\n        notificationTargets: [\n          {\n            type: 'ms-teams',\n            properties: {\n              webhookUrl: $Secret('ms-teams-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("email"),
                "properties": z.object({
                  "sender": z.string().describe("#### The email address of the sender.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```"),
                  "recipient": z.string().describe("#### The email address of the recipient.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("discord"),
                "properties": z.object({
                  "webhookUrl": z.string().describe("#### Discord Webhook URL for the channel. Store as `$Secret()` for security.\n\n---\n\nCreate a webhook in your Discord channel settings (Edit Channel → Integrations → Webhooks).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest.ts\n      memory: 256\n      timeout: 60\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 30000\n          description: Ingest job is taking too long\n          notificationTargets:\n            - type: discord\n              properties:\n                webhookUrl: $Secret('discord-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest.ts' }\n    },\n    memory: 256,\n    timeout: 60,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 30000 } },\n        description: 'Ingest job is taking too long',\n        notificationTargets: [\n          {\n            type: 'discord',\n            properties: {\n              webhookUrl: $Secret('discord-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { ingestFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("webhook"),
                "properties": z.object({
                  "url": z.string().describe("#### The URL to send webhook POST requests to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```"),
                  "secret": z.string().optional().describe("#### Optional signing secret for HMAC-SHA256 payload verification.\n\n---\n\nIf provided, each request includes an `X-Stacktape-Signature` header.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional(),
                  "headers": z.record(z.string(), z.any()).optional().describe("#### Optional custom headers to include in each request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n                headers:\n                  X-Environment: production\n                  Authorization: $Secret('webhook-auth-header')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret'),\n              headers: {\n                'X-Environment': 'production',\n                Authorization: $Secret('webhook-auth-header')\n              }\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional() }).strict()
                .optional().describe("").optional() }).strict()
            ])).optional().describe("#### Where to send notifications when the alarm fires — Slack, MS Teams, or email.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API returning too many errors\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 2\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C12345678\n                accessToken: $Secret('slack-bot-token')\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API returning too many errors',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: { conversationId: 'C12345678', accessToken: $Secret('slack-bot-token') }\n          },\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          },\n          {\n            type: 'ms-teams',\n            properties: { webhookUrl: $Secret('teams-webhook-url') }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
            "includeInHistory": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether alarm state changes should appear in monitoring history.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Jobs queue backing up\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          includeInHistory: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Jobs queue backing up',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        includeInHistory: false\n      }\n    ]\n  });\n\n  return { resources: { jobsQueue } };\n});\n```").default(true),
            "description": z.string().optional().describe("#### Custom alarm description used in notification messages and the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 80\n          description: Database CPU sustained above 80% — consider scaling up\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: { thresholdPercent: 80 }\n        },\n        description: 'Database CPU sustained above 80% — consider scaling up'\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").optional() }).strict()
        ])).optional().describe("#### Alarms for this Convex deployment (backend container, ALB, database). Merged with global\nalarms from the Stacktape Console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 85\n        - trigger:\n            type: application-load-balancer-unhealthy-targets\n            properties:\n              thresholdPercent: 50\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: {\n            thresholdPercent: 85\n          }\n        }\n      },\n      {\n        trigger: {\n          type: 'application-load-balancer-unhealthy-targets',\n          properties: {\n            thresholdPercent: 50\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { backend } };\n});\n```").optional(),
        "disabledGlobalAlarms": z.array(z.string()).optional().describe("#### Global alarm names to exclude from this deployment.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      disabledGlobalAlarms:\n        - high-db-connections\n        - alb-5xx-rate\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    disabledGlobalAlarms: ['high-db-connections', 'alb-5xx-rate']\n  });\n  return { resources: { backend } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Self-hosted Convex backend on AWS — reactive database, functions, file storage, and dashboard.\n\n---\n\nProvisions everything needed to run [Convex](https://www.convex.dev) self-hosted on your own AWS account:\na Fargate-based backend container, a PostgreSQL database, five S3 buckets (modules, files, search,\nexports, snapshot imports), an ALB for HTTPS + WebSocket traffic, and an optional admin dashboard.\n\nOn every `stacktape deploy`, after the infrastructure is healthy, Stacktape runs `npx convex deploy`\nfrom the parent project directory to push the latest function code to the freshly-deployed backend, with\nthe admin key injected automatically.\n\n---\n\n##### Single-instance constraint\n\nThe open-source convex-backend distribution is **single-process**: it cannot be horizontally scaled.\nRunning two backends against the same Postgres would corrupt MVCC transaction validation and break\nreactive query invalidation. Stacktape therefore enforces one active backend task.\n\nScale **vertically** by bumping `backend.resources.cpu`/`memory` (or by switching to `instanceTypes`\nwith EC2). A single 4 vCPU / 8 GB backend comfortably handles thousands of concurrent reactive\nsubscribers per Convex's own self-hosted guidance.\n\n---\n\n##### Cost\n\nThe smallest viable configuration (single-AZ `db.t4g.micro` Postgres, 0.5 vCPU / 1 GB Fargate\nbackend, 0.25 vCPU / 512 MB dashboard, ALB, S3) lands around **$45–65/month idle**. Production\nconfigurations with larger backends scale up from there.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backend:\n    type: convex\n    properties:\n      appDirectory: ./convex\n      backend:\n        resources:\n          cpu: 1\n          memory: 2048\n      dashboard:\n        enabled: true\n      database:\n        engine:\n          type: postgres\n          properties:\n            version: '16.6'\n            primaryInstance:\n              instanceSize: db.t4g.small\n      customDomains:\n        cloud:\n          domainName: api.myapp.com\n        site:\n          domainName: webhooks.myapp.com\n        dashboard:\n          domainName: convex-admin.myapp.com\n      deletionProtection: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Convex, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backend = new Convex({\n    appDirectory: './convex',\n    backend: {\n      resources: {\n        cpu: 1,\n        memory: 2048\n      }\n    },\n    dashboard: {\n      enabled: true\n    },\n    database: {\n      engine: {\n        type: 'postgres',\n        properties: {\n          version: '16.6',\n          primaryInstance: {\n            instanceSize: 'db.t4g.small'\n          }\n        }\n      }\n    },\n    customDomains: {\n      cloud: { domainName: 'api.myapp.com' },\n      site: { domainName: 'webhooks.myapp.com' },\n      dashboard: { domainName: 'convex-admin.myapp.com' }\n    },\n    deletionProtection: true\n  });\n  return { resources: { backend } };\n});\n```"), z.object({
      "type": z.literal("custom-resource-definition"),
      "properties": z.object({
        "packaging": z.union([z.object({
            "type": z.literal("stacktape-lambda-buildpack"),
            "properties": z.object({
              "handlerFunction": z.string().optional().describe("#### The name of the handler function to be executed when the Lambda is invoked.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         handlerFunction: handler\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       handlerFunction: 'handler'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
              "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "languageSpecificConfig": z.union([z.object({
                  "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                  "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                  "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                , z.object({
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                  "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                  "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                , z.object({
                  "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                , z.record(z.string(), z.any()), z.object({
                  "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                , z.object({
                  "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                , z.object({
                  "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                  "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
              ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### A zero-config buildpack that packages your code for AWS Lambda.\n\n---\n\nThe `stacktape-lambda-buildpack` automatically bundles your code and dependencies into an optimized Lambda deployment package.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, Go, Ruby, PHP, and .NET.\n\nFor JS/TS, your code is bundled into a single file. Source maps are automatically generated.\nPackages are cached based on a checksum, so unchanged code is not re-packaged."), z.object({
            "type": z.literal("custom-artifact"),
            "properties": z.object({
              "packagePath": z.string().describe("#### The path to a pre-built deployment package.\n\n---\n\nIf the path points to a directory or a non-zip file, Stacktape will automatically zip it for you.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: custom-artifact\n       properties:\n         packagePath: ./dist/lambda.zip\n         handler: index.js:handler\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'custom-artifact',\n     properties: {\n       packagePath: './dist/lambda.zip',\n       handler: 'index.js:handler'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```"),
              "handler": z.string().optional().describe("#### The handler function to be executed when the Lambda is invoked.\n\n---\n\nThe syntax is `{{filepath}}:{{functionName}}`.\n\nExample: `my-lambda/index.js:default`\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: custom-artifact\n       properties:\n         packagePath: ./dist/lambda.zip\n         handler: my-lambda/index.js:default\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'custom-artifact',\n     properties: {\n       packagePath: './dist/lambda.zip',\n       handler: 'my-lambda/index.js:default'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Uses a pre-built artifact for Lambda deployment.\n\n---\n\nWith `custom-artifact`, you provide a path to your own pre-built deployment package.\nIf the specified path is a directory or an unzipped file, Stacktape will automatically zip it.\n\nThis is useful when you have custom build processes or need full control over the packaging.")]).describe("#### How the Lambda function code is packaged and deployed.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  stripeWebhookProvisioner:\n    type: custom-resource-definition\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: provisioners/stripe-webhook.ts\n      environment:\n        - name: STRIPE_SECRET_KEY\n          value: $Secret('stripe-key')\n      timeout: 30\n  webhookEndpoint:\n    type: custom-resource-instance\n    properties:\n      definitionName: stripeWebhookProvisioner\n      resourceProperties:\n        url: https://api.example.com/stripe/webhook\n        events:\n          - checkout.session.completed\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { CustomResourceDefinition, CustomResourceInstance, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const stripeWebhookProvisioner = new CustomResourceDefinition({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'provisioners/stripe-webhook.ts' }\n    },\n    environment: [{ name: 'STRIPE_SECRET_KEY', value: $Secret('stripe-key') }],\n    timeout: 30\n  });\n\n  const webhookEndpoint = new CustomResourceInstance({\n    definitionName: 'stripeWebhookProvisioner',\n    resourceProperties: {\n      url: 'https://api.example.com/stripe/webhook',\n      events: ['checkout.session.completed']\n    }\n  });\n\n  return { resources: { stripeWebhookProvisioner, webhookEndpoint } };\n});\n```"),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables injected into the Lambda function. Use `$ResourceParam()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n  cdnProvisioner:\n    type: custom-resource-definition\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: provisioners/fastly-purge.ts\n      environment:\n        - name: FASTLY_API_TOKEN\n          value: $Secret('fastly-token')\n        - name: ORIGIN_BUCKET\n          value: $ResourceParam('assetsBucket', 'name')\n      timeout: 45\n  cdnConfig:\n    type: custom-resource-instance\n    properties:\n      definitionName: cdnProvisioner\n      resourceProperties:\n        serviceId: abc123\n```\n\n**Example (TypeScript):**\n\n```ts\nimport {\n  Bucket,\n  CustomResourceDefinition,\n  CustomResourceInstance,\n  $Secret,\n  $ResourceParam,\n  defineConfig\n} from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({});\n\n  const cdnProvisioner = new CustomResourceDefinition({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'provisioners/fastly-purge.ts' }\n    },\n    environment: [\n      { name: 'FASTLY_API_TOKEN', value: $Secret('fastly-token') },\n      { name: 'ORIGIN_BUCKET', value: $ResourceParam('assetsBucket', 'name') }\n    ],\n    timeout: 45\n  });\n\n  const cdnConfig = new CustomResourceInstance({\n    definitionName: 'cdnProvisioner',\n    resourceProperties: { serviceId: 'abc123' }\n  });\n\n  return { resources: { assetsBucket, cdnProvisioner, cdnConfig } };\n});\n```").optional(),
        "runtime": z.enum(["dotnet6","dotnet7","dotnet8","java11","java17","java8","java8.al2","nodejs18.x","nodejs20.x","nodejs22.x","nodejs24.x","provided.al2","provided.al2023","python3.10","python3.11","python3.12","python3.13","python3.8","python3.9","ruby3.3"]).optional().describe("#### Lambda runtime. Auto-detected from file extension if not specified.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  datadogMonitorProvisioner:\n    type: custom-resource-definition\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: provisioners/datadog_monitor.py\n      runtime: python3.12\n      environment:\n        - name: DD_API_KEY\n          value: $Secret('datadog-api-key')\n        - name: DD_APP_KEY\n          value: $Secret('datadog-app-key')\n      timeout: 60\n  apiLatencyMonitor:\n    type: custom-resource-instance\n    properties:\n      definitionName: datadogMonitorProvisioner\n      resourceProperties:\n        name: API p99 latency\n        query: avg(last_5m):p99:trace.http.request{service:api} > 2\n        thresholdSeconds: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { CustomResourceDefinition, CustomResourceInstance, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const datadogMonitorProvisioner = new CustomResourceDefinition({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'provisioners/datadog_monitor.py' }\n    },\n    runtime: 'python3.12',\n    environment: [\n      { name: 'DD_API_KEY', value: $Secret('datadog-api-key') },\n      { name: 'DD_APP_KEY', value: $Secret('datadog-app-key') }\n    ],\n    timeout: 60\n  });\n\n  const apiLatencyMonitor = new CustomResourceInstance({\n    definitionName: 'datadogMonitorProvisioner',\n    resourceProperties: {\n      name: 'API p99 latency',\n      query: 'avg(last_5m):p99:trace.http.request{service:api} > 2',\n      thresholdSeconds: 2\n    }\n  });\n\n  return { resources: { datadogMonitorProvisioner, apiLatencyMonitor } };\n});\n```").optional(),
        "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Max: 900.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  algoliaIndexProvisioner:\n    type: custom-resource-definition\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: provisioners/algolia-index.ts\n      environment:\n        - name: ALGOLIA_APP_ID\n          value: $Secret('algolia-app-id')\n        - name: ALGOLIA_ADMIN_KEY\n          value: $Secret('algolia-admin-key')\n      timeout: 300\n  productsIndex:\n    type: custom-resource-instance\n    properties:\n      definitionName: algoliaIndexProvisioner\n      resourceProperties:\n        indexName: products\n        searchableAttributes:\n          - title\n          - description\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { CustomResourceDefinition, CustomResourceInstance, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const algoliaIndexProvisioner = new CustomResourceDefinition({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'provisioners/algolia-index.ts' }\n    },\n    environment: [\n      { name: 'ALGOLIA_APP_ID', value: $Secret('algolia-app-id') },\n      { name: 'ALGOLIA_ADMIN_KEY', value: $Secret('algolia-admin-key') }\n    ],\n    timeout: 300\n  });\n\n  const productsIndex = new CustomResourceInstance({\n    definitionName: 'algoliaIndexProvisioner',\n    resourceProperties: {\n      indexName: 'products',\n      searchableAttributes: ['title', 'description']\n    }\n  });\n\n  return { resources: { algoliaIndexProvisioner, productsIndex } };\n});\n```").default(10),
        "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128–10,240). CPU scales proportionally — 1,769 MB = 1 vCPU.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  imageOptimizerProvisioner:\n    type: custom-resource-definition\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: provisioners/optimize-images.ts\n      timeout: 600\n      memory: 2048\n  bannerOptimization:\n    type: custom-resource-instance\n    properties:\n      definitionName: imageOptimizerProvisioner\n      resourceProperties:\n        sourcePrefix: raw/banners/\n        targetPrefix: optimized/banners/\n        quality: 80\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { CustomResourceDefinition, CustomResourceInstance, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const imageOptimizerProvisioner = new CustomResourceDefinition({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'provisioners/optimize-images.ts' }\n    },\n    timeout: 600,\n    memory: 2048\n  });\n\n  const bannerOptimization = new CustomResourceInstance({\n    definitionName: 'imageOptimizerProvisioner',\n    resourceProperties: {\n      sourcePrefix: 'raw/banners/',\n      targetPrefix: 'optimized/banners/',\n      quality: 80\n    }\n  });\n\n  return { resources: { imageOptimizerProvisioner, bannerOptimization } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Lambda-backed provisioning logic for resources not natively supported by Stacktape/CloudFormation.\n\n---\n\nYour Lambda function runs on stack create, update, and delete events to manage external resources\n(third-party APIs, SaaS services, custom infrastructure). Pair with `custom-resource-instance` to use."), z.object({
      "type": z.literal("custom-resource-instance"),
      "properties": z.object({
        "definitionName": z.string().describe("#### Name of the `custom-resource-definition` in your config that provides the backing Lambda.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  slackProvisioner:\n    type: custom-resource-definition\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: provisioners/slack-channel.ts\n      runtime: nodejs22.x\n      timeout: 60\n  releaseChannel:\n    type: custom-resource-instance\n    properties:\n      definitionName: slackProvisioner\n      resourceProperties:\n        channelName: product-releases\n        isPrivate: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { CustomResourceDefinition, CustomResourceInstance, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const slackProvisioner = new CustomResourceDefinition({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'provisioners/slack-channel.ts' }\n    },\n    runtime: 'nodejs22.x',\n    timeout: 60\n  });\n\n  const releaseChannel = new CustomResourceInstance({\n    definitionName: 'slackProvisioner',\n    resourceProperties: { channelName: 'product-releases', isPrivate: false }\n  });\n\n  return { resources: { slackProvisioner, releaseChannel } };\n});\n```"),
        "resourceProperties": z.record(z.string(), z.any()).describe("#### Key-value pairs passed to the Lambda function during create/update/delete events.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  dnsProvisioner:\n    type: custom-resource-definition\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: provisioners/cloudflare-dns.ts\n      environment:\n        - name: CLOUDFLARE_API_TOKEN\n          value: $Secret('cloudflare-token')\n      timeout: 120\n  appDnsRecord:\n    type: custom-resource-instance\n    properties:\n      definitionName: dnsProvisioner\n      resourceProperties:\n        recordType: CNAME\n        name: app.example.com\n        target: my-load-balancer.eu-west-1.elb.amazonaws.com\n        ttl: 300\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { CustomResourceDefinition, CustomResourceInstance, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const dnsProvisioner = new CustomResourceDefinition({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'provisioners/cloudflare-dns.ts' }\n    },\n    environment: [{ name: 'CLOUDFLARE_API_TOKEN', value: $Secret('cloudflare-token') }],\n    timeout: 120\n  });\n\n  const appDnsRecord = new CustomResourceInstance({\n    definitionName: 'dnsProvisioner',\n    resourceProperties: {\n      recordType: 'CNAME',\n      name: 'app.example.com',\n      target: 'my-load-balancer.eu-west-1.elb.amazonaws.com',\n      ttl: 300\n    }\n  });\n\n  return { resources: { dnsProvisioner, appDnsRecord } };\n});\n```") }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### An instance of a `custom-resource-definition`. Pass properties to the backing Lambda function."), z.object({
      "type": z.literal("deployment-script"),
      "properties": z.object({
        "trigger": z.enum(["after:deploy","before:delete"]).describe("#### When to run: `after:deploy` (fails → rollback) or `before:delete` (fails → deletion continues).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  cleanup:\n    type: deployment-script\n    properties:\n      trigger: before:delete\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/cleanup.ts\n      timeout: 300\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const cleanup = new DeploymentScript({\n    trigger: 'before:delete',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/cleanup.ts' }),\n    timeout: 300\n  });\n\n  return { resources: { cleanup } };\n});\n```"),
        "packaging": z.union([z.object({
            "type": z.literal("stacktape-lambda-buildpack"),
            "properties": z.object({
              "handlerFunction": z.string().optional().describe("#### The name of the handler function to be executed when the Lambda is invoked.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         handlerFunction: handler\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       handlerFunction: 'handler'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
              "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "languageSpecificConfig": z.union([z.object({
                  "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                  "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                  "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                , z.object({
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                  "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                  "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                , z.object({
                  "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                , z.record(z.string(), z.any()), z.object({
                  "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                , z.object({
                  "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                , z.object({
                  "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                  "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
              ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### A zero-config buildpack that packages your code for AWS Lambda.\n\n---\n\nThe `stacktape-lambda-buildpack` automatically bundles your code and dependencies into an optimized Lambda deployment package.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, Go, Ruby, PHP, and .NET.\n\nFor JS/TS, your code is bundled into a single file. Source maps are automatically generated.\nPackages are cached based on a checksum, so unchanged code is not re-packaged."), z.object({
            "type": z.literal("custom-artifact"),
            "properties": z.object({
              "packagePath": z.string().describe("#### The path to a pre-built deployment package.\n\n---\n\nIf the path points to a directory or a non-zip file, Stacktape will automatically zip it for you.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: custom-artifact\n       properties:\n         packagePath: ./dist/lambda.zip\n         handler: index.js:handler\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'custom-artifact',\n     properties: {\n       packagePath: './dist/lambda.zip',\n       handler: 'index.js:handler'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```"),
              "handler": z.string().optional().describe("#### The handler function to be executed when the Lambda is invoked.\n\n---\n\nThe syntax is `{{filepath}}:{{functionName}}`.\n\nExample: `my-lambda/index.js:default`\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: custom-artifact\n       properties:\n         packagePath: ./dist/lambda.zip\n         handler: my-lambda/index.js:default\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'custom-artifact',\n     properties: {\n       packagePath: './dist/lambda.zip',\n       handler: 'my-lambda/index.js:default'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Uses a pre-built artifact for Lambda deployment.\n\n---\n\nWith `custom-artifact`, you provide a path to your own pre-built deployment package.\nIf the specified path is a directory or an unzipped file, Stacktape will automatically zip it.\n\nThis is useful when you have custom build processes or need full control over the packaging.")]).describe("#### How the script code is packaged. Use `stacktape-lambda-buildpack` for auto-bundling.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  seedData:\n    type: deployment-script\n    properties:\n      trigger: after:deploy\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/seed.ts\n      timeout: 120\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const seedData = new DeploymentScript({\n    trigger: 'after:deploy',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/seed.ts' }),\n    timeout: 120\n  });\n\n  return { resources: { seedData } };\n});\n```"),
        "runtime": z.enum(["dotnet6","dotnet7","dotnet8","java11","java17","java8","java8.al2","nodejs18.x","nodejs20.x","nodejs22.x","nodejs24.x","provided.al2","provided.al2023","python3.10","python3.11","python3.12","python3.13","python3.8","python3.9","ruby3.3"]).optional().describe("#### Lambda runtime. Auto-detected from file extension if not specified.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  runMigrations:\n    type: deployment-script\n    properties:\n      trigger: after:deploy\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/migrate.ts\n      runtime: nodejs22.x\n      timeout: 120\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const runMigrations = new DeploymentScript({\n    trigger: 'after:deploy',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/migrate.ts' }),\n    runtime: 'nodejs22.x',\n    timeout: 120\n  });\n\n  return { resources: { runMigrations } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables injected at runtime. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  seedData:\n    type: deployment-script\n    properties:\n      trigger: after:deploy\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/seed.ts\n      environment:\n        - name: STAGE\n          value: $Stage()\n        - name: ADMIN_API_KEY\n          value: $Secret('admin-api-key')\n      timeout: 120\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging, $Stage, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const seedData = new DeploymentScript({\n    trigger: 'after:deploy',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/seed.ts' }),\n    environment: [\n      { name: 'STAGE', value: $Stage() },\n      { name: 'ADMIN_API_KEY', value: $Secret('admin-api-key') }\n    ],\n    timeout: 120\n  });\n\n  return { resources: { seedData } };\n});\n```").optional(),
        "parameters": z.record(z.string(), z.any()).optional().describe("#### Structured data passed to the handler function as the event payload. Not for secrets — use `environment`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  seedData:\n    type: deployment-script\n    properties:\n      trigger: after:deploy\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/seed.ts\n      parameters:\n        seedCount: 100\n        truncateFirst: true\n        adminEmail: admin@example.com\n      timeout: 120\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const seedData = new DeploymentScript({\n    trigger: 'after:deploy',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/seed.ts' }),\n    parameters: {\n      seedCount: 100,\n      truncateFirst: true,\n      adminEmail: 'admin@example.com'\n    },\n    timeout: 120\n  });\n\n  return { resources: { seedData } };\n});\n```").optional(),
        "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128–10,240). CPU scales proportionally — 1,769 MB = 1 vCPU.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  runMigrations:\n    type: deployment-script\n    properties:\n      trigger: after:deploy\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/migrate.ts\n      memory: 1024\n      timeout: 120\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const runMigrations = new DeploymentScript({\n    trigger: 'after:deploy',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/migrate.ts' }),\n    memory: 1024,\n    timeout: 120\n  });\n\n  return { resources: { runMigrations } };\n});\n```").optional(),
        "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Max: 900 (15 minutes).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  longSeed:\n    type: deployment-script\n    properties:\n      trigger: after:deploy\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/seed.ts\n      timeout: 900\n      memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const longSeed = new DeploymentScript({\n    trigger: 'after:deploy',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/seed.ts' }),\n    timeout: 900,\n    memory: 1024\n  });\n\n  return { resources: { longSeed } };\n});\n```").default(10),
        "joinDefaultVpc": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Connect to VPC resources (databases, Redis). **Warning:** function loses direct internet access.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  runMigrations:\n    type: deployment-script\n    properties:\n      trigger: after:deploy\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/migrate.ts\n      connectTo:\n        - mainDatabase\n      joinDefaultVpc: true\n      timeout: 120\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          primaryInstance:\n            instanceSize: db.t4g.micro\n          version: '16.2'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging, RelationalDatabase, RdsEnginePostgres, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      primaryInstance: { instanceSize: 'db.t4g.micro' },\n      version: '16.2'\n    })\n  });\n\n  const runMigrations = new DeploymentScript({\n    trigger: 'after:deploy',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/migrate.ts' }),\n    connectTo: ['mainDatabase'],\n    joinDefaultVpc: true,\n    timeout: 120\n  });\n\n  return { resources: { mainDatabase, runMigrations } };\n});\n```").optional(),
        "storage": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Ephemeral `/tmp` storage in MB (512–10,240).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  exportData:\n    type: deployment-script\n    properties:\n      trigger: before:delete\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/export.ts\n      storage: 2048\n      timeout: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const exportData = new DeploymentScript({\n    trigger: 'before:delete',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/export.ts' }),\n    storage: 2048,\n    timeout: 600\n  });\n\n  return { resources: { exportData } };\n});\n```").default(512),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Run a script during deploy or delete — database migrations, seed data, cleanup tasks.\n\n---\n\nExecutes as a Lambda function. Use `after:deploy` to run migrations after resources are ready,\nor `before:delete` for cleanup. Can also be triggered manually with `stacktape deployment-script:run`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  runMigrations:\n    type: deployment-script\n    properties:\n      trigger: after:deploy\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./scripts/migrate.ts\n      connectTo:\n        - mainDatabase\n      joinDefaultVpc: true\n      timeout: 120\n      memory: 512\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          primaryInstance:\n            instanceSize: db.t4g.micro\n          version: '16.2'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, DeploymentScript, StacktapeLambdaBuildpackPackaging, RelationalDatabase, RdsEnginePostgres, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      primaryInstance: { instanceSize: 'db.t4g.micro' },\n      version: '16.2'\n    })\n  });\n\n  const runMigrations = new DeploymentScript({\n    trigger: 'after:deploy',\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './scripts/migrate.ts' }),\n    connectTo: ['mainDatabase'],\n    joinDefaultVpc: true,\n    timeout: 120,\n    memory: 512\n  });\n\n  return { resources: { mainDatabase, runMigrations } };\n});\n```"), z.object({
      "type": z.literal("dynamo-db-table"),
      "properties": z.object({
        "primaryKey": z.object({
          "partitionKey": z.object({
            "name": z.string().describe("#### Attribute name (e.g., `userId`, `email`, `createdAt`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  productsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: sku\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const productsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: {\n        name: 'sku',\n        type: 'string'\n      }\n    }\n  });\n\n  return { resources: { productsTable } };\n});\n```"),
            "type": z.enum(["binary","number","string"]).describe("#### Attribute data type: `string`, `number`, or `binary`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  metricsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: metricId\n          type: string\n        sortKey:\n          name: timestamp\n          type: number\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const metricsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'metricId', type: 'string' },\n      sortKey: {\n        name: 'timestamp',\n        type: 'number'\n      }\n    }\n  });\n\n  return { resources: { metricsTable } };\n});\n```") }).strict()
          .describe("#### The main key attribute (e.g., `userId`, `orderId`). Must be unique if no sort key is used.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  devicesTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: deviceId\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const devicesTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'deviceId', type: 'string' }\n    }\n  });\n\n  return { resources: { devicesTable } };\n});\n```"),
          "sortKey": z.object({
            "name": z.string().describe("#### Attribute name (e.g., `userId`, `email`, `createdAt`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  productsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: sku\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const productsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: {\n        name: 'sku',\n        type: 'string'\n      }\n    }\n  });\n\n  return { resources: { productsTable } };\n});\n```"),
            "type": z.enum(["binary","number","string"]).describe("#### Attribute data type: `string`, `number`, or `binary`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  metricsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: metricId\n          type: string\n        sortKey:\n          name: timestamp\n          type: number\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const metricsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'metricId', type: 'string' },\n      sortKey: {\n        name: 'timestamp',\n        type: 'number'\n      }\n    }\n  });\n\n  return { resources: { metricsTable } };\n});\n```") }).strict()
          .optional().describe("#### Optional second key for composite keys. Enables range queries and multiple items per partition key.\n\n---\n\nE.g., partition key `userId` + sort key `createdAt` lets you query all items for a user sorted by date.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  eventsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: userId\n          type: string\n        sortKey:\n          name: createdAt\n          type: number\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const eventsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'userId', type: 'string' },\n      sortKey: { name: 'createdAt', type: 'number' }\n    }\n  });\n\n  return { resources: { eventsTable } };\n});\n```").optional() }).strict()
        .describe("#### The primary key that uniquely identifies each item.\n\n---\n\n- **Simple key**: Just a `partitionKey` (e.g., `userId`).\n- **Composite key**: `partitionKey` + `sortKey` (e.g., `userId` + `createdAt`).\n\n> **Cannot be changed after creation.** Every item must include the primary key attribute(s).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: customerId\n          type: string\n        sortKey:\n          name: orderId\n          type: string\n  reconcileJob:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/reconcile.ts\n      events:\n        - type: schedule\n          properties:\n            scheduleRate: rate(1 hour)\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'customerId', type: 'string' },\n      sortKey: { name: 'orderId', type: 'string' }\n    }\n  });\n\n  const reconcileJob = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/reconcile.ts' }\n    },\n    events: [\n      {\n        type: 'schedule',\n        properties: { scheduleRate: 'rate(1 hour)' }\n      }\n    ]\n  });\n\n  return { resources: { ordersTable, reconcileJob } };\n});\n```"),
        "provisionedThroughput": z.object({
          "readUnits": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Read capacity units per second. 1 unit = one 4 KB strongly consistent read (or two eventually consistent).\n\n---\n\nRequests exceeding this limit get throttled. Use `readScaling` to auto-adjust.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  catalogTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: itemId\n          type: string\n      provisionedThroughput:\n        readUnits: 50\n        writeUnits: 5\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const catalogTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'itemId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 50,\n      writeUnits: 5\n    }\n  });\n\n  return { resources: { catalogTable } };\n});\n```"),
          "writeUnits": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Write capacity units per second. 1 unit = one 1 KB write.\n\n---\n\nRequests exceeding this limit get throttled. Use `writeScaling` to auto-adjust.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: recordId\n          type: string\n      provisionedThroughput:\n        readUnits: 5\n        writeUnits: 40\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'recordId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 5,\n      writeUnits: 40\n    }\n  });\n\n  return { resources: { ingestTable } };\n});\n```"),
          "writeScaling": z.object({
            "minUnits": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Minimum write units. Capacity never scales below this.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: orderId\n          type: string\n      provisionedThroughput:\n        readUnits: 20\n        writeUnits: 10\n        writeScaling:\n          minUnits: 10\n          maxUnits: 100\n          keepUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'orderId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 20,\n      writeUnits: 10,\n      writeScaling: {\n        minUnits: 10,\n        maxUnits: 100,\n        keepUtilizationUnder: 70\n      }\n    }\n  });\n\n  return { resources: { ordersTable } };\n});\n```"),
            "maxUnits": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Maximum write units. Capacity never scales above this.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: orderId\n          type: string\n      provisionedThroughput:\n        readUnits: 20\n        writeUnits: 10\n        writeScaling:\n          minUnits: 10\n          maxUnits: 100\n          keepUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'orderId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 20,\n      writeUnits: 10,\n      writeScaling: {\n        minUnits: 10,\n        maxUnits: 100,\n        keepUtilizationUnder: 70\n      }\n    }\n  });\n\n  return { resources: { ordersTable } };\n});\n```"),
            "keepUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Target utilization percentage (e.g., 70). Scales up when usage exceeds this, down when it drops.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: orderId\n          type: string\n      provisionedThroughput:\n        readUnits: 20\n        writeUnits: 10\n        writeScaling:\n          minUnits: 10\n          maxUnits: 100\n          keepUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'orderId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 20,\n      writeUnits: 10,\n      writeScaling: {\n        minUnits: 10,\n        maxUnits: 100,\n        keepUtilizationUnder: 70\n      }\n    }\n  });\n\n  return { resources: { ordersTable } };\n});\n```") }).strict()
          .optional().describe("#### Auto-scale write capacity based on actual usage. Scales up/down between min and max units.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: orderId\n          type: string\n      provisionedThroughput:\n        readUnits: 20\n        writeUnits: 10\n        writeScaling:\n          minUnits: 10\n          maxUnits: 100\n          keepUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'orderId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 20,\n      writeUnits: 10,\n      writeScaling: {\n        minUnits: 10,\n        maxUnits: 100,\n        keepUtilizationUnder: 70\n      }\n    }\n  });\n\n  return { resources: { ordersTable } };\n});\n```").optional(),
          "readScaling": z.object({
            "minUnits": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Minimum read units. Capacity never scales below this.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: orderId\n          type: string\n      provisionedThroughput:\n        readUnits: 20\n        writeUnits: 10\n        readScaling:\n          minUnits: 20\n          maxUnits: 200\n          keepUtilizationUnder: 65\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'orderId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 20,\n      writeUnits: 10,\n      readScaling: {\n        minUnits: 20,\n        maxUnits: 200,\n        keepUtilizationUnder: 65\n      }\n    }\n  });\n\n  return { resources: { ordersTable } };\n});\n```"),
            "maxUnits": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Maximum read units. Capacity never scales above this.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: orderId\n          type: string\n      provisionedThroughput:\n        readUnits: 20\n        writeUnits: 10\n        readScaling:\n          minUnits: 20\n          maxUnits: 200\n          keepUtilizationUnder: 65\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'orderId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 20,\n      writeUnits: 10,\n      readScaling: {\n        minUnits: 20,\n        maxUnits: 200,\n        keepUtilizationUnder: 65\n      }\n    }\n  });\n\n  return { resources: { ordersTable } };\n});\n```"),
            "keepUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Target utilization percentage (e.g., 70). Scales up when usage exceeds this, down when it drops.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: orderId\n          type: string\n      provisionedThroughput:\n        readUnits: 20\n        writeUnits: 10\n        readScaling:\n          minUnits: 20\n          maxUnits: 200\n          keepUtilizationUnder: 65\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'orderId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 20,\n      writeUnits: 10,\n      readScaling: {\n        minUnits: 20,\n        maxUnits: 200,\n        keepUtilizationUnder: 65\n      }\n    }\n  });\n\n  return { resources: { ordersTable } };\n});\n```") }).strict()
          .optional().describe("#### Auto-scale read capacity based on actual usage. Scales up/down between min and max units.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: orderId\n          type: string\n      provisionedThroughput:\n        readUnits: 20\n        writeUnits: 10\n        readScaling:\n          minUnits: 20\n          maxUnits: 200\n          keepUtilizationUnder: 65\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'orderId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 20,\n      writeUnits: 10,\n      readScaling: {\n        minUnits: 20,\n        maxUnits: 200,\n        keepUtilizationUnder: 65\n      }\n    }\n  });\n\n  return { resources: { ordersTable } };\n});\n```").optional() }).strict()
        .optional().describe("#### Fixed-capacity mode with predictable pricing. Omit for on-demand (pay-per-request) mode.\n\n---\n\n- **On-demand** (default, no config): Pay per read/write. Best for unpredictable or variable traffic.\n- **Provisioned**: Set fixed read/write capacity. Cheaper at steady, predictable load. Can auto-scale.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  sessionsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: sessionId\n          type: string\n      provisionedThroughput:\n        readUnits: 25\n        writeUnits: 10\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sessionsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'sessionId', type: 'string' }\n    },\n    provisionedThroughput: {\n      readUnits: 25,\n      writeUnits: 10\n    }\n  });\n\n  return { resources: { sessionsTable } };\n});\n```").optional(),
        "enablePointInTimeRecovery": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Enable continuous backups with point-in-time recovery (restore to any second in the last 35 days).\n\n---\n\nRestores always create a new table. Adds ~20% to storage cost.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: paymentId\n          type: string\n      enablePointInTimeRecovery: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'paymentId', type: 'string' }\n    },\n    enablePointInTimeRecovery: true\n  });\n\n  return { resources: { paymentsTable } };\n});\n```").optional(),
        "streamType": z.enum(["KEYS_ONLY","NEW_AND_OLD_IMAGES","NEW_IMAGE","OLD_IMAGE"]).optional().describe("#### Stream item changes to trigger functions or batch jobs in real time.\n\n---\n\n- `KEYS_ONLY`: Only key attributes of the changed item.\n- `NEW_IMAGE`: The full item after the change.\n- `OLD_IMAGE`: The full item before the change.\n- `NEW_AND_OLD_IMAGES`: Both before and after — useful for change tracking and auditing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  productsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: productId\n          type: string\n      streamType: NEW_AND_OLD_IMAGES\n  auditLogger:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      events:\n        - type: dynamo-db-stream\n          properties:\n            streamArn: $ResourceParam('productsTable', 'streamArn')\n            batchSize: 25\n            startingPosition: LATEST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, LambdaFunction, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const productsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'productId', type: 'string' }\n    },\n    streamType: 'NEW_AND_OLD_IMAGES'\n  });\n\n  const auditLogger = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/audit.ts' }\n    },\n    events: [\n      {\n        type: 'dynamo-db-stream',\n        properties: {\n          streamArn: $ResourceParam('productsTable', 'streamArn'),\n          batchSize: 25,\n          startingPosition: 'LATEST'\n        }\n      }\n    ]\n  });\n\n  return { resources: { productsTable, auditLogger } };\n});\n```").optional(),
        "secondaryIndexes": z.array(z.object({
          "name": z.string().describe("#### Name of the index (used when querying).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: orderId\n          type: string\n      secondaryIndexes:\n        -\n          name: byCustomer\n          partitionKey:\n            name: customerId\n            type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'orderId', type: 'string' }\n    },\n    secondaryIndexes: [\n      {\n        name: 'byCustomer',\n        partitionKey: { name: 'customerId', type: 'string' }\n      }\n    ]\n  });\n\n  return { resources: { ordersTable } };\n});\n```"),
          "partitionKey": z.object({
            "name": z.string().describe("#### Attribute name (e.g., `userId`, `email`, `createdAt`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  productsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: sku\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const productsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: {\n        name: 'sku',\n        type: 'string'\n      }\n    }\n  });\n\n  return { resources: { productsTable } };\n});\n```"),
            "type": z.enum(["binary","number","string"]).describe("#### Attribute data type: `string`, `number`, or `binary`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  metricsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: metricId\n          type: string\n        sortKey:\n          name: timestamp\n          type: number\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const metricsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'metricId', type: 'string' },\n      sortKey: {\n        name: 'timestamp',\n        type: 'number'\n      }\n    }\n  });\n\n  return { resources: { metricsTable } };\n});\n```") }).strict()
          .describe("#### Partition key for this index — the attribute you'll query by.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ticketsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: ticketId\n          type: string\n      secondaryIndexes:\n        - name: byAssignee\n          partitionKey:\n            name: assigneeId\n            type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ticketsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'ticketId', type: 'string' }\n    },\n    secondaryIndexes: [\n      {\n        name: 'byAssignee',\n        partitionKey: { name: 'assigneeId', type: 'string' }\n      }\n    ]\n  });\n\n  return { resources: { ticketsTable } };\n});\n```"),
          "sortKey": z.object({
            "name": z.string().describe("#### Attribute name (e.g., `userId`, `email`, `createdAt`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  productsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: sku\n          type: string\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const productsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: {\n        name: 'sku',\n        type: 'string'\n      }\n    }\n  });\n\n  return { resources: { productsTable } };\n});\n```"),
            "type": z.enum(["binary","number","string"]).describe("#### Attribute data type: `string`, `number`, or `binary`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  metricsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: metricId\n          type: string\n        sortKey:\n          name: timestamp\n          type: number\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const metricsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'metricId', type: 'string' },\n      sortKey: {\n        name: 'timestamp',\n        type: 'number'\n      }\n    }\n  });\n\n  return { resources: { metricsTable } };\n});\n```") }).strict()
          .optional().describe("#### Optional sort key for range queries within a partition.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ticketsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: ticketId\n          type: string\n      secondaryIndexes:\n        - name: byAssigneePriority\n          partitionKey:\n            name: assigneeId\n            type: string\n          sortKey:\n            name: priority\n            type: number\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ticketsTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'ticketId', type: 'string' }\n    },\n    secondaryIndexes: [\n      {\n        name: 'byAssigneePriority',\n        partitionKey: { name: 'assigneeId', type: 'string' },\n        sortKey: { name: 'priority', type: 'number' }\n      }\n    ]\n  });\n\n  return { resources: { ticketsTable } };\n});\n```").optional(),
          "projections": z.array(z.string()).optional().describe("#### Extra attributes to copy into the index. Only projected attributes are available when querying.\n\n---\n\nThe table's primary key is always projected. List additional attributes you need in query results.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  usersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: userId\n          type: string\n      secondaryIndexes:\n        - name: byEmail\n          partitionKey:\n            name: email\n            type: string\n          projections:\n            - displayName\n            - avatarUrl\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const usersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'userId', type: 'string' }\n    },\n    secondaryIndexes: [\n      {\n        name: 'byEmail',\n        partitionKey: { name: 'email', type: 'string' },\n        projections: ['displayName', 'avatarUrl']\n      }\n    ]\n  });\n\n  return { resources: { usersTable } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Additional indexes for querying by attributes other than the primary key.\n\n---\n\nWithout indexes, you can only query by primary key. Add a secondary index to query by\nany attribute (e.g., query orders by `status` or users by `email`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  usersTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: userId\n          type: string\n      secondaryIndexes:\n        - name: byEmail\n          partitionKey:\n            name: email\n            type: string\n        - name: byStatusCreatedAt\n          partitionKey:\n            name: status\n            type: string\n          sortKey:\n            name: createdAt\n            type: number\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const usersTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'userId', type: 'string' }\n    },\n    secondaryIndexes: [\n      {\n        name: 'byEmail',\n        partitionKey: { name: 'email', type: 'string' }\n      },\n      {\n        name: 'byStatusCreatedAt',\n        partitionKey: { name: 'status', type: 'string' },\n        sortKey: { name: 'createdAt', type: 'number' }\n      }\n    ]\n  });\n\n  return { resources: { usersTable } };\n});\n```").optional(),
        "dev": z.object({
          "remote": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use the deployed AWS resource instead of a local emulation.\n\n---\n\nBy default, databases, Redis, and DynamoDB run locally in Docker during dev mode.\nSet to `true` to connect to the real deployed resource instead (must be deployed first).\n\nUseful when local emulation doesn't match production behavior closely enough,\nor when you need to work with real data.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      connectTo:\n        - sessionsTable\n  sessionsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: id\n          type: string\n      dev:\n        remote: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, DynamoDbTable, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sessionsTable = new DynamoDbTable({\n    primaryKey: { partitionKey: { name: 'id', type: 'string' } },\n    dev: { remote: true }\n  });\n\n  const myApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    connectTo: [sessionsTable]\n  });\n\n  return { resources: { myApi, sessionsTable } };\n});\n```").default(false) }).strict()
        .optional().describe("#### Dev mode: runs locally in Docker by default. Set `remote: true` to use the deployed table.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  cacheTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: cacheKey\n          type: string\n      dev:\n        remote: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { DynamoDbTable, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const cacheTable = new DynamoDbTable({\n    primaryKey: {\n      partitionKey: { name: 'cacheKey', type: 'string' }\n    },\n    dev: {\n      remote: false\n    }\n  });\n\n  return { resources: { cacheTable } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Serverless NoSQL database with single-digit millisecond reads/writes at any scale.\n\n---\n\nNo servers to manage, no capacity planning needed (in on-demand mode). Pay per read/write.\nGreat for user profiles, session data, IoT data, and any key-value or document workload."), z.object({
      "type": z.literal("edge-lambda-function"),
      "properties": z.object({
        "packaging": z.union([z.object({
            "type": z.literal("stacktape-lambda-buildpack"),
            "properties": z.object({
              "handlerFunction": z.string().optional().describe("#### The name of the handler function to be executed when the Lambda is invoked.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         handlerFunction: handler\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       handlerFunction: 'handler'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
              "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "languageSpecificConfig": z.union([z.object({
                  "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                  "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                  "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                , z.object({
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                  "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                  "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                , z.object({
                  "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                , z.record(z.string(), z.any()), z.object({
                  "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                , z.object({
                  "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                , z.object({
                  "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                  "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
              ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### A zero-config buildpack that packages your code for AWS Lambda.\n\n---\n\nThe `stacktape-lambda-buildpack` automatically bundles your code and dependencies into an optimized Lambda deployment package.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, Go, Ruby, PHP, and .NET.\n\nFor JS/TS, your code is bundled into a single file. Source maps are automatically generated.\nPackages are cached based on a checksum, so unchanged code is not re-packaged."), z.object({
            "type": z.literal("custom-artifact"),
            "properties": z.object({
              "packagePath": z.string().describe("#### The path to a pre-built deployment package.\n\n---\n\nIf the path points to a directory or a non-zip file, Stacktape will automatically zip it for you.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: custom-artifact\n       properties:\n         packagePath: ./dist/lambda.zip\n         handler: index.js:handler\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'custom-artifact',\n     properties: {\n       packagePath: './dist/lambda.zip',\n       handler: 'index.js:handler'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```"),
              "handler": z.string().optional().describe("#### The handler function to be executed when the Lambda is invoked.\n\n---\n\nThe syntax is `{{filepath}}:{{functionName}}`.\n\nExample: `my-lambda/index.js:default`\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: custom-artifact\n       properties:\n         packagePath: ./dist/lambda.zip\n         handler: my-lambda/index.js:default\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'custom-artifact',\n     properties: {\n       packagePath: './dist/lambda.zip',\n       handler: 'my-lambda/index.js:default'\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Uses a pre-built artifact for Lambda deployment.\n\n---\n\nWith `custom-artifact`, you provide a path to your own pre-built deployment package.\nIf the specified path is a directory or an unzipped file, Stacktape will automatically zip it.\n\nThis is useful when you have custom build processes or need full control over the packaging.")]).describe("#### How the function code is packaged and deployed.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webEdgeFn:\n    type: edge-lambda-function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/edge/viewer-request.ts\n  website:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: dist\n      hostingContentType: single-page-app\n      edgeFunctions:\n        onRequest: webEdgeFn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webEdgeFn = new EdgeLambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: {\n        entryfilePath: 'src/edge/viewer-request.ts'\n      }\n    }\n  });\n\n  const website = new HostingBucket({\n    uploadDirectoryPath: 'dist',\n    hostingContentType: 'single-page-app',\n    edgeFunctions: {\n      onRequest: 'webEdgeFn'\n    }\n  });\n\n  return { resources: { webEdgeFn, website } };\n});\n```"),
        "runtime": z.enum(["nodejs18.x","nodejs20.x","nodejs22.x","nodejs24.x","python3.10","python3.11","python3.12","python3.13","python3.8","python3.9"]).optional().describe("#### Lambda runtime. Auto-detected from file extension. Edge functions support Node.js and Python only.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webEdgeFn:\n    type: edge-lambda-function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/edge/viewer-request.py\n      runtime: python3.12\n  website:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: dist\n      hostingContentType: single-page-app\n      edgeFunctions:\n        onRequest: webEdgeFn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webEdgeFn = new EdgeLambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: {\n        entryfilePath: 'src/edge/viewer-request.py'\n      }\n    },\n    runtime: 'python3.12'\n  });\n\n  const website = new HostingBucket({\n    uploadDirectoryPath: 'dist',\n    hostingContentType: 'single-page-app',\n    edgeFunctions: {\n      onRequest: 'webEdgeFn'\n    }\n  });\n\n  return { resources: { webEdgeFn, website } };\n});\n```").optional(),
        "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB. Max depends on event type: viewer events = 128 MB, origin events = 10,240 MB.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  originEdgeFn:\n    type: edge-lambda-function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/edge/origin-request.ts\n      memory: 512\n  website:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: dist\n      hostingContentType: static-website\n      edgeFunctions:\n        onOriginResponse: originEdgeFn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const originEdgeFn = new EdgeLambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: {\n        entryfilePath: 'src/edge/origin-request.ts'\n      }\n    },\n    memory: 512\n  });\n\n  const website = new HostingBucket({\n    uploadDirectoryPath: 'dist',\n    hostingContentType: 'static-website',\n    edgeFunctions: {\n      onOriginResponse: 'originEdgeFn'\n    }\n  });\n\n  return { resources: { originEdgeFn, website } };\n});\n```").default(128),
        "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Viewer events: max 5s. Origin events: max 30s.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  originEdgeFn:\n    type: edge-lambda-function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/edge/origin-request.ts\n      memory: 256\n      timeout: 10\n  website:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: dist\n      hostingContentType: static-website\n      edgeFunctions:\n        onOriginResponse: originEdgeFn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const originEdgeFn = new EdgeLambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: {\n        entryfilePath: 'src/edge/origin-request.ts'\n      }\n    },\n    memory: 256,\n    timeout: 10\n  });\n\n  const website = new HostingBucket({\n    uploadDirectoryPath: 'dist',\n    hostingContentType: 'static-website',\n    edgeFunctions: {\n      onOriginResponse: 'originEdgeFn'\n    }\n  });\n\n  return { resources: { originEdgeFn, website } };\n});\n```").default(3),
        "connectTo": z.array(z.string()).optional().describe("#### Grant access to other resources in your stack (IAM permissions only — no env vars or VPC access).\n\n---\n\nEdge Lambda functions **cannot** use environment variables or connect to VPC resources.\n`connectTo` only sets up IAM permissions (e.g., S3 bucket access, DynamoDB, SES).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  configBucket:\n    type: bucket\n    properties: {}\n  authEdgeFn:\n    type: edge-lambda-function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/edge/auth-check.ts\n      connectTo:\n        - configBucket\n  website:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: dist\n      hostingContentType: single-page-app\n      edgeFunctions:\n        onRequest: authEdgeFn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, EdgeLambdaFunction, HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const configBucket = new Bucket({});\n\n  const authEdgeFn = new EdgeLambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: {\n        entryfilePath: 'src/edge/auth-check.ts'\n      }\n    },\n    connectTo: ['configBucket']\n  });\n\n  const website = new HostingBucket({\n    uploadDirectoryPath: 'dist',\n    hostingContentType: 'single-page-app',\n    edgeFunctions: {\n      onRequest: 'authEdgeFn'\n    }\n  });\n\n  return { resources: { configBucket, authEdgeFn, website } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Custom IAM policy statements for fine-grained AWS permissions beyond what `connectTo` provides.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authEdgeFn:\n    type: edge-lambda-function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/edge/auth-check.ts\n      iamRoleStatements:\n        - Sid: ReadEdgeSecrets\n          Effect: Allow\n          Action:\n            - secretsmanager:GetSecretValue\n          Resource:\n            - arn:aws:secretsmanager:us-east-1:123456789012:secret:edge-jwt-key-*\n  website:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: dist\n      hostingContentType: single-page-app\n      edgeFunctions:\n        onRequest: authEdgeFn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authEdgeFn = new EdgeLambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: {\n        entryfilePath: 'src/edge/auth-check.ts'\n      }\n    },\n    iamRoleStatements: [\n      {\n        Sid: 'ReadEdgeSecrets',\n        Effect: 'Allow',\n        Action: ['secretsmanager:GetSecretValue'],\n        Resource: ['arn:aws:secretsmanager:us-east-1:123456789012:secret:edge-jwt-key-*']\n      }\n    ]\n  });\n\n  const website = new HostingBucket({\n    uploadDirectoryPath: 'dist',\n    hostingContentType: 'single-page-app',\n    edgeFunctions: {\n      onRequest: 'authEdgeFn'\n    }\n  });\n\n  return { resources: { authEdgeFn, website } };\n});\n```").optional(),
        "logging": z.object({
          "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highVolumeFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highVolumeFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { highVolumeFunction } };\n});\n```").default(false),
          "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs. Longer retention = higher storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/audit.ts' } },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditFunction } };\n});\n```").default(90),
          "logForwarding": z.union([z.object({
              "type": z.literal("http-endpoint"),
              "properties": z.object({
                "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("highlight"),
              "properties": z.object({
                "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("datadog"),
              "properties": z.object({
                "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
            }).strict()
          ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Logging config. Logs are sent to CloudWatch **in the region where the function executed** (not your stack region).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webEdgeFn:\n    type: edge-lambda-function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/edge/viewer-request.ts\n      logging:\n        retentionDays: 30\n  website:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: dist\n      hostingContentType: single-page-app\n      edgeFunctions:\n        onRequest: webEdgeFn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webEdgeFn = new EdgeLambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: {\n        entryfilePath: 'src/edge/viewer-request.ts'\n      }\n    },\n    logging: {\n      retentionDays: 30\n    }\n  });\n\n  const website = new HostingBucket({\n    uploadDirectoryPath: 'dist',\n    hostingContentType: 'single-page-app',\n    edgeFunctions: {\n      onRequest: 'webEdgeFn'\n    }\n  });\n\n  return { resources: { webEdgeFn, website } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Lambda function that runs at CDN edge locations for request/response manipulation.\n\n---\n\nRuns on CloudFront events (viewer request, origin request, etc.) to modify headers, rewrite URLs,\nimplement A/B testing, or add auth checks at the edge. Referenced from CDN `edgeFunctions` config."), z.object({
      "type": z.literal("efs-filesystem"),
      "properties": z.object({
        "backupEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Enable daily automatic backups with 35-day retention. Incremental (only changes are copied).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mediaStorage:\n    type: efs-filesystem\n    properties:\n      backupEnabled: true\n      throughputMode: elastic\n  cmsWorkload:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: cms\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-repo/cms\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: mediaStorage\n                mountPath: /var/www/uploads\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EfsFilesystem, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mediaStorage = new EfsFilesystem({\n    backupEnabled: true,\n    throughputMode: 'elastic'\n  });\n\n  const cmsWorkload = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'cms',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-repo/cms' } },\n        volumeMounts: [\n          { type: 'efs', properties: { efsFilesystemName: 'mediaStorage', mountPath: '/var/www/uploads' } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n\n  return { resources: { mediaStorage, cmsWorkload } };\n});\n```").optional(),
        "throughputMode": z.enum(["bursting","elastic","provisioned"]).optional().describe("#### How throughput scales with your workload.\n\n---\n\n- **`elastic`** (recommended): Auto-scales throughput. Best for spiky workloads (web apps, CI/CD).\n- **`provisioned`**: Fixed throughput you set via `provisionedThroughputInMibps`. Best for steady high-throughput workloads.\n- **`bursting`**: Throughput scales with storage size (50 KiB/s per GiB). Can run out of burst credits.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  modelStorage:\n    type: efs-filesystem\n    properties:\n      backupEnabled: false\n      throughputMode: provisioned\n      provisionedThroughputInMibps: 100\n  inferenceWorkload:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: inference\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-repo/ml-inference\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: modelStorage\n                mountPath: /models\n      resources:\n        cpu: 2\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EfsFilesystem, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const modelStorage = new EfsFilesystem({\n    backupEnabled: false,\n    throughputMode: 'provisioned',\n    provisionedThroughputInMibps: 100\n  });\n\n  const inferenceWorkload = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'inference',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-repo/ml-inference' } },\n        volumeMounts: [\n          { type: 'efs', properties: { efsFilesystemName: 'modelStorage', mountPath: '/models' } }\n        ]\n      }\n    ],\n    resources: { cpu: 2, memory: 4096 }\n  });\n\n  return { resources: { modelStorage, inferenceWorkload } };\n});\n```").default("elastic"),
        "provisionedThroughputInMibps": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Guaranteed throughput in MiB/s. Required when `throughputMode` is `provisioned`.\n\n---\n\nE.g., `100` = 100 MiB/s. Additional fees apply based on the provisioned amount. Can be changed anytime.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highThroughputStorage:\n    type: efs-filesystem\n    properties:\n      throughputMode: provisioned\n      provisionedThroughputInMibps: 256\n  dataWorkload:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: processor\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-repo/data-processor\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: highThroughputStorage\n                mountPath: /shared\n      resources:\n        cpu: 4\n        memory: 8192\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EfsFilesystem, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highThroughputStorage = new EfsFilesystem({\n    throughputMode: 'provisioned',\n    provisionedThroughputInMibps: 256\n  });\n\n  const dataWorkload = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'processor',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-repo/data-processor' } },\n        volumeMounts: [\n          { type: 'efs', properties: { efsFilesystemName: 'highThroughputStorage', mountPath: '/shared' } }\n        ]\n      }\n    ],\n    resources: { cpu: 4, memory: 8192 }\n  });\n\n  return { resources: { highThroughputStorage, dataWorkload } };\n});\n```").optional() }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Shared file storage that multiple containers can read/write simultaneously.\n\n---\n\nPersistent, elastic (grows/shrinks automatically), and accessible from any container in your stack\nvia `volumeMounts`. Use for shared uploads, CMS media, ML model files, or anything that needs to\nsurvive container restarts. Pay only for storage used (~$0.30/GB/month for standard access).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  sharedStorage:\n    type: efs-filesystem\n    properties:\n      backupEnabled: true\n      throughputMode: elastic\n  apiWorkload:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api-container\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-repo/my-api\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 1\n        memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EfsFilesystem, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({\n    backupEnabled: true,\n    throughputMode: 'elastic'\n  });\n\n  const apiWorkload = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api-container',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-repo/my-api' } },\n        volumeMounts: [\n          { type: 'efs', properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' } }\n        ]\n      }\n    ],\n    resources: { cpu: 1, memory: 2048 }\n  });\n\n  return { resources: { sharedStorage, apiWorkload } };\n});\n```"), z.object({
      "type": z.literal("event-bus"),
      "properties": z.object({
        "eventSourceName": z.string().optional().describe("#### Partner event source name. Only needed for receiving events from third-party SaaS integrations.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  partnerEvents:\n    type: event-bus\n    properties:\n      eventSourceName: aws.partner/example.com/12345/my-integration\n  partnerHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handle-partner-event.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: partnerEvents\n            eventPattern:\n              source:\n                - aws.partner/example.com/12345/my-integration\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EventBus, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const partnerEvents = new EventBus({\n    eventSourceName: 'aws.partner/example.com/12345/my-integration'\n  });\n\n  const partnerHandler = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/handle-partner-event.ts' }\n    },\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'partnerEvents',\n          eventPattern: { source: ['aws.partner/example.com/12345/my-integration'] }\n        }\n      }\n    ]\n  });\n\n  return { resources: { partnerEvents, partnerHandler } };\n});\n```").optional(),
        "archivation": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable event archiving. Disabling deletes the archive.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  domainEvents:\n    type: event-bus\n    properties:\n      archivation:\n        enabled: true\n        retentionDays: 30\n  eventConsumer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/consumer.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: domainEvents\n            eventPattern:\n              source:\n                - my.domain\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EventBus, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const domainEvents = new EventBus({\n    archivation: {\n      enabled: true,\n      retentionDays: 30\n    }\n  });\n\n  const eventConsumer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/consumer.ts' }\n    },\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'domainEvents',\n          eventPattern: { source: ['my.domain'] }\n        }\n      }\n    ]\n  });\n\n  return { resources: { domainEvents, eventConsumer } };\n});\n```").default(false),
          "retentionDays": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Days to keep archived events. Omit to keep indefinitely.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  replayableEvents:\n    type: event-bus\n    properties:\n      archivation:\n        enabled: true\n        retentionDays: 365\n  replayHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/replay-handler.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: replayableEvents\n            eventPattern:\n              source:\n                - my.events\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EventBus, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const replayableEvents = new EventBus({\n    archivation: {\n      enabled: true,\n      retentionDays: 365\n    }\n  });\n\n  const replayHandler = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/replay-handler.ts' }\n    },\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'replayableEvents',\n          eventPattern: { source: ['my.events'] }\n        }\n      }\n    ]\n  });\n\n  return { resources: { replayableEvents, replayHandler } };\n});\n```").optional() }).strict()
        .optional().describe("#### Archive events to store and replay them later. Useful for debugging, testing, or error recovery.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditEvents:\n    type: event-bus\n    properties:\n      archivation:\n        enabled: true\n        retentionDays: 90\n  auditLogger:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit-logger.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: auditEvents\n            eventPattern:\n              source:\n                - my.audit\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EventBus, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditEvents = new EventBus({\n    archivation: {\n      enabled: true,\n      retentionDays: 90\n    }\n  });\n\n  const auditLogger = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/audit-logger.ts' }\n    },\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'auditEvents',\n          eventPattern: { source: ['my.audit'] }\n        }\n      }\n    ]\n  });\n\n  return { resources: { auditEvents, auditLogger } };\n});\n```").optional() }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Central event bus for decoupling services. Publish events and trigger functions, queues, or batch jobs.\n\n---\n\nUse to build event-driven architectures where producers and consumers are independent.\nFunctions, batch jobs, and other resources can subscribe to specific event patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EventBus, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n\n  const orderProcessor = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/process-order.ts' }\n    },\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] }\n        }\n      }\n    ]\n  });\n\n  return { resources: { orderEvents, orderProcessor } };\n});\n```"), z.object({
      "type": z.literal("hosting-bucket"),
      "properties": z.object({
        "uploadDirectoryPath": z.string().describe("#### Path to the build output directory (e.g., `dist`, `build`, `out`).\n\n---\n\nThis folder's contents are uploaded to the bucket on every deploy.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' }\n  });\n  return { resources: { frontend } };\n});\n```"),
        "build": z.object({
          "command": z.string().describe("#### Command to run (e.g., `npm run build`, `vite build`, `npm run dev`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n        workingDirectory: .\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: {\n      command: 'npm run build',\n      workingDirectory: '.'\n    }\n  });\n  return { resources: { frontend } };\n});\n```"),
          "workingDirectory": z.string().optional().describe("#### Working directory for the command (relative to project root).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./apps/web/dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n        workingDirectory: ./apps/web\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './apps/web/dist',\n    hostingContentType: 'single-page-app',\n    build: {\n      command: 'npm run build',\n      workingDirectory: './apps/web'\n    }\n  });\n  return { resources: { frontend } };\n});\n```").default(".") }).strict()
        .optional().describe("#### Build command that produces the files to upload (e.g., `npm run build`).\n\n---\n\nRuns during the packaging phase, in parallel with other resources. Bundle size is shown in deploy logs.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n        workingDirectory: .\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: {\n      command: 'npm run build',\n      workingDirectory: '.'\n    }\n  });\n  return { resources: { frontend } };\n});\n```").optional(),
        "dev": z.object({
          "command": z.string().describe("#### Command to run (e.g., `npm run build`, `vite build`, `npm run dev`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n        workingDirectory: .\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: {\n      command: 'npm run build',\n      workingDirectory: '.'\n    }\n  });\n  return { resources: { frontend } };\n});\n```"),
          "workingDirectory": z.string().optional().describe("#### Working directory for the command (relative to project root).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./apps/web/dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n        workingDirectory: ./apps/web\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './apps/web/dist',\n    hostingContentType: 'single-page-app',\n    build: {\n      command: 'npm run build',\n      workingDirectory: './apps/web'\n    }\n  });\n  return { resources: { frontend } };\n});\n```").default(".") }).strict()
        .optional().describe("#### Dev server command for local development (e.g., `npm run dev`, `vite`).\n\n---\n\nUsed by `stacktape dev`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n      dev:\n        command: npm run dev\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' },\n    dev: { command: 'npm run dev' }\n  });\n  return { resources: { frontend } };\n});\n```").optional(),
        "excludeFilesPatterns": z.array(z.string()).optional().describe("#### Glob patterns for files to skip during upload (relative to `uploadDirectoryPath`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: static-website\n      build:\n        command: npm run build\n      excludeFilesPatterns:\n        - '**/*.map'\n        - 'stats.html'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'static-website',\n    build: { command: 'npm run build' },\n    excludeFilesPatterns: ['**/*.map', 'stats.html']\n  });\n  return { resources: { frontend } };\n});\n```").optional(),
        "hostingContentType": z.enum(["astro-static-website","gatsby-static-website","nuxt-static-website","single-page-app","static-website","sveltekit-static-website"]).optional().describe("#### Optimizes caching and routing for your type of frontend app.\n\n---\n\n- **`single-page-app`**: For React, Vue, Angular, or any SPA built with Vite/Webpack.\n  Enables client-side routing (e.g., `/about` serves `index.html`). HTML is never browser-cached;\n  hashed assets (`.js`, `.css`) are cached forever.\n\n- **`static-website`** (default): For multi-page static sites. All files are CDN-cached\n  but never browser-cached, so users always see the latest content after a deploy.\n\n- **`astro-static-website`** / **`sveltekit-static-website`** / **`nuxt-static-website`**:\n  Framework-specific presets that cache hashed build assets (`_astro/`, `_app/`, `_nuxt/`)\n  indefinitely while keeping HTML fresh.\n\n- **`gatsby-static-website`**: Gatsby-specific caching following their recommendations.\n\nYou can override any preset's behavior using `fileOptions`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' }\n  });\n  return { resources: { frontend } };\n});\n```"),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Custom domains (e.g., `www.example.com`). Stacktape auto-creates DNS records and TLS certificates.\n\n---\n\nYour domain must be added as a Route53 hosted zone in your AWS account first.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n      customDomains:\n        - domainName: www.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' },\n    customDomains: [{ domainName: 'www.example.com' }]\n  });\n  return { resources: { frontend } };\n});\n```").optional(),
        "disableUrlNormalization": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable clean URL normalization (e.g., `/about` → `/about.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./public\n      hostingContentType: static-website\n      build:\n        command: npm run build\n      disableUrlNormalization: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './public',\n    hostingContentType: 'static-website',\n    build: { command: 'npm run build' },\n    disableUrlNormalization: true\n  });\n  return { resources: { frontend } };\n});\n```").default(false),
        "edgeFunctions": z.object({
          "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
          "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
          "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
          "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
        .optional().describe("#### Run edge functions on CDN requests/responses (URL rewrites, auth, A/B testing).\n\n---\n\n- `onRequest`: Before cache lookup and before forwarding to the bucket.\n- `onResponse`: Before returning the response to the client.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  authChecker:\n    type: edge-lambda-function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./edge/auth.ts\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n      edgeFunctions:\n        onRequest: authChecker\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, EdgeLambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const authChecker = new EdgeLambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: './edge/auth.ts' }\n    }\n  });\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' },\n    edgeFunctions: { onRequest: 'authChecker' }\n  });\n  return { resources: { authChecker, frontend } };\n});\n```").optional(),
        "errorDocument": z.string().optional().describe("#### Page to show for 404 errors (e.g., `/error.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: static-website\n      build:\n        command: npm run build\n      errorDocument: /error.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'static-website',\n    build: { command: 'npm run build' },\n    errorDocument: '/error.html'\n  });\n  return { resources: { frontend } };\n});\n```").optional(),
        "indexDocument": z.string().optional().describe("#### Page served for requests to `/`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: static-website\n      build:\n        command: npm run build\n      indexDocument: /index.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'static-website',\n    build: { command: 'npm run build' },\n    indexDocument: '/index.html'\n  });\n  return { resources: { frontend } };\n});\n```").default("/index.html"),
        "injectEnvironment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Inject deploy-time values into HTML files as `window.STP_INJECTED_ENV.VARIABLE_NAME`.\n\n---\n\nUseful for making API URLs, User Pool IDs, and other dynamic values\navailable to your frontend JavaScript without rebuilding.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n      injectEnvironment:\n        - name: API_URL\n          value: $ResourceParam('apiGateway', 'url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, HttpApiGateway, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({});\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' },\n    injectEnvironment: { API_URL: $ResourceParam('apiGateway', 'url') }\n  });\n  return { resources: { apiGateway, frontend } };\n});\n```").optional(),
        "writeDotenvFilesTo": z.string().optional().describe("#### Write deploy-time values to a `.env` file in the specified directory.\n\n---\n\nMerges with existing `.env` content if the file already exists.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n      injectEnvironment:\n        - name: API_URL\n          value: $ResourceParam('apiGateway', 'url')\n      writeDotenvFilesTo: ./\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, HttpApiGateway, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({});\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' },\n    injectEnvironment: { API_URL: $ResourceParam('apiGateway', 'url') },\n    writeDotenvFilesTo: './'\n  });\n  return { resources: { apiGateway, frontend } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this site. Must have `scope: cdn`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  siteFirewall:\n    type: web-app-firewall\n    properties:\n      scope: cdn\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n      useFirewall: siteFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const siteFirewall = new WebAppFirewall({ scope: 'cdn' });\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' },\n    useFirewall: 'siteFirewall'\n  });\n  return { resources: { siteFirewall, frontend } };\n});\n```").optional(),
        "fileOptions": z.array(z.object({
          "includePattern": z.string().describe("#### Glob pattern for files this rule applies to (e.g., `*.html`, `assets/**`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
          "excludePattern": z.string().optional().describe("#### Glob pattern for files to exclude even if they match `includePattern`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            excludePattern: 'assets/uncompressed/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          excludePattern: 'assets/uncompressed/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "headers": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### HTTP headers (e.g., `Cache-Control`) for matching files. Forwarded through CDN to the browser.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "tags": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### Tags for matching files. Can be used to target files with `lifecycleRules`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'reports/**'\n            tags:\n              - key: category\n                value: report\n      lifecycleRules:\n        - type: expiration\n          properties:\n            tags:\n              - key: category\n                value: report\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'reports/**',\n          tags: [{ key: 'category', value: 'report' }]\n        }\n      ]\n    },\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          tags: [{ key: 'category', value: 'report' }],\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Set HTTP headers (e.g., `Cache-Control`) for files matching specific patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n      fileOptions:\n        - includePattern: 'assets/**'\n          headers:\n            - key: Cache-Control\n              value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' },\n    fileOptions: [\n      {\n        includePattern: 'assets/**',\n        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n      }\n    ]\n  });\n  return { resources: { frontend } };\n});\n```").optional(),
        "routeRewrites": z.array(z.object({
          "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `*.jpg`, `/docs/v2/*`). Wildcards supported.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
          "routePrefix": z.string().optional().describe("#### Prepend a path prefix to requests before forwarding to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routePrefix: /v2\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routePrefix: '/v2',\n        routeTo: { type: 'http-api-gateway', properties: { httpApiGatewayName: 'backendApi' } }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, api } };\n});\n```").optional(),
          "routeTo": z.union([z.object({
              "type": z.literal("application-load-balancer"),
              "properties": z.object({
                "loadBalancerName": z.string().describe("#### Name of the `application-load-balancer` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```"),
                "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Listener port on the load balancer. Only needed if using custom listeners.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              listenerPort: 8080\nappLb:\n  type: application-load-balancer\n  properties:\n    listeners:\n      - port: 8080\n        protocol: HTTP\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({\n  listeners: [{ port: 8080, protocol: 'HTTP' }]\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            listenerPort: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional(),
                "originDomainName": z.string().optional().describe("#### Explicit origin domain. Only needed if the ALB has no `customDomains` and uses `customCertificateArns`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              originDomainName: app.internal.example.com\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            originDomainName: 'app.internal.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("http-api-gateway"),
              "properties": z.object({
                "httpApiGatewayName": z.string().describe("#### Name of the `http-api-gateway` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnApi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst cdnApi = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routeTo: {\n          type: 'http-api-gateway',\n          properties: {\n            httpApiGatewayName: 'backendApi'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, cdnApi } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("function"),
              "properties": z.object({
                "functionName": z.string().describe("#### Name of the `function` resource to route to. The function must have `url.enabled: true`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /render/*\n          routeTo:\n            type: function\n            properties:\n              functionName: renderer\nrenderer:\n  type: function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/render.ts\n    url:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst renderer = new LambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/render.ts' }),\n  url: { enabled: true }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/render/*',\n        routeTo: {\n          type: 'function',\n          properties: {\n            functionName: 'renderer'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { renderer, api } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("custom-origin"),
              "properties": z.object({
                "domainName": z.string().describe("#### Domain name of the external origin (e.g., `api.example.com`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                "protocol": z.enum(["HTTP","HTTPS"]).optional().describe("#### Protocol for connecting to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTPS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTPS'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```").default("HTTPS"),
                "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port on the origin. Defaults to 443 for HTTPS, 80 for HTTP.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTP\n              port: 8080\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTP',\n            port: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("bucket"),
              "properties": z.object({
                "bucketName": z.string().describe("#### Name of the `bucket` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
                "disableUrlNormalization": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable clean URL normalization (e.g., `/about` → `/about.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n              disableUrlNormalization: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets',\n            disableUrlNormalization: true\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").default(false) }).strict()
            }).strict()
          ]).optional().describe("#### Where to send matching requests. Can be a bucket, API Gateway, load balancer, Lambda, or external domain.\n\n---\n\nIf not set, requests go to the default origin (the resource this CDN is attached to).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /static/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/static/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
          "cachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Override caching behavior for requests matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          cachingOptions:\n            defaultTTL: 604800\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        cachingOptions: { defaultTTL: 604800 }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
          "forwardingOptions": z.object({
            "customRequestHeaders": z.array(z.object({
              "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
              "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
            ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cookies": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "headers": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "queryString": z.object({
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Override which headers, cookies, and query params are forwarded for this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          forwardingOptions:\n            cookies:\n              none: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        forwardingOptions: { cookies: { none: true } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
          "edgeFunctions": z.object({
            "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
            "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
            "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
            "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Run edge functions on requests/responses matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /private/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          edgeFunctions:\n            onRequest: authAtEdge\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/private/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        edgeFunctions: { onRequest: 'authAtEdge' }\n      }\n    ]\n  }\n});\nreturn { resources: { authAtEdge, assets, api } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Route specific URL patterns to different origins (e.g., `/api/*` → a Lambda function).\n\n---\n\nEvaluated in order; first match wins. Unmatched requests go to the bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n  frontend:\n    type: hosting-bucket\n    properties:\n      uploadDirectoryPath: ./dist\n      hostingContentType: single-page-app\n      build:\n        command: npm run build\n      routeRewrites:\n        - path: /api/*\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: apiGateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HostingBucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({});\n  const frontend = new HostingBucket({\n    uploadDirectoryPath: './dist',\n    hostingContentType: 'single-page-app',\n    build: { command: 'npm run build' },\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routeTo: {\n          type: 'http-api-gateway',\n          properties: { httpApiGatewayName: 'apiGateway' }\n        }\n      }\n    ]\n  });\n  return { resources: { apiGateway, frontend } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Host a static website (React, Vue, Astro, etc.) on S3 + CloudFront CDN.\n\n---\n\nCombines S3 storage with a global CDN for fast, cheap, and scalable static site hosting.\nIncludes build step, custom domains, caching presets, and environment injection."), z.object({
      "type": z.literal("kinesis-stream"),
      "properties": z.object({
        "capacityMode": z.enum(["ON_DEMAND","PROVISIONED"]).optional().describe("#### How the stream scales.\n\n---\n\n- **`ON_DEMAND`**: Auto-scales, pay per GB. Recommended for most use cases.\n- **`PROVISIONED`**: You choose a fixed number of shards (1 MB/s write, 2 MB/s read each). More predictable costs.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\n# capacityMode\nresources:\n  eventStream:\n    type: kinesis-stream\n    properties:\n      capacityMode: ON_DEMAND\n      retentionPeriodHours: 48\n  streamProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-records.ts\n      events:\n        - type: kinesis-stream\n          properties:\n            kinesisStreamName: eventStream\n            batchSize: 100\n            startingPosition: LATEST\n```\n\n**Example (TypeScript):**\n\n```ts\n// capacityMode\nimport { KinesisStream, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const eventStream = new KinesisStream({\n    capacityMode: 'ON_DEMAND',\n    retentionPeriodHours: 48\n  });\n\n  const streamProcessor = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/process-records.ts' }\n    },\n    events: [\n      {\n        type: 'kinesis-stream',\n        properties: {\n          kinesisStreamName: 'eventStream',\n          batchSize: 100,\n          startingPosition: 'LATEST'\n        }\n      }\n    ]\n  });\n\n  return { resources: { eventStream, streamProcessor } };\n});\n```"),
        "shardCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Number of shards. Only used when `capacityMode` is `PROVISIONED`.\n\n---\n\nEach shard: 1 MB/s write (1,000 records/s), 2 MB/s read (shared across consumers).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\n# shardCount\nresources:\n  clickstream:\n    type: kinesis-stream\n    properties:\n      capacityMode: PROVISIONED\n      shardCount: 4\n      retentionPeriodHours: 72\n  analyticsConsumer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/analytics.ts\n      events:\n        - type: kinesis-stream\n          properties:\n            kinesisStreamName: clickstream\n            batchSize: 500\n```\n\n**Example (TypeScript):**\n\n```ts\n// shardCount\nimport { KinesisStream, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const clickstream = new KinesisStream({\n    capacityMode: 'PROVISIONED',\n    shardCount: 4,\n    retentionPeriodHours: 72\n  });\n\n  const analyticsConsumer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/analytics.ts' }\n    },\n    events: [\n      {\n        type: 'kinesis-stream',\n        properties: {\n          kinesisStreamName: 'clickstream',\n          batchSize: 500\n        }\n      }\n    ]\n  });\n\n  return { resources: { clickstream, analyticsConsumer } };\n});\n```").default(1),
        "retentionPeriodHours": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How long records stay in the stream (hours). Range: 24–8,760 (365 days). Beyond 24h costs extra.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\n# retentionPeriodHours\nresources:\n  auditTrail:\n    type: kinesis-stream\n    properties:\n      capacityMode: ON_DEMAND\n      retentionPeriodHours: 168\n  auditConsumer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      events:\n        - type: kinesis-stream\n          properties:\n            kinesisStreamName: auditTrail\n            startingPosition: TRIM_HORIZON\n```\n\n**Example (TypeScript):**\n\n```ts\n// retentionPeriodHours\nimport { KinesisStream, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditTrail = new KinesisStream({\n    capacityMode: 'ON_DEMAND',\n    retentionPeriodHours: 168\n  });\n\n  const auditConsumer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/audit.ts' }\n    },\n    events: [\n      {\n        type: 'kinesis-stream',\n        properties: {\n          kinesisStreamName: 'auditTrail',\n          startingPosition: 'TRIM_HORIZON'\n        }\n      }\n    ]\n  });\n\n  return { resources: { auditTrail, auditConsumer } };\n});\n```").default(24),
        "encryption": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable server-side encryption.\n\n---\n\n**Example (YAML):**\n\n```yaml\n# encryption.enabled\nresources:\n  encryptedStream:\n    type: kinesis-stream\n    properties:\n      capacityMode: ON_DEMAND\n      retentionPeriodHours: 24\n      encryption:\n        enabled: true\n  encryptedConsumer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/consume.ts\n      events:\n        - type: kinesis-stream\n          properties:\n            kinesisStreamName: encryptedStream\n```\n\n**Example (TypeScript):**\n\n```ts\n// encryption.enabled\nimport { KinesisStream, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const encryptedStream = new KinesisStream({\n    capacityMode: 'ON_DEMAND',\n    retentionPeriodHours: 24,\n    encryption: {\n      enabled: true\n    }\n  });\n\n  const encryptedConsumer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/consume.ts' }\n    },\n    events: [\n      {\n        type: 'kinesis-stream',\n        properties: {\n          kinesisStreamName: 'encryptedStream'\n        }\n      }\n    ]\n  });\n\n  return { resources: { encryptedStream, encryptedConsumer } };\n});\n```").default(false),
          "kmsKeyArn": z.string().optional().describe("#### ARN of your own KMS key. If omitted, uses the AWS-managed `alias/aws/kinesis` key (no extra cost).\n\n---\n\n**Example (YAML):**\n\n```yaml\n# encryption.kmsKeyArn\nresources:\n  cmkStream:\n    type: kinesis-stream\n    properties:\n      capacityMode: ON_DEMAND\n      encryption:\n        enabled: true\n        kmsKeyArn: arn:aws:kms:eu-west-1:123456789012:key/1234abcd-12ab-34cd-56ef-1234567890ab\n  cmkConsumer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/consume.ts\n      events:\n        - type: kinesis-stream\n          properties:\n            kinesisStreamName: cmkStream\n```\n\n**Example (TypeScript):**\n\n```ts\n// encryption.kmsKeyArn\nimport { KinesisStream, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const cmkStream = new KinesisStream({\n    capacityMode: 'ON_DEMAND',\n    encryption: {\n      enabled: true,\n      kmsKeyArn: 'arn:aws:kms:eu-west-1:123456789012:key/1234abcd-12ab-34cd-56ef-1234567890ab'\n    }\n  });\n\n  const cmkConsumer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/consume.ts' }\n    },\n    events: [\n      {\n        type: 'kinesis-stream',\n        properties: {\n          kinesisStreamName: 'cmkStream'\n        }\n      }\n    ]\n  });\n\n  return { resources: { cmkStream, cmkConsumer } };\n});\n```").optional() }).strict()
        .optional().describe("#### Encrypt data at rest using a KMS key.\n\n---\n\n**Example (YAML):**\n\n```yaml\n# encryption\nresources:\n  secureStream:\n    type: kinesis-stream\n    properties:\n      capacityMode: ON_DEMAND\n      encryption:\n        enabled: true\n  secureConsumer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/consume.ts\n      events:\n        - type: kinesis-stream\n          properties:\n            kinesisStreamName: secureStream\n```\n\n**Example (TypeScript):**\n\n```ts\n// encryption\nimport { KinesisStream, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const secureStream = new KinesisStream({\n    capacityMode: 'ON_DEMAND',\n    encryption: {\n      enabled: true\n    }\n  });\n\n  const secureConsumer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/consume.ts' }\n    },\n    events: [\n      {\n        type: 'kinesis-stream',\n        properties: {\n          kinesisStreamName: 'secureStream'\n        }\n      }\n    ]\n  });\n\n  return { resources: { secureStream, secureConsumer } };\n});\n```").optional(),
        "enableEnhancedFanOut": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Give each consumer its own dedicated 2 MB/s read throughput (instead of sharing).\n\n---\n\nUse when you have multiple consumers reading from the same stream and need low latency.\nEnhanced fan-out consumers are auto-created when a Lambda uses `autoCreateConsumer: true`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\n# enableEnhancedFanOut\nresources:\n  fanoutStream:\n    type: kinesis-stream\n    properties:\n      capacityMode: ON_DEMAND\n      enableEnhancedFanOut: true\n  realtimeConsumer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/realtime.ts\n      events:\n        - type: kinesis-stream\n          properties:\n            kinesisStreamName: fanoutStream\n            autoCreateConsumer: true\n            startingPosition: LATEST\n```\n\n**Example (TypeScript):**\n\n```ts\n// enableEnhancedFanOut\nimport { KinesisStream, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const fanoutStream = new KinesisStream({\n    capacityMode: 'ON_DEMAND',\n    enableEnhancedFanOut: true\n  });\n\n  const realtimeConsumer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/realtime.ts' }\n    },\n    events: [\n      {\n        type: 'kinesis-stream',\n        properties: {\n          kinesisStreamName: 'fanoutStream',\n          autoCreateConsumer: true,\n          startingPosition: 'LATEST'\n        }\n      }\n    ]\n  });\n\n  return { resources: { fanoutStream, realtimeConsumer } };\n});\n```").default(false) }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Real-time data stream for ingesting high-volume events (logs, clickstreams, IoT, analytics).\n\n---\n\nContinuously captures data from many producers. Consumers (Lambda functions, etc.) process records in order.\nUse when you need real-time processing with sub-second latency, not just async messaging (use SQS for that)."), z.object({
      "type": z.literal("mongo-db-atlas-cluster"),
      "properties": z.object({
        "diskSizeGB": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Disk size in GB. Not available for shared tiers (M2/M5). M10+ auto-scales storage by default.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ordersDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      diskSizeGB: 40\n      enableBackups: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - ordersDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ordersDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    diskSizeGB: 40,\n    enableBackups: true\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [ordersDb]\n  });\n  return { resources: { ordersDb, api } };\n});\n```").optional(),
        "clusterTier": z.enum(["M10","M100","M140","M2","M20","M200","M200 Low-CPU (R200)","M200_NVME","M30","M300","M300 Low-CPU (R300)","M40","M40 Low-CPU (R40)","M400","M400 Low-CPU (R400)","M400_NVME","M40_NVME","M5","M50","M50 Low-CPU (R50)","M50_NVME","M60","M60 Low-CPU (R60)","M60_NVME","M700","M700 Low-CPU (R700)","M80","M80 Low-CPU (R80)","M80_NVME"]).describe("#### Instance size. M2/M5 = shared (cheapest). M10+ = dedicated (more features, auto-scaling, backups).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  appDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M20\n      version: '7.0'\n      enableBackups: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - appDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDb = new MongoDbAtlasCluster({\n    clusterTier: 'M20',\n    version: '7.0',\n    enableBackups: true\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [appDb]\n  });\n  return { resources: { appDb, api } };\n});\n```"),
        "version": z.enum(["5.0","6.0","7.0"]).optional().describe("#### MongoDB engine version.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  contentDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '6.0'\n      enableBackups: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - contentDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const contentDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '6.0',\n    enableBackups: true\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [contentDb]\n  });\n  return { resources: { contentDb, api } };\n});\n```").default("7.0"),
        "numShards": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Number of shards. More than 1 enables sharded mode for horizontal scaling. Requires M30+.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  shardedDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M30\n      version: '7.0'\n      numShards: 2\n      enableBackups: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - shardedDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const shardedDb = new MongoDbAtlasCluster({\n    clusterTier: 'M30',\n    version: '7.0',\n    numShards: 2,\n    enableBackups: true\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [shardedDb]\n  });\n  return { resources: { shardedDb, api } };\n});\n```").default(1),
        "replication": z.object({
          "numAnalyticsNodes": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Read-only nodes for long-running analytics queries. Prevents impact on primary workload performance.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  warehouseDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M30\n      version: '7.0'\n      enableBackups: true\n      replication:\n        numElectableNodes: 3\n        numAnalyticsNodes: 2\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - warehouseDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const warehouseDb = new MongoDbAtlasCluster({\n    clusterTier: 'M30',\n    version: '7.0',\n    enableBackups: true,\n    replication: {\n      numElectableNodes: 3,\n      numAnalyticsNodes: 2\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [warehouseDb]\n  });\n  return { resources: { warehouseDb, api } };\n});\n```").optional(),
          "numElectableNodes": z.union([z.literal(3), z.literal(5), z.literal(7)]).optional().describe("#### Nodes that can become primary. More = better redundancy. Must be odd.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  haDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M30\n      version: '7.0'\n      enableBackups: true\n      replication:\n        numElectableNodes: 5\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - haDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const haDb = new MongoDbAtlasCluster({\n    clusterTier: 'M30',\n    version: '7.0',\n    enableBackups: true,\n    replication: {\n      numElectableNodes: 5\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [haDb]\n  });\n  return { resources: { haDb, api } };\n});\n```").default(3),
          "numReadOnlyNodes": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Read-only replica nodes for scaling read throughput.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  readScaleDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M30\n      version: '7.0'\n      enableBackups: true\n      replication:\n        numElectableNodes: 3\n        numReadOnlyNodes: 3\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - readScaleDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const readScaleDb = new MongoDbAtlasCluster({\n    clusterTier: 'M30',\n    version: '7.0',\n    enableBackups: true,\n    replication: {\n      numElectableNodes: 3,\n      numReadOnlyNodes: 3\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [readScaleDb]\n  });\n  return { resources: { readScaleDb, api } };\n});\n```").optional() }).strict()
        .optional().describe("#### Node count configuration: electable, read-only, and analytics nodes. Default: 3 electable nodes.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  analyticsDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M30\n      version: '7.0'\n      replication:\n        numElectableNodes: 3\n        numReadOnlyNodes: 2\n        numAnalyticsNodes: 1\n      enableBackups: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - analyticsDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const analyticsDb = new MongoDbAtlasCluster({\n    clusterTier: 'M30',\n    version: '7.0',\n    replication: {\n      numElectableNodes: 3,\n      numReadOnlyNodes: 2,\n      numAnalyticsNodes: 1\n    },\n    enableBackups: true\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [analyticsDb]\n  });\n  return { resources: { analyticsDb, api } };\n});\n```").optional(),
        "enableBackups": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Enable daily snapshots (18:00 UTC). M10+ only — M2/M5 get automatic snapshots with different rules.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - paymentsDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [paymentsDb]\n  });\n  return { resources: { paymentsDb, api } };\n});\n```").optional(),
        "enablePointInTimeRecovery": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Restore to any second within the last 7 days. Requires `enableBackups: true` and M10+.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ledgerDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n      enablePointInTimeRecovery: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - ledgerDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ledgerDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true,\n    enablePointInTimeRecovery: true\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [ledgerDb]\n  });\n  return { resources: { ledgerDb, api } };\n});\n```").optional(),
        "biConnector": z.object({
          "readPreference": z.enum(["analytics","primary","secondary"]).optional().describe("#### Which node type the BI Connector reads from. Use `analytics` to avoid impacting production queries.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  biDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M30\n      version: '7.0'\n      enableBackups: true\n      replication:\n        numElectableNodes: 3\n        numAnalyticsNodes: 1\n      biConnector:\n        enabled: true\n        readPreference: secondary\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - biDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const biDb = new MongoDbAtlasCluster({\n    clusterTier: 'M30',\n    version: '7.0',\n    enableBackups: true,\n    replication: {\n      numElectableNodes: 3,\n      numAnalyticsNodes: 1\n    },\n    biConnector: {\n      enabled: true,\n      readPreference: 'secondary'\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [biDb]\n  });\n  return { resources: { biDb, api } };\n});\n```").optional(),
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable the BI Connector for SQL-based access.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  sqlAccessDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M30\n      version: '7.0'\n      enableBackups: true\n      replication:\n        numElectableNodes: 3\n        numAnalyticsNodes: 1\n      biConnector:\n        enabled: true\n        readPreference: analytics\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - sqlAccessDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sqlAccessDb = new MongoDbAtlasCluster({\n    clusterTier: 'M30',\n    version: '7.0',\n    enableBackups: true,\n    replication: {\n      numElectableNodes: 3,\n      numAnalyticsNodes: 1\n    },\n    biConnector: {\n      enabled: true,\n      readPreference: 'analytics'\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [sqlAccessDb]\n  });\n  return { resources: { sqlAccessDb, api } };\n});\n```").default(false) }).strict()
        .optional().describe("#### BI Connector for SQL-based access to MongoDB data. CPU-intensive — may degrade M10/M20 performance.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportingDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M30\n      version: '7.0'\n      enableBackups: true\n      replication:\n        numElectableNodes: 3\n        numAnalyticsNodes: 1\n      biConnector:\n        enabled: true\n        readPreference: analytics\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - reportingDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportingDb = new MongoDbAtlasCluster({\n    clusterTier: 'M30',\n    version: '7.0',\n    enableBackups: true,\n    replication: {\n      numElectableNodes: 3,\n      numAnalyticsNodes: 1\n    },\n    biConnector: {\n      enabled: true,\n      readPreference: 'analytics'\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [reportingDb]\n  });\n  return { resources: { reportingDb, api } };\n});\n```").optional(),
        "autoScaling": z.object({
          "minClusterTier": z.enum(["M10","M100","M140","M20","M200","M200 Low-CPU (R200)","M200_NVME","M30","M300","M300 Low-CPU (R300)","M40","M40 Low-CPU (R40)","M400 Low-CPU (R400)","M400_NVME","M40_NVME","M50","M50 Low-CPU (R50)","M50_NVME","M60","M60 Low-CPU (R60)","M60_NVME","M700 Low-CPU (R700)","M80","M80 Low-CPU (R80)","M80_NVME"]).optional().describe("#### Lowest tier the cluster can scale down to. Prevents unexpected cost increases from always scaling up.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  floorDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M20\n      version: '7.0'\n      enableBackups: true\n      autoScaling:\n        minClusterTier: M20\n        maxClusterTier: M50\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - floorDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const floorDb = new MongoDbAtlasCluster({\n    clusterTier: 'M20',\n    version: '7.0',\n    enableBackups: true,\n    autoScaling: {\n      minClusterTier: 'M20',\n      maxClusterTier: 'M50'\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [floorDb]\n  });\n  return { resources: { floorDb, api } };\n});\n```").optional(),
          "maxClusterTier": z.enum(["M10","M100","M140","M20","M200","M200 Low-CPU (R200)","M200_NVME","M30","M300","M300 Low-CPU (R300)","M40","M40 Low-CPU (R40)","M400 Low-CPU (R400)","M400_NVME","M40_NVME","M50","M50 Low-CPU (R50)","M50_NVME","M60","M60 Low-CPU (R60)","M60_NVME","M700 Low-CPU (R700)","M80","M80 Low-CPU (R80)","M80_NVME"]).optional().describe("#### Highest tier the cluster can scale up to. Set a ceiling to control maximum costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ceilingDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n      autoScaling:\n        minClusterTier: M10\n        maxClusterTier: M30\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - ceilingDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ceilingDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true,\n    autoScaling: {\n      minClusterTier: 'M10',\n      maxClusterTier: 'M30'\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [ceilingDb]\n  });\n  return { resources: { ceilingDb, api } };\n});\n```").optional(),
          "disableDiskScaling": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Prevent automatic disk expansion. By default, storage grows when usage hits 90%. Storage never scales down.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  fixedDiskDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      diskSizeGB: 80\n      enableBackups: true\n      autoScaling:\n        minClusterTier: M10\n        maxClusterTier: M40\n        disableDiskScaling: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - fixedDiskDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const fixedDiskDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    diskSizeGB: 80,\n    enableBackups: true,\n    autoScaling: {\n      minClusterTier: 'M10',\n      maxClusterTier: 'M40',\n      disableDiskScaling: true\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [fixedDiskDb]\n  });\n  return { resources: { fixedDiskDb, api } };\n});\n```").optional(),
          "disableScaleDown": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Prevent automatic scale-down. The cluster can only scale up, never back down to a smaller tier.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  scaleUpOnlyDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n      autoScaling:\n        minClusterTier: M10\n        maxClusterTier: M40\n        disableScaleDown: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - scaleUpOnlyDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const scaleUpOnlyDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true,\n    autoScaling: {\n      minClusterTier: 'M10',\n      maxClusterTier: 'M40',\n      disableScaleDown: true\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [scaleUpOnlyDb]\n  });\n  return { resources: { scaleUpOnlyDb, api } };\n});\n```").optional() }).strict()
        .optional().describe("#### Auto-scale tier and/or storage based on CPU/memory usage. Set min/max tier to control costs.\n\n---\n\nScales up when CPU or memory exceeds 75% for 1 hour. Scales down when both are below 50% for 24 hours.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  scalingDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n      autoScaling:\n        minClusterTier: M10\n        maxClusterTier: M40\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - scalingDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const scalingDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true,\n    autoScaling: {\n      minClusterTier: 'M10',\n      maxClusterTier: 'M40'\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [scalingDb]\n  });\n  return { resources: { scalingDb, api } };\n});\n```").optional(),
        "adminUserCredentials": z.object({
          "userName": z.string().describe("#### Name of the admin user\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  adminDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n      adminUserCredentials:\n        userName: opsadmin\n        password: $Secret('mongo-admin-password')\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - adminDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const adminDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true,\n    adminUserCredentials: {\n      userName: 'opsadmin',\n      password: $Secret('mongo-admin-password')\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [adminDb]\n  });\n  return { resources: { adminDb, api } };\n});\n```"),
          "password": z.string().describe("#### Password for the admin user\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  securedDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n      adminUserCredentials:\n        userName: dbadmin\n        password: $Secret('mongo-admin-password')\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - securedDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const securedDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true,\n    adminUserCredentials: {\n      userName: 'dbadmin',\n      password: $Secret('mongo-admin-password')\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [securedDb]\n  });\n  return { resources: { securedDb, api } };\n});\n```") }).strict()
        .optional().describe("#### Admin user for direct database access (e.g., from your local machine or admin tools).\n\n---\n\nNot required for app-to-database access via `connectTo` — that's handled automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  primaryDb:\n    type: mongo-db-atlas-cluster\n    properties:\n      clusterTier: M10\n      version: '7.0'\n      enableBackups: true\n      adminUserCredentials:\n        userName: dbadmin\n        password: $Secret('mongo-admin-password')\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - primaryDb\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MongoDbAtlasCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const primaryDb = new MongoDbAtlasCluster({\n    clusterTier: 'M10',\n    version: '7.0',\n    enableBackups: true,\n    adminUserCredentials: {\n      userName: 'dbadmin',\n      password: $Secret('mongo-admin-password')\n    }\n  });\n  const api = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/index.ts' } },\n    connectTo: [primaryDb]\n  });\n  return { resources: { primaryDb, api } };\n});\n```").optional() }).strict()
    }).strict()
    .describe("#### Managed MongoDB database (Atlas) deployed into your AWS account and managed within your stack.\n\n---\n\nDocument database with flexible schemas — great for content management, user profiles, catalogs, and apps\nwhere your data model evolves. Starts at M2 (shared, ~$9/month) or M10 (dedicated, ~$57/month)."), z.object({
      "type": z.literal("network-load-balancer"),
      "properties": z.object({
        "interface": z.enum(["internal","internet"]).optional().describe("#### `internet` (public) or `internal` (VPC-only).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  internalMqttLb:\n    type: network-load-balancer\n    properties:\n      interface: internal\n      listeners:\n        - protocol: TCP\n          port: 1883\n\n  mqttBroker:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.25\n        memory: 512\n      containers:\n        - name: broker\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/broker.ts\n          environment:\n            - name: PORT\n              value: 1883\n          events:\n            - type: network-load-balancer\n              properties:\n                containerPort: 1883\n                loadBalancerName: internalMqttLb\n                listenerPort: 1883\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NetworkLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const internalMqttLb = new NetworkLoadBalancer({\n    interface: 'internal',\n    listeners: [{ protocol: 'TCP', port: 1883 }]\n  });\n\n  const mqttBroker = new MultiContainerWorkload({\n    resources: { cpu: 0.25, memory: 512 },\n    containers: [\n      {\n        name: 'broker',\n        packaging: { type: 'stacktape-image-buildpack', properties: { entryfilePath: 'src/broker.ts' } },\n        environment: { PORT: 1883 },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: { containerPort: 1883, loadBalancerName: 'internalMqttLb', listenerPort: 1883 }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { internalMqttLb, mqttBroker } };\n});\n```").default("internet"),
        "customDomains": z.union([z.array(z.string()), z.array(z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          )]).optional().describe("#### Custom domains.\n\n---\n\nBy default, Stacktape creates DNS records and TLS certificates for each domain.\nIf you manage DNS yourself, set `disableDnsRecordCreation` and provide `customCertificateArn`.\n\nBackward compatible format `string[]` is still supported.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  secureLb:\n    type: network-load-balancer\n    properties:\n      customDomains:\n        - domainName: mqtt.mydomain.com\n        - domainName: legacy.mydomain.com\n          disableDnsRecordCreation: true\n      listeners:\n        - protocol: TLS\n          port: 8883\n\n  mqttBroker:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: broker\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/broker.ts\n          environment:\n            - name: PORT\n              value: 8883\n          events:\n            - type: network-load-balancer\n              properties:\n                containerPort: 8883\n                loadBalancerName: secureLb\n                listenerPort: 8883\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NetworkLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const secureLb = new NetworkLoadBalancer({\n    customDomains: [\n      { domainName: 'mqtt.mydomain.com' },\n      { domainName: 'legacy.mydomain.com', disableDnsRecordCreation: true }\n    ],\n    listeners: [{ protocol: 'TLS', port: 8883 }]\n  });\n\n  const mqttBroker = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'broker',\n        packaging: { type: 'stacktape-image-buildpack', properties: { entryfilePath: 'src/broker.ts' } },\n        environment: { PORT: 8883 },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: { containerPort: 8883, loadBalancerName: 'secureLb', listenerPort: 8883 }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { secureLb, mqttBroker } };\n});\n```").optional(),
        "listeners": z.array(z.object({
          "protocol": z.enum(["TCP","TLS"]).describe("#### `TCP` (raw) or `TLS` (encrypted). TLS requires a certificate (auto-created with `customDomains` or via `customCertificateArns`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tlsLb:\n    type: network-load-balancer\n    properties:\n      customDomains:\n        - domainName: mqtt.mydomain.com\n      listeners:\n        - protocol: TLS\n          port: 8883\n\n  mqttBroker:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.25\n        memory: 512\n      containers:\n        - name: broker\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/broker.ts\n          environment:\n            - name: PORT\n              value: 8883\n          events:\n            - type: network-load-balancer\n              properties:\n                containerPort: 8883\n                loadBalancerName: tlsLb\n                listenerPort: 8883\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NetworkLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tlsLb = new NetworkLoadBalancer({\n    customDomains: [{ domainName: 'mqtt.mydomain.com' }],\n    listeners: [\n      { protocol: 'TLS', port: 8883 }\n    ]\n  });\n\n  const mqttBroker = new MultiContainerWorkload({\n    resources: { cpu: 0.25, memory: 512 },\n    containers: [\n      {\n        name: 'broker',\n        packaging: { type: 'stacktape-image-buildpack', properties: { entryfilePath: 'src/broker.ts' } },\n        environment: { PORT: 8883 },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: { containerPort: 8883, loadBalancerName: 'tlsLb', listenerPort: 8883 }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { tlsLb, mqttBroker } };\n});\n```"),
          "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Port this listener accepts traffic on.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpLb:\n    type: network-load-balancer\n    properties:\n      listeners:\n        - protocol: TCP\n          port: 7777\n\n  gameServer:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: game-server\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          environment:\n            - name: PORT\n              value: 7777\n          events:\n            - type: network-load-balancer\n              properties:\n                containerPort: 7777\n                loadBalancerName: tcpLb\n                listenerPort: 7777\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NetworkLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tcpLb = new NetworkLoadBalancer({\n    listeners: [\n      {\n        protocol: 'TCP',\n        port: 7777\n      }\n    ]\n  });\n\n  const gameServer = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'game-server',\n        packaging: { type: 'stacktape-image-buildpack', properties: { entryfilePath: 'src/server.ts' } },\n        environment: { PORT: 7777 },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: { containerPort: 7777, loadBalancerName: 'tcpLb', listenerPort: 7777 }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { tcpLb, gameServer } };\n});\n```"),
          "customCertificateArns": z.array(z.string()).optional().describe("#### ARNs of your own ACM certificates. Not needed if using `customDomains` or TCP protocol.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  byoCertLb:\n    type: network-load-balancer\n    properties:\n      listeners:\n        - protocol: TLS\n          port: 8883\n          customCertificateArns:\n            - arn:aws:acm:eu-west-1:123456789012:certificate/2b8f1c3e-4a5d-6e7f-8a9b-0c1d2e3f4a5b\n\n  mqttBroker:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.25\n        memory: 512\n      containers:\n        - name: broker\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/broker.ts\n          environment:\n            - name: PORT\n              value: 8883\n          events:\n            - type: network-load-balancer\n              properties:\n                containerPort: 8883\n                loadBalancerName: byoCertLb\n                listenerPort: 8883\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NetworkLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const byoCertLb = new NetworkLoadBalancer({\n    listeners: [\n      {\n        protocol: 'TLS',\n        port: 8883,\n        customCertificateArns: [\n          'arn:aws:acm:eu-west-1:123456789012:certificate/2b8f1c3e-4a5d-6e7f-8a9b-0c1d2e3f4a5b'\n        ]\n      }\n    ]\n  });\n\n  const mqttBroker = new MultiContainerWorkload({\n    resources: { cpu: 0.25, memory: 512 },\n    containers: [\n      {\n        name: 'broker',\n        packaging: { type: 'stacktape-image-buildpack', properties: { entryfilePath: 'src/broker.ts' } },\n        environment: { PORT: 8883 },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: { containerPort: 8883, loadBalancerName: 'byoCertLb', listenerPort: 8883 }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { byoCertLb, mqttBroker } };\n});\n```").optional(),
          "whitelistIps": z.array(z.string()).optional().describe("#### Restrict access to specific IP addresses/CIDRs. Default: all IPs allowed.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  restrictedLb:\n    type: network-load-balancer\n    properties:\n      listeners:\n        - protocol: TCP\n          port: 5432\n          whitelistIps:\n            - 203.0.113.0/24\n            - 198.51.100.42/32\n\n  tcpProxy:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.25\n        memory: 512\n      containers:\n        - name: proxy\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/proxy.ts\n          environment:\n            - name: PORT\n              value: 5432\n          events:\n            - type: network-load-balancer\n              properties:\n                containerPort: 5432\n                loadBalancerName: restrictedLb\n                listenerPort: 5432\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NetworkLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const restrictedLb = new NetworkLoadBalancer({\n    listeners: [\n      {\n        protocol: 'TCP',\n        port: 5432,\n        whitelistIps: ['203.0.113.0/24', '198.51.100.42/32']\n      }\n    ]\n  });\n\n  const tcpProxy = new MultiContainerWorkload({\n    resources: { cpu: 0.25, memory: 512 },\n    containers: [\n      {\n        name: 'proxy',\n        packaging: { type: 'stacktape-image-buildpack', properties: { entryfilePath: 'src/proxy.ts' } },\n        environment: { PORT: 5432 },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: { containerPort: 5432, loadBalancerName: 'restrictedLb', listenerPort: 5432 }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { restrictedLb, tcpProxy } };\n});\n```").optional() }).strict()
        ).describe("#### Listeners define which ports and protocols (TCP/TLS) this load balancer accepts traffic on.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  multiPortLb:\n    type: network-load-balancer\n    properties:\n      listeners:\n        - protocol: TCP\n          port: 9000\n        - protocol: TCP\n          port: 9001\n\n  gameServer:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: game-server\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          environment:\n            - name: GAME_PORT\n              value: 9000\n            - name: ADMIN_PORT\n              value: 9001\n          events:\n            - type: network-load-balancer\n              properties:\n                containerPort: 9000\n                loadBalancerName: multiPortLb\n                listenerPort: 9000\n            - type: network-load-balancer\n              properties:\n                containerPort: 9001\n                loadBalancerName: multiPortLb\n                listenerPort: 9001\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NetworkLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const multiPortLb = new NetworkLoadBalancer({\n    listeners: [\n      { protocol: 'TCP', port: 9000 },\n      { protocol: 'TCP', port: 9001 }\n    ]\n  });\n\n  const gameServer = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'game-server',\n        packaging: { type: 'stacktape-image-buildpack', properties: { entryfilePath: 'src/server.ts' } },\n        environment: { GAME_PORT: 9000, ADMIN_PORT: 9001 },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: { containerPort: 9000, loadBalancerName: 'multiPortLb', listenerPort: 9000 }\n          },\n          {\n            type: 'network-load-balancer',\n            properties: { containerPort: 9001, loadBalancerName: 'multiPortLb', listenerPort: 9001 }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { multiPortLb, gameServer } };\n});\n```") }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### TCP/TLS load balancer for non-HTTP traffic (MQTT, game servers, custom protocols).\n\n---\n\nHandles millions of connections with ultra-low latency. Use when you need raw TCP/TLS\ninstead of HTTP routing. Does not support CDN, firewall, or gradual deployments.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  gameTrafficLb:\n    type: network-load-balancer\n    properties:\n      interface: internet\n      listeners:\n        - protocol: TCP\n          port: 9000\n\n  gameServer:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: udp-game-server\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          environment:\n            - name: PORT\n              value: 9000\n          events:\n            - type: network-load-balancer\n              properties:\n                containerPort: 9000\n                loadBalancerName: gameTrafficLb\n                listenerPort: 9000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NetworkLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const gameTrafficLb = new NetworkLoadBalancer({\n    interface: 'internet',\n    listeners: [{ protocol: 'TCP', port: 9000 }]\n  });\n\n  const gameServer = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'udp-game-server',\n        packaging: { type: 'stacktape-image-buildpack', properties: { entryfilePath: 'src/server.ts' } },\n        environment: { PORT: 9000 },\n        events: [\n          {\n            type: 'network-load-balancer',\n            properties: { containerPort: 9000, loadBalancerName: 'gameTrafficLb', listenerPort: 9000 }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { gameTrafficLb, gameServer } };\n});\n```"), z.object({
      "type": z.literal("nextjs-web"),
      "properties": z.object({
        "appDirectory": z.string().describe("#### Directory containing your `next.config.js`. For monorepos, point to the Next.js workspace.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./apps/storefront\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './apps/storefront',\n    environment: { NODE_ENV: 'production' }\n  });\n  return { resources: { web } };\n});\n```"),
        "serverLambda": z.object({
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128–10,240). CPU scales proportionally — 1,769 MB = 1 vCPU.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      serverLambda:\n        memory: 3008\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    serverLambda: {\n      memory: 3008\n    }\n  });\n  return { resources: { web } };\n});\n```").default(1024),
          "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Max: 30.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      serverLambda:\n        timeout: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    serverLambda: {\n      timeout: 30\n    }\n  });\n  return { resources: { web } };\n});\n```").default(30),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highVolumeFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highVolumeFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { highVolumeFunction } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs. Longer retention = higher storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/audit.ts' } },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditFunction } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Logging config. Logs are sent to CloudWatch. View with `stacktape logs` or in the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      serverLambda:\n        logging:\n          retentionDays: 90\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    serverLambda: {\n      logging: {\n        retentionDays: 90\n      }\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
          "joinDefaultVpc": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Connect to VPC resources (databases, Redis). **Warning:** function loses direct internet access.\n\n---\n\nS3 and DynamoDB remain accessible via auto-created VPC endpoints.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db.password')\n      engine:\n        type: postgres\n        properties:\n          primaryInstance:\n            instanceSize: db.t4g.micro\n          version: '16.6'\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      connectTo:\n        - database\n      serverLambda:\n        joinDefaultVpc: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db.password') },\n    engine: {\n      type: 'postgres',\n      properties: {\n        primaryInstance: { instanceSize: 'db.t4g.micro' },\n        version: '16.6'\n      }\n    }\n  });\n  const web = new NextjsWeb({\n    appDirectory: './',\n    connectTo: [database],\n    serverLambda: {\n      joinDefaultVpc: true\n    }\n  });\n  return { resources: { database, web } };\n});\n```").optional() }).strict()
        .optional().describe("#### Customize the SSR Lambda function (memory, timeout, VPC, logging).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      serverLambda:\n        memory: 2048\n        timeout: 25\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    serverLambda: {\n      memory: 2048,\n      timeout: 25\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "warmServerInstances": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Number of Lambda instances to keep warm (pre-initialized) to reduce cold starts.\n\n---\n\nA separate \"warmer\" function periodically pings the SSR Lambda. Not available with `useEdgeLambda: true`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      warmServerInstances: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    warmServerInstances: 2\n  });\n  return { resources: { web } };\n});\n```").default(0),
        "useEdgeLambda": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Run SSR at CloudFront edge locations for lower latency worldwide.\n\n---\n\n**Trade-offs:** Slower deploys, no `warmServerInstances`, no response streaming.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      useEdgeLambda: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    useEdgeLambda: true\n  });\n  return { resources: { web } };\n});\n```").default(false),
        "buildCommand": z.string().optional().describe("#### Override the default `next build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      buildCommand: pnpm run build\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    buildCommand: 'pnpm run build'\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "dev": z.object({
          "command": z.string().optional().describe("#### Dev server command (e.g., `npm run dev`).\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      dev:\n        command: pnpm run dev\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    dev: {\n      command: 'pnpm run dev'\n    }\n  });\n  return { resources: { web } };\n});\n```").default("next dev") }).strict()
        .optional().describe("#### Dev server config for `stacktape dev`. Defaults to `next dev`.").optional(),
        "fileOptions": z.array(z.object({
          "includePattern": z.string().describe("#### Glob pattern for files this rule applies to (e.g., `*.html`, `assets/**`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
          "excludePattern": z.string().optional().describe("#### Glob pattern for files to exclude even if they match `includePattern`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            excludePattern: 'assets/uncompressed/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          excludePattern: 'assets/uncompressed/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "headers": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### HTTP headers (e.g., `Cache-Control`) for matching files. Forwarded through CDN to the browser.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "tags": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### Tags for matching files. Can be used to target files with `lifecycleRules`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'reports/**'\n            tags:\n              - key: category\n                value: report\n      lifecycleRules:\n        - type: expiration\n          properties:\n            tags:\n              - key: category\n                value: report\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'reports/**',\n          tags: [{ key: 'category', value: 'report' }]\n        }\n      ]\n    },\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          tags: [{ key: 'category', value: 'report' }],\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Set custom headers (e.g., `Cache-Control`) for static files matching a pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      fileOptions:\n        - includePattern: '**/*.{js,css,woff2}'\n          headers:\n            - key: Cache-Control\n              value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    fileOptions: [\n      {\n        includePattern: '**/*.{js,css,woff2}',\n        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n      }\n    ]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables for the SSR function. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db.password')\n      engine:\n        type: postgres\n        properties:\n          primaryInstance:\n            instanceSize: db.t4g.micro\n          version: '16.6'\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      connectTo:\n        - database\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n        - name: STRIPE_SECRET_KEY\n          value: $Secret('stripe.secretKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, RelationalDatabase, defineConfig, $Secret, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db.password') },\n    engine: {\n      type: 'postgres',\n      properties: {\n        primaryInstance: { instanceSize: 'db.t4g.micro' },\n        version: '16.6'\n      }\n    }\n  });\n  const web = new NextjsWeb({\n    appDirectory: './',\n    connectTo: [database],\n    environment: {\n      NODE_ENV: 'production',\n      DATABASE_URL: $ResourceParam('database', 'connectionString'),\n      STRIPE_SECRET_KEY: $Secret('stripe.secretKey')\n    }\n  });\n  return { resources: { database, web } };\n});\n```").optional(),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Attach custom domains with auto-managed DNS records and TLS certificates.\n\n---\n\n**Prerequisite:** A Route 53 hosted zone for your domain must exist in your AWS account.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      customDomains:\n        - domainName: app.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    customDomains: [{ domainName: 'app.example.com' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this app. Firewall `scope` must be `cdn`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webFirewall:\n    type: web-app-firewall\n    properties:\n      scope: cdn\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      useFirewall: webFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webFirewall = new WebAppFirewall({ scope: 'cdn' });\n  const web = new NextjsWeb({\n    appDirectory: './',\n    useFirewall: 'webFirewall'\n  });\n  return { resources: { webFirewall, web } };\n});\n```").optional(),
        "streamingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Stream SSR responses for faster Time to First Byte and up to 20 MB response size (vs 6 MB default).\n\n---\n\nNot compatible with `useEdgeLambda: true`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      streamingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    streamingEnabled: true\n  });\n  return { resources: { web } };\n});\n```").default(false),
        "cdn": z.object({
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      disableInvalidationAfterDeploy: true\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(false),
          "defaultCachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Override default SSR caching behavior for all routes handled by the server.\n\n---\n\nUseful when you want to cache SSR responses longer than the framework defaults.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n          cacheKeyParameters:\n            queryString:\n              all: true\n            cookies:\n              none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 3600,\n        cacheKeyParameters: {\n          queryString: { all: true },\n          cookies: { none: true }\n        }\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "pathCachingOverrides": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `/_server-islands/*`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          -\n            path: /_nuxt/*\n            cachingOptions:\n              minTTL: 31536000\n              defaultTTL: 31536000\n              maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_nuxt/*',\n          cachingOptions: { minTTL: 31536000, defaultTTL: 31536000, maxTTL: 31536000 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```"),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .describe("#### Caching behavior override for this path pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n              minTTL: 0\n              defaultTTL: 15\n              maxTTL: 300\n              cacheKeyParameters:\n                queryString:\n                  whitelist:\n                    - page\n                    - limit\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/api/*',\n          cachingOptions: {\n            cacheMethods: ['GET', 'HEAD'],\n            minTTL: 0,\n            defaultTTL: 15,\n            maxTTL: 300,\n            cacheKeyParameters: {\n              queryString: { whitelist: ['page', 'limit'] }\n            }\n          }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```") }).strict()
          ).optional().describe("#### Override caching for specific CDN path patterns.\n\n---\n\nMatches existing framework-managed paths (e.g. `_astro/*`, `_next/data/*`) or adds\nnew server-path caching rules (e.g. `/_server-islands/*`) while preserving managed routing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              minTTL: 0\n              defaultTTL: 0\n              maxTTL: 0\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n                - OPTIONS\n              defaultTTL: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_server-islands/*',\n          cachingOptions: { minTTL: 0, defaultTTL: 0, maxTTL: 0 }\n        },\n        {\n          path: '/api/*',\n          cachingOptions: { cacheMethods: ['GET', 'HEAD', 'OPTIONS'], defaultTTL: 30 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### CDN cache controls for SSR routes and specific path patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      cdn:\n        disableInvalidationAfterDeploy: false\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              defaultTTL: 300\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './',\n    cdn: {\n      disableInvalidationAfterDeploy: false,\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 3600\n      },\n      pathCachingOverrides: [\n        {\n          path: '/_server-islands/*',\n          cachingOptions: { defaultTTL: 300 }\n        }\n      ]\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Deploy a Next.js app with SSR on AWS Lambda, static assets on S3, and a CloudFront CDN.\n\n---\n\nHandles ISR (Incremental Static Regeneration), image optimization, and middleware out of the box.\nOptionally deploy to Lambda@Edge for lower latency or enable response streaming.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NextjsWeb({\n    appDirectory: './'\n  });\n  return { resources: { web } };\n});\n```"), z.object({
      "type": z.literal("nuxt-web"),
      "properties": z.object({
        "appDirectory": z.string().optional().describe("#### Directory containing your `nuxt.config.ts`. For monorepos, point to the Nuxt workspace.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nuxt-web\n    properties:\n      appDirectory: ./apps/storefront\n      buildCommand: npm run build\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NuxtWeb({\n    appDirectory: './apps/storefront',\n    buildCommand: 'npm run build'\n  });\n  return { resources: { web } };\n});\n```"),
        "buildCommand": z.string().optional().describe("#### Override the default `nuxt build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      buildCommand: npm run generate:ssr\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NuxtWeb({\n    appDirectory: '.',\n    buildCommand: 'npm run generate:ssr'\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables for the SSR function. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  db:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t3.micro\n  web:\n    type: nuxt-web\n    properties:\n      environment:\n        - name: NUXT_PUBLIC_API_BASE\n          value: https://api.example.com\n        - name: DATABASE_URL\n          value: $ResourceParam('db', 'connectionString')\n        - name: SESSION_SECRET\n          value: $Secret('session.secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, RelationalDatabase, defineConfig, $Secret, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const db = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db.password') },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.6',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    }\n  });\n  const web = new NuxtWeb({\n    environment: {\n      NUXT_PUBLIC_API_BASE: 'https://api.example.com',\n      DATABASE_URL: $ResourceParam('db', 'connectionString'),\n      SESSION_SECRET: $Secret('session.secret')\n    }\n  });\n  return { resources: { db, web } };\n});\n```").optional(),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Attach custom domains with auto-managed DNS records and TLS certificates.\n\n---\n\n**Prerequisite:** A Route 53 hosted zone for your domain must exist in your AWS account.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nuxt-web\n    properties:\n      customDomains:\n        - domainName: www.example.com\n        - domainName: shop.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NuxtWeb({\n    customDomains: [\n      { domainName: 'www.example.com' },\n      { domainName: 'shop.example.com', disableDnsRecordCreation: true }\n    ]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "serverLambda": z.object({
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128–10,240). CPU scales proportionally — 1,769 MB = 1 vCPU.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      environment:\n        - name: NODE_ENV\n          value: production\n      serverLambda:\n        memory: 2048\n        timeout: 20\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    environment: { NODE_ENV: 'production' },\n    serverLambda: {\n      memory: 2048,\n      timeout: 20\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(1024),
          "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Max: 30.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        timeout: 25\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      timeout: 25\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(30),
          "joinDefaultVpc": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Connect to VPC resources (databases, Redis). **Warning:** function loses direct internet access.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('database.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      connectTo:\n        - database\n      environment:\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n      serverLambda:\n        memory: 1024\n        joinDefaultVpc: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, RelationalDatabase, $Secret, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('database.password') },\n    engine: {\n      type: 'postgres',\n      properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t4g.micro' } }\n    },\n    accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' }\n  });\n\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    connectTo: [database],\n    environment: { DATABASE_URL: $ResourceParam('database', 'connectionString') },\n    serverLambda: {\n      memory: 1024,\n      joinDefaultVpc: true\n    }\n  });\n\n  return { resources: { database, webApp } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highVolumeFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highVolumeFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { highVolumeFunction } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs. Longer retention = higher storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/audit.ts' } },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditFunction } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Logging config. Logs are sent to CloudWatch. View with `stacktape logs` or in the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        logging:\n          retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      logging: {\n        retentionDays: 30\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Customize the SSR Lambda function (memory, timeout, VPC, logging).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nuxt-web\n    properties:\n      serverLambda:\n        memory: 2048\n        timeout: 25\n        joinDefaultVpc: true\n        logging:\n          retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NuxtWeb({\n    serverLambda: {\n      memory: 2048,\n      timeout: 25,\n      joinDefaultVpc: true,\n      logging: { retentionDays: 30 }\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this app. Firewall `scope` must be `cdn`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  cdnFirewall:\n    type: web-app-firewall\n    properties:\n      scope: cdn\n      defaultAction: Allow\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 10\n  web:\n    type: nuxt-web\n    properties:\n      useFirewall: cdnFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const cdnFirewall = new WebAppFirewall({\n    scope: 'cdn',\n    defaultAction: 'Allow',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: { name: 'AWSManagedRulesCommonRuleSet', vendorName: 'AWS', priority: 10 }\n      }\n    ]\n  });\n  const web = new NuxtWeb({\n    useFirewall: 'cdnFirewall'\n  });\n  return { resources: { cdnFirewall, web } };\n});\n```").optional(),
        "dev": z.object({
          "command": z.string().optional().describe("#### Override the default dev server command (e.g., `npm run dev`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      dev:\n        command: npm run dev\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    dev: {\n      command: 'npm run dev'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "workingDirectory": z.string().optional().describe("#### Working directory for the dev command, relative to project root.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: packages/web\n      dev:\n        command: npm run dev\n        workingDirectory: packages/web\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: 'packages/web',\n    dev: {\n      command: 'npm run dev',\n      workingDirectory: 'packages/web'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Dev server config for `stacktape dev`. Defaults to `nuxt dev`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nuxt-web\n    properties:\n      appDirectory: ./apps/web\n      dev:\n        command: npm run dev\n        workingDirectory: ./apps/web\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NuxtWeb({\n    appDirectory: './apps/web',\n    dev: {\n      command: 'npm run dev',\n      workingDirectory: './apps/web'\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "fileOptions": z.array(z.object({
          "includePattern": z.string().describe("#### Glob pattern for files this rule applies to (e.g., `*.html`, `assets/**`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
          "excludePattern": z.string().optional().describe("#### Glob pattern for files to exclude even if they match `includePattern`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            excludePattern: 'assets/uncompressed/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          excludePattern: 'assets/uncompressed/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "headers": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### HTTP headers (e.g., `Cache-Control`) for matching files. Forwarded through CDN to the browser.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "tags": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### Tags for matching files. Can be used to target files with `lifecycleRules`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'reports/**'\n            tags:\n              - key: category\n                value: report\n      lifecycleRules:\n        - type: expiration\n          properties:\n            tags:\n              - key: category\n                value: report\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'reports/**',\n          tags: [{ key: 'category', value: 'report' }]\n        }\n      ]\n    },\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          tags: [{ key: 'category', value: 'report' }],\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Set custom headers (e.g., `Cache-Control`) for static files matching a pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nuxt-web\n    properties:\n      fileOptions:\n        - includePattern: '_nuxt/**'\n          headers:\n            - key: Cache-Control\n              value: 'public, max-age=31536000, immutable'\n        - includePattern: '*.html'\n          headers:\n            - key: Cache-Control\n              value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NuxtWeb({\n    fileOptions: [\n      {\n        includePattern: '_nuxt/**',\n        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n      },\n      {\n        includePattern: '*.html',\n        headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n      }\n    ]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "cdn": z.object({
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      disableInvalidationAfterDeploy: true\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(false),
          "defaultCachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Override default SSR caching behavior for all routes handled by the server.\n\n---\n\nUseful when you want to cache SSR responses longer than the framework defaults.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n          cacheKeyParameters:\n            queryString:\n              all: true\n            cookies:\n              none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 3600,\n        cacheKeyParameters: {\n          queryString: { all: true },\n          cookies: { none: true }\n        }\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "pathCachingOverrides": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `/_server-islands/*`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          -\n            path: /_nuxt/*\n            cachingOptions:\n              minTTL: 31536000\n              defaultTTL: 31536000\n              maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_nuxt/*',\n          cachingOptions: { minTTL: 31536000, defaultTTL: 31536000, maxTTL: 31536000 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```"),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .describe("#### Caching behavior override for this path pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n              minTTL: 0\n              defaultTTL: 15\n              maxTTL: 300\n              cacheKeyParameters:\n                queryString:\n                  whitelist:\n                    - page\n                    - limit\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/api/*',\n          cachingOptions: {\n            cacheMethods: ['GET', 'HEAD'],\n            minTTL: 0,\n            defaultTTL: 15,\n            maxTTL: 300,\n            cacheKeyParameters: {\n              queryString: { whitelist: ['page', 'limit'] }\n            }\n          }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```") }).strict()
          ).optional().describe("#### Override caching for specific CDN path patterns.\n\n---\n\nMatches existing framework-managed paths (e.g. `_astro/*`, `_next/data/*`) or adds\nnew server-path caching rules (e.g. `/_server-islands/*`) while preserving managed routing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              minTTL: 0\n              defaultTTL: 0\n              maxTTL: 0\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n                - OPTIONS\n              defaultTTL: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_server-islands/*',\n          cachingOptions: { minTTL: 0, defaultTTL: 0, maxTTL: 0 }\n        },\n        {\n          path: '/api/*',\n          cachingOptions: { cacheMethods: ['GET', 'HEAD', 'OPTIONS'], defaultTTL: 30 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### CDN cache controls for SSR routes and specific path patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: nuxt-web\n    properties:\n      cdn:\n        disableInvalidationAfterDeploy: false\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n        pathCachingOverrides:\n          - path: '/_nuxt/*'\n            cachingOptions:\n              defaultTTL: 31536000\n              maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new NuxtWeb({\n    cdn: {\n      disableInvalidationAfterDeploy: false,\n      defaultCachingOptions: { minTTL: 0, defaultTTL: 60, maxTTL: 3600 },\n      pathCachingOverrides: [\n        { path: '/_nuxt/*', cachingOptions: { defaultTTL: 31536000, maxTTL: 31536000 } }\n      ]\n    }\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Deploy a Nuxt SSR app with Lambda (Nitro aws-lambda preset), S3 for static assets, and CloudFront CDN.\n\n---\n\nFor static-only Nuxt sites, use `hosting-bucket` with `hostingContentType: 'nuxt-static-website'` instead."), z.object({
      "type": z.literal("open-search-domain"),
      "properties": z.object({
        "version": z.enum(["1.0","1.1","1.2","1.3","2.11","2.13","2.15","2.17","2.3","2.5","2.7","2.9"]).optional().describe("#### OpenSearch engine version. Pin this to avoid surprises when the default changes.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.15'\n      clusterConfig:\n        instanceType: t3.medium.search\n        instanceCount: 1\n      storage:\n        size: 20\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.15',\n    clusterConfig: {\n      instanceType: 't3.medium.search',\n      instanceCount: 1\n    },\n    storage: {\n      size: 20\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```"),
        "clusterConfig": z.object({
          "instanceType": z.string().describe("#### Instance type for data nodes (e.g., `t3.medium.search`, `r6g.large.search`).\n\n---\n\nData nodes store data and handle queries. For production, pair with dedicated master nodes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```"),
          "instanceCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Number of data nodes. More nodes = more storage capacity and query throughput.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 4\n      storage:\n        size: 80\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 4\n    },\n    storage: {\n      size: 80\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```"),
          "dedicatedMasterType": z.string().optional().describe("#### Instance type for dedicated master nodes (e.g., `m5.large.search`). Manages cluster state, not data.\n\n---\n\nRecommended for clusters with 3+ data nodes to prevent split-brain. Use an odd count (3, 5, or 7).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 3\n        dedicatedMasterType: m5.large.search\n        dedicatedMasterCount: 3\n      storage:\n        size: 50\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 3,\n      dedicatedMasterType: 'm5.large.search',\n      dedicatedMasterCount: 3\n    },\n    storage: {\n      size: 50\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional(),
          "dedicatedMasterCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Number of dedicated master nodes. Must be odd (3, 5, or 7) for quorum.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 3\n        dedicatedMasterType: m5.large.search\n        dedicatedMasterCount: 3\n      storage:\n        size: 50\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 3,\n      dedicatedMasterType: 'm5.large.search',\n      dedicatedMasterCount: 3\n    },\n    storage: {\n      size: 50\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional(),
          "warmType": z.string().optional().describe("#### Instance type for warm (UltraWarm) nodes — cheaper storage for infrequently accessed data.\n\n---\n\nData on warm nodes is still searchable but with higher query latency. Great for retaining old logs\nor time-series data at lower cost.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 3\n        dedicatedMasterType: m5.large.search\n        dedicatedMasterCount: 3\n        warmType: ultrawarm1.medium.search\n        warmCount: 2\n      storage:\n        size: 50\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 3,\n      dedicatedMasterType: 'm5.large.search',\n      dedicatedMasterCount: 3,\n      warmType: 'ultrawarm1.medium.search',\n      warmCount: 2\n    },\n    storage: {\n      size: 50\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional(),
          "warmCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Number of warm (UltraWarm) nodes for lower-cost storage of older data.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 3\n        dedicatedMasterType: m5.large.search\n        dedicatedMasterCount: 3\n        warmType: ultrawarm1.medium.search\n        warmCount: 2\n      storage:\n        size: 50\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 3,\n      dedicatedMasterType: 'm5.large.search',\n      dedicatedMasterCount: 3,\n      warmType: 'ultrawarm1.medium.search',\n      warmCount: 2\n    },\n    storage: {\n      size: 50\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional(),
          "multiAzDisabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable Multi-AZ replication. Not recommended — reduces availability and data durability.\n\n---\n\nMulti-AZ is auto-enabled for clusters with 2+ nodes. It distributes nodes across availability zones\nso the cluster survives an AZ outage.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: t3.medium.search\n        instanceCount: 2\n        multiAzDisabled: true\n      storage:\n        size: 20\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 't3.medium.search',\n      instanceCount: 2,\n      multiAzDisabled: true\n    },\n    storage: {\n      size: 20\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").default(false),
          "standbyEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Enable Multi-AZ with a standby AZ for highest availability (99.99% SLA).\n\n---\n\nDistributes nodes across 3 AZs with one as standby. The standby takes over instantly during failures\nwithout re-balancing. Requires: version 1.3+, 3 dedicated master + data nodes, GP3/SSD instances.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 3\n        dedicatedMasterType: m5.large.search\n        dedicatedMasterCount: 3\n        standbyEnabled: true\n      storage:\n        size: 50\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 3,\n      dedicatedMasterType: 'm5.large.search',\n      dedicatedMasterCount: 3,\n      standbyEnabled: true\n    },\n    storage: {\n      size: 50\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").default(false) }).strict()
        .optional().describe("#### Instance types, counts, and cluster topology (data nodes, master nodes, warm storage).\n\n---\n\nDefaults to a single `m4.large.search` node if not specified.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 3\n        dedicatedMasterType: m5.large.search\n        dedicatedMasterCount: 3\n      storage:\n        size: 50\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 3,\n      dedicatedMasterType: 'm5.large.search',\n      dedicatedMasterCount: 3\n    },\n    storage: {\n      size: 50\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional(),
        "storage": z.object({
          "size": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### EBS volume size per data node in GiB. Min/max depends on instance type (typically 10–16,384 GiB).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 100\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 100\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```"),
          "iops": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Provisioned IOPS per data node. GP3 volumes only.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 100\n        iops: 5000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 100,\n      iops: 5000\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").default(3000),
          "throughput": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Provisioned throughput per data node in MiB/s. GP3 volumes only.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 100\n        iops: 5000\n        throughput: 250\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 100,\n      iops: 5000,\n      throughput: 250\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").default(125) }).strict()
        .optional().describe("#### EBS volume size, IOPS, and throughput per data node. Only for EBS-backed instance types.\n\n---\n\n`iops` and `throughput` settings only apply to GP3 volumes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 100\n        iops: 4000\n        throughput: 250\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 100,\n      iops: 4000,\n      throughput: 250\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional(),
        "userPool": z.string().optional().describe("#### Name of a `user-pool` resource in your config. Enables login to OpenSearch Dashboards via Cognito.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  dashboardUsers:\n    type: user-auth-pool\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: t3.medium.search\n        instanceCount: 1\n      storage:\n        size: 20\n      userPool: dashboardUsers\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, UserAuthPool, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const dashboardUsers = new UserAuthPool({});\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 't3.medium.search',\n      instanceCount: 1\n    },\n    storage: {\n      size: 20\n    },\n    userPool: 'dashboardUsers'\n  });\n  return { resources: { dashboardUsers, searchEngine } };\n});\n```").optional(),
        "logging": z.object({
          "errorLogs": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable this log type.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        errorLogs:\n          retentionDays: 30\n        searchSlowLogs:\n          disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      errorLogs: {\n        retentionDays: 30\n      },\n      searchSlowLogs: {\n        disabled: true\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### Days to keep logs in CloudWatch before automatic deletion.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        errorLogs:\n          retentionDays: 90\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      errorLogs: {\n        retentionDays: 90\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```\n\nWhen omitted, Stacktape retains `errorLogs` for 30 days and `searchSlowLogs` and `indexSlowLogs` for 5 days.\nThe default depends on the containing log type and is applied during synthesis.").optional() }).strict()
          .optional().describe("#### Error logs — script compilation errors, invalid queries, indexing issues, snapshot failures.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        errorLogs:\n          retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      errorLogs: {\n        retentionDays: 30\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional(),
          "searchSlowLogs": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable this log type.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        errorLogs:\n          retentionDays: 30\n        searchSlowLogs:\n          disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      errorLogs: {\n        retentionDays: 30\n      },\n      searchSlowLogs: {\n        disabled: true\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### Days to keep logs in CloudWatch before automatic deletion.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        errorLogs:\n          retentionDays: 90\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      errorLogs: {\n        retentionDays: 90\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```\n\nWhen omitted, Stacktape retains `errorLogs` for 30 days and `searchSlowLogs` and `indexSlowLogs` for 5 days.\nThe default depends on the containing log type and is applied during synthesis.").optional() }).strict()
          .optional().describe("#### Search slow logs — queries exceeding thresholds you configure in OpenSearch index settings.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        searchSlowLogs:\n          retentionDays: 14\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      searchSlowLogs: {\n        retentionDays: 14\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional(),
          "indexSlowLogs": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable this log type.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        errorLogs:\n          retentionDays: 30\n        searchSlowLogs:\n          disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      errorLogs: {\n        retentionDays: 30\n      },\n      searchSlowLogs: {\n        disabled: true\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### Days to keep logs in CloudWatch before automatic deletion.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        errorLogs:\n          retentionDays: 90\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      errorLogs: {\n        retentionDays: 90\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```\n\nWhen omitted, Stacktape retains `errorLogs` for 30 days and `searchSlowLogs` and `indexSlowLogs` for 5 days.\nThe default depends on the containing log type and is applied during synthesis.").optional() }).strict()
          .optional().describe("#### Indexing slow logs — indexing operations exceeding thresholds you configure in OpenSearch index settings.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        indexSlowLogs:\n          retentionDays: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      indexSlowLogs: {\n        retentionDays: 7\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional() }).strict()
        .optional().describe("#### Error logs, search slow logs, and indexing slow logs. Sent to CloudWatch automatically.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      logging:\n        errorLogs:\n          retentionDays: 30\n        searchSlowLogs:\n          retentionDays: 14\n        indexSlowLogs:\n          retentionDays: 14\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    logging: {\n      errorLogs: {\n        retentionDays: 30\n      },\n      searchSlowLogs: {\n        retentionDays: 14\n      },\n      indexSlowLogs: {\n        retentionDays: 14\n      }\n    }\n  });\n  return { resources: { searchEngine } };\n});\n```").optional(),
        "accessibility": z.object({
          "accessibilityMode": z.enum(["internet","scoping-workloads-in-vpc","vpc"]).describe("#### How the domain can be reached.\n\n---\n\n- **`internet`**: Accessible from anywhere (still requires IAM credentials).\n- **`vpc`**: Only accessible from resources inside your VPC (functions with `joinDefaultVpc: true`, containers, batch jobs).\n- **`scoping-workloads-in-vpc`**: Like `vpc`, but also requires security-group access via `connectTo`.\n\n**Cannot be changed after creation** — switching between internet and VPC modes requires a new domain.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n  indexer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/indexer.ts\n      joinDefaultVpc: true\n      connectTo:\n        - searchEngine\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    accessibility: {\n      accessibilityMode: 'scoping-workloads-in-vpc'\n    }\n  });\n  const indexer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: {\n        entryfilePath: 'src/indexer.ts'\n      }\n    },\n    joinDefaultVpc: true,\n    connectTo: [searchEngine]\n  });\n  return { resources: { searchEngine, indexer } };\n});\n```").default("internet") }).strict()
        .optional().describe("#### Network access mode: public internet (default), VPC-only, or VPC with security-group scoping.\n\n---\n\nEven in `internet` mode, access requires IAM credentials. VPC modes add network-level isolation.\n**Warning:** you cannot switch between `internet` and `vpc`/`scoping-workloads-in-vpc` after creation.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  searchEngine:\n    type: open-search-domain\n    properties:\n      version: '2.17'\n      clusterConfig:\n        instanceType: r6g.large.search\n        instanceCount: 2\n      storage:\n        size: 50\n      accessibility:\n        accessibilityMode: vpc\n  indexer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/indexer.ts\n      joinDefaultVpc: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { OpenSearchDomain, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const searchEngine = new OpenSearchDomain({\n    version: '2.17',\n    clusterConfig: {\n      instanceType: 'r6g.large.search',\n      instanceCount: 2\n    },\n    storage: {\n      size: 50\n    },\n    accessibility: {\n      accessibilityMode: 'vpc'\n    }\n  });\n  const indexer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: {\n        entryfilePath: 'src/indexer.ts'\n      }\n    },\n    joinDefaultVpc: true\n  });\n  return { resources: { searchEngine, indexer } };\n});\n```").optional() }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Managed search and analytics engine (OpenSearch/Elasticsearch compatible).\n\n---\n\nFull-text search, log analytics, and real-time dashboards. Use for search features in your app,\ncentralized logging, or time-series data analysis. Costs start at ~$50/month (single small node)."), z.object({
      "type": z.literal("private-service"),
      "properties": z.object({
        "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port this service listens on. Injected as the `PORT` env var.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  internalApi:\n    type: private-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      port: 8080\n      resources:\n        cpu: 1\n        memory: 1024\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { PrivateService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const internalApi = new PrivateService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: {\n        entryfilePath: 'src/server.ts'\n      }\n    },\n    port: 8080,\n    resources: {\n      cpu: 1,\n      memory: 1024\n    },\n    environment: {\n      NODE_ENV: 'production'\n    }\n  });\n\n  return { resources: { internalApi } };\n});\n```").default(3000),
        "protocol": z.enum(["grpc","http","http2"]).optional().describe("#### Protocol for metrics collection. Set to enable protocol-specific metrics (e.g., HTTP 5xx tracking).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  grpcBackend:\n    type: private-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/grpc-server.ts\n      port: 50051\n      protocol: grpc\n      resources:\n        cpu: 2\n        memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { PrivateService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const grpcBackend = new PrivateService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: {\n        entryfilePath: 'src/grpc-server.ts'\n      }\n    },\n    port: 50051,\n    protocol: 'grpc',\n    resources: {\n      cpu: 2,\n      memory: 2048\n    }\n  });\n\n  return { resources: { grpcBackend } };\n});\n```").optional(),
        "loadBalancing": z.object({
          "type": z.enum(["application-load-balancer","service-connect"]).describe("---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  notificationsService:\n    type: private-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/notifications.ts\n      port: 3000\n      resources:\n        cpu: 1\n        memory: 1024\n      loadBalancing:\n        type: service-connect\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { PrivateService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const notificationsService = new PrivateService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: {\n        entryfilePath: 'src/notifications.ts'\n      }\n    },\n    port: 3000,\n    resources: {\n      cpu: 1,\n      memory: 1024\n    },\n    loadBalancing: {\n      type: 'service-connect'\n    }\n  });\n\n  return { resources: { notificationsService } };\n});\n```") }).strict()
        .optional().describe("#### How traffic reaches this service from other resources.\n\n---\n\n- **`service-connect`** (default, ~$0.50/mo): Direct container-to-container. Cheapest option.\n  Only reachable from other container-based resources in the stack.\n- **`application-load-balancer`** (~$18/mo): HTTP load balancer. Reachable from any VPC resource.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsService:\n    type: private-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      port: 3000\n      resources:\n        cpu: 1\n        memory: 1024\n      loadBalancing:\n        type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { PrivateService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsService = new PrivateService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: {\n        entryfilePath: 'src/payments.ts'\n      }\n    },\n    port: 3000,\n    resources: {\n      cpu: 1,\n      memory: 1024\n    },\n    loadBalancing: {\n      type: 'application-load-balancer'\n    }\n  });\n\n  return { resources: { paymentsService } };\n});\n```"),
        "packaging": z.union([z.object({
            "type": z.literal("prebuilt-image"),
            "properties": z.object({
              "repositoryCredentialsSecretArn": z.string().optional().describe("#### The ARN of a secret containing credentials for a private container registry.\n\n---\n\nThe secret must be a JSON object with `username` and `password` keys.\nYou can create secrets using the `stacktape secret:create` command.\n\n**Example (YAML):**\n\n```yaml\nresources:\n privateImageService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: registry.example.com/my-org/my-app:latest\n         repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const privateImageService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'registry.example.com/my-org/my-app:latest',\n       repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { privateImageService } };\n});\n```").optional(),
              "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: node:22-alpine\n         entryPoint:\n           - /bin/sh\n           - -c\n           - node server.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'node:22-alpine',\n       entryPoint: ['/bin/sh', '-c', 'node server.js']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
              "image": z.string().describe("#### The name or URL of the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu'\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```"),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n           command:\n             - -i\n             - input.mp4\n             - output.webm\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu',\n         command: ['-i', 'input.mp4', 'output.webm']\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```").optional() }).strict()
            .describe("#### Configures a pre-built container image.") }).strict()
          .describe("#### Uses a pre-built container image.\n\n---\n\nWith `prebuilt-image`, you provide a reference to an existing container image.\nThis can be a public image from Docker Hub or a private image from any container registry.\n\nFor private registries, configure `repositoryCredentialsSecretArn` with credentials stored in AWS Secrets Manager."), z.object({
            "type": z.literal("custom-dockerfile"),
            "properties": z.object({
              "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: custom-dockerfile\n       properties:\n         buildContextPath: ./app\n         entryPoint:\n           - /app/entrypoint.sh\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'custom-dockerfile',\n     properties: {\n       buildContextPath: './app',\n       entryPoint: ['/app/entrypoint.sh']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
              "dockerfilePath": z.string().optional().describe("#### The path to the Dockerfile, relative to `buildContextPath`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           dockerfilePath: docker/Dockerfile.prod\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         dockerfilePath: 'docker/Dockerfile.prod'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
              "buildContextPath": z.string().describe("#### The path to the build context directory, relative to your Stacktape configuration file.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
              "buildArgs": z.array(z.object({
                "argName": z.string().describe("#### Argument name\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                "value": z.string().describe("#### Argument value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```") }).strict()
              ).optional().describe("#### A list of arguments to pass to the `docker build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: NODE_ENV\n               value: production\n             - argName: BUILD_VERSION\n               value: \"1.4.2\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           { argName: 'NODE_ENV', value: 'production' },\n           { argName: 'BUILD_VERSION', value: '1.4.2' }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           command:\n             - node\n             - dist/process.js\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         command: ['node', 'dist/process.js']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional() }).strict()
            .describe("#### Configures an image to be built by Stacktape from a specified Dockerfile.") }).strict()
          .describe("#### Builds a container image from your own Dockerfile.\n\n---\n\nWith `custom-dockerfile`, you provide a path to your Dockerfile and build context.\nStacktape builds the image and uploads it to a managed ECR repository.\n\nThis gives you full control over the container environment and is ideal for complex setups."), z.object({
            "type": z.literal("external-buildpack"),
            "properties": z.object({
              "builder": z.string().optional().describe("#### The Buildpack Builder to use.\n\n---\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           builder: paketobuildpacks/builder-jammy-full\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         builder: 'paketobuildpacks/builder-jammy-full'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
              "buildpacks": z.array(z.string()).optional().describe("#### The specific Buildpack to use.\n\n---\n\nBy default, the buildpack is detected automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           buildpacks:\n             - paketo-buildpacks/nodejs\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         buildpacks: ['paketo-buildpacks/nodejs']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional(),
              "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           command:\n             - npm\n             - run\n             - import\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         command: ['npm', 'run', 'import']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Builds a container image using an external buildpack.\n\n---\n\nExternal buildpacks (buildpacks.io) automatically detect your application type\nand build an optimized container image with zero configuration.\n\nThe default builder is `paketobuildpacks/builder-jammy-base`.\nYou can find buildpacks for almost any language or framework."), z.object({
            "type": z.literal("nixpacks"),
            "properties": z.object({
              "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
              "buildImage": z.string().optional().describe("#### The base image to use for building the application.\n\n---\n\nFor more details, see the [Nixpacks documentation](https://nixpacks.com/docs/configuration/file#build-image).\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         buildImage: ubuntu:22.04\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       buildImage: 'ubuntu:22.04'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "providers": z.array(z.string()).optional().describe("#### A list of providers to use for determining the build and runtime environments.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         providers:\n           - node\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       providers: ['node']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startCmd": z.string().optional().describe("#### The command to execute when starting the application.\n\n---\n\nThis overrides the default start command inferred by Nixpacks.\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startCmd: node dist/worker.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startCmd: 'node dist/worker.js'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startRunImage": z.string().optional().describe("#### The base image to use for running the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startRunImage: gcr.io/distroless/nodejs22-debian12\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startRunImage: 'gcr.io/distroless/nodejs22-debian12'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startOnlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in the runtime environment; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startOnlyIncludeFiles:\n           - dist\n           - node_modules\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startOnlyIncludeFiles: ['dist', 'node_modules']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "phases": z.array(z.object({
                "name": z.string().describe("#### The name of the build phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                "cmds": z.array(z.string()).optional().describe("#### A list of shell commands to execute in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n               - npm prune --omit=dev\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build', 'npm prune --omit=dev']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixPkgs": z.array(z.string()).optional().describe("#### A list of Nix packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - ffmpeg\n               - imagemagick\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['ffmpeg', 'imagemagick']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixLibs": z.array(z.string()).optional().describe("#### A list of Nix libraries to include in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixLibs:\n               - openssl\n               - zlib\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixLibs: ['openssl', 'zlib']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixOverlay": z.array(z.string()).optional().describe("#### A list of Nix overlay files to apply in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixOverlay:\n               - ./nix/overlay.nix\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixOverlay: ['./nix/overlay.nix']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixpkgsArchive": z.string().optional().describe("#### The Nixpkgs archive to use.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - nodejs_22\n             nixpkgsArchive: bf3287dac860542719b3849d6970d22f635f9da1\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['nodejs_22'],\n           nixpkgsArchive: 'bf3287dac860542719b3849d6970d22f635f9da1'\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "aptPkgs": z.array(z.string()).optional().describe("#### A list of APT packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             aptPkgs:\n               - libpq-dev\n               - poppler-utils\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           aptPkgs: ['libpq-dev', 'poppler-utils']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "cacheDirectories": z.array(z.string()).optional().describe("#### A list of directories to cache between builds to speed up subsequent builds.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n             cacheDirectories:\n               - node_modules/.cache\n               - /root/.npm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci'],\n           cacheDirectories: ['node_modules/.cache', '/root/.npm']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "onlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in this phase; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n             onlyIncludeFiles:\n               - src\n               - package.json\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build'],\n           onlyIncludeFiles: ['src', 'package.json']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
              ).optional().describe("#### The build phases for the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n           - name: build\n             cmds:\n               - npm run build\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         { name: 'install', cmds: ['npm ci'] },\n         { name: 'build', cmds: ['npm run build'] }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Builds a container image using Nixpacks.\n\n---\n\nNixpacks automatically detects your application type and builds an optimized container image.\nIn most cases, no configuration is required.\n\nIt supports a wide range of languages and frameworks out of the box."), z.object({
            "type": z.literal("stacktape-image-buildpack"),
            "properties": z.object({
              "languageSpecificConfig": z.union([z.object({
                  "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                  "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                  "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                , z.object({
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                  "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                  "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                , z.object({
                  "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                , z.record(z.string(), z.any()), z.object({
                  "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                , z.object({
                  "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                , z.object({
                  "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                  "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
              ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { apiService } };\n});\n```").optional(),
              "requiresGlibcBinaries": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use glibc instead of musl (Alpine default). Enable if native dependencies require glibc.\n\n---\n\nResults in a larger image. Common packages needing this: `sharp`, `canvas`, `bcrypt`, `puppeteer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         requiresGlibcBinaries: true\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       requiresGlibcBinaries: true\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
              "customDockerBuildCommands": z.array(z.string()).optional().describe("#### A list of commands to be executed during the `docker build` process.\n\n---\n\nThese commands are executed using the `RUN` directive in the Dockerfile.\nThis is useful for installing additional system dependencies in your container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         customDockerBuildCommands:\n           - apt-get update && apt-get install -y poppler-utils\n           - fc-cache -f\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       customDockerBuildCommands: [\n         'apt-get update && apt-get install -y poppler-utils',\n         'fc-cache -f'\n       ]\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
              "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
              "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            .describe("#### Configures an image to be built automatically by Stacktape from your source code.") }).strict()
          .describe("#### A zero-config buildpack that creates a container image from your source code.\n\n---\n\nThe `stacktape-image-buildpack` automatically bundles your code and dependencies into an optimized container image.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, and Go.\n\nFor JS/TS, your code is bundled into a single file with source maps.\nThe resulting image is uploaded to a managed ECR repository.")]).describe("#### Configures the container image for the service.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return { resources: { api } };\n});\n```"),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables injected into the container at runtime.\n\n---\n\nUse for configuration like API keys, feature flags, or secrets.\nVariables from `connectTo` (e.g., `STP_MY_DATABASE_CONNECTION_STRING`) are added automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: LOG_LEVEL\n          value: info\n        - name: DATABASE_URL\n          value: $ResourceParam('mainDb', 'connectionString')\n      connectTo:\n        - mainDb\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $ResourceParam, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      LOG_LEVEL: 'info',\n      DATABASE_URL: $ResourceParam('mainDb', 'connectionString')\n    },\n    connectTo: [mainDb]\n  });\n\n  return { resources: { api, mainDb } };\n});\n```").optional(),
        "logging": z.object({
          "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            disabled: true\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { disabled: true }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(false),
          "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 30 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(90),
          "logForwarding": z.union([z.object({
              "type": z.literal("http-endpoint"),
              "properties": z.object({
                "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("highlight"),
              "properties": z.object({
                "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("datadog"),
              "properties": z.object({
                "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
            }).strict()
          ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Logging configuration.\n\n---\n\nContainer output (`stdout`/`stderr`) is automatically sent to CloudWatch and retained for 90 days.\nView logs with `stacktape logs` or in the Stacktape Console.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      logging:\n        retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    logging: { retentionDays: 30 }\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
        "resources": z.object({
          "cpu": z.union([z.literal(0.25), z.literal(0.5), z.literal(1), z.literal(16), z.literal(2), z.literal(4), z.literal(8)]).optional().describe("#### vCPUs for the workload (Fargate). Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 2\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 2,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB. Must be compatible with the vCPU count on Fargate.\n\n---\n\nFargate valid combos: 0.25 vCPU → 512-2048 MB, 0.5 → 1024-4096, 1 → 2048-8192, 2 → 4096-16384,\n4 → 8192-30720, 8 → 16384-61440, 16 → 32768-122880.\nFor EC2: auto-detected from instance type if omitted.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 1,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "instanceTypes": z.array(z.string()).optional().describe("#### EC2 instance types for the workload (e.g., `t3.medium`, `c6g.large`). Use instead of `cpu`/`memory`.\n\n---\n\nFirst type in the list is preferred. Instances auto-scale and are refreshed weekly for patching.\nTip: specify a single type and omit `cpu`/`memory` for optimal sizing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - c6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['c6g.large']\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "enableWarmPool": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Keep pre-initialized EC2 instances ready for faster scaling. Only works with a single instance type.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - t3.medium\n        enableWarmPool: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['t3.medium'],\n      enableWarmPool: true\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "architecture": z.enum(["arm64","x86_64"]).optional().describe("#### CPU architecture for Fargate. `arm64` is ~20% cheaper. Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 0.5,\n      memory: 1024,\n      architecture: 'arm64'\n    }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
        .describe("#### CPU, memory, and compute engine for the container.\n\n---\n\nTwo compute engines:\n- **Fargate** (default): Serverless — just specify `cpu` and `memory`.\n- **EC2**: Specify `instanceTypes` for more control and potentially lower cost.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 2048\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 1, memory: 2048, architecture: 'arm64' }\n  });\n\n  return { resources: { api } };\n});\n```"),
        "scaling": z.object({
          "minInstances": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum running instances. Set to 0 is not supported — minimum is 1.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8\n    }\n  });\n  return { resources: { app } };\n});\n```").default(1),
          "maxInstances": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum running instances. Traffic is distributed across all instances.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8\n    }\n  });\n  return { resources: { app } };\n});\n```").default(1),
          "scalingPolicy": z.object({
            "keepAvgCpuUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Scale out when avg CPU exceeds this %, scale in when it drops below.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 65\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: {\n        keepAvgCpuUtilizationUnder: 65\n      }\n    }\n  });\n  return { resources: { app } };\n});\n```").default(80),
            "keepAvgMemoryUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Scale out when avg memory exceeds this %, scale in when it drops below.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgMemoryUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: {\n        keepAvgMemoryUtilizationUnder: 70\n      }\n    }\n  });\n  return { resources: { app } };\n});\n```").default(80) }).strict()
          .optional().describe("#### When to scale: CPU and/or memory utilization targets.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 60\n          keepAvgMemoryUtilizationUnder: 75\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: { keepAvgCpuUtilizationUnder: 60, keepAvgMemoryUtilizationUnder: 75 }\n    }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
        .optional().describe("#### Auto-scaling: add/remove container instances based on demand.\n\n---\n\nTraffic is automatically distributed across all running containers.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 10\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 10,\n      scalingPolicy: { keepAvgCpuUtilizationUnder: 70 }\n    }\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
        "internalHealthCheck": z.object({
          "healthCheckCommand": z.array(z.string()).describe("#### Command to check health. E.g., `[\"CMD-SHELL\", \"curl -f http://localhost/ || exit 1\"]`. Exit 0 = healthy.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1']\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "intervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks (5-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            intervalSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          intervalSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(30),
          "timeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a check is considered failed (2-60).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            timeoutSeconds: 10\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          timeoutSeconds: 10\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(5),
          "retries": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Consecutive failures before marking unhealthy (1-10).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            retries: 5\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          retries: 5\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(3),
          "startPeriodSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Grace period (seconds) before counting failures. Gives the container time to start (0-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            startPeriodSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          startPeriodSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
        .optional().describe("#### Health check that auto-replaces unhealthy containers.\n\n---\n\nIf a container fails the health check, it's terminated and replaced automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      internalHealthCheck:\n        healthCheckCommand:\n          - CMD-SHELL\n          - curl -f http://localhost:3000/health || exit 1\n        intervalSeconds: 30\n        retries: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    internalHealthCheck: {\n      healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/health || exit 1'],\n      intervalSeconds: 30,\n      retries: 3\n    }\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
        "stopTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds to wait for graceful shutdown before force-killing the container.\n\n---\n\nThe container receives `SIGTERM` first, then `SIGKILL` after this timeout. Must be 2-120.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      stopTimeout: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    stopTimeout: 30\n  });\n\n  return { resources: { api } };\n});\n```").default(2),
        "enableRemoteSessions": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Allow SSH-like access to running containers for debugging.\n\n---\n\nEnables `stacktape container:session` to open an interactive shell inside the container.\nAdds a small SSM agent that uses minimal CPU/memory.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      enableRemoteSessions: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    enableRemoteSessions: true\n  });\n\n  return { resources: { api } };\n});\n```").default(false),
        "volumeMounts": z.array(z.object({
          "type": z.literal("efs").describe("#### The type of the volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
          "properties": z.object({
            "efsFilesystemName": z.string().describe("#### Name of the `efs-filesystem` resource defined in your config.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "rootDirectory": z.string().optional().describe("#### Subdirectory within the EFS filesystem to mount. Restricts access to that directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                rootDirectory: /uploads\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              rootDirectory: '/uploads',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "mountPath": z.string().describe("#### Absolute path inside the container where the volume is mounted (e.g., `/data`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
          .describe("#### Properties for the EFS volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
        ).optional().describe("#### Persistent EFS volumes shared across containers and restarts.\n\n---\n\nData stored in EFS volumes persists even when containers are replaced.\nMultiple containers can mount the same volume. All data is encrypted in transit.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      volumeMounts:\n        - type: efs\n          properties:\n            efsFilesystemName: sharedData\n            mountPath: /data\n  sharedData:\n    type: efs-filesystem\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, EfsFilesystem, ContainerEfsMount, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedData = new EfsFilesystem({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    volumeMounts: [\n      new ContainerEfsMount({ type: 'efs', properties: { efsFilesystemName: 'sharedData', mountPath: '/data' } })\n    ]\n  });\n\n  return { resources: { api, sharedData } };\n});\n```").optional(),
        "sideContainers": z.array(z.object({
          "containerType": z.enum(["always-running","run-on-init"]).describe("#### When and how this sidecar container runs.\n\n---\n\n- **`run-on-init`**: Must exit with code 0 before the main container starts. Use for migrations or setup.\n- **`always-running`**: Runs alongside the main container for its entire lifetime. If it crashes, the whole task fails.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      sideContainers:\n        - name: migrations\n          containerType: run-on-init\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/migrate.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    sideContainers: [\n      {\n        name: 'migrations',\n        containerType: 'run-on-init',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/migrate.ts' })\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```"),
          "name": z.string().describe("#### Unique container name within this workload.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' })\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "packaging": z.union([z.object({
              "type": z.literal("prebuilt-image"),
              "properties": z.object({
                "repositoryCredentialsSecretArn": z.string().optional().describe("#### The ARN of a secret containing credentials for a private container registry.\n\n---\n\nThe secret must be a JSON object with `username` and `password` keys.\nYou can create secrets using the `stacktape secret:create` command.\n\n**Example (YAML):**\n\n```yaml\nresources:\n privateImageService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: registry.example.com/my-org/my-app:latest\n         repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const privateImageService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'registry.example.com/my-org/my-app:latest',\n       repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { privateImageService } };\n});\n```").optional(),
                "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: node:22-alpine\n         entryPoint:\n           - /bin/sh\n           - -c\n           - node server.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'node:22-alpine',\n       entryPoint: ['/bin/sh', '-c', 'node server.js']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
                "image": z.string().describe("#### The name or URL of the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu'\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n           command:\n             - -i\n             - input.mp4\n             - output.webm\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu',\n         command: ['-i', 'input.mp4', 'output.webm']\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```").optional() }).strict()
              .describe("#### Configures a pre-built container image.") }).strict()
            .describe("#### Uses a pre-built container image.\n\n---\n\nWith `prebuilt-image`, you provide a reference to an existing container image.\nThis can be a public image from Docker Hub or a private image from any container registry.\n\nFor private registries, configure `repositoryCredentialsSecretArn` with credentials stored in AWS Secrets Manager."), z.object({
              "type": z.literal("custom-dockerfile"),
              "properties": z.object({
                "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: custom-dockerfile\n       properties:\n         buildContextPath: ./app\n         entryPoint:\n           - /app/entrypoint.sh\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'custom-dockerfile',\n     properties: {\n       buildContextPath: './app',\n       entryPoint: ['/app/entrypoint.sh']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
                "dockerfilePath": z.string().optional().describe("#### The path to the Dockerfile, relative to `buildContextPath`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           dockerfilePath: docker/Dockerfile.prod\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         dockerfilePath: 'docker/Dockerfile.prod'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "buildContextPath": z.string().describe("#### The path to the build context directory, relative to your Stacktape configuration file.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                "buildArgs": z.array(z.object({
                  "argName": z.string().describe("#### Argument name\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                  "value": z.string().describe("#### Argument value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```") }).strict()
                ).optional().describe("#### A list of arguments to pass to the `docker build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: NODE_ENV\n               value: production\n             - argName: BUILD_VERSION\n               value: \"1.4.2\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           { argName: 'NODE_ENV', value: 'production' },\n           { argName: 'BUILD_VERSION', value: '1.4.2' }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           command:\n             - node\n             - dist/process.js\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         command: ['node', 'dist/process.js']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built by Stacktape from a specified Dockerfile.") }).strict()
            .describe("#### Builds a container image from your own Dockerfile.\n\n---\n\nWith `custom-dockerfile`, you provide a path to your Dockerfile and build context.\nStacktape builds the image and uploads it to a managed ECR repository.\n\nThis gives you full control over the container environment and is ideal for complex setups."), z.object({
              "type": z.literal("external-buildpack"),
              "properties": z.object({
                "builder": z.string().optional().describe("#### The Buildpack Builder to use.\n\n---\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           builder: paketobuildpacks/builder-jammy-full\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         builder: 'paketobuildpacks/builder-jammy-full'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "buildpacks": z.array(z.string()).optional().describe("#### The specific Buildpack to use.\n\n---\n\nBy default, the buildpack is detected automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           buildpacks:\n             - paketo-buildpacks/nodejs\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         buildpacks: ['paketo-buildpacks/nodejs']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional(),
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           command:\n             - npm\n             - run\n             - import\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         command: ['npm', 'run', 'import']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Builds a container image using an external buildpack.\n\n---\n\nExternal buildpacks (buildpacks.io) automatically detect your application type\nand build an optimized container image with zero configuration.\n\nThe default builder is `paketobuildpacks/builder-jammy-base`.\nYou can find buildpacks for almost any language or framework."), z.object({
              "type": z.literal("nixpacks"),
              "properties": z.object({
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                "buildImage": z.string().optional().describe("#### The base image to use for building the application.\n\n---\n\nFor more details, see the [Nixpacks documentation](https://nixpacks.com/docs/configuration/file#build-image).\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         buildImage: ubuntu:22.04\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       buildImage: 'ubuntu:22.04'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "providers": z.array(z.string()).optional().describe("#### A list of providers to use for determining the build and runtime environments.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         providers:\n           - node\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       providers: ['node']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startCmd": z.string().optional().describe("#### The command to execute when starting the application.\n\n---\n\nThis overrides the default start command inferred by Nixpacks.\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startCmd: node dist/worker.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startCmd: 'node dist/worker.js'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startRunImage": z.string().optional().describe("#### The base image to use for running the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startRunImage: gcr.io/distroless/nodejs22-debian12\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startRunImage: 'gcr.io/distroless/nodejs22-debian12'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startOnlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in the runtime environment; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startOnlyIncludeFiles:\n           - dist\n           - node_modules\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startOnlyIncludeFiles: ['dist', 'node_modules']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "phases": z.array(z.object({
                  "name": z.string().describe("#### The name of the build phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                  "cmds": z.array(z.string()).optional().describe("#### A list of shell commands to execute in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n               - npm prune --omit=dev\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build', 'npm prune --omit=dev']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixPkgs": z.array(z.string()).optional().describe("#### A list of Nix packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - ffmpeg\n               - imagemagick\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['ffmpeg', 'imagemagick']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixLibs": z.array(z.string()).optional().describe("#### A list of Nix libraries to include in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixLibs:\n               - openssl\n               - zlib\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixLibs: ['openssl', 'zlib']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixOverlay": z.array(z.string()).optional().describe("#### A list of Nix overlay files to apply in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixOverlay:\n               - ./nix/overlay.nix\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixOverlay: ['./nix/overlay.nix']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixpkgsArchive": z.string().optional().describe("#### The Nixpkgs archive to use.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - nodejs_22\n             nixpkgsArchive: bf3287dac860542719b3849d6970d22f635f9da1\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['nodejs_22'],\n           nixpkgsArchive: 'bf3287dac860542719b3849d6970d22f635f9da1'\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "aptPkgs": z.array(z.string()).optional().describe("#### A list of APT packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             aptPkgs:\n               - libpq-dev\n               - poppler-utils\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           aptPkgs: ['libpq-dev', 'poppler-utils']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "cacheDirectories": z.array(z.string()).optional().describe("#### A list of directories to cache between builds to speed up subsequent builds.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n             cacheDirectories:\n               - node_modules/.cache\n               - /root/.npm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci'],\n           cacheDirectories: ['node_modules/.cache', '/root/.npm']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "onlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in this phase; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n             onlyIncludeFiles:\n               - src\n               - package.json\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build'],\n           onlyIncludeFiles: ['src', 'package.json']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
                ).optional().describe("#### The build phases for the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n           - name: build\n             cmds:\n               - npm run build\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         { name: 'install', cmds: ['npm ci'] },\n         { name: 'build', cmds: ['npm run build'] }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Builds a container image using Nixpacks.\n\n---\n\nNixpacks automatically detects your application type and builds an optimized container image.\nIn most cases, no configuration is required.\n\nIt supports a wide range of languages and frameworks out of the box."), z.object({
              "type": z.literal("stacktape-image-buildpack"),
              "properties": z.object({
                "languageSpecificConfig": z.union([z.object({
                    "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                    "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                    "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                  , z.object({
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                    "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                    "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                  , z.object({
                    "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                  , z.record(z.string(), z.any()), z.object({
                    "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                  , z.object({
                    "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                  , z.object({
                    "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                    "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
                ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { apiService } };\n});\n```").optional(),
                "requiresGlibcBinaries": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use glibc instead of musl (Alpine default). Enable if native dependencies require glibc.\n\n---\n\nResults in a larger image. Common packages needing this: `sharp`, `canvas`, `bcrypt`, `puppeteer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         requiresGlibcBinaries: true\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       requiresGlibcBinaries: true\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "customDockerBuildCommands": z.array(z.string()).optional().describe("#### A list of commands to be executed during the `docker build` process.\n\n---\n\nThese commands are executed using the `RUN` directive in the Dockerfile.\nThis is useful for installing additional system dependencies in your container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         customDockerBuildCommands:\n           - apt-get update && apt-get install -y poppler-utils\n           - fc-cache -f\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       customDockerBuildCommands: [\n         'apt-get update && apt-get install -y poppler-utils',\n         'fc-cache -f'\n       ]\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
                "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built automatically by Stacktape from your source code.") }).strict()
            .describe("#### A zero-config buildpack that creates a container image from your source code.\n\n---\n\nThe `stacktape-image-buildpack` automatically bundles your code and dependencies into an optimized container image.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, and Go.\n\nFor JS/TS, your code is bundled into a single file with source maps.\nThe resulting image is uploaded to a managed ECR repository.")]).describe("#### How to build or specify the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: custom-dockerfile\n            properties:\n              buildContextPath: ./api\n              dockerfilePath: Dockerfile\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, CustomDockerfilePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new CustomDockerfilePackaging({ buildContextPath: './api', dockerfilePath: 'Dockerfile' })\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "essential": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### If `true` (default), the entire workload restarts when this container fails.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n        - name: metrics-sidecar\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: prom/statsd-exporter:latest\n          essential: false\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, PrebuiltImagePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) },\n      {\n        name: 'metrics-sidecar',\n        packaging: new PrebuiltImagePackaging({ image: 'prom/statsd-exporter:latest' }),\n        essential: false\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            disabled: true\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { disabled: true }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 30 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Container logging (stdout/stderr). Sent to CloudWatch, viewable with `stacktape logs`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 14\n            disabled: false\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 14, disabled: false }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "dependsOn": z.array(z.object({
            "containerName": z.string().describe("The name of the container that this container depends on."),
            "condition": z.enum(["COMPLETE","HEALTHY","START","SUCCESS"]).describe("#### The condition that the dependency container must meet.\n---\nAvailable conditions:\n- `START`: The dependency has started.\n- `COMPLETE`: The dependency has finished executing (regardless of success).\n- `SUCCESS`: The dependency has finished with an exit code of `0`.\n- `HEALTHY`: The dependency has passed its first health check.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: redis\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: redis:7\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - redis-cli ping || exit 1\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          dependsOn:\n            - containerName: redis\n              condition: HEALTHY\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, PrebuiltImagePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'redis',\n        packaging: new PrebuiltImagePackaging({ image: 'redis:7' }),\n        internalHealthCheck: { healthCheckCommand: ['CMD-SHELL', 'redis-cli ping || exit 1'] }\n      },\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        dependsOn: [\n          {\n            containerName: 'redis',\n            condition: 'HEALTHY'\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
          ).optional().describe("#### Start this container only after the listed containers reach a specific state.\n\n---\n\nE.g., wait for a database sidecar to be `HEALTHY` before starting the app container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: migrations\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/migrate.ts\n          essential: false\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          dependsOn:\n            - containerName: migrations\n              condition: SUCCESS\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'migrations', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/migrate.ts' }), essential: false },\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        dependsOn: [{ containerName: 'migrations', condition: 'SUCCESS' }]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "environment": z.array(z.object({
            "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
            "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
          ).optional().describe("#### Environment variables injected at runtime. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          environment:\n            - name: NODE_ENV\n              value: production\n            - name: DATABASE_URL\n              value: $ResourceParam('appDb', 'connectionString')\n            - name: STRIPE_KEY\n              value: $Secret('stripe.secretKey')\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - appDb\n  appDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t3.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, RelationalDatabase, StacktapeImageBuildpackPackaging, $ResourceParam, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db.password') },\n    engine: { type: 'postgres', properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t3.micro' } } }\n  });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        environment: {\n          NODE_ENV: 'production',\n          DATABASE_URL: $ResourceParam('appDb', 'connectionString'),\n          STRIPE_KEY: $Secret('stripe.secretKey')\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [appDb]\n  });\n  return { resources: { app, appDb } };\n});\n```").optional(),
          "internalHealthCheck": z.object({
            "healthCheckCommand": z.array(z.string()).describe("#### Command to check health. E.g., `[\"CMD-SHELL\", \"curl -f http://localhost/ || exit 1\"]`. Exit 0 = healthy.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1']\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
            "intervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks (5-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            intervalSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          intervalSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(30),
            "timeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a check is considered failed (2-60).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            timeoutSeconds: 10\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          timeoutSeconds: 10\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(5),
            "retries": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Consecutive failures before marking unhealthy (1-10).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            retries: 5\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          retries: 5\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(3),
            "startPeriodSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Grace period (seconds) before counting failures. Gives the container time to start (0-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            startPeriodSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          startPeriodSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
          .optional().describe("#### Command-based health check. If it fails on an essential container, the workload instance is replaced.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/health || exit 1\n            intervalSeconds: 30\n            retries: 3\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/health || exit 1'],\n          intervalSeconds: 30,\n          retries: 3\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "stopTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds to wait after SIGTERM before SIGKILL (2-120).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          stopTimeout: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        stopTimeout: 30\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(2),
          "volumeMounts": z.array(z.object({
            "type": z.literal("efs").describe("#### The type of the volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "properties": z.object({
              "efsFilesystemName": z.string().describe("#### Name of the `efs-filesystem` resource defined in your config.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
              "rootDirectory": z.string().optional().describe("#### Subdirectory within the EFS filesystem to mount. Restricts access to that directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                rootDirectory: /uploads\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              rootDirectory: '/uploads',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
              "mountPath": z.string().describe("#### Absolute path inside the container where the volume is mounted (e.g., `/data`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
            .describe("#### Properties for the EFS volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
          ).optional().describe("#### Mount EFS volumes for persistent, shared storage across containers.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          { type: 'efs', properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Helper containers that run alongside the main container.\n\n---\n\n- **`run-on-init`**: Runs to completion before the main container starts (e.g., database migrations).\n- **`always-running`**: Runs for the entire lifecycle (e.g., log forwarders, monitoring agents).\n  Can reach the main container via `localhost`.").optional(),
        "usePrivateSubnetsWithNAT": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Deploy in private subnets with a static outbound IP via NAT Gateway.\n\n---\n\nThe container won't have a public IP. All outbound traffic routes through a NAT Gateway,\ngiving you a static IP you can whitelist in external services (APIs, payment gateways, etc.).\n\nConfigure the number of NAT Gateways in `stackConfig.vpc.nat`.\n\n**Adds cost:** NAT Gateway ~$32/month + data processing fees.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      usePrivateSubnetsWithNAT: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    usePrivateSubnetsWithNAT: true\n  });\n\n  return { resources: { api } };\n});\n```").default(false),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Always-on container with a private endpoint, reachable only from other resources in your stack.\n\n---\n\nUse for internal APIs, microservices, or gRPC servers that shouldn't be publicly accessible.\nOther containers in the same stack can reach it by name (e.g., `http://myService:3000`)."), z.object({
      "type": z.literal("redis-cluster"),
      "properties": z.object({
        "enableSharding": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Split data across multiple shards for horizontal scaling.\n\n---\n\nEach shard has its own primary + replicas. Routing is automatic.\n\n> **Must be set at creation time** — can't be added later.\n> Requires `numReplicaNodes >= 1`. Replica count can't be changed after creation.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.m7g.large\n      enableSharding: true\n      numShards: 3\n      numReplicaNodes: 1\n\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      joinDefaultVpc: true\n      connectTo:\n        - redis\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.m7g.large',\n    enableSharding: true,\n    numShards: 3,\n    numReplicaNodes: 1\n  });\n\n  const worker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: './src/worker.ts' }\n    },\n    joinDefaultVpc: true,\n    connectTo: [redis]\n  });\n\n  return { resources: { redis, worker } };\n});\n```").optional(),
        "numShards": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Number of shards (only with `enableSharding: true`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.m7g.large\n      enableSharding: true\n      numShards: 4\n      numReplicaNodes: 1\n\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      joinDefaultVpc: true\n      connectTo:\n        - redis\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.m7g.large',\n    enableSharding: true,\n    numShards: 4,\n    numReplicaNodes: 1\n  });\n\n  const worker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: './src/worker.ts' }\n    },\n    joinDefaultVpc: true,\n    connectTo: [redis]\n  });\n\n  return { resources: { redis, worker } };\n});\n```").default(1),
        "numReplicaNodes": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Read replicas per shard. Increases read throughput and availability.\n\n---\n\nIf the primary fails and `enableAutomaticFailover` is on, a replica takes over.\nCan't be changed after creation for sharded clusters.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      numReplicaNodes: 2\n      enableAutomaticFailover: true\n\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      joinDefaultVpc: true\n      connectTo:\n        - redis\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    numReplicaNodes: 2,\n    enableAutomaticFailover: true\n  });\n\n  const worker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: './src/worker.ts' }\n    },\n    joinDefaultVpc: true,\n    connectTo: [redis]\n  });\n\n  return { resources: { redis, worker } };\n});\n```").default(0),
        "enableAutomaticFailover": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Auto-promote a replica to primary if the primary node fails.\n\n---\n\nRequires `numReplicaNodes >= 1`. Always enabled for sharded clusters.\n\n> Deploy replicas first, then enable failover in a separate deployment.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      numReplicaNodes: 1\n      enableAutomaticFailover: true\n\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      joinDefaultVpc: true\n      connectTo:\n        - redis\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    numReplicaNodes: 1,\n    enableAutomaticFailover: true\n  });\n\n  const worker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: './src/worker.ts' }\n    },\n    joinDefaultVpc: true,\n    connectTo: [redis]\n  });\n\n  return { resources: { redis, worker } };\n});\n```").optional(),
        "instanceSize": z.enum(["cache.m4.10xlarge","cache.m4.2xlarge","cache.m4.4xlarge","cache.m4.large","cache.m4.xlarge","cache.m5.12xlarge","cache.m5.24xlarge","cache.m5.2xlarge","cache.m5.4xlarge","cache.m5.large","cache.m5.xlarge","cache.m6g.12xlarge","cache.m6g.16xlarge","cache.m6g.2xlarge","cache.m6g.4xlarge","cache.m6g.8xlarge","cache.m6g.large","cache.m6g.xlarge","cache.m7g.12xlarge","cache.m7g.16xlarge","cache.m7g.2xlarge","cache.m7g.4xlarge","cache.m7g.8xlarge","cache.m7g.large","cache.m7g.xlarge","cache.r4.16xlarge","cache.r4.2xlarge","cache.r4.4xlarge","cache.r4.8xlarge","cache.r4.large","cache.r4.xlarge","cache.r5.12xlarge","cache.r5.24xlarge","cache.r5.2xlarge","cache.r5.4xlarge","cache.r5.large","cache.r5.xlarge","cache.r6g.12xlarge","cache.r6g.16xlarge","cache.r6g.2xlarge","cache.r6g.4xlarge","cache.r6g.8xlarge","cache.r6g.large","cache.r6g.xlarge","cache.r7g.12xlarge","cache.r7g.16xlarge","cache.r7g.2xlarge","cache.r7g.4xlarge","cache.r7g.8xlarge","cache.r7g.large","cache.r7g.xlarge","cache.t2.medium","cache.t2.micro","cache.t2.small","cache.t3.medium","cache.t3.micro","cache.t3.small","cache.t4g.medium","cache.t4g.micro","cache.t4g.small"]).describe("#### The size of each Redis node. Affects memory, performance, and cost.\n\n---\n\n**Quick guide:**\n- **`cache.t4g.micro`** (~$0.016/hr, 0.5 GB): Development, testing, low-traffic apps.\n- **`cache.t4g.small`** (~$0.032/hr, 1.37 GB): Small production apps, session stores.\n- **`cache.m7g.large`** (~$0.15/hr, 6.38 GB): Production workloads with moderate data.\n- **`cache.r7g.large`** (~$0.20/hr, 13.07 GB): Large datasets, memory-heavy caching.\n\n**Families:** `t` = burstable (cheap, variable). `m` = general purpose. `r` = memory-optimized.\nSuffix `g` = ARM/Graviton (better price-performance).\n\nThis size applies to every node (primary + replicas). You can change it later without data loss.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.r7g.large\n      engineVersion: '7.1'\n\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      joinDefaultVpc: true\n      connectTo:\n        - redis\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.r7g.large',\n    engineVersion: '7.1'\n  });\n\n  const worker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: './src/worker.ts' }\n    },\n    joinDefaultVpc: true,\n    connectTo: [redis]\n  });\n\n  return { resources: { redis, worker } };\n});\n```"),
        "logging": z.object({
          "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable slow query logging.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    logging: {\n      disabled: true\n    }\n  });\n\n  return { resources: { redis } };\n});\n```").default(false),
          "format": z.enum(["json","text"]).optional().describe("#### Log format.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      logging:\n        format: text\n        retentionDays: 90\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    logging: {\n      format: 'text',\n      retentionDays: 90\n    }\n  });\n\n  return { resources: { redis } };\n});\n```"),
          "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      logging:\n        format: json\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    logging: {\n      format: 'json',\n      retentionDays: 365\n    }\n  });\n\n  return { resources: { redis } };\n});\n```").default(30),
          "logForwarding": z.union([z.object({
              "type": z.literal("http-endpoint"),
              "properties": z.object({
                "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("highlight"),
              "properties": z.object({
                "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("datadog"),
              "properties": z.object({
                "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
            }).strict()
          ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Slow query logging. Sent to CloudWatch; view with `stacktape logs`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      logging:\n        disabled: false\n        format: json\n        retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    logging: {\n      disabled: false,\n      format: 'json',\n      retentionDays: 30\n    }\n  });\n\n  return { resources: { redis } };\n});\n```").optional(),
        "automatedBackupRetentionDays": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Days to keep automated daily backups. Set to 0 to disable.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.m7g.large\n      automatedBackupRetentionDays: 7\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.m7g.large',\n    automatedBackupRetentionDays: 7\n  });\n\n  return { resources: { redis } };\n});\n```").default(0),
        "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port the cluster listens on.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      port: 6380\n\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      joinDefaultVpc: true\n      connectTo:\n        - redis\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    port: 6380\n  });\n\n  const worker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: './src/worker.ts' }\n    },\n    joinDefaultVpc: true,\n    connectTo: [redis]\n  });\n\n  return { resources: { redis, worker } };\n});\n```").default(6379),
        "defaultUserPassword": z.string().describe("#### Cluster password. 16-128 chars, printable ASCII only. Cannot contain `/`, `\"`, or `@`.\n\n---\n\nAll traffic is encrypted in transit. Use `$Secret()` instead of hardcoding:\n```yml\ndefaultUserPassword: $Secret('redis.password')\n```\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small'\n  });\n\n  return { resources: { redis } };\n});\n```"),
        "accessibility": z.object({
          "accessibilityMode": z.enum(["scoping-workloads-in-vpc","vpc"]).describe("#### Who can connect to this cluster.\n\n---\n\n- **`vpc`** (default): Any resource in the same VPC (functions with `joinDefaultVpc: true`, containers, batch jobs).\n- **`scoping-workloads-in-vpc`**: Only resources that list this cluster in their `connectTo`.\n\nRedis clusters don't have public IPs — you can't connect from your local machine directly.\nUse a bastion host for local access.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      accessibility:\n        accessibilityMode: vpc\n\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      joinDefaultVpc: true\n      connectTo:\n        - redis\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    accessibility: {\n      accessibilityMode: 'vpc'\n    }\n  });\n\n  const worker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: './src/worker.ts' }\n    },\n    joinDefaultVpc: true,\n    connectTo: [redis]\n  });\n\n  return { resources: { redis, worker } };\n});\n```").default("vpc") }).strict()
        .optional().describe("#### Network access control: `vpc` (default) or `scoping-workloads-in-vpc` (most restrictive).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      joinDefaultVpc: true\n      connectTo:\n        - redis\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    accessibility: {\n      accessibilityMode: 'scoping-workloads-in-vpc'\n    }\n  });\n\n  const worker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: './src/worker.ts' }\n    },\n    joinDefaultVpc: true,\n    connectTo: [redis]\n  });\n\n  return { resources: { redis, worker } };\n});\n```").optional(),
        "engineVersion": z.enum(["6.0","6.2","7.0","7.1"]).optional().describe("#### Redis engine version.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      engineVersion: '7.1'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    engineVersion: '7.1'\n  });\n\n  return { resources: { redis } };\n});\n```").default("6.2"),
        "dev": z.object({
          "remote": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use the deployed AWS resource instead of a local emulation.\n\n---\n\nBy default, databases, Redis, and DynamoDB run locally in Docker during dev mode.\nSet to `true` to connect to the real deployed resource instead (must be deployed first).\n\nUseful when local emulation doesn't match production behavior closely enough,\nor when you need to work with real data.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  myApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      connectTo:\n        - sessionsTable\n  sessionsTable:\n    type: dynamo-db-table\n    properties:\n      primaryKey:\n        partitionKey:\n          name: id\n          type: string\n      dev:\n        remote: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, DynamoDbTable, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sessionsTable = new DynamoDbTable({\n    primaryKey: { partitionKey: { name: 'id', type: 'string' } },\n    dev: { remote: true }\n  });\n\n  const myApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    connectTo: [sessionsTable]\n  });\n\n  return { resources: { myApi, sessionsTable } };\n});\n```").default(false) }).strict()
        .optional().describe("#### Dev mode: runs locally in Docker by default. Set `remote: true` to use the deployed cluster.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  redis:\n    type: redis-cluster\n    properties:\n      defaultUserPassword: $Secret('redis.password')\n      instanceSize: cache.t4g.small\n      dev:\n        remote: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RedisCluster, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const redis = new RedisCluster({\n    defaultUserPassword: $Secret('redis.password'),\n    instanceSize: 'cache.t4g.small',\n    dev: {\n      remote: true\n    }\n  });\n\n  return { resources: { redis } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### In-memory data store for caching, sessions, queues, and real-time data. Sub-millisecond latency."), z.object({
      "type": z.literal("remix-web"),
      "properties": z.object({
        "appDirectory": z.string().optional().describe("#### Directory containing your `vite.config.ts` (or `remix.config.js`). For monorepos, point to the Remix workspace.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: remix-web\n    properties:\n      appDirectory: ./apps/storefront\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RemixWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new RemixWeb({\n    appDirectory: './apps/storefront',\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").default("."),
        "buildCommand": z.string().optional().describe("#### Override the default `remix vite:build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: remix-web\n    properties:\n      buildCommand: npm run build:remix\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RemixWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new RemixWeb({\n    buildCommand: 'npm run build:remix',\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables for the SSR function. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t3.micro\n  web:\n    type: remix-web\n    properties:\n      connectTo:\n        - database\n      environment:\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n        - name: SESSION_SECRET\n          value: $Secret('session-secret')\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RemixWeb, defineConfig, $ResourceParam, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: {\n      type: 'postgres',\n      properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t3.micro' } }\n    }\n  });\n  const web = new RemixWeb({\n    connectTo: ['database'],\n    environment: [\n      { name: 'DATABASE_URL', value: $ResourceParam('database', 'connectionString') },\n      { name: 'SESSION_SECRET', value: $Secret('session-secret') },\n      { name: 'NODE_ENV', value: 'production' }\n    ]\n  });\n  return { resources: { database, web } };\n});\n```").optional(),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Attach custom domains with auto-managed DNS records and TLS certificates.\n\n---\n\n**Prerequisite:** A Route 53 hosted zone for your domain must exist in your AWS account.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: remix-web\n    properties:\n      customDomains:\n        - domainName: app.example.com\n        - domainName: www.example.com\n          disableDnsRecordCreation: true\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RemixWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new RemixWeb({\n    customDomains: [\n      { domainName: 'app.example.com' },\n      { domainName: 'www.example.com', disableDnsRecordCreation: true }\n    ],\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "serverLambda": z.object({
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128–10,240). CPU scales proportionally — 1,769 MB = 1 vCPU.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      environment:\n        - name: NODE_ENV\n          value: production\n      serverLambda:\n        memory: 2048\n        timeout: 20\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    environment: { NODE_ENV: 'production' },\n    serverLambda: {\n      memory: 2048,\n      timeout: 20\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(1024),
          "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Max: 30.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        timeout: 25\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      timeout: 25\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(30),
          "joinDefaultVpc": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Connect to VPC resources (databases, Redis). **Warning:** function loses direct internet access.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('database.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      connectTo:\n        - database\n      environment:\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n      serverLambda:\n        memory: 1024\n        joinDefaultVpc: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, RelationalDatabase, $Secret, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('database.password') },\n    engine: {\n      type: 'postgres',\n      properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t4g.micro' } }\n    },\n    accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' }\n  });\n\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    connectTo: [database],\n    environment: { DATABASE_URL: $ResourceParam('database', 'connectionString') },\n    serverLambda: {\n      memory: 1024,\n      joinDefaultVpc: true\n    }\n  });\n\n  return { resources: { database, webApp } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highVolumeFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highVolumeFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { highVolumeFunction } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs. Longer retention = higher storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/audit.ts' } },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditFunction } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Logging config. Logs are sent to CloudWatch. View with `stacktape logs` or in the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        logging:\n          retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      logging: {\n        retentionDays: 30\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Customize the SSR Lambda function (memory, timeout, VPC, logging).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: remix-web\n    properties:\n      serverLambda:\n        memory: 2048\n        timeout: 25\n        joinDefaultVpc: true\n        logging:\n          retentionDays: 90\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RemixWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new RemixWeb({\n    serverLambda: {\n      memory: 2048,\n      timeout: 25,\n      joinDefaultVpc: true,\n      logging: { retentionDays: 90 }\n    },\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this app. Firewall `scope` must be `cdn`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  firewall:\n    type: web-app-firewall\n    properties:\n      scope: cdn\n  web:\n    type: remix-web\n    properties:\n      useFirewall: firewall\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, RemixWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const firewall = new WebAppFirewall({ scope: 'cdn' });\n  const web = new RemixWeb({\n    useFirewall: 'firewall',\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { firewall, web } };\n});\n```").optional(),
        "dev": z.object({
          "command": z.string().optional().describe("#### Override the default dev server command (e.g., `npm run dev`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      dev:\n        command: npm run dev\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    dev: {\n      command: 'npm run dev'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "workingDirectory": z.string().optional().describe("#### Working directory for the dev command, relative to project root.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: packages/web\n      dev:\n        command: npm run dev\n        workingDirectory: packages/web\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: 'packages/web',\n    dev: {\n      command: 'npm run dev',\n      workingDirectory: 'packages/web'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Dev server config for `stacktape dev`. Defaults to `remix vite:dev`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: remix-web\n    properties:\n      dev:\n        command: npm run dev\n        workingDirectory: ./apps/storefront\n      appDirectory: ./apps/storefront\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RemixWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new RemixWeb({\n    dev: {\n      command: 'npm run dev',\n      workingDirectory: './apps/storefront'\n    },\n    appDirectory: './apps/storefront',\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "fileOptions": z.array(z.object({
          "includePattern": z.string().describe("#### Glob pattern for files this rule applies to (e.g., `*.html`, `assets/**`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
          "excludePattern": z.string().optional().describe("#### Glob pattern for files to exclude even if they match `includePattern`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            excludePattern: 'assets/uncompressed/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          excludePattern: 'assets/uncompressed/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "headers": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### HTTP headers (e.g., `Cache-Control`) for matching files. Forwarded through CDN to the browser.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "tags": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### Tags for matching files. Can be used to target files with `lifecycleRules`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'reports/**'\n            tags:\n              - key: category\n                value: report\n      lifecycleRules:\n        - type: expiration\n          properties:\n            tags:\n              - key: category\n                value: report\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'reports/**',\n          tags: [{ key: 'category', value: 'report' }]\n        }\n      ]\n    },\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          tags: [{ key: 'category', value: 'report' }],\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Set custom headers (e.g., `Cache-Control`) for static files matching a pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: remix-web\n    properties:\n      fileOptions:\n        - includePattern: 'assets/**'\n          headers:\n            - key: Cache-Control\n              value: 'public, max-age=31536000, immutable'\n        - includePattern: '*.html'\n          headers:\n            - key: Cache-Control\n              value: no-cache\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RemixWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new RemixWeb({\n    fileOptions: [\n      {\n        includePattern: 'assets/**',\n        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n      },\n      {\n        includePattern: '*.html',\n        headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n      }\n    ],\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "cdn": z.object({
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      disableInvalidationAfterDeploy: true\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(false),
          "defaultCachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Override default SSR caching behavior for all routes handled by the server.\n\n---\n\nUseful when you want to cache SSR responses longer than the framework defaults.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n          cacheKeyParameters:\n            queryString:\n              all: true\n            cookies:\n              none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 3600,\n        cacheKeyParameters: {\n          queryString: { all: true },\n          cookies: { none: true }\n        }\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "pathCachingOverrides": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `/_server-islands/*`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          -\n            path: /_nuxt/*\n            cachingOptions:\n              minTTL: 31536000\n              defaultTTL: 31536000\n              maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_nuxt/*',\n          cachingOptions: { minTTL: 31536000, defaultTTL: 31536000, maxTTL: 31536000 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```"),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .describe("#### Caching behavior override for this path pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n              minTTL: 0\n              defaultTTL: 15\n              maxTTL: 300\n              cacheKeyParameters:\n                queryString:\n                  whitelist:\n                    - page\n                    - limit\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/api/*',\n          cachingOptions: {\n            cacheMethods: ['GET', 'HEAD'],\n            minTTL: 0,\n            defaultTTL: 15,\n            maxTTL: 300,\n            cacheKeyParameters: {\n              queryString: { whitelist: ['page', 'limit'] }\n            }\n          }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```") }).strict()
          ).optional().describe("#### Override caching for specific CDN path patterns.\n\n---\n\nMatches existing framework-managed paths (e.g. `_astro/*`, `_next/data/*`) or adds\nnew server-path caching rules (e.g. `/_server-islands/*`) while preserving managed routing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              minTTL: 0\n              defaultTTL: 0\n              maxTTL: 0\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n                - OPTIONS\n              defaultTTL: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_server-islands/*',\n          cachingOptions: { minTTL: 0, defaultTTL: 0, maxTTL: 0 }\n        },\n        {\n          path: '/api/*',\n          cachingOptions: { cacheMethods: ['GET', 'HEAD', 'OPTIONS'], defaultTTL: 30 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### CDN cache controls for SSR routes and specific path patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: remix-web\n    properties:\n      cdn:\n        disableInvalidationAfterDeploy: false\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n        pathCachingOverrides:\n          - path: '/_server-islands/*'\n            cachingOptions:\n              defaultTTL: 300\n              cacheKeyParameters:\n                queryString:\n                  all: true\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RemixWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new RemixWeb({\n    cdn: {\n      disableInvalidationAfterDeploy: false,\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 3600\n      },\n      pathCachingOverrides: [\n        {\n          path: '/_server-islands/*',\n          cachingOptions: {\n            defaultTTL: 300,\n            cacheKeyParameters: { queryString: { all: true } }\n          }\n        }\n      ]\n    },\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Deploy a Remix SSR app with Lambda for server rendering, S3 for static assets, and CloudFront CDN."), z.object({
      "type": z.literal("sns-topic"),
      "properties": z.object({
        "smsDisplayName": z.string().optional().describe("#### Sender name shown on SMS messages sent to subscribers (e.g., \"MyApp\"). Max 11 characters.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEventsTopic:\n    type: sns-topic\n    properties:\n      smsDisplayName: ShopAlerts\n  orderNotifier:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/notify.ts\n      events:\n        - type: sns\n          properties:\n            snsTopicName: orderEventsTopic\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SnsTopic, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEventsTopic = new SnsTopic({\n    smsDisplayName: 'ShopAlerts'\n  });\n\n  const orderNotifier = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/notify.ts' }\n    },\n    events: [{ type: 'sns', properties: { snsTopicName: 'orderEventsTopic' } }]\n  });\n\n  return { resources: { orderEventsTopic, orderNotifier } };\n});\n```").optional(),
        "fifoEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Guarantees message order and exactly-once delivery. Use for financial transactions, sequential workflows.\n\n---\n\nFIFO topics can only deliver to FIFO SQS queues (not email, SMS, or HTTP).\nRequires either `contentBasedDeduplication: true` or a unique `MessageDeduplicationId` per message.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  transactionsTopic:\n    type: sns-topic\n    properties:\n      fifoEnabled: true\n      contentBasedDeduplication: true\n  paymentsProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-payment.ts\n      events:\n        - type: sns\n          properties:\n            snsTopicName: transactionsTopic\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SnsTopic, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const transactionsTopic = new SnsTopic({\n    fifoEnabled: true,\n    contentBasedDeduplication: true\n  });\n\n  const paymentsProcessor = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/process-payment.ts' }\n    },\n    events: [{ type: 'sns', properties: { snsTopicName: 'transactionsTopic' } }]\n  });\n\n  return { resources: { transactionsTopic, paymentsProcessor } };\n});\n```").default(false),
        "contentBasedDeduplication": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Automatically deduplicates messages based on content (SHA-256 hash) within a 5-minute window.\n\n---\n\nSaves you from generating a unique deduplication ID for each message. Requires `fifoEnabled: true`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditTopic:\n    type: sns-topic\n    properties:\n      fifoEnabled: true\n      contentBasedDeduplication: true\n  auditLogger:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      events:\n        - type: sns\n          properties:\n            snsTopicName: auditTopic\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SnsTopic, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditTopic = new SnsTopic({\n    fifoEnabled: true,\n    contentBasedDeduplication: true\n  });\n\n  const auditLogger = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/audit.ts' }\n    },\n    events: [{ type: 'sns', properties: { snsTopicName: 'auditTopic' } }]\n  });\n\n  return { resources: { auditTopic, auditLogger } };\n});\n```").default(false) }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Pub/sub messaging: publish once, deliver to many subscribers (Lambda, SQS, email, SMS, HTTP).\n\n---\n\nServerless, pay-per-message. Use when one event needs to trigger multiple actions — e.g., order placed\ntriggers email confirmation + inventory update + analytics. Subscribers are configured on the subscriber side."), z.object({
      "type": z.literal("solidstart-web"),
      "properties": z.object({
        "appDirectory": z.string().optional().describe("#### Directory containing your `app.config.ts`. For monorepos, point to the SolidStart workspace.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: solidstart-web\n    properties:\n      appDirectory: ./packages/web\n      environment:\n        - name: PUBLIC_API_URL\n          value: https://api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SolidStartWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SolidStartWeb({\n    appDirectory: './packages/web',\n    environment: [{ name: 'PUBLIC_API_URL', value: 'https://api.example.com' }]\n  });\n  return { resources: { web } };\n});\n```"),
        "buildCommand": z.string().optional().describe("#### Override the default `vinxi build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: solidstart-web\n    properties:\n      buildCommand: pnpm run build\n      environment:\n        - name: PUBLIC_STAGE\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SolidStartWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SolidStartWeb({\n    buildCommand: 'pnpm run build',\n    environment: [{ name: 'PUBLIC_STAGE', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables for the SSR function. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      credentials:\n        masterUserPassword: $Secret('db-password')\n  web:\n    type: solidstart-web\n    properties:\n      environment:\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n        - name: SESSION_SECRET\n          value: $Secret('session-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SolidStartWeb, RelationalDatabase, defineConfig, $ResourceParam, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    engine: { type: 'postgres', properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t3.micro' } } },\n    credentials: { masterUserPassword: $Secret('db-password') }\n  });\n  const web = new SolidStartWeb({\n    environment: [\n      { name: 'DATABASE_URL', value: $ResourceParam('database', 'connectionString') },\n      { name: 'SESSION_SECRET', value: $Secret('session-secret') }\n    ]\n  });\n  return { resources: { database, web } };\n});\n```").optional(),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Attach custom domains with auto-managed DNS records and TLS certificates.\n\n---\n\n**Prerequisite:** A Route 53 hosted zone for your domain must exist in your AWS account.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: solidstart-web\n    properties:\n      customDomains:\n        - domainName: app.example.com\n      environment:\n        - name: PUBLIC_API_URL\n          value: https://api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SolidStartWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SolidStartWeb({\n    customDomains: [{ domainName: 'app.example.com' }],\n    environment: [{ name: 'PUBLIC_API_URL', value: 'https://api.example.com' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "serverLambda": z.object({
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128–10,240). CPU scales proportionally — 1,769 MB = 1 vCPU.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      environment:\n        - name: NODE_ENV\n          value: production\n      serverLambda:\n        memory: 2048\n        timeout: 20\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    environment: { NODE_ENV: 'production' },\n    serverLambda: {\n      memory: 2048,\n      timeout: 20\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(1024),
          "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Max: 30.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        timeout: 25\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      timeout: 25\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(30),
          "joinDefaultVpc": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Connect to VPC resources (databases, Redis). **Warning:** function loses direct internet access.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('database.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      connectTo:\n        - database\n      environment:\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n      serverLambda:\n        memory: 1024\n        joinDefaultVpc: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, RelationalDatabase, $Secret, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('database.password') },\n    engine: {\n      type: 'postgres',\n      properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t4g.micro' } }\n    },\n    accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' }\n  });\n\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    connectTo: [database],\n    environment: { DATABASE_URL: $ResourceParam('database', 'connectionString') },\n    serverLambda: {\n      memory: 1024,\n      joinDefaultVpc: true\n    }\n  });\n\n  return { resources: { database, webApp } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highVolumeFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highVolumeFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { highVolumeFunction } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs. Longer retention = higher storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/audit.ts' } },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditFunction } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Logging config. Logs are sent to CloudWatch. View with `stacktape logs` or in the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        logging:\n          retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      logging: {\n        retentionDays: 30\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Customize the SSR Lambda function (memory, timeout, VPC, logging).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: solidstart-web\n    properties:\n      serverLambda:\n        memory: 2048\n        timeout: 20\n        logging:\n          retentionDays: 30\n      environment:\n        - name: PUBLIC_API_URL\n          value: https://api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SolidStartWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SolidStartWeb({\n    serverLambda: {\n      memory: 2048,\n      timeout: 20,\n      logging: { retentionDays: 30 }\n    },\n    environment: [{ name: 'PUBLIC_API_URL', value: 'https://api.example.com' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this app. Firewall `scope` must be `cdn`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  firewall:\n    type: web-app-firewall\n    properties:\n      scope: cdn\n  web:\n    type: solidstart-web\n    properties:\n      useFirewall: firewall\n      customDomains:\n        - domainName: app.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SolidStartWeb, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const firewall = new WebAppFirewall({ scope: 'cdn' });\n  const web = new SolidStartWeb({\n    useFirewall: 'firewall',\n    customDomains: [{ domainName: 'app.example.com' }]\n  });\n  return { resources: { firewall, web } };\n});\n```").optional(),
        "dev": z.object({
          "command": z.string().optional().describe("#### Override the default dev server command (e.g., `npm run dev`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      dev:\n        command: npm run dev\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    dev: {\n      command: 'npm run dev'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "workingDirectory": z.string().optional().describe("#### Working directory for the dev command, relative to project root.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: packages/web\n      dev:\n        command: npm run dev\n        workingDirectory: packages/web\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: 'packages/web',\n    dev: {\n      command: 'npm run dev',\n      workingDirectory: 'packages/web'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Dev server config for `stacktape dev`. Defaults to `vinxi dev`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: solidstart-web\n    properties:\n      dev:\n        command: pnpm run dev\n        workingDirectory: ./packages/web\n      environment:\n        - name: PUBLIC_API_URL\n          value: https://api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SolidStartWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SolidStartWeb({\n    dev: {\n      command: 'pnpm run dev',\n      workingDirectory: './packages/web'\n    },\n    environment: [{ name: 'PUBLIC_API_URL', value: 'https://api.example.com' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "fileOptions": z.array(z.object({
          "includePattern": z.string().describe("#### Glob pattern for files this rule applies to (e.g., `*.html`, `assets/**`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
          "excludePattern": z.string().optional().describe("#### Glob pattern for files to exclude even if they match `includePattern`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            excludePattern: 'assets/uncompressed/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          excludePattern: 'assets/uncompressed/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "headers": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### HTTP headers (e.g., `Cache-Control`) for matching files. Forwarded through CDN to the browser.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "tags": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### Tags for matching files. Can be used to target files with `lifecycleRules`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'reports/**'\n            tags:\n              - key: category\n                value: report\n      lifecycleRules:\n        - type: expiration\n          properties:\n            tags:\n              - key: category\n                value: report\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'reports/**',\n          tags: [{ key: 'category', value: 'report' }]\n        }\n      ]\n    },\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          tags: [{ key: 'category', value: 'report' }],\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Set custom headers (e.g., `Cache-Control`) for static files matching a pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: solidstart-web\n    properties:\n      fileOptions:\n        - includePattern: '**/*.html'\n          headers:\n            - key: Cache-Control\n              value: no-cache\n        - includePattern: assets/**\n          headers:\n            - key: Cache-Control\n              value: public, max-age=31536000, immutable\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SolidStartWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SolidStartWeb({\n    fileOptions: [\n      { includePattern: '**/*.html', headers: [{ key: 'Cache-Control', value: 'no-cache' }] },\n      {\n        includePattern: 'assets/**',\n        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n      }\n    ]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "cdn": z.object({
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      disableInvalidationAfterDeploy: true\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(false),
          "defaultCachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Override default SSR caching behavior for all routes handled by the server.\n\n---\n\nUseful when you want to cache SSR responses longer than the framework defaults.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n          cacheKeyParameters:\n            queryString:\n              all: true\n            cookies:\n              none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 3600,\n        cacheKeyParameters: {\n          queryString: { all: true },\n          cookies: { none: true }\n        }\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "pathCachingOverrides": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `/_server-islands/*`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          -\n            path: /_nuxt/*\n            cachingOptions:\n              minTTL: 31536000\n              defaultTTL: 31536000\n              maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_nuxt/*',\n          cachingOptions: { minTTL: 31536000, defaultTTL: 31536000, maxTTL: 31536000 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```"),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .describe("#### Caching behavior override for this path pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n              minTTL: 0\n              defaultTTL: 15\n              maxTTL: 300\n              cacheKeyParameters:\n                queryString:\n                  whitelist:\n                    - page\n                    - limit\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/api/*',\n          cachingOptions: {\n            cacheMethods: ['GET', 'HEAD'],\n            minTTL: 0,\n            defaultTTL: 15,\n            maxTTL: 300,\n            cacheKeyParameters: {\n              queryString: { whitelist: ['page', 'limit'] }\n            }\n          }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```") }).strict()
          ).optional().describe("#### Override caching for specific CDN path patterns.\n\n---\n\nMatches existing framework-managed paths (e.g. `_astro/*`, `_next/data/*`) or adds\nnew server-path caching rules (e.g. `/_server-islands/*`) while preserving managed routing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              minTTL: 0\n              defaultTTL: 0\n              maxTTL: 0\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n                - OPTIONS\n              defaultTTL: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_server-islands/*',\n          cachingOptions: { minTTL: 0, defaultTTL: 0, maxTTL: 0 }\n        },\n        {\n          path: '/api/*',\n          cachingOptions: { cacheMethods: ['GET', 'HEAD', 'OPTIONS'], defaultTTL: 30 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### CDN cache controls for SSR routes and specific path patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: solidstart-web\n    properties:\n      cdn:\n        disableInvalidationAfterDeploy: false\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              defaultTTL: 0\n              maxTTL: 0\n      customDomains:\n        - domainName: app.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SolidStartWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SolidStartWeb({\n    cdn: {\n      disableInvalidationAfterDeploy: false,\n      defaultCachingOptions: { minTTL: 0, defaultTTL: 60, maxTTL: 3600 },\n      pathCachingOverrides: [\n        { path: '/_server-islands/*', cachingOptions: { defaultTTL: 0, maxTTL: 0 } }\n      ]\n    },\n    customDomains: [{ domainName: 'app.example.com' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Deploy a SolidStart SSR app with Lambda (AWS adapter), S3 for static assets, and CloudFront CDN."), z.object({
      "type": z.literal("sqs-queue"),
      "properties": z.object({
        "delayMessagesSecond": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Delay (in seconds) before new messages become visible to consumers. Range: 0–900.\n\n---\n\nUseful for introducing a buffer, e.g., waiting for related data to be available before processing.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  emailQueue:\n    type: sqs-queue\n    properties:\n      delayMessagesSecond: 30\n      visibilityTimeoutSeconds: 60\n  emailWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/email-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: emailQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const emailQueue = new SqsQueue({\n    delayMessagesSecond: 30,\n    visibilityTimeoutSeconds: 60\n  });\n\n  const emailWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/email-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'emailQueue' } }]\n  });\n\n  return { resources: { emailQueue, emailWorker } };\n});\n```").default(0),
        "maxMessageSizeBytes": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum message size in bytes. Range: 1,024 (1 KB) to 262,144 (256 KB).\n\n---\n\nMessages larger than this limit are rejected. For payloads over 256 KB, store the data in S3 and send the reference.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  uploadEventsQueue:\n    type: sqs-queue\n    properties:\n      maxMessageSizeBytes: 65536\n      messageRetentionPeriodSeconds: 604800\n  uploadProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/upload-processor.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: uploadEventsQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const uploadEventsQueue = new SqsQueue({\n    maxMessageSizeBytes: 65536,\n    messageRetentionPeriodSeconds: 604800\n  });\n\n  const uploadProcessor = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/upload-processor.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'uploadEventsQueue' } }]\n  });\n\n  return { resources: { uploadEventsQueue, uploadProcessor } };\n});\n```").default(262144),
        "messageRetentionPeriodSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How long unprocessed messages stay in the queue before being deleted. Range: 60s to 1,209,600s (14 days).\n\n---\n\nDefault is 4 days (345,600s). Increase if consumers might fall behind or be temporarily offline.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  reportJobsQueue:\n    type: sqs-queue\n    properties:\n      messageRetentionPeriodSeconds: 1209600\n      visibilityTimeoutSeconds: 300\n  reportWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/report-worker.ts\n      timeout: 240\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: reportJobsQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const reportJobsQueue = new SqsQueue({\n    messageRetentionPeriodSeconds: 1209600,\n    visibilityTimeoutSeconds: 300\n  });\n\n  const reportWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/report-worker.ts' }\n    },\n    timeout: 240,\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'reportJobsQueue' } }]\n  });\n\n  return { resources: { reportJobsQueue, reportWorker } };\n});\n```").default(345600),
        "longPollingSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds the queue waits for messages before returning an empty response. Range: 0–20.\n\n---\n\nSet to `1`–`20` to enable long polling, which reduces costs by making fewer API calls.\nWith short polling (`0`), the consumer gets an immediate (often empty) response and must poll again.\n\nRecommended: `20` for most workloads — it's the most cost-effective.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  imageJobsQueue:\n    type: sqs-queue\n    properties:\n      longPollingSeconds: 20\n      visibilityTimeoutSeconds: 120\n  imageWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/image-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: imageJobsQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const imageJobsQueue = new SqsQueue({\n    longPollingSeconds: 20,\n    visibilityTimeoutSeconds: 120\n  });\n\n  const imageWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/image-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'imageJobsQueue' } }]\n  });\n\n  return { resources: { imageJobsQueue, imageWorker } };\n});\n```").default(0),
        "visibilityTimeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How long (seconds) a message is hidden from other consumers after being received. Range: 0–43,200 (12 hours).\n\n---\n\nAfter a consumer picks up a message, it must delete it before this timeout expires — otherwise it becomes\nvisible again and can be processed by another consumer (duplicate processing).\n\nSet this higher than your expected processing time. If your tasks take 2 minutes, use at least 150 seconds.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  videoEncodeQueue:\n    type: sqs-queue\n    properties:\n      visibilityTimeoutSeconds: 900\n      longPollingSeconds: 20\n  videoEncoder:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/video-encoder.ts\n      timeout: 600\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: videoEncodeQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const videoEncodeQueue = new SqsQueue({\n    visibilityTimeoutSeconds: 900,\n    longPollingSeconds: 20\n  });\n\n  const videoEncoder = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/video-encoder.ts' }\n    },\n    timeout: 600,\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'videoEncodeQueue' } }]\n  });\n\n  return { resources: { videoEncodeQueue, videoEncoder } };\n});\n```").default(30),
        "fifoEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Creates a FIFO queue that guarantees message order and exactly-once delivery.\n\n---\n\nUse when processing order matters (e.g., financial transactions, sequential workflows).\nFIFO queues have lower throughput (~300 msg/s without batching, ~3,000 with) compared to standard queues.\n\nRequires either `contentBasedDeduplication: true` or a `MessageDeduplicationId` on each message.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  transactionsQueue:\n    type: sqs-queue\n    properties:\n      fifoEnabled: true\n      contentBasedDeduplication: true\n      visibilityTimeoutSeconds: 60\n  transactionProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/transaction-processor.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: transactionsQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const transactionsQueue = new SqsQueue({\n    fifoEnabled: true,\n    contentBasedDeduplication: true,\n    visibilityTimeoutSeconds: 60\n  });\n\n  const transactionProcessor = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/transaction-processor.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'transactionsQueue' } }]\n  });\n\n  return { resources: { transactionsQueue, transactionProcessor } };\n});\n```").default(false),
        "fifoHighThroughput": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Enables high-throughput mode for FIFO queues (up to ~70,000 msg/s per queue).\n\n---\n\nMessages are partitioned by `MessageGroupId` — order is guaranteed within each group but not across groups.\nRequires `fifoEnabled: true`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  clickstreamQueue:\n    type: sqs-queue\n    properties:\n      fifoEnabled: true\n      fifoHighThroughput: true\n      contentBasedDeduplication: true\n  clickstreamConsumer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/clickstream-consumer.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: clickstreamQueue\n            batchSize: 10\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const clickstreamQueue = new SqsQueue({\n    fifoEnabled: true,\n    fifoHighThroughput: true,\n    contentBasedDeduplication: true\n  });\n\n  const clickstreamConsumer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/clickstream-consumer.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'clickstreamQueue', batchSize: 10 } }]\n  });\n\n  return { resources: { clickstreamQueue, clickstreamConsumer } };\n});\n```").optional(),
        "contentBasedDeduplication": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Automatically deduplicates messages based on their content (SHA-256 hash of the body).\n\n---\n\nWithin the 5-minute deduplication window, identical messages are delivered only once.\nSaves you from having to generate a unique `MessageDeduplicationId` for each message.\nRequires `fifoEnabled: true`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEventsQueue:\n    type: sqs-queue\n    properties:\n      fifoEnabled: true\n      contentBasedDeduplication: true\n      visibilityTimeoutSeconds: 90\n  orderEventConsumer:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/order-event-consumer.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: orderEventsQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEventsQueue = new SqsQueue({\n    fifoEnabled: true,\n    contentBasedDeduplication: true,\n    visibilityTimeoutSeconds: 90\n  });\n\n  const orderEventConsumer = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/order-event-consumer.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'orderEventsQueue' } }]\n  });\n\n  return { resources: { orderEventsQueue, orderEventConsumer } };\n});\n```").optional(),
        "redrivePolicy": z.object({
          "targetSqsQueueName": z.string().optional().describe("#### Name of another `sqs-queue` in your config to use as the dead-letter queue.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  notificationsDlq:\n    type: sqs-queue\n  notificationsQueue:\n    type: sqs-queue\n    properties:\n      redrivePolicy:\n        targetSqsQueueName: notificationsDlq\n        maxReceiveCount: 3\n  notificationsWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/notifications-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: notificationsQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const notificationsDlq = new SqsQueue({});\n\n  const notificationsQueue = new SqsQueue({\n    redrivePolicy: {\n      targetSqsQueueName: 'notificationsDlq',\n      maxReceiveCount: 3\n    }\n  });\n\n  const notificationsWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/notifications-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'notificationsQueue' } }]\n  });\n\n  return { resources: { notificationsDlq, notificationsQueue, notificationsWorker } };\n});\n```").optional(),
          "targetSqsQueueArn": z.string().optional().describe("#### ARN of an external SQS queue to use as the dead-letter queue. Use when the DLQ is in another stack or account.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  eventsQueue:\n    type: sqs-queue\n    properties:\n      redrivePolicy:\n        targetSqsQueueArn: arn:aws:sqs:eu-west-1:123456789012:shared-dead-letter-queue\n        maxReceiveCount: 4\n  eventsWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/events-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: eventsQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const eventsQueue = new SqsQueue({\n    redrivePolicy: {\n      targetSqsQueueArn: 'arn:aws:sqs:eu-west-1:123456789012:shared-dead-letter-queue',\n      maxReceiveCount: 4\n    }\n  });\n\n  const eventsWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/events-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'eventsQueue' } }]\n  });\n\n  return { resources: { eventsQueue, eventsWorker } };\n});\n```").optional(),
          "maxReceiveCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### How many times a message can be received (and fail) before being moved to the dead-letter queue.\n\n---\n\nA typical starting value is `3`–`5`. Set lower for fast-failing workloads, higher for retryable transient errors.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsDlq:\n    type: sqs-queue\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      redrivePolicy:\n        targetSqsQueueName: jobsDlq\n        maxReceiveCount: 3\n  jobsWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/jobs-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: jobsQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsDlq = new SqsQueue({});\n\n  const jobsQueue = new SqsQueue({\n    redrivePolicy: {\n      targetSqsQueueName: 'jobsDlq',\n      maxReceiveCount: 3\n    }\n  });\n\n  const jobsWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/jobs-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'jobsQueue' } }]\n  });\n\n  return { resources: { jobsDlq, jobsQueue, jobsWorker } };\n});\n```") }).strict()
        .optional().describe("#### Moves messages that fail processing too many times to a dead-letter queue for inspection.\n\n---\n\nAfter `maxReceiveCount` failed attempts, the message is automatically moved to a separate queue\nso you can investigate and reprocess it. Prevents poison messages from blocking the queue.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsDlq:\n    type: sqs-queue\n    properties:\n      messageRetentionPeriodSeconds: 1209600\n  paymentsQueue:\n    type: sqs-queue\n    properties:\n      redrivePolicy:\n        targetSqsQueueName: paymentsDlq\n        maxReceiveCount: 5\n      visibilityTimeoutSeconds: 60\n  paymentsWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: paymentsQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsDlq = new SqsQueue({ messageRetentionPeriodSeconds: 1209600 });\n\n  const paymentsQueue = new SqsQueue({\n    redrivePolicy: {\n      targetSqsQueueName: 'paymentsDlq',\n      maxReceiveCount: 5\n    },\n    visibilityTimeoutSeconds: 60\n  });\n\n  const paymentsWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'paymentsQueue' } }]\n  });\n\n  return { resources: { paymentsDlq, paymentsQueue, paymentsWorker } };\n});\n```").optional(),
        "alarms": z.array(z.object({
          "trigger": z.union([z.object({
              "type": z.literal("sqs-queue-received-messages-count"),
              "properties": z.object({
                "thresholdCount": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when received message count crosses this threshold.\n\n---\n\nDefault: fires if **average** messages received per period > `thresholdCount`.\nCustomize with `statistic` and `comparisonOperator`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestionQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Ingestion backlog growing\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 5000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestionQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Ingestion backlog growing',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: {\n            thresholdCount: 5000\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { ingestionQueue } };\n});\n```"),
                "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```"),
                "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("sqs-queue-not-empty").describe("#### Fires when the SQS queue has unprocessed messages.\n\n---\n\nThe queue is considered \"not empty\" if any of these are non-zero: visible messages,\nin-flight messages, messages received, or messages sent.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  deadLetterQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Dead-letter queue has messages — investigate failures\n          trigger:\n            type: sqs-queue-not-empty\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const deadLetterQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Dead-letter queue has messages — investigate failures',\n        trigger: {\n          type: 'sqs-queue-not-empty'\n        },\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { deadLetterQueue } };\n});\n```") }).strict()
          ]),
          "evaluation": z.object({
            "period": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Duration of one evaluation period in seconds. Must be a multiple of 60.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker running too long\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          evaluation:\n            period: 300\n            evaluationPeriods: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker running too long',\n        trigger: {\n          type: 'lambda-duration',\n          properties: { thresholdMilliseconds: 5000 }\n        },\n        evaluation: {\n          period: 300,\n          evaluationPeriods: 2\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default(60),
            "evaluationPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many recent periods to evaluate. Prevents alarms from firing on short spikes.\n\n---\n\nExample: set to `5` with `breachedPeriods: 3` — the alarm fires only if the threshold is breached\nin at least 3 of the last 5 periods.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/payments.ts\n      alarms:\n        - description: Payments error rate elevated\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/payments.ts' }),\n    alarms: [\n      {\n        description: 'Payments error rate elevated',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 1 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { paymentsFn } };\n});\n```").default(1),
            "breachedPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many periods (within `evaluationPeriods`) must breach the threshold to fire the alarm.\n\n---\n\nMust be ≤ `evaluationPeriods`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/checkout.ts\n      alarms:\n        - description: Checkout errors persisting across multiple periods\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 2\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/checkout.ts' }),\n    alarms: [\n      {\n        description: 'Checkout errors persisting across multiple periods',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { checkoutFn } };\n});\n```").default(1) }).strict()
          .optional().describe("#### How long and how often to evaluate the metric before triggering.\n\n---\n\nControls the evaluation window (period), how many periods to look at, and how many must breach\nthe threshold to fire the alarm. Useful for filtering out short spikes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/api.ts\n      memory: 512\n      timeout: 10\n      alarms:\n        - description: API error rate too high\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    alarms: [\n      {\n        description: 'API error rate too high',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFunction } };\n});\n```").optional(),
          "notificationTargets": z.array(z.union([z.object({
              "type": z.literal("slack"),
              "properties": z.object({
                "conversationId": z.string().describe("#### The Slack channel or DM ID to send notifications to.\n\n---\n\nTo find the ID: open the channel, click its name, and look at the bottom of the **About** tab.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```"),
                "accessToken": z.string().describe("#### Bot User OAuth Token for your Slack app. Store as `$Secret()` for security.\n\n---\n\nCreate a Slack app, add the `chat:write` scope, install it to your workspace, then copy the Bot User OAuth Token.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("ms-teams"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Incoming Webhook URL for the MS Teams channel. Store as `$Secret()` for security.\n\n---\n\nCreate an Incoming Webhook connector in your Teams channel settings to get this URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 1024\n      timeout: 15\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          description: API function is running slow\n          notificationTargets:\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('ms-teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/api.ts' }\n    },\n    memory: 1024,\n    timeout: 15,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 5000 } },\n        description: 'API function is running slow',\n        notificationTargets: [\n          {\n            type: 'ms-teams',\n            properties: {\n              webhookUrl: $Secret('ms-teams-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("email"),
              "properties": z.object({
                "sender": z.string().describe("#### The email address of the sender.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```"),
                "recipient": z.string().describe("#### The email address of the recipient.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("discord"),
              "properties": z.object({
                "webhookUrl": z.string().describe("#### Discord Webhook URL for the channel. Store as `$Secret()` for security.\n\n---\n\nCreate a webhook in your Discord channel settings (Edit Channel → Integrations → Webhooks).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest.ts\n      memory: 256\n      timeout: 60\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 30000\n          description: Ingest job is taking too long\n          notificationTargets:\n            - type: discord\n              properties:\n                webhookUrl: $Secret('discord-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest.ts' }\n    },\n    memory: 256,\n    timeout: 60,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 30000 } },\n        description: 'Ingest job is taking too long',\n        notificationTargets: [\n          {\n            type: 'discord',\n            properties: {\n              webhookUrl: $Secret('discord-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { ingestFunction } };\n});\n```") }).strict()
              .optional().describe("").optional() }).strict()
            , z.object({
              "type": z.literal("webhook"),
              "properties": z.object({
                "url": z.string().describe("#### The URL to send webhook POST requests to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```"),
                "secret": z.string().optional().describe("#### Optional signing secret for HMAC-SHA256 payload verification.\n\n---\n\nIf provided, each request includes an `X-Stacktape-Signature` header.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional(),
                "headers": z.record(z.string(), z.any()).optional().describe("#### Optional custom headers to include in each request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n                headers:\n                  X-Environment: production\n                  Authorization: $Secret('webhook-auth-header')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret'),\n              headers: {\n                'X-Environment': 'production',\n                Authorization: $Secret('webhook-auth-header')\n              }\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional() }).strict()
              .optional().describe("").optional() }).strict()
          ])).optional().describe("#### Where to send notifications when the alarm fires — Slack, MS Teams, or email.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API returning too many errors\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 2\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C12345678\n                accessToken: $Secret('slack-bot-token')\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API returning too many errors',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: { conversationId: 'C12345678', accessToken: $Secret('slack-bot-token') }\n          },\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          },\n          {\n            type: 'ms-teams',\n            properties: { webhookUrl: $Secret('teams-webhook-url') }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "includeInHistory": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether alarm state changes should appear in monitoring history.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Jobs queue backing up\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          includeInHistory: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Jobs queue backing up',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        includeInHistory: false\n      }\n    ]\n  });\n\n  return { resources: { jobsQueue } };\n});\n```").default(true),
          "description": z.string().optional().describe("#### Custom alarm description used in notification messages and the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 80\n          description: Database CPU sustained above 80% — consider scaling up\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: { thresholdPercent: 80 }\n        },\n        description: 'Database CPU sustained above 80% — consider scaling up'\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Additional alarms associated with this resource.\n\n---\n\nThese alarms will be merged with any alarms configured globally in the [console](https://console.stacktape.com/alarms).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  backlogQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-token')\n  backlogWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/backlog-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: backlogQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const backlogQueue = new SqsQueue({\n    alarms: [\n      {\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  const backlogWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/backlog-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'backlogQueue' } }]\n  });\n\n  return { resources: { backlogQueue, backlogWorker } };\n});\n```").optional(),
        "disabledGlobalAlarms": z.array(z.string()).optional().describe("#### Disables globally configured alarms for this resource.\n\n---\n\nProvide a list of alarm names as configured in the [console](https://console.stacktape.com/alarms).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  lowPriorityQueue:\n    type: sqs-queue\n    properties:\n      disabledGlobalAlarms:\n        - sqs-queue-not-empty\n      messageRetentionPeriodSeconds: 86400\n  lowPriorityWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/low-priority-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: lowPriorityQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const lowPriorityQueue = new SqsQueue({\n    disabledGlobalAlarms: ['sqs-queue-not-empty'],\n    messageRetentionPeriodSeconds: 86400\n  });\n\n  const lowPriorityWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/low-priority-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'lowPriorityQueue' } }]\n  });\n\n  return { resources: { lowPriorityQueue, lowPriorityWorker } };\n});\n```").optional(),
        "policyStatements": z.array(z.object({
          "Effect": z.string().describe("#### `Allow` or `Deny` access for the specified actions and principal.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  restrictedQueue:\n    type: sqs-queue\n    properties:\n      policyStatements:\n        - Effect: Deny\n          Principal: \"*\"\n          Action:\n            - sqs:DeleteQueue\n  restrictedWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/restricted-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: restrictedQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const restrictedQueue = new SqsQueue({\n    policyStatements: [\n      {\n        Effect: 'Deny',\n        Principal: '*',\n        Action: ['sqs:DeleteQueue']\n      }\n    ]\n  });\n\n  const restrictedWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/restricted-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'restrictedQueue' } }]\n  });\n\n  return { resources: { restrictedQueue, restrictedWorker } };\n});\n```"),
          "Action": z.array(z.string()).describe("#### SQS actions to allow or deny. E.g., `[\"sqs:SendMessage\"]` or `[\"sqs:*\"]`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestQueue:\n    type: sqs-queue\n    properties:\n      policyStatements:\n        - Effect: Allow\n          Principal:\n            Service: events.amazonaws.com\n          Action:\n            - sqs:SendMessage\n            - sqs:GetQueueAttributes\n  ingestWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: ingestQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestQueue = new SqsQueue({\n    policyStatements: [\n      {\n        Effect: 'Allow',\n        Principal: { Service: 'events.amazonaws.com' },\n        Action: ['sqs:SendMessage', 'sqs:GetQueueAttributes']\n      }\n    ]\n  });\n\n  const ingestWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'ingestQueue' } }]\n  });\n\n  return { resources: { ingestQueue, ingestWorker } };\n});\n```"),
          "Condition": z.any().optional().describe("#### Optional conditions for when this statement applies (e.g., restrict by source ARN or IP range).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  topicBoundQueue:\n    type: sqs-queue\n    properties:\n      policyStatements:\n        - Effect: Allow\n          Principal: \"*\"\n          Action:\n            - sqs:SendMessage\n          Condition:\n            ArnEquals:\n              aws:SourceArn: arn:aws:sns:eu-west-1:123456789012:order-events-topic\n  topicBoundWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/topic-bound-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: topicBoundQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const topicBoundQueue = new SqsQueue({\n    policyStatements: [\n      {\n        Effect: 'Allow',\n        Principal: '*',\n        Action: ['sqs:SendMessage'],\n        Condition: {\n          ArnEquals: {\n            'aws:SourceArn': 'arn:aws:sns:eu-west-1:123456789012:order-events-topic'\n          }\n        }\n      }\n    ]\n  });\n\n  const topicBoundWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/topic-bound-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'topicBoundQueue' } }]\n  });\n\n  return { resources: { topicBoundQueue, topicBoundWorker } };\n});\n```").optional(),
          "Principal": z.any().describe("#### Who gets access: AWS account ID, IAM ARN, or `\"*\"` for everyone. E.g., `{ \"Service\": \"sns.amazonaws.com\" }`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  partnerQueue:\n    type: sqs-queue\n    properties:\n      policyStatements:\n        - Effect: Allow\n          Principal:\n            AWS: arn:aws:iam::210987654321:root\n          Action:\n            - sqs:SendMessage\n            - sqs:ReceiveMessage\n  partnerWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/partner-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: partnerQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const partnerQueue = new SqsQueue({\n    policyStatements: [\n      {\n        Effect: 'Allow',\n        Principal: { AWS: 'arn:aws:iam::210987654321:root' },\n        Action: ['sqs:SendMessage', 'sqs:ReceiveMessage']\n      }\n    ]\n  });\n\n  const partnerWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/partner-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'partnerQueue' } }]\n  });\n\n  return { resources: { partnerQueue, partnerWorker } };\n});\n```") }).strict()
        ).optional().describe("#### Custom access-control statements added to the queue's resource policy.\n\n---\n\nThese are merged with policies Stacktape auto-generates. Use to grant cross-account access or allow\nspecific AWS services (e.g., SNS) to send messages to this queue.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  crossAccountQueue:\n    type: sqs-queue\n    properties:\n      policyStatements:\n        - Effect: Allow\n          Principal:\n            Service: sns.amazonaws.com\n          Action:\n            - sqs:SendMessage\n  crossAccountWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/cross-account-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: crossAccountQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const crossAccountQueue = new SqsQueue({\n    policyStatements: [\n      {\n        Effect: 'Allow',\n        Principal: { Service: 'sns.amazonaws.com' },\n        Action: ['sqs:SendMessage']\n      }\n    ]\n  });\n\n  const crossAccountWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/cross-account-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'crossAccountQueue' } }]\n  });\n\n  return { resources: { crossAccountQueue, crossAccountWorker } };\n});\n```").optional(),
        "events": z.array(z.object({
          "type": z.literal("event-bus"),
          "properties": z.object({
            "messageGroupId": z.string().optional().describe("#### Message group ID for FIFO queues. Required when the target queue has `fifoEnabled: true`.\n\n---\n\nMessages in the same group are processed in strict order. Different groups can be processed in parallel.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditEventBus:\n    type: event-bus\n  auditQueue:\n    type: sqs-queue\n    properties:\n      fifoEnabled: true\n      contentBasedDeduplication: true\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: auditEventBus\n            eventPattern:\n              detail-type:\n                - UserAction\n            messageGroupId: audit-events\n  auditWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: auditQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, EventBus, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditEventBus = new EventBus({});\n\n  const auditQueue = new SqsQueue({\n    fifoEnabled: true,\n    contentBasedDeduplication: true,\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'auditEventBus',\n          eventPattern: { 'detail-type': ['UserAction'] },\n          messageGroupId: 'audit-events'\n        }\n      }\n    ]\n  });\n\n  const auditWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/audit-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'auditQueue' } }]\n  });\n\n  return { resources: { auditEventBus, auditQueue, auditWorker } };\n});\n```").optional(),
            "eventBusArn": z.string().optional().describe("#### The ARN of an existing event bus.\n\n---\n\nUse this to subscribe to an event bus that is not managed by your stack.\nYou must specify only one of `eventBusArn`, `eventBusName`, or `useDefaultBus`.").optional(),
            "eventBusName": z.string().optional().describe("#### The name of an event bus defined in your stack's resources.\n\n---\n\nYou must specify only one of `eventBusArn`, `eventBusName`, or `useDefaultBus`.").optional(),
            "useDefaultBus": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Uses the default AWS event bus.\n\n---\n\nYou must specify only one of `eventBusArn`, `eventBusName`, or `useDefaultBus`.").optional(),
            "eventPattern": z.object({
              "version": z.any().optional().describe("#### Filter by event version.").optional(), "detail-type": z.any().optional().describe("#### Filter by event detail-type (e.g., `[\"OrderPlaced\"]`). This is the primary field for routing custom events.").optional(),
              "source": z.any().optional().describe("#### Filter by event source (e.g., `[\"my-app\"]` or `[\"aws.ec2\"]` for AWS service events).").optional(),
              "account": z.any().optional().describe("#### Filter by AWS account ID.").optional(),
              "region": z.any().optional().describe("#### Filter by AWS region.").optional(),
              "resources": z.any().optional().describe("#### Filter by resource ARNs.").optional(),
              "detail": z.any().optional().describe("#### Filter by event payload content. Supports nested matching, prefix/suffix, numeric comparisons.").optional(), "replay-name": z.any().optional().describe("#### Filter by replay name (only present on replayed events).").optional() }).strict()
            .describe("#### A pattern to filter events from the event bus.\n\n---\n\nOnly events that match this pattern will trigger the target.\nFor details on the syntax, see the [AWS EventBridge documentation on event patterns](https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-event-patterns.html)."),
            "onDeliveryFailure": z.object({
              "sqsQueueArn": z.string().optional().describe("#### The ARN of the SQS queue for failed events.").optional(),
              "sqsQueueName": z.string().optional().describe("#### The name of an SQS queue (defined in your Stacktape configuration) for failed events.").optional() }).strict()
            .optional().describe("#### A destination for events that fail to be delivered to the target.\n\n---\n\nIn rare cases, an event might fail to be delivered. This property specifies an SQS queue where failed events will be sent.").optional(),
            "input": z.any().optional().describe("#### A fixed JSON object to be passed as the event payload.\n\n---\n\nIf you need to customize the payload based on the event, use `inputTransformer` instead.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n            input:\n              source: my-custom-event\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EventBus, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n  const orderProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/process-order.ts' }),\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] },\n          input: { source: 'my-custom-event' }\n        }\n      }\n    ]\n  });\n  return { resources: { orderEvents, orderProcessor } };\n});\n```").optional(),
            "inputPath": z.string().optional().describe("#### A JSONPath expression to extract a portion of the event to pass to the target.\n\n---\n\nThis is useful for forwarding only a specific part of the event payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n            inputPath: $.detail\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EventBus, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n  const orderProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/process-order.ts' }),\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] },\n          inputPath: '$.detail'\n        }\n      }\n    ]\n  });\n  return { resources: { orderEvents, orderProcessor } };\n});\n```").optional(),
            "inputTransformer": z.object({
              "inputPathsMap": z.any().optional().describe("#### A map of key-value pairs to extract from the event payload.\n\n---\n\nEach value is a JSONPath expression that extracts data from the event. These extracted values can then be used in the `inputTemplate`.").optional(),
              "inputTemplate": z.any().describe("#### A template for constructing a new event payload.\n\n---\n\nUse placeholders (`<placeholder>`) to insert the values extracted with `inputPathsMap`.") }).strict()
            .optional().describe("#### Customizes the event payload sent to the target.\n\n---\n\nThis allows you to extract values from the original event and use them to construct a new payload.\nYou can only use one of `input`, `inputPath`, or `inputTransformer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  orderEvents:\n    type: event-bus\n  orderProcessor:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/process-order.ts\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: orderEvents\n            eventPattern:\n              source:\n                - my.orders\n            inputTransformer:\n              inputPathsMap:\n                instanceId: $.detail.instance-id\n                instanceState: $.detail.state\n              inputTemplate:\n                message: Instance <instanceId> is now in state <instanceState>.\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, EventBus, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const orderEvents = new EventBus({});\n  const orderProcessor = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/process-order.ts' }),\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'orderEvents',\n          eventPattern: { source: ['my.orders'] },\n          inputTransformer: {\n            inputPathsMap: {\n              instanceId: '$.detail.instance-id',\n              instanceState: '$.detail.state'\n            },\n            inputTemplate: { message: 'Instance <instanceId> is now in state <instanceState>.' }\n          }\n        }\n      }\n    ]\n  });\n  return { resources: { orderEvents, orderProcessor } };\n});\n```").optional() }).strict()
          .describe("#### Properties of the integration\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  shipmentQueue:\n    type: sqs-queue\n    properties:\n      events:\n        - type: event-bus\n          properties:\n            useDefaultBus: true\n            eventPattern:\n              source:\n                - logistics-service\n              detail-type:\n                - ShipmentDispatched\n  shipmentWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/shipment-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: shipmentQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const shipmentQueue = new SqsQueue({\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          useDefaultBus: true,\n          eventPattern: {\n            source: ['logistics-service'],\n            'detail-type': ['ShipmentDispatched']\n          }\n        }\n      }\n    ]\n  });\n\n  const shipmentWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/shipment-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'shipmentQueue' } }]\n  });\n\n  return { resources: { shipmentQueue, shipmentWorker } };\n});\n```") }).strict()
        .describe("#### Routes events from an EventBridge event bus to this queue when they match a specified pattern.")).optional().describe("#### A list of event sources that trigger message delivery to this queue.\n\n---\n\nCurrently supports EventBridge event bus integration for delivering events directly to the queue.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  appEventBus:\n    type: event-bus\n  orderQueue:\n    type: sqs-queue\n    properties:\n      events:\n        - type: event-bus\n          properties:\n            eventBusName: appEventBus\n            eventPattern:\n              detail-type:\n                - OrderPlaced\n  orderWorker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/order-worker.ts\n      events:\n        - type: sqs\n          properties:\n            sqsQueueName: orderQueue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, EventBus, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appEventBus = new EventBus({});\n\n  const orderQueue = new SqsQueue({\n    events: [\n      {\n        type: 'event-bus',\n        properties: {\n          eventBusName: 'appEventBus',\n          eventPattern: { 'detail-type': ['OrderPlaced'] }\n        }\n      }\n    ]\n  });\n\n  const orderWorker = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/order-worker.ts' }\n    },\n    events: [{ type: 'sqs', properties: { sqsQueueName: 'orderQueue' } }]\n  });\n\n  return { resources: { appEventBus, orderQueue, orderWorker } };\n});\n```").optional() }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Message queue for decoupling services. Producers send messages, consumers process them at their own pace.\n\n---\n\nFully managed, serverless, pay-per-message. Use for background processing, task queues, or buffering between services."), z.object({
      "type": z.literal("state-machine"),
      "properties": z.object({
        "definition": z.object({
          "Comment": z.string().optional().describe("#### A human-readable description of the state machine.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  sendEmail:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/send-email.ts\n\n  notifyWorkflow:\n    type: state-machine\n    properties:\n      definition:\n        Comment: Sends a welcome email to newly registered users\n        StartAt: SendEmail\n        States:\n          SendEmail:\n            Type: Task\n            Resource:\n              Fn::GetAtt:\n                - SendEmailFunction\n                - Arn\n            End: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, StateMachine, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sendEmail = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/send-email.ts' })\n  });\n  const notifyWorkflow = new StateMachine({\n    definition: {\n      Comment: 'Sends a welcome email to newly registered users',\n      StartAt: 'SendEmail',\n      States: {\n        SendEmail: {\n          Type: 'Task',\n          Resource: { 'Fn::GetAtt': ['SendEmailFunction', 'Arn'] },\n          End: true\n        }\n      }\n    }\n  });\n  return { resources: { sendEmail, notifyWorkflow } };\n});\n```").optional(),
          "StartAt": z.string().describe("#### The name of the state to start the execution at.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  validateInput:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/validate-input.ts\n\n  ingestWorkflow:\n    type: state-machine\n    properties:\n      definition:\n        Comment: Validate incoming data before processing\n        StartAt: ValidateInput\n        States:\n          ValidateInput:\n            Type: Task\n            Resource:\n              Fn::GetAtt:\n                - ValidateInputFunction\n                - Arn\n            End: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, StateMachine, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const validateInput = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/validate-input.ts' })\n  });\n  const ingestWorkflow = new StateMachine({\n    definition: {\n      Comment: 'Validate incoming data before processing',\n      StartAt: 'ValidateInput',\n      States: {\n        ValidateInput: {\n          Type: 'Task',\n          Resource: { 'Fn::GetAtt': ['ValidateInputFunction', 'Arn'] },\n          End: true\n        }\n      }\n    }\n  });\n  return { resources: { validateInput, ingestWorkflow } };\n});\n```"),
          "States": z.record(z.string(), z.union([z.object({
              "Type": z.string(),
              "Next": z.string().optional().describe("").optional(),
              "End": z.literal(true).optional().describe("").optional(),
              "Comment": z.string().optional().describe("").optional(),
              "OutputPath": z.string().optional().describe("").optional(),
              "InputPath": z.string().optional().describe("").optional(),
              "Choices": z.array(z.object({
                "Variable": z.string().optional().describe("").optional(),
                "Next": z.string().optional().describe("").optional(),
                "And": z.array(z.any()).optional().describe("").optional(),
                "Or": z.array(z.any()).optional().describe("").optional(),
                "Not": z.any().optional(),
                "BooleanEquals": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("").optional(),
                "NumericEquals": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "NumericGreaterThan": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "NumericGreaterThanEquals": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "NumericLessThan": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "NumericLessThanEquals": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "StringEquals": z.string().optional().describe("").optional(),
                "StringGreaterThan": z.string().optional().describe("").optional(),
                "StringGreaterThanEquals": z.string().optional().describe("").optional(),
                "StringLessThan": z.string().optional().describe("").optional(),
                "StringLessThanEquals": z.string().optional().describe("").optional(),
                "TimestampEquals": z.string().optional().describe("").optional(),
                "TimestampGreaterThan": z.string().optional().describe("").optional(),
                "TimestampGreaterThanEquals": z.string().optional().describe("").optional(),
                "TimestampLessThan": z.string().optional().describe("").optional(),
                "TimestampLessThanEquals": z.string().optional().describe("").optional() }).catchall(z.any())),
              "Default": z.string().optional().describe("").optional() }).strict()
            , z.object({
              "Type": z.string(),
              "Comment": z.string().optional().describe("").optional(),
              "OutputPath": z.string().optional().describe("").optional(),
              "InputPath": z.string().optional().describe("").optional(),
              "Cause": z.string().optional().describe("").optional(),
              "Error": z.string().optional().describe("").optional() }).strict()
            , z.object({
              "Type": z.string(),
              "Next": z.string().optional().describe("").optional(),
              "End": z.literal(true).optional().describe("").optional(),
              "Comment": z.string().optional().describe("").optional(),
              "OutputPath": z.string().optional().describe("").optional(),
              "InputPath": z.string().optional().describe("").optional(),
              "ResultPath": z.string().optional().describe("").optional(),
              "ItemsPath": z.string().optional().describe("").optional(),
              "MaxConcurrency": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
              "Iterator": z.object({
                "name": z.string(),
                "type": z.literal("state-machine"),
                "configParentResourceType": z.literal("state-machine"),
                "nameChain": z.array(z.string()) }).strict()
              ,
              "Parameters": z.record(z.string(), z.any()).optional().describe("").optional(),
              "Retry": z.array(z.object({
                "ErrorEquals": z.array(z.string()),
                "IntervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "MaxAttempts": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "BackoffRate": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional() }).catchall(z.any())).optional().describe("").optional(),
              "Catch": z.array(z.object({
                "ErrorEquals": z.array(z.string()),
                "Next": z.string() }).catchall(z.any())).optional().describe("").optional() }).strict()
            , z.object({
              "Type": z.string(),
              "Next": z.string().optional().describe("").optional(),
              "End": z.literal(true).optional().describe("").optional(),
              "Comment": z.string().optional().describe("").optional(),
              "OutputPath": z.string().optional().describe("").optional(),
              "InputPath": z.string().optional().describe("").optional(),
              "ResultPath": z.string().optional().describe("").optional(),
              "Branches": z.array(z.object({
                "name": z.string(),
                "type": z.literal("state-machine"),
                "configParentResourceType": z.literal("state-machine"),
                "nameChain": z.array(z.string()) }).strict()
              ),
              "Retry": z.array(z.object({
                "ErrorEquals": z.array(z.string()),
                "IntervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "MaxAttempts": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "BackoffRate": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional() }).catchall(z.any())).optional().describe("").optional(),
              "Catch": z.array(z.object({
                "ErrorEquals": z.array(z.string()),
                "Next": z.string() }).catchall(z.any())).optional().describe("").optional() }).strict()
            , z.object({
              "Type": z.string(),
              "Next": z.string().optional().describe("").optional(),
              "End": z.literal(true).optional().describe("").optional(),
              "Comment": z.string().optional().describe("").optional(),
              "OutputPath": z.string().optional().describe("").optional(),
              "InputPath": z.string().optional().describe("").optional(),
              "ResultPath": z.string().optional().describe("").optional(),
              "Parameters": z.record(z.string(), z.any()).optional().describe("").optional(),
              "Result": z.any().optional().describe("").optional() }).strict()
            , z.object({
              "Type": z.string(),
              "Comment": z.string().optional().describe("").optional() }).strict()
            , z.object({
              "Type": z.string(),
              "Next": z.string().optional().describe("").optional(),
              "End": z.literal(true).optional().describe("").optional(),
              "Comment": z.string().optional().describe("").optional(),
              "OutputPath": z.string().optional().describe("").optional(),
              "InputPath": z.string().optional().describe("").optional(),
              "Resource": z.union([z.record(z.string(), z.any()), z.string()]),
              "ResultPath": z.string().optional().describe("").optional(),
              "Retry": z.array(z.object({
                "ErrorEquals": z.array(z.string()),
                "IntervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "MaxAttempts": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
                "BackoffRate": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional() }).catchall(z.any())).optional().describe("").optional(),
              "Catch": z.array(z.object({
                "ErrorEquals": z.array(z.string()),
                "Next": z.string() }).catchall(z.any())).optional().describe("").optional(),
              "TimeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
              "HeartbeatSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
              "Parameters": z.record(z.string(), z.any()).optional().describe("").optional() }).strict()
            , z.object({
              "Type": z.string(),
              "Next": z.string().optional().describe("").optional(),
              "End": z.literal(true).optional().describe("").optional(),
              "Comment": z.string().optional().describe("").optional(),
              "OutputPath": z.string().optional().describe("").optional(),
              "InputPath": z.string().optional().describe("").optional(),
              "Seconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
              "Timestamp": z.string().optional().describe("").optional(),
              "SecondsPath": z.string().optional().describe("").optional(),
              "TimestampPath": z.string().optional().describe("").optional() }).strict()
          ])).describe("#### An object containing the states of the state machine.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  extract:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/extract.ts\n  transform:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/transform.ts\n\n  etlWorkflow:\n    type: state-machine\n    properties:\n      definition:\n        Comment: Two-step ETL pipeline\n        StartAt: Extract\n        States:\n          Extract:\n            Type: Task\n            Resource:\n              Fn::GetAtt:\n                - ExtractFunction\n                - Arn\n            Next: Transform\n          Transform:\n            Type: Task\n            Resource:\n              Fn::GetAtt:\n                - TransformFunction\n                - Arn\n            End: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, StateMachine, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const extract = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/extract.ts' })\n  });\n  const transform = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/transform.ts' })\n  });\n  const etlWorkflow = new StateMachine({\n    definition: {\n      Comment: 'Two-step ETL pipeline',\n      StartAt: 'Extract',\n      States: {\n        Extract: {\n          Type: 'Task',\n          Resource: { 'Fn::GetAtt': ['ExtractFunction', 'Arn'] },\n          Next: 'Transform'\n        },\n        Transform: {\n          Type: 'Task',\n          Resource: { 'Fn::GetAtt': ['TransformFunction', 'Arn'] },\n          End: true\n        }\n      }\n    }\n  });\n  return { resources: { extract, transform, etlWorkflow } };\n});\n```"),
          "Version": z.string().optional().describe("#### The version of the Amazon States Language.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  doWork:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/do-work.ts\n\n  versionedWorkflow:\n    type: state-machine\n    properties:\n      definition:\n        Comment: Pins the Amazon States Language version\n        Version: '1.0'\n        StartAt: DoWork\n        States:\n          DoWork:\n            Type: Task\n            Resource:\n              Fn::GetAtt:\n                - DoWorkFunction\n                - Arn\n            End: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, StateMachine, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const doWork = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/do-work.ts' })\n  });\n  const versionedWorkflow = new StateMachine({\n    definition: {\n      Comment: 'Pins the Amazon States Language version',\n      Version: '1.0',\n      StartAt: 'DoWork',\n      States: {\n        DoWork: {\n          Type: 'Task',\n          Resource: { 'Fn::GetAtt': ['DoWorkFunction', 'Arn'] },\n          End: true\n        }\n      }\n    }\n  });\n  return { resources: { doWork, versionedWorkflow } };\n});\n```").optional(),
          "TimeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### The maximum time, in seconds, that a state machine can run.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  longTask:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/long-task.ts\n\n  boundedWorkflow:\n    type: state-machine\n    properties:\n      definition:\n        Comment: Fails the execution if it runs longer than 1 hour\n        StartAt: LongTask\n        TimeoutSeconds: 3600\n        States:\n          LongTask:\n            Type: Task\n            Resource:\n              Fn::GetAtt:\n                - LongTaskFunction\n                - Arn\n            End: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, StateMachine, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const longTask = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/long-task.ts' })\n  });\n  const boundedWorkflow = new StateMachine({\n    definition: {\n      Comment: 'Fails the execution if it runs longer than 1 hour',\n      StartAt: 'LongTask',\n      TimeoutSeconds: 3600,\n      States: {\n        LongTask: {\n          Type: 'Task',\n          Resource: { 'Fn::GetAtt': ['LongTaskFunction', 'Arn'] },\n          End: true\n        }\n      }\n    }\n  });\n  return { resources: { longTask, boundedWorkflow } };\n});\n```").optional() }).strict()
        .describe("#### The workflow definition in [Amazon States Language (ASL)](https://states-language.net/spec.html).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  chargeCard:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/charge-card.ts\n\n  paymentWorkflow:\n    type: state-machine\n    properties:\n      definition:\n        Comment: Charge the customer's card\n        StartAt: ChargeCard\n        States:\n          ChargeCard:\n            Type: Task\n            Resource:\n              Fn::GetAtt:\n                - ChargeCardFunction\n                - Arn\n            End: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, StateMachine, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const chargeCard = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/charge-card.ts' })\n  });\n  const paymentWorkflow = new StateMachine({\n    definition: {\n      Comment: \"Charge the customer's card\",\n      StartAt: 'ChargeCard',\n      States: {\n        ChargeCard: {\n          Type: 'Task',\n          Resource: { 'Fn::GetAtt': ['ChargeCardFunction', 'Arn'] },\n          End: true\n        }\n      }\n    }\n  });\n  return { resources: { chargeCard, paymentWorkflow } };\n});\n```") }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Visual workflow engine for orchestrating Lambda functions, API calls, and other AWS services.\n\n---\n\nDefine multi-step workflows with branching, retries, parallel execution, and error handling —\nall without writing orchestration code. Pay per state transition (~$0.025/1,000 transitions).\nDefined using [Amazon States Language (ASL)](https://states-language.net/spec.html).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processOrder:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/process-order.ts\n\n  orderWorkflow:\n    type: state-machine\n    properties:\n      definition:\n        StartAt: ProcessOrder\n        States:\n          ProcessOrder:\n            Type: Task\n            Resource:\n              Fn::GetAtt:\n                - ProcessOrderFunction\n                - Arn\n            End: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, StateMachine, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processOrder = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/process-order.ts' })\n  });\n  const orderWorkflow = new StateMachine({\n    definition: {\n      StartAt: 'ProcessOrder',\n      States: {\n        ProcessOrder: {\n          Type: 'Task',\n          Resource: { 'Fn::GetAtt': ['ProcessOrderFunction', 'Arn'] },\n          End: true\n        }\n      }\n    }\n  });\n  return { resources: { processOrder, orderWorkflow } };\n});\n```"), z.object({
      "type": z.literal("sveltekit-web"),
      "properties": z.object({
        "appDirectory": z.string().optional().describe("#### Directory containing your `svelte.config.js`. For monorepos, point to the SvelteKit workspace.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: sveltekit-web\n    properties:\n      appDirectory: ./apps/storefront\n      environment:\n        - name: PUBLIC_API_URL\n          value: https://api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SvelteKitWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SvelteKitWeb({\n    appDirectory: './apps/storefront',\n    environment: [{ name: 'PUBLIC_API_URL', value: 'https://api.example.com' }]\n  });\n  return { resources: { web } };\n});\n```"),
        "buildCommand": z.string().optional().describe("#### Override the default `vite build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: sveltekit-web\n    properties:\n      buildCommand: pnpm run build\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SvelteKitWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SvelteKitWeb({\n    buildCommand: 'pnpm run build',\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables for the SSR function. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      credentials:\n        masterUserPassword: $Secret('database.password')\n  web:\n    type: sveltekit-web\n    properties:\n      connectTo:\n        - database\n      environment:\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n        - name: STRIPE_SECRET_KEY\n          value: $Secret('stripe.secretKey')\n        - name: PUBLIC_APP_NAME\n          value: My Storefront\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, SvelteKitWeb, defineConfig, $Secret, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    engine: {\n      type: 'postgres',\n      properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t4g.micro' } }\n    },\n    credentials: { masterUserPassword: $Secret('database.password') }\n  });\n  const web = new SvelteKitWeb({\n    connectTo: ['database'],\n    environment: [\n      { name: 'DATABASE_URL', value: $ResourceParam('database', 'connectionString') },\n      { name: 'STRIPE_SECRET_KEY', value: $Secret('stripe.secretKey') },\n      { name: 'PUBLIC_APP_NAME', value: 'My Storefront' }\n    ]\n  });\n  return { resources: { database, web } };\n});\n```").optional(),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Attach custom domains with auto-managed DNS records and TLS certificates.\n\n---\n\n**Prerequisite:** A Route 53 hosted zone for your domain must exist in your AWS account.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: sveltekit-web\n    properties:\n      customDomains:\n        - domainName: app.example.com\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SvelteKitWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SvelteKitWeb({\n    customDomains: [{ domainName: 'app.example.com' }],\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "serverLambda": z.object({
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128–10,240). CPU scales proportionally — 1,769 MB = 1 vCPU.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      environment:\n        - name: NODE_ENV\n          value: production\n      serverLambda:\n        memory: 2048\n        timeout: 20\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    environment: { NODE_ENV: 'production' },\n    serverLambda: {\n      memory: 2048,\n      timeout: 20\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(1024),
          "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Max: 30.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        timeout: 25\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      timeout: 25\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(30),
          "joinDefaultVpc": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Connect to VPC resources (databases, Redis). **Warning:** function loses direct internet access.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('database.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      connectTo:\n        - database\n      environment:\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n      serverLambda:\n        memory: 1024\n        joinDefaultVpc: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, RelationalDatabase, $Secret, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('database.password') },\n    engine: {\n      type: 'postgres',\n      properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t4g.micro' } }\n    },\n    accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' }\n  });\n\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    connectTo: [database],\n    environment: { DATABASE_URL: $ResourceParam('database', 'connectionString') },\n    serverLambda: {\n      memory: 1024,\n      joinDefaultVpc: true\n    }\n  });\n\n  return { resources: { database, webApp } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highVolumeFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highVolumeFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { highVolumeFunction } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs. Longer retention = higher storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/audit.ts' } },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditFunction } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Logging config. Logs are sent to CloudWatch. View with `stacktape logs` or in the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        logging:\n          retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      logging: {\n        retentionDays: 30\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Customize the SSR Lambda function (memory, timeout, VPC, logging).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      credentials:\n        masterUserPassword: $Secret('database.password')\n  web:\n    type: sveltekit-web\n    properties:\n      connectTo:\n        - database\n      serverLambda:\n        memory: 2048\n        timeout: 20\n        joinDefaultVpc: true\n        logging:\n          retentionDays: 90\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, SvelteKitWeb, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    engine: {\n      type: 'postgres',\n      properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t4g.micro' } }\n    },\n    credentials: { masterUserPassword: $Secret('database.password') }\n  });\n  const web = new SvelteKitWeb({\n    connectTo: ['database'],\n    serverLambda: {\n      memory: 2048,\n      timeout: 20,\n      joinDefaultVpc: true,\n      logging: { retentionDays: 90 }\n    }\n  });\n  return { resources: { database, web } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this app. Firewall `scope` must be `cdn`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  firewall:\n    type: web-app-firewall\n    properties:\n      scope: cdn\n  web:\n    type: sveltekit-web\n    properties:\n      useFirewall: firewall\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, SvelteKitWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const firewall = new WebAppFirewall({ scope: 'cdn' });\n  const web = new SvelteKitWeb({\n    useFirewall: 'firewall',\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { firewall, web } };\n});\n```").optional(),
        "dev": z.object({
          "command": z.string().optional().describe("#### Override the default dev server command (e.g., `npm run dev`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      dev:\n        command: npm run dev\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    dev: {\n      command: 'npm run dev'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "workingDirectory": z.string().optional().describe("#### Working directory for the dev command, relative to project root.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: packages/web\n      dev:\n        command: npm run dev\n        workingDirectory: packages/web\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: 'packages/web',\n    dev: {\n      command: 'npm run dev',\n      workingDirectory: 'packages/web'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Dev server config for `stacktape dev`. Defaults to `vite dev`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: sveltekit-web\n    properties:\n      dev:\n        command: pnpm run dev\n        workingDirectory: ./apps/storefront\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SvelteKitWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SvelteKitWeb({\n    dev: {\n      command: 'pnpm run dev',\n      workingDirectory: './apps/storefront'\n    },\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "fileOptions": z.array(z.object({
          "includePattern": z.string().describe("#### Glob pattern for files this rule applies to (e.g., `*.html`, `assets/**`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
          "excludePattern": z.string().optional().describe("#### Glob pattern for files to exclude even if they match `includePattern`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            excludePattern: 'assets/uncompressed/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          excludePattern: 'assets/uncompressed/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "headers": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### HTTP headers (e.g., `Cache-Control`) for matching files. Forwarded through CDN to the browser.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "tags": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### Tags for matching files. Can be used to target files with `lifecycleRules`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'reports/**'\n            tags:\n              - key: category\n                value: report\n      lifecycleRules:\n        - type: expiration\n          properties:\n            tags:\n              - key: category\n                value: report\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'reports/**',\n          tags: [{ key: 'category', value: 'report' }]\n        }\n      ]\n    },\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          tags: [{ key: 'category', value: 'report' }],\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Set custom headers (e.g., `Cache-Control`) for static files matching a pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: sveltekit-web\n    properties:\n      fileOptions:\n        - includePattern: '_app/immutable/**'\n          headers:\n            - key: Cache-Control\n              value: 'public, max-age=31536000, immutable'\n        - includePattern: '*.html'\n          headers:\n            - key: Cache-Control\n              value: no-cache\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SvelteKitWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SvelteKitWeb({\n    fileOptions: [\n      {\n        includePattern: '_app/immutable/**',\n        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n      },\n      {\n        includePattern: '*.html',\n        headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n      }\n    ],\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "cdn": z.object({
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      disableInvalidationAfterDeploy: true\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(false),
          "defaultCachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Override default SSR caching behavior for all routes handled by the server.\n\n---\n\nUseful when you want to cache SSR responses longer than the framework defaults.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n          cacheKeyParameters:\n            queryString:\n              all: true\n            cookies:\n              none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 3600,\n        cacheKeyParameters: {\n          queryString: { all: true },\n          cookies: { none: true }\n        }\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "pathCachingOverrides": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `/_server-islands/*`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          -\n            path: /_nuxt/*\n            cachingOptions:\n              minTTL: 31536000\n              defaultTTL: 31536000\n              maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_nuxt/*',\n          cachingOptions: { minTTL: 31536000, defaultTTL: 31536000, maxTTL: 31536000 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```"),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .describe("#### Caching behavior override for this path pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n              minTTL: 0\n              defaultTTL: 15\n              maxTTL: 300\n              cacheKeyParameters:\n                queryString:\n                  whitelist:\n                    - page\n                    - limit\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/api/*',\n          cachingOptions: {\n            cacheMethods: ['GET', 'HEAD'],\n            minTTL: 0,\n            defaultTTL: 15,\n            maxTTL: 300,\n            cacheKeyParameters: {\n              queryString: { whitelist: ['page', 'limit'] }\n            }\n          }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```") }).strict()
          ).optional().describe("#### Override caching for specific CDN path patterns.\n\n---\n\nMatches existing framework-managed paths (e.g. `_astro/*`, `_next/data/*`) or adds\nnew server-path caching rules (e.g. `/_server-islands/*`) while preserving managed routing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              minTTL: 0\n              defaultTTL: 0\n              maxTTL: 0\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n                - OPTIONS\n              defaultTTL: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_server-islands/*',\n          cachingOptions: { minTTL: 0, defaultTTL: 0, maxTTL: 0 }\n        },\n        {\n          path: '/api/*',\n          cachingOptions: { cacheMethods: ['GET', 'HEAD', 'OPTIONS'], defaultTTL: 30 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### CDN cache controls for SSR routes and specific path patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  web:\n    type: sveltekit-web\n    properties:\n      cdn:\n        disableInvalidationAfterDeploy: false\n        defaultCachingOptions:\n          minTTL: 0\n          maxTTL: 86400\n          defaultTTL: 3600\n        pathCachingOverrides:\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n                - OPTIONS\n              minTTL: 0\n              maxTTL: 0\n              defaultTTL: 0\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SvelteKitWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new SvelteKitWeb({\n    cdn: {\n      disableInvalidationAfterDeploy: false,\n      defaultCachingOptions: { minTTL: 0, maxTTL: 86400, defaultTTL: 3600 },\n      pathCachingOverrides: [\n        {\n          path: '/api/*',\n          cachingOptions: {\n            cacheMethods: ['GET', 'HEAD', 'OPTIONS'],\n            minTTL: 0,\n            maxTTL: 0,\n            defaultTTL: 0\n          }\n        }\n      ]\n    },\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Deploy a SvelteKit SSR app with Lambda (AWS adapter), S3 for static assets, and CloudFront CDN.\n\n---\n\nFor static-only SvelteKit sites, use `hosting-bucket` with `hostingContentType: 'sveltekit-static-website'` instead."), z.object({
      "type": z.literal("tanstack-web"),
      "properties": z.object({
        "appDirectory": z.string().optional().describe("#### Directory containing your `app.config.ts`. For monorepos, point to the TanStack Start workspace.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\n# appDirectory\nresources:\n  web:\n    type: tanstack-web\n    properties:\n      appDirectory: ./packages/web\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\n// appDirectory\nimport { TanStackWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new TanStackWeb({\n    appDirectory: './packages/web',\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```"),
        "buildCommand": z.string().optional().describe("#### Override the default `vinxi build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\n# buildCommand\nresources:\n  web:\n    type: tanstack-web\n    properties:\n      buildCommand: npm run build\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\n// buildCommand\nimport { TanStackWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new TanStackWeb({\n    buildCommand: 'npm run build',\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables for the SSR function. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\n# environment\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t3.micro\n  web:\n    type: tanstack-web\n    properties:\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n        - name: API_KEY\n          value: $Secret('api-key')\n```\n\n**Example (TypeScript):**\n\n```ts\n// environment\nimport { TanStackWeb, RelationalDatabase, defineConfig, $Secret, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: { type: 'postgres', properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t3.micro' } } }\n  });\n  const web = new TanStackWeb({\n    environment: [\n      { name: 'NODE_ENV', value: 'production' },\n      { name: 'DATABASE_URL', value: $ResourceParam('database', 'connectionString') },\n      { name: 'API_KEY', value: $Secret('api-key') }\n    ]\n  });\n  return { resources: { database, web } };\n});\n```").optional(),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Attach custom domains with auto-managed DNS records and TLS certificates.\n\n---\n\n**Prerequisite:** A Route 53 hosted zone for your domain must exist in your AWS account.\n\n**Example (YAML):**\n\n```yaml\n# customDomains\nresources:\n  web:\n    type: tanstack-web\n    properties:\n      customDomains:\n        - domainName: app.mydomain.com\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\n// customDomains\nimport { TanStackWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new TanStackWeb({\n    customDomains: [{ domainName: 'app.mydomain.com' }],\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "serverLambda": z.object({
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB (128–10,240). CPU scales proportionally — 1,769 MB = 1 vCPU.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      environment:\n        - name: NODE_ENV\n          value: production\n      serverLambda:\n        memory: 2048\n        timeout: 20\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    environment: { NODE_ENV: 'production' },\n    serverLambda: {\n      memory: 2048,\n      timeout: 20\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(1024),
          "timeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Max execution time in seconds. Max: 30.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        timeout: 25\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      timeout: 25\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(30),
          "joinDefaultVpc": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Connect to VPC resources (databases, Redis). **Warning:** function loses direct internet access.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  database:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('database.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n      accessibility:\n        accessibilityMode: scoping-workloads-in-vpc\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      connectTo:\n        - database\n      environment:\n        - name: DATABASE_URL\n          value: $ResourceParam('database', 'connectionString')\n      serverLambda:\n        memory: 1024\n        joinDefaultVpc: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, RelationalDatabase, $Secret, $ResourceParam, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const database = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('database.password') },\n    engine: {\n      type: 'postgres',\n      properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t4g.micro' } }\n    },\n    accessibility: { accessibilityMode: 'scoping-workloads-in-vpc' }\n  });\n\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    connectTo: [database],\n    environment: { DATABASE_URL: $ResourceParam('database', 'connectionString') },\n    serverLambda: {\n      memory: 1024,\n      joinDefaultVpc: true\n    }\n  });\n\n  return { resources: { database, webApp } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch logging entirely.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  highVolumeFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/handler.ts\n      logging:\n        disabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const highVolumeFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/handler.ts' } },\n    logging: {\n      disabled: true\n    }\n  });\n  return { resources: { highVolumeFunction } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs. Longer retention = higher storage cost.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  auditFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/audit.ts\n      logging:\n        retentionDays: 365\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const auditFunction = new LambdaFunction({\n    packaging: { type: 'stacktape-lambda-buildpack', properties: { entryfilePath: 'src/audit.ts' } },\n    logging: {\n      retentionDays: 365\n    }\n  });\n  return { resources: { auditFunction } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://pub.highlight.io/v1/logs/firehose") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Logging config. Logs are sent to CloudWatch. View with `stacktape logs` or in the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      serverLambda:\n        memory: 1024\n        logging:\n          retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    serverLambda: {\n      memory: 1024,\n      logging: {\n        retentionDays: 30\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Customize the SSR Lambda function (memory, timeout, VPC, logging).\n\n---\n\n**Example (YAML):**\n\n```yaml\n# serverLambda\nresources:\n  web:\n    type: tanstack-web\n    properties:\n      serverLambda:\n        memory: 2048\n        timeout: 20\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\n// serverLambda\nimport { TanStackWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new TanStackWeb({\n    serverLambda: { memory: 2048, timeout: 20 },\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this app. Firewall `scope` must be `cdn`.\n\n---\n\n**Example (YAML):**\n\n```yaml\n# useFirewall\nresources:\n  firewall:\n    type: web-app-firewall\n    properties:\n      scope: cdn\n  web:\n    type: tanstack-web\n    properties:\n      useFirewall: firewall\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\n// useFirewall\nimport { TanStackWeb, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const firewall = new WebAppFirewall({ scope: 'cdn' });\n  const web = new TanStackWeb({\n    useFirewall: 'firewall',\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { firewall, web } };\n});\n```").optional(),
        "dev": z.object({
          "command": z.string().optional().describe("#### Override the default dev server command (e.g., `npm run dev`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      dev:\n        command: npm run dev\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    dev: {\n      command: 'npm run dev'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "workingDirectory": z.string().optional().describe("#### Working directory for the dev command, relative to project root.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: packages/web\n      dev:\n        command: npm run dev\n        workingDirectory: packages/web\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: 'packages/web',\n    dev: {\n      command: 'npm run dev',\n      workingDirectory: 'packages/web'\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### Dev server config for `stacktape dev`. Defaults to `vinxi dev`.\n\n---\n\n**Example (YAML):**\n\n```yaml\n# dev\nresources:\n  web:\n    type: tanstack-web\n    properties:\n      dev:\n        command: npm run dev\n        workingDirectory: ./packages/web\n      environment:\n        - name: NODE_ENV\n          value: development\n```\n\n**Example (TypeScript):**\n\n```ts\n// dev\nimport { TanStackWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new TanStackWeb({\n    dev: { command: 'npm run dev', workingDirectory: './packages/web' },\n    environment: [{ name: 'NODE_ENV', value: 'development' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "fileOptions": z.array(z.object({
          "includePattern": z.string().describe("#### Glob pattern for files this rule applies to (e.g., `*.html`, `assets/**`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: '*.html'\n            headers:\n              - key: Cache-Control\n                value: no-cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: '*.html',\n          headers: [{ key: 'Cache-Control', value: 'no-cache' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
          "excludePattern": z.string().optional().describe("#### Glob pattern for files to exclude even if they match `includePattern`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            excludePattern: 'assets/uncompressed/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          excludePattern: 'assets/uncompressed/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "headers": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### HTTP headers (e.g., `Cache-Control`) for matching files. Forwarded through CDN to the browser.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional(),
          "tags": z.array(z.object({
            "key": z.string().describe("#### Key\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```"),
            "value": z.string().describe("#### Value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'assets/**'\n            headers:\n              - key: Cache-Control\n                value: 'public, max-age=31536000, immutable'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'assets/**',\n          headers: [\n            {\n              key: 'Cache-Control',\n              value: 'public, max-age=31536000, immutable'\n            }\n          ]\n        }\n      ]\n    }\n  });\n  return { resources: { assetsBucket } };\n});\n```") }).strict()
          ).optional().describe("#### Tags for matching files. Can be used to target files with `lifecycleRules`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  assetsBucket:\n    type: bucket\n    properties:\n      directoryUpload:\n        directoryPath: ./dist\n        fileOptions:\n          - includePattern: 'reports/**'\n            tags:\n              - key: category\n                value: report\n      lifecycleRules:\n        - type: expiration\n          properties:\n            tags:\n              - key: category\n                value: report\n            daysAfterUpload: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const assetsBucket = new Bucket({\n    directoryUpload: {\n      directoryPath: './dist',\n      fileOptions: [\n        {\n          includePattern: 'reports/**',\n          tags: [{ key: 'category', value: 'report' }]\n        }\n      ]\n    },\n    lifecycleRules: [\n      {\n        type: 'expiration',\n        properties: {\n          tags: [{ key: 'category', value: 'report' }],\n          daysAfterUpload: 30\n        }\n      }\n    ]\n  });\n  return { resources: { assetsBucket } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Set custom headers (e.g., `Cache-Control`) for static files matching a pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\n# fileOptions\nresources:\n  web:\n    type: tanstack-web\n    properties:\n      fileOptions:\n        - includePattern: 'assets/**'\n          headers:\n            - key: Cache-Control\n              value: 'public, max-age=31536000, immutable'\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\n// fileOptions\nimport { TanStackWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new TanStackWeb({\n    fileOptions: [\n      {\n        includePattern: 'assets/**',\n        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }]\n      }\n    ],\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "cdn": z.object({
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      disableInvalidationAfterDeploy: true\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").default(false),
          "defaultCachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Override default SSR caching behavior for all routes handled by the server.\n\n---\n\nUseful when you want to cache SSR responses longer than the framework defaults.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        defaultCachingOptions:\n          minTTL: 0\n          defaultTTL: 60\n          maxTTL: 3600\n          cacheKeyParameters:\n            queryString:\n              all: true\n            cookies:\n              none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      defaultCachingOptions: {\n        minTTL: 0,\n        defaultTTL: 60,\n        maxTTL: 3600,\n        cacheKeyParameters: {\n          queryString: { all: true },\n          cookies: { none: true }\n        }\n      }\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional(),
          "pathCachingOverrides": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `/_server-islands/*`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          -\n            path: /_nuxt/*\n            cachingOptions:\n              minTTL: 31536000\n              defaultTTL: 31536000\n              maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_nuxt/*',\n          cachingOptions: { minTTL: 31536000, defaultTTL: 31536000, maxTTL: 31536000 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```"),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .describe("#### Caching behavior override for this path pattern.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n              minTTL: 0\n              defaultTTL: 15\n              maxTTL: 300\n              cacheKeyParameters:\n                queryString:\n                  whitelist:\n                    - page\n                    - limit\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/api/*',\n          cachingOptions: {\n            cacheMethods: ['GET', 'HEAD'],\n            minTTL: 0,\n            defaultTTL: 15,\n            maxTTL: 300,\n            cacheKeyParameters: {\n              queryString: { whitelist: ['page', 'limit'] }\n            }\n          }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```") }).strict()
          ).optional().describe("#### Override caching for specific CDN path patterns.\n\n---\n\nMatches existing framework-managed paths (e.g. `_astro/*`, `_next/data/*`) or adds\nnew server-path caching rules (e.g. `/_server-islands/*`) while preserving managed routing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApp:\n    type: nuxt-web\n    properties:\n      appDirectory: .\n      cdn:\n        pathCachingOverrides:\n          - path: /_server-islands/*\n            cachingOptions:\n              minTTL: 0\n              defaultTTL: 0\n              maxTTL: 0\n          - path: /api/*\n            cachingOptions:\n              cacheMethods:\n                - GET\n                - HEAD\n                - OPTIONS\n              defaultTTL: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { NuxtWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApp = new NuxtWeb({\n    appDirectory: '.',\n    cdn: {\n      pathCachingOverrides: [\n        {\n          path: '/_server-islands/*',\n          cachingOptions: { minTTL: 0, defaultTTL: 0, maxTTL: 0 }\n        },\n        {\n          path: '/api/*',\n          cachingOptions: { cacheMethods: ['GET', 'HEAD', 'OPTIONS'], defaultTTL: 30 }\n        }\n      ]\n    }\n  });\n\n  return { resources: { webApp } };\n});\n```").optional() }).strict()
        .optional().describe("#### CDN cache controls for SSR routes and specific path patterns.\n\n---\n\n**Example (YAML):**\n\n```yaml\n# cdn\nresources:\n  web:\n    type: tanstack-web\n    properties:\n      cdn:\n        disableInvalidationAfterDeploy: false\n        defaultCachingOptions:\n          defaultTTL: 60\n      environment:\n        - name: NODE_ENV\n          value: production\n```\n\n**Example (TypeScript):**\n\n```ts\n// cdn\nimport { TanStackWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const web = new TanStackWeb({\n    cdn: {\n      disableInvalidationAfterDeploy: false,\n      defaultCachingOptions: { defaultTTL: 60 }\n    },\n    environment: [{ name: 'NODE_ENV', value: 'production' }]\n  });\n  return { resources: { web } };\n});\n```").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Deploy a TanStack Start SSR app with Lambda (Nitro aws-lambda preset), S3 for static assets, and CloudFront CDN."), z.object({
      "type": z.literal("upstash-redis"),
      "properties": z.object({
        "enableEviction": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Auto-remove old keys when memory is full. Prioritizes keys with TTL set. Enable for cache use cases.\n\n---\n\nWithout eviction, writes fail once the memory limit is reached. Enable this for caching workloads.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  cache:\n    type: upstash-redis\n    properties:\n      enableEviction: true\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      connectTo:\n        - cache\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UpstashRedis, LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const cache = new UpstashRedis({\n    enableEviction: true\n  });\n\n  const api = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    connectTo: [cache]\n  });\n\n  return { resources: { cache, api } };\n});\n```\n\n**Example (YAML):**\n\n```yaml\nresources:\n  sessionStore:\n    type: upstash-redis\n    properties:\n      enableEviction: false\n  api:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      environment:\n        - name: REDIS_URL\n          value: $ResourceParam('sessionStore', 'redisUrl')\n        - name: REDIS_REST_URL\n          value: $ResourceParam('sessionStore', 'restUrl')\n        - name: REDIS_REST_TOKEN\n          value: $ResourceParam('sessionStore', 'restToken')\n      connectTo:\n        - sessionStore\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { UpstashRedis, LambdaFunction, defineConfig, $ResourceParam } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sessionStore = new UpstashRedis({\n    enableEviction: false\n  });\n\n  const api = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    environment: {\n      REDIS_URL: $ResourceParam('sessionStore', 'redisUrl'),\n      REDIS_REST_URL: $ResourceParam('sessionStore', 'restUrl'),\n      REDIS_REST_TOKEN: $ResourceParam('sessionStore', 'restToken')\n    },\n    connectTo: [sessionStore]\n  });\n\n  return { resources: { sessionStore, api } };\n});\n```").default(false) }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Serverless Redis by Upstash — pay-per-request with no idle costs.\n\n---\n\nPerfect for Lambda-based apps where a traditional Redis cluster would be wasteful.\nAccessible over HTTPS (REST API) or standard Redis protocol. Great for caching, sessions, rate limiting."), z.object({
      "type": z.literal("web-app-firewall"),
      "properties": z.object({
        "scope": z.enum(["cdn","regional"]).describe("#### `cdn` for CloudFront-attached resources, `regional` for ALBs, User Pools, or direct API Gateways.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```"),
        "defaultAction": z.enum(["Allow","Block"]).optional().describe("#### What happens when no rule matches a request.\n\n---\n\n- **`Allow`** (recommended): Allow all traffic, block only what rules catch.\n- **`Block`**: Block all traffic, allow only what rules explicitly permit (returns 403).\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  adminFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      defaultAction: Block\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n  adminService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/admin.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: adminFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const adminFirewall = new WebAppFirewall({\n    scope: 'regional',\n    defaultAction: 'Block',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const adminService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/admin.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'adminFirewall'\n  });\n\n  return { resources: { adminFirewall, adminService } };\n});\n```"),
        "rules": z.array(z.union([z.object({
            "type": z.literal("managed-rule-group"),
            "properties": z.object({
              "vendorName": z.string().describe("#### Vendor name (e.g., `AWS` for AWS-managed rules).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```"),
              "excludedRules": z.array(z.string()).optional().describe("#### Rules within this group to skip (by rule name). Useful for disabling false positives.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n            excludedRules:\n              - SizeRestrictions_BODY\n              - GenericRFI_BODY\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0,\n          excludedRules: ['SizeRestrictions_BODY', 'GenericRFI_BODY']\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").optional(),
              "overrideAction": z.enum(["Count","None"]).optional().describe("#### `None` = apply normally, `Count` = log matches without blocking (dry-run mode).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n            overrideAction: Count\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0,\n          overrideAction: 'Count'\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").optional(),
              "priority": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Evaluation order. Lower = evaluated first. Must be unique across all rules.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesKnownBadInputsRuleSet\n            vendorName: AWS\n            priority: 1\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      },\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesKnownBadInputsRuleSet',\n          vendorName: 'AWS',\n          priority: 1\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```"),
              "name": z.string(),
              "disableMetrics": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch metrics for this rule.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n            disableMetrics: true\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0,\n          disableMetrics: true\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").default(false),
              "sampledRequestsEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Save samples of requests matching this rule for inspection in the WAF console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n            sampledRequestsEnabled: true\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0,\n          sampledRequestsEnabled: true\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").default(false) }).strict()
          }).strict()
          , z.object({
            "type": z.literal("custom-rule-group"),
            "properties": z.object({
              "arn": z.string().describe("#### ARN of the custom WAF rule group.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: custom-rule-group\n          properties:\n            name: CompanyRuleGroup\n            arn: arn:aws:wafv2:eu-west-1:123456789012:regional/rulegroup/company-rules/abcd1234-5678-90ef-ghij-klmnopqrstuv\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'custom-rule-group',\n        properties: {\n          name: 'CompanyRuleGroup',\n          arn: 'arn:aws:wafv2:eu-west-1:123456789012:regional/rulegroup/company-rules/abcd1234-5678-90ef-ghij-klmnopqrstuv',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```"),
              "overrideAction": z.enum(["Count","None"]).optional().describe("#### `None` = apply normally, `Count` = log matches without blocking (dry-run mode).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: custom-rule-group\n          properties:\n            name: CompanyRuleGroup\n            arn: arn:aws:wafv2:eu-west-1:123456789012:regional/rulegroup/company-rules/abcd1234-5678-90ef-ghij-klmnopqrstuv\n            priority: 0\n            overrideAction: Count\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'custom-rule-group',\n        properties: {\n          name: 'CompanyRuleGroup',\n          arn: 'arn:aws:wafv2:eu-west-1:123456789012:regional/rulegroup/company-rules/abcd1234-5678-90ef-ghij-klmnopqrstuv',\n          priority: 0,\n          overrideAction: 'Count'\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").optional(),
              "priority": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Evaluation order. Lower = evaluated first. Must be unique across all rules.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesKnownBadInputsRuleSet\n            vendorName: AWS\n            priority: 1\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      },\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesKnownBadInputsRuleSet',\n          vendorName: 'AWS',\n          priority: 1\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```"),
              "name": z.string(),
              "disableMetrics": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch metrics for this rule.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n            disableMetrics: true\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0,\n          disableMetrics: true\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").default(false),
              "sampledRequestsEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Save samples of requests matching this rule for inspection in the WAF console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n            sampledRequestsEnabled: true\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0,\n          sampledRequestsEnabled: true\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").default(false) }).strict()
          }).strict()
          , z.object({
            "type": z.literal("rate-based-rule"),
            "properties": z.object({
              "limit": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Max requests per IP in a 5-minute window. Range: 100–20,000,000. Exceeding triggers the `action`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: rate-based-rule\n          properties:\n            name: RateLimitPerIp\n            limit: 2000\n            aggregateBasedOn: IP\n            action: Block\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'rate-based-rule',\n        properties: {\n          name: 'RateLimitPerIp',\n          limit: 2000,\n          aggregateBasedOn: 'IP',\n          action: 'Block',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```"),
              "aggregateBasedOn": z.enum(["FORWARDED_IP","IP"]).optional().describe("#### `IP` = direct client IP, `FORWARDED_IP` = IP from a header (e.g., `X-Forwarded-For` behind a proxy).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: rate-based-rule\n          properties:\n            name: RateLimitForwarded\n            limit: 2000\n            aggregateBasedOn: FORWARDED_IP\n            forwardedIPConfig:\n              headerName: X-Forwarded-For\n              fallbackBehavior: NO_MATCH\n            action: Block\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'rate-based-rule',\n        properties: {\n          name: 'RateLimitForwarded',\n          limit: 2000,\n          aggregateBasedOn: 'FORWARDED_IP',\n          forwardedIPConfig: {\n            headerName: 'X-Forwarded-For',\n            fallbackBehavior: 'NO_MATCH'\n          },\n          action: 'Block',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").optional(),
              "forwardedIPConfig": z.object({
                "fallbackBehavior": z.enum(["MATCH","NO_MATCH"]).describe("#### What to do when the header is missing. `MATCH` = apply rule action, `NO_MATCH` = skip.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: rate-based-rule\n          properties:\n            name: RateLimitForwarded\n            limit: 2000\n            aggregateBasedOn: FORWARDED_IP\n            forwardedIPConfig:\n              headerName: X-Forwarded-For\n              fallbackBehavior: MATCH\n            action: Block\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'rate-based-rule',\n        properties: {\n          name: 'RateLimitForwarded',\n          limit: 2000,\n          aggregateBasedOn: 'FORWARDED_IP',\n          forwardedIPConfig: {\n            headerName: 'X-Forwarded-For',\n            fallbackBehavior: 'MATCH'\n          },\n          action: 'Block',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```"),
                "headerName": z.string().describe("#### HTTP header containing the client IP (e.g., `X-Forwarded-For`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: rate-based-rule\n          properties:\n            name: RateLimitForwarded\n            limit: 2000\n            aggregateBasedOn: FORWARDED_IP\n            forwardedIPConfig:\n              headerName: X-Forwarded-For\n              fallbackBehavior: NO_MATCH\n            action: Block\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'rate-based-rule',\n        properties: {\n          name: 'RateLimitForwarded',\n          limit: 2000,\n          aggregateBasedOn: 'FORWARDED_IP',\n          forwardedIPConfig: {\n            headerName: 'X-Forwarded-For',\n            fallbackBehavior: 'NO_MATCH'\n          },\n          action: 'Block',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```") }).strict()
              .optional().describe("#### Header and fallback settings when using `FORWARDED_IP` aggregation.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: rate-based-rule\n          properties:\n            name: RateLimitForwarded\n            limit: 2000\n            aggregateBasedOn: FORWARDED_IP\n            forwardedIPConfig:\n              headerName: X-Forwarded-For\n              fallbackBehavior: NO_MATCH\n            action: Block\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'rate-based-rule',\n        properties: {\n          name: 'RateLimitForwarded',\n          limit: 2000,\n          aggregateBasedOn: 'FORWARDED_IP',\n          forwardedIPConfig: {\n            headerName: 'X-Forwarded-For',\n            fallbackBehavior: 'NO_MATCH'\n          },\n          action: 'Block',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").optional(),
              "action": z.enum(["Allow","Block","Captcha","Challenge","Count"]).optional().describe("#### What to do when the rate limit is exceeded.\n\n---\n\n- `Block`: Return 403 (most common for rate limiting).\n- `Count`: Log only, don't block (useful for testing thresholds).\n- `Captcha`/`Challenge`: Verify the client is human.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: rate-based-rule\n          properties:\n            name: RateLimitPerIp\n            limit: 500\n            aggregateBasedOn: IP\n            action: Captcha\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'rate-based-rule',\n        properties: {\n          name: 'RateLimitPerIp',\n          limit: 500,\n          aggregateBasedOn: 'IP',\n          action: 'Captcha',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```"),
              "priority": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Evaluation order. Lower = evaluated first. Must be unique across all rules.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesKnownBadInputsRuleSet\n            vendorName: AWS\n            priority: 1\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      },\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesKnownBadInputsRuleSet',\n          vendorName: 'AWS',\n          priority: 1\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```"),
              "name": z.string(),
              "disableMetrics": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch metrics for this rule.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n            disableMetrics: true\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0,\n          disableMetrics: true\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").default(false),
              "sampledRequestsEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Save samples of requests matching this rule for inspection in the WAF console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n            sampledRequestsEnabled: true\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0,\n          sampledRequestsEnabled: true\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").default(false) }).strict()
          }).strict()
        ])).optional().describe("#### Firewall rules: managed rule groups (AWS presets), custom rule groups, or rate-based rules.\n\n---\n\nIf omitted, Stacktape uses `AWSManagedRulesCommonRuleSet` + `AWSManagedRulesKnownBadInputsRuleSet` by default.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesKnownBadInputsRuleSet\n            vendorName: AWS\n            priority: 1\n        - type: rate-based-rule\n          properties:\n            name: RateLimitPerIp\n            limit: 2000\n            aggregateBasedOn: IP\n            priority: 2\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      },\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesKnownBadInputsRuleSet',\n          vendorName: 'AWS',\n          priority: 1\n        }\n      },\n      {\n        type: 'rate-based-rule',\n        properties: {\n          name: 'RateLimitPerIp',\n          limit: 2000,\n          aggregateBasedOn: 'IP',\n          priority: 2\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").optional(),
        "customResponseBodies": z.record(z.string(), z.object({
          "contentType": z.string().describe("#### MIME type: `application/json`, `text/plain`, or `text/html`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  siteFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      defaultAction: Block\n      customResponseBodies:\n        blockedPage:\n          contentType: text/html\n          content: '<html><body><h1>403 Forbidden</h1></body></html>'\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n  siteService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: siteFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const siteFirewall = new WebAppFirewall({\n    scope: 'regional',\n    defaultAction: 'Block',\n    customResponseBodies: {\n      blockedPage: {\n        contentType: 'text/html',\n        content: '<html><body><h1>403 Forbidden</h1></body></html>'\n      }\n    },\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const siteService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'siteFirewall'\n  });\n\n  return { resources: { siteFirewall, siteService } };\n});\n```"),
          "content": z.string().describe("#### Response body content.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  siteFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      defaultAction: Block\n      customResponseBodies:\n        blockedPlain:\n          contentType: text/plain\n          content: 'Your request was blocked by our web application firewall.'\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n  siteService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: siteFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const siteFirewall = new WebAppFirewall({\n    scope: 'regional',\n    defaultAction: 'Block',\n    customResponseBodies: {\n      blockedPlain: {\n        contentType: 'text/plain',\n        content: 'Your request was blocked by our web application firewall.'\n      }\n    },\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const siteService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'siteFirewall'\n  });\n\n  return { resources: { siteFirewall, siteService } };\n});\n```") }).strict()
        ).optional().describe("#### Custom response bodies for `Block` actions. Map of key → content type + body.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      defaultAction: Block\n      customResponseBodies:\n        accessDenied:\n          contentType: application/json\n          content: '{\"error\":\"Access denied by web application firewall\"}'\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    defaultAction: 'Block',\n    customResponseBodies: {\n      accessDenied: {\n        contentType: 'application/json',\n        content: '{\"error\":\"Access denied by web application firewall\"}'\n      }\n    },\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").optional(),
        "captchaImmunityTime": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds a solved CAPTCHA stays valid before requiring re-verification.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  botFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      captchaImmunityTime: 600\n      rules:\n        - type: rate-based-rule\n          properties:\n            name: SuspiciousTraffic\n            limit: 500\n            aggregateBasedOn: IP\n            action: Captcha\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: botFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const botFirewall = new WebAppFirewall({\n    scope: 'regional',\n    captchaImmunityTime: 600,\n    rules: [\n      {\n        type: 'rate-based-rule',\n        properties: {\n          name: 'SuspiciousTraffic',\n          limit: 500,\n          aggregateBasedOn: 'IP',\n          action: 'Captcha',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'botFirewall'\n  });\n\n  return { resources: { botFirewall, apiService } };\n});\n```").default(300),
        "challengeImmunityTime": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds a solved challenge stays valid before requiring re-verification.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  botFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      challengeImmunityTime: 900\n      rules:\n        - type: rate-based-rule\n          properties:\n            name: SuspiciousTraffic\n            limit: 500\n            aggregateBasedOn: IP\n            action: Challenge\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: botFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const botFirewall = new WebAppFirewall({\n    scope: 'regional',\n    challengeImmunityTime: 900,\n    rules: [\n      {\n        type: 'rate-based-rule',\n        properties: {\n          name: 'SuspiciousTraffic',\n          limit: 500,\n          aggregateBasedOn: 'IP',\n          action: 'Challenge',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'botFirewall'\n  });\n\n  return { resources: { botFirewall, apiService } };\n});\n```").default(300),
        "tokenDomains": z.array(z.string()).optional().describe("#### Domains accepted in WAF tokens. Enables token sharing across multiple protected websites.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  edgeFirewall:\n    type: web-app-firewall\n    properties:\n      scope: cdn\n      tokenDomains:\n        - app.example.com\n        - admin.example.com\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n  web:\n    type: nextjs-web\n    properties:\n      appDirectory: ./\n      useFirewall: edgeFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, NextjsWeb, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const edgeFirewall = new WebAppFirewall({\n    scope: 'cdn',\n    tokenDomains: ['app.example.com', 'admin.example.com'],\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const web = new NextjsWeb({\n    appDirectory: './',\n    useFirewall: 'edgeFirewall'\n  });\n\n  return { resources: { edgeFirewall, web } };\n});\n```").optional(),
        "disableMetrics": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable CloudWatch metrics for the firewall.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      disableMetrics: true\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    disableMetrics: true,\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").default(false),
        "sampledRequestsEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Save samples of matched requests for inspection in the AWS WAF console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n      sampledRequestsEnabled: true\n      rules:\n        - type: managed-rule-group\n          properties:\n            name: AWSManagedRulesCommonRuleSet\n            vendorName: AWS\n            priority: 0\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: apiFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebAppFirewall, WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFirewall = new WebAppFirewall({\n    scope: 'regional',\n    sampledRequestsEnabled: true,\n    rules: [\n      {\n        type: 'managed-rule-group',\n        properties: {\n          name: 'AWSManagedRulesCommonRuleSet',\n          vendorName: 'AWS',\n          priority: 0\n        }\n      }\n    ]\n  });\n\n  const apiService = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 },\n    loadBalancing: { type: 'application-load-balancer' },\n    useFirewall: 'apiFirewall'\n  });\n\n  return { resources: { apiFirewall, apiService } };\n});\n```").default(false) }).strict()
      .optional().describe("").optional(),
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Protects your APIs and websites from common attacks (SQL injection, XSS, bots, DDoS).\n\n---\n\nAttach to an HTTP API Gateway, Application Load Balancer, or CDN. Comes with AWS-managed rule sets\nby default. Costs ~$5/month base + $1/million requests inspected."), z.object({
      "type": z.literal("web-service"),
      "properties": z.object({
        "cors": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable CORS. With no other options, uses permissive defaults (`*` origins, common headers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").default(false),
          "allowedOrigins": z.array(z.string()).optional().describe("#### Allowed origins (e.g., `https://myapp.com`). Use `*` for any origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n          - https://admin.example.com\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com', 'https://admin.example.com']\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").default(["*"]),
          "allowedHeaders": z.array(z.string()).optional().describe("#### Allowed request headers in CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        allowedHeaders:\n          - Content-Type\n          - Authorization\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      allowedHeaders: ['Content-Type', 'Authorization']\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
          "allowedMethods": z.array(z.enum(["*","DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### Allowed HTTP methods. Auto-detected from integrations if not set.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        allowedMethods:\n          - GET\n          - POST\n          - OPTIONS\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      allowedMethods: ['GET', 'POST', 'OPTIONS']\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
          "allowCredentials": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Allow cookies/auth headers in cross-origin requests.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        allowCredentials: true\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      allowCredentials: true\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
          "exposedResponseHeaders": z.array(z.string()).optional().describe("#### Response headers accessible to browser JavaScript.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        exposedResponseHeaders:\n          - X-Request-Id\n          - X-Total-Count\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      exposedResponseHeaders: ['X-Request-Id', 'X-Total-Count']\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional(),
          "maxAge": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How long (seconds) browsers can cache preflight responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiGateway:\n    type: http-api-gateway\n    properties:\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        maxAge: 3600\n  apiHandler:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/index.ts\n      memory: 512\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: apiGateway\n            path: /{proxy+}\n            method: '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, HttpApiIntegration, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiGateway = new HttpApiGateway({\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      maxAge: 3600\n    }\n  });\n  const apiHandler = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/index.ts' }),\n    memory: 512,\n    events: [new HttpApiIntegration({ httpApiGatewayName: 'apiGateway', path: '/{proxy+}', method: '*' })]\n  });\n  return { resources: { apiGateway, apiHandler } };\n});\n```").optional() }).strict()
        .optional().describe("#### CORS settings. Overrides any CORS headers from your application.\n\n---\n\nOnly works with `http-api-gateway` load balancing (the default).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      cors:\n        enabled: true\n        allowedOrigins:\n          - https://app.example.com\n        allowedMethods:\n          - GET\n          - POST\n        allowCredentials: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    cors: {\n      enabled: true,\n      allowedOrigins: ['https://app.example.com'],\n      allowedMethods: ['GET', 'POST'],\n      allowCredentials: true\n    }\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```").optional(),
        "customDomains": z.array(z.object({
          "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
          "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
          "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
        ).optional().describe("#### Custom domains (e.g., `api.example.com`). Stacktape auto-creates DNS records and TLS certificates.\n\n---\n\nYour domain must be added as a Route53 hosted zone in your AWS account first.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    customDomains: [{ domainName: 'api.example.com' }]\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```").optional(),
        "loadBalancing": z.union([z.object({
            "type": z.literal("http-api-gateway") }).strict()
          , z.object({
            "type": z.literal("application-load-balancer"),
            "properties": z.object({
              "healthcheckPath": z.string().optional().describe("#### Path the load balancer pings to check container health.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: application-load-balancer\n        properties:\n          healthcheckPath: /health\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'application-load-balancer',\n      properties: {\n        healthcheckPath: '/health'\n      }\n    }\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```"),
              "healthcheckInterval": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: application-load-balancer\n        properties:\n          healthcheckPath: /health\n          healthcheckInterval: 10\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'application-load-balancer',\n      properties: {\n        healthcheckPath: '/health',\n        healthcheckInterval: 10\n      }\n    }\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```").default(5),
              "healthcheckTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a health check is considered failed.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: application-load-balancer\n        properties:\n          healthcheckPath: /health\n          healthcheckInterval: 10\n          healthcheckTimeout: 5\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'application-load-balancer',\n      properties: {\n        healthcheckPath: '/health',\n        healthcheckInterval: 10,\n        healthcheckTimeout: 5\n      }\n    }\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```").default(4) }).strict()
            .optional().describe("").optional() }).strict()
          , z.object({
            "type": z.literal("network-load-balancer"),
            "properties": z.object({
              "healthcheckPath": z.string().optional().describe("#### Health check path (only used when `healthCheckProtocol` is `HTTP`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: network-load-balancer\n        properties:\n          healthCheckProtocol: HTTP\n          healthcheckPath: /health\n          ports:\n            - port: 443\n              containerPort: 8080\n              protocol: TLS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tcpService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'network-load-balancer',\n      properties: {\n        healthCheckProtocol: 'HTTP',\n        healthcheckPath: '/health',\n        ports: [{ port: 443, containerPort: 8080, protocol: 'TLS' }]\n      }\n    }\n  });\n\n  return {\n    resources: { tcpService }\n  };\n});\n```").default("/"),
              "healthcheckInterval": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks (5-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: network-load-balancer\n        properties:\n          healthcheckInterval: 30\n          ports:\n            - port: 443\n              containerPort: 8080\n              protocol: TLS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tcpService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'network-load-balancer',\n      properties: {\n        healthcheckInterval: 30,\n        ports: [{ port: 443, containerPort: 8080, protocol: 'TLS' }]\n      }\n    }\n  });\n\n  return {\n    resources: { tcpService }\n  };\n});\n```").default(5),
              "healthcheckTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a health check is considered failed (2-120).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: network-load-balancer\n        properties:\n          healthcheckInterval: 30\n          healthcheckTimeout: 10\n          ports:\n            - port: 443\n              containerPort: 8080\n              protocol: TLS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tcpService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'network-load-balancer',\n      properties: {\n        healthcheckInterval: 30,\n        healthcheckTimeout: 10,\n        ports: [{ port: 443, containerPort: 8080, protocol: 'TLS' }]\n      }\n    }\n  });\n\n  return {\n    resources: { tcpService }\n  };\n});\n```").default(4),
              "healthCheckProtocol": z.enum(["HTTP","TCP"]).optional().describe("#### Health check protocol: `TCP` (port check) or `HTTP` (path check).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: network-load-balancer\n        properties:\n          healthCheckProtocol: HTTP\n          healthcheckPath: /health\n          ports:\n            - port: 443\n              containerPort: 8080\n              protocol: TLS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tcpService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'network-load-balancer',\n      properties: {\n        healthCheckProtocol: 'HTTP',\n        healthcheckPath: '/health',\n        ports: [{ port: 443, containerPort: 8080, protocol: 'TLS' }]\n      }\n    }\n  });\n\n  return {\n    resources: { tcpService }\n  };\n});\n```").default("TCP"),
              "healthCheckPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Health check port. Defaults to the traffic port.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: network-load-balancer\n        properties:\n          healthCheckProtocol: TCP\n          healthCheckPort: 8081\n          ports:\n            - port: 443\n              containerPort: 8080\n              protocol: TLS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tcpService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'network-load-balancer',\n      properties: {\n        healthCheckProtocol: 'TCP',\n        healthCheckPort: 8081,\n        ports: [{ port: 443, containerPort: 8080, protocol: 'TLS' }]\n      }\n    }\n  });\n\n  return {\n    resources: { tcpService }\n  };\n});\n```").optional(),
              "ports": z.array(z.object({
                "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Public port exposed by the load balancer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: network-load-balancer\n        properties:\n          ports:\n            - port: 443\n              containerPort: 8080\n              protocol: TLS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tcpService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'network-load-balancer',\n      properties: {\n        ports: [\n          {\n            port: 443,\n            containerPort: 8080,\n            protocol: 'TLS'\n          }\n        ]\n      }\n    }\n  });\n\n  return {\n    resources: { tcpService }\n  };\n});\n```"),
                "protocol": z.enum(["TCP","TLS"]).optional().describe("#### Protocol: `TLS` (encrypted) or `TCP` (raw).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: network-load-balancer\n        properties:\n          ports:\n            - port: 8883\n              containerPort: 1883\n              protocol: TCP\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tcpService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'network-load-balancer',\n      properties: {\n        ports: [\n          {\n            port: 8883,\n            containerPort: 1883,\n            protocol: 'TCP'\n          }\n        ]\n      }\n    }\n  });\n\n  return {\n    resources: { tcpService }\n  };\n});\n```").default("TLS"),
                "containerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port on the container that receives the traffic. Defaults to `port`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  tcpService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: network-load-balancer\n        properties:\n          ports:\n            - port: 443\n              protocol: TLS\n              containerPort: 8080\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const tcpService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'network-load-balancer',\n      properties: {\n        ports: [\n          {\n            port: 443,\n            protocol: 'TLS',\n            containerPort: 8080\n          }\n        ]\n      }\n    }\n  });\n\n  return {\n    resources: { tcpService }\n  };\n});\n```").optional() }).strict()
              ) }).strict()
          }).strict()
        ]).optional().describe("#### How traffic reaches your containers. Affects pricing, features, and protocol support.\n\n---\n\n- **`http-api-gateway`** (default): Pay-per-request (~$1/million requests). Best for most apps.\n  Cheapest at low traffic, but costs grow with volume.\n\n- **`application-load-balancer`**: Flat ~$18/month + usage. Required for gradual deployments\n  (`deployment`), top-level firewalls (`useFirewall`), and WebSocket support.\n  More cost-effective above ~500k requests/day. AWS Free Tier eligible.\n\n- **`network-load-balancer`**: For non-HTTP traffic (TCP/TLS) like MQTT, game servers, or custom protocols.\n  Requires explicit `ports` configuration. Does not support CDN, top-level firewall, or gradual deployments.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'application-load-balancer'\n    }\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```").optional(),
        "cdn": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Enable CDN (CloudFront) for faster global delivery and lower bandwidth costs.\n\n---\n\nCaches responses at edge locations worldwide so users get content from the nearest server.\nThe CDN itself has no monthly fee — you pay per request (~$0.01/10k) and per GB transferred.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "cachingOptions": z.object({
            "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
            "cacheKeyParameters": z.object({
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control how long and what gets cached at the CDN edge.\n\n---\n\nWhen the origin response has no `Cache-Control` header, defaults apply:\n- **Bucket origins**: cached for 6 months (or until invalidated on deploy).\n- **API Gateway / Load Balancer origins**: not cached.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400,\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "forwardingOptions": z.object({
            "customRequestHeaders": z.array(z.object({
              "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
              "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
            ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "cookies": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "headers": z.object({
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "queryString": z.object({
              "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
            "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Control which headers, cookies, and query params are forwarded to your origin.\n\n---\n\nBy default, all headers/cookies/query params are forwarded. Use this to restrict\nwhat reaches your app (e.g., strip cookies for static content).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { none: true }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "routeRewrites": z.array(z.object({
            "path": z.string().describe("#### URL path pattern to match (e.g., `/api/*`, `*.jpg`, `/docs/v2/*`). Wildcards supported.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
            "routePrefix": z.string().optional().describe("#### Prepend a path prefix to requests before forwarding to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routePrefix: /v2\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routePrefix: '/v2',\n        routeTo: { type: 'http-api-gateway', properties: { httpApiGatewayName: 'backendApi' } }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, api } };\n});\n```").optional(),
            "routeTo": z.union([z.object({
                "type": z.literal("application-load-balancer"),
                "properties": z.object({
                  "loadBalancerName": z.string().describe("#### Name of the `application-load-balancer` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```"),
                  "listenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Listener port on the load balancer. Only needed if using custom listeners.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              listenerPort: 8080\nappLb:\n  type: application-load-balancer\n  properties:\n    listeners:\n      - port: 8080\n        protocol: HTTP\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({\n  listeners: [{ port: 8080, protocol: 'HTTP' }]\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            listenerPort: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional(),
                  "originDomainName": z.string().optional().describe("#### Explicit origin domain. Only needed if the ALB has no `customDomains` and uses `customCertificateArns`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /app/*\n          routeTo:\n            type: application-load-balancer\n            properties:\n              loadBalancerName: appLb\n              originDomainName: app.internal.example.com\nappLb:\n  type: application-load-balancer\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst appLb = new ApplicationLoadBalancer({});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/app/*',\n        routeTo: {\n          type: 'application-load-balancer',\n          properties: {\n            loadBalancerName: 'appLb',\n            originDomainName: 'app.internal.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { appLb, api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("http-api-gateway"),
                "properties": z.object({
                  "httpApiGatewayName": z.string().describe("#### Name of the `http-api-gateway` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnApi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /api/*\n          routeTo:\n            type: http-api-gateway\n            properties:\n              httpApiGatewayName: backendApi\nbackendApi:\n  type: http-api-gateway\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst backendApi = new HttpApiGateway({});\nconst cdnApi = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/api/*',\n        routeTo: {\n          type: 'http-api-gateway',\n          properties: {\n            httpApiGatewayName: 'backendApi'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { backendApi, cdnApi } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("function"),
                "properties": z.object({
                  "functionName": z.string().describe("#### Name of the `function` resource to route to. The function must have `url.enabled: true`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /render/*\n          routeTo:\n            type: function\n            properties:\n              functionName: renderer\nrenderer:\n  type: function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/render.ts\n    url:\n      enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst renderer = new LambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/render.ts' }),\n  url: { enabled: true }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/render/*',\n        routeTo: {\n          type: 'function',\n          properties: {\n            functionName: 'renderer'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { renderer, api } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("custom-origin"),
                "properties": z.object({
                  "domainName": z.string().describe("#### Domain name of the external origin (e.g., `api.example.com`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "protocol": z.enum(["HTTP","HTTPS"]).optional().describe("#### Protocol for connecting to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTPS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTPS'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                  "port": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Port on the origin. Defaults to 443 for HTTPS, 80 for HTTP.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /legacy/*\n          routeTo:\n            type: custom-origin\n            properties:\n              domainName: api.example.com\n              protocol: HTTP\n              port: 8080\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/legacy/*',\n        routeTo: {\n          type: 'custom-origin',\n          properties: {\n            domainName: 'api.example.com',\n            protocol: 'HTTP',\n            port: 8080\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("bucket"),
                "properties": z.object({
                  "bucketName": z.string().describe("#### Name of the `bucket` resource to route to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets'\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```"),
                  "disableUrlNormalization": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable clean URL normalization (e.g., `/about` → `/about.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n              disableUrlNormalization: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: {\n          type: 'bucket',\n          properties: {\n            bucketName: 'assets',\n            disableUrlNormalization: true\n          }\n        }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").default(false) }).strict()
              }).strict()
            ]).optional().describe("#### Where to send matching requests. Can be a bucket, API Gateway, load balancer, Lambda, or external domain.\n\n---\n\nIf not set, requests go to the default origin (the resource this CDN is attached to).\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /static/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/static/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "cachingOptions": z.object({
              "cacheMethods": z.array(z.enum(["GET","HEAD","OPTIONS"])).optional().describe("#### HTTP methods to cache. Use `['GET', 'HEAD', 'OPTIONS']` if your API uses CORS preflight.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheMethods: ['GET', 'HEAD', 'OPTIONS']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "minTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum cache time (seconds). Overrides `Cache-Control: max-age` if the origin sets a lower value.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        minTTL: 60\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      minTTL: 60\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "maxTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum cache time (seconds). Caps how long the CDN caches content, even if the origin says longer.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        maxTTL: 31536000\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      maxTTL: 31536000\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "defaultTTL": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Default cache time (seconds). Used when the origin response has no `Cache-Control` or `Expires` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        defaultTTL: 86400\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      defaultTTL: 86400\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "disableCompression": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable automatic Gzip/Brotli compression. Compression is on by default and reduces transfer size/cost.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        disableCompression: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      disableCompression: true\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").default(false),
              "cacheKeyParameters": z.object({
                "cookies": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          whitelist: ['sessionId']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "allExcept": z.array(z.string()).optional().describe("#### All cookies except the listed ones are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            allExcept:\n              - tracking\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          allExcept: ['tracking']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which cookies to include in the cache key. Different cookie values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          cookies:\n            whitelist:\n              - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        cookies: { whitelist: ['sessionId'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "headers": z.object({
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: {\n          whitelist: ['Authorization']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which headers to include in the cache key. Different header values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "queryString": z.object({
                  "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          all: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          none: true\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                  "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are included in the cache key.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: {\n          whitelist: ['page']\n        }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
                .optional().describe("#### Which query params to include in the cache key. Different param values = different cached responses.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - lang\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['lang'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers, cookies, and query params make responses unique in the cache.\n\n---\n\nDefaults: **Bucket** = URL path only. **API/ALB** = URL path + query string.\nValues included in the cache key are always forwarded to the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cacheKeyParameters:\n          queryString:\n            whitelist:\n              - page\n          headers:\n            whitelist:\n              - Authorization\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cacheKeyParameters: {\n        queryString: { whitelist: ['page'] },\n        headers: { whitelist: ['Authorization'] }\n      }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cachePolicyId": z.string().optional().describe("#### Use a pre-existing AWS cache policy ID instead of configuring TTL and cache key options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      cachingOptions:\n        cachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    cachingOptions: {\n      cachePolicyId: '658327ea-f89d-4fab-a63d-7e88639e58f6'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override caching behavior for requests matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          cachingOptions:\n            defaultTTL: 604800\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        cachingOptions: { defaultTTL: 604800 }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "forwardingOptions": z.object({
              "customRequestHeaders": z.array(z.object({
                "headerName": z.string().describe("#### Name of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```"),
                "value": z.string().describe("#### Value of the header\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Api-Key\n            value: $Secret('cdn-origin-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [\n        {\n          headerName: 'X-Api-Key',\n          value: $Secret('cdn-origin-key')\n        }\n      ]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```") }).strict()
              ).optional().describe("#### Static headers the CDN adds to every request sent to the origin (e.g., API keys, custom identifiers).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        customRequestHeaders:\n          - headerName: X-Forwarded-By\n            value: stacktape-cdn\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      customRequestHeaders: [{ headerName: 'X-Forwarded-By', value: 'stacktape-cdn' }]\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "allowedMethods": z.array(z.enum(["DELETE","GET","HEAD","OPTIONS","PATCH","POST","PUT"])).optional().describe("#### HTTP methods forwarded to the origin. Default: all methods.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        allowedMethods:\n          - GET\n          - HEAD\n          - OPTIONS\n          - POST\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      allowedMethods: ['GET', 'HEAD', 'OPTIONS', 'POST']\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "cookies": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    whitelist: ['sessionId']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All cookies are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { cookies: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which cookies to forward to the origin. Default: all cookies.\n\n---\n\nCookies in the cache key (see `cachingOptions`) are always forwarded regardless of this setting.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        cookies:\n          whitelist:\n            - sessionId\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      cookies: { whitelist: ['sessionId'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "headers": z.object({
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed headers are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    whitelist: ['User-Agent']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewer": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Forward all headers from the viewer's request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewer: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewer: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allViewerAndWhitelistCloudFront": z.array(z.string()).optional().describe("#### Forward all viewer headers plus the listed CloudFront-specific headers (e.g., `CloudFront-Viewer-Country`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allViewerAndWhitelistCloudFront:\n            - CloudFront-Viewer-Country\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allViewerAndWhitelistCloudFront: ['CloudFront-Viewer-Country']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "allExcept": z.array(z.string()).optional().describe("#### Forward all viewer headers except the listed ones.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          allExcept:\n            - Host\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { headers: {\n    allExcept: ['Host']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which headers to forward to the origin. Default: all headers.\n\n---\n\n> The `Authorization` header must be added to `cachingOptions.cacheKeyParameters` to be forwarded.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        headers:\n          whitelist:\n            - User-Agent\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      headers: { whitelist: ['User-Agent'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "queryString": z.object({
                "all": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### All query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          all: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    all: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "none": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### No query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          none: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    none: true\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional(),
                "whitelist": z.array(z.string()).optional().describe("#### Only the listed query parameters are forwarded to the origin.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: { enabled: true, forwardingOptions: { queryString: {\n    whitelist: ['page']\n  } } }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
              .optional().describe("#### Which query params to forward to the origin. Default: all query params.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        queryString:\n          whitelist:\n            - page\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      queryString: { whitelist: ['page'] }\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
              "originRequestPolicyId": z.string().optional().describe("#### Use a pre-existing AWS origin request policy ID instead of configuring forwarding options here.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      forwardingOptions:\n        originRequestPolicyId: 88a5eaf4-2fd4-4709-b370-b4c650ea3fcf\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    forwardingOptions: {\n      originRequestPolicyId: '88a5eaf4-2fd4-4709-b370-b4c650ea3fcf'\n    }\n  }\n});\nreturn { resources: { api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Override which headers, cookies, and query params are forwarded for this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          forwardingOptions:\n            cookies:\n              none: true\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        forwardingOptions: { cookies: { none: true } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
            "edgeFunctions": z.object({
              "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
              "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
              "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
              "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
            .optional().describe("#### Run edge functions on requests/responses matching this route.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /private/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\n          edgeFunctions:\n            onRequest: authAtEdge\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/private/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } },\n        edgeFunctions: { onRequest: 'authAtEdge' }\n      }\n    ]\n  }\n});\nreturn { resources: { authAtEdge, assets, api } };\n});\n```").optional() }).strict()
          ).optional().describe("#### Route specific URL patterns to different origins (e.g., `/api/*` → Lambda, `/assets/*` → S3).\n\n---\n\nEvaluated in order; first match wins. Unmatched requests go to the default origin.\nEach route can have its own caching and forwarding settings.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      routeRewrites:\n        - path: /assets/*\n          routeTo:\n            type: bucket\n            properties:\n              bucketName: assets\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' }\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    routeRewrites: [\n      {\n        path: '/assets/*',\n        routeTo: { type: 'bucket', properties: { bucketName: 'assets' } }\n      }\n    ]\n  }\n});\nreturn { resources: { assets, api } };\n});\n```").optional(),
          "customDomains": z.array(z.object({
            "domainName": z.string().describe("#### Your domain name (e.g., `mydomain.com` or `api.mydomain.com`).\n\n---\n\nDon't include the protocol (`https://`). The domain must have a Route53 hosted zone\nin your AWS account, with your registrar's nameservers pointing to it.\n\nStacktape automatically creates a DNS record and provisions a free TLS certificate.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      { domainName: 'api.example.com' }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```"),
            "customCertificateArn": z.string().optional().describe("#### Use your own TLS certificate instead of the auto-generated one.\n\n---\n\nProvide the ARN of an ACM certificate from your AWS account.\nOnly needed if you have specific certificate requirements (e.g., EV/OV certs).\nBy default, Stacktape provisions and renews free certificates automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          customCertificateArn: arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        customCertificateArn:\n          'arn:aws:acm:us-east-1:123456789012:certificate/abcd1234-ef56-7890-abcd-ef1234567890'\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").optional(),
            "disableDnsRecordCreation": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip DNS record creation for this domain.\n\n---\n\nSet to `true` if you manage DNS records yourself (e.g., through Cloudflare or another DNS provider).\nStacktape will still provision the TLS certificate but won't touch your DNS.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      customDomains:\n        - domainName: api.example.com\n          disableDnsRecordCreation: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    customDomains: [\n      {\n        domainName: 'api.example.com',\n        disableDnsRecordCreation: true\n      }\n    ]\n  });\n\n  return { resources: { webApi } };\n});\n```").default(false) }).strict()
          ).optional().describe("#### Custom domains (e.g., `cdn.example.com`). Stacktape auto-creates DNS records and TLS certificates.\n\n---\n\nYour domain must be added as a Route53 hosted zone in your AWS account first.\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      customDomains:\n        - domainName: cdn.example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    customDomains: [{ domainName: 'cdn.example.com' }]\n  }\n});\nreturn { resources: { assets } };\n});\n```").optional(),
          "edgeFunctions": z.object({
            "onRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run when the CDN receives a request (before cache lookup).\n\n---\n\nUse to modify the request, add auth checks, or return an immediate response without hitting the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
            "onResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before returning the response to the client.\n\n---\n\nUse to modify response headers, add security headers, or set cookies.\nDoes not run if the origin returned a 400+ error or if `onRequest` already generated a response.\n\n**Example (YAML):**\n\n```yaml\nresources:\nsecurityHeaders:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/headers.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onResponse: securityHeaders\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst securityHeaders = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/headers.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onResponse: 'securityHeaders'\n    }\n  }\n});\nreturn { resources: { securityHeaders, api } };\n});\n```").optional(),
            "onOriginRequest": z.string().optional().describe("#### Name of an `edge-lambda-function` to run before forwarding to the origin (after cache miss).\n\n---\n\nOnly runs on cache misses. Use to modify the request before it reaches your origin.\n\n> **Warning:** Stacktape uses this trigger internally for bucket/web-service/API Gateway CDN setups.\n> Overriding it may break default behavior. Only use if you know what you're doing.\n\n**Example (YAML):**\n\n```yaml\nresources:\nrewriteOrigin:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/origin-rewrite.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginRequest: rewriteOrigin\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst rewriteOrigin = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/origin-rewrite.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginRequest: 'rewriteOrigin'\n    }\n  }\n});\nreturn { resources: { rewriteOrigin, api } };\n});\n```").optional(),
            "onOriginResponse": z.string().optional().describe("#### Name of an `edge-lambda-function` to run after the origin responds (before caching).\n\n---\n\nModify the response before it's cached and returned. Changes are cached as if they came from the origin.\n\n**Example (YAML):**\n\n```yaml\nresources:\ncacheTuner:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/cache-tune.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onOriginResponse: cacheTuner\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cacheTuner = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/cache-tune.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onOriginResponse: 'cacheTuner'\n    }\n  }\n});\nreturn { resources: { cacheTuner, api } };\n});\n```").optional() }).strict()
          .optional().describe("#### Run edge functions on CDN requests/responses (URL rewrites, auth, A/B testing).\n\n---\n\n- `onRequest`: Before cache lookup — modify the request, add auth, or return early.\n- `onResponse`: Before returning to the client — modify headers, add cookies.\n\n**Example (YAML):**\n\n```yaml\nresources:\nauthAtEdge:\n  type: edge-lambda-function\n  properties:\n    packaging:\n      type: stacktape-lambda-buildpack\n      properties:\n        entryfilePath: ./src/edge/auth.ts\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      edgeFunctions:\n        onRequest: authAtEdge\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { EdgeLambdaFunction, HttpApiGateway, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst authAtEdge = new EdgeLambdaFunction({\n  packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/edge/auth.ts' })\n});\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    edgeFunctions: {\n      onRequest: 'authAtEdge'\n    }\n  }\n});\nreturn { resources: { authAtEdge, api } };\n});\n```").optional(),
          "cloudfrontPriceClass": z.enum(["PriceClass_100","PriceClass_200","PriceClass_All"]).optional().describe("#### Which regions the CDN serves from. Fewer regions = lower cost, but slower for distant users.\n\n---\n\n- **`PriceClass_100`**: North America + Europe. Cheapest option. Good if your users are in the US/EU.\n- **`PriceClass_200`**: Adds Asia, Middle East, Africa.\n- **`PriceClass_All`** (default): All regions worldwide, including South America and Oceania.\n\nThe CDN itself has no monthly base cost - you only pay per request and per GB transferred.\nThe price class controls which edge locations are used, and some regions cost more per request.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      cloudfrontPriceClass: PriceClass_100\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    cloudfrontPriceClass: 'PriceClass_100'\n  }\n});\nreturn { resources: { assets } };\n});\n```"),
          "defaultRoutePrefix": z.string().optional().describe("#### Prepend a path prefix to all requests forwarded to the origin.\n\n---\n\nE.g., with prefix `/v2`, a request for `/users` is forwarded to the origin as `/v2/users`.\n\n**Example (YAML):**\n\n```yaml\nresources:\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      defaultRoutePrefix: /v2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    defaultRoutePrefix: '/v2'\n  }\n});\nreturn { resources: { api } };\n});\n```").optional(),
          "errorDocument": z.string().optional().describe("#### Page to show for 404 errors (e.g., `/error.html`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      errorDocument: /error.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    errorDocument: '/error.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/404.html"),
          "indexDocument": z.string().optional().describe("#### Page served for requests to `/`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\nwebapp:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./dist\n    cdn:\n      enabled: true\n      indexDocument: /index.html\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst webapp = new Bucket({\n  directoryUpload: { directoryPath: './dist' },\n  cdn: {\n    enabled: true,\n    indexDocument: '/index.html'\n  }\n});\nreturn { resources: { webapp } };\n});\n```").default("/index.html"),
          "disableInvalidationAfterDeploy": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip clearing the CDN cache after each deploy.\n\n---\n\nBy default, all cached content is flushed on every deploy so users see the latest version.\nSet to `true` if you manage cache invalidation yourself or want to keep cached content between deploys.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\nassets:\n  type: bucket\n  properties:\n    directoryUpload:\n      directoryPath: ./public\n    cdn:\n      enabled: true\n      disableInvalidationAfterDeploy: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { Bucket, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst assets = new Bucket({\n  directoryUpload: { directoryPath: './public' },\n  cdn: {\n    enabled: true,\n    disableInvalidationAfterDeploy: true\n  }\n});\nreturn { resources: { assets } };\n});\n```").default(false),
          "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this CDN from common web exploits.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\ncdnFirewall:\n  type: web-app-firewall\n  properties:\n    scope: cdn\napi:\n  type: http-api-gateway\n  properties:\n    cdn:\n      enabled: true\n      useFirewall: cdnFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, WebAppFirewall, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\nconst cdnFirewall = new WebAppFirewall({ scope: 'cdn' });\nconst api = new HttpApiGateway({\n  cdn: {\n    enabled: true,\n    useFirewall: 'cdnFirewall'\n  }\n});\nreturn { resources: { cdnFirewall, api } };\n});\n```").optional() }).strict()
        .optional().describe("#### Put a CDN (CloudFront) in front of this service for caching and lower latency worldwide.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      cdn:\n        enabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    cdn: {\n      enabled: true\n    }\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```").optional(),
        "alarms": z.array(z.union([z.object({
            "trigger": z.union([z.object({
                "type": z.literal("application-load-balancer-custom"),
                "properties": z.object({
                  "metric": z.enum(["ActiveConnectionCount","AnomalousHostCount","ClientTLSNegotiationErrorCount","ConsumedLCUs","DesyncMitigationMode_NonCompliant_Request_Count","DroppedInvalidHeaderRequestCount","ELBAuthError","ELBAuthFailure","ELBAuthLatency","ELBAuthRefreshTokenSuccess","ELBAuthSuccess","ELBAuthUserClaimsSizeExceeded","ForwardedInvalidHeaderRequestCount","GrpcRequestCount","HTTPCode_ELB_3XX_Count","HTTPCode_ELB_4XX_Count","HTTPCode_ELB_500_Count","HTTPCode_ELB_502_Count","HTTPCode_ELB_503_Count","HTTPCode_ELB_504_Count","HTTPCode_ELB_5XX_Count","HTTPCode_Target_2XX_Count","HTTPCode_Target_3XX_Count","HTTPCode_Target_4XX_Count","HTTPCode_Target_5XX_Count","HTTP_Fixed_Response_Count","HTTP_Redirect_Count","HTTP_Redirect_Url_Limit_Exceeded_Count","HealthyHostCount","HealthyStateDNS","HealthyStateRouting","IPv6ProcessedBytes","IPv6RequestCount","LambdaInternalError","LambdaTargetProcessedBytes","LambdaUserError","MitigatedHostCount","NewConnectionCount","NonStickyRequestCount","ProcessedBytes","RejectedConnectionCount","RequestCount","RequestCountPerTarget","RuleEvaluations","TargetConnectionErrorCount","TargetResponseTime","TargetTLSNegotiationErrorCount","UnHealthyHostCount","UnhealthyRoutingRequestCount","UnhealthyStateDNS","UnhealthyStateRouting"]).describe("#### The metric to monitor on the Load Balancer.\n\n---\n\nThe threshold will be compared against the calculated value of `statistic(METRIC)`, where:\n- `statistic` is the function applied to the metric values collected during the evaluation period (default: `avg`).\n- `METRIC` is the chosen metric.\n\n**Available Metrics:**\n\n- `ActiveConnectionCount`: The total number of concurrent TCP connections active from clients to the load balancer and from the load balancer to targets.\n- `AnomalousHostCount`: The number of hosts detected with anomalies.\n- `ClientTLSNegotiationErrorCount`: The number of TLS connections initiated by the client that did not establish a session with the load balancer due to a TLS error.\n- `ConsumedLCUs`: The number of load balancer capacity units (LCU) used by your load balancer.\n- `DesyncMitigationMode_NonCompliant_Request_Count`: The number of requests that do not comply with RFC 7230.\n- `DroppedInvalidHeaderRequestCount`: The number of requests where the load balancer removed HTTP headers with invalid fields before routing the request.\n- `MitigatedHostCount`: The number of targets under mitigation.\n- `ForwardedInvalidHeaderRequestCount`: The number of requests routed by the load balancer that had HTTP headers with invalid fields.\n- `GrpcRequestCount`: The number of gRPC requests processed over IPv4 and IPv6.\n- `HTTP_Fixed_Response_Count`: The number of successful fixed-response actions.\n- `HTTP_Redirect_Count`: The number of successful redirect actions.\n- `HTTP_Redirect_Url_Limit_Exceeded_Count`: The number of redirect actions that failed because the URL in the response location header exceeded 8K.\n- `HTTPCode_ELB_3XX_Count`: The number of HTTP 3XX redirection codes originating from the load balancer.\n- `HTTPCode_ELB_4XX_Count`: The number of HTTP 4XX client error codes originating from the load balancer.\n- `HTTPCode_ELB_5XX_Count`: The number of HTTP 5XX server error codes originating from the load balancer.\n- `HTTPCode_ELB_500_Count`: The number of HTTP 500 error codes originating from the load balancer.\n- `HTTPCode_ELB_502_Count`: The number of HTTP 502 error codes originating from the load balancer.\n- `HTTPCode_ELB_503_Count`: The number of HTTP 503 error codes originating from the load balancer.\n- `HTTPCode_ELB_504_Count`: The number of HTTP 504 error codes originating from the load balancer.\n- `IPv6ProcessedBytes`: The total number of bytes processed by the load balancer over IPv6.\n- `IPv6RequestCount`: The number of IPv6 requests received by the load balancer.\n- `NewConnectionCount`: The total number of new TCP connections established from clients to the load balancer and from the load balancer to targets.\n- `NonStickyRequestCount`: The number of requests where the load balancer chose a new target because it could not use an existing sticky session.\n- `ProcessedBytes`: The total number of bytes processed by the load balancer over IPv4 and IPv6.\n- `RejectedConnectionCount`: The number of connections rejected because the load balancer reached its maximum number of connections.\n- `RequestCount`: The number of requests processed over IPv4 and IPv6.\n- `RuleEvaluations`: The number of rules processed by the load balancer, averaged over an hour.\n- `HealthyHostCount`: The number of targets that are considered healthy.\n- `HTTPCode_Target_2XX_Count`: The number of HTTP 2XX response codes generated by the targets.\n- `HTTPCode_Target_3XX_Count`: The number of HTTP 3XX response codes generated by the targets.\n- `HTTPCode_Target_4XX_Count`: The number of HTTP 4XX response codes generated by the targets.\n- `HTTPCode_Target_5XX_Count`: The number of HTTP 5XX response codes generated by the targets.\n- `RequestCountPerTarget`: The average number of requests per target in a target group.\n- `TargetConnectionErrorCount`: The number of connections that were not successfully established between the load balancer and a target.\n- `TargetResponseTime`: The time elapsed (in seconds) from when a request leaves the load balancer until the target starts sending response headers.\n- `TargetTLSNegotiationErrorCount`: The number of TLS connections initiated by the load balancer that did not establish a session with the target.\n- `UnHealthyHostCount`: The number of targets that are considered unhealthy.\n- `HealthyStateDNS`: The number of zones that meet the DNS healthy state requirements.\n- `HealthyStateRouting`: The number of zones that meet the routing healthy state requirements.\n- `UnhealthyRoutingRequestCount`: The number of requests routed using the routing failover action (fail open).\n- `UnhealthyStateDNS`: The number of zones that do not meet the DNS healthy state requirements.\n- `UnhealthyStateRouting`: The number of zones that do not meet the routing healthy state requirements.\n- `LambdaInternalError`: The number of requests to a Lambda function that failed due to an issue internal to the load balancer or AWS Lambda.\n- `LambdaTargetProcessedBytes`: The total number of bytes processed by the load balancer for requests to and responses from a Lambda function.\n- `LambdaUserError`: The number of requests to a Lambda function that failed due to an issue with the Lambda function itself.\n- `ELBAuthError`: The number of user authentications that could not be completed due to an internal error.\n- `ELBAuthFailure`: The number of user authentications that could not be completed because the IdP denied access.\n- `ELBAuthLatency`: The time elapsed (in milliseconds) to query the IdP for the ID token and user info.\n- `ELBAuthRefreshTokenSuccess`: The number of times the load balancer successfully refreshed user claims using a refresh token.\n- `ELBAuthSuccess`: The number of successful authentication actions.\n- `ELBAuthUserClaimsSizeExceeded`: The number of times a configured IdP returned user claims that exceeded 11K bytes in size.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n  publicLb:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Too many 5xx errors returned by the load balancer\n          trigger:\n            type: application-load-balancer-custom\n            properties:\n              metric: HTTPCode_ELB_5XX_Count\n              threshold: 50\n              statistic: sum\n              comparisonOperator: GreaterThanThreshold\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, ApplicationLoadBalancer, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  const publicLb = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Too many 5xx errors returned by the load balancer',\n        trigger: {\n          type: 'application-load-balancer-custom',\n          properties: {\n            metric: 'HTTPCode_ELB_5XX_Count',\n            threshold: 50,\n            statistic: 'sum',\n            comparisonOperator: 'GreaterThanThreshold'\n          }\n        },\n        evaluation: { period: 60, evaluationPeriods: 5, breachedPeriods: 3 },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { webApi, publicLb } };\n});\n```"),
                  "threshold": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### The threshold that triggers the alarm.\n\n---\n\nThe threshold is compared against the calculated value of `statistic(METRIC)`, where:\n- `statistic` is the function applied to the metric values collected during the evaluation period (default: `avg`).\n- `METRIC` is the chosen metric.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  webApi:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/index.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n  publicLb:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Target response time is too high\n          trigger:\n            type: application-load-balancer-custom\n            properties:\n              metric: TargetResponseTime\n              threshold: 2\n              statistic: p95\n              comparisonOperator: GreaterThanThreshold\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, ApplicationLoadBalancer, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const webApi = new WebService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/index.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  const publicLb = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Target response time is too high',\n        trigger: {\n          type: 'application-load-balancer-custom',\n          properties: {\n            metric: 'TargetResponseTime',\n            threshold: 2,\n            statistic: 'p95',\n            comparisonOperator: 'GreaterThanThreshold'\n          }\n        },\n        evaluation: { period: 60, evaluationPeriods: 5, breachedPeriods: 3 },\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@example.com',\n              recipient: 'oncall@example.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { webApi, publicLb } };\n});\n```"),
                  "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("application-load-balancer-error-rate"),
                "properties": z.object({
                  "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when 4xx/5xx error rate exceeds this percentage.\n\n---\n\nExample: `5` fires the alarm if more than 5% of requests return errors.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  loadBalancer:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Load balancer error rate too high\n          trigger:\n            type: application-load-balancer-error-rate\n            properties:\n              thresholdPercent: 5\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const loadBalancer = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Load balancer error rate too high',\n        trigger: {\n          type: 'application-load-balancer-error-rate',\n          properties: {\n            thresholdPercent: 5\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { loadBalancer } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("application-load-balancer-unhealthy-targets"),
                "properties": z.object({
                  "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when the percentage of unhealthy targets exceeds this value.\n\n---\n\nIf the load balancer has multiple target groups, the alarm fires if *any* group breaches the threshold.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  loadBalancer:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Too many unhealthy targets behind the load balancer\n          trigger:\n            type: application-load-balancer-unhealthy-targets\n            properties:\n              thresholdPercent: 50\n  apiService:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: api\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-org/api:latest\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: loadBalancer\n                priority: 1\n                containerPort: 3000\n                paths:\n                  - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const loadBalancer = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Too many unhealthy targets behind the load balancer',\n        trigger: {\n          type: 'application-load-balancer-unhealthy-targets',\n          properties: {\n            thresholdPercent: 50\n          }\n        }\n      }\n    ]\n  });\n\n  const apiService = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'api',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/api:latest' } },\n        events: [\n          {\n            type: 'application-load-balancer',\n            properties: {\n              loadBalancerName: 'loadBalancer',\n              priority: 1,\n              containerPort: 3000,\n              paths: ['*']\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { loadBalancer, apiService } };\n});\n```"),
                  "onlyIncludeTargets": z.array(z.string()).optional().describe("#### Only monitor health of these target container services. If omitted, monitors all targets.\n\n---\n\nOnly services actually targeted by the load balancer can be listed.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  loadBalancer:\n    type: application-load-balancer\n    properties:\n      alarms:\n        - description: Critical service has unhealthy targets\n          trigger:\n            type: application-load-balancer-unhealthy-targets\n            properties:\n              thresholdPercent: 25\n              onlyIncludeTargets:\n                - apiService\n  apiService:\n    type: multi-container-workload\n    properties:\n      resources:\n        cpu: 0.5\n        memory: 1024\n      containers:\n        - name: api\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: my-org/api:latest\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: loadBalancer\n                priority: 1\n                containerPort: 3000\n                paths:\n                  - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { ApplicationLoadBalancer, MultiContainerWorkload, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const loadBalancer = new ApplicationLoadBalancer({\n    alarms: [\n      {\n        description: 'Critical service has unhealthy targets',\n        trigger: {\n          type: 'application-load-balancer-unhealthy-targets',\n          properties: {\n            thresholdPercent: 25,\n            onlyIncludeTargets: ['apiService']\n          }\n        }\n      }\n    ]\n  });\n\n  const apiService = new MultiContainerWorkload({\n    resources: { cpu: 0.5, memory: 1024 },\n    containers: [\n      {\n        name: 'api',\n        packaging: { type: 'prebuilt-image', properties: { image: 'my-org/api:latest' } },\n        events: [\n          {\n            type: 'application-load-balancer',\n            properties: {\n              loadBalancerName: 'loadBalancer',\n              priority: 1,\n              containerPort: 3000,\n              paths: ['*']\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { loadBalancer, apiService } };\n});\n```").optional(),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```") }).strict()
              }).strict()
            ]),
            "evaluation": z.object({
              "period": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Duration of one evaluation period in seconds. Must be a multiple of 60.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker running too long\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          evaluation:\n            period: 300\n            evaluationPeriods: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker running too long',\n        trigger: {\n          type: 'lambda-duration',\n          properties: { thresholdMilliseconds: 5000 }\n        },\n        evaluation: {\n          period: 300,\n          evaluationPeriods: 2\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default(60),
              "evaluationPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many recent periods to evaluate. Prevents alarms from firing on short spikes.\n\n---\n\nExample: set to `5` with `breachedPeriods: 3` — the alarm fires only if the threshold is breached\nin at least 3 of the last 5 periods.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/payments.ts\n      alarms:\n        - description: Payments error rate elevated\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/payments.ts' }),\n    alarms: [\n      {\n        description: 'Payments error rate elevated',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 1 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { paymentsFn } };\n});\n```").default(1),
              "breachedPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many periods (within `evaluationPeriods`) must breach the threshold to fire the alarm.\n\n---\n\nMust be ≤ `evaluationPeriods`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/checkout.ts\n      alarms:\n        - description: Checkout errors persisting across multiple periods\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 2\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/checkout.ts' }),\n    alarms: [\n      {\n        description: 'Checkout errors persisting across multiple periods',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { checkoutFn } };\n});\n```").default(1) }).strict()
            .optional().describe("#### How long and how often to evaluate the metric before triggering.\n\n---\n\nControls the evaluation window (period), how many periods to look at, and how many must breach\nthe threshold to fire the alarm. Useful for filtering out short spikes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/api.ts\n      memory: 512\n      timeout: 10\n      alarms:\n        - description: API error rate too high\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    alarms: [\n      {\n        description: 'API error rate too high',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFunction } };\n});\n```").optional(),
            "notificationTargets": z.array(z.union([z.object({
                "type": z.literal("slack"),
                "properties": z.object({
                  "conversationId": z.string().describe("#### The Slack channel or DM ID to send notifications to.\n\n---\n\nTo find the ID: open the channel, click its name, and look at the bottom of the **About** tab.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```"),
                  "accessToken": z.string().describe("#### Bot User OAuth Token for your Slack app. Store as `$Secret()` for security.\n\n---\n\nCreate a Slack app, add the `chat:write` scope, install it to your workspace, then copy the Bot User OAuth Token.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("ms-teams"),
                "properties": z.object({
                  "webhookUrl": z.string().describe("#### Incoming Webhook URL for the MS Teams channel. Store as `$Secret()` for security.\n\n---\n\nCreate an Incoming Webhook connector in your Teams channel settings to get this URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 1024\n      timeout: 15\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          description: API function is running slow\n          notificationTargets:\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('ms-teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/api.ts' }\n    },\n    memory: 1024,\n    timeout: 15,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 5000 } },\n        description: 'API function is running slow',\n        notificationTargets: [\n          {\n            type: 'ms-teams',\n            properties: {\n              webhookUrl: $Secret('ms-teams-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("email"),
                "properties": z.object({
                  "sender": z.string().describe("#### The email address of the sender.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```"),
                  "recipient": z.string().describe("#### The email address of the recipient.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("discord"),
                "properties": z.object({
                  "webhookUrl": z.string().describe("#### Discord Webhook URL for the channel. Store as `$Secret()` for security.\n\n---\n\nCreate a webhook in your Discord channel settings (Edit Channel → Integrations → Webhooks).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest.ts\n      memory: 256\n      timeout: 60\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 30000\n          description: Ingest job is taking too long\n          notificationTargets:\n            - type: discord\n              properties:\n                webhookUrl: $Secret('discord-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest.ts' }\n    },\n    memory: 256,\n    timeout: 60,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 30000 } },\n        description: 'Ingest job is taking too long',\n        notificationTargets: [\n          {\n            type: 'discord',\n            properties: {\n              webhookUrl: $Secret('discord-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { ingestFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("webhook"),
                "properties": z.object({
                  "url": z.string().describe("#### The URL to send webhook POST requests to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```"),
                  "secret": z.string().optional().describe("#### Optional signing secret for HMAC-SHA256 payload verification.\n\n---\n\nIf provided, each request includes an `X-Stacktape-Signature` header.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional(),
                  "headers": z.record(z.string(), z.any()).optional().describe("#### Optional custom headers to include in each request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n                headers:\n                  X-Environment: production\n                  Authorization: $Secret('webhook-auth-header')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret'),\n              headers: {\n                'X-Environment': 'production',\n                Authorization: $Secret('webhook-auth-header')\n              }\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional() }).strict()
                .optional().describe("").optional() }).strict()
            ])).optional().describe("#### Where to send notifications when the alarm fires — Slack, MS Teams, or email.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API returning too many errors\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 2\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C12345678\n                accessToken: $Secret('slack-bot-token')\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API returning too many errors',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: { conversationId: 'C12345678', accessToken: $Secret('slack-bot-token') }\n          },\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          },\n          {\n            type: 'ms-teams',\n            properties: { webhookUrl: $Secret('teams-webhook-url') }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
            "includeInHistory": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether alarm state changes should appear in monitoring history.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Jobs queue backing up\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          includeInHistory: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Jobs queue backing up',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        includeInHistory: false\n      }\n    ]\n  });\n\n  return { resources: { jobsQueue } };\n});\n```").default(true),
            "description": z.string().optional().describe("#### Custom alarm description used in notification messages and the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 80\n          description: Database CPU sustained above 80% — consider scaling up\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: { thresholdPercent: 80 }\n        },\n        description: 'Database CPU sustained above 80% — consider scaling up'\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").optional() }).strict()
          , z.object({
            "trigger": z.union([z.object({
                "type": z.literal("http-api-gateway-error-rate"),
                "properties": z.object({
                  "thresholdPercent": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when 4xx/5xx error rate exceeds this percentage.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API Gateway error rate too high\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API Gateway error rate too high',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: {\n            thresholdPercent: 3\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("http-api-gateway-latency"),
                "properties": z.object({
                  "thresholdMilliseconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Fires when request-to-response latency exceeds this value (ms).\n\n---\n\nDefault: fires if **average** latency > threshold. Customize with `statistic` and `comparisonOperator`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API latency above SLA\n          trigger:\n            type: http-api-gateway-latency\n            properties:\n              thresholdMilliseconds: 1000\n              statistic: p95\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API latency above SLA',\n        trigger: {\n          type: 'http-api-gateway-latency',\n          properties: {\n            thresholdMilliseconds: 1000,\n            statistic: 'p95'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```"),
                  "comparisonOperator": z.enum(["GreaterThanOrEqualToThreshold","GreaterThanThreshold","LessThanOrEqualToThreshold","LessThanThreshold"]).optional().describe("#### How to compare the metric value against the threshold.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - description: Database free memory dropped below threshold\n          trigger:\n            type: database-free-memory\n            properties:\n              thresholdMB: 512\n              comparisonOperator: LessThanThreshold\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        description: 'Database free memory dropped below threshold',\n        trigger: {\n          type: 'database-free-memory',\n          properties: {\n            thresholdMB: 512,\n            comparisonOperator: 'LessThanThreshold'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").default("GreaterThanThreshold"),
                  "statistic": z.enum(["avg","max","min","p90","p95","p99","sum"]).optional().describe("#### How to aggregate metric values within each period: `avg`, `sum`, `min`, `max`, `p90`, `p95`, `p99`.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: 99th-percentile duration too high\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 3000\n              statistic: p99\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: '99th-percentile duration too high',\n        trigger: {\n          type: 'lambda-duration',\n          properties: {\n            thresholdMilliseconds: 3000,\n            statistic: 'p99'\n          }\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default("avg") }).strict()
              }).strict()
            ]),
            "evaluation": z.object({
              "period": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Duration of one evaluation period in seconds. Must be a multiple of 60.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      alarms:\n        - description: Worker running too long\n          trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          evaluation:\n            period: 300\n            evaluationPeriods: 2\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/worker.ts' }),\n    alarms: [\n      {\n        description: 'Worker running too long',\n        trigger: {\n          type: 'lambda-duration',\n          properties: { thresholdMilliseconds: 5000 }\n        },\n        evaluation: {\n          period: 300,\n          evaluationPeriods: 2\n        }\n      }\n    ]\n  });\n\n  return { resources: { worker } };\n});\n```").default(60),
              "evaluationPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many recent periods to evaluate. Prevents alarms from firing on short spikes.\n\n---\n\nExample: set to `5` with `breachedPeriods: 3` — the alarm fires only if the threshold is breached\nin at least 3 of the last 5 periods.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/payments.ts\n      alarms:\n        - description: Payments error rate elevated\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/payments.ts' }),\n    alarms: [\n      {\n        description: 'Payments error rate elevated',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 1 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { paymentsFn } };\n});\n```").default(1),
              "breachedPeriods": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### How many periods (within `evaluationPeriods`) must breach the threshold to fire the alarm.\n\n---\n\nMust be ≤ `evaluationPeriods`.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  checkoutFn:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/checkout.ts\n      alarms:\n        - description: Checkout errors persisting across multiple periods\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 2\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const checkoutFn = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/checkout.ts' }),\n    alarms: [\n      {\n        description: 'Checkout errors persisting across multiple periods',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { checkoutFn } };\n});\n```").default(1) }).strict()
            .optional().describe("#### How long and how often to evaluate the metric before triggering.\n\n---\n\nControls the evaluation window (period), how many periods to look at, and how many must breach\nthe threshold to fire the alarm. Useful for filtering out short spikes.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: ./src/api.ts\n      memory: 512\n      timeout: 10\n      alarms:\n        - description: API error rate too high\n          trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          evaluation:\n            period: 60\n            evaluationPeriods: 5\n            breachedPeriods: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: './src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    alarms: [\n      {\n        description: 'API error rate too high',\n        trigger: {\n          type: 'lambda-error-rate',\n          properties: { thresholdPercent: 5 }\n        },\n        evaluation: {\n          period: 60,\n          evaluationPeriods: 5,\n          breachedPeriods: 3\n        }\n      }\n    ]\n  });\n\n  return { resources: { apiFunction } };\n});\n```").optional(),
            "notificationTargets": z.array(z.union([z.object({
                "type": z.literal("slack"),
                "properties": z.object({
                  "conversationId": z.string().describe("#### The Slack channel or DM ID to send notifications to.\n\n---\n\nTo find the ID: open the channel, click its name, and look at the bottom of the **About** tab.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```"),
                  "accessToken": z.string().describe("#### Bot User OAuth Token for your Slack app. Store as `$Secret()` for security.\n\n---\n\nCreate a Slack app, add the `chat:write` scope, install it to your workspace, then copy the Bot User OAuth Token.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  workerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 5\n          description: Worker error rate is too high\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C0123456789\n                accessToken: $Secret('slack-bot-token')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const workerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 5 } },\n        description: 'Worker error rate is too high',\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: {\n              conversationId: 'C0123456789',\n              accessToken: $Secret('slack-bot-token')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { workerFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("ms-teams"),
                "properties": z.object({
                  "webhookUrl": z.string().describe("#### Incoming Webhook URL for the MS Teams channel. Store as `$Secret()` for security.\n\n---\n\nCreate an Incoming Webhook connector in your Teams channel settings to get this URL.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 1024\n      timeout: 15\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 5000\n          description: API function is running slow\n          notificationTargets:\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('ms-teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/api.ts' }\n    },\n    memory: 1024,\n    timeout: 15,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 5000 } },\n        description: 'API function is running slow',\n        notificationTargets: [\n          {\n            type: 'ms-teams',\n            properties: {\n              webhookUrl: $Secret('ms-teams-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("email"),
                "properties": z.object({
                  "sender": z.string().describe("#### The email address of the sender.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```"),
                  "recipient": z.string().describe("#### The email address of the recipient.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  paymentsFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/payments.ts\n      memory: 512\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 1\n          description: Payment processing failures detected\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@mycompany.com\n                recipient: oncall@mycompany.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const paymentsFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/payments.ts' }\n    },\n    memory: 512,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 1 } },\n        description: 'Payment processing failures detected',\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@mycompany.com',\n              recipient: 'oncall@mycompany.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { paymentsFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("discord"),
                "properties": z.object({
                  "webhookUrl": z.string().describe("#### Discord Webhook URL for the channel. Store as `$Secret()` for security.\n\n---\n\nCreate a webhook in your Discord channel settings (Edit Channel → Integrations → Webhooks).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  ingestFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/ingest.ts\n      memory: 256\n      timeout: 60\n      alarms:\n        - trigger:\n            type: lambda-duration\n            properties:\n              thresholdMilliseconds: 30000\n          description: Ingest job is taking too long\n          notificationTargets:\n            - type: discord\n              properties:\n                webhookUrl: $Secret('discord-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const ingestFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/ingest.ts' }\n    },\n    memory: 256,\n    timeout: 60,\n    alarms: [\n      {\n        trigger: { type: 'lambda-duration', properties: { thresholdMilliseconds: 30000 } },\n        description: 'Ingest job is taking too long',\n        notificationTargets: [\n          {\n            type: 'discord',\n            properties: {\n              webhookUrl: $Secret('discord-webhook-url')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { ingestFunction } };\n});\n```") }).strict()
                .optional().describe("").optional() }).strict()
              , z.object({
                "type": z.literal("webhook"),
                "properties": z.object({
                  "url": z.string().describe("#### The URL to send webhook POST requests to.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```"),
                  "secret": z.string().optional().describe("#### Optional signing secret for HMAC-SHA256 payload verification.\n\n---\n\nIf provided, each request includes an `X-Stacktape-Signature` header.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret')\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional(),
                  "headers": z.record(z.string(), z.any()).optional().describe("#### Optional custom headers to include in each request.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  schedulerFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/scheduler.ts\n      memory: 256\n      timeout: 30\n      alarms:\n        - trigger:\n            type: lambda-error-rate\n            properties:\n              thresholdPercent: 10\n          description: Scheduler errors detected\n          notificationTargets:\n            - type: webhook\n              properties:\n                url: https://ops.mycompany.com/hooks/stacktape-alarms\n                secret: $Secret('webhook-signing-secret')\n                headers:\n                  X-Environment: production\n                  Authorization: $Secret('webhook-auth-header')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const schedulerFunction = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/scheduler.ts' }\n    },\n    memory: 256,\n    timeout: 30,\n    alarms: [\n      {\n        trigger: { type: 'lambda-error-rate', properties: { thresholdPercent: 10 } },\n        description: 'Scheduler errors detected',\n        notificationTargets: [\n          {\n            type: 'webhook',\n            properties: {\n              url: 'https://ops.mycompany.com/hooks/stacktape-alarms',\n              secret: $Secret('webhook-signing-secret'),\n              headers: {\n                'X-Environment': 'production',\n                Authorization: $Secret('webhook-auth-header')\n              }\n            }\n          }\n        ]\n      }\n    ]\n  });\n  return { resources: { schedulerFunction } };\n});\n```").optional() }).strict()
                .optional().describe("").optional() }).strict()
            ])).optional().describe("#### Where to send notifications when the alarm fires — Slack, MS Teams, or email.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: http-api-gateway\n    properties:\n      alarms:\n        - description: API returning too many errors\n          trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 2\n          notificationTargets:\n            - type: slack\n              properties:\n                conversationId: C12345678\n                accessToken: $Secret('slack-bot-token')\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n            - type: ms-teams\n              properties:\n                webhookUrl: $Secret('teams-webhook-url')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { HttpApiGateway, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new HttpApiGateway({\n    alarms: [\n      {\n        description: 'API returning too many errors',\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: { thresholdPercent: 2 }\n        },\n        notificationTargets: [\n          {\n            type: 'slack',\n            properties: { conversationId: 'C12345678', accessToken: $Secret('slack-bot-token') }\n          },\n          {\n            type: 'email',\n            properties: { sender: 'alerts@example.com', recipient: 'oncall@example.com' }\n          },\n          {\n            type: 'ms-teams',\n            properties: { webhookUrl: $Secret('teams-webhook-url') }\n          }\n        ]\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
            "includeInHistory": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Whether alarm state changes should appear in monitoring history.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  jobsQueue:\n    type: sqs-queue\n    properties:\n      alarms:\n        - description: Jobs queue backing up\n          trigger:\n            type: sqs-queue-received-messages-count\n            properties:\n              thresholdCount: 1000\n          includeInHistory: false\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({\n    alarms: [\n      {\n        description: 'Jobs queue backing up',\n        trigger: {\n          type: 'sqs-queue-received-messages-count',\n          properties: { thresholdCount: 1000 }\n        },\n        includeInHistory: false\n      }\n    ]\n  });\n\n  return { resources: { jobsQueue } };\n});\n```").default(true),
            "description": z.string().optional().describe("#### Custom alarm description used in notification messages and the AWS console.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDatabase:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserName: admin\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n      alarms:\n        - trigger:\n            type: database-cpu-utilization\n            properties:\n              thresholdPercent: 80\n          description: Database CPU sustained above 80% — consider scaling up\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDatabase = new RelationalDatabase({\n    credentials: {\n      masterUserName: 'admin',\n      masterUserPassword: $Secret('db-password')\n    },\n    engine: {\n      type: 'postgres',\n      properties: {\n        version: '16.2',\n        primaryInstance: { instanceSize: 'db.t3.micro' }\n      }\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'database-cpu-utilization',\n          properties: { thresholdPercent: 80 }\n        },\n        description: 'Database CPU sustained above 80% — consider scaling up'\n      }\n    ]\n  });\n\n  return { resources: { mainDatabase } };\n});\n```").optional() }).strict()
        ])).optional().describe("#### Alarms for this service (merged with global alarms from the Stacktape Console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      alarms:\n        - trigger:\n            type: http-api-gateway-error-rate\n            properties:\n              thresholdPercent: 5\n          notificationTargets:\n            - type: email\n              properties:\n                sender: alerts@example.com\n                recipient: oncall@example.com\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    alarms: [\n      {\n        trigger: {\n          type: 'http-api-gateway-error-rate',\n          properties: {\n            thresholdPercent: 5\n          }\n        },\n        notificationTargets: [\n          {\n            type: 'email',\n            properties: {\n              sender: 'alerts@example.com',\n              recipient: 'oncall@example.com'\n            }\n          }\n        ]\n      }\n    ]\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```").optional(),
        "disabledGlobalAlarms": z.array(z.string()).optional().describe("#### Global alarm names to exclude from this service.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      disabledGlobalAlarms:\n        - high-error-rate\n        - high-latency\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    disabledGlobalAlarms: ['high-error-rate', 'high-latency']\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```").optional(),
        "deployment": z.object({
          "strategy": z.enum(["AllAtOnce","Canary10Percent15Minutes","Canary10Percent5Minutes","Linear10PercentEvery1Minutes","Linear10PercentEvery3Minutes"]).describe("#### How traffic shifts to the new version during deployment.\n\n---\n\n- `Canary10Percent5Minutes`: 10% first, then all after 5 min.\n- `Canary10Percent15Minutes`: 10% first, then all after 15 min.\n- `Linear10PercentEvery1Minutes`: 10% more every minute.\n- `Linear10PercentEvery3Minutes`: 10% more every 3 minutes.\n- `AllAtOnce`: Instant switch.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n      deployment:\n        strategy: Linear10PercentEvery3Minutes\n  appLb:\n    type: application-load-balancer\n  smokeTest:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/smoke-test.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, LambdaFunction, StacktapeImageBuildpackPackaging, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const smokeTest = new LambdaFunction({ packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/smoke-test.ts' }) });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    deployment: {\n      strategy: 'Linear10PercentEvery3Minutes'\n    }\n  });\n  return { resources: { app, appLb, smokeTest } };\n});\n```"),
          "beforeAllowTrafficFunction": z.string().optional().describe("#### Lambda function to run before traffic shifts to the new version (for validation/smoke tests).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n      deployment:\n        strategy: Canary10Percent5Minutes\n        beforeAllowTrafficFunction: smokeTest\n  appLb:\n    type: application-load-balancer\n  smokeTest:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/smoke-test.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, LambdaFunction, StacktapeImageBuildpackPackaging, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const smokeTest = new LambdaFunction({ packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/smoke-test.ts' }) });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes',\n      beforeAllowTrafficFunction: 'smokeTest'\n    }\n  });\n  return { resources: { app, appLb, smokeTest } };\n});\n```").optional(),
          "afterTrafficShiftFunction": z.string().optional().describe("#### Lambda function to run after all traffic has shifted (for post-deployment checks).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n      deployment:\n        strategy: Canary10Percent5Minutes\n        afterTrafficShiftFunction: smokeTest\n  appLb:\n    type: application-load-balancer\n  smokeTest:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/smoke-test.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, LambdaFunction, StacktapeImageBuildpackPackaging, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const smokeTest = new LambdaFunction({ packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/smoke-test.ts' }) });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes',\n      afterTrafficShiftFunction: 'smokeTest'\n    }\n  });\n  return { resources: { app, appLb, smokeTest } };\n});\n```").optional(),
          "testListenerPort": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### ALB listener port for test traffic. Only needed with `beforeAllowTrafficFunction` and custom listeners.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: web\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/index.ts\n          events:\n            - type: application-load-balancer\n              properties:\n                loadBalancerName: appLb\n                priority: 1\n                containerPort: 3000\n      resources:\n        cpu: 0.5\n        memory: 1024\n      deployment:\n        strategy: Canary10Percent5Minutes\n        beforeAllowTrafficFunction: smokeTest\n        testListenerPort: 8443\n  appLb:\n    type: application-load-balancer\n  smokeTest:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/smoke-test.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, ApplicationLoadBalancer, LambdaFunction, StacktapeImageBuildpackPackaging, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appLb = new ApplicationLoadBalancer({});\n  const smokeTest = new LambdaFunction({ packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/smoke-test.ts' }) });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'web',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/index.ts' }),\n        events: [\n          { type: 'application-load-balancer', properties: { loadBalancerName: 'appLb', priority: 1, containerPort: 3000 } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes',\n      beforeAllowTrafficFunction: 'smokeTest',\n      testListenerPort: 8443\n    }\n  });\n  return { resources: { app, appLb, smokeTest } };\n});\n```").optional() }).strict()
        .optional().describe("#### Gradual traffic shifting for safe deployments (canary, linear, or all-at-once).\n\n---\n\nRequires `loadBalancing` type `application-load-balancer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: application-load-balancer\n      deployment:\n        strategy: Canary10Percent5Minutes\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'application-load-balancer'\n    },\n    deployment: {\n      strategy: 'Canary10Percent5Minutes'\n    }\n  });\n\n  return {\n    resources: { apiService }\n  };\n});\n```").optional(),
        "useFirewall": z.string().optional().describe("#### Name of a `web-app-firewall` resource to protect this service from common web exploits.\n\n---\n\nAttaches a regional firewall directly to the service's application load balancer.\nRequires `loadBalancing` type `application-load-balancer`.\n\nTo protect a CDN-enabled service at CloudFront instead, use `cdn.useFirewall`\nwith a `web-app-firewall` resource whose `scope` is `cdn`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  serviceFirewall:\n    type: web-app-firewall\n    properties:\n      scope: regional\n  apiService:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/main.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      loadBalancing:\n        type: application-load-balancer\n      useFirewall: serviceFirewall\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { defineConfig, WebService, WebAppFirewall, StacktapeImageBuildpackPackaging } from 'stacktape';\n\nexport default defineConfig(() => {\n  const serviceFirewall = new WebAppFirewall({\n    scope: 'regional'\n  });\n\n  const apiService = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: 'src/main.ts'\n    }),\n    resources: {\n      cpu: 1,\n      memory: 2048\n    },\n    loadBalancing: {\n      type: 'application-load-balancer'\n    },\n    useFirewall: 'serviceFirewall'\n  });\n\n  return {\n    resources: { serviceFirewall, apiService }\n  };\n});\n```").optional(),
        "packaging": z.union([z.object({
            "type": z.literal("prebuilt-image"),
            "properties": z.object({
              "repositoryCredentialsSecretArn": z.string().optional().describe("#### The ARN of a secret containing credentials for a private container registry.\n\n---\n\nThe secret must be a JSON object with `username` and `password` keys.\nYou can create secrets using the `stacktape secret:create` command.\n\n**Example (YAML):**\n\n```yaml\nresources:\n privateImageService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: registry.example.com/my-org/my-app:latest\n         repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const privateImageService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'registry.example.com/my-org/my-app:latest',\n       repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { privateImageService } };\n});\n```").optional(),
              "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: node:22-alpine\n         entryPoint:\n           - /bin/sh\n           - -c\n           - node server.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'node:22-alpine',\n       entryPoint: ['/bin/sh', '-c', 'node server.js']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
              "image": z.string().describe("#### The name or URL of the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu'\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```"),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n           command:\n             - -i\n             - input.mp4\n             - output.webm\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu',\n         command: ['-i', 'input.mp4', 'output.webm']\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```").optional() }).strict()
            .describe("#### Configures a pre-built container image.") }).strict()
          .describe("#### Uses a pre-built container image.\n\n---\n\nWith `prebuilt-image`, you provide a reference to an existing container image.\nThis can be a public image from Docker Hub or a private image from any container registry.\n\nFor private registries, configure `repositoryCredentialsSecretArn` with credentials stored in AWS Secrets Manager."), z.object({
            "type": z.literal("custom-dockerfile"),
            "properties": z.object({
              "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: custom-dockerfile\n       properties:\n         buildContextPath: ./app\n         entryPoint:\n           - /app/entrypoint.sh\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'custom-dockerfile',\n     properties: {\n       buildContextPath: './app',\n       entryPoint: ['/app/entrypoint.sh']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
              "dockerfilePath": z.string().optional().describe("#### The path to the Dockerfile, relative to `buildContextPath`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           dockerfilePath: docker/Dockerfile.prod\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         dockerfilePath: 'docker/Dockerfile.prod'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
              "buildContextPath": z.string().describe("#### The path to the build context directory, relative to your Stacktape configuration file.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
              "buildArgs": z.array(z.object({
                "argName": z.string().describe("#### Argument name\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                "value": z.string().describe("#### Argument value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```") }).strict()
              ).optional().describe("#### A list of arguments to pass to the `docker build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: NODE_ENV\n               value: production\n             - argName: BUILD_VERSION\n               value: \"1.4.2\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           { argName: 'NODE_ENV', value: 'production' },\n           { argName: 'BUILD_VERSION', value: '1.4.2' }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           command:\n             - node\n             - dist/process.js\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         command: ['node', 'dist/process.js']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional() }).strict()
            .describe("#### Configures an image to be built by Stacktape from a specified Dockerfile.") }).strict()
          .describe("#### Builds a container image from your own Dockerfile.\n\n---\n\nWith `custom-dockerfile`, you provide a path to your Dockerfile and build context.\nStacktape builds the image and uploads it to a managed ECR repository.\n\nThis gives you full control over the container environment and is ideal for complex setups."), z.object({
            "type": z.literal("external-buildpack"),
            "properties": z.object({
              "builder": z.string().optional().describe("#### The Buildpack Builder to use.\n\n---\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           builder: paketobuildpacks/builder-jammy-full\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         builder: 'paketobuildpacks/builder-jammy-full'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
              "buildpacks": z.array(z.string()).optional().describe("#### The specific Buildpack to use.\n\n---\n\nBy default, the buildpack is detected automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           buildpacks:\n             - paketo-buildpacks/nodejs\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         buildpacks: ['paketo-buildpacks/nodejs']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional(),
              "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           command:\n             - npm\n             - run\n             - import\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         command: ['npm', 'run', 'import']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Builds a container image using an external buildpack.\n\n---\n\nExternal buildpacks (buildpacks.io) automatically detect your application type\nand build an optimized container image with zero configuration.\n\nThe default builder is `paketobuildpacks/builder-jammy-base`.\nYou can find buildpacks for almost any language or framework."), z.object({
            "type": z.literal("nixpacks"),
            "properties": z.object({
              "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
              "buildImage": z.string().optional().describe("#### The base image to use for building the application.\n\n---\n\nFor more details, see the [Nixpacks documentation](https://nixpacks.com/docs/configuration/file#build-image).\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         buildImage: ubuntu:22.04\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       buildImage: 'ubuntu:22.04'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "providers": z.array(z.string()).optional().describe("#### A list of providers to use for determining the build and runtime environments.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         providers:\n           - node\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       providers: ['node']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startCmd": z.string().optional().describe("#### The command to execute when starting the application.\n\n---\n\nThis overrides the default start command inferred by Nixpacks.\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startCmd: node dist/worker.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startCmd: 'node dist/worker.js'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startRunImage": z.string().optional().describe("#### The base image to use for running the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startRunImage: gcr.io/distroless/nodejs22-debian12\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startRunImage: 'gcr.io/distroless/nodejs22-debian12'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startOnlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in the runtime environment; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startOnlyIncludeFiles:\n           - dist\n           - node_modules\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startOnlyIncludeFiles: ['dist', 'node_modules']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "phases": z.array(z.object({
                "name": z.string().describe("#### The name of the build phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                "cmds": z.array(z.string()).optional().describe("#### A list of shell commands to execute in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n               - npm prune --omit=dev\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build', 'npm prune --omit=dev']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixPkgs": z.array(z.string()).optional().describe("#### A list of Nix packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - ffmpeg\n               - imagemagick\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['ffmpeg', 'imagemagick']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixLibs": z.array(z.string()).optional().describe("#### A list of Nix libraries to include in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixLibs:\n               - openssl\n               - zlib\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixLibs: ['openssl', 'zlib']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixOverlay": z.array(z.string()).optional().describe("#### A list of Nix overlay files to apply in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixOverlay:\n               - ./nix/overlay.nix\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixOverlay: ['./nix/overlay.nix']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixpkgsArchive": z.string().optional().describe("#### The Nixpkgs archive to use.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - nodejs_22\n             nixpkgsArchive: bf3287dac860542719b3849d6970d22f635f9da1\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['nodejs_22'],\n           nixpkgsArchive: 'bf3287dac860542719b3849d6970d22f635f9da1'\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "aptPkgs": z.array(z.string()).optional().describe("#### A list of APT packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             aptPkgs:\n               - libpq-dev\n               - poppler-utils\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           aptPkgs: ['libpq-dev', 'poppler-utils']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "cacheDirectories": z.array(z.string()).optional().describe("#### A list of directories to cache between builds to speed up subsequent builds.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n             cacheDirectories:\n               - node_modules/.cache\n               - /root/.npm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci'],\n           cacheDirectories: ['node_modules/.cache', '/root/.npm']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "onlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in this phase; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n             onlyIncludeFiles:\n               - src\n               - package.json\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build'],\n           onlyIncludeFiles: ['src', 'package.json']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
              ).optional().describe("#### The build phases for the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n           - name: build\n             cmds:\n               - npm run build\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         { name: 'install', cmds: ['npm ci'] },\n         { name: 'build', cmds: ['npm run build'] }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Builds a container image using Nixpacks.\n\n---\n\nNixpacks automatically detects your application type and builds an optimized container image.\nIn most cases, no configuration is required.\n\nIt supports a wide range of languages and frameworks out of the box."), z.object({
            "type": z.literal("stacktape-image-buildpack"),
            "properties": z.object({
              "languageSpecificConfig": z.union([z.object({
                  "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                  "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                  "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                , z.object({
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                  "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                  "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                , z.object({
                  "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                , z.record(z.string(), z.any()), z.object({
                  "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                , z.object({
                  "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                , z.object({
                  "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                  "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
              ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { apiService } };\n});\n```").optional(),
              "requiresGlibcBinaries": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use glibc instead of musl (Alpine default). Enable if native dependencies require glibc.\n\n---\n\nResults in a larger image. Common packages needing this: `sharp`, `canvas`, `bcrypt`, `puppeteer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         requiresGlibcBinaries: true\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       requiresGlibcBinaries: true\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
              "customDockerBuildCommands": z.array(z.string()).optional().describe("#### A list of commands to be executed during the `docker build` process.\n\n---\n\nThese commands are executed using the `RUN` directive in the Dockerfile.\nThis is useful for installing additional system dependencies in your container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         customDockerBuildCommands:\n           - apt-get update && apt-get install -y poppler-utils\n           - fc-cache -f\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       customDockerBuildCommands: [\n         'apt-get update && apt-get install -y poppler-utils',\n         'fc-cache -f'\n       ]\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
              "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
              "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            .describe("#### Configures an image to be built automatically by Stacktape from your source code.") }).strict()
          .describe("#### A zero-config buildpack that creates a container image from your source code.\n\n---\n\nThe `stacktape-image-buildpack` automatically bundles your code and dependencies into an optimized container image.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, and Go.\n\nFor JS/TS, your code is bundled into a single file with source maps.\nThe resulting image is uploaded to a managed ECR repository.")]).describe("#### Configures the container image for the service.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return { resources: { api } };\n});\n```"),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables injected into the container at runtime.\n\n---\n\nUse for configuration like API keys, feature flags, or secrets.\nVariables from `connectTo` (e.g., `STP_MY_DATABASE_CONNECTION_STRING`) are added automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: LOG_LEVEL\n          value: info\n        - name: DATABASE_URL\n          value: $ResourceParam('mainDb', 'connectionString')\n      connectTo:\n        - mainDb\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $ResourceParam, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      LOG_LEVEL: 'info',\n      DATABASE_URL: $ResourceParam('mainDb', 'connectionString')\n    },\n    connectTo: [mainDb]\n  });\n\n  return { resources: { api, mainDb } };\n});\n```").optional(),
        "logging": z.object({
          "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            disabled: true\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { disabled: true }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(false),
          "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 30 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(90),
          "logForwarding": z.union([z.object({
              "type": z.literal("http-endpoint"),
              "properties": z.object({
                "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("highlight"),
              "properties": z.object({
                "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("datadog"),
              "properties": z.object({
                "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
            }).strict()
          ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Logging configuration.\n\n---\n\nContainer output (`stdout`/`stderr`) is automatically sent to CloudWatch and retained for 90 days.\nView logs with `stacktape logs` or in the Stacktape Console.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      logging:\n        retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    logging: { retentionDays: 30 }\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
        "resources": z.object({
          "cpu": z.union([z.literal(0.25), z.literal(0.5), z.literal(1), z.literal(16), z.literal(2), z.literal(4), z.literal(8)]).optional().describe("#### vCPUs for the workload (Fargate). Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 2\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 2,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB. Must be compatible with the vCPU count on Fargate.\n\n---\n\nFargate valid combos: 0.25 vCPU → 512-2048 MB, 0.5 → 1024-4096, 1 → 2048-8192, 2 → 4096-16384,\n4 → 8192-30720, 8 → 16384-61440, 16 → 32768-122880.\nFor EC2: auto-detected from instance type if omitted.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 1,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "instanceTypes": z.array(z.string()).optional().describe("#### EC2 instance types for the workload (e.g., `t3.medium`, `c6g.large`). Use instead of `cpu`/`memory`.\n\n---\n\nFirst type in the list is preferred. Instances auto-scale and are refreshed weekly for patching.\nTip: specify a single type and omit `cpu`/`memory` for optimal sizing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - c6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['c6g.large']\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "enableWarmPool": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Keep pre-initialized EC2 instances ready for faster scaling. Only works with a single instance type.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - t3.medium\n        enableWarmPool: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['t3.medium'],\n      enableWarmPool: true\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "architecture": z.enum(["arm64","x86_64"]).optional().describe("#### CPU architecture for Fargate. `arm64` is ~20% cheaper. Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 0.5,\n      memory: 1024,\n      architecture: 'arm64'\n    }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
        .describe("#### CPU, memory, and compute engine for the container.\n\n---\n\nTwo compute engines:\n- **Fargate** (default): Serverless — just specify `cpu` and `memory`.\n- **EC2**: Specify `instanceTypes` for more control and potentially lower cost.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 2048\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 1, memory: 2048, architecture: 'arm64' }\n  });\n\n  return { resources: { api } };\n});\n```"),
        "scaling": z.object({
          "minInstances": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum running instances. Set to 0 is not supported — minimum is 1.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8\n    }\n  });\n  return { resources: { app } };\n});\n```").default(1),
          "maxInstances": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum running instances. Traffic is distributed across all instances.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8\n    }\n  });\n  return { resources: { app } };\n});\n```").default(1),
          "scalingPolicy": z.object({
            "keepAvgCpuUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Scale out when avg CPU exceeds this %, scale in when it drops below.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 65\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: {\n        keepAvgCpuUtilizationUnder: 65\n      }\n    }\n  });\n  return { resources: { app } };\n});\n```").default(80),
            "keepAvgMemoryUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Scale out when avg memory exceeds this %, scale in when it drops below.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgMemoryUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: {\n        keepAvgMemoryUtilizationUnder: 70\n      }\n    }\n  });\n  return { resources: { app } };\n});\n```").default(80) }).strict()
          .optional().describe("#### When to scale: CPU and/or memory utilization targets.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 60\n          keepAvgMemoryUtilizationUnder: 75\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: { keepAvgCpuUtilizationUnder: 60, keepAvgMemoryUtilizationUnder: 75 }\n    }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
        .optional().describe("#### Auto-scaling: add/remove container instances based on demand.\n\n---\n\nTraffic is automatically distributed across all running containers.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 10\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 10,\n      scalingPolicy: { keepAvgCpuUtilizationUnder: 70 }\n    }\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
        "internalHealthCheck": z.object({
          "healthCheckCommand": z.array(z.string()).describe("#### Command to check health. E.g., `[\"CMD-SHELL\", \"curl -f http://localhost/ || exit 1\"]`. Exit 0 = healthy.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1']\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "intervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks (5-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            intervalSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          intervalSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(30),
          "timeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a check is considered failed (2-60).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            timeoutSeconds: 10\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          timeoutSeconds: 10\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(5),
          "retries": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Consecutive failures before marking unhealthy (1-10).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            retries: 5\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          retries: 5\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(3),
          "startPeriodSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Grace period (seconds) before counting failures. Gives the container time to start (0-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            startPeriodSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          startPeriodSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
        .optional().describe("#### Health check that auto-replaces unhealthy containers.\n\n---\n\nIf a container fails the health check, it's terminated and replaced automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      internalHealthCheck:\n        healthCheckCommand:\n          - CMD-SHELL\n          - curl -f http://localhost:3000/health || exit 1\n        intervalSeconds: 30\n        retries: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    internalHealthCheck: {\n      healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/health || exit 1'],\n      intervalSeconds: 30,\n      retries: 3\n    }\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
        "stopTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds to wait for graceful shutdown before force-killing the container.\n\n---\n\nThe container receives `SIGTERM` first, then `SIGKILL` after this timeout. Must be 2-120.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      stopTimeout: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    stopTimeout: 30\n  });\n\n  return { resources: { api } };\n});\n```").default(2),
        "enableRemoteSessions": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Allow SSH-like access to running containers for debugging.\n\n---\n\nEnables `stacktape container:session` to open an interactive shell inside the container.\nAdds a small SSM agent that uses minimal CPU/memory.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      enableRemoteSessions: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    enableRemoteSessions: true\n  });\n\n  return { resources: { api } };\n});\n```").default(false),
        "volumeMounts": z.array(z.object({
          "type": z.literal("efs").describe("#### The type of the volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
          "properties": z.object({
            "efsFilesystemName": z.string().describe("#### Name of the `efs-filesystem` resource defined in your config.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "rootDirectory": z.string().optional().describe("#### Subdirectory within the EFS filesystem to mount. Restricts access to that directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                rootDirectory: /uploads\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              rootDirectory: '/uploads',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "mountPath": z.string().describe("#### Absolute path inside the container where the volume is mounted (e.g., `/data`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
          .describe("#### Properties for the EFS volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
        ).optional().describe("#### Persistent EFS volumes shared across containers and restarts.\n\n---\n\nData stored in EFS volumes persists even when containers are replaced.\nMultiple containers can mount the same volume. All data is encrypted in transit.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      volumeMounts:\n        - type: efs\n          properties:\n            efsFilesystemName: sharedData\n            mountPath: /data\n  sharedData:\n    type: efs-filesystem\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, EfsFilesystem, ContainerEfsMount, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedData = new EfsFilesystem({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    volumeMounts: [\n      new ContainerEfsMount({ type: 'efs', properties: { efsFilesystemName: 'sharedData', mountPath: '/data' } })\n    ]\n  });\n\n  return { resources: { api, sharedData } };\n});\n```").optional(),
        "sideContainers": z.array(z.object({
          "containerType": z.enum(["always-running","run-on-init"]).describe("#### When and how this sidecar container runs.\n\n---\n\n- **`run-on-init`**: Must exit with code 0 before the main container starts. Use for migrations or setup.\n- **`always-running`**: Runs alongside the main container for its entire lifetime. If it crashes, the whole task fails.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      sideContainers:\n        - name: migrations\n          containerType: run-on-init\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/migrate.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    sideContainers: [\n      {\n        name: 'migrations',\n        containerType: 'run-on-init',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/migrate.ts' })\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```"),
          "name": z.string().describe("#### Unique container name within this workload.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' })\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "packaging": z.union([z.object({
              "type": z.literal("prebuilt-image"),
              "properties": z.object({
                "repositoryCredentialsSecretArn": z.string().optional().describe("#### The ARN of a secret containing credentials for a private container registry.\n\n---\n\nThe secret must be a JSON object with `username` and `password` keys.\nYou can create secrets using the `stacktape secret:create` command.\n\n**Example (YAML):**\n\n```yaml\nresources:\n privateImageService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: registry.example.com/my-org/my-app:latest\n         repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const privateImageService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'registry.example.com/my-org/my-app:latest',\n       repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { privateImageService } };\n});\n```").optional(),
                "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: node:22-alpine\n         entryPoint:\n           - /bin/sh\n           - -c\n           - node server.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'node:22-alpine',\n       entryPoint: ['/bin/sh', '-c', 'node server.js']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
                "image": z.string().describe("#### The name or URL of the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu'\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n           command:\n             - -i\n             - input.mp4\n             - output.webm\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu',\n         command: ['-i', 'input.mp4', 'output.webm']\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```").optional() }).strict()
              .describe("#### Configures a pre-built container image.") }).strict()
            .describe("#### Uses a pre-built container image.\n\n---\n\nWith `prebuilt-image`, you provide a reference to an existing container image.\nThis can be a public image from Docker Hub or a private image from any container registry.\n\nFor private registries, configure `repositoryCredentialsSecretArn` with credentials stored in AWS Secrets Manager."), z.object({
              "type": z.literal("custom-dockerfile"),
              "properties": z.object({
                "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: custom-dockerfile\n       properties:\n         buildContextPath: ./app\n         entryPoint:\n           - /app/entrypoint.sh\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'custom-dockerfile',\n     properties: {\n       buildContextPath: './app',\n       entryPoint: ['/app/entrypoint.sh']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
                "dockerfilePath": z.string().optional().describe("#### The path to the Dockerfile, relative to `buildContextPath`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           dockerfilePath: docker/Dockerfile.prod\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         dockerfilePath: 'docker/Dockerfile.prod'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "buildContextPath": z.string().describe("#### The path to the build context directory, relative to your Stacktape configuration file.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                "buildArgs": z.array(z.object({
                  "argName": z.string().describe("#### Argument name\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                  "value": z.string().describe("#### Argument value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```") }).strict()
                ).optional().describe("#### A list of arguments to pass to the `docker build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: NODE_ENV\n               value: production\n             - argName: BUILD_VERSION\n               value: \"1.4.2\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           { argName: 'NODE_ENV', value: 'production' },\n           { argName: 'BUILD_VERSION', value: '1.4.2' }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           command:\n             - node\n             - dist/process.js\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         command: ['node', 'dist/process.js']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built by Stacktape from a specified Dockerfile.") }).strict()
            .describe("#### Builds a container image from your own Dockerfile.\n\n---\n\nWith `custom-dockerfile`, you provide a path to your Dockerfile and build context.\nStacktape builds the image and uploads it to a managed ECR repository.\n\nThis gives you full control over the container environment and is ideal for complex setups."), z.object({
              "type": z.literal("external-buildpack"),
              "properties": z.object({
                "builder": z.string().optional().describe("#### The Buildpack Builder to use.\n\n---\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           builder: paketobuildpacks/builder-jammy-full\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         builder: 'paketobuildpacks/builder-jammy-full'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "buildpacks": z.array(z.string()).optional().describe("#### The specific Buildpack to use.\n\n---\n\nBy default, the buildpack is detected automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           buildpacks:\n             - paketo-buildpacks/nodejs\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         buildpacks: ['paketo-buildpacks/nodejs']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional(),
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           command:\n             - npm\n             - run\n             - import\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         command: ['npm', 'run', 'import']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Builds a container image using an external buildpack.\n\n---\n\nExternal buildpacks (buildpacks.io) automatically detect your application type\nand build an optimized container image with zero configuration.\n\nThe default builder is `paketobuildpacks/builder-jammy-base`.\nYou can find buildpacks for almost any language or framework."), z.object({
              "type": z.literal("nixpacks"),
              "properties": z.object({
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                "buildImage": z.string().optional().describe("#### The base image to use for building the application.\n\n---\n\nFor more details, see the [Nixpacks documentation](https://nixpacks.com/docs/configuration/file#build-image).\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         buildImage: ubuntu:22.04\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       buildImage: 'ubuntu:22.04'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "providers": z.array(z.string()).optional().describe("#### A list of providers to use for determining the build and runtime environments.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         providers:\n           - node\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       providers: ['node']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startCmd": z.string().optional().describe("#### The command to execute when starting the application.\n\n---\n\nThis overrides the default start command inferred by Nixpacks.\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startCmd: node dist/worker.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startCmd: 'node dist/worker.js'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startRunImage": z.string().optional().describe("#### The base image to use for running the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startRunImage: gcr.io/distroless/nodejs22-debian12\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startRunImage: 'gcr.io/distroless/nodejs22-debian12'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startOnlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in the runtime environment; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startOnlyIncludeFiles:\n           - dist\n           - node_modules\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startOnlyIncludeFiles: ['dist', 'node_modules']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "phases": z.array(z.object({
                  "name": z.string().describe("#### The name of the build phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                  "cmds": z.array(z.string()).optional().describe("#### A list of shell commands to execute in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n               - npm prune --omit=dev\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build', 'npm prune --omit=dev']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixPkgs": z.array(z.string()).optional().describe("#### A list of Nix packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - ffmpeg\n               - imagemagick\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['ffmpeg', 'imagemagick']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixLibs": z.array(z.string()).optional().describe("#### A list of Nix libraries to include in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixLibs:\n               - openssl\n               - zlib\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixLibs: ['openssl', 'zlib']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixOverlay": z.array(z.string()).optional().describe("#### A list of Nix overlay files to apply in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixOverlay:\n               - ./nix/overlay.nix\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixOverlay: ['./nix/overlay.nix']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixpkgsArchive": z.string().optional().describe("#### The Nixpkgs archive to use.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - nodejs_22\n             nixpkgsArchive: bf3287dac860542719b3849d6970d22f635f9da1\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['nodejs_22'],\n           nixpkgsArchive: 'bf3287dac860542719b3849d6970d22f635f9da1'\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "aptPkgs": z.array(z.string()).optional().describe("#### A list of APT packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             aptPkgs:\n               - libpq-dev\n               - poppler-utils\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           aptPkgs: ['libpq-dev', 'poppler-utils']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "cacheDirectories": z.array(z.string()).optional().describe("#### A list of directories to cache between builds to speed up subsequent builds.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n             cacheDirectories:\n               - node_modules/.cache\n               - /root/.npm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci'],\n           cacheDirectories: ['node_modules/.cache', '/root/.npm']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "onlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in this phase; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n             onlyIncludeFiles:\n               - src\n               - package.json\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build'],\n           onlyIncludeFiles: ['src', 'package.json']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
                ).optional().describe("#### The build phases for the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n           - name: build\n             cmds:\n               - npm run build\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         { name: 'install', cmds: ['npm ci'] },\n         { name: 'build', cmds: ['npm run build'] }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Builds a container image using Nixpacks.\n\n---\n\nNixpacks automatically detects your application type and builds an optimized container image.\nIn most cases, no configuration is required.\n\nIt supports a wide range of languages and frameworks out of the box."), z.object({
              "type": z.literal("stacktape-image-buildpack"),
              "properties": z.object({
                "languageSpecificConfig": z.union([z.object({
                    "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                    "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                    "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                  , z.object({
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                    "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                    "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                  , z.object({
                    "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                  , z.record(z.string(), z.any()), z.object({
                    "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                  , z.object({
                    "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                  , z.object({
                    "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                    "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
                ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { apiService } };\n});\n```").optional(),
                "requiresGlibcBinaries": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use glibc instead of musl (Alpine default). Enable if native dependencies require glibc.\n\n---\n\nResults in a larger image. Common packages needing this: `sharp`, `canvas`, `bcrypt`, `puppeteer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         requiresGlibcBinaries: true\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       requiresGlibcBinaries: true\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "customDockerBuildCommands": z.array(z.string()).optional().describe("#### A list of commands to be executed during the `docker build` process.\n\n---\n\nThese commands are executed using the `RUN` directive in the Dockerfile.\nThis is useful for installing additional system dependencies in your container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         customDockerBuildCommands:\n           - apt-get update && apt-get install -y poppler-utils\n           - fc-cache -f\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       customDockerBuildCommands: [\n         'apt-get update && apt-get install -y poppler-utils',\n         'fc-cache -f'\n       ]\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
                "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built automatically by Stacktape from your source code.") }).strict()
            .describe("#### A zero-config buildpack that creates a container image from your source code.\n\n---\n\nThe `stacktape-image-buildpack` automatically bundles your code and dependencies into an optimized container image.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, and Go.\n\nFor JS/TS, your code is bundled into a single file with source maps.\nThe resulting image is uploaded to a managed ECR repository.")]).describe("#### How to build or specify the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: custom-dockerfile\n            properties:\n              buildContextPath: ./api\n              dockerfilePath: Dockerfile\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, CustomDockerfilePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new CustomDockerfilePackaging({ buildContextPath: './api', dockerfilePath: 'Dockerfile' })\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "essential": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### If `true` (default), the entire workload restarts when this container fails.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n        - name: metrics-sidecar\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: prom/statsd-exporter:latest\n          essential: false\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, PrebuiltImagePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) },\n      {\n        name: 'metrics-sidecar',\n        packaging: new PrebuiltImagePackaging({ image: 'prom/statsd-exporter:latest' }),\n        essential: false\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            disabled: true\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { disabled: true }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 30 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Container logging (stdout/stderr). Sent to CloudWatch, viewable with `stacktape logs`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 14\n            disabled: false\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 14, disabled: false }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "dependsOn": z.array(z.object({
            "containerName": z.string().describe("The name of the container that this container depends on."),
            "condition": z.enum(["COMPLETE","HEALTHY","START","SUCCESS"]).describe("#### The condition that the dependency container must meet.\n---\nAvailable conditions:\n- `START`: The dependency has started.\n- `COMPLETE`: The dependency has finished executing (regardless of success).\n- `SUCCESS`: The dependency has finished with an exit code of `0`.\n- `HEALTHY`: The dependency has passed its first health check.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: redis\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: redis:7\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - redis-cli ping || exit 1\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          dependsOn:\n            - containerName: redis\n              condition: HEALTHY\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, PrebuiltImagePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'redis',\n        packaging: new PrebuiltImagePackaging({ image: 'redis:7' }),\n        internalHealthCheck: { healthCheckCommand: ['CMD-SHELL', 'redis-cli ping || exit 1'] }\n      },\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        dependsOn: [\n          {\n            containerName: 'redis',\n            condition: 'HEALTHY'\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
          ).optional().describe("#### Start this container only after the listed containers reach a specific state.\n\n---\n\nE.g., wait for a database sidecar to be `HEALTHY` before starting the app container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: migrations\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/migrate.ts\n          essential: false\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          dependsOn:\n            - containerName: migrations\n              condition: SUCCESS\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'migrations', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/migrate.ts' }), essential: false },\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        dependsOn: [{ containerName: 'migrations', condition: 'SUCCESS' }]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "environment": z.array(z.object({
            "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
            "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
          ).optional().describe("#### Environment variables injected at runtime. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          environment:\n            - name: NODE_ENV\n              value: production\n            - name: DATABASE_URL\n              value: $ResourceParam('appDb', 'connectionString')\n            - name: STRIPE_KEY\n              value: $Secret('stripe.secretKey')\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - appDb\n  appDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t3.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, RelationalDatabase, StacktapeImageBuildpackPackaging, $ResourceParam, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db.password') },\n    engine: { type: 'postgres', properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t3.micro' } } }\n  });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        environment: {\n          NODE_ENV: 'production',\n          DATABASE_URL: $ResourceParam('appDb', 'connectionString'),\n          STRIPE_KEY: $Secret('stripe.secretKey')\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [appDb]\n  });\n  return { resources: { app, appDb } };\n});\n```").optional(),
          "internalHealthCheck": z.object({
            "healthCheckCommand": z.array(z.string()).describe("#### Command to check health. E.g., `[\"CMD-SHELL\", \"curl -f http://localhost/ || exit 1\"]`. Exit 0 = healthy.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1']\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
            "intervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks (5-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            intervalSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          intervalSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(30),
            "timeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a check is considered failed (2-60).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            timeoutSeconds: 10\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          timeoutSeconds: 10\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(5),
            "retries": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Consecutive failures before marking unhealthy (1-10).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            retries: 5\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          retries: 5\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(3),
            "startPeriodSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Grace period (seconds) before counting failures. Gives the container time to start (0-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            startPeriodSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          startPeriodSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
          .optional().describe("#### Command-based health check. If it fails on an essential container, the workload instance is replaced.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/health || exit 1\n            intervalSeconds: 30\n            retries: 3\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/health || exit 1'],\n          intervalSeconds: 30,\n          retries: 3\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "stopTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds to wait after SIGTERM before SIGKILL (2-120).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          stopTimeout: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        stopTimeout: 30\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(2),
          "volumeMounts": z.array(z.object({
            "type": z.literal("efs").describe("#### The type of the volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "properties": z.object({
              "efsFilesystemName": z.string().describe("#### Name of the `efs-filesystem` resource defined in your config.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
              "rootDirectory": z.string().optional().describe("#### Subdirectory within the EFS filesystem to mount. Restricts access to that directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                rootDirectory: /uploads\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              rootDirectory: '/uploads',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
              "mountPath": z.string().describe("#### Absolute path inside the container where the volume is mounted (e.g., `/data`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
            .describe("#### Properties for the EFS volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
          ).optional().describe("#### Mount EFS volumes for persistent, shared storage across containers.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          { type: 'efs', properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Helper containers that run alongside the main container.\n\n---\n\n- **`run-on-init`**: Runs to completion before the main container starts (e.g., database migrations).\n- **`always-running`**: Runs for the entire lifecycle (e.g., log forwarders, monitoring agents).\n  Can reach the main container via `localhost`.").optional(),
        "usePrivateSubnetsWithNAT": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Deploy in private subnets with a static outbound IP via NAT Gateway.\n\n---\n\nThe container won't have a public IP. All outbound traffic routes through a NAT Gateway,\ngiving you a static IP you can whitelist in external services (APIs, payment gateways, etc.).\n\nConfigure the number of NAT Gateways in `stackConfig.vpc.nat`.\n\n**Adds cost:** NAT Gateway ~$32/month + data processing fees.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      usePrivateSubnetsWithNAT: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    usePrivateSubnetsWithNAT: true\n  });\n\n  return { resources: { api } };\n});\n```").default(false),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### A container running 24/7 with a public HTTPS URL.\n\n---\n\nUse for APIs, web apps, and any service that needs to be always-on and reachable from the internet.\nIncludes TLS/SSL, auto-scaling, health checks, and zero-downtime deployments."), z.object({
      "type": z.literal("worker-service").describe("#### The type identifier for a worker service.\n\n---\n\n**Example (YAML):**\n\n```yaml\n# Always-on worker that processes background jobs from a queue.\nresources:\n  jobProcessor:\n    type: worker-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: ./src/worker.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - jobsQueue\n  jobsQueue:\n    type: sqs-queue\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { SqsQueue, StacktapeImageBuildpackPackaging, WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const jobsQueue = new SqsQueue({});\n  const jobProcessor = new WorkerService({\n    packaging: new StacktapeImageBuildpackPackaging({\n      entryfilePath: './src/worker.ts'\n    }),\n    resources: {\n      cpu: 0.5,\n      memory: 1024\n    },\n    connectTo: [jobsQueue]\n  });\n\n  return {\n    resources: { jobProcessor, jobsQueue }\n  };\n});\n```"),
      "properties": z.object({
        "packaging": z.union([z.object({
            "type": z.literal("prebuilt-image"),
            "properties": z.object({
              "repositoryCredentialsSecretArn": z.string().optional().describe("#### The ARN of a secret containing credentials for a private container registry.\n\n---\n\nThe secret must be a JSON object with `username` and `password` keys.\nYou can create secrets using the `stacktape secret:create` command.\n\n**Example (YAML):**\n\n```yaml\nresources:\n privateImageService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: registry.example.com/my-org/my-app:latest\n         repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const privateImageService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'registry.example.com/my-org/my-app:latest',\n       repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { privateImageService } };\n});\n```").optional(),
              "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: node:22-alpine\n         entryPoint:\n           - /bin/sh\n           - -c\n           - node server.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'node:22-alpine',\n       entryPoint: ['/bin/sh', '-c', 'node server.js']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
              "image": z.string().describe("#### The name or URL of the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu'\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```"),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n           command:\n             - -i\n             - input.mp4\n             - output.webm\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu',\n         command: ['-i', 'input.mp4', 'output.webm']\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```").optional() }).strict()
            .describe("#### Configures a pre-built container image.") }).strict()
          .describe("#### Uses a pre-built container image.\n\n---\n\nWith `prebuilt-image`, you provide a reference to an existing container image.\nThis can be a public image from Docker Hub or a private image from any container registry.\n\nFor private registries, configure `repositoryCredentialsSecretArn` with credentials stored in AWS Secrets Manager."), z.object({
            "type": z.literal("custom-dockerfile"),
            "properties": z.object({
              "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: custom-dockerfile\n       properties:\n         buildContextPath: ./app\n         entryPoint:\n           - /app/entrypoint.sh\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'custom-dockerfile',\n     properties: {\n       buildContextPath: './app',\n       entryPoint: ['/app/entrypoint.sh']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
              "dockerfilePath": z.string().optional().describe("#### The path to the Dockerfile, relative to `buildContextPath`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           dockerfilePath: docker/Dockerfile.prod\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         dockerfilePath: 'docker/Dockerfile.prod'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
              "buildContextPath": z.string().describe("#### The path to the build context directory, relative to your Stacktape configuration file.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
              "buildArgs": z.array(z.object({
                "argName": z.string().describe("#### Argument name\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                "value": z.string().describe("#### Argument value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```") }).strict()
              ).optional().describe("#### A list of arguments to pass to the `docker build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: NODE_ENV\n               value: production\n             - argName: BUILD_VERSION\n               value: \"1.4.2\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           { argName: 'NODE_ENV', value: 'production' },\n           { argName: 'BUILD_VERSION', value: '1.4.2' }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           command:\n             - node\n             - dist/process.js\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         command: ['node', 'dist/process.js']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional() }).strict()
            .describe("#### Configures an image to be built by Stacktape from a specified Dockerfile.") }).strict()
          .describe("#### Builds a container image from your own Dockerfile.\n\n---\n\nWith `custom-dockerfile`, you provide a path to your Dockerfile and build context.\nStacktape builds the image and uploads it to a managed ECR repository.\n\nThis gives you full control over the container environment and is ideal for complex setups."), z.object({
            "type": z.literal("external-buildpack"),
            "properties": z.object({
              "builder": z.string().optional().describe("#### The Buildpack Builder to use.\n\n---\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           builder: paketobuildpacks/builder-jammy-full\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         builder: 'paketobuildpacks/builder-jammy-full'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
              "buildpacks": z.array(z.string()).optional().describe("#### The specific Buildpack to use.\n\n---\n\nBy default, the buildpack is detected automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           buildpacks:\n             - paketo-buildpacks/nodejs\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         buildpacks: ['paketo-buildpacks/nodejs']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional(),
              "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           command:\n             - npm\n             - run\n             - import\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         command: ['npm', 'run', 'import']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Builds a container image using an external buildpack.\n\n---\n\nExternal buildpacks (buildpacks.io) automatically detect your application type\nand build an optimized container image with zero configuration.\n\nThe default builder is `paketobuildpacks/builder-jammy-base`.\nYou can find buildpacks for almost any language or framework."), z.object({
            "type": z.literal("nixpacks"),
            "properties": z.object({
              "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
              "buildImage": z.string().optional().describe("#### The base image to use for building the application.\n\n---\n\nFor more details, see the [Nixpacks documentation](https://nixpacks.com/docs/configuration/file#build-image).\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         buildImage: ubuntu:22.04\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       buildImage: 'ubuntu:22.04'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "providers": z.array(z.string()).optional().describe("#### A list of providers to use for determining the build and runtime environments.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         providers:\n           - node\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       providers: ['node']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startCmd": z.string().optional().describe("#### The command to execute when starting the application.\n\n---\n\nThis overrides the default start command inferred by Nixpacks.\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startCmd: node dist/worker.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startCmd: 'node dist/worker.js'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startRunImage": z.string().optional().describe("#### The base image to use for running the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startRunImage: gcr.io/distroless/nodejs22-debian12\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startRunImage: 'gcr.io/distroless/nodejs22-debian12'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startOnlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in the runtime environment; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startOnlyIncludeFiles:\n           - dist\n           - node_modules\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startOnlyIncludeFiles: ['dist', 'node_modules']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "phases": z.array(z.object({
                "name": z.string().describe("#### The name of the build phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                "cmds": z.array(z.string()).optional().describe("#### A list of shell commands to execute in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n               - npm prune --omit=dev\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build', 'npm prune --omit=dev']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixPkgs": z.array(z.string()).optional().describe("#### A list of Nix packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - ffmpeg\n               - imagemagick\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['ffmpeg', 'imagemagick']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixLibs": z.array(z.string()).optional().describe("#### A list of Nix libraries to include in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixLibs:\n               - openssl\n               - zlib\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixLibs: ['openssl', 'zlib']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixOverlay": z.array(z.string()).optional().describe("#### A list of Nix overlay files to apply in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixOverlay:\n               - ./nix/overlay.nix\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixOverlay: ['./nix/overlay.nix']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixpkgsArchive": z.string().optional().describe("#### The Nixpkgs archive to use.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - nodejs_22\n             nixpkgsArchive: bf3287dac860542719b3849d6970d22f635f9da1\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['nodejs_22'],\n           nixpkgsArchive: 'bf3287dac860542719b3849d6970d22f635f9da1'\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "aptPkgs": z.array(z.string()).optional().describe("#### A list of APT packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             aptPkgs:\n               - libpq-dev\n               - poppler-utils\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           aptPkgs: ['libpq-dev', 'poppler-utils']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "cacheDirectories": z.array(z.string()).optional().describe("#### A list of directories to cache between builds to speed up subsequent builds.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n             cacheDirectories:\n               - node_modules/.cache\n               - /root/.npm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci'],\n           cacheDirectories: ['node_modules/.cache', '/root/.npm']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "onlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in this phase; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n             onlyIncludeFiles:\n               - src\n               - package.json\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build'],\n           onlyIncludeFiles: ['src', 'package.json']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
              ).optional().describe("#### The build phases for the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n           - name: build\n             cmds:\n               - npm run build\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         { name: 'install', cmds: ['npm ci'] },\n         { name: 'build', cmds: ['npm run build'] }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Builds a container image using Nixpacks.\n\n---\n\nNixpacks automatically detects your application type and builds an optimized container image.\nIn most cases, no configuration is required.\n\nIt supports a wide range of languages and frameworks out of the box."), z.object({
            "type": z.literal("stacktape-image-buildpack"),
            "properties": z.object({
              "languageSpecificConfig": z.union([z.object({
                  "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                  "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                  "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                , z.object({
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                  "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                  "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                , z.object({
                  "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                , z.record(z.string(), z.any()), z.object({
                  "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                , z.object({
                  "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                , z.object({
                  "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                  "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
              ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { apiService } };\n});\n```").optional(),
              "requiresGlibcBinaries": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use glibc instead of musl (Alpine default). Enable if native dependencies require glibc.\n\n---\n\nResults in a larger image. Common packages needing this: `sharp`, `canvas`, `bcrypt`, `puppeteer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         requiresGlibcBinaries: true\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       requiresGlibcBinaries: true\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
              "customDockerBuildCommands": z.array(z.string()).optional().describe("#### A list of commands to be executed during the `docker build` process.\n\n---\n\nThese commands are executed using the `RUN` directive in the Dockerfile.\nThis is useful for installing additional system dependencies in your container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         customDockerBuildCommands:\n           - apt-get update && apt-get install -y poppler-utils\n           - fc-cache -f\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       customDockerBuildCommands: [\n         'apt-get update && apt-get install -y poppler-utils',\n         'fc-cache -f'\n       ]\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
              "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
              "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            .describe("#### Configures an image to be built automatically by Stacktape from your source code.") }).strict()
          .describe("#### A zero-config buildpack that creates a container image from your source code.\n\n---\n\nThe `stacktape-image-buildpack` automatically bundles your code and dependencies into an optimized container image.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, and Go.\n\nFor JS/TS, your code is bundled into a single file with source maps.\nThe resulting image is uploaded to a managed ECR repository.")]).describe("#### Configures the container image for the service.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return { resources: { api } };\n});\n```"),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("#### Environment variables injected into the container at runtime.\n\n---\n\nUse for configuration like API keys, feature flags, or secrets.\nVariables from `connectTo` (e.g., `STP_MY_DATABASE_CONNECTION_STRING`) are added automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: LOG_LEVEL\n          value: info\n        - name: DATABASE_URL\n          value: $ResourceParam('mainDb', 'connectionString')\n      connectTo:\n        - mainDb\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $ResourceParam, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      LOG_LEVEL: 'info',\n      DATABASE_URL: $ResourceParam('mainDb', 'connectionString')\n    },\n    connectTo: [mainDb]\n  });\n\n  return { resources: { api, mainDb } };\n});\n```").optional(),
        "logging": z.object({
          "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            disabled: true\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { disabled: true }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(false),
          "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 30 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(90),
          "logForwarding": z.union([z.object({
              "type": z.literal("http-endpoint"),
              "properties": z.object({
                "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            }).strict()
            , z.object({
              "type": z.literal("highlight"),
              "properties": z.object({
                "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
            }).strict()
            , z.object({
              "type": z.literal("datadog"),
              "properties": z.object({
                "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
            }).strict()
          ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
        .optional().describe("#### Logging configuration.\n\n---\n\nContainer output (`stdout`/`stderr`) is automatically sent to CloudWatch and retained for 90 days.\nView logs with `stacktape logs` or in the Stacktape Console.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      logging:\n        retentionDays: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    logging: { retentionDays: 30 }\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
        "resources": z.object({
          "cpu": z.union([z.literal(0.25), z.literal(0.5), z.literal(1), z.literal(16), z.literal(2), z.literal(4), z.literal(8)]).optional().describe("#### vCPUs for the workload (Fargate). Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 2\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 2,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "memory": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Memory in MB. Must be compatible with the vCPU count on Fargate.\n\n---\n\nFargate valid combos: 0.25 vCPU → 512-2048 MB, 0.5 → 1024-4096, 1 → 2048-8192, 2 → 4096-16384,\n4 → 8192-30720, 8 → 16384-61440, 16 → 32768-122880.\nFor EC2: auto-detected from instance type if omitted.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 4096\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 1,\n      memory: 4096\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "instanceTypes": z.array(z.string()).optional().describe("#### EC2 instance types for the workload (e.g., `t3.medium`, `c6g.large`). Use instead of `cpu`/`memory`.\n\n---\n\nFirst type in the list is preferred. Instances auto-scale and are refreshed weekly for patching.\nTip: specify a single type and omit `cpu`/`memory` for optimal sizing.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - c6g.large\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['c6g.large']\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "enableWarmPool": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Keep pre-initialized EC2 instances ready for faster scaling. Only works with a single instance type.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        instanceTypes:\n          - t3.medium\n        enableWarmPool: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      instanceTypes: ['t3.medium'],\n      enableWarmPool: true\n    }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "architecture": z.enum(["arm64","x86_64"]).optional().describe("#### CPU architecture for Fargate. `arm64` is ~20% cheaper. Ignored when using `instanceTypes`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: {\n      cpu: 0.5,\n      memory: 1024,\n      architecture: 'arm64'\n    }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
        .describe("#### CPU, memory, and compute engine for the container.\n\n---\n\nTwo compute engines:\n- **Fargate** (default): Serverless — just specify `cpu` and `memory`.\n- **EC2**: Specify `instanceTypes` for more control and potentially lower cost.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 1\n        memory: 2048\n        architecture: arm64\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 1, memory: 2048, architecture: 'arm64' }\n  });\n\n  return { resources: { api } };\n});\n```"),
        "scaling": z.object({
          "minInstances": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Minimum running instances. Set to 0 is not supported — minimum is 1.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8\n    }\n  });\n  return { resources: { app } };\n});\n```").default(1),
          "maxInstances": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Maximum running instances. Traffic is distributed across all instances.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8\n    }\n  });\n  return { resources: { app } };\n});\n```").default(1),
          "scalingPolicy": z.object({
            "keepAvgCpuUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Scale out when avg CPU exceeds this %, scale in when it drops below.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 65\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: {\n        keepAvgCpuUtilizationUnder: 65\n      }\n    }\n  });\n  return { resources: { app } };\n});\n```").default(80),
            "keepAvgMemoryUtilizationUnder": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Scale out when avg memory exceeds this %, scale in when it drops below.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgMemoryUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: {\n        keepAvgMemoryUtilizationUnder: 70\n      }\n    }\n  });\n  return { resources: { app } };\n});\n```").default(80) }).strict()
          .optional().describe("#### When to scale: CPU and/or memory utilization targets.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 8\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 60\n          keepAvgMemoryUtilizationUnder: 75\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 8,\n      scalingPolicy: { keepAvgCpuUtilizationUnder: 60, keepAvgMemoryUtilizationUnder: 75 }\n    }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
        .optional().describe("#### Auto-scaling: add/remove container instances based on demand.\n\n---\n\nTraffic is automatically distributed across all running containers.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      scaling:\n        minInstances: 2\n        maxInstances: 10\n        scalingPolicy:\n          keepAvgCpuUtilizationUnder: 70\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    scaling: {\n      minInstances: 2,\n      maxInstances: 10,\n      scalingPolicy: { keepAvgCpuUtilizationUnder: 70 }\n    }\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
        "internalHealthCheck": z.object({
          "healthCheckCommand": z.array(z.string()).describe("#### Command to check health. E.g., `[\"CMD-SHELL\", \"curl -f http://localhost/ || exit 1\"]`. Exit 0 = healthy.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1']\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "intervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks (5-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            intervalSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          intervalSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(30),
          "timeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a check is considered failed (2-60).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            timeoutSeconds: 10\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          timeoutSeconds: 10\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(5),
          "retries": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Consecutive failures before marking unhealthy (1-10).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            retries: 5\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          retries: 5\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(3),
          "startPeriodSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Grace period (seconds) before counting failures. Gives the container time to start (0-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            startPeriodSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          startPeriodSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
        .optional().describe("#### Health check that auto-replaces unhealthy containers.\n\n---\n\nIf a container fails the health check, it's terminated and replaced automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      internalHealthCheck:\n        healthCheckCommand:\n          - CMD-SHELL\n          - curl -f http://localhost:3000/health || exit 1\n        intervalSeconds: 30\n        retries: 3\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    internalHealthCheck: {\n      healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/health || exit 1'],\n      intervalSeconds: 30,\n      retries: 3\n    }\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
        "stopTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds to wait for graceful shutdown before force-killing the container.\n\n---\n\nThe container receives `SIGTERM` first, then `SIGKILL` after this timeout. Must be 2-120.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      stopTimeout: 30\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    stopTimeout: 30\n  });\n\n  return { resources: { api } };\n});\n```").default(2),
        "enableRemoteSessions": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Allow SSH-like access to running containers for debugging.\n\n---\n\nEnables `stacktape container:session` to open an interactive shell inside the container.\nAdds a small SSM agent that uses minimal CPU/memory.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      enableRemoteSessions: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    enableRemoteSessions: true\n  });\n\n  return { resources: { api } };\n});\n```").default(false),
        "volumeMounts": z.array(z.object({
          "type": z.literal("efs").describe("#### The type of the volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
          "properties": z.object({
            "efsFilesystemName": z.string().describe("#### Name of the `efs-filesystem` resource defined in your config.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "rootDirectory": z.string().optional().describe("#### Subdirectory within the EFS filesystem to mount. Restricts access to that directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                rootDirectory: /uploads\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              rootDirectory: '/uploads',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "mountPath": z.string().describe("#### Absolute path inside the container where the volume is mounted (e.g., `/data`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
          .describe("#### Properties for the EFS volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
        ).optional().describe("#### Persistent EFS volumes shared across containers and restarts.\n\n---\n\nData stored in EFS volumes persists even when containers are replaced.\nMultiple containers can mount the same volume. All data is encrypted in transit.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      volumeMounts:\n        - type: efs\n          properties:\n            efsFilesystemName: sharedData\n            mountPath: /data\n  sharedData:\n    type: efs-filesystem\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, EfsFilesystem, ContainerEfsMount, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedData = new EfsFilesystem({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    volumeMounts: [\n      new ContainerEfsMount({ type: 'efs', properties: { efsFilesystemName: 'sharedData', mountPath: '/data' } })\n    ]\n  });\n\n  return { resources: { api, sharedData } };\n});\n```").optional(),
        "sideContainers": z.array(z.object({
          "containerType": z.enum(["always-running","run-on-init"]).describe("#### When and how this sidecar container runs.\n\n---\n\n- **`run-on-init`**: Must exit with code 0 before the main container starts. Use for migrations or setup.\n- **`always-running`**: Runs alongside the main container for its entire lifetime. If it crashes, the whole task fails.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      sideContainers:\n        - name: migrations\n          containerType: run-on-init\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/migrate.ts\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    sideContainers: [\n      {\n        name: 'migrations',\n        containerType: 'run-on-init',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/migrate.ts' })\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```"),
          "name": z.string().describe("#### Unique container name within this workload.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' })\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "packaging": z.union([z.object({
              "type": z.literal("prebuilt-image"),
              "properties": z.object({
                "repositoryCredentialsSecretArn": z.string().optional().describe("#### The ARN of a secret containing credentials for a private container registry.\n\n---\n\nThe secret must be a JSON object with `username` and `password` keys.\nYou can create secrets using the `stacktape secret:create` command.\n\n**Example (YAML):**\n\n```yaml\nresources:\n privateImageService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: registry.example.com/my-org/my-app:latest\n         repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const privateImageService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'registry.example.com/my-org/my-app:latest',\n       repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { privateImageService } };\n});\n```").optional(),
                "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: node:22-alpine\n         entryPoint:\n           - /bin/sh\n           - -c\n           - node server.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'node:22-alpine',\n       entryPoint: ['/bin/sh', '-c', 'node server.js']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
                "image": z.string().describe("#### The name or URL of the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu'\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n           command:\n             - -i\n             - input.mp4\n             - output.webm\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu',\n         command: ['-i', 'input.mp4', 'output.webm']\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```").optional() }).strict()
              .describe("#### Configures a pre-built container image.") }).strict()
            .describe("#### Uses a pre-built container image.\n\n---\n\nWith `prebuilt-image`, you provide a reference to an existing container image.\nThis can be a public image from Docker Hub or a private image from any container registry.\n\nFor private registries, configure `repositoryCredentialsSecretArn` with credentials stored in AWS Secrets Manager."), z.object({
              "type": z.literal("custom-dockerfile"),
              "properties": z.object({
                "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: custom-dockerfile\n       properties:\n         buildContextPath: ./app\n         entryPoint:\n           - /app/entrypoint.sh\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'custom-dockerfile',\n     properties: {\n       buildContextPath: './app',\n       entryPoint: ['/app/entrypoint.sh']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
                "dockerfilePath": z.string().optional().describe("#### The path to the Dockerfile, relative to `buildContextPath`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           dockerfilePath: docker/Dockerfile.prod\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         dockerfilePath: 'docker/Dockerfile.prod'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "buildContextPath": z.string().describe("#### The path to the build context directory, relative to your Stacktape configuration file.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                "buildArgs": z.array(z.object({
                  "argName": z.string().describe("#### Argument name\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                  "value": z.string().describe("#### Argument value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```") }).strict()
                ).optional().describe("#### A list of arguments to pass to the `docker build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: NODE_ENV\n               value: production\n             - argName: BUILD_VERSION\n               value: \"1.4.2\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           { argName: 'NODE_ENV', value: 'production' },\n           { argName: 'BUILD_VERSION', value: '1.4.2' }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           command:\n             - node\n             - dist/process.js\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         command: ['node', 'dist/process.js']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built by Stacktape from a specified Dockerfile.") }).strict()
            .describe("#### Builds a container image from your own Dockerfile.\n\n---\n\nWith `custom-dockerfile`, you provide a path to your Dockerfile and build context.\nStacktape builds the image and uploads it to a managed ECR repository.\n\nThis gives you full control over the container environment and is ideal for complex setups."), z.object({
              "type": z.literal("external-buildpack"),
              "properties": z.object({
                "builder": z.string().optional().describe("#### The Buildpack Builder to use.\n\n---\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           builder: paketobuildpacks/builder-jammy-full\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         builder: 'paketobuildpacks/builder-jammy-full'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "buildpacks": z.array(z.string()).optional().describe("#### The specific Buildpack to use.\n\n---\n\nBy default, the buildpack is detected automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           buildpacks:\n             - paketo-buildpacks/nodejs\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         buildpacks: ['paketo-buildpacks/nodejs']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional(),
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
                "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           command:\n             - npm\n             - run\n             - import\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         command: ['npm', 'run', 'import']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Builds a container image using an external buildpack.\n\n---\n\nExternal buildpacks (buildpacks.io) automatically detect your application type\nand build an optimized container image with zero configuration.\n\nThe default builder is `paketobuildpacks/builder-jammy-base`.\nYou can find buildpacks for almost any language or framework."), z.object({
              "type": z.literal("nixpacks"),
              "properties": z.object({
                "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                "buildImage": z.string().optional().describe("#### The base image to use for building the application.\n\n---\n\nFor more details, see the [Nixpacks documentation](https://nixpacks.com/docs/configuration/file#build-image).\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         buildImage: ubuntu:22.04\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       buildImage: 'ubuntu:22.04'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "providers": z.array(z.string()).optional().describe("#### A list of providers to use for determining the build and runtime environments.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         providers:\n           - node\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       providers: ['node']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startCmd": z.string().optional().describe("#### The command to execute when starting the application.\n\n---\n\nThis overrides the default start command inferred by Nixpacks.\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startCmd: node dist/worker.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startCmd: 'node dist/worker.js'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startRunImage": z.string().optional().describe("#### The base image to use for running the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startRunImage: gcr.io/distroless/nodejs22-debian12\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startRunImage: 'gcr.io/distroless/nodejs22-debian12'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "startOnlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in the runtime environment; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startOnlyIncludeFiles:\n           - dist\n           - node_modules\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startOnlyIncludeFiles: ['dist', 'node_modules']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "phases": z.array(z.object({
                  "name": z.string().describe("#### The name of the build phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                  "cmds": z.array(z.string()).optional().describe("#### A list of shell commands to execute in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n               - npm prune --omit=dev\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build', 'npm prune --omit=dev']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixPkgs": z.array(z.string()).optional().describe("#### A list of Nix packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - ffmpeg\n               - imagemagick\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['ffmpeg', 'imagemagick']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixLibs": z.array(z.string()).optional().describe("#### A list of Nix libraries to include in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixLibs:\n               - openssl\n               - zlib\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixLibs: ['openssl', 'zlib']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixOverlay": z.array(z.string()).optional().describe("#### A list of Nix overlay files to apply in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixOverlay:\n               - ./nix/overlay.nix\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixOverlay: ['./nix/overlay.nix']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "nixpkgsArchive": z.string().optional().describe("#### The Nixpkgs archive to use.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - nodejs_22\n             nixpkgsArchive: bf3287dac860542719b3849d6970d22f635f9da1\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['nodejs_22'],\n           nixpkgsArchive: 'bf3287dac860542719b3849d6970d22f635f9da1'\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "aptPkgs": z.array(z.string()).optional().describe("#### A list of APT packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             aptPkgs:\n               - libpq-dev\n               - poppler-utils\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           aptPkgs: ['libpq-dev', 'poppler-utils']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "cacheDirectories": z.array(z.string()).optional().describe("#### A list of directories to cache between builds to speed up subsequent builds.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n             cacheDirectories:\n               - node_modules/.cache\n               - /root/.npm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci'],\n           cacheDirectories: ['node_modules/.cache', '/root/.npm']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                  "onlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in this phase; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n             onlyIncludeFiles:\n               - src\n               - package.json\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build'],\n           onlyIncludeFiles: ['src', 'package.json']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
                ).optional().describe("#### The build phases for the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n           - name: build\n             cmds:\n               - npm run build\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         { name: 'install', cmds: ['npm ci'] },\n         { name: 'build', cmds: ['npm run build'] }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
            }).strict()
            .describe("#### Builds a container image using Nixpacks.\n\n---\n\nNixpacks automatically detects your application type and builds an optimized container image.\nIn most cases, no configuration is required.\n\nIt supports a wide range of languages and frameworks out of the box."), z.object({
              "type": z.literal("stacktape-image-buildpack"),
              "properties": z.object({
                "languageSpecificConfig": z.union([z.object({
                    "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                    "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                    "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                    "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                  , z.object({
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                    "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                    "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                    "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                  , z.object({
                    "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                    "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                  , z.record(z.string(), z.any()), z.object({
                    "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                  , z.object({
                    "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                  , z.object({
                    "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                    "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
                ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { apiService } };\n});\n```").optional(),
                "requiresGlibcBinaries": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use glibc instead of musl (Alpine default). Enable if native dependencies require glibc.\n\n---\n\nResults in a larger image. Common packages needing this: `sharp`, `canvas`, `bcrypt`, `puppeteer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         requiresGlibcBinaries: true\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       requiresGlibcBinaries: true\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "customDockerBuildCommands": z.array(z.string()).optional().describe("#### A list of commands to be executed during the `docker build` process.\n\n---\n\nThese commands are executed using the `RUN` directive in the Dockerfile.\nThis is useful for installing additional system dependencies in your container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         customDockerBuildCommands:\n           - apt-get update && apt-get install -y poppler-utils\n           - fc-cache -f\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       customDockerBuildCommands: [\n         'apt-get update && apt-get install -y poppler-utils',\n         'fc-cache -f'\n       ]\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
                "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
                "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              .describe("#### Configures an image to be built automatically by Stacktape from your source code.") }).strict()
            .describe("#### A zero-config buildpack that creates a container image from your source code.\n\n---\n\nThe `stacktape-image-buildpack` automatically bundles your code and dependencies into an optimized container image.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, and Go.\n\nFor JS/TS, your code is bundled into a single file with source maps.\nThe resulting image is uploaded to a managed ECR repository.")]).describe("#### How to build or specify the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: custom-dockerfile\n            properties:\n              buildContextPath: ./api\n              dockerfilePath: Dockerfile\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, CustomDockerfilePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new CustomDockerfilePackaging({ buildContextPath: './api', dockerfilePath: 'Dockerfile' })\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
          "essential": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### If `true` (default), the entire workload restarts when this container fails.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n        - name: metrics-sidecar\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: prom/statsd-exporter:latest\n          essential: false\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, PrebuiltImagePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'api', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }) },\n      {\n        name: 'metrics-sidecar',\n        packaging: new PrebuiltImagePackaging({ image: 'prom/statsd-exporter:latest' }),\n        essential: false\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "logging": z.object({
            "disabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Disable logging to CloudWatch.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            disabled: true\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { disabled: true }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(false),
            "retentionDays": z.union([z.literal(1), z.literal(120), z.literal(14), z.literal(150), z.literal(180), z.literal(1827), z.literal(3), z.literal(30), z.literal(365), z.literal(3653), z.literal(400), z.literal(5), z.literal(545), z.literal(60), z.literal(7), z.literal(731), z.literal(90)]).optional().describe("#### How many days to keep logs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 30 }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(90),
            "logForwarding": z.union([z.object({
                "type": z.literal("http-endpoint"),
                "properties": z.object({
                  "endpointUrl": z.string().describe("#### HTTPS endpoint URL where logs are sent.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "gzipEncodingEnabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Compress request body with GZIP to reduce transfer costs.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            gzipEncodingEnabled: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          gzipEncodingEnabled: true\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(false),
                  "parameters": z.record(z.string(), z.string()).optional().describe("#### Extra metadata sent in the `X-Amz-Firehose-Common-Attributes` header.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            parameters:\n              environment: production\n              team: backend\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          parameters: {\n            environment: 'production',\n            team: 'backend'\n          }\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional(),
                  "retryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Total retry time (seconds) before sending failed logs to a backup S3 bucket.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            retryDuration: 600\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          retryDuration: 600\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default(300),
                  "accessKey": z.string().optional().describe("#### Auth credential sent in `X-Amz-Firehose-Access-Key` header. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: http-endpoint\n          properties:\n            endpointUrl: https://logs.example.com/v1/ingest\n            accessKey: $Secret('logForwarding.accessKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'http-endpoint',\n        properties: {\n          endpointUrl: 'https://logs.example.com/v1/ingest',\n          accessKey: $Secret('logForwarding.accessKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
              }).strict()
              , z.object({
                "type": z.literal("highlight"),
                "properties": z.object({
                  "projectId": z.string().describe("#### Your Highlight.io project ID (from the Highlight console).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Highlight.io endpoint. Override for self-hosted or regional endpoints.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: highlight\n          properties:\n            projectId: \"1jdkoe52\"\n            endpointUrl: https://pub.highlight.io/v1/logs/firehose\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'highlight',\n        properties: {\n          projectId: '1jdkoe52',\n          endpointUrl: 'https://pub.highlight.io/v1/logs/firehose'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```") }).strict()
              }).strict()
              , z.object({
                "type": z.literal("datadog"),
                "properties": z.object({
                  "apiKey": z.string().describe("#### Your Datadog API key. Store as `$Secret()` for security.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey')\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```"),
                  "endpointUrl": z.string().optional().describe("#### Datadog endpoint. Use the EU URL if your account is in the EU region.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      logging:\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n            endpointUrl: https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    logging: {\n      logForwarding: {\n        type: 'datadog',\n        properties: {\n          apiKey: $Secret('datadog.apiKey'),\n          endpointUrl: 'https://aws-kinesis-http-intake.logs.datadoghq.eu/v1/input'\n        }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").default("https://aws-kinesis-http-intake.logs.datadoghq.com/v1/input") }).strict()
              }).strict()
            ]).optional().describe("#### Forward logs to an external service (Datadog, Highlight.io, or any HTTP endpoint).\n\n---\n\nUses Kinesis Data Firehose (~$0.03/GB). Failed deliveries go to a backup S3 bucket.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  apiFunction:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/api.ts\n      memory: 512\n      timeout: 10\n      logging:\n        retentionDays: 90\n        logForwarding:\n          type: datadog\n          properties:\n            apiKey: $Secret('datadog.apiKey')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, StacktapeLambdaBuildpackPackaging, defineConfig, $Secret } from 'stacktape';\n\nexport default defineConfig(() => {\n  const apiFunction = new LambdaFunction({\n    packaging: new StacktapeLambdaBuildpackPackaging({ entryfilePath: 'src/api.ts' }),\n    memory: 512,\n    timeout: 10,\n    logging: {\n      retentionDays: 90,\n      logForwarding: {\n        type: 'datadog',\n        properties: { apiKey: $Secret('datadog.apiKey') }\n      }\n    }\n  });\n  return { resources: { apiFunction } };\n});\n```").optional() }).strict()
          .optional().describe("#### Container logging (stdout/stderr). Sent to CloudWatch, viewable with `stacktape logs`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          logging:\n            retentionDays: 14\n            disabled: false\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        logging: { retentionDays: 14, disabled: false }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "dependsOn": z.array(z.object({
            "containerName": z.string().describe("The name of the container that this container depends on."),
            "condition": z.enum(["COMPLETE","HEALTHY","START","SUCCESS"]).describe("#### The condition that the dependency container must meet.\n---\nAvailable conditions:\n- `START`: The dependency has started.\n- `COMPLETE`: The dependency has finished executing (regardless of success).\n- `SUCCESS`: The dependency has finished with an exit code of `0`.\n- `HEALTHY`: The dependency has passed its first health check.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: redis\n          packaging:\n            type: prebuilt-image\n            properties:\n              image: redis:7\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - redis-cli ping || exit 1\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          dependsOn:\n            - containerName: redis\n              condition: HEALTHY\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, PrebuiltImagePackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'redis',\n        packaging: new PrebuiltImagePackaging({ image: 'redis:7' }),\n        internalHealthCheck: { healthCheckCommand: ['CMD-SHELL', 'redis-cli ping || exit 1'] }\n      },\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        dependsOn: [\n          {\n            containerName: 'redis',\n            condition: 'HEALTHY'\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```") }).strict()
          ).optional().describe("#### Start this container only after the listed containers reach a specific state.\n\n---\n\nE.g., wait for a database sidecar to be `HEALTHY` before starting the app container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: migrations\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/migrate.ts\n          essential: false\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          dependsOn:\n            - containerName: migrations\n              condition: SUCCESS\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      { name: 'migrations', packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/migrate.ts' }), essential: false },\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        dependsOn: [{ containerName: 'migrations', condition: 'SUCCESS' }]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "environment": z.array(z.object({
            "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
            "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
          ).optional().describe("#### Environment variables injected at runtime. Use `$ResourceParam()` or `$Secret()` for dynamic values.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          environment:\n            - name: NODE_ENV\n              value: production\n            - name: DATABASE_URL\n              value: $ResourceParam('appDb', 'connectionString')\n            - name: STRIPE_KEY\n              value: $Secret('stripe.secretKey')\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - appDb\n  appDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db.password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.6'\n          primaryInstance:\n            instanceSize: db.t3.micro\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, RelationalDatabase, StacktapeImageBuildpackPackaging, $ResourceParam, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const appDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db.password') },\n    engine: { type: 'postgres', properties: { version: '16.6', primaryInstance: { instanceSize: 'db.t3.micro' } } }\n  });\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        environment: {\n          NODE_ENV: 'production',\n          DATABASE_URL: $ResourceParam('appDb', 'connectionString'),\n          STRIPE_KEY: $Secret('stripe.secretKey')\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [appDb]\n  });\n  return { resources: { app, appDb } };\n});\n```").optional(),
          "internalHealthCheck": z.object({
            "healthCheckCommand": z.array(z.string()).describe("#### Command to check health. E.g., `[\"CMD-SHELL\", \"curl -f http://localhost/ || exit 1\"]`. Exit 0 = healthy.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1']\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```"),
            "intervalSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds between health checks (5-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            intervalSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          intervalSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(30),
            "timeoutSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds before a check is considered failed (2-60).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            timeoutSeconds: 10\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          timeoutSeconds: 10\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(5),
            "retries": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Consecutive failures before marking unhealthy (1-10).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            retries: 5\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          retries: 5\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(3),
            "startPeriodSeconds": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Grace period (seconds) before counting failures. Gives the container time to start (0-300).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/ || exit 1\n            startPeriodSeconds: 60\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1'],\n          startPeriodSeconds: 60\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional() }).strict()
          .optional().describe("#### Command-based health check. If it fails on an essential container, the workload instance is replaced.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          internalHealthCheck:\n            healthCheckCommand:\n              - CMD-SHELL\n              - curl -f http://localhost:3000/health || exit 1\n            intervalSeconds: 30\n            retries: 3\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        internalHealthCheck: {\n          healthCheckCommand: ['CMD-SHELL', 'curl -f http://localhost:3000/health || exit 1'],\n          intervalSeconds: 30,\n          retries: 3\n        }\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").optional(),
          "stopTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("#### Seconds to wait after SIGTERM before SIGKILL (2-120).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          stopTimeout: 30\n      resources:\n        cpu: 0.5\n        memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        stopTimeout: 30\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app } };\n});\n```").default(2),
          "volumeMounts": z.array(z.object({
            "type": z.literal("efs").describe("#### The type of the volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
            "properties": z.object({
              "efsFilesystemName": z.string().describe("#### Name of the `efs-filesystem` resource defined in your config.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
              "rootDirectory": z.string().optional().describe("#### Subdirectory within the EFS filesystem to mount. Restricts access to that directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                rootDirectory: /uploads\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              rootDirectory: '/uploads',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```"),
              "mountPath": z.string().describe("#### Absolute path inside the container where the volume is mounted (e.g., `/data`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: {\n              efsFilesystemName: 'sharedStorage',\n              mountPath: '/data'\n            }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
            .describe("#### Properties for the EFS volume mount.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          {\n            type: 'efs',\n            properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' }\n          }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```") }).strict()
          ).optional().describe("#### Mount EFS volumes for persistent, shared storage across containers.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  app:\n    type: multi-container-workload\n    properties:\n      containers:\n        - name: api\n          packaging:\n            type: stacktape-image-buildpack\n            properties:\n              entryfilePath: src/server.ts\n          volumeMounts:\n            - type: efs\n              properties:\n                efsFilesystemName: sharedStorage\n                mountPath: /data\n      resources:\n        cpu: 0.5\n        memory: 1024\n  sharedStorage:\n    type: efs-filesystem\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { MultiContainerWorkload, EfsFilesystem, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const sharedStorage = new EfsFilesystem({});\n  const app = new MultiContainerWorkload({\n    containers: [\n      {\n        name: 'api',\n        packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n        volumeMounts: [\n          { type: 'efs', properties: { efsFilesystemName: 'sharedStorage', mountPath: '/data' } }\n        ]\n      }\n    ],\n    resources: { cpu: 0.5, memory: 1024 }\n  });\n  return { resources: { app, sharedStorage } };\n});\n```").optional() }).strict()
        ).optional().describe("#### Helper containers that run alongside the main container.\n\n---\n\n- **`run-on-init`**: Runs to completion before the main container starts (e.g., database migrations).\n- **`always-running`**: Runs for the entire lifecycle (e.g., log forwarders, monitoring agents).\n  Can reach the main container via `localhost`.").optional(),
        "usePrivateSubnetsWithNAT": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Deploy in private subnets with a static outbound IP via NAT Gateway.\n\n---\n\nThe container won't have a public IP. All outbound traffic routes through a NAT Gateway,\ngiving you a static IP you can whitelist in external services (APIs, payment gateways, etc.).\n\nConfigure the number of NAT Gateways in `stackConfig.vpc.nat`.\n\n**Adds cost:** NAT Gateway ~$32/month + data processing fees.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      usePrivateSubnetsWithNAT: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    usePrivateSubnetsWithNAT: true\n  });\n\n  return { resources: { api } };\n});\n```").default(false),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    .describe("#### Always-on container with no public URL. For background workers, queue processors, and internal tasks.\n\n---\n\nRuns 24/7 inside your VPC. Not reachable from the internet. Can connect to databases, queues, and other resources."), z.object({
      "type": z.literal("agentcore-runtime"),
      "properties": z.object({
        "packaging": z.union([z.object({
            "type": z.literal("prebuilt-image"),
            "properties": z.object({
              "repositoryCredentialsSecretArn": z.string().optional().describe("#### The ARN of a secret containing credentials for a private container registry.\n\n---\n\nThe secret must be a JSON object with `username` and `password` keys.\nYou can create secrets using the `stacktape secret:create` command.\n\n**Example (YAML):**\n\n```yaml\nresources:\n privateImageService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: registry.example.com/my-org/my-app:latest\n         repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const privateImageService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'registry.example.com/my-org/my-app:latest',\n       repositoryCredentialsSecretArn: $Secret('registry-credentials.arn')\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { privateImageService } };\n});\n```").optional(),
              "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: prebuilt-image\n       properties:\n         image: node:22-alpine\n         entryPoint:\n           - /bin/sh\n           - -c\n           - node server.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'prebuilt-image',\n     properties: {\n       image: 'node:22-alpine',\n       entryPoint: ['/bin/sh', '-c', 'node server.js']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
              "image": z.string().describe("#### The name or URL of the container image.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu'\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```"),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n encoder:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: prebuilt-image\n         properties:\n           image: jrottenberg/ffmpeg:6.1-ubuntu\n           command:\n             - -i\n             - input.mp4\n             - output.webm\n     resources:\n       cpu: 2\n       memory: 7680\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const encoder = new BatchJob({\n   container: {\n     packaging: {\n       type: 'prebuilt-image',\n       properties: {\n         image: 'jrottenberg/ffmpeg:6.1-ubuntu',\n         command: ['-i', 'input.mp4', 'output.webm']\n       }\n     }\n   },\n   resources: {\n     cpu: 2,\n     memory: 7680\n   }\n });\n return { resources: { encoder } };\n});\n```").optional() }).strict()
            .describe("#### Configures a pre-built container image.") }).strict()
          .describe("#### Uses a pre-built container image.\n\n---\n\nWith `prebuilt-image`, you provide a reference to an existing container image.\nThis can be a public image from Docker Hub or a private image from any container registry.\n\nFor private registries, configure `repositoryCredentialsSecretArn` with credentials stored in AWS Secrets Manager."), z.object({
            "type": z.literal("custom-dockerfile"),
            "properties": z.object({
              "entryPoint": z.array(z.string()).optional().describe("#### A script to be executed when the container starts.\n\n---\n\nThis overrides the `ENTRYPOINT` instruction in the Dockerfile.\n\n**Example (YAML):**\n\n```yaml\nresources:\n appService:\n   type: web-service\n   properties:\n     packaging:\n       type: custom-dockerfile\n       properties:\n         buildContextPath: ./app\n         entryPoint:\n           - /app/entrypoint.sh\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const appService = new WebService({\n   packaging: {\n     type: 'custom-dockerfile',\n     properties: {\n       buildContextPath: './app',\n       entryPoint: ['/app/entrypoint.sh']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { appService } };\n});\n```").optional(),
              "dockerfilePath": z.string().optional().describe("#### The path to the Dockerfile, relative to `buildContextPath`.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           dockerfilePath: docker/Dockerfile.prod\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         dockerfilePath: 'docker/Dockerfile.prod'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
              "buildContextPath": z.string().describe("#### The path to the build context directory, relative to your Stacktape configuration file.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
              "buildArgs": z.array(z.object({
                "argName": z.string().describe("#### Argument name\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```"),
                "value": z.string().describe("#### Argument value\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: PYTHON_VERSION\n               value: \"3.12\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           {\n             argName: 'PYTHON_VERSION',\n             value: '3.12'\n           }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```") }).strict()
              ).optional().describe("#### A list of arguments to pass to the `docker build` command.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           buildArgs:\n             - argName: NODE_ENV\n               value: production\n             - argName: BUILD_VERSION\n               value: \"1.4.2\"\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         buildArgs: [\n           { argName: 'NODE_ENV', value: 'production' },\n           { argName: 'BUILD_VERSION', value: '1.4.2' }\n         ]\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional(),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nThis overrides the `CMD` instruction in the Dockerfile.\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n processor:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: custom-dockerfile\n         properties:\n           buildContextPath: ./worker\n           command:\n             - node\n             - dist/process.js\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const processor = new BatchJob({\n   container: {\n     packaging: {\n       type: 'custom-dockerfile',\n       properties: {\n         buildContextPath: './worker',\n         command: ['node', 'dist/process.js']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { processor } };\n});\n```").optional() }).strict()
            .describe("#### Configures an image to be built by Stacktape from a specified Dockerfile.") }).strict()
          .describe("#### Builds a container image from your own Dockerfile.\n\n---\n\nWith `custom-dockerfile`, you provide a path to your Dockerfile and build context.\nStacktape builds the image and uploads it to a managed ECR repository.\n\nThis gives you full control over the container environment and is ideal for complex setups."), z.object({
            "type": z.literal("external-buildpack"),
            "properties": z.object({
              "builder": z.string().optional().describe("#### The Buildpack Builder to use.\n\n---\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           builder: paketobuildpacks/builder-jammy-full\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         builder: 'paketobuildpacks/builder-jammy-full'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
              "buildpacks": z.array(z.string()).optional().describe("#### The specific Buildpack to use.\n\n---\n\nBy default, the buildpack is detected automatically.\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           buildpacks:\n             - paketo-buildpacks/nodejs\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         buildpacks: ['paketo-buildpacks/nodejs']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional(),
              "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer'\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```"),
              "command": z.array(z.string()).optional().describe("#### A command to be executed when the container starts.\n\n---\n\nExample: `['/app/start.sh']`\n\n**Example (YAML):**\n\n```yaml\nresources:\n importer:\n   type: batch-job\n   properties:\n     container:\n       packaging:\n         type: external-buildpack\n         properties:\n           sourceDirectoryPath: ./importer\n           command:\n             - npm\n             - run\n             - import\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const importer = new BatchJob({\n   container: {\n     packaging: {\n       type: 'external-buildpack',\n       properties: {\n         sourceDirectoryPath: './importer',\n         command: ['npm', 'run', 'import']\n       }\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { importer } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Builds a container image using an external buildpack.\n\n---\n\nExternal buildpacks (buildpacks.io) automatically detect your application type\nand build an optimized container image with zero configuration.\n\nThe default builder is `paketobuildpacks/builder-jammy-base`.\nYou can find buildpacks for almost any language or framework."), z.object({
            "type": z.literal("nixpacks"),
            "properties": z.object({
              "sourceDirectoryPath": z.string().describe("#### The path to the source code directory.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
              "buildImage": z.string().optional().describe("#### The base image to use for building the application.\n\n---\n\nFor more details, see the [Nixpacks documentation](https://nixpacks.com/docs/configuration/file#build-image).\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         buildImage: ubuntu:22.04\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       buildImage: 'ubuntu:22.04'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "providers": z.array(z.string()).optional().describe("#### A list of providers to use for determining the build and runtime environments.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         providers:\n           - node\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       providers: ['node']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startCmd": z.string().optional().describe("#### The command to execute when starting the application.\n\n---\n\nThis overrides the default start command inferred by Nixpacks.\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startCmd: node dist/worker.js\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startCmd: 'node dist/worker.js'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startRunImage": z.string().optional().describe("#### The base image to use for running the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startRunImage: gcr.io/distroless/nodejs22-debian12\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startRunImage: 'gcr.io/distroless/nodejs22-debian12'\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "startOnlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in the runtime environment; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         startOnlyIncludeFiles:\n           - dist\n           - node_modules\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       startOnlyIncludeFiles: ['dist', 'node_modules']\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
              "phases": z.array(z.object({
                "name": z.string().describe("#### The name of the build phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```"),
                "cmds": z.array(z.string()).optional().describe("#### A list of shell commands to execute in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n               - npm prune --omit=dev\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build', 'npm prune --omit=dev']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixPkgs": z.array(z.string()).optional().describe("#### A list of Nix packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - ffmpeg\n               - imagemagick\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['ffmpeg', 'imagemagick']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixLibs": z.array(z.string()).optional().describe("#### A list of Nix libraries to include in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixLibs:\n               - openssl\n               - zlib\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixLibs: ['openssl', 'zlib']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixOverlay": z.array(z.string()).optional().describe("#### A list of Nix overlay files to apply in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixOverlay:\n               - ./nix/overlay.nix\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixOverlay: ['./nix/overlay.nix']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "nixpkgsArchive": z.string().optional().describe("#### The Nixpkgs archive to use.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             nixPkgs:\n               - nodejs_22\n             nixpkgsArchive: bf3287dac860542719b3849d6970d22f635f9da1\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           nixPkgs: ['nodejs_22'],\n           nixpkgsArchive: 'bf3287dac860542719b3849d6970d22f635f9da1'\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "aptPkgs": z.array(z.string()).optional().describe("#### A list of APT packages to install in this phase.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: setup\n             aptPkgs:\n               - libpq-dev\n               - poppler-utils\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'setup',\n           aptPkgs: ['libpq-dev', 'poppler-utils']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "cacheDirectories": z.array(z.string()).optional().describe("#### A list of directories to cache between builds to speed up subsequent builds.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n             cacheDirectories:\n               - node_modules/.cache\n               - /root/.npm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'install',\n           cmds: ['npm ci'],\n           cacheDirectories: ['node_modules/.cache', '/root/.npm']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional(),
                "onlyIncludeFiles": z.array(z.string()).optional().describe("#### A list of file paths to include in this phase; all other files will be excluded.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: build\n             cmds:\n               - npm run build\n             onlyIncludeFiles:\n               - src\n               - package.json\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         {\n           name: 'build',\n           cmds: ['npm run build'],\n           onlyIncludeFiles: ['src', 'package.json']\n         }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
              ).optional().describe("#### The build phases for the application.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n worker:\n   type: worker-service\n   properties:\n     packaging:\n       type: nixpacks\n       properties:\n         sourceDirectoryPath: ./worker\n         phases:\n           - name: install\n             cmds:\n               - npm ci\n           - name: build\n             cmds:\n               - npm run build\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const worker = new WorkerService({\n   packaging: {\n     type: 'nixpacks',\n     properties: {\n       sourceDirectoryPath: './worker',\n       phases: [\n         { name: 'install', cmds: ['npm ci'] },\n         { name: 'build', cmds: ['npm run build'] }\n       ]\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { worker } };\n});\n```").optional() }).strict()
          }).strict()
          .describe("#### Builds a container image using Nixpacks.\n\n---\n\nNixpacks automatically detects your application type and builds an optimized container image.\nIn most cases, no configuration is required.\n\nIt supports a wide range of languages and frameworks out of the box."), z.object({
            "type": z.literal("stacktape-image-buildpack"),
            "properties": z.object({
              "languageSpecificConfig": z.union([z.object({
                  "tsConfigPath": z.string().optional().describe("#### The path to the `tsconfig.json` file.\n\n---\n\nThis is primarily used to resolve path aliases during the build process.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           tsConfigPath: src/tsconfig.build.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         tsConfigPath: 'src/tsconfig.build.json'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "emitTsDecoratorMetadata": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Emit TypeScript decorator metadata. Required by NestJS, TypeORM, and similar frameworks.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main.ts\n         languageSpecificConfig:\n           emitTsDecoratorMetadata: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main.ts',\n       languageSpecificConfig: {\n         emitTsDecoratorMetadata: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromBundle": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the main bundle.\n\n---\n\nThese dependencies will be treated as \"external\" and will not be bundled directly into your application's code.\nInstead, they will be installed separately in the deployment package.\nUse `*` to exclude all dependencies from the bundle.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n             - \"@prisma/client\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp', '@prisma/client']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputModuleFormat": z.enum(["cjs","esm"]).optional().describe("#### Output module format: `cjs` (CommonJS) or `esm` (ES Modules, enables top-level `await`).\n\n---\n\n**Note:** Some npm packages don't support ESM. ESM may also produce less readable stack traces.\n\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputModuleFormat: esm\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, Node.js 23 and earlier use CommonJS output, while Node.js 24 and later use ESM output.\nThe default is selected by the packaging runtime after resolving `nodeVersion`.").optional(),
                  "nodeVersion": z.union([z.literal(16), z.literal(17), z.literal(18), z.literal(19), z.literal(20), z.literal(21), z.literal(22), z.literal(23), z.literal(24)]).optional().describe("#### The major version of Node.js the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```\n\nWhen omitted, image buildpacks use Node.js 24. Lambda buildpacks use the function's `runtime`, falling back to\nNode.js 24 when neither setting is present. An explicit `nodeVersion` takes precedence over both.").optional(),
                  "disableSourceMaps": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Skip generating source maps. Reduces package size but makes production errors harder to debug.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           disableSourceMaps: true\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         disableSourceMaps: true\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "outputSourceMapsTo": z.string().optional().describe("#### Save source maps to a local directory instead of uploading them to AWS.\n\n---\n\nUseful for uploading to external error tracking (Sentry, Datadog, etc.). CloudWatch stack traces won't be mapped.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           outputSourceMapsTo: ./build/sourcemaps\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         outputSourceMapsTo: './build/sourcemaps'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
                  "dependenciesToExcludeFromDeploymentPackage": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\nThis only applies to dependencies that are not statically bundled.\nTo exclude a dependency from the static bundle, use `dependenciesToExcludeFromBundle`.\nUse `*` to exclude all non-bundled dependencies.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         languageSpecificConfig:\n           dependenciesToExcludeFromBundle:\n             - sharp\n           dependenciesToExcludeFromDeploymentPackage:\n             - \"@types/node\"\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       languageSpecificConfig: {\n         dependenciesToExcludeFromBundle: ['sharp'],\n         dependenciesToExcludeFromDeploymentPackage: ['@types/node']\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
                , z.object({
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's dependency file.\n\n---\n\nThis can be a `requirements.txt`, `Pipfile`, or `pyproject.toml` file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "packageManager": z.literal("uv").optional().describe("#### The Python package manager to use.\n\n---\n\nStacktape uses `uv` for dependency resolution and installation. This option is kept\nfor compatibility and must be set to `uv` if provided.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManager: uv\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManager: 'uv'\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOptionalDependencies": z.array(z.string()).optional().describe("#### Optional dependency extras to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--extra <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOptionalDependencies:\n             - postgres\n             - redis\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOptionalDependencies: ['postgres', 'redis']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithGroups": z.array(z.string()).optional().describe("#### Dependency groups to include from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithGroups:\n             - prod\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithGroups: ['prod']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvWithoutGroups": z.array(z.string()).optional().describe("#### Dependency groups to exclude from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--no-group <name>`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvWithoutGroups:\n             - dev\n             - test\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvWithoutGroups: ['dev', 'test']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "uvOnlyGroups": z.array(z.string()).optional().describe("#### Only include these dependency groups from `pyproject.toml`.\n\n---\n\nEach value is passed to `uv pip compile` as `--only-group <name>`.\nThis omits the project dependencies and default groups, matching `uv` behavior.\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           packageManagerFile: app/pyproject.toml\n           uvOnlyGroups:\n             - runtime\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         packageManagerFile: 'app/pyproject.toml',\n         uvOnlyGroups: ['runtime']\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").optional(),
                  "pythonVersion": z.union([z.literal(2.7), z.literal(3.11), z.literal(3.12), z.literal(3.13), z.literal(3.14), z.literal(3.6), z.literal(3.7), z.literal(3.8), z.literal(3.9)]).optional().describe("#### The Python version the buildpack uses to create the artifact. For Lambda packaging, keep the function's `runtime` aligned with this value.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(3.12),
                  "runAppAs": z.enum(["ASGI","WSGI"]).optional().describe("#### Python server type: `WSGI` (Flask, Django) or `ASGI` (FastAPI, Starlette).\n\n---\n\nOnly for `stacktape-image-buildpack`. Auto-binds to the `PORT` env var.\nSet `entryfilePath` to `module/file.py:app` (e.g., `app/main.py:app`).\n\n**Example (YAML):**\n\n```yaml\nresources:\n fastapiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: app/main.py:app\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           runAppAs: ASGI\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const fastapiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py:app',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         runAppAs: 'ASGI'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { fastapiService } };\n});\n```").optional(),
                  "minify": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Minify Python code to reduce package size. Makes production stack traces harder to read.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n pyApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: app/main.py\n         languageSpecificConfig:\n           pythonVersion: 3.12\n           minify: false\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const pyApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'app/main.py',\n       languageSpecificConfig: {\n         pythonVersion: 3.12,\n         minify: false\n       }\n     }\n   },\n   memory: 512\n });\n return { resources: { pyApi } };\n});\n```").default(true) }).strict()
                , z.object({
                  "useMaven": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Specifies whether to use Maven instead of Gradle.\n\n---\n\nBy default, Stacktape uses Gradle to build Java projects.\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "packageManagerFile": z.string().optional().describe("#### The path to your project's build file (`pom.xml` for Maven or `build.gradle` for Gradle).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           useMaven: true\n           packageManagerFile: pom.xml\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         useMaven: true,\n         packageManagerFile: 'pom.xml'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").optional(),
                  "javaVersion": z.union([z.literal(11), z.literal(17), z.literal(19), z.literal(8)]).optional().describe("#### The version of Java to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n javaApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/main/java/com/example/Handler.java\n         languageSpecificConfig:\n           javaVersion: 17\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const javaApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/main/java/com/example/Handler.java',\n       languageSpecificConfig: {\n         javaVersion: 17\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { javaApi } };\n});\n```").default(11) }).strict()
                , z.record(z.string(), z.any()), z.object({
                  "rubyVersion": z.union([z.literal(3.2), z.literal(3.3)]).optional().describe("#### The version of Ruby to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n rubyApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: config.ru\n         languageSpecificConfig:\n           rubyVersion: 3.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const rubyApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'config.ru',\n       languageSpecificConfig: {\n         rubyVersion: 3.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { rubyApi } };\n});\n```").default(3.3) }).strict()
                , z.object({
                  "phpVersion": z.union([z.literal(8.2), z.literal(8.3)]).optional().describe("#### The version of PHP to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n phpApi:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: public/index.php\n         languageSpecificConfig:\n           phpVersion: 8.3\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const phpApi = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'public/index.php',\n       languageSpecificConfig: {\n         phpVersion: 8.3\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { phpApi } };\n});\n```").default(8.3) }).strict()
                , z.object({
                  "projectFile": z.string().optional().describe("#### The path to your .NET project file (.csproj).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj'\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").optional(),
                  "dotnetVersion": z.union([z.literal(6), z.literal(8)]).optional().describe("#### The version of .NET to use.\n\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n dotnetApi:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/Function.cs\n         languageSpecificConfig:\n           projectFile: src/Api.csproj\n           dotnetVersion: 8\n     memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const dotnetApi = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/Function.cs',\n       languageSpecificConfig: {\n         projectFile: 'src/Api.csproj',\n         dotnetVersion: 8\n       }\n     }\n   },\n   memory: 1024\n });\n return { resources: { dotnetApi } };\n});\n```").default(8) }).strict()
              ]).optional().describe("#### Language-specific packaging configuration.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiService:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         languageSpecificConfig:\n           nodeVersion: 22\n           outputModuleFormat: esm\n     resources:\n       cpu: 0.5\n       memory: 1024\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiService = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       languageSpecificConfig: {\n         nodeVersion: 22,\n         outputModuleFormat: 'esm'\n       }\n     }\n   },\n   resources: {\n     cpu: 0.5,\n     memory: 1024\n   }\n });\n return { resources: { apiService } };\n});\n```").optional(),
              "requiresGlibcBinaries": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("#### Use glibc instead of musl (Alpine default). Enable if native dependencies require glibc.\n\n---\n\nResults in a larger image. Common packages needing this: `sharp`, `canvas`, `bcrypt`, `puppeteer`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         requiresGlibcBinaries: true\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       requiresGlibcBinaries: true\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
              "customDockerBuildCommands": z.array(z.string()).optional().describe("#### A list of commands to be executed during the `docker build` process.\n\n---\n\nThese commands are executed using the `RUN` directive in the Dockerfile.\nThis is useful for installing additional system dependencies in your container.\n\n**Example (YAML):**\n\n```yaml\nresources:\n imageProcessor:\n   type: web-service\n   properties:\n     packaging:\n       type: stacktape-image-buildpack\n       properties:\n         entryfilePath: src/server.ts\n         customDockerBuildCommands:\n           - apt-get update && apt-get install -y poppler-utils\n           - fc-cache -f\n     resources:\n       cpu: 1\n       memory: 2048\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const imageProcessor = new WebService({\n   packaging: {\n     type: 'stacktape-image-buildpack',\n     properties: {\n       entryfilePath: 'src/server.ts',\n       customDockerBuildCommands: [\n         'apt-get update && apt-get install -y poppler-utils',\n         'fc-cache -f'\n       ]\n     }\n   },\n   resources: {\n     cpu: 1,\n     memory: 2048\n   }\n });\n return { resources: { imageProcessor } };\n});\n```").optional(),
              "entryfilePath": z.string().describe("#### Path to your app's entry point, relative to the Stacktape config file.\n\n---\n\nFor JS/TS: code is bundled into a single file. Dependencies with native binaries are installed separately.\nFor Python: use `module/file.py:app` format when using `runAppAs` (WSGI/ASGI).\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n     memory: 512\n     timeout: 15\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts'\n     }\n   },\n   memory: 512,\n   timeout: 15\n });\n return { resources: { apiFunction } };\n});\n```"),
              "includeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly include in the deployment package.\n\n---\n\nThe path is relative to your Stacktape configuration file.\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         includeFiles:\n           - templates/**/*.html\n           - config/defaults.json\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       includeFiles: ['templates/**/*.html', 'config/defaults.json']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeFiles": z.array(z.string()).optional().describe("#### A glob pattern of files to explicitly exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeFiles:\n           - \"**/*.test.ts\"\n           - \"**/__mocks__/**\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeFiles: ['**/*.test.ts', '**/__mocks__/**']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional(),
              "excludeDependencies": z.array(z.string()).optional().describe("#### A list of dependencies to exclude from the deployment package.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n apiFunction:\n   type: function\n   properties:\n     packaging:\n       type: stacktape-lambda-buildpack\n       properties:\n         entryfilePath: src/handlers/api.ts\n         excludeDependencies:\n           - aws-sdk\n           - \"@aws-sdk/client-s3\"\n     memory: 512\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n const apiFunction = new LambdaFunction({\n   packaging: {\n     type: 'stacktape-lambda-buildpack',\n     properties: {\n       entryfilePath: 'src/handlers/api.ts',\n       excludeDependencies: ['aws-sdk', '@aws-sdk/client-s3']\n     }\n   },\n   memory: 512\n });\n return { resources: { apiFunction } };\n});\n```").optional() }).strict()
            .describe("#### Configures an image to be built automatically by Stacktape from your source code.") }).strict()
          .describe("#### A zero-config buildpack that creates a container image from your source code.\n\n---\n\nThe `stacktape-image-buildpack` automatically bundles your code and dependencies into an optimized container image.\n\n**Supported languages:** JavaScript, TypeScript, Python, Java, and Go.\n\nFor JS/TS, your code is bundled into a single file with source maps.\nThe resulting image is uploaded to a managed ECR repository.")]),
        "description": z.string().optional().describe("").optional(),
        "protocol": z.enum(["A2A","AGUI","HTTP","MCP"]).optional().describe("").optional(),
        "environment": z.array(z.object({
          "name": z.string().describe("#### Environment variable name (e.g., `DATABASE_URL`, `API_KEY`).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      environment:\n        - name: NODE_ENV\n          value: production\n        - name: MAX_CONNECTIONS\n          value: 50\n        - name: STRIPE_KEY\n          value: $Secret('stripe.secret-key')\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    environment: {\n      NODE_ENV: 'production',\n      MAX_CONNECTIONS: 50,\n      STRIPE_KEY: $Secret('stripe.secret-key')\n    }\n  });\n\n  return { resources: { api } };\n});\n```"),
          "value": z.union([z.string().describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).describe("#### Environment variable value. Numbers and booleans are converted to strings."), z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).describe("#### Environment variable value. Numbers and booleans are converted to strings.")]).describe("#### Environment variable value. Numbers and booleans are converted to strings.") }).strict()
        ).optional().describe("").optional(),
        "useMemory": z.string().optional().describe("").optional(),
        "useGateway": z.string().optional().describe("").optional(),
        "useBrowser": z.string().optional().describe("").optional(),
        "useCodeInterpreter": z.string().optional().describe("").optional(),
        "endpoints": z.array(z.union([z.object({
            "name": z.string(),
            "description": z.string().optional().describe("").optional(),
            "runtimeVersion": z.string().optional().describe("").optional() }).strict()
          , z.string()])).optional().describe("").optional(),
        "lifecycle": z.object({
          "maxLifetime": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
          "idleRuntimeSessionTimeout": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional() }).strict()
        .optional().describe("").optional(),
        "requestHeaders": z.array(z.string()).optional().describe("").optional(),
        "authorizer": z.object({
          "discoveryUrl": z.string(),
          "allowedAudience": z.array(z.string()).optional().describe("").optional(),
          "allowedClients": z.array(z.string()).optional().describe("").optional(),
          "allowedScopes": z.array(z.string()).optional().describe("").optional() }).strict()
        .optional().describe("").optional(),
        "tags": z.array(z.object({
          "name": z.string().describe("#### Tag name (1-128 characters)."),
          "value": z.string().describe("#### Tag value (1-256 characters).") }).strict()
        ).optional().describe("").optional(),
        "connectTo": z.array(z.string()).optional().describe("#### Give this resource access to other resources in your stack.\n\n---\n\nList the names of resources this workload needs to communicate with. Stacktape automatically:\n- **Grants IAM permissions** (e.g., S3 read/write, SQS send/receive)\n- **Opens network access** (security group rules for databases, Redis)\n- **Injects environment variables** with connection details: `STP_[RESOURCE_NAME]_[PARAM]`\n\nExample: `connectTo: [\"myDatabase\", \"myBucket\"]` gives this workload full access to both\nresources and injects `STP_MY_DATABASE_CONNECTION_STRING`, `STP_MY_BUCKET_NAME`, etc.\n\n---\n\n#### What each resource type provides:\n\n**`Bucket`** — read/write/delete objects → `NAME`, `ARN`\n\n**`DynamoDbTable`** — CRUD + scan/query → `NAME`, `ARN`, `STREAM_ARN`\n\n**`RelationalDatabase`** — network access + connection details → `CONNECTION_STRING`, `HOST`, `PORT`.\nAurora also gets `READER_CONNECTION_STRING`, `READER_HOST`.\n\n**`MongoDbAtlasCluster`** — temporary credential-less access → `CONNECTION_STRING`\n\n**`RedisCluster`** — network access → `HOST`, `READER_HOST`, `PORT`\n\n**`Function`** — invoke permission → `ARN`\n\n**`BatchJob`** — submit/list/terminate → `JOB_DEFINITION_ARN`, `STATE_MACHINE_ARN`\n\n**`EventBus`** — publish events → `ARN`\n\n**`UserAuthPool`** — full control → `ID`, `CLIENT_ID`, `ARN`\n\n**`SqsQueue`** — send/receive/delete → `ARN`, `NAME`, `URL`\n\n**`SnsTopic`** — publish/subscribe → `ARN`, `NAME`\n\n**`UpstashRedis`** → `HOST`, `PORT`, `PASSWORD`, `REST_TOKEN`, `REST_URL`, `REDIS_URL`\n\n**`PrivateService`** → `ADDRESS`\n\n**`aws:ses`** — full SES email sending permissions\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.5\n        memory: 1024\n      connectTo:\n        - mainDb\n        - uploads\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t4g.micro\n  uploads:\n    type: bucket\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, RelationalDatabase, Bucket, RdsEnginePostgres, StacktapeImageBuildpackPackaging, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({ version: '16.2', primaryInstance: { instanceSize: 'db.t4g.micro' } })\n  });\n  const uploads = new Bucket({});\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.5, memory: 1024 },\n    connectTo: [mainDb, uploads]\n  });\n\n  return { resources: { api, mainDb, uploads } };\n});\n```").optional(),
        "iamRoleStatements": z.array(z.object({
          "Sid": z.string().optional().describe("#### Optional identifier for this statement (for readability).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Sid: AllowBedrockInvoke\n          Effect: Allow\n          Action:\n            - bedrock:InvokeModel\n          Resource:\n            - 'arn:aws:bedrock:*::foundation-model/*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Sid: 'AllowBedrockInvoke',\n        Effect: 'Allow',\n        Action: ['bedrock:InvokeModel'],\n        Resource: ['arn:aws:bedrock:*::foundation-model/*']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Effect": z.string().optional().describe("#### Whether to allow or deny the specified actions."),
          "Action": z.array(z.string()).optional().describe("#### AWS actions to allow or deny (e.g., `s3:GetObject`, `sqs:SendMessage`).").optional(),
          "Condition": z.any().optional().describe("#### Conditions under which this statement applies (e.g., IP restrictions, tag-based access).\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - s3:GetObject\n          Resource:\n            - 'arn:aws:s3:::my-secure-bucket/*'\n          Condition:\n            Bool:\n              'aws:SecureTransport': 'true'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['s3:GetObject'],\n        Resource: ['arn:aws:s3:::my-secure-bucket/*'],\n        Condition: { Bool: { 'aws:SecureTransport': 'true' } }\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```").optional(),
          "Resource": z.array(z.string()).describe("#### ARNs of the AWS resources this statement applies to. Use `\"*\"` for all resources.\n\n---\n\n**Example (YAML):**\n\n```yaml\nresources:\n  api:\n    type: web-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/server.ts\n      resources:\n        cpu: 0.25\n        memory: 512\n      iamRoleStatements:\n        - Resource:\n            - '*'\n          Action:\n            - rekognition:DetectLabels\n            - rekognition:DetectText\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WebService, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const api = new WebService({\n    packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/server.ts' }),\n    resources: { cpu: 0.25, memory: 512 },\n    iamRoleStatements: [\n      {\n        Resource: ['*'],\n        Action: ['rekognition:DetectLabels', 'rekognition:DetectText']\n      }\n    ]\n  });\n\n  return { resources: { api } };\n});\n```") }).strict()
        ).optional().describe("#### Raw IAM policy statements for permissions not covered by `connectTo`.\n\n---\n\nAdded as a separate policy alongside auto-generated permissions. Use this for\naccessing AWS services directly (e.g., Rekognition, Textract, Bedrock).\n\n**Example (YAML):**\n\n```yaml\nresources:\n  processor:\n    type: batch-job\n    properties:\n      container:\n        packaging:\n          type: stacktape-image-buildpack\n          properties:\n            entryfilePath: src/job.ts\n      resources:\n        cpu: 1\n        memory: 2048\n      iamRoleStatements:\n        - Effect: Allow\n          Action:\n            - textract:AnalyzeDocument\n          Resource:\n            - '*'\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { BatchJob, StacktapeImageBuildpackPackaging, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const processor = new BatchJob({\n    container: { packaging: new StacktapeImageBuildpackPackaging({ entryfilePath: 'src/job.ts' }) },\n    resources: { cpu: 1, memory: 2048 },\n    iamRoleStatements: [\n      {\n        Effect: 'Allow',\n        Action: ['textract:AnalyzeDocument'],\n        Resource: ['*']\n      }\n    ]\n  });\n\n  return { resources: { processor } };\n});\n```").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    , z.object({
      "type": z.literal("agentcore-memory"),
      "properties": z.object({
        "description": z.string().optional().describe("").optional(),
        "expirationDays": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
        "eventExpiryDuration": z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number()).optional().describe("").optional(),
        "encryptionKeyArn": z.string().optional().describe("").optional(),
        "memoryStrategies": z.array(z.any()).optional().describe("").optional(),
        "tags": z.array(z.object({
          "name": z.string().describe("#### Tag name (1-128 characters)."),
          "value": z.string().describe("#### Tag value (1-256 characters).") }).strict()
        ).optional().describe("").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    , z.object({
      "type": z.literal("agentcore-gateway"),
      "properties": z.object({
        "description": z.string().optional().describe("").optional(),
        "authorizer": z.object({
          "discoveryUrl": z.string(),
          "allowedAudience": z.array(z.string()).optional().describe("").optional(),
          "allowedClients": z.array(z.string()).optional().describe("").optional(),
          "allowedScopes": z.array(z.string()).optional().describe("").optional() }).strict()
        .optional().describe("").optional(),
        "tools": z.array(z.object({
          "name": z.string(),
          "description": z.string().optional().describe("").optional(),
          "function": z.string().optional().describe("").optional(),
          "lambdaArn": z.string().optional().describe("").optional(),
          "toolSchema": z.array(z.object({
            "name": z.string(),
            "description": z.string().optional().describe("").optional(),
            "inputSchema": z.any(),
            "outputSchema": z.any().optional().describe("").optional() }).strict()
          ) }).strict()
        ).optional().describe("").optional(),
        "instructions": z.string().optional().describe("").optional(),
        "supportedVersions": z.array(z.string()).optional().describe("").optional(),
        "searchType": z.string().optional().describe("").optional(),
        "exceptionLevel": z.literal("DEBUG").optional().describe("").optional(),
        "tags": z.array(z.object({
          "name": z.string().describe("#### Tag name (1-128 characters)."),
          "value": z.string().describe("#### Tag value (1-256 characters).") }).strict()
        ).optional().describe("").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    , z.object({
      "type": z.literal("agentcore-browser"),
      "properties": z.object({
        "description": z.string().optional().describe("").optional(),
        "recording": z.object({
          "enabled": z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean()).optional().describe("").optional(),
          "bucketName": z.string().optional().describe("").optional(),
          "prefix": z.string().optional().describe("").optional() }).strict()
        .optional().describe("").optional(),
        "tags": z.array(z.object({
          "name": z.string().describe("#### Tag name (1-128 characters)."),
          "value": z.string().describe("#### Tag value (1-256 characters).") }).strict()
        ).optional().describe("").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
    , z.object({
      "type": z.literal("agentcore-code-interpreter"),
      "properties": z.object({
        "description": z.string().optional().describe("").optional(),
        "tags": z.array(z.object({
          "name": z.string().describe("#### Tag name (1-128 characters)."),
          "value": z.string().describe("#### Tag value (1-256 characters).") }).strict()
        ).optional().describe("").optional() }).strict()
      ,
      "overrides": z.record(z.string(), z.record(z.string(), z.any())).optional().describe("#### Escape hatch to modify the underlying CloudFormation resources Stacktape creates.\n\n---\n\nUse dot-notation paths to override specific properties on any child resource.\nFind resource logical IDs with `stacktape info:stack`.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  mainDb:\n    type: relational-database\n    properties:\n      credentials:\n        masterUserPassword: $Secret('db-password')\n      engine:\n        type: postgres\n        properties:\n          version: '16.2'\n          primaryInstance:\n            instanceSize: db.t3.micro\n    overrides:\n      MainDbDatabaseInstance:\n        Properties.StorageEncrypted: true\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { RelationalDatabase, RdsEnginePostgres, $Secret, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainDb = new RelationalDatabase({\n    credentials: { masterUserPassword: $Secret('db-password') },\n    engine: new RdsEnginePostgres({\n      version: '16.2',\n      primaryInstance: { instanceSize: 'db.t3.micro' }\n    }),\n    overrides: {\n      MainDbDatabaseInstance: { 'Properties.StorageEncrypted': true }\n    }\n  });\n\n  return { resources: { mainDb } };\n});\n```").optional() }).strict()
  ])).describe("#### Your app's infrastructure: APIs, databases, containers, functions, buckets, and more.\n\n---\n\nEach entry is a named resource (e.g., `myApi`, `myDatabase`). Stacktape creates and manages\nthe underlying AWS resources for you. Use `stacktape info:stack` to inspect them.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  notificationSender:\n    type: function\n    properties:\n      packaging:\n        type: stacktape-lambda-buildpack\n        properties:\n          entryfilePath: src/send-notification.ts\n      events:\n        - type: http-api-gateway\n          properties:\n            httpApiGatewayName: mainGateway\n            method: POST\n            path: /notify\n  mainGateway:\n    type: http-api-gateway\n    properties: {}\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { LambdaFunction, HttpApiGateway, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const mainGateway = new HttpApiGateway({});\n  const notificationSender = new LambdaFunction({\n    packaging: {\n      type: 'stacktape-lambda-buildpack',\n      properties: { entryfilePath: 'src/send-notification.ts' }\n    },\n    events: [\n      {\n        type: 'http-api-gateway',\n        properties: { httpApiGatewayName: 'mainGateway', method: 'POST', path: '/notify' }\n      }\n    ]\n  });\n\n  return {\n    resources: { notificationSender, mainGateway }\n  };\n});\n```"),
  "cloudformationResources": z.record(z.string(), z.object({
    "Type": z.string(),
    "DependsOn": z.union([z.array(z.string()), z.object({
        "name": z.string(),
        "payload": z.any() }).strict()
      , z.string()]).optional().describe("").optional(),
    "Properties": z.record(z.string(), z.any()).optional().describe("").optional(),
    "Metadata": z.record(z.string(), z.any()).optional().describe("").optional(),
    "CreationPolicy": z.object({
      "AutoScalingCreationPolicy": z.object({
        "MinSuccessfulInstancesPercent": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number())]).optional().describe("").optional() }).strict()
      .optional().describe("").optional(),
      "ResourceSignal": z.object({
        "Count": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number())]).optional().describe("").optional(),
        "Timeout": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.string()]).optional().describe("").optional() }).strict()
      .optional().describe("").optional() }).strict()
    .optional().describe("").optional(),
    "DeletionPolicy": z.enum(["Delete","Retain","Snapshot"]).optional().describe("").optional(),
    "UpdatePolicy": z.object({
      "AutoScalingReplacingUpdate": z.object({
        "WillReplace": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean())]).optional().describe("").optional() }).strict()
      .optional().describe("").optional(),
      "AutoScalingRollingUpdate": z.object({
        "MaxBatchSize": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number())]).optional().describe("").optional(),
        "MinInstancesInService": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number())]).optional().describe("").optional(),
        "MinSuccessfulInstancesPercent": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.preprocess((val) => typeof val === "number" ? val : typeof val === "string" ? Number(val) : val, z.number())]).optional().describe("").optional(),
        "PauseTime": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.string()]).optional().describe("").optional(),
        "SuspendProcesses": z.union([z.array(z.string()), z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
        ]).optional().describe("").optional(),
        "WaitOnResourceSignals": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean())]).optional().describe("").optional() }).strict()
      .optional().describe("").optional(),
      "AutoScalingScheduledAction": z.object({
        "IgnoreUnmodifiedGroupSizeProperties": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.preprocess((val) => typeof val === "boolean" ? val : val === "true" ? true : val === "false" ? false : val, z.boolean())]).optional().describe("").optional() }).strict()
      .optional().describe("").optional(),
      "CodeDeployLambdaAliasUpdate": z.object({
        "AfterAllowTrafficHook": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.string()]),
        "ApplicationName": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.string()]),
        "BeforeAllowTrafficHook": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.string()]),
        "DeploymentGroupName": z.union([z.object({
            "name": z.string(),
            "payload": z.any() }).strict()
          , z.string()]) }).strict()
      .optional().describe("").optional() }).strict()
    .optional().describe("").optional(),
    "Condition": z.union([z.object({
        "name": z.string(),
        "payload": z.any() }).strict()
      , z.string()]).optional().describe("").optional() }).strict()
  ).optional().describe("#### Escape hatch: add raw AWS CloudFormation resources alongside Stacktape-managed ones.\n\n---\n\nFor advanced use cases where Stacktape doesn't have a built-in resource type.\nThese are merged into the CloudFormation template as-is. Use `stacktape info:stack`\nto check existing logical names and avoid conflicts.\n\nDoes not count towards your resource limit.\n\n**Example (YAML):**\n\n```yaml\nresources:\n  worker:\n    type: worker-service\n    properties:\n      packaging:\n        type: stacktape-image-buildpack\n        properties:\n          entryfilePath: src/worker.ts\n      resources:\n        cpu: 0.25\n        memory: 512\ncloudformationResources:\n  MyRawSnsTopic:\n    Type: AWS::SNS::Topic\n    Properties:\n      TopicName: legacy-events\n      DisplayName: Legacy Events Topic\n```\n\n**Example (TypeScript):**\n\n```ts\nimport { WorkerService, defineConfig } from 'stacktape';\n\nexport default defineConfig(() => {\n  const worker = new WorkerService({\n    packaging: {\n      type: 'stacktape-image-buildpack',\n      properties: { entryfilePath: 'src/worker.ts' }\n    },\n    resources: { cpu: 0.25, memory: 512 }\n  });\n\n  return {\n    resources: { worker },\n    cloudformationResources: {\n      MyRawSnsTopic: {\n        Type: 'AWS::SNS::Topic',\n        Properties: {\n          TopicName: 'legacy-events',\n          DisplayName: 'Legacy Events Topic'\n        }\n      }\n    }\n  };\n});\n```").optional() }).strict()

export type StacktapeConfigSchema = z.infer<typeof stacktapeConfigSchema>
