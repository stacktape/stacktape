{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "generate": {
      "description": "Regenerate the temporary @stacktape/config ambient bridge from the package's exports",
      "inputs": ["$TURBO_DEFAULT$", "../../packages/config/src/**", "../../packages/config/package.json"],
      "outputs": ["types/config-package-bridge.generated.d.ts"]
    },
    "generate:check": {
      "description": "Verify the committed ambient bridge matches the package's exports",
      "cache": false
    }
  }
}
