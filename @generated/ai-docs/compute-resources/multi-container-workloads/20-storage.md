# Storage

Each workload instance has 20GB of _ephemeral storage_, which is shared among all containers within that instance. This storage is deleted when the instance is removed. For persistent storage, use [Buckets](../../../other-resources/buckets.md).