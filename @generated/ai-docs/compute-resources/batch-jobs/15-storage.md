# Storage

Each batch job has its own _ephemeral storage_ with a fixed size of **20GB**. This storage is temporary and is deleted after the job completes or fails. To store data permanently, use [Buckets](../../../other-resources/buckets.md).