# When to use them

NLBs are a good choice for applications that use TCP protocols other than HTTP/S. If your application uses HTTP or HTTPS, an [Application Load Balancer](../../other-resources/application-load-balancers/index.md) is a better choice.